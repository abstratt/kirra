(function(){

if (!window.qx) window.qx = {};

qx.$$start = new Date();

if (!qx.$$environment) qx.$$environment = {};
var envinfo = {"qx.application":"kirra_qooxdoo.Application","qx.debug":false,"qx.debug.databinding":false,"qx.debug.dispose":false,"qx.debug.io":false,"qx.debug.ui.queue":false,"qx.mobile.emulatetouch":true,"qx.optimization.basecalls":true,"qx.optimization.comments":true,"qx.optimization.privates":true,"qx.optimization.strings":true,"qx.optimization.variants":true,"qx.revision":"","qx.theme":"custom","qx.version":"3.5"};
for (var k in envinfo) qx.$$environment[k] = envinfo[k];

if (!qx.$$libraries) qx.$$libraries = {};
var libinfo = {"__out__":{"sourceUri":"script"},"kirra_qooxdoo":{"resourceUri":"resource","sourceUri":"script"},"qx":{"resourceUri":"resource","sourceUri":"script","sourceViewUri":"https://github.com/qooxdoo/qooxdoo/blob/%{qxGitBranch}/framework/source/class/%{classFilePath}#L%{lineNumber}"}};
for (var k in libinfo) qx.$$libraries[k] = libinfo[k];

qx.$$resources = {};
qx.$$translations = {"C":null,"en":null};
qx.$$locales = {"C":null,"en":null};
qx.$$packageData = {};
qx.$$g = {}

qx.$$loader = {
  parts : {"boot":[0]},
  packages : {"0":{"uris":["__out__:kirra_qooxdoo.ef3d6249551f.js"]}},
  urisBefore : [],
  cssBefore : ["./resource/kirra_qooxdoo/css/custom.css","./resource/kirra_qooxdoo/css/styles.css"],
  boot : "boot",
  closureParts : {},
  bootIsInline : true,
  addNoCacheParam : false,

  decodeUris : function(compressedUris)
  {
    var libs = qx.$$libraries;
    var uris = [];
    for (var i=0; i<compressedUris.length; i++)
    {
      var uri = compressedUris[i].split(":");
      var euri;
      if (uri.length==2 && uri[0] in libs) {
        var prefix = libs[uri[0]].sourceUri;
        euri = prefix + "/" + uri[1];
      } else {
        euri = compressedUris[i];
      }
      if (qx.$$loader.addNoCacheParam) {
        euri += "?nocache=" + Math.random();
      }
      
      uris.push(euri);
    }
    return uris;
  }
};

var readyStateValue = {"complete" : true};
if (document.documentMode && document.documentMode < 10 ||
    (typeof window.ActiveXObject !== "undefined" && !document.documentMode)) {
  readyStateValue["loaded"] = true;
}

function loadScript(uri, callback) {
  var elem = document.createElement("script");
  elem.charset = "utf-8";
  elem.src = uri;
  elem.onreadystatechange = elem.onload = function() {
    if (!this.readyState || readyStateValue[this.readyState]) {
      elem.onreadystatechange = elem.onload = null;
      if (typeof callback === "function") {
        callback();
      }
    }
  };

  if (isLoadParallel) {
    elem.async = null;
  }

  var head = document.getElementsByTagName("head")[0];
  head.appendChild(elem);
}

function loadCss(uri) {
  var elem = document.createElement("link");
  elem.rel = "stylesheet";
  elem.type = "text/css";
  elem.href = uri;
  elem.onload = onLoadCss();
  var head = document.getElementsByTagName("head")[0];
  head.appendChild(elem);
}

function onLoadCss() {
  cssFilesToLoad = cssFilesToLoad - 1;
  if(cssFilesToLoad == 0) {
    setTimeout(initScripts,0);
  }
}

var isWebkit = /AppleWebKit\/([^ ]+)/.test(navigator.userAgent);
var isLoadParallel = 'async' in document.createElement('script');

function loadScriptList(list, callback) {
  if (list.length == 0) {
    callback();
    return;
  }

  var item;

  if (isLoadParallel) {
    while (list.length) {
      item = list.shift();
      if (list.length) {
        loadScript(item);
      } else {
        loadScript(item, callback);
      }
    }
  } else {
    item = list.shift();
    loadScript(item,  function() {
      if (isWebkit) {
        // force async, else Safari fails with a "maximum recursion depth exceeded"
        window.setTimeout(function() {
          loadScriptList(list, callback);
        }, 0);
      } else {
        loadScriptList(list, callback);
      }
    });
  }
}

var fireContentLoadedEvent = function() {
  qx.$$domReady = true;
  document.removeEventListener('DOMContentLoaded', fireContentLoadedEvent, false);
};
if (document.addEventListener) {
  document.addEventListener('DOMContentLoaded', fireContentLoadedEvent, false);
}

qx.$$loader.importPackageData = function (dataMap, callback) {
  if (dataMap["resources"]){
    var resMap = dataMap["resources"];
    for (var k in resMap) qx.$$resources[k] = resMap[k];
  }
  if (dataMap["locales"]){
    var locMap = dataMap["locales"];
    var qxlocs = qx.$$locales;
    for (var lang in locMap){
      if (!qxlocs[lang]) qxlocs[lang] = locMap[lang];
      else
        for (var k in locMap[lang]) qxlocs[lang][k] = locMap[lang][k];
    }
  }
  if (dataMap["translations"]){
    var trMap   = dataMap["translations"];
    var qxtrans = qx.$$translations;
    for (var lang in trMap){
      if (!qxtrans[lang]) qxtrans[lang] = trMap[lang];
      else
        for (var k in trMap[lang]) qxtrans[lang][k] = trMap[lang][k];
    }
  }
  if (callback){
    callback(dataMap);
  }
}

qx.$$loader.signalStartup = function ()
{
  qx.$$loader.scriptLoaded = true;
  if (window.qx && qx.event && qx.event.handler && qx.event.handler.Application) {
    qx.event.handler.Application.onScriptLoaded();
    qx.$$loader.applicationHandlerReady = true;
  } else {
    qx.$$loader.applicationHandlerReady = false;
  }
}


// Load all stuff

var cssFilesToLoad = 0;

qx.$$loader.init = function() {
  var l=qx.$$loader;
  if (l.cssBefore.length>0) {

    cssFilesToLoad = l.cssBefore.length;

    for (var i=0, m=l.cssBefore.length; i<m; i++) {
      loadCss(l.cssBefore[i]);
    }
  } else {
    initScripts();
  }
}

// Init scripts...
function initScripts() {
  var l=qx.$$loader;
  if (l.urisBefore.length>0){
    loadScriptList(l.urisBefore, function(){
      l.initUris();
    });
  } else {
    l.initUris();
  }
}

// Load qooxdoo boot stuff
qx.$$loader.initUris = function(){
  var l=qx.$$loader;
  var bootPackageHash=l.parts[l.boot][0];
  if (l.bootIsInline){
    l.importPackageData(qx.$$packageData[bootPackageHash]);
    l.signalStartup();
  } else {
    loadScriptList(l.decodeUris(l.packages[l.parts[l.boot][0]].uris), function(){
      // Opera needs this extra time to parse the scripts
      window.setTimeout(function(){
        l.importPackageData(qx.$$packageData[bootPackageHash] || {});
        l.signalStartup();
      }, 0);
    });
  }
}
})();

qx.$$packageData['0']={"locales":{"C":{"alternateQuotationEnd":"’","alternateQuotationStart":"‘","cldr_am":"AM","cldr_date_format_full":"EEEE, MMMM d, y","cldr_date_format_long":"MMMM d, y","cldr_date_format_medium":"MMM d, y","cldr_date_format_short":"M/d/yy","cldr_date_time_format_EHm":"E HH:mm","cldr_date_time_format_EHms":"E HH:mm:ss","cldr_date_time_format_Ed":"d E","cldr_date_time_format_Ehm":"E h:mm a","cldr_date_time_format_Ehms":"E h:mm:ss a","cldr_date_time_format_Gy":"y G","cldr_date_time_format_GyMMM":"MMM y G","cldr_date_time_format_GyMMMEd":"E, MMM d, y G","cldr_date_time_format_GyMMMd":"MMM d, y G","cldr_date_time_format_H":"HH","cldr_date_time_format_Hm":"HH:mm","cldr_date_time_format_Hms":"HH:mm:ss","cldr_date_time_format_M":"L","cldr_date_time_format_MEd":"E, M/d","cldr_date_time_format_MMM":"LLL","cldr_date_time_format_MMMEd":"E, MMM d","cldr_date_time_format_MMMd":"MMM d","cldr_date_time_format_Md":"M/d","cldr_date_time_format_d":"d","cldr_date_time_format_h":"h a","cldr_date_time_format_hm":"h:mm a","cldr_date_time_format_hms":"h:mm:ss a","cldr_date_time_format_ms":"mm:ss","cldr_date_time_format_y":"y","cldr_date_time_format_yM":"M/y","cldr_date_time_format_yMEd":"E, M/d/y","cldr_date_time_format_yMMM":"MMM y","cldr_date_time_format_yMMMEd":"E, MMM d, y","cldr_date_time_format_yMMMd":"MMM d, y","cldr_date_time_format_yMd":"M/d/y","cldr_date_time_format_yQQQ":"QQQ y","cldr_date_time_format_yQQQQ":"QQQQ y","cldr_day_format_abbreviated_fri":"Fri","cldr_day_format_abbreviated_mon":"Mon","cldr_day_format_abbreviated_sat":"Sat","cldr_day_format_abbreviated_sun":"Sun","cldr_day_format_abbreviated_thu":"Thu","cldr_day_format_abbreviated_tue":"Tue","cldr_day_format_abbreviated_wed":"Wed","cldr_day_format_short_fri":"Fr","cldr_day_format_short_mon":"Mo","cldr_day_format_short_sat":"Sa","cldr_day_format_short_sun":"Su","cldr_day_format_short_thu":"Th","cldr_day_format_short_tue":"Tu","cldr_day_format_short_wed":"We","cldr_day_format_wide_fri":"Friday","cldr_day_format_wide_mon":"Monday","cldr_day_format_wide_sat":"Saturday","cldr_day_format_wide_sun":"Sunday","cldr_day_format_wide_thu":"Thursday","cldr_day_format_wide_tue":"Tuesday","cldr_day_format_wide_wed":"Wednesday","cldr_day_stand-alone_narrow_fri":"F","cldr_day_stand-alone_narrow_mon":"M","cldr_day_stand-alone_narrow_sat":"S","cldr_day_stand-alone_narrow_sun":"S","cldr_day_stand-alone_narrow_thu":"T","cldr_day_stand-alone_narrow_tue":"T","cldr_day_stand-alone_narrow_wed":"W","cldr_month_format_abbreviated_1":"Jan","cldr_month_format_abbreviated_10":"Oct","cldr_month_format_abbreviated_11":"Nov","cldr_month_format_abbreviated_12":"Dec","cldr_month_format_abbreviated_2":"Feb","cldr_month_format_abbreviated_3":"Mar","cldr_month_format_abbreviated_4":"Apr","cldr_month_format_abbreviated_5":"May","cldr_month_format_abbreviated_6":"Jun","cldr_month_format_abbreviated_7":"Jul","cldr_month_format_abbreviated_8":"Aug","cldr_month_format_abbreviated_9":"Sep","cldr_month_format_wide_1":"January","cldr_month_format_wide_10":"October","cldr_month_format_wide_11":"November","cldr_month_format_wide_12":"December","cldr_month_format_wide_2":"February","cldr_month_format_wide_3":"March","cldr_month_format_wide_4":"April","cldr_month_format_wide_5":"May","cldr_month_format_wide_6":"June","cldr_month_format_wide_7":"July","cldr_month_format_wide_8":"August","cldr_month_format_wide_9":"September","cldr_month_stand-alone_narrow_1":"J","cldr_month_stand-alone_narrow_10":"O","cldr_month_stand-alone_narrow_11":"N","cldr_month_stand-alone_narrow_12":"D","cldr_month_stand-alone_narrow_2":"F","cldr_month_stand-alone_narrow_3":"M","cldr_month_stand-alone_narrow_4":"A","cldr_month_stand-alone_narrow_5":"M","cldr_month_stand-alone_narrow_6":"J","cldr_month_stand-alone_narrow_7":"J","cldr_month_stand-alone_narrow_8":"A","cldr_month_stand-alone_narrow_9":"S","cldr_number_decimal_separator":".","cldr_number_group_separator":",","cldr_number_percent_format":"#,##0%","cldr_pm":"PM","cldr_time_format_full":"h:mm:ss a zzzz","cldr_time_format_long":"h:mm:ss a z","cldr_time_format_medium":"h:mm:ss a","cldr_time_format_short":"h:mm a","quotationEnd":"”","quotationStart":"“"},"en":{"alternateQuotationEnd":"’","alternateQuotationStart":"‘","cldr_am":"AM","cldr_date_format_full":"EEEE, MMMM d, y","cldr_date_format_long":"MMMM d, y","cldr_date_format_medium":"MMM d, y","cldr_date_format_short":"M/d/yy","cldr_date_time_format_EHm":"E HH:mm","cldr_date_time_format_EHms":"E HH:mm:ss","cldr_date_time_format_Ed":"d E","cldr_date_time_format_Ehm":"E h:mm a","cldr_date_time_format_Ehms":"E h:mm:ss a","cldr_date_time_format_Gy":"y G","cldr_date_time_format_GyMMM":"MMM y G","cldr_date_time_format_GyMMMEd":"E, MMM d, y G","cldr_date_time_format_GyMMMd":"MMM d, y G","cldr_date_time_format_H":"HH","cldr_date_time_format_Hm":"HH:mm","cldr_date_time_format_Hms":"HH:mm:ss","cldr_date_time_format_M":"L","cldr_date_time_format_MEd":"E, M/d","cldr_date_time_format_MMM":"LLL","cldr_date_time_format_MMMEd":"E, MMM d","cldr_date_time_format_MMMd":"MMM d","cldr_date_time_format_Md":"M/d","cldr_date_time_format_d":"d","cldr_date_time_format_h":"h a","cldr_date_time_format_hm":"h:mm a","cldr_date_time_format_hms":"h:mm:ss a","cldr_date_time_format_ms":"mm:ss","cldr_date_time_format_y":"y","cldr_date_time_format_yM":"M/y","cldr_date_time_format_yMEd":"E, M/d/y","cldr_date_time_format_yMMM":"MMM y","cldr_date_time_format_yMMMEd":"E, MMM d, y","cldr_date_time_format_yMMMd":"MMM d, y","cldr_date_time_format_yMd":"M/d/y","cldr_date_time_format_yQQQ":"QQQ y","cldr_date_time_format_yQQQQ":"QQQQ y","cldr_day_format_abbreviated_fri":"Fri","cldr_day_format_abbreviated_mon":"Mon","cldr_day_format_abbreviated_sat":"Sat","cldr_day_format_abbreviated_sun":"Sun","cldr_day_format_abbreviated_thu":"Thu","cldr_day_format_abbreviated_tue":"Tue","cldr_day_format_abbreviated_wed":"Wed","cldr_day_format_short_fri":"Fr","cldr_day_format_short_mon":"Mo","cldr_day_format_short_sat":"Sa","cldr_day_format_short_sun":"Su","cldr_day_format_short_thu":"Th","cldr_day_format_short_tue":"Tu","cldr_day_format_short_wed":"We","cldr_day_format_wide_fri":"Friday","cldr_day_format_wide_mon":"Monday","cldr_day_format_wide_sat":"Saturday","cldr_day_format_wide_sun":"Sunday","cldr_day_format_wide_thu":"Thursday","cldr_day_format_wide_tue":"Tuesday","cldr_day_format_wide_wed":"Wednesday","cldr_day_stand-alone_narrow_fri":"F","cldr_day_stand-alone_narrow_mon":"M","cldr_day_stand-alone_narrow_sat":"S","cldr_day_stand-alone_narrow_sun":"S","cldr_day_stand-alone_narrow_thu":"T","cldr_day_stand-alone_narrow_tue":"T","cldr_day_stand-alone_narrow_wed":"W","cldr_month_format_abbreviated_1":"Jan","cldr_month_format_abbreviated_10":"Oct","cldr_month_format_abbreviated_11":"Nov","cldr_month_format_abbreviated_12":"Dec","cldr_month_format_abbreviated_2":"Feb","cldr_month_format_abbreviated_3":"Mar","cldr_month_format_abbreviated_4":"Apr","cldr_month_format_abbreviated_5":"May","cldr_month_format_abbreviated_6":"Jun","cldr_month_format_abbreviated_7":"Jul","cldr_month_format_abbreviated_8":"Aug","cldr_month_format_abbreviated_9":"Sep","cldr_month_format_wide_1":"January","cldr_month_format_wide_10":"October","cldr_month_format_wide_11":"November","cldr_month_format_wide_12":"December","cldr_month_format_wide_2":"February","cldr_month_format_wide_3":"March","cldr_month_format_wide_4":"April","cldr_month_format_wide_5":"May","cldr_month_format_wide_6":"June","cldr_month_format_wide_7":"July","cldr_month_format_wide_8":"August","cldr_month_format_wide_9":"September","cldr_month_stand-alone_narrow_1":"J","cldr_month_stand-alone_narrow_10":"O","cldr_month_stand-alone_narrow_11":"N","cldr_month_stand-alone_narrow_12":"D","cldr_month_stand-alone_narrow_2":"F","cldr_month_stand-alone_narrow_3":"M","cldr_month_stand-alone_narrow_4":"A","cldr_month_stand-alone_narrow_5":"M","cldr_month_stand-alone_narrow_6":"J","cldr_month_stand-alone_narrow_7":"J","cldr_month_stand-alone_narrow_8":"A","cldr_month_stand-alone_narrow_9":"S","cldr_number_decimal_separator":".","cldr_number_group_separator":",","cldr_number_percent_format":"#,##0%","cldr_pm":"PM","cldr_time_format_full":"h:mm:ss a zzzz","cldr_time_format_long":"h:mm:ss a z","cldr_time_format_medium":"h:mm:ss a","cldr_time_format_short":"h:mm a","quotationEnd":"”","quotationStart":"“"}},"resources":{"kirra_qooxdoo/css/custom.css":"kirra_qooxdoo","kirra_qooxdoo/css/styles.css":"kirra_qooxdoo","kirra_qooxdoo/test.png":[32,32,"png","kirra_qooxdoo"],"qx/mobile/css/LICENSE":"qx","qx/mobile/css/custom.css":"qx","qx/mobile/css/flat.css":"qx","qx/mobile/css/indigo.css":"qx","qx/mobile/js/iscroll.js":"qx","qx/mobile/js/iscroll.min.js":"qx","qx/static/blank.gif":[1,1,"gif","qx"],"qx/static/blank.html":"qx"},"translations":{"C":{},"en":{}}};

(function(){

  var b = ".prototype",c = "function",d = "Boolean",e = "Error",f = "Object.keys requires an object as argument.",g = "constructor",h = "warn",j = "default",k = "hasOwnProperty",m = "string",n = "Object",o = "toLocaleString",p = "error",q = "toString",r = "qx.debug",s = "()",t = "RegExp",u = "String",v = "info",w = "BROKEN_IE",x = "isPrototypeOf",y = "Date",z = "",A = "qx.Bootstrap",B = "Function",C = "]",D = "Cannot call super class. Method is not derived: ",E = "Array",F = "[Class ",G = "valueOf",H = "Number",I = "Class",J = "debug",K = "ES5",L = ".",M = "propertyIsEnumerable",N = "object";
  if(!window.qx){

    window.qx = {
    };
  };
  qx.Bootstrap = {
    genericToString : function(){

      return F + this.classname + C;
    },
    createNamespace : function(name, object){

      var splits = name.split(L);
      var part = splits[0];
      var parent = this.__a && this.__a[part] ? this.__a : window;
      for(var i = 0,len = splits.length - 1;i < len;i++,part = splits[i]){

        if(!parent[part]){

          parent = parent[part] = {
          };
        } else {

          parent = parent[part];
        };
      };
      parent[part] = object;
      return part;
    },
    setDisplayName : function(fcn, classname, name){

      fcn.displayName = classname + L + name + s;
    },
    setDisplayNames : function(functionMap, classname){

      for(var name in functionMap){

        var value = functionMap[name];
        if(value instanceof Function){

          value.displayName = classname + L + name + s;
        };
      };
    },
    base : function(args, varargs){

      if(qx.Bootstrap.DEBUG){

        if(!qx.Bootstrap.isFunction(args.callee.base)){

          throw new Error(D + args.callee.displayName);
        };
      };
      if(arguments.length === 1){

        return args.callee.base.call(this);
      } else {

        return args.callee.base.apply(this, Array.prototype.slice.call(arguments, 1));
      };
    },
    define : function(name, config){

      if(!config){

        config = {
          statics : {
          }
        };
      };
      var clazz;
      var proto = null;
      qx.Bootstrap.setDisplayNames(config.statics, name);
      if(config.members || config.extend){

        qx.Bootstrap.setDisplayNames(config.members, name + b);
        clazz = config.construct || new Function;
        if(config.extend){

          this.extendClass(clazz, clazz, config.extend, name, basename);
        };
        var statics = config.statics || {
        };
        for(var i = 0,keys = qx.Bootstrap.keys(statics),l = keys.length;i < l;i++){

          var key = keys[i];
          clazz[key] = statics[key];
        };
        proto = clazz.prototype;
        proto.base = qx.Bootstrap.base;
        var members = config.members || {
        };
        var key,member;
        for(var i = 0,keys = qx.Bootstrap.keys(members),l = keys.length;i < l;i++){

          key = keys[i];
          member = members[key];
          if(member instanceof Function && proto[key]){

            member.base = proto[key];
          };
          proto[key] = member;
        };
      } else {

        clazz = config.statics || {
        };
        if(qx.Bootstrap.$$registry && qx.Bootstrap.$$registry[name]){

          var formerClass = qx.Bootstrap.$$registry[name];
          if(this.keys(clazz).length !== 0){

            if(config.defer){

              config.defer(clazz, proto);
            };
            for(var curProp in clazz){

              formerClass[curProp] = clazz[curProp];
            };
            return formerClass;
          };
        };
      };
      clazz.$$type = I;
      if(!clazz.hasOwnProperty(q)){

        clazz.toString = this.genericToString;
      };
      var basename = name ? this.createNamespace(name, clazz) : z;
      clazz.name = clazz.classname = name;
      clazz.basename = basename;
      clazz.$$events = config.events;
      if(config.defer){

        config.defer(clazz, proto);
      };
      if(name != null){

        qx.Bootstrap.$$registry[name] = clazz;
      };
      return clazz;
    }
  };
  qx.Bootstrap.define(A, {
    statics : {
      __a : null,
      LOADSTART : qx.$$start || new Date(),
      DEBUG : (function(){

        var debug = true;
        if(qx.$$environment && qx.$$environment[r] === false){

          debug = false;
        };
        return debug;
      })(),
      getEnvironmentSetting : function(key){

        if(qx.$$environment){

          return qx.$$environment[key];
        };
      },
      setEnvironmentSetting : function(key, value){

        if(!qx.$$environment){

          qx.$$environment = {
          };
        };
        if(qx.$$environment[key] === undefined){

          qx.$$environment[key] = value;
        };
      },
      createNamespace : qx.Bootstrap.createNamespace,
      setRoot : function(root){

        this.__a = root;
      },
      base : qx.Bootstrap.base,
      define : qx.Bootstrap.define,
      setDisplayName : qx.Bootstrap.setDisplayName,
      setDisplayNames : qx.Bootstrap.setDisplayNames,
      genericToString : qx.Bootstrap.genericToString,
      extendClass : function(clazz, construct, superClass, name, basename){

        var superproto = superClass.prototype;
        var helper = new Function();
        helper.prototype = superproto;
        var proto = new helper();
        clazz.prototype = proto;
        proto.name = proto.classname = name;
        proto.basename = basename;
        construct.base = superClass;
        clazz.superclass = superClass;
        construct.self = clazz.constructor = proto.constructor = clazz;
      },
      getByName : function(name){

        return qx.Bootstrap.$$registry[name];
      },
      $$registry : {
      },
      objectGetLength : function(map){

        return qx.Bootstrap.keys(map).length;
      },
      objectMergeWith : function(target, source, overwrite){

        if(overwrite === undefined){

          overwrite = true;
        };
        for(var key in source){

          if(overwrite || target[key] === undefined){

            target[key] = source[key];
          };
        };
        return target;
      },
      __b : [x, k, o, q, G, M, g],
      keys : ({
        "ES5" : Object.keys,
        "BROKEN_IE" : function(map){

          if(map === null || (typeof map != N && typeof map != c)){

            throw new TypeError(f);
          };
          var arr = [];
          var hasOwnProperty = Object.prototype.hasOwnProperty;
          for(var key in map){

            if(hasOwnProperty.call(map, key)){

              arr.push(key);
            };
          };
          var shadowedKeys = qx.Bootstrap.__b;
          for(var i = 0,a = shadowedKeys,l = a.length;i < l;i++){

            if(hasOwnProperty.call(map, a[i])){

              arr.push(a[i]);
            };
          };
          return arr;
        },
        "default" : function(map){

          if(map === null || (typeof map != N && typeof map != c)){

            throw new TypeError(f);
          };
          var arr = [];
          var hasOwnProperty = Object.prototype.hasOwnProperty;
          for(var key in map){

            if(hasOwnProperty.call(map, key)){

              arr.push(key);
            };
          };
          return arr;
        }
      })[typeof (Object.keys) == c ? K : (function(){

        for(var key in {
          toString : 1
        }){

          return key;
        };
      })() !== q ? w : j],
      __c : {
        "[object String]" : u,
        "[object Array]" : E,
        "[object Object]" : n,
        "[object RegExp]" : t,
        "[object Number]" : H,
        "[object Boolean]" : d,
        "[object Date]" : y,
        "[object Function]" : B,
        "[object Error]" : e
      },
      bind : function(func, self, varargs){

        var fixedArgs = Array.prototype.slice.call(arguments, 2, arguments.length);
        return function(){

          var args = Array.prototype.slice.call(arguments, 0, arguments.length);
          return func.apply(self, fixedArgs.concat(args));
        };
      },
      firstUp : function(str){

        return str.charAt(0).toUpperCase() + str.substr(1);
      },
      firstLow : function(str){

        return str.charAt(0).toLowerCase() + str.substr(1);
      },
      getClass : function(value){

        var classString = Object.prototype.toString.call(value);
        return (qx.Bootstrap.__c[classString] || classString.slice(8, -1));
      },
      isString : function(value){

        return (value !== null && (typeof value === m || qx.Bootstrap.getClass(value) == u || value instanceof String || (!!value && !!value.$$isString)));
      },
      isArray : function(value){

        return (value !== null && (value instanceof Array || (value && qx.data && qx.data.IListData && qx.util.OOUtil.hasInterface(value.constructor, qx.data.IListData)) || qx.Bootstrap.getClass(value) == E || (!!value && !!value.$$isArray)));
      },
      isObject : function(value){

        return (value !== undefined && value !== null && qx.Bootstrap.getClass(value) == n);
      },
      isFunction : function(value){

        return qx.Bootstrap.getClass(value) == B;
      },
      $$logs : [],
      debug : function(object, message){

        qx.Bootstrap.$$logs.push([J, arguments]);
      },
      info : function(object, message){

        qx.Bootstrap.$$logs.push([v, arguments]);
      },
      warn : function(object, message){

        qx.Bootstrap.$$logs.push([h, arguments]);
      },
      error : function(object, message){

        qx.Bootstrap.$$logs.push([p, arguments]);
      },
      trace : function(object){
      }
    }
  });
})();
(function(){

  var a = "qx.util.OOUtil";
  qx.Bootstrap.define(a, {
    statics : {
      classIsDefined : function(name){

        return qx.Bootstrap.getByName(name) !== undefined;
      },
      getPropertyDefinition : function(clazz, name){

        while(clazz){

          if(clazz.$$properties && clazz.$$properties[name]){

            return clazz.$$properties[name];
          };
          clazz = clazz.superclass;
        };
        return null;
      },
      hasProperty : function(clazz, name){

        return !!qx.util.OOUtil.getPropertyDefinition(clazz, name);
      },
      getEventType : function(clazz, name){

        var clazz = clazz.constructor;
        while(clazz.superclass){

          if(clazz.$$events && clazz.$$events[name] !== undefined){

            return clazz.$$events[name];
          };
          clazz = clazz.superclass;
        };
        return null;
      },
      supportsEvent : function(clazz, name){

        return !!qx.util.OOUtil.getEventType(clazz, name);
      },
      getByInterface : function(clazz, iface){

        var list,i,l;
        while(clazz){

          if(clazz.$$implements){

            list = clazz.$$flatImplements;
            for(i = 0,l = list.length;i < l;i++){

              if(list[i] === iface){

                return clazz;
              };
            };
          };
          clazz = clazz.superclass;
        };
        return null;
      },
      hasInterface : function(clazz, iface){

        return !!qx.util.OOUtil.getByInterface(clazz, iface);
      },
      getMixins : function(clazz){

        var list = [];
        while(clazz){

          if(clazz.$$includes){

            list.push.apply(list, clazz.$$flatIncludes);
          };
          clazz = clazz.superclass;
        };
        return list;
      }
    }
  });
})();
(function(){

  var a = "qx.bom.client.Xml.getSelectSingleNode",b = "qx.bom.client.Stylesheet.getInsertRule",c = "qx.bom.client.Html.getDataset",d = "qx.bom.client.PhoneGap.getPhoneGap",e = "qx.bom.client.EcmaScript.getArrayReduce",f = "qx.core.Environment for a list of predefined keys.",g = '] found, and no default ("default") given',h = "qx.bom.client.Html.getAudioAif",j = "qx.bom.client.CssTransform.get3D",k = "qx.bom.client.EcmaScript.getArrayLastIndexOf",l = " is not a valid key. Please see the API-doc of ",m = ' type)',n = "qx.bom.client.EcmaScript.getArrayForEach",o = "qx.bom.client.Xml.getAttributeNS",p = "qx.bom.client.Stylesheet.getRemoveImport",q = "qx.bom.client.Css.getUserModify",r = "qx.bom.client.Css.getBoxShadow",s = "qx.bom.client.Html.getXul",t = "qx.bom.client.Plugin.getWindowsMedia",u = ":",v = "qx.blankpage",w = "The environment key 'json' is deprecated ",x = "qx.bom.client.Html.getVideo",y = "qx.bom.client.Device.getName",z = "qx.bom.client.Event.getTouch",A = "qx.optimization.strings",B = "qx.debug.property.level",C = "qx.bom.client.EcmaScript.getArrayFilter",D = "qx.bom.client.EcmaScript.getStringTrim",E = "qx.optimization.variables",F = "qx.bom.client.EcmaScript.getStackTrace",G = "qx.bom.client.EcmaScript.getDateNow",H = "qx.bom.client.EcmaScript.getArrayEvery",I = "qx.bom.client.Xml.getImplementation",J = "qx.bom.client.Html.getConsole",K = "qx.bom.client.Engine.getVersion",L = "qx.bom.client.Device.getType",M = "qx.bom.client.Plugin.getQuicktime",N = "qx.bom.client.Html.getNaturalDimensions",O = "qx.bom.client.Xml.getSelectNodes",P = "qx.bom.client.Xml.getElementsByTagNameNS",Q = "qx.nativeScrollBars",R = "qx.bom.client.Html.getDataUrl",S = "qx.bom.client.Flash.isAvailable",T = "qx.bom.client.Html.getCanvas",U = "qx.dyntheme",V = "qx.bom.client.Device.getPixelRatio",W = "qx.bom.client.Css.getBoxModel",X = "qx.bom.client.Plugin.getSilverlight",Y = "qx/static/blank.html",ba = "qx.bom.client.EcmaScript.getArrayMap",bb = "qx.bom.client.Css.getUserSelect",bc = "qx.bom.client.Css.getRadialGradient",bd = "json",be = "module.property",bf = "qx.bom.client.Plugin.getWindowsMediaVersion",bg = "qx.bom.client.Stylesheet.getCreateStyleSheet",bh = 'No match for variant "',bi = "qx.bom.client.Locale.getLocale",bj = "module.events",bk = "qx.bom.client.Plugin.getSkype",bl = "module.databinding",bm = "qx.bom.client.Html.getFileReader",bn = "qx.bom.client.Css.getBorderImage",bo = "qx.bom.client.Stylesheet.getDeleteRule",bp = "qx.bom.client.EcmaScript.getErrorToString",bq = "qx.bom.client.Plugin.getDivXVersion",br = "qx.bom.client.Scroll.scrollBarOverlayed",bs = "qx.bom.client.Plugin.getPdfVersion",bt = "qx.bom.client.Xml.getCreateNode",bu = "qx.bom.client.Css.getAlphaImageLoaderNeeded",bv = "qx.bom.client.Css.getLinearGradient",bw = "qx.bom.client.Transport.getXmlHttpRequest",bx = "qx.bom.client.Css.getBorderImageSyntax",by = "qx.bom.client.Html.getClassList",bz = "qx.bom.client.Event.getHelp",bA = "qx.optimization.comments",bB = "qx.bom.client.Locale.getVariant",bC = "qx.bom.client.Css.getBoxSizing",bD = "qx.bom.client.OperatingSystem.getName",bE = "module.logger",bF = "qx.mobile.emulatetouch",bG = "qx.bom.client.Html.getIsEqualNode",bH = "qx.bom.client.Html.getAudioWav",bI = "qx.bom.client.Browser.getName",bJ = "qx.bom.client.Css.getInlineBlock",bK = "qx.bom.client.Plugin.getPdf",bL = "qx.dynlocale",bM = "qx.bom.client.Device.getTouch",bN = "qx.emulatemouse",bO = '" (',bP = "qx.bom.client.Html.getAudio",bQ = "qx.core.Environment",bR = "qx.bom.client.EcmaScript.getFunctionBind",bS = "qx.bom.client.CssTransform.getSupport",bT = "qx.bom.client.Html.getTextContent",bU = "qx.bom.client.Css.getPlaceholder",bV = "qx.bom.client.Css.getFloat",bW = "default",bX = ' in variants [',bY = "false",ca = "qx.bom.client.Css.getFilterGradient",cb = "qx.bom.client.Html.getHistoryState",cc = "qxenv",cd = "qx.bom.client.Html.getSessionStorage",ce = "qx.bom.client.Html.getAudioAu",cf = "qx.bom.client.Css.getOpacity",cg = "qx.bom.client.Css.getFilterTextShadow",ch = "qx.bom.client.Html.getVml",ci = "qx.bom.client.Transport.getMaxConcurrentRequestCount",cj = "qx.bom.client.Event.getHashChange",ck = "qx.bom.client.Css.getRgba",cl = "qx.debug.dispose",cm = "qx.bom.client.Css.getBorderRadius",cn = "qx.bom.client.EcmaScript.getArraySome",co = "qx.bom.client.Transport.getSsl",cp = "qx.bom.client.Html.getWebWorker",cq = "qx.bom.client.Json.getJson",cr = "qx.bom.client.Browser.getQuirksMode",cs = "and will eventually be removed.",ct = "qx.bom.client.Css.getTextOverflow",cu = "qx.bom.client.EcmaScript.getArrayIndexOf",cv = "qx.bom.client.Xml.getQualifiedItem",cw = "qx.bom.client.Html.getVideoOgg",cx = "&",cy = "qx.bom.client.EcmaScript.getArrayReduceRight",cz = "qx.bom.client.Engine.getMsPointer",cA = "qx.bom.client.Browser.getDocumentMode",cB = "qx.allowUrlVariants",cC = "qx.debug.ui.queue",cD = "|",cE = "qx.bom.client.Html.getContains",cF = "qx.bom.client.Plugin.getActiveX",cG = ".",cH = "qx.bom.client.Xml.getDomProperties",cI = "qx.bom.client.CssAnimation.getSupport",cJ = "qx.debug.databinding",cK = "qx.optimization.basecalls",cL = "qx.bom.client.Browser.getVersion",cM = "qx.bom.client.Css.getUserSelectNone",cN = "true",cO = "qx.bom.client.Html.getSvg",cP = "qx.bom.client.EcmaScript.getObjectKeys",cQ = "qx.bom.client.Plugin.getDivX",cR = "qx.bom.client.Runtime.getName",cS = "qx.bom.client.Html.getLocalStorage",cT = "qx.allowUrlSettings",cU = "qx.bom.client.Flash.getStrictSecurityModel",cV = "qx.aspects",cW = "qx.debug",cX = "qx.bom.client.Css.getPointerEvents",cY = "qx.dynamicmousewheel",da = "qx.bom.client.Html.getAudioMp3",db = "qx.bom.client.Engine.getName",dc = "qx.bom.client.Html.getUserDataStorage",dd = "qx.bom.client.Plugin.getGears",de = "qx.bom.client.Plugin.getQuicktimeVersion",df = "qx.bom.client.Html.getAudioOgg",dg = "qx.bom.client.Css.getTextShadow",dh = "qx.bom.client.Plugin.getSilverlightVersion",di = "qx.bom.client.Html.getCompareDocumentPosition",dj = "qx.bom.client.Flash.getExpressInstall",dk = "qx.bom.client.Html.getSelection",dl = "qx.bom.client.OperatingSystem.getVersion",dm = "qx.bom.client.Html.getXPath",dn = "qx.bom.client.Html.getGeoLocation",dp = "qx.optimization.privates",dq = "qx.bom.client.Scroll.getNativeScroll",dr = "qx.bom.client.Css.getAppearance",ds = "qx.bom.client.CssTransition.getSupport",dt = "qx.bom.client.Stylesheet.getAddImport",du = "qx.optimization.variants",dv = "qx.bom.client.Html.getVideoWebm",dw = "qx.bom.client.Flash.getVersion",dx = "qx.bom.client.CssAnimation.getRequestAnimationFrame",dy = "qx.bom.client.Css.getLegacyWebkitGradient",dz = "qx.bom.client.PhoneGap.getNotification",dA = "qx.bom.client.Html.getVideoH264",dB = "qx.bom.client.Xml.getCreateElementNS",dC = "qx.bom.client.Xml.getDomParser";
  qx.Bootstrap.define(bQ, {
    statics : {
      _checks : {
      },
      _asyncChecks : {
      },
      __d : {
      },
      _checksMap : {
        "engine.version" : K,
        "engine.name" : db,
        "browser.name" : bI,
        "browser.version" : cL,
        "browser.documentmode" : cA,
        "browser.quirksmode" : cr,
        "runtime.name" : cR,
        "device.name" : y,
        "device.type" : L,
        "device.pixelRatio" : V,
        "device.touch" : bM,
        "locale" : bi,
        "locale.variant" : bB,
        "os.name" : bD,
        "os.version" : dl,
        "os.scrollBarOverlayed" : br,
        "plugin.gears" : dd,
        "plugin.activex" : cF,
        "plugin.skype" : bk,
        "plugin.quicktime" : M,
        "plugin.quicktime.version" : de,
        "plugin.windowsmedia" : t,
        "plugin.windowsmedia.version" : bf,
        "plugin.divx" : cQ,
        "plugin.divx.version" : bq,
        "plugin.silverlight" : X,
        "plugin.silverlight.version" : dh,
        "plugin.flash" : S,
        "plugin.flash.version" : dw,
        "plugin.flash.express" : dj,
        "plugin.flash.strictsecurity" : cU,
        "plugin.pdf" : bK,
        "plugin.pdf.version" : bs,
        "io.maxrequests" : ci,
        "io.ssl" : co,
        "io.xhr" : bw,
        "event.touch" : z,
        "event.mspointer" : cz,
        "event.help" : bz,
        "event.hashchange" : cj,
        "ecmascript.error.stacktrace" : F,
        "ecmascript.array.indexof" : cu,
        "ecmascript.array.lastindexof" : k,
        "ecmascript.array.foreach" : n,
        "ecmascript.array.filter" : C,
        "ecmascript.array.map" : ba,
        "ecmascript.array.some" : cn,
        "ecmascript.array.every" : H,
        "ecmascript.array.reduce" : e,
        "ecmascript.array.reduceright" : cy,
        "ecmascript.function.bind" : bR,
        "ecmascript.object.keys" : cP,
        "ecmascript.date.now" : G,
        "ecmascript.error.toString" : bp,
        "ecmascript.string.trim" : D,
        "html.webworker" : cp,
        "html.filereader" : bm,
        "html.geolocation" : dn,
        "html.audio" : bP,
        "html.audio.ogg" : df,
        "html.audio.mp3" : da,
        "html.audio.wav" : bH,
        "html.audio.au" : ce,
        "html.audio.aif" : h,
        "html.video" : x,
        "html.video.ogg" : cw,
        "html.video.h264" : dA,
        "html.video.webm" : dv,
        "html.storage.local" : cS,
        "html.storage.session" : cd,
        "html.storage.userdata" : dc,
        "html.classlist" : by,
        "html.xpath" : dm,
        "html.xul" : s,
        "html.canvas" : T,
        "html.svg" : cO,
        "html.vml" : ch,
        "html.dataset" : c,
        "html.dataurl" : R,
        "html.console" : J,
        "html.stylesheet.createstylesheet" : bg,
        "html.stylesheet.insertrule" : b,
        "html.stylesheet.deleterule" : bo,
        "html.stylesheet.addimport" : dt,
        "html.stylesheet.removeimport" : p,
        "html.element.contains" : cE,
        "html.element.compareDocumentPosition" : di,
        "html.element.textcontent" : bT,
        "html.image.naturaldimensions" : N,
        "html.history.state" : cb,
        "html.selection" : dk,
        "html.node.isequalnode" : bG,
        "json" : cq,
        "css.textoverflow" : ct,
        "css.placeholder" : bU,
        "css.borderradius" : cm,
        "css.borderimage" : bn,
        "css.borderimage.standardsyntax" : bx,
        "css.boxshadow" : r,
        "css.gradient.linear" : bv,
        "css.gradient.filter" : ca,
        "css.gradient.radial" : bc,
        "css.gradient.legacywebkit" : dy,
        "css.boxmodel" : W,
        "css.rgba" : ck,
        "css.userselect" : bb,
        "css.userselect.none" : cM,
        "css.usermodify" : q,
        "css.appearance" : dr,
        "css.float" : bV,
        "css.boxsizing" : bC,
        "css.animation" : cI,
        "css.animation.requestframe" : dx,
        "css.transform" : bS,
        "css.transform.3d" : j,
        "css.transition" : ds,
        "css.inlineblock" : bJ,
        "css.opacity" : cf,
        "css.textShadow" : dg,
        "css.textShadow.filter" : cg,
        "css.alphaimageloaderneeded" : bu,
        "css.pointerevents" : cX,
        "phonegap" : d,
        "phonegap.notification" : dz,
        "xml.implementation" : I,
        "xml.domparser" : dC,
        "xml.selectsinglenode" : a,
        "xml.selectnodes" : O,
        "xml.getelementsbytagnamens" : P,
        "xml.domproperties" : cH,
        "xml.attributens" : o,
        "xml.createnode" : bt,
        "xml.getqualifieditem" : cv,
        "xml.createelementns" : dB,
        "qx.mobile.nativescroll" : dq
      },
      get : function(key){

        if(qx.Bootstrap.DEBUG){

          if(key === bd){

            qx.Bootstrap.warn(w + cs);
          };
        };
        if(this.__d[key] != undefined){

          return this.__d[key];
        };
        var check = this._checks[key];
        if(check){

          var value = check();
          this.__d[key] = value;
          return value;
        };
        var classAndMethod = this._getClassNameFromEnvKey(key);
        if(classAndMethod[0] != undefined){

          var clazz = classAndMethod[0];
          var method = classAndMethod[1];
          var value = clazz[method]();
          this.__d[key] = value;
          return value;
        };
        if(qx.Bootstrap.DEBUG){

          qx.Bootstrap.warn(key + l + f);
          qx.Bootstrap.trace(this);
        };
      },
      _getClassNameFromEnvKey : function(key){

        var envmappings = this._checksMap;
        if(envmappings[key] != undefined){

          var implementation = envmappings[key];
          var lastdot = implementation.lastIndexOf(cG);
          if(lastdot > -1){

            var classname = implementation.slice(0, lastdot);
            var methodname = implementation.slice(lastdot + 1);
            var clazz = qx.Bootstrap.getByName(classname);
            if(clazz != undefined){

              return [clazz, methodname];
            };
          };
        };
        return [undefined, undefined];
      },
      getAsync : function(key, callback, self){

        var env = this;
        if(this.__d[key] != undefined){

          window.setTimeout(function(){

            callback.call(self, env.__d[key]);
          }, 0);
          return;
        };
        var check = this._asyncChecks[key];
        if(check){

          check(function(result){

            env.__d[key] = result;
            callback.call(self, result);
          });
          return;
        };
        var classAndMethod = this._getClassNameFromEnvKey(key);
        if(classAndMethod[0] != undefined){

          var clazz = classAndMethod[0];
          var method = classAndMethod[1];
          clazz[method](function(result){

            env.__d[key] = result;
            callback.call(self, result);
          });
          return;
        };
        if(qx.Bootstrap.DEBUG){

          qx.Bootstrap.warn(key + l + f);
          qx.Bootstrap.trace(this);
        };
      },
      select : function(key, values){

        return this.__e(this.get(key), values);
      },
      selectAsync : function(key, values, self){

        this.getAsync(key, function(result){

          var value = this.__e(key, values);
          value.call(self, result);
        }, this);
      },
      __e : function(key, values){

        var value = values[key];
        if(values.hasOwnProperty(key)){

          return value;
        };
        for(var id in values){

          if(id.indexOf(cD) != -1){

            var ids = id.split(cD);
            for(var i = 0;i < ids.length;i++){

              if(ids[i] == key){

                return values[id];
              };
            };
          };
        };
        if(values[bW] !== undefined){

          return values[bW];
        };
        if(qx.Bootstrap.DEBUG){

          throw new Error(bh + key + bO + (typeof key) + m + bX + qx.Bootstrap.keys(values) + g);
        };
      },
      filter : function(map){

        var returnArray = [];
        for(var check in map){

          if(this.get(check)){

            returnArray.push(map[check]);
          };
        };
        return returnArray;
      },
      invalidateCacheKey : function(key){

        delete this.__d[key];
      },
      add : function(key, check){

        if(this._checks[key] == undefined){

          if(check instanceof Function){

            this._checks[key] = check;
          } else {

            this._checks[key] = this.__h(check);
          };
        };
      },
      addAsync : function(key, check){

        if(this._checks[key] == undefined){

          this._asyncChecks[key] = check;
        };
      },
      getChecks : function(){

        return this._checks;
      },
      getAsyncChecks : function(){

        return this._asyncChecks;
      },
      _initDefaultQxValues : function(){

        this.add(cN, function(){

          return true;
        });
        this.add(cT, function(){

          return false;
        });
        this.add(cB, function(){

          return false;
        });
        this.add(B, function(){

          return 0;
        });
        this.add(cW, function(){

          return true;
        });
        this.add(cC, function(){

          return true;
        });
        this.add(cV, function(){

          return false;
        });
        this.add(bL, function(){

          return true;
        });
        this.add(U, function(){

          return true;
        });
        this.add(bF, function(){

          return false;
        });
        this.add(bN, function(){

          return false;
        });
        this.add(v, function(){

          return Y;
        });
        this.add(cY, function(){

          return true;
        });
        this.add(cJ, function(){

          return false;
        });
        this.add(cl, function(){

          return false;
        });
        this.add(cK, function(){

          return false;
        });
        this.add(bA, function(){

          return false;
        });
        this.add(dp, function(){

          return false;
        });
        this.add(A, function(){

          return false;
        });
        this.add(E, function(){

          return false;
        });
        this.add(du, function(){

          return false;
        });
        this.add(bl, function(){

          return true;
        });
        this.add(bE, function(){

          return true;
        });
        this.add(be, function(){

          return true;
        });
        this.add(bj, function(){

          return true;
        });
        this.add(Q, function(){

          return false;
        });
      },
      __f : function(){

        if(qx && qx.$$environment){

          for(var key in qx.$$environment){

            var value = qx.$$environment[key];
            this._checks[key] = this.__h(value);
          };
        };
      },
      __g : function(){

        if(window.document && window.document.location){

          var urlChecks = window.document.location.search.slice(1).split(cx);
          for(var i = 0;i < urlChecks.length;i++){

            var check = urlChecks[i].split(u);
            if(check.length != 3 || check[0] != cc){

              continue;
            };
            var key = check[1];
            var value = decodeURIComponent(check[2]);
            if(value == cN){

              value = true;
            } else if(value == bY){

              value = false;
            } else if(/^(\d|\.)+$/.test(value)){

              value = parseFloat(value);
            };;
            this._checks[key] = this.__h(value);
          };
        };
      },
      __h : function(value){

        return qx.Bootstrap.bind(function(value){

          return value;
        }, null, value);
      }
    },
    defer : function(statics){

      statics._initDefaultQxValues();
      statics.__f();
      if(statics.get(cT) === true){

        statics.__g();
      };
    }
  });
})();
(function(){

  var a = "ecmascript.array.lastindexof",b = "function",c = "stack",d = "ecmascript.array.map",f = "ecmascript.date.now",g = "ecmascript.array.reduce",h = "e",i = "qx.bom.client.EcmaScript",j = "ecmascript.object.keys",k = "ecmascript.error.stacktrace",l = "ecmascript.string.trim",m = "ecmascript.array.indexof",n = "stacktrace",o = "ecmascript.error.toString",p = "[object Error]",q = "ecmascript.array.foreach",r = "ecmascript.function.bind",s = "ecmascript.array.reduceright",t = "ecmascript.array.some",u = "ecmascript.array.filter",v = "ecmascript.array.every";
  qx.Bootstrap.define(i, {
    statics : {
      getStackTrace : function(){

        var propName;
        var e = new Error(h);
        propName = e.stack ? c : e.stacktrace ? n : null;
        if(!propName){

          try{

            throw e;
          } catch(ex) {

            e = ex;
          };
        };
        return e.stacktrace ? n : e.stack ? c : null;
      },
      getArrayIndexOf : function(){

        return !!Array.prototype.indexOf;
      },
      getArrayLastIndexOf : function(){

        return !!Array.prototype.lastIndexOf;
      },
      getArrayForEach : function(){

        return !!Array.prototype.forEach;
      },
      getArrayFilter : function(){

        return !!Array.prototype.filter;
      },
      getArrayMap : function(){

        return !!Array.prototype.map;
      },
      getArraySome : function(){

        return !!Array.prototype.some;
      },
      getArrayEvery : function(){

        return !!Array.prototype.every;
      },
      getArrayReduce : function(){

        return !!Array.prototype.reduce;
      },
      getArrayReduceRight : function(){

        return !!Array.prototype.reduceRight;
      },
      getErrorToString : function(){

        return typeof Error.prototype.toString == b && Error.prototype.toString() !== p;
      },
      getFunctionBind : function(){

        return typeof Function.prototype.bind === b;
      },
      getObjectKeys : function(){

        return !!Object.keys;
      },
      getDateNow : function(){

        return !!Date.now;
      },
      getStringTrim : function(){

        return typeof String.prototype.trim === b;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(m, statics.getArrayIndexOf);
      qx.core.Environment.add(a, statics.getArrayLastIndexOf);
      qx.core.Environment.add(q, statics.getArrayForEach);
      qx.core.Environment.add(u, statics.getArrayFilter);
      qx.core.Environment.add(d, statics.getArrayMap);
      qx.core.Environment.add(t, statics.getArraySome);
      qx.core.Environment.add(v, statics.getArrayEvery);
      qx.core.Environment.add(g, statics.getArrayReduce);
      qx.core.Environment.add(s, statics.getArrayReduceRight);
      qx.core.Environment.add(f, statics.getDateNow);
      qx.core.Environment.add(o, statics.getErrorToString);
      qx.core.Environment.add(k, statics.getStackTrace);
      qx.core.Environment.add(r, statics.getFunctionBind);
      qx.core.Environment.add(j, statics.getObjectKeys);
      qx.core.Environment.add(l, statics.getStringTrim);
    }
  });
})();
(function(){

  var a = "qx.lang.normalize.Function",b = "ecmascript.function.bind",c = "function",d = "Function.prototype.bind called on incompatible ";
  qx.Bootstrap.define(a, {
    defer : function(){

      if(!qx.core.Environment.get(b)){

        var slice = Array.prototype.slice;
        Function.prototype.bind = function(that){

          var target = this;
          if(typeof target != c){

            throw new TypeError(d + target);
          };
          var args = slice.call(arguments, 1);
          var bound = function(){

            if(this instanceof bound){

              var F = function(){
              };
              F.prototype = target.prototype;
              var self = new F;
              var result = target.apply(self, args.concat(slice.call(arguments)));
              if(Object(result) === result){

                return result;
              };
              return self;
            } else {

              return target.apply(that, args.concat(slice.call(arguments)));
            };
          };
          return bound;
        };
      };
    }
  });
})();
(function(){

  var a = "function",b = "ecmascript.array.lastindexof",c = "ecmascript.array.map",d = "ecmascript.array.filter",e = "Length is 0 and no second argument given",f = "qx.lang.normalize.Array",g = "ecmascript.array.indexof",h = "First argument is not callable",j = "ecmascript.array.reduce",k = "ecmascript.array.foreach",m = "ecmascript.array.reduceright",n = "ecmascript.array.some",o = "ecmascript.array.every";
  qx.Bootstrap.define(f, {
    defer : function(){

      if(!qx.core.Environment.get(g)){

        Array.prototype.indexOf = function(searchElement, fromIndex){

          if(fromIndex == null){

            fromIndex = 0;
          } else if(fromIndex < 0){

            fromIndex = Math.max(0, this.length + fromIndex);
          };
          for(var i = fromIndex;i < this.length;i++){

            if(this[i] === searchElement){

              return i;
            };
          };
          return -1;
        };
      };
      if(!qx.core.Environment.get(b)){

        Array.prototype.lastIndexOf = function(searchElement, fromIndex){

          if(fromIndex == null){

            fromIndex = this.length - 1;
          } else if(fromIndex < 0){

            fromIndex = Math.max(0, this.length + fromIndex);
          };
          for(var i = fromIndex;i >= 0;i--){

            if(this[i] === searchElement){

              return i;
            };
          };
          return -1;
        };
      };
      if(!qx.core.Environment.get(k)){

        Array.prototype.forEach = function(callback, obj){

          var l = this.length;
          for(var i = 0;i < l;i++){

            var value = this[i];
            if(value !== undefined){

              callback.call(obj || window, value, i, this);
            };
          };
        };
      };
      if(!qx.core.Environment.get(d)){

        Array.prototype.filter = function(callback, obj){

          var res = [];
          var l = this.length;
          for(var i = 0;i < l;i++){

            var value = this[i];
            if(value !== undefined){

              if(callback.call(obj || window, value, i, this)){

                res.push(this[i]);
              };
            };
          };
          return res;
        };
      };
      if(!qx.core.Environment.get(c)){

        Array.prototype.map = function(callback, obj){

          var res = [];
          var l = this.length;
          for(var i = 0;i < l;i++){

            var value = this[i];
            if(value !== undefined){

              res[i] = callback.call(obj || window, value, i, this);
            };
          };
          return res;
        };
      };
      if(!qx.core.Environment.get(n)){

        Array.prototype.some = function(callback, obj){

          var l = this.length;
          for(var i = 0;i < l;i++){

            var value = this[i];
            if(value !== undefined){

              if(callback.call(obj || window, value, i, this)){

                return true;
              };
            };
          };
          return false;
        };
      };
      if(!qx.core.Environment.get(o)){

        Array.prototype.every = function(callback, obj){

          var l = this.length;
          for(var i = 0;i < l;i++){

            var value = this[i];
            if(value !== undefined){

              if(!callback.call(obj || window, value, i, this)){

                return false;
              };
            };
          };
          return true;
        };
      };
      if(!qx.core.Environment.get(j)){

        Array.prototype.reduce = function(callback, init){

          if(typeof callback !== a){

            throw new TypeError(h);
          };
          if(init === undefined && this.length === 0){

            throw new TypeError(e);
          };
          var ret = init === undefined ? this[0] : init;
          for(var i = init === undefined ? 1 : 0;i < this.length;i++){

            if(i in this){

              ret = callback.call(undefined, ret, this[i], i, this);
            };
          };
          return ret;
        };
      };
      if(!qx.core.Environment.get(m)){

        Array.prototype.reduceRight = function(callback, init){

          if(typeof callback !== a){

            throw new TypeError(h);
          };
          if(init === undefined && this.length === 0){

            throw new TypeError(e);
          };
          var ret = init === undefined ? this[this.length - 1] : init;
          for(var i = init === undefined ? this.length - 2 : this.length - 1;i >= 0;i--){

            if(i in this){

              ret = callback.call(undefined, ret, this[i], i, this);
            };
          };
          return ret;
        };
      };
    }
  });
})();
(function(){

  var a = "qx.Mixin",b = ".prototype",c = "]",d = 'Conflict between mixin "',e = "constructor",f = "Array",g = '"!',h = '" and "',j = "destruct",k = '" in property "',m = "Mixin",n = '" in member "',o = "[Mixin ";
  qx.Bootstrap.define(a, {
    statics : {
      define : function(name, config){

        if(config){

          if(config.include && !(qx.Bootstrap.getClass(config.include) === f)){

            config.include = [config.include];
          };
          {
          };
          var mixin = config.statics ? config.statics : {
          };
          qx.Bootstrap.setDisplayNames(mixin, name);
          for(var key in mixin){

            if(mixin[key] instanceof Function){

              mixin[key].$$mixin = mixin;
            };
          };
          if(config.construct){

            mixin.$$constructor = config.construct;
            qx.Bootstrap.setDisplayName(config.construct, name, e);
          };
          if(config.include){

            mixin.$$includes = config.include;
          };
          if(config.properties){

            mixin.$$properties = config.properties;
          };
          if(config.members){

            mixin.$$members = config.members;
            qx.Bootstrap.setDisplayNames(config.members, name + b);
          };
          for(var key in mixin.$$members){

            if(mixin.$$members[key] instanceof Function){

              mixin.$$members[key].$$mixin = mixin;
            };
          };
          if(config.events){

            mixin.$$events = config.events;
          };
          if(config.destruct){

            mixin.$$destructor = config.destruct;
            qx.Bootstrap.setDisplayName(config.destruct, name, j);
          };
        } else {

          var mixin = {
          };
        };
        mixin.$$type = m;
        mixin.name = name;
        mixin.toString = this.genericToString;
        mixin.basename = qx.Bootstrap.createNamespace(name, mixin);
        this.$$registry[name] = mixin;
        return mixin;
      },
      checkCompatibility : function(mixins){

        var list = this.flatten(mixins);
        var len = list.length;
        if(len < 2){

          return true;
        };
        var properties = {
        };
        var members = {
        };
        var events = {
        };
        var mixin;
        for(var i = 0;i < len;i++){

          mixin = list[i];
          for(var key in mixin.events){

            if(events[key]){

              throw new Error(d + mixin.name + h + events[key] + n + key + g);
            };
            events[key] = mixin.name;
          };
          for(var key in mixin.properties){

            if(properties[key]){

              throw new Error(d + mixin.name + h + properties[key] + k + key + g);
            };
            properties[key] = mixin.name;
          };
          for(var key in mixin.members){

            if(members[key]){

              throw new Error(d + mixin.name + h + members[key] + n + key + g);
            };
            members[key] = mixin.name;
          };
        };
        return true;
      },
      isCompatible : function(mixin, clazz){

        var list = qx.util.OOUtil.getMixins(clazz);
        list.push(mixin);
        return qx.Mixin.checkCompatibility(list);
      },
      getByName : function(name){

        return this.$$registry[name];
      },
      isDefined : function(name){

        return this.getByName(name) !== undefined;
      },
      getTotalNumber : function(){

        return qx.Bootstrap.objectGetLength(this.$$registry);
      },
      flatten : function(mixins){

        if(!mixins){

          return [];
        };
        var list = mixins.concat();
        for(var i = 0,l = mixins.length;i < l;i++){

          if(mixins[i].$$includes){

            list.push.apply(list, this.flatten(mixins[i].$$includes));
          };
        };
        return list;
      },
      genericToString : function(){

        return o + this.name + c;
      },
      $$registry : {
      },
      __i : null,
      __j : function(name, config){
      }
    }
  });
})();
(function(){

  var a = "qx.core.Aspect",b = "before",c = "*",d = "static";
  qx.Bootstrap.define(a, {
    statics : {
      __k : [],
      wrap : function(fullName, fcn, type){

        var before = [];
        var after = [];
        var reg = this.__k;
        var entry;
        for(var i = 0;i < reg.length;i++){

          entry = reg[i];
          if((entry.type == null || type == entry.type || entry.type == c) && (entry.name == null || fullName.match(entry.name))){

            entry.pos == -1 ? before.push(entry.fcn) : after.push(entry.fcn);
          };
        };
        if(before.length === 0 && after.length === 0){

          return fcn;
        };
        var wrapper = function(){

          for(var i = 0;i < before.length;i++){

            before[i].call(this, fullName, fcn, type, arguments);
          };
          var ret = fcn.apply(this, arguments);
          for(var i = 0;i < after.length;i++){

            after[i].call(this, fullName, fcn, type, arguments, ret);
          };
          return ret;
        };
        if(type !== d){

          wrapper.self = fcn.self;
          wrapper.base = fcn.base;
        };
        fcn.wrapper = wrapper;
        wrapper.original = fcn;
        return wrapper;
      },
      addAdvice : function(fcn, position, type, name){

        this.__k.push({
          fcn : fcn,
          pos : position === b ? -1 : 1,
          type : type,
          name : name
        });
      }
    }
  });
})();
(function(){

  var a = '',b = "ecmascript.string.trim",c = "qx.lang.normalize.String";
  qx.Bootstrap.define(c, {
    defer : function(){

      if(!qx.core.Environment.get(b)){

        String.prototype.trim = function(context){

          return this.replace(/^\s+|\s+$/g, a);
        };
      };
    }
  });
})();
(function(){

  var a = "ecmascript.object.keys",b = "qx.lang.normalize.Object";
  qx.Bootstrap.define(b, {
    defer : function(){

      if(!qx.core.Environment.get(a)){

        Object.keys = qx.Bootstrap.keys;
      };
    }
  });
})();
(function(){

  var a = 'Implementation of method "',b = '"',c = "function",d = '" is not supported by Class "',e = "Boolean",f = "qx.Interface",g = 'The event "',h = '" required by interface "',j = '" is missing in class "',k = '"!',m = 'The property "',n = "Interface",o = "toggle",p = "]",q = "[Interface ",r = "is",s = "Array",t = 'Implementation of member "';
  qx.Bootstrap.define(f, {
    statics : {
      define : function(name, config){

        if(config){

          if(config.extend && !(qx.Bootstrap.getClass(config.extend) === s)){

            config.extend = [config.extend];
          };
          {
          };
          var iface = config.statics ? config.statics : {
          };
          if(config.extend){

            iface.$$extends = config.extend;
          };
          if(config.properties){

            iface.$$properties = config.properties;
          };
          if(config.members){

            iface.$$members = config.members;
          };
          if(config.events){

            iface.$$events = config.events;
          };
        } else {

          var iface = {
          };
        };
        iface.$$type = n;
        iface.name = name;
        iface.toString = this.genericToString;
        iface.basename = qx.Bootstrap.createNamespace(name, iface);
        qx.Interface.$$registry[name] = iface;
        return iface;
      },
      getByName : function(name){

        return this.$$registry[name];
      },
      isDefined : function(name){

        return this.getByName(name) !== undefined;
      },
      getTotalNumber : function(){

        return qx.Bootstrap.objectGetLength(this.$$registry);
      },
      flatten : function(ifaces){

        if(!ifaces){

          return [];
        };
        var list = ifaces.concat();
        for(var i = 0,l = ifaces.length;i < l;i++){

          if(ifaces[i].$$extends){

            list.push.apply(list, this.flatten(ifaces[i].$$extends));
          };
        };
        return list;
      },
      __l : function(object, clazz, iface, wrap, shouldThrow){

        var members = iface.$$members;
        if(members){

          for(var key in members){

            if(qx.Bootstrap.isFunction(members[key])){

              var isPropertyMethod = this.__m(clazz, key);
              var hasMemberFunction = isPropertyMethod || qx.Bootstrap.isFunction(object[key]);
              if(!hasMemberFunction){

                if(shouldThrow){

                  throw new Error(a + key + j + clazz.classname + h + iface.name + b);
                } else {

                  return false;
                };
              };
              var shouldWrapFunction = wrap === true && !isPropertyMethod && !qx.util.OOUtil.hasInterface(clazz, iface);
              if(shouldWrapFunction){

                object[key] = this.__p(iface, object[key], key, members[key]);
              };
            } else {

              if(typeof object[key] === undefined){

                if(typeof object[key] !== c){

                  if(shouldThrow){

                    throw new Error(t + key + j + clazz.classname + h + iface.name + b);
                  } else {

                    return false;
                  };
                };
              };
            };
          };
        };
        if(!shouldThrow){

          return true;
        };
      },
      __m : function(clazz, methodName){

        var match = methodName.match(/^(is|toggle|get|set|reset)(.*)$/);
        if(!match){

          return false;
        };
        var propertyName = qx.Bootstrap.firstLow(match[2]);
        var isPropertyMethod = qx.util.OOUtil.getPropertyDefinition(clazz, propertyName);
        if(!isPropertyMethod){

          return false;
        };
        var isBoolean = match[0] == r || match[0] == o;
        if(isBoolean){

          return qx.util.OOUtil.getPropertyDefinition(clazz, propertyName).check == e;
        };
        return true;
      },
      __n : function(clazz, iface, shouldThrow){

        if(iface.$$properties){

          for(var key in iface.$$properties){

            if(!qx.util.OOUtil.getPropertyDefinition(clazz, key)){

              if(shouldThrow){

                throw new Error(m + key + d + clazz.classname + k);
              } else {

                return false;
              };
            };
          };
        };
        if(!shouldThrow){

          return true;
        };
      },
      __o : function(clazz, iface, shouldThrow){

        if(iface.$$events){

          for(var key in iface.$$events){

            if(!qx.util.OOUtil.supportsEvent(clazz, key)){

              if(shouldThrow){

                throw new Error(g + key + d + clazz.classname + k);
              } else {

                return false;
              };
            };
          };
        };
        if(!shouldThrow){

          return true;
        };
      },
      assertObject : function(object, iface){

        var clazz = object.constructor;
        this.__l(object, clazz, iface, false, true);
        this.__n(clazz, iface, true);
        this.__o(clazz, iface, true);
        var extend = iface.$$extends;
        if(extend){

          for(var i = 0,l = extend.length;i < l;i++){

            this.assertObject(object, extend[i]);
          };
        };
      },
      assert : function(clazz, iface, wrap){

        this.__l(clazz.prototype, clazz, iface, wrap, true);
        this.__n(clazz, iface, true);
        this.__o(clazz, iface, true);
        var extend = iface.$$extends;
        if(extend){

          for(var i = 0,l = extend.length;i < l;i++){

            this.assert(clazz, extend[i], wrap);
          };
        };
      },
      objectImplements : function(object, iface){

        var clazz = object.constructor;
        if(!this.__l(object, clazz, iface) || !this.__n(clazz, iface) || !this.__o(clazz, iface)){

          return false;
        };
        var extend = iface.$$extends;
        if(extend){

          for(var i = 0,l = extend.length;i < l;i++){

            if(!this.objectImplements(object, extend[i])){

              return false;
            };
          };
        };
        return true;
      },
      classImplements : function(clazz, iface){

        if(!this.__l(clazz.prototype, clazz, iface) || !this.__n(clazz, iface) || !this.__o(clazz, iface)){

          return false;
        };
        var extend = iface.$$extends;
        if(extend){

          for(var i = 0,l = extend.length;i < l;i++){

            if(!this.has(clazz, extend[i])){

              return false;
            };
          };
        };
        return true;
      },
      genericToString : function(){

        return q + this.name + p;
      },
      $$registry : {
      },
      __p : function(iface, origFunction, functionName, preCondition){
      },
      __i : null,
      __j : function(name, config){
      }
    }
  });
})();
(function(){

  var a = "",b = "qx.lang.normalize.Error",c = ": ",d = "Error",e = "ecmascript.error.toString";
  qx.Bootstrap.define(b, {
    defer : function(){

      if(!qx.core.Environment.get(e)){

        Error.prototype.toString = function(){

          var name = this.name || d;
          var message = this.message || a;
          if(name === a && message === a){

            return d;
          };
          if(name === a){

            return message;
          };
          if(message === a){

            return name;
          };
          return name + c + message;
        };
      };
    }
  });
})();
(function(){

  var a = "qx.lang.normalize.Date",b = "ecmascript.date.now";
  qx.Bootstrap.define(a, {
    defer : function(){

      if(!qx.core.Environment.get(b)){

        Date.now = function(){

          return +new Date();
        };
      };
    }
  });
})();
(function(){

  var b = '!==inherit){',c = 'qx.lang.Type.isString(value) && qx.util.ColorUtil.isValidPropertyValue(value)',d = 'value !== null && qx.theme.manager.Font.getInstance().isDynamic(value)',e = "set",f = ';',g = "resetThemed",h = 'value !== null && value.nodeType === 9 && value.documentElement',j = '===value)return value;',k = 'value !== null && value.$$type === "Mixin"',m = 'return init;',n = 'var init=this.',o = 'value !== null && value.nodeType === 1 && value.attributes',p = "var parent = this.getLayoutParent();",q = "Error in property ",r = 'var a=this._getChildren();if(a)for(var i=0,l=a.length;i<l;i++){',s = "property",t = "();",u = '.validate.call(this, value);',v = 'qx.core.Assert.assertInstance(value, Date, msg) || true',w = 'else{',x = "if (!parent) return;",y = " in method ",z = 'qx.core.Assert.assertInstance(value, Error, msg) || true',A = '=computed;',B = 'Undefined value is not allowed!',C = '(backup);',D = 'else ',E = '=true;',F = 'if(old===undefined)old=this.',G = 'if(computed===inherit){',H = 'old=computed=this.',I = "inherit",J = 'if(this.',K = 'return this.',L = 'else if(this.',M = 'Is invalid!',N = 'if(value===undefined)prop.error(this,2,"',O = '", "',P = 'var computed, old=this.',Q = 'else if(computed===undefined)',R = 'delete this.',S = "resetRuntime",T = "': ",U = " of class ",V = 'value !== null && value.nodeType !== undefined',W = '===undefined)return;',X = 'value !== null && qx.theme.manager.Decoration.getInstance().isValidPropertyValue(value)',Y = "reset",ba = "string",bb = "')){",bc = "module.events",bd = "return this.",be = 'qx.core.Assert.assertPositiveInteger(value, msg) || true',bf = 'else this.',bg = 'value=this.',bh = '","',bi = 'if(init==qx.core.Property.$$inherit)init=null;',bj = "get",bk = 'value !== null && value.$$type === "Interface"',bl = 'var inherit=prop.$$inherit;',bm = "', qx.event.type.Data, [computed, old]",bn = "var value = parent.",bo = "$$useinit_",bp = 'computed=undefined;delete this.',bq = "(value);",br = 'this.',bs = "setThemed",bt = '",value);',bu = 'computed=value;',bv = '}else{',bw = "$$runtime_",bx = 'Requires exactly one argument!',by = ';}',bz = '(value);',bA = "$$user_",bB = '!==undefined)',bC = '){',bD = 'qx.core.Assert.assertArray(value, msg) || true',bE = 'if(computed===undefined||computed===inherit){',bF = ";",bG = 'qx.core.Assert.assertPositiveNumber(value, msg) || true',bH = ".prototype",bI = "Boolean",bJ = ")}",bK = "(a[",bL = '(computed, old, "',bM = "setRuntime",bN = 'return value;',bO = "this.",bP = 'if(init==qx.core.Property.$$inherit)throw new Error("Inheritable property ',bQ = "if(reg.hasListener(this, '",bR = 'Does not allow any arguments!',bS = ')a[i].',bT = "()",bU = "var a=arguments[0] instanceof Array?arguments[0]:arguments;",bV = '.$$properties.',bW = 'value !== null && value.$$type === "Theme"',bX = 'old=this.',bY = "var reg=qx.event.Registration;",ca = "())",cb = '=value;',cc = 'return null;',cd = 'qx.core.Assert.assertObject(value, msg) || true',ce = '");',cf = 'if(old===computed)return value;',cg = 'qx.core.Assert.assertString(value, msg) || true',ch = 'if(old===undefined)old=null;',ci = 'var pa=this.getLayoutParent();if(pa)computed=pa.',cj = "if (value===undefined) value = parent.",ck = 'value !== null && value.$$type === "Class"',cl = 'qx.core.Assert.assertFunction(value, msg) || true',cm = '!==undefined&&',cn = 'var computed, old;',co = 'var backup=computed;',cp = ".",cq = '}',cr = "object",cs = "$$init_",ct = "$$theme_",cu = '!==undefined){',cv = 'if(computed===undefined)computed=null;',cw = "Unknown reason: ",cx = "init",cy = 'qx.core.Assert.assertMap(value, msg) || true',cz = "qx.aspects",cA = 'qx.core.Assert.assertNumber(value, msg) || true',cB = 'if((computed===undefined||computed===inherit)&&',cC = "reg.fireEvent(this, '",cD = 'Null value is not allowed!',cE = 'qx.core.Assert.assertInteger(value, msg) || true',cF = "value",cG = "shorthand",cH = 'computed=this.',cI = 'qx.core.Assert.assertInstance(value, RegExp, msg) || true',cJ = 'value !== null && value.type !== undefined',cK = 'value !== null && value.document',cL = "",cM = 'throw new Error("Property ',cN = "(!this.",cO = 'qx.core.Assert.assertBoolean(value, msg) || true',cP = 'if(a[i].',cQ = ' of an instance of ',cR = "toggle",cS = "refresh",cT = "$$inherit_",cU = 'var prop=qx.core.Property;',cV = "boolean",cW = " with incoming value '",cX = "a=qx.lang.Array.fromShortHand(qx.lang.Array.fromArguments(a));",cY = 'if(computed===undefined||computed==inherit)computed=null;',da = "qx.core.Property",db = "is",dc = ' is not (yet) ready!");',dd = "]);",de = 'Could not change or apply init value after constructing phase!';
  qx.Bootstrap.define(da, {
    statics : {
      __q : function(){

        if(qx.core.Environment.get(bc)){

          qx.event.type.Data;
          qx.event.dispatch.Direct;
        };
      },
      __r : {
        "Boolean" : cO,
        "String" : cg,
        "Number" : cA,
        "Integer" : cE,
        "PositiveNumber" : bG,
        "PositiveInteger" : be,
        "Error" : z,
        "RegExp" : cI,
        "Object" : cd,
        "Array" : bD,
        "Map" : cy,
        "Function" : cl,
        "Date" : v,
        "Node" : V,
        "Element" : o,
        "Document" : h,
        "Window" : cK,
        "Event" : cJ,
        "Class" : ck,
        "Mixin" : k,
        "Interface" : bk,
        "Theme" : bW,
        "Color" : c,
        "Decorator" : X,
        "Font" : d
      },
      __s : {
        "Node" : true,
        "Element" : true,
        "Document" : true,
        "Window" : true,
        "Event" : true
      },
      $$inherit : I,
      $$store : {
        runtime : {
        },
        user : {
        },
        theme : {
        },
        inherit : {
        },
        init : {
        },
        useinit : {
        }
      },
      $$method : {
        get : {
        },
        set : {
        },
        reset : {
        },
        init : {
        },
        refresh : {
        },
        setRuntime : {
        },
        resetRuntime : {
        },
        setThemed : {
        },
        resetThemed : {
        }
      },
      $$allowedKeys : {
        name : ba,
        dereference : cV,
        inheritable : cV,
        nullable : cV,
        themeable : cV,
        refine : cV,
        init : null,
        apply : ba,
        event : ba,
        check : null,
        transform : ba,
        deferredInit : cV,
        validate : null
      },
      $$allowedGroupKeys : {
        name : ba,
        group : cr,
        mode : ba,
        themeable : cV
      },
      $$inheritable : {
      },
      __t : function(clazz){

        var inheritables = this.__u(clazz);
        if(!inheritables.length){

          var refresher = function(){
          };
        } else {

          refresher = this.__v(inheritables);
        };
        clazz.prototype.$$refreshInheritables = refresher;
      },
      __u : function(clazz){

        var inheritable = [];
        while(clazz){

          var properties = clazz.$$properties;
          if(properties){

            for(var name in this.$$inheritable){

              if(properties[name] && properties[name].inheritable){

                inheritable.push(name);
              };
            };
          };
          clazz = clazz.superclass;
        };
        return inheritable;
      },
      __v : function(inheritables){

        var inherit = this.$$store.inherit;
        var init = this.$$store.init;
        var refresh = this.$$method.refresh;
        var code = [p, x];
        for(var i = 0,l = inheritables.length;i < l;i++){

          var name = inheritables[i];
          code.push(bn, inherit[name], bF, cj, init[name], bF, bO, refresh[name], bq);
        };
        return new Function(code.join(cL));
      },
      attachRefreshInheritables : function(clazz){

        clazz.prototype.$$refreshInheritables = function(){

          qx.core.Property.__t(clazz);
          return this.$$refreshInheritables();
        };
      },
      attachMethods : function(clazz, name, config){

        config.group ? this.__w(clazz, config, name) : this.__x(clazz, config, name);
      },
      __w : function(clazz, config, name){

        var upname = qx.Bootstrap.firstUp(name);
        var members = clazz.prototype;
        var themeable = config.themeable === true;
        {
        };
        var setter = [];
        var resetter = [];
        if(themeable){

          var styler = [];
          var unstyler = [];
        };
        var argHandler = bU;
        setter.push(argHandler);
        if(themeable){

          styler.push(argHandler);
        };
        if(config.mode == cG){

          var shorthand = cX;
          setter.push(shorthand);
          if(themeable){

            styler.push(shorthand);
          };
        };
        for(var i = 0,a = config.group,l = a.length;i < l;i++){

          {
          };
          setter.push(bO, this.$$method.set[a[i]], bK, i, dd);
          resetter.push(bO, this.$$method.reset[a[i]], t);
          if(themeable){

            {
            };
            styler.push(bO, this.$$method.setThemed[a[i]], bK, i, dd);
            unstyler.push(bO, this.$$method.resetThemed[a[i]], t);
          };
        };
        this.$$method.set[name] = e + upname;
        members[this.$$method.set[name]] = new Function(setter.join(cL));
        this.$$method.reset[name] = Y + upname;
        members[this.$$method.reset[name]] = new Function(resetter.join(cL));
        if(themeable){

          this.$$method.setThemed[name] = bs + upname;
          members[this.$$method.setThemed[name]] = new Function(styler.join(cL));
          this.$$method.resetThemed[name] = g + upname;
          members[this.$$method.resetThemed[name]] = new Function(unstyler.join(cL));
        };
      },
      __x : function(clazz, config, name){

        var upname = qx.Bootstrap.firstUp(name);
        var members = clazz.prototype;
        {
        };
        if(config.dereference === undefined && typeof config.check === ba){

          config.dereference = this.__y(config.check);
        };
        var method = this.$$method;
        var store = this.$$store;
        store.runtime[name] = bw + name;
        store.user[name] = bA + name;
        store.theme[name] = ct + name;
        store.init[name] = cs + name;
        store.inherit[name] = cT + name;
        store.useinit[name] = bo + name;
        method.get[name] = bj + upname;
        members[method.get[name]] = function(){

          return qx.core.Property.executeOptimizedGetter(this, clazz, name, bj);
        };
        method.set[name] = e + upname;
        members[method.set[name]] = function(value){

          return qx.core.Property.executeOptimizedSetter(this, clazz, name, e, arguments);
        };
        method.reset[name] = Y + upname;
        members[method.reset[name]] = function(){

          return qx.core.Property.executeOptimizedSetter(this, clazz, name, Y);
        };
        if(config.inheritable || config.apply || config.event || config.deferredInit){

          method.init[name] = cx + upname;
          members[method.init[name]] = function(value){

            return qx.core.Property.executeOptimizedSetter(this, clazz, name, cx, arguments);
          };
          {
          };
        };
        if(config.inheritable){

          method.refresh[name] = cS + upname;
          members[method.refresh[name]] = function(value){

            return qx.core.Property.executeOptimizedSetter(this, clazz, name, cS, arguments);
          };
          {
          };
        };
        method.setRuntime[name] = bM + upname;
        members[method.setRuntime[name]] = function(value){

          return qx.core.Property.executeOptimizedSetter(this, clazz, name, bM, arguments);
        };
        method.resetRuntime[name] = S + upname;
        members[method.resetRuntime[name]] = function(){

          return qx.core.Property.executeOptimizedSetter(this, clazz, name, S);
        };
        if(config.themeable){

          method.setThemed[name] = bs + upname;
          members[method.setThemed[name]] = function(value){

            return qx.core.Property.executeOptimizedSetter(this, clazz, name, bs, arguments);
          };
          method.resetThemed[name] = g + upname;
          members[method.resetThemed[name]] = function(){

            return qx.core.Property.executeOptimizedSetter(this, clazz, name, g);
          };
          {
          };
        };
        if(config.check === bI){

          members[cR + upname] = new Function(bd + method.set[name] + cN + method.get[name] + ca);
          members[db + upname] = new Function(bd + method.get[name] + bT);
          {
          };
        };
        {
        };
      },
      __y : function(check){

        return !!this.__s[check];
      },
      __z : {
        '0' : de,
        '1' : bx,
        '2' : B,
        '3' : bR,
        '4' : cD,
        '5' : M
      },
      error : function(obj, id, property, variant, value){

        var classname = obj.constructor.classname;
        var msg = q + property + U + classname + y + this.$$method[variant][property] + cW + value + T;
        throw new Error(msg + (this.__z[id] || cw + id));
      },
      __A : function(instance, members, name, variant, code, args){

        var store = this.$$method[variant][name];
        {

          members[store] = new Function(cF, code.join(cL));
        };
        if(qx.core.Environment.get(cz)){

          members[store] = qx.core.Aspect.wrap(instance.classname + cp + store, members[store], s);
        };
        qx.Bootstrap.setDisplayName(members[store], instance.classname + bH, store);
        if(args === undefined){

          return instance[store]();
        } else {

          return instance[store](args[0]);
        };
      },
      executeOptimizedGetter : function(instance, clazz, name, variant){

        var config = clazz.$$properties[name];
        var members = clazz.prototype;
        var code = [];
        var store = this.$$store;
        code.push(J, store.runtime[name], bB);
        code.push(K, store.runtime[name], f);
        if(config.inheritable){

          code.push(L, store.inherit[name], bB);
          code.push(K, store.inherit[name], f);
          code.push(D);
        };
        code.push(J, store.user[name], bB);
        code.push(K, store.user[name], f);
        if(config.themeable){

          code.push(L, store.theme[name], bB);
          code.push(K, store.theme[name], f);
        };
        if(config.deferredInit && config.init === undefined){

          code.push(L, store.init[name], bB);
          code.push(K, store.init[name], f);
        };
        code.push(D);
        if(config.init !== undefined){

          if(config.inheritable){

            code.push(n, store.init[name], f);
            if(config.nullable){

              code.push(bi);
            } else if(config.init !== undefined){

              code.push(K, store.init[name], f);
            } else {

              code.push(bP, name, cQ, clazz.classname, dc);
            };
            code.push(m);
          } else {

            code.push(K, store.init[name], f);
          };
        } else if(config.inheritable || config.nullable){

          code.push(cc);
        } else {

          code.push(cM, name, cQ, clazz.classname, dc);
        };
        return this.__A(instance, members, name, variant, code);
      },
      executeOptimizedSetter : function(instance, clazz, name, variant, args){

        var config = clazz.$$properties[name];
        var members = clazz.prototype;
        var code = [];
        var incomingValue = variant === e || variant === bs || variant === bM || (variant === cx && config.init === undefined);
        var hasCallback = config.apply || config.event || config.inheritable;
        var store = this.__B(variant, name);
        this.__C(code, config, name, variant, incomingValue);
        if(incomingValue){

          this.__D(code, clazz, config, name);
        };
        if(hasCallback){

          this.__E(code, incomingValue, store, variant);
        };
        if(config.inheritable){

          code.push(bl);
        };
        {
        };
        if(!hasCallback){

          this.__G(code, name, variant, incomingValue);
        } else {

          this.__H(code, config, name, variant, incomingValue);
        };
        if(config.inheritable){

          this.__I(code, config, name, variant);
        } else if(hasCallback){

          this.__J(code, config, name, variant);
        };
        if(hasCallback){

          this.__K(code, config, name, variant);
          if(config.inheritable && members._getChildren){

            this.__L(code, name);
          };
        };
        if(incomingValue){

          code.push(bN);
        };
        return this.__A(instance, members, name, variant, code, args);
      },
      __B : function(variant, name){

        if(variant === bM || variant === S){

          var store = this.$$store.runtime[name];
        } else if(variant === bs || variant === g){

          store = this.$$store.theme[name];
        } else if(variant === cx){

          store = this.$$store.init[name];
        } else {

          store = this.$$store.user[name];
        };;
        return store;
      },
      __C : function(code, config, name, variant, incomingValue){

        {

          if(!config.nullable || config.check || config.inheritable){

            code.push(cU);
          };
          if(variant === e){

            code.push(N, name, bh, variant, bt);
          };
        };
      },
      __D : function(code, clazz, config, name){

        if(config.transform){

          code.push(bg, config.transform, bz);
        };
        if(config.validate){

          if(typeof config.validate === ba){

            code.push(br, config.validate, bz);
          } else if(config.validate instanceof Function){

            code.push(clazz.classname, bV, name);
            code.push(u);
          };
        };
      },
      __E : function(code, incomingValue, store, variant){

        var resetValue = (variant === Y || variant === g || variant === S);
        if(incomingValue){

          code.push(J, store, j);
        } else if(resetValue){

          code.push(J, store, W);
        };
      },
      __F : undefined,
      __G : function(code, name, variant, incomingValue){

        if(variant === bM){

          code.push(br, this.$$store.runtime[name], cb);
        } else if(variant === S){

          code.push(J, this.$$store.runtime[name], bB);
          code.push(R, this.$$store.runtime[name], f);
        } else if(variant === e){

          code.push(br, this.$$store.user[name], cb);
        } else if(variant === Y){

          code.push(J, this.$$store.user[name], bB);
          code.push(R, this.$$store.user[name], f);
        } else if(variant === bs){

          code.push(br, this.$$store.theme[name], cb);
        } else if(variant === g){

          code.push(J, this.$$store.theme[name], bB);
          code.push(R, this.$$store.theme[name], f);
        } else if(variant === cx && incomingValue){

          code.push(br, this.$$store.init[name], cb);
        };;;;;;
      },
      __H : function(code, config, name, variant, incomingValue){

        if(config.inheritable){

          code.push(P, this.$$store.inherit[name], f);
        } else {

          code.push(cn);
        };
        code.push(J, this.$$store.runtime[name], cu);
        if(variant === bM){

          code.push(cH, this.$$store.runtime[name], cb);
        } else if(variant === S){

          code.push(R, this.$$store.runtime[name], f);
          code.push(J, this.$$store.user[name], bB);
          code.push(cH, this.$$store.user[name], f);
          code.push(L, this.$$store.theme[name], bB);
          code.push(cH, this.$$store.theme[name], f);
          code.push(L, this.$$store.init[name], cu);
          code.push(cH, this.$$store.init[name], f);
          code.push(br, this.$$store.useinit[name], E);
          code.push(cq);
        } else {

          code.push(H, this.$$store.runtime[name], f);
          if(variant === e){

            code.push(br, this.$$store.user[name], cb);
          } else if(variant === Y){

            code.push(R, this.$$store.user[name], f);
          } else if(variant === bs){

            code.push(br, this.$$store.theme[name], cb);
          } else if(variant === g){

            code.push(R, this.$$store.theme[name], f);
          } else if(variant === cx && incomingValue){

            code.push(br, this.$$store.init[name], cb);
          };;;;
        };
        code.push(cq);
        code.push(L, this.$$store.user[name], cu);
        if(variant === e){

          if(!config.inheritable){

            code.push(bX, this.$$store.user[name], f);
          };
          code.push(cH, this.$$store.user[name], cb);
        } else if(variant === Y){

          if(!config.inheritable){

            code.push(bX, this.$$store.user[name], f);
          };
          code.push(R, this.$$store.user[name], f);
          code.push(J, this.$$store.runtime[name], bB);
          code.push(cH, this.$$store.runtime[name], f);
          code.push(J, this.$$store.theme[name], bB);
          code.push(cH, this.$$store.theme[name], f);
          code.push(L, this.$$store.init[name], cu);
          code.push(cH, this.$$store.init[name], f);
          code.push(br, this.$$store.useinit[name], E);
          code.push(cq);
        } else {

          if(variant === bM){

            code.push(cH, this.$$store.runtime[name], cb);
          } else if(config.inheritable){

            code.push(cH, this.$$store.user[name], f);
          } else {

            code.push(H, this.$$store.user[name], f);
          };
          if(variant === bs){

            code.push(br, this.$$store.theme[name], cb);
          } else if(variant === g){

            code.push(R, this.$$store.theme[name], f);
          } else if(variant === cx && incomingValue){

            code.push(br, this.$$store.init[name], cb);
          };;
        };
        code.push(cq);
        if(config.themeable){

          code.push(L, this.$$store.theme[name], cu);
          if(!config.inheritable){

            code.push(bX, this.$$store.theme[name], f);
          };
          if(variant === bM){

            code.push(cH, this.$$store.runtime[name], cb);
          } else if(variant === e){

            code.push(cH, this.$$store.user[name], cb);
          } else if(variant === bs){

            code.push(cH, this.$$store.theme[name], cb);
          } else if(variant === g){

            code.push(R, this.$$store.theme[name], f);
            code.push(J, this.$$store.init[name], cu);
            code.push(cH, this.$$store.init[name], f);
            code.push(br, this.$$store.useinit[name], E);
            code.push(cq);
          } else if(variant === cx){

            if(incomingValue){

              code.push(br, this.$$store.init[name], cb);
            };
            code.push(cH, this.$$store.theme[name], f);
          } else if(variant === cS){

            code.push(cH, this.$$store.theme[name], f);
          };;;;;
          code.push(cq);
        };
        code.push(L, this.$$store.useinit[name], bC);
        if(!config.inheritable){

          code.push(bX, this.$$store.init[name], f);
        };
        if(variant === cx){

          if(incomingValue){

            code.push(cH, this.$$store.init[name], cb);
          } else {

            code.push(cH, this.$$store.init[name], f);
          };
        } else if(variant === e || variant === bM || variant === bs || variant === cS){

          code.push(R, this.$$store.useinit[name], f);
          if(variant === bM){

            code.push(cH, this.$$store.runtime[name], cb);
          } else if(variant === e){

            code.push(cH, this.$$store.user[name], cb);
          } else if(variant === bs){

            code.push(cH, this.$$store.theme[name], cb);
          } else if(variant === cS){

            code.push(cH, this.$$store.init[name], f);
          };;;
        };
        code.push(cq);
        if(variant === e || variant === bM || variant === bs || variant === cx){

          code.push(w);
          if(variant === bM){

            code.push(cH, this.$$store.runtime[name], cb);
          } else if(variant === e){

            code.push(cH, this.$$store.user[name], cb);
          } else if(variant === bs){

            code.push(cH, this.$$store.theme[name], cb);
          } else if(variant === cx){

            if(incomingValue){

              code.push(cH, this.$$store.init[name], cb);
            } else {

              code.push(cH, this.$$store.init[name], f);
            };
            code.push(br, this.$$store.useinit[name], E);
          };;;
          code.push(cq);
        };
      },
      __I : function(code, config, name, variant){

        code.push(bE);
        if(variant === cS){

          code.push(bu);
        } else {

          code.push(ci, this.$$store.inherit[name], f);
        };
        code.push(cB);
        code.push(br, this.$$store.init[name], cm);
        code.push(br, this.$$store.init[name], b);
        code.push(cH, this.$$store.init[name], f);
        code.push(br, this.$$store.useinit[name], E);
        code.push(bv);
        code.push(R, this.$$store.useinit[name], by);
        code.push(cq);
        code.push(cf);
        code.push(G);
        code.push(bp, this.$$store.inherit[name], f);
        code.push(cq);
        code.push(Q);
        code.push(R, this.$$store.inherit[name], f);
        code.push(bf, this.$$store.inherit[name], A);
        code.push(co);
        if(config.init !== undefined && variant !== cx){

          code.push(F, this.$$store.init[name], bF);
        } else {

          code.push(ch);
        };
        code.push(cY);
      },
      __J : function(code, config, name, variant){

        if(variant !== e && variant !== bM && variant !== bs){

          code.push(cv);
        };
        code.push(cf);
        if(config.init !== undefined && variant !== cx){

          code.push(F, this.$$store.init[name], bF);
        } else {

          code.push(ch);
        };
      },
      __K : function(code, config, name, variant){

        if(config.apply){

          code.push(br, config.apply, bL, name, O, variant, ce);
        };
        if(config.event){

          code.push(bY, bQ, config.event, bb, cC, config.event, bm, bJ);
        };
      },
      __L : function(code, name){

        code.push(r);
        code.push(cP, this.$$method.refresh[name], bS, this.$$method.refresh[name], C);
        code.push(cq);
      }
    }
  });
})();
(function(){

  var b = ".prototype",c = "$$init_",d = "constructor",e = "Property module disabled.",f = "extend",g = "module.property",h = "singleton",j = "qx.event.type.Data",k = "module.events",m = "qx.aspects",n = "toString",o = 'extend',p = "Array",q = "static",r = "",s = "Events module not enabled.",t = "]",u = "Class",v = "qx.Class",w = '"extend" parameter is null or undefined',x = "[Class ",y = "destructor",z = "destruct",A = ".",B = "member";
  qx.Bootstrap.define(v, {
    statics : {
      __M : qx.core.Environment.get(g) ? qx.core.Property : null,
      define : function(name, config){

        if(!config){

          config = {
          };
        };
        if(config.include && !(qx.Bootstrap.getClass(config.include) === p)){

          config.include = [config.include];
        };
        if(config.implement && !(qx.Bootstrap.getClass(config.implement) === p)){

          config.implement = [config.implement];
        };
        var implicitType = false;
        if(!config.hasOwnProperty(f) && !config.type){

          config.type = q;
          implicitType = true;
        };
        {
        };
        var clazz = this.__P(name, config.type, config.extend, config.statics, config.construct, config.destruct, config.include);
        if(config.extend){

          if(config.properties){

            this.__R(clazz, config.properties, true);
          };
          if(config.members){

            this.__T(clazz, config.members, true, true, false);
          };
          if(config.events){

            this.__Q(clazz, config.events, true);
          };
          if(config.include){

            for(var i = 0,l = config.include.length;i < l;i++){

              this.__X(clazz, config.include[i], false);
            };
          };
        } else if(config.hasOwnProperty(o) && false){

          throw new Error(w);
        };
        if(config.environment){

          for(var key in config.environment){

            qx.core.Environment.add(key, config.environment[key]);
          };
        };
        if(config.implement){

          for(var i = 0,l = config.implement.length;i < l;i++){

            this.__V(clazz, config.implement[i]);
          };
        };
        {
        };
        if(config.defer){

          config.defer.self = clazz;
          config.defer(clazz, clazz.prototype, {
            add : function(name, config){

              var properties = {
              };
              properties[name] = config;
              qx.Class.__R(clazz, properties, true);
            }
          });
        };
        return clazz;
      },
      undefine : function(name){

        delete this.$$registry[name];
        var ns = name.split(A);
        var objects = [window];
        for(var i = 0;i < ns.length;i++){

          objects.push(objects[i][ns[i]]);
        };
        for(var i = objects.length - 1;i >= 1;i--){

          var last = objects[i];
          var parent = objects[i - 1];
          if(qx.Bootstrap.isFunction(last) || qx.Bootstrap.objectGetLength(last) === 0){

            delete parent[ns[i - 1]];
          } else {

            break;
          };
        };
      },
      isDefined : qx.util.OOUtil.classIsDefined,
      getTotalNumber : function(){

        return qx.Bootstrap.objectGetLength(this.$$registry);
      },
      getByName : qx.Bootstrap.getByName,
      include : function(clazz, mixin){

        {
        };
        qx.Class.__X(clazz, mixin, false);
      },
      patch : function(clazz, mixin){

        {
        };
        qx.Class.__X(clazz, mixin, true);
      },
      isSubClassOf : function(clazz, superClass){

        if(!clazz){

          return false;
        };
        if(clazz == superClass){

          return true;
        };
        if(clazz.prototype instanceof superClass){

          return true;
        };
        return false;
      },
      getPropertyDefinition : qx.util.OOUtil.getPropertyDefinition,
      getProperties : function(clazz){

        var list = [];
        while(clazz){

          if(clazz.$$properties){

            list.push.apply(list, Object.keys(clazz.$$properties));
          };
          clazz = clazz.superclass;
        };
        return list;
      },
      getByProperty : function(clazz, name){

        while(clazz){

          if(clazz.$$properties && clazz.$$properties[name]){

            return clazz;
          };
          clazz = clazz.superclass;
        };
        return null;
      },
      hasProperty : qx.util.OOUtil.hasProperty,
      getEventType : qx.util.OOUtil.getEventType,
      supportsEvent : qx.util.OOUtil.supportsEvent,
      hasOwnMixin : function(clazz, mixin){

        return clazz.$$includes && clazz.$$includes.indexOf(mixin) !== -1;
      },
      getByMixin : function(clazz, mixin){

        var list,i,l;
        while(clazz){

          if(clazz.$$includes){

            list = clazz.$$flatIncludes;
            for(i = 0,l = list.length;i < l;i++){

              if(list[i] === mixin){

                return clazz;
              };
            };
          };
          clazz = clazz.superclass;
        };
        return null;
      },
      getMixins : qx.util.OOUtil.getMixins,
      hasMixin : function(clazz, mixin){

        return !!this.getByMixin(clazz, mixin);
      },
      hasOwnInterface : function(clazz, iface){

        return clazz.$$implements && clazz.$$implements.indexOf(iface) !== -1;
      },
      getByInterface : qx.util.OOUtil.getByInterface,
      getInterfaces : function(clazz){

        var list = [];
        while(clazz){

          if(clazz.$$implements){

            list.push.apply(list, clazz.$$flatImplements);
          };
          clazz = clazz.superclass;
        };
        return list;
      },
      hasInterface : qx.util.OOUtil.hasInterface,
      implementsInterface : function(obj, iface){

        var clazz = obj.constructor;
        if(this.hasInterface(clazz, iface)){

          return true;
        };
        if(qx.Interface.objectImplements(obj, iface)){

          return true;
        };
        if(qx.Interface.classImplements(clazz, iface)){

          return true;
        };
        return false;
      },
      getInstance : function(){

        if(!this.$$instance){

          this.$$allowconstruct = true;
          this.$$instance = new this();
          delete this.$$allowconstruct;
        };
        return this.$$instance;
      },
      genericToString : function(){

        return x + this.classname + t;
      },
      $$registry : qx.Bootstrap.$$registry,
      __i : null,
      __N : null,
      __j : function(name, config){
      },
      __O : function(clazz){
      },
      __P : function(name, type, extend, statics, construct, destruct, mixins){

        var clazz;
        if(!extend && qx.core.Environment.get(m) == false){

          clazz = statics || {
          };
          qx.Bootstrap.setDisplayNames(clazz, name);
        } else {

          clazz = {
          };
          if(extend){

            if(!construct){

              construct = this.__Y();
            };
            if(this.__bb(extend, mixins)){

              clazz = this.__bc(construct, name, type);
            } else {

              clazz = construct;
            };
            if(type === h){

              clazz.getInstance = this.getInstance;
            };
            qx.Bootstrap.setDisplayName(construct, name, d);
          };
          if(statics){

            qx.Bootstrap.setDisplayNames(statics, name);
            var key;
            for(var i = 0,a = Object.keys(statics),l = a.length;i < l;i++){

              key = a[i];
              var staticValue = statics[key];
              if(qx.core.Environment.get(m)){

                if(staticValue instanceof Function){

                  staticValue = qx.core.Aspect.wrap(name + A + key, staticValue, q);
                };
                clazz[key] = staticValue;
              } else {

                clazz[key] = staticValue;
              };
            };
          };
        };
        var basename = name ? qx.Bootstrap.createNamespace(name, clazz) : r;
        clazz.name = clazz.classname = name;
        clazz.basename = basename;
        clazz.$$type = u;
        if(type){

          clazz.$$classtype = type;
        };
        if(!clazz.hasOwnProperty(n)){

          clazz.toString = this.genericToString;
        };
        if(extend){

          qx.Bootstrap.extendClass(clazz, construct, extend, name, basename);
          if(destruct){

            if(qx.core.Environment.get(m)){

              destruct = qx.core.Aspect.wrap(name, destruct, y);
            };
            clazz.$$destructor = destruct;
            qx.Bootstrap.setDisplayName(destruct, name, z);
          };
        };
        this.$$registry[name] = clazz;
        return clazz;
      },
      __Q : function(clazz, events, patch){

        {

          var key,key;
        };
        if(clazz.$$events){

          for(var key in events){

            clazz.$$events[key] = events[key];
          };
        } else {

          clazz.$$events = events;
        };
      },
      __R : function(clazz, properties, patch){

        if(!qx.core.Environment.get(g)){

          throw new Error(e);
        };
        var config;
        if(patch === undefined){

          patch = false;
        };
        var proto = clazz.prototype;
        for(var name in properties){

          config = properties[name];
          {
          };
          config.name = name;
          if(!config.refine){

            if(clazz.$$properties === undefined){

              clazz.$$properties = {
              };
            };
            clazz.$$properties[name] = config;
          };
          if(config.init !== undefined){

            clazz.prototype[c + name] = config.init;
          };
          if(config.event !== undefined){

            if(!qx.core.Environment.get(k)){

              throw new Error(s);
            };
            var event = {
            };
            event[config.event] = j;
            this.__Q(clazz, event, patch);
          };
          if(config.inheritable){

            this.__M.$$inheritable[name] = true;
            if(!proto.$$refreshInheritables){

              this.__M.attachRefreshInheritables(clazz);
            };
          };
          if(!config.refine){

            this.__M.attachMethods(clazz, name, config);
          };
        };
      },
      __S : null,
      __T : function(clazz, members, patch, base, wrap){

        var proto = clazz.prototype;
        var key,member;
        qx.Bootstrap.setDisplayNames(members, clazz.classname + b);
        for(var i = 0,a = Object.keys(members),l = a.length;i < l;i++){

          key = a[i];
          member = members[key];
          {
          };
          if(base !== false && member instanceof Function && member.$$type == null){

            if(wrap == true){

              member = this.__U(member, proto[key]);
            } else {

              if(proto[key]){

                member.base = proto[key];
              };
              member.self = clazz;
            };
            if(qx.core.Environment.get(m)){

              member = qx.core.Aspect.wrap(clazz.classname + A + key, member, B);
            };
          };
          proto[key] = member;
        };
      },
      __U : function(member, base){

        if(base){

          return function(){

            var oldBase = member.base;
            member.base = base;
            var retval = member.apply(this, arguments);
            member.base = oldBase;
            return retval;
          };
        } else {

          return member;
        };
      },
      __V : function(clazz, iface){

        {
        };
        var list = qx.Interface.flatten([iface]);
        if(clazz.$$implements){

          clazz.$$implements.push(iface);
          clazz.$$flatImplements.push.apply(clazz.$$flatImplements, list);
        } else {

          clazz.$$implements = [iface];
          clazz.$$flatImplements = list;
        };
      },
      __W : function(clazz){

        var name = clazz.classname;
        var wrapper = this.__bc(clazz, name, clazz.$$classtype);
        for(var i = 0,a = Object.keys(clazz),l = a.length;i < l;i++){

          key = a[i];
          wrapper[key] = clazz[key];
        };
        wrapper.prototype = clazz.prototype;
        var members = clazz.prototype;
        for(var i = 0,a = Object.keys(members),l = a.length;i < l;i++){

          key = a[i];
          var method = members[key];
          if(method && method.self == clazz){

            method.self = wrapper;
          };
        };
        for(var key in this.$$registry){

          var construct = this.$$registry[key];
          if(!construct){

            continue;
          };
          if(construct.base == clazz){

            construct.base = wrapper;
          };
          if(construct.superclass == clazz){

            construct.superclass = wrapper;
          };
          if(construct.$$original){

            if(construct.$$original.base == clazz){

              construct.$$original.base = wrapper;
            };
            if(construct.$$original.superclass == clazz){

              construct.$$original.superclass = wrapper;
            };
          };
        };
        qx.Bootstrap.createNamespace(name, wrapper);
        this.$$registry[name] = wrapper;
        return wrapper;
      },
      __X : function(clazz, mixin, patch){

        {
        };
        if(this.hasMixin(clazz, mixin)){

          return;
        };
        var isConstructorWrapped = clazz.$$original;
        if(mixin.$$constructor && !isConstructorWrapped){

          clazz = this.__W(clazz);
        };
        var list = qx.Mixin.flatten([mixin]);
        var entry;
        for(var i = 0,l = list.length;i < l;i++){

          entry = list[i];
          if(entry.$$events){

            this.__Q(clazz, entry.$$events, patch);
          };
          if(entry.$$properties){

            this.__R(clazz, entry.$$properties, patch);
          };
          if(entry.$$members){

            this.__T(clazz, entry.$$members, patch, patch, patch);
          };
        };
        if(clazz.$$includes){

          clazz.$$includes.push(mixin);
          clazz.$$flatIncludes.push.apply(clazz.$$flatIncludes, list);
        } else {

          clazz.$$includes = [mixin];
          clazz.$$flatIncludes = list;
        };
      },
      __Y : function(){

        function defaultConstructor(){

          defaultConstructor.base.apply(this, arguments);
        };
        return defaultConstructor;
      },
      __ba : function(){

        return function(){
        };
      },
      __bb : function(base, mixins){

        {
        };
        if(base && base.$$includes){

          var baseMixins = base.$$flatIncludes;
          for(var i = 0,l = baseMixins.length;i < l;i++){

            if(baseMixins[i].$$constructor){

              return true;
            };
          };
        };
        if(mixins){

          var flatMixins = qx.Mixin.flatten(mixins);
          for(var i = 0,l = flatMixins.length;i < l;i++){

            if(flatMixins[i].$$constructor){

              return true;
            };
          };
        };
        return false;
      },
      __bc : function(construct, name, type){

        var wrapper = function(){

          var clazz = wrapper;
          {
          };
          var retval = clazz.$$original.apply(this, arguments);
          if(clazz.$$includes){

            var mixins = clazz.$$flatIncludes;
            for(var i = 0,l = mixins.length;i < l;i++){

              if(mixins[i].$$constructor){

                mixins[i].$$constructor.apply(this, arguments);
              };
            };
          };
          {
          };
          return retval;
        };
        if(qx.core.Environment.get(m)){

          var aspectWrapper = qx.core.Aspect.wrap(name, wrapper, d);
          wrapper.$$original = construct;
          wrapper.constructor = aspectWrapper;
          wrapper = aspectWrapper;
        };
        wrapper.$$original = construct;
        construct.wrapper = wrapper;
        return wrapper;
      }
    },
    defer : function(){

      if(qx.core.Environment.get(m)){

        for(var classname in qx.Bootstrap.$$registry){

          var statics = qx.Bootstrap.$$registry[classname];
          for(var key in statics){

            if(statics[key] instanceof Function){

              statics[key] = qx.core.Aspect.wrap(classname + A + key, statics[key], q);
            };
          };
        };
      };
    }
  });
})();
(function(){

  var a = "qx.data.MBinding";
  qx.Mixin.define(a, {
    members : {
      bind : function(sourcePropertyChain, targetObject, targetProperty, options){

        return qx.data.SingleValueBinding.bind(this, sourcePropertyChain, targetObject, targetProperty, options);
      },
      removeBinding : function(id){

        qx.data.SingleValueBinding.removeBindingFromObject(this, id);
      },
      removeAllBindings : function(){

        qx.data.SingleValueBinding.removeAllBindingsForObject(this);
      },
      getBindings : function(){

        return qx.data.SingleValueBinding.getAllBindingsForObject(this);
      }
    }
  });
})();
(function(){

  var a = ". Error message: ",b = "Boolean",c = "set",d = "deepBinding",f = ") to the object '",g = "item",h = "Please use only one array at a time: ",k = "Integer",l = "reset",m = " of object ",n = "qx.data.SingleValueBinding",p = "Binding property ",q = "Failed so set value ",r = "change",s = "Binding could not be found!",t = "get",u = "^",v = " does not work.",w = "String",x = "Binding from '",y = "",z = "PositiveNumber",A = "]",B = "[",C = ".",D = "PositiveInteger",E = 'No number or \'last\' value hast been given in an array binding: ',F = "' (",G = " on ",H = "Binding does not exist!",I = "Number",J = ").",K = "Date",L = " not possible: No event available. ",M = "last";
  qx.Class.define(n, {
    statics : {
      __bd : {
      },
      bind : function(sourceObject, sourcePropertyChain, targetObject, targetPropertyChain, options){

        {
        };
        var targetListenerMap = this.__bf(sourceObject, sourcePropertyChain, targetObject, targetPropertyChain, options);
        var propertyNames = sourcePropertyChain.split(C);
        var arrayIndexValues = this.__bl(propertyNames);
        var sources = [];
        var listeners = [];
        var listenerIds = [];
        var eventNames = [];
        var source = sourceObject;
        try{

          for(var i = 0;i < propertyNames.length;i++){

            if(arrayIndexValues[i] !== y){

              eventNames.push(r);
            } else {

              eventNames.push(this.__bg(source, propertyNames[i]));
            };
            sources[i] = source;
            if(i == propertyNames.length - 1){

              if(arrayIndexValues[i] !== y){

                var itemIndex = arrayIndexValues[i] === M ? source.length - 1 : arrayIndexValues[i];
                var currentValue = source.getItem(itemIndex);
                this.__bk(currentValue, targetObject, targetPropertyChain, options, sourceObject);
                listenerIds[i] = this.__bm(source, eventNames[i], targetObject, targetPropertyChain, options, arrayIndexValues[i]);
              } else {

                if(propertyNames[i] != null && source[t + qx.lang.String.firstUp(propertyNames[i])] != null){

                  var currentValue = source[t + qx.lang.String.firstUp(propertyNames[i])]();
                  this.__bk(currentValue, targetObject, targetPropertyChain, options, sourceObject);
                };
                listenerIds[i] = this.__bm(source, eventNames[i], targetObject, targetPropertyChain, options);
              };
            } else {

              var context = {
                index : i,
                propertyNames : propertyNames,
                sources : sources,
                listenerIds : listenerIds,
                arrayIndexValues : arrayIndexValues,
                targetObject : targetObject,
                targetPropertyChain : targetPropertyChain,
                options : options,
                listeners : listeners
              };
              var listener = qx.lang.Function.bind(this.__be, this, context);
              listeners.push(listener);
              listenerIds[i] = source.addListener(eventNames[i], listener);
            };
            if(source[t + qx.lang.String.firstUp(propertyNames[i])] == null){

              source = null;
            } else if(arrayIndexValues[i] !== y){

              source = source[t + qx.lang.String.firstUp(propertyNames[i])](arrayIndexValues[i]);
            } else {

              source = source[t + qx.lang.String.firstUp(propertyNames[i])]();
            };
            if(!source){

              break;
            };
          };
        } catch(ex) {

          for(var i = 0;i < sources.length;i++){

            if(sources[i] && listenerIds[i]){

              sources[i].removeListenerById(listenerIds[i]);
            };
          };
          var targets = targetListenerMap.targets;
          var targetIds = targetListenerMap.listenerIds;
          for(var i = 0;i < targets.length;i++){

            if(targets[i] && targetIds[i]){

              targets[i].removeListenerById(targetIds[i]);
            };
          };
          throw ex;
        };
        var id = {
          type : d,
          listenerIds : listenerIds,
          sources : sources,
          targetListenerIds : targetListenerMap.listenerIds,
          targets : targetListenerMap.targets
        };
        this.__bn(id, sourceObject, sourcePropertyChain, targetObject, targetPropertyChain);
        return id;
      },
      __be : function(context){

        if(context.options && context.options.onUpdate){

          context.options.onUpdate(context.sources[context.index], context.targetObject);
        };
        for(var j = context.index + 1;j < context.propertyNames.length;j++){

          var source = context.sources[j];
          context.sources[j] = null;
          if(!source){

            continue;
          };
          source.removeListenerById(context.listenerIds[j]);
        };
        var source = context.sources[context.index];
        for(var j = context.index + 1;j < context.propertyNames.length;j++){

          if(context.arrayIndexValues[j - 1] !== y){

            source = source[t + qx.lang.String.firstUp(context.propertyNames[j - 1])](context.arrayIndexValues[j - 1]);
          } else {

            source = source[t + qx.lang.String.firstUp(context.propertyNames[j - 1])]();
          };
          context.sources[j] = source;
          if(!source){

            if(context.options && context.options.converter){

              var ignoreConverter = false;
              if(context.options.ignoreConverter){

                var currentSourceChain = context.propertyNames.slice(0, j).join(C);
                var match = currentSourceChain.match(new RegExp(u + context.options.ignoreConverter));
                ignoreConverter = match ? match.length > 0 : false;
              };
              var data = null;
              if(!ignoreConverter){

                data = context.options.converter();
              };
              this.__bi(context.targetObject, context.targetPropertyChain, data);
            } else {

              this.__bh(context.targetObject, context.targetPropertyChain);
            };
            break;
          };
          if(j == context.propertyNames.length - 1){

            if(qx.Class.implementsInterface(source, qx.data.IListData)){

              var itemIndex = context.arrayIndexValues[j] === M ? source.length - 1 : context.arrayIndexValues[j];
              var currentValue = source.getItem(itemIndex);
              this.__bk(currentValue, context.targetObject, context.targetPropertyChain, context.options, context.sources[context.index]);
              context.listenerIds[j] = this.__bm(source, r, context.targetObject, context.targetPropertyChain, context.options, context.arrayIndexValues[j]);
            } else {

              if(context.propertyNames[j] != null && source[t + qx.lang.String.firstUp(context.propertyNames[j])] != null){

                var currentValue = source[t + qx.lang.String.firstUp(context.propertyNames[j])]();
                this.__bk(currentValue, context.targetObject, context.targetPropertyChain, context.options, context.sources[context.index]);
              };
              var eventName = this.__bg(source, context.propertyNames[j]);
              context.listenerIds[j] = this.__bm(source, eventName, context.targetObject, context.targetPropertyChain, context.options);
            };
          } else {

            if(context.listeners[j] == null){

              var listener = qx.lang.Function.bind(this.__be, this, context);
              context.listeners.push(listener);
            };
            if(qx.Class.implementsInterface(source, qx.data.IListData)){

              var eventName = r;
            } else {

              var eventName = this.__bg(source, context.propertyNames[j]);
            };
            context.listenerIds[j] = source.addListener(eventName, context.listeners[j]);
          };
        };
      },
      __bf : function(sourceObject, sourcePropertyChain, targetObject, targetPropertyChain, options){

        var propertyNames = targetPropertyChain.split(C);
        var arrayIndexValues = this.__bl(propertyNames);
        var targets = [];
        var listeners = [];
        var listenerIds = [];
        var eventNames = [];
        var target = targetObject;
        for(var i = 0;i < propertyNames.length - 1;i++){

          if(arrayIndexValues[i] !== y){

            eventNames.push(r);
          } else {

            try{

              eventNames.push(this.__bg(target, propertyNames[i]));
            } catch(e) {

              break;
            };
          };
          targets[i] = target;
          var listener = function(){

            for(var j = i + 1;j < propertyNames.length - 1;j++){

              var target = targets[j];
              targets[j] = null;
              if(!target){

                continue;
              };
              target.removeListenerById(listenerIds[j]);
            };
            var target = targets[i];
            for(var j = i + 1;j < propertyNames.length - 1;j++){

              var firstUpPropName = qx.lang.String.firstUp(propertyNames[j - 1]);
              if(arrayIndexValues[j - 1] !== y){

                var currentIndex = arrayIndexValues[j - 1] === M ? target.getLength() - 1 : arrayIndexValues[j - 1];
                target = target[t + firstUpPropName](currentIndex);
              } else {

                target = target[t + firstUpPropName]();
              };
              targets[j] = target;
              if(listeners[j] == null){

                listeners.push(listener);
              };
              if(qx.Class.implementsInterface(target, qx.data.IListData)){

                var eventName = r;
              } else {

                try{

                  var eventName = qx.data.SingleValueBinding.__bg(target, propertyNames[j]);
                } catch(e) {

                  break;
                };
              };
              listenerIds[j] = target.addListener(eventName, listeners[j]);
            };
            qx.data.SingleValueBinding.updateTarget(sourceObject, sourcePropertyChain, targetObject, targetPropertyChain, options);
          };
          listeners.push(listener);
          listenerIds[i] = target.addListener(eventNames[i], listener);
          var firstUpPropName = qx.lang.String.firstUp(propertyNames[i]);
          if(target[t + firstUpPropName] == null){

            target = null;
          } else if(arrayIndexValues[i] !== y){

            target = target[t + firstUpPropName](arrayIndexValues[i]);
          } else {

            target = target[t + firstUpPropName]();
          };
          if(!target){

            break;
          };
        };
        return {
          listenerIds : listenerIds,
          targets : targets
        };
      },
      updateTarget : function(sourceObject, sourcePropertyChain, targetObject, targetPropertyChain, options){

        var value = this.resolvePropertyChain(sourceObject, sourcePropertyChain);
        value = qx.data.SingleValueBinding.__bo(value, targetObject, targetPropertyChain, options, sourceObject);
        this.__bi(targetObject, targetPropertyChain, value);
      },
      resolvePropertyChain : function(o, propertyChain){

        var source = this.__bj(o, propertyChain);
        var value;
        if(source != null){

          var lastProperty = propertyChain.substring(propertyChain.lastIndexOf(C) + 1, propertyChain.length);
          if(lastProperty.charAt(lastProperty.length - 1) == A){

            var index = lastProperty.substring(lastProperty.lastIndexOf(B) + 1, lastProperty.length - 1);
            var prop = lastProperty.substring(0, lastProperty.lastIndexOf(B));
            var sourceArray = source[t + qx.lang.String.firstUp(prop)]();
            if(index == M){

              index = sourceArray.length - 1;
            };
            if(sourceArray != null){

              value = sourceArray.getItem(index);
            };
          } else {

            value = source[t + qx.lang.String.firstUp(lastProperty)]();
          };
        };
        return value;
      },
      __bg : function(source, propertyname){

        var eventName = this.__bp(source, propertyname);
        if(eventName == null){

          if(qx.Class.supportsEvent(source.constructor, propertyname)){

            eventName = propertyname;
          } else if(qx.Class.supportsEvent(source.constructor, r + qx.lang.String.firstUp(propertyname))){

            eventName = r + qx.lang.String.firstUp(propertyname);
          } else {

            throw new qx.core.AssertionError(p + propertyname + m + source + L);
          };
        };
        return eventName;
      },
      __bh : function(targetObject, targetPropertyChain){

        var target = this.__bj(targetObject, targetPropertyChain);
        if(target != null){

          var lastProperty = targetPropertyChain.substring(targetPropertyChain.lastIndexOf(C) + 1, targetPropertyChain.length);
          if(lastProperty.charAt(lastProperty.length - 1) == A){

            this.__bi(targetObject, targetPropertyChain, null);
            return;
          };
          if(target[l + qx.lang.String.firstUp(lastProperty)] != undefined){

            target[l + qx.lang.String.firstUp(lastProperty)]();
          } else {

            target[c + qx.lang.String.firstUp(lastProperty)](null);
          };
        };
      },
      __bi : function(targetObject, targetPropertyChain, value){

        var target = this.__bj(targetObject, targetPropertyChain);
        if(target != null){

          var lastProperty = targetPropertyChain.substring(targetPropertyChain.lastIndexOf(C) + 1, targetPropertyChain.length);
          if(lastProperty.charAt(lastProperty.length - 1) == A){

            var index = lastProperty.substring(lastProperty.lastIndexOf(B) + 1, lastProperty.length - 1);
            var prop = lastProperty.substring(0, lastProperty.lastIndexOf(B));
            var targetArray = targetObject;
            if(!qx.Class.implementsInterface(targetArray, qx.data.IListData)){

              targetArray = target[t + qx.lang.String.firstUp(prop)]();
            };
            if(index == M){

              index = targetArray.length - 1;
            };
            if(targetArray != null){

              targetArray.setItem(index, value);
            };
          } else {

            target[c + qx.lang.String.firstUp(lastProperty)](value);
          };
        };
      },
      __bj : function(targetObject, targetPropertyChain){

        var properties = targetPropertyChain.split(C);
        var target = targetObject;
        for(var i = 0;i < properties.length - 1;i++){

          try{

            var property = properties[i];
            if(property.indexOf(A) == property.length - 1){

              var index = property.substring(property.indexOf(B) + 1, property.length - 1);
              property = property.substring(0, property.indexOf(B));
            };
            if(property != y){

              target = target[t + qx.lang.String.firstUp(property)]();
            };
            if(index != null){

              if(index == M){

                index = target.length - 1;
              };
              target = target.getItem(index);
              index = null;
            };
          } catch(ex) {

            return null;
          };
        };
        return target;
      },
      __bk : function(value, targetObject, targetPropertyChain, options, sourceObject){

        value = this.__bo(value, targetObject, targetPropertyChain, options, sourceObject);
        if(value === undefined){

          this.__bh(targetObject, targetPropertyChain);
        };
        if(value !== undefined){

          try{

            this.__bi(targetObject, targetPropertyChain, value);
            if(options && options.onUpdate){

              options.onUpdate(sourceObject, targetObject, value);
            };
          } catch(e) {

            if(!(e instanceof qx.core.ValidationError)){

              throw e;
            };
            if(options && options.onSetFail){

              options.onSetFail(e);
            } else {

              qx.log.Logger.warn(q + value + G + targetObject + a + e);
            };
          };
        };
      },
      __bl : function(propertyNames){

        var arrayIndexValues = [];
        for(var i = 0;i < propertyNames.length;i++){

          var name = propertyNames[i];
          if(qx.lang.String.endsWith(name, A)){

            var arrayIndex = name.substring(name.indexOf(B) + 1, name.indexOf(A));
            if(name.indexOf(A) != name.length - 1){

              throw new Error(h + name + v);
            };
            if(arrayIndex !== M){

              if(arrayIndex == y || isNaN(parseInt(arrayIndex, 10))){

                throw new Error(E + name + v);
              };
            };
            if(name.indexOf(B) != 0){

              propertyNames[i] = name.substring(0, name.indexOf(B));
              arrayIndexValues[i] = y;
              arrayIndexValues[i + 1] = arrayIndex;
              propertyNames.splice(i + 1, 0, g);
              i++;
            } else {

              arrayIndexValues[i] = arrayIndex;
              propertyNames.splice(i, 1, g);
            };
          } else {

            arrayIndexValues[i] = y;
          };
        };
        return arrayIndexValues;
      },
      __bm : function(sourceObject, sourceEvent, targetObject, targetProperty, options, arrayIndex){

        {

          var eventType;
        };
        var bindListener = function(arrayIndex, e){

          if(arrayIndex !== y){

            if(arrayIndex === M){

              arrayIndex = sourceObject.length - 1;
            };
            var data = sourceObject.getItem(arrayIndex);
            if(data === undefined){

              qx.data.SingleValueBinding.__bh(targetObject, targetProperty);
            };
            var start = e.getData().start;
            var end = e.getData().end;
            if(arrayIndex < start || arrayIndex > end){

              return;
            };
          } else {

            var data = e.getData();
          };
          {
          };
          data = qx.data.SingleValueBinding.__bo(data, targetObject, targetProperty, options, sourceObject);
          {
          };
          try{

            if(data !== undefined){

              qx.data.SingleValueBinding.__bi(targetObject, targetProperty, data);
            } else {

              qx.data.SingleValueBinding.__bh(targetObject, targetProperty);
            };
            if(options && options.onUpdate){

              options.onUpdate(sourceObject, targetObject, data);
            };
          } catch(ex) {

            if(!(ex instanceof qx.core.ValidationError)){

              throw ex;
            };
            if(options && options.onSetFail){

              options.onSetFail(ex);
            } else {

              qx.log.Logger.warn(q + data + G + targetObject + a + ex);
            };
          };
        };
        if(!arrayIndex){

          arrayIndex = y;
        };
        bindListener = qx.lang.Function.bind(bindListener, sourceObject, arrayIndex);
        var id = sourceObject.addListener(sourceEvent, bindListener);
        return id;
      },
      __bn : function(id, sourceObject, sourceEvent, targetObject, targetProperty){

        if(this.__bd[sourceObject.toHashCode()] === undefined){

          this.__bd[sourceObject.toHashCode()] = [];
        };
        this.__bd[sourceObject.toHashCode()].push([id, sourceObject, sourceEvent, targetObject, targetProperty]);
      },
      __bo : function(value, targetObject, targetPropertyChain, options, sourceObject){

        if(options && options.converter){

          var model;
          if(targetObject.getModel){

            model = targetObject.getModel();
          };
          return options.converter(value, model, sourceObject, targetObject);
        } else {

          var target = this.__bj(targetObject, targetPropertyChain);
          var lastProperty = targetPropertyChain.substring(targetPropertyChain.lastIndexOf(C) + 1, targetPropertyChain.length);
          if(target == null){

            return value;
          };
          var propertieDefinition = qx.Class.getPropertyDefinition(target.constructor, lastProperty);
          var check = propertieDefinition == null ? y : propertieDefinition.check;
          return this.__bq(value, check);
        };
      },
      __bp : function(sourceObject, sourceProperty){

        var propertieDefinition = qx.Class.getPropertyDefinition(sourceObject.constructor, sourceProperty);
        if(propertieDefinition == null){

          return null;
        };
        return propertieDefinition.event;
      },
      __bq : function(data, targetCheck){

        var dataType = qx.lang.Type.getClass(data);
        if((dataType == I || dataType == w) && (targetCheck == k || targetCheck == D)){

          data = parseInt(data, 10);
        };
        if((dataType == b || dataType == I || dataType == K) && targetCheck == w){

          data = data + y;
        };
        if((dataType == I || dataType == w) && (targetCheck == I || targetCheck == z)){

          data = parseFloat(data);
        };
        return data;
      },
      removeBindingFromObject : function(sourceObject, id){

        if(id.type == d){

          for(var i = 0;i < id.sources.length;i++){

            if(id.sources[i]){

              id.sources[i].removeListenerById(id.listenerIds[i]);
            };
          };
          for(var i = 0;i < id.targets.length;i++){

            if(id.targets[i]){

              id.targets[i].removeListenerById(id.targetListenerIds[i]);
            };
          };
        } else {

          sourceObject.removeListenerById(id);
        };
        var bindings = this.__bd[sourceObject.toHashCode()];
        if(bindings != undefined){

          for(var i = 0;i < bindings.length;i++){

            if(bindings[i][0] == id){

              qx.lang.Array.remove(bindings, bindings[i]);
              return;
            };
          };
        };
        throw new Error(s);
      },
      removeAllBindingsForObject : function(object){

        {
        };
        var bindings = this.__bd[object.toHashCode()];
        if(bindings != undefined){

          for(var i = bindings.length - 1;i >= 0;i--){

            this.removeBindingFromObject(object, bindings[i][0]);
          };
        };
      },
      getAllBindingsForObject : function(object){

        if(this.__bd[object.toHashCode()] === undefined){

          this.__bd[object.toHashCode()] = [];
        };
        return this.__bd[object.toHashCode()];
      },
      removeAllBindings : function(){

        for(var hash in this.__bd){

          var object = qx.core.ObjectRegistry.fromHashCode(hash);
          if(object == null){

            delete this.__bd[hash];
            continue;
          };
          this.removeAllBindingsForObject(object);
        };
        this.__bd = {
        };
      },
      getAllBindings : function(){

        return this.__bd;
      },
      showBindingInLog : function(object, id){

        var binding;
        for(var i = 0;i < this.__bd[object.toHashCode()].length;i++){

          if(this.__bd[object.toHashCode()][i][0] == id){

            binding = this.__bd[object.toHashCode()][i];
            break;
          };
        };
        if(binding === undefined){

          var message = H;
        } else {

          var message = x + binding[1] + F + binding[2] + f + binding[3] + F + binding[4] + J;
        };
        qx.log.Logger.debug(message);
      },
      showAllBindingsInLog : function(){

        for(var hash in this.__bd){

          var object = qx.core.ObjectRegistry.fromHashCode(hash);
          for(var i = 0;i < this.__bd[hash].length;i++){

            this.showBindingInLog(object, this.__bd[hash][i][0]);
          };
        };
      }
    }
  });
})();
(function(){

  var a = "-",b = "]",c = '\\u',d = "undefined",e = "",f = '\\$1',g = "0041-005A0061-007A00AA00B500BA00C0-00D600D8-00F600F8-02C102C6-02D102E0-02E402EC02EE0370-037403760377037A-037D03860388-038A038C038E-03A103A3-03F503F7-0481048A-05250531-055605590561-058705D0-05EA05F0-05F20621-064A066E066F0671-06D306D506E506E606EE06EF06FA-06FC06FF07100712-072F074D-07A507B107CA-07EA07F407F507FA0800-0815081A082408280904-0939093D09500958-0961097109720979-097F0985-098C098F09900993-09A809AA-09B009B209B6-09B909BD09CE09DC09DD09DF-09E109F009F10A05-0A0A0A0F0A100A13-0A280A2A-0A300A320A330A350A360A380A390A59-0A5C0A5E0A72-0A740A85-0A8D0A8F-0A910A93-0AA80AAA-0AB00AB20AB30AB5-0AB90ABD0AD00AE00AE10B05-0B0C0B0F0B100B13-0B280B2A-0B300B320B330B35-0B390B3D0B5C0B5D0B5F-0B610B710B830B85-0B8A0B8E-0B900B92-0B950B990B9A0B9C0B9E0B9F0BA30BA40BA8-0BAA0BAE-0BB90BD00C05-0C0C0C0E-0C100C12-0C280C2A-0C330C35-0C390C3D0C580C590C600C610C85-0C8C0C8E-0C900C92-0CA80CAA-0CB30CB5-0CB90CBD0CDE0CE00CE10D05-0D0C0D0E-0D100D12-0D280D2A-0D390D3D0D600D610D7A-0D7F0D85-0D960D9A-0DB10DB3-0DBB0DBD0DC0-0DC60E01-0E300E320E330E40-0E460E810E820E840E870E880E8A0E8D0E94-0E970E99-0E9F0EA1-0EA30EA50EA70EAA0EAB0EAD-0EB00EB20EB30EBD0EC0-0EC40EC60EDC0EDD0F000F40-0F470F49-0F6C0F88-0F8B1000-102A103F1050-1055105A-105D106110651066106E-10701075-1081108E10A0-10C510D0-10FA10FC1100-1248124A-124D1250-12561258125A-125D1260-1288128A-128D1290-12B012B2-12B512B8-12BE12C012C2-12C512C8-12D612D8-13101312-13151318-135A1380-138F13A0-13F41401-166C166F-167F1681-169A16A0-16EA1700-170C170E-17111720-17311740-17511760-176C176E-17701780-17B317D717DC1820-18771880-18A818AA18B0-18F51900-191C1950-196D1970-19741980-19AB19C1-19C71A00-1A161A20-1A541AA71B05-1B331B45-1B4B1B83-1BA01BAE1BAF1C00-1C231C4D-1C4F1C5A-1C7D1CE9-1CEC1CEE-1CF11D00-1DBF1E00-1F151F18-1F1D1F20-1F451F48-1F4D1F50-1F571F591F5B1F5D1F5F-1F7D1F80-1FB41FB6-1FBC1FBE1FC2-1FC41FC6-1FCC1FD0-1FD31FD6-1FDB1FE0-1FEC1FF2-1FF41FF6-1FFC2071207F2090-209421022107210A-211321152119-211D212421262128212A-212D212F-2139213C-213F2145-2149214E218321842C00-2C2E2C30-2C5E2C60-2CE42CEB-2CEE2D00-2D252D30-2D652D6F2D80-2D962DA0-2DA62DA8-2DAE2DB0-2DB62DB8-2DBE2DC0-2DC62DC8-2DCE2DD0-2DD62DD8-2DDE2E2F300530063031-3035303B303C3041-3096309D-309F30A1-30FA30FC-30FF3105-312D3131-318E31A0-31B731F0-31FF3400-4DB54E00-9FCBA000-A48CA4D0-A4FDA500-A60CA610-A61FA62AA62BA640-A65FA662-A66EA67F-A697A6A0-A6E5A717-A71FA722-A788A78BA78CA7FB-A801A803-A805A807-A80AA80C-A822A840-A873A882-A8B3A8F2-A8F7A8FBA90A-A925A930-A946A960-A97CA984-A9B2A9CFAA00-AA28AA40-AA42AA44-AA4BAA60-AA76AA7AAA80-AAAFAAB1AAB5AAB6AAB9-AABDAAC0AAC2AADB-AADDABC0-ABE2AC00-D7A3D7B0-D7C6D7CB-D7FBF900-FA2DFA30-FA6DFA70-FAD9FB00-FB06FB13-FB17FB1DFB1F-FB28FB2A-FB36FB38-FB3CFB3EFB40FB41FB43FB44FB46-FBB1FBD3-FD3DFD50-FD8FFD92-FDC7FDF0-FDFBFE70-FE74FE76-FEFCFF21-FF3AFF41-FF5AFF66-FFBEFFC2-FFC7FFCA-FFCFFFD2-FFD7FFDA-FFDC",h = "\\\\",j = '-',k = "g",l = "\\\"",m = "qx.lang.String",n = "(^|[^",o = "0",p = "%",q = '"',r = ' ',s = '\n',t = "])[";
  qx.Bootstrap.define(m, {
    statics : {
      __br : g,
      __bs : null,
      __bt : {
      },
      camelCase : function(str){

        var result = this.__bt[str];
        if(!result){

          result = str.replace(/\-([a-z])/g, function(match, chr){

            return chr.toUpperCase();
          });
          if(str.indexOf(a) >= 0){

            this.__bt[str] = result;
          };
        };
        return result;
      },
      hyphenate : function(str){

        var result = this.__bt[str];
        if(!result){

          result = str.replace(/[A-Z]/g, function(match){

            return (j + match.charAt(0).toLowerCase());
          });
          if(str.indexOf(a) == -1){

            this.__bt[str] = result;
          };
        };
        return result;
      },
      capitalize : function(str){

        if(this.__bs === null){

          var unicodeEscapePrefix = c;
          this.__bs = new RegExp(n + this.__br.replace(/[0-9A-F]{4}/g, function(match){

            return unicodeEscapePrefix + match;
          }) + t + this.__br.replace(/[0-9A-F]{4}/g, function(match){

            return unicodeEscapePrefix + match;
          }) + b, k);
        };
        return str.replace(this.__bs, function(match){

          return match.toUpperCase();
        });
      },
      clean : function(str){

        return str.replace(/\s+/g, r).trim();
      },
      trimLeft : function(str){

        return str.replace(/^\s+/, e);
      },
      trimRight : function(str){

        return str.replace(/\s+$/, e);
      },
      startsWith : function(fullstr, substr){

        return fullstr.indexOf(substr) === 0;
      },
      endsWith : function(fullstr, substr){

        return fullstr.substring(fullstr.length - substr.length, fullstr.length) === substr;
      },
      repeat : function(str, times){

        return str.length > 0 ? new Array(times + 1).join(str) : e;
      },
      pad : function(str, length, ch){

        var padLength = length - str.length;
        if(padLength > 0){

          if(typeof ch === d){

            ch = o;
          };
          return this.repeat(ch, padLength) + str;
        } else {

          return str;
        };
      },
      firstUp : qx.Bootstrap.firstUp,
      firstLow : qx.Bootstrap.firstLow,
      contains : function(str, substring){

        return str.indexOf(substring) != -1;
      },
      format : function(pattern, args){

        var str = pattern;
        var i = args.length;
        while(i--){

          str = str.replace(new RegExp(p + (i + 1), k), args[i] + e);
        };
        return str;
      },
      escapeRegexpChars : function(str){

        return str.replace(/([.*+?^${}()|[\]\/\\])/g, f);
      },
      toArray : function(str){

        return str.split(/\B|\b/g);
      },
      stripTags : function(str){

        return str.replace(/<\/?[^>]+>/gi, e);
      },
      stripScripts : function(str, exec){

        var scripts = e;
        var text = str.replace(/<script[^>]*>([\s\S]*?)<\/script>/gi, function(){

          scripts += arguments[1] + s;
          return e;
        });
        if(exec === true){

          qx.lang.Function.globalEval(scripts);
        };
        return text;
      },
      quote : function(str){

        return q + str.replace(/\\/g, h).replace(/\"/g, l) + q;
      }
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "[object Array]",d = "qx.lang.Array",e = "Cannot clean-up map entry doneObjects[",f = "]",g = "qx",h = "number",j = "][",k = "string";
  qx.Bootstrap.define(d, {
    statics : {
      cast : function(object, constructor, offset){

        if(object.constructor === constructor){

          return object;
        };
        if(qx.data && qx.data.IListData){

          if(qx.Class && qx.Class.hasInterface(object, qx.data.IListData)){

            var object = object.toArray();
          };
        };
        var ret = new constructor;
        if((qx.core.Environment.get(b) == a)){

          if(object.item){

            for(var i = offset || 0,l = object.length;i < l;i++){

              ret.push(object[i]);
            };
            return ret;
          };
        };
        if(Object.prototype.toString.call(object) === c && offset == null){

          ret.push.apply(ret, object);
        } else {

          ret.push.apply(ret, Array.prototype.slice.call(object, offset || 0));
        };
        return ret;
      },
      fromArguments : function(args, offset){

        return Array.prototype.slice.call(args, offset || 0);
      },
      fromCollection : function(coll){

        if((qx.core.Environment.get(b) == a)){

          if(coll.item){

            var arr = [];
            for(var i = 0,l = coll.length;i < l;i++){

              arr[i] = coll[i];
            };
            return arr;
          };
        };
        return Array.prototype.slice.call(coll, 0);
      },
      fromShortHand : function(input){

        var len = input.length;
        var result = qx.lang.Array.clone(input);
        switch(len){case 1:
        result[1] = result[2] = result[3] = result[0];
        break;case 2:
        result[2] = result[0];case 3:
        result[3] = result[1];};
        return result;
      },
      clone : function(arr){

        return arr.concat();
      },
      insertAt : function(arr, obj, i){

        arr.splice(i, 0, obj);
        return arr;
      },
      insertBefore : function(arr, obj, obj2){

        var i = arr.indexOf(obj2);
        if(i == -1){

          arr.push(obj);
        } else {

          arr.splice(i, 0, obj);
        };
        return arr;
      },
      insertAfter : function(arr, obj, obj2){

        var i = arr.indexOf(obj2);
        if(i == -1 || i == (arr.length - 1)){

          arr.push(obj);
        } else {

          arr.splice(i + 1, 0, obj);
        };
        return arr;
      },
      removeAt : function(arr, i){

        return arr.splice(i, 1)[0];
      },
      removeAll : function(arr){

        arr.length = 0;
        return this;
      },
      append : function(arr1, arr2){

        {
        };
        Array.prototype.push.apply(arr1, arr2);
        return arr1;
      },
      exclude : function(arr1, arr2){

        {
        };
        for(var i = 0,il = arr2.length,index;i < il;i++){

          index = arr1.indexOf(arr2[i]);
          if(index != -1){

            arr1.splice(index, 1);
          };
        };
        return arr1;
      },
      remove : function(arr, obj){

        var i = arr.indexOf(obj);
        if(i != -1){

          arr.splice(i, 1);
          return obj;
        };
      },
      contains : function(arr, obj){

        return arr.indexOf(obj) !== -1;
      },
      equals : function(arr1, arr2){

        var length = arr1.length;
        if(length !== arr2.length){

          return false;
        };
        for(var i = 0;i < length;i++){

          if(arr1[i] !== arr2[i]){

            return false;
          };
        };
        return true;
      },
      sum : function(arr){

        var result = 0;
        for(var i = 0,l = arr.length;i < l;i++){

          result += arr[i];
        };
        return result;
      },
      max : function(arr){

        {
        };
        var i,len = arr.length,result = arr[0];
        for(i = 1;i < len;i++){

          if(arr[i] > result){

            result = arr[i];
          };
        };
        return result === undefined ? null : result;
      },
      min : function(arr){

        {
        };
        var i,len = arr.length,result = arr[0];
        for(i = 1;i < len;i++){

          if(arr[i] < result){

            result = arr[i];
          };
        };
        return result === undefined ? null : result;
      },
      unique : function(arr){

        var ret = [],doneStrings = {
        },doneNumbers = {
        },doneObjects = {
        };
        var value,count = 0;
        var key = g + Date.now();
        var hasNull = false,hasFalse = false,hasTrue = false;
        for(var i = 0,len = arr.length;i < len;i++){

          value = arr[i];
          if(value === null){

            if(!hasNull){

              hasNull = true;
              ret.push(value);
            };
          } else if(value === undefined){
          } else if(value === false){

            if(!hasFalse){

              hasFalse = true;
              ret.push(value);
            };
          } else if(value === true){

            if(!hasTrue){

              hasTrue = true;
              ret.push(value);
            };
          } else if(typeof value === k){

            if(!doneStrings[value]){

              doneStrings[value] = 1;
              ret.push(value);
            };
          } else if(typeof value === h){

            if(!doneNumbers[value]){

              doneNumbers[value] = 1;
              ret.push(value);
            };
          } else {

            var hash = value[key];
            if(hash == null){

              hash = value[key] = count++;
            };
            if(!doneObjects[hash]){

              doneObjects[hash] = value;
              ret.push(value);
            };
          };;;;;
        };
        for(var hash in doneObjects){

          try{

            delete doneObjects[hash][key];
          } catch(ex) {

            try{

              doneObjects[hash][key] = null;
            } catch(ex1) {

              throw new Error(e + hash + j + key + f);
            };
          };
        };
        return ret;
      }
    }
  });
})();
(function(){

  var a = "[object Opera]",b = "function",c = "[^\\.0-9]",d = "4.0",e = "gecko",f = "1.9.0.0",g = "Version/",h = "9.0",i = "8.0",j = "Gecko",k = "Maple",l = "AppleWebKit/",m = "Trident",n = "Unsupported client: ",o = "",p = "opera",q = "engine.version",r = "! Assumed gecko version 1.9.0.0 (Firefox 3.0).",s = "mshtml",t = "engine.name",u = "webkit",v = "5.0",w = ".",x = "qx.bom.client.Engine";
  qx.Bootstrap.define(x, {
    statics : {
      getVersion : function(){

        var agent = window.navigator.userAgent;
        var version = o;
        if(qx.bom.client.Engine.__bu()){

          if(/Opera[\s\/]([0-9]+)\.([0-9])([0-9]*)/.test(agent)){

            if(agent.indexOf(g) != -1){

              var match = agent.match(/Version\/(\d+)\.(\d+)/);
              version = match[1] + w + match[2].charAt(0) + w + match[2].substring(1, match[2].length);
            } else {

              version = RegExp.$1 + w + RegExp.$2;
              if(RegExp.$3 != o){

                version += w + RegExp.$3;
              };
            };
          };
        } else if(qx.bom.client.Engine.__bv()){

          if(/AppleWebKit\/([^ ]+)/.test(agent)){

            version = RegExp.$1;
            var invalidCharacter = RegExp(c).exec(version);
            if(invalidCharacter){

              version = version.slice(0, invalidCharacter.index);
            };
          };
        } else if(qx.bom.client.Engine.__bx() || qx.bom.client.Engine.__bw()){

          if(/rv\:([^\);]+)(\)|;)/.test(agent)){

            version = RegExp.$1;
          };
        } else if(qx.bom.client.Engine.__by()){

          var isTrident = /Trident\/([^\);]+)(\)|;)/.test(agent);
          if(/MSIE\s+([^\);]+)(\)|;)/.test(agent)){

            version = RegExp.$1;
            if(version < 8 && isTrident){

              if(RegExp.$1 == d){

                version = i;
              } else if(RegExp.$1 == v){

                version = h;
              };
            };
          } else if(isTrident){

            var match = /\brv\:(\d+?\.\d+?)\b/.exec(agent);
            if(match){

              version = match[1];
            };
          };
        } else {

          var failFunction = window.qxFail;
          if(failFunction && typeof failFunction === b){

            version = failFunction().FULLVERSION;
          } else {

            version = f;
            qx.Bootstrap.warn(n + agent + r);
          };
        };;;
        return version;
      },
      getName : function(){

        var name;
        if(qx.bom.client.Engine.__bu()){

          name = p;
        } else if(qx.bom.client.Engine.__bv()){

          name = u;
        } else if(qx.bom.client.Engine.__bx() || qx.bom.client.Engine.__bw()){

          name = e;
        } else if(qx.bom.client.Engine.__by()){

          name = s;
        } else {

          var failFunction = window.qxFail;
          if(failFunction && typeof failFunction === b){

            name = failFunction().NAME;
          } else {

            name = e;
            qx.Bootstrap.warn(n + window.navigator.userAgent + r);
          };
        };;;
        return name;
      },
      __bu : function(){

        return window.opera && Object.prototype.toString.call(window.opera) == a;
      },
      __bv : function(){

        return window.navigator.userAgent.indexOf(l) != -1;
      },
      __bw : function(){

        return window.navigator.userAgent.indexOf(k) != -1;
      },
      __bx : function(){

        return window.controllers && window.navigator.product === j && window.navigator.userAgent.indexOf(k) == -1 && window.navigator.userAgent.indexOf(m) == -1;
      },
      __by : function(){

        return window.navigator.cpuClass && (/MSIE\s+([^\);]+)(\)|;)/.test(window.navigator.userAgent) || /Trident\/\d+?\.\d+?/.test(window.navigator.userAgent));
      }
    },
    defer : function(statics){

      qx.core.Environment.add(q, statics.getVersion);
      qx.core.Environment.add(t, statics.getName);
    }
  });
})();
(function(){

  var a = 'anonymous()',b = "()",c = "qx.globalErrorHandling",d = "qx.lang.Function",e = ".",f = ".prototype.",g = ".constructor()";
  qx.Bootstrap.define(d, {
    statics : {
      getCaller : function(args){

        return args.caller ? args.caller.callee : args.callee.caller;
      },
      getName : function(fcn){

        if(fcn.displayName){

          return fcn.displayName;
        };
        if(fcn.$$original || fcn.wrapper || fcn.classname){

          return fcn.classname + g;
        };
        if(fcn.$$mixin){

          for(var key in fcn.$$mixin.$$members){

            if(fcn.$$mixin.$$members[key] == fcn){

              return fcn.$$mixin.name + f + key + b;
            };
          };
          for(var key in fcn.$$mixin){

            if(fcn.$$mixin[key] == fcn){

              return fcn.$$mixin.name + e + key + b;
            };
          };
        };
        if(fcn.self){

          var clazz = fcn.self.constructor;
          if(clazz){

            for(var key in clazz.prototype){

              if(clazz.prototype[key] == fcn){

                return clazz.classname + f + key + b;
              };
            };
            for(var key in clazz){

              if(clazz[key] == fcn){

                return clazz.classname + e + key + b;
              };
            };
          };
        };
        var fcnReResult = fcn.toString().match(/function\s*(\w*)\s*\(.*/);
        if(fcnReResult && fcnReResult.length >= 1 && fcnReResult[1]){

          return fcnReResult[1] + b;
        };
        return a;
      },
      globalEval : function(data){

        if(window.execScript){

          return window.execScript(data);
        } else {

          return eval.call(window, data);
        };
      },
      create : function(func, options){

        {
        };
        if(!options){

          return func;
        };
        if(!(options.self || options.args || options.delay != null || options.periodical != null || options.attempt)){

          return func;
        };
        return function(event){

          {
          };
          var args = qx.lang.Array.fromArguments(arguments);
          if(options.args){

            args = options.args.concat(args);
          };
          if(options.delay || options.periodical){

            var returns = function(){

              return func.apply(options.self || this, args);
            };
            if(qx.core.Environment.get(c)){

              returns = qx.event.GlobalError.observeMethod(returns);
            };
            if(options.delay){

              return window.setTimeout(returns, options.delay);
            };
            if(options.periodical){

              return window.setInterval(returns, options.periodical);
            };
          } else if(options.attempt){

            var ret = false;
            try{

              ret = func.apply(options.self || this, args);
            } catch(ex) {
            };
            return ret;
          } else {

            return func.apply(options.self || this, args);
          };
        };
      },
      bind : function(func, self, varargs){

        return this.create(func, {
          self : self,
          args : arguments.length > 2 ? qx.lang.Array.fromArguments(arguments, 2) : null
        });
      },
      curry : function(func, varargs){

        return this.create(func, {
          args : arguments.length > 1 ? qx.lang.Array.fromArguments(arguments, 1) : null
        });
      },
      listener : function(func, self, varargs){

        if(arguments.length < 3){

          return function(event){

            return func.call(self || this, event || window.event);
          };
        } else {

          var optargs = qx.lang.Array.fromArguments(arguments, 2);
          return function(event){

            var args = [event || window.event];
            args.push.apply(args, optargs);
            func.apply(self || this, args);
          };
        };
      },
      attempt : function(func, self, varargs){

        return this.create(func, {
          self : self,
          attempt : true,
          args : arguments.length > 2 ? qx.lang.Array.fromArguments(arguments, 2) : null
        })();
      },
      delay : function(func, delay, self, varargs){

        return this.create(func, {
          delay : delay,
          self : self,
          args : arguments.length > 3 ? qx.lang.Array.fromArguments(arguments, 3) : null
        })();
      },
      periodical : function(func, interval, self, varargs){

        return this.create(func, {
          periodical : interval,
          self : self,
          args : arguments.length > 3 ? qx.lang.Array.fromArguments(arguments, 3) : null
        })();
      }
    }
  });
})();
(function(){

  var a = "qx.globalErrorHandling",b = "qx.event.GlobalError";
  qx.Bootstrap.define(b, {
    statics : {
      __bz : null,
      __bA : null,
      __bB : null,
      __bC : function(){

        if(qx.core && qx.core.Environment){

          return qx.core.Environment.get(a);
        } else {

          return !!qx.Bootstrap.getEnvironmentSetting(a);
        };
      },
      setErrorHandler : function(callback, context){

        this.__bz = callback || null;
        this.__bB = context || window;
        if(this.__bC()){

          if(callback && window.onerror){

            var wrappedHandler = qx.Bootstrap.bind(this.__bD, this);
            if(this.__bA == null){

              this.__bA = window.onerror;
            };
            var self = this;
            window.onerror = function(msg, uri, lineNumber){

              self.__bA(msg, uri, lineNumber);
              wrappedHandler(msg, uri, lineNumber);
            };
          };
          if(callback && !window.onerror){

            window.onerror = qx.Bootstrap.bind(this.__bD, this);
          };
          if(this.__bz == null){

            if(this.__bA != null){

              window.onerror = this.__bA;
              this.__bA = null;
            } else {

              window.onerror = null;
            };
          };
        };
      },
      __bD : function(msg, uri, lineNumber){

        if(this.__bz){

          this.handleError(new qx.core.WindowError(msg, uri, lineNumber));
        };
      },
      observeMethod : function(method){

        if(this.__bC()){

          var self = this;
          return function(){

            if(!self.__bz){

              return method.apply(this, arguments);
            };
            try{

              return method.apply(this, arguments);
            } catch(ex) {

              self.handleError(new qx.core.GlobalError(ex, arguments));
            };
          };
        } else {

          return method;
        };
      },
      handleError : function(ex){

        if(this.__bz){

          this.__bz.call(this.__bB, ex);
        };
      }
    },
    defer : function(statics){

      if(qx.core && qx.core.Environment){

        qx.core.Environment.add(a, true);
      } else {

        qx.Bootstrap.setEnvironmentSetting(a, true);
      };
      statics.setErrorHandler(null, null);
    }
  });
})();
(function(){

  var a = "",b = "qx.core.WindowError";
  qx.Bootstrap.define(b, {
    extend : Error,
    construct : function(failMessage, uri, lineNumber){

      var inst = Error.call(this, failMessage);
      if(inst.stack){

        this.stack = inst.stack;
      };
      if(inst.stacktrace){

        this.stacktrace = inst.stacktrace;
      };
      this.__bE = failMessage;
      this.__bF = uri || a;
      this.__bG = lineNumber === undefined ? -1 : lineNumber;
    },
    members : {
      __bE : null,
      __bF : null,
      __bG : null,
      toString : function(){

        return this.__bE;
      },
      getUri : function(){

        return this.__bF;
      },
      getLineNumber : function(){

        return this.__bG;
      }
    }
  });
})();
(function(){

  var a = "GlobalError: ",b = "qx.core.GlobalError";
  qx.Bootstrap.define(b, {
    extend : Error,
    construct : function(exc, args){

      if(qx.Bootstrap.DEBUG){

        qx.core.Assert.assertNotUndefined(exc);
      };
      this.__bE = a + (exc && exc.message ? exc.message : exc);
      var inst = Error.call(this, this.__bE);
      if(inst.stack){

        this.stack = inst.stack;
      };
      if(inst.stacktrace){

        this.stacktrace = inst.stacktrace;
      };
      this.__bH = args;
      this.__bI = exc;
    },
    members : {
      __bI : null,
      __bH : null,
      __bE : null,
      toString : function(){

        return this.__bE;
      },
      getArguments : function(){

        return this.__bH;
      },
      getSourceException : function(){

        return this.__bI;
      }
    },
    destruct : function(){

      this.__bI = null;
      this.__bH = null;
      this.__bE = null;
    }
  });
})();
(function(){

  var a = "qx.lang.Type",b = "Error",c = "RegExp",d = "Date",e = "Number",f = "Boolean";
  qx.Bootstrap.define(a, {
    statics : {
      getClass : qx.Bootstrap.getClass,
      isString : qx.Bootstrap.isString,
      isArray : qx.Bootstrap.isArray,
      isObject : qx.Bootstrap.isObject,
      isFunction : qx.Bootstrap.isFunction,
      isRegExp : function(value){

        return this.getClass(value) == c;
      },
      isNumber : function(value){

        return (value !== null && (this.getClass(value) == e || value instanceof Number));
      },
      isBoolean : function(value){

        return (value !== null && (this.getClass(value) == f || value instanceof Boolean));
      },
      isDate : function(value){

        return (value !== null && (this.getClass(value) == d || value instanceof Date));
      },
      isError : function(value){

        return (value !== null && (this.getClass(value) == b || value instanceof Error));
      }
    }
  });
})();
(function(){

  var a = " != ",b = "qx.core.Object",c = "Expected value to be an array but found ",d = "' (rgb(",f = ") was fired.",g = "Expected value to be an integer >= 0 but found ",h = "' to be not equal with '",j = "' to '",k = "Expected object '",m = "Called assertTrue with '",n = "Expected value to be a map but found ",o = "The function did not raise an exception!",p = "Expected value to be undefined but found ",q = "Expected value to be a DOM element but found  '",r = "Expected value to be a regular expression but found ",s = "' to implement the interface '",t = "Expected value to be null but found ",u = "Invalid argument 'type'",v = "Called assert with 'false'",w = "Assertion error! ",x = "'",y = "null",z = "' but found '",A = "'undefined'",B = ",",C = "' must must be a key of the map '",D = "Expected '",E = "The String '",F = "Expected value to be a string but found ",G = "Event (",H = "Expected value to be the CSS color '",I = "!",J = "Expected value not to be undefined but found undefined!",K = "qx.util.ColorUtil",L = ": ",M = "The raised exception does not have the expected type! ",N = ") not fired.",O = "'!",P = "qx.core.Assert",Q = "",R = "Expected value to be typeof object but found ",S = "' but found ",T = "' (identical) but found '",U = "' must have any of the values defined in the array '",V = "Expected value to be a number but found ",W = "Called assertFalse with '",X = "qx.ui.core.Widget",Y = "]",ba = "Expected value to be a qooxdoo object but found ",bb = "' arguments.",bc = "Expected value '%1' to be in the range '%2'..'%3'!",bd = "Array[",be = "' does not match the regular expression '",bf = "' to be not identical with '",bg = "Expected [",bh = "' arguments but found '",bi = "', which cannot be converted to a CSS color!",bj = ", ",bk = "qx.core.AssertionError",bl = "Expected value to be a boolean but found ",bm = "Expected value not to be null but found null!",bn = "))!",bo = "Expected value to be a qooxdoo widget but found ",bp = "The value '",bq = "Expected value to be typeof '",br = "\n Stack trace: \n",bs = "Expected value to be typeof function but found ",bt = "Expected value to be an integer but found ",bu = "Called fail().",bv = "The parameter 're' must be a string or a regular expression.",bw = ")), but found value '",bx = "qx.util.ColorUtil not available! Your code must have a dependency on 'qx.util.ColorUtil'",by = "Expected value to be a number >= 0 but found ",bz = "Expected value to be instanceof '",bA = "], but found [",bB = "Wrong number of arguments given. Expected '",bC = "object";
  qx.Class.define(P, {
    statics : {
      __bJ : true,
      __bK : function(comment, msgvarargs){

        var msg = Q;
        for(var i = 1,l = arguments.length;i < l;i++){

          msg = msg + this.__bL(arguments[i] === undefined ? A : arguments[i]);
        };
        var fullComment = Q;
        if(msg){

          fullComment = comment + L + msg;
        } else {

          fullComment = comment;
        };
        var errorMsg = w + fullComment;
        if(qx.Class.isDefined(bk)){

          var err = new qx.core.AssertionError(comment, msg);
          if(this.__bJ){

            qx.Bootstrap.error(errorMsg + br + err.getStackTrace());
          };
          throw err;
        } else {

          if(this.__bJ){

            qx.Bootstrap.error(errorMsg);
          };
          throw new Error(errorMsg);
        };
      },
      __bL : function(value){

        var stringValue;
        if(value === null){

          stringValue = y;
        } else if(qx.lang.Type.isArray(value) && value.length > 10){

          stringValue = bd + value.length + Y;
        } else if((value instanceof Object) && (value.toString == null)){

          stringValue = qx.lang.Json.stringify(value, null, 2);
        } else {

          try{

            stringValue = value.toString();
          } catch(e) {

            stringValue = Q;
          };
        };;
        return stringValue;
      },
      assert : function(condition, msg){

        condition == true || this.__bK(msg || Q, v);
      },
      fail : function(msg, compact){

        var msgvarargs = compact ? Q : bu;
        this.__bK(msg || Q, msgvarargs);
      },
      assertTrue : function(value, msg){

        (value === true) || this.__bK(msg || Q, m, value, x);
      },
      assertFalse : function(value, msg){

        (value === false) || this.__bK(msg || Q, W, value, x);
      },
      assertEquals : function(expected, found, msg){

        expected == found || this.__bK(msg || Q, D, expected, z, found, O);
      },
      assertNotEquals : function(expected, found, msg){

        expected != found || this.__bK(msg || Q, D, expected, h, found, O);
      },
      assertIdentical : function(expected, found, msg){

        expected === found || this.__bK(msg || Q, D, expected, T, found, O);
      },
      assertNotIdentical : function(expected, found, msg){

        expected !== found || this.__bK(msg || Q, D, expected, bf, found, O);
      },
      assertNotUndefined : function(value, msg){

        value !== undefined || this.__bK(msg || Q, J);
      },
      assertUndefined : function(value, msg){

        value === undefined || this.__bK(msg || Q, p, value, I);
      },
      assertNotNull : function(value, msg){

        value !== null || this.__bK(msg || Q, bm);
      },
      assertNull : function(value, msg){

        value === null || this.__bK(msg || Q, t, value, I);
      },
      assertJsonEquals : function(expected, found, msg){

        this.assertEquals(qx.lang.Json.stringify(expected), qx.lang.Json.stringify(found), msg);
      },
      assertMatch : function(str, re, msg){

        this.assertString(str);
        this.assert(qx.lang.Type.isRegExp(re) || qx.lang.Type.isString(re), bv);
        str.search(re) >= 0 || this.__bK(msg || Q, E, str, be, re.toString(), O);
      },
      assertArgumentsCount : function(args, minCount, maxCount, msg){

        var argCount = args.length;
        (argCount >= minCount && argCount <= maxCount) || this.__bK(msg || Q, bB, minCount, j, maxCount, bh, argCount, bb);
      },
      assertEventFired : function(obj, event, invokeFunc, listenerFunc, msg){

        var called = false;
        var listener = function(e){

          if(listenerFunc){

            listenerFunc.call(obj, e);
          };
          called = true;
        };
        var id;
        try{

          id = obj.addListener(event, listener, obj);
          invokeFunc.call(obj);
        } catch(ex) {

          throw ex;
        }finally{

          try{

            obj.removeListenerById(id);
          } catch(ex) {
          };
        };
        called === true || this.__bK(msg || Q, G, event, N);
      },
      assertEventNotFired : function(obj, event, invokeFunc, msg){

        var called = false;
        var listener = function(e){

          called = true;
        };
        var id = obj.addListener(event, listener, obj);
        invokeFunc.call();
        called === false || this.__bK(msg || Q, G, event, f);
        obj.removeListenerById(id);
      },
      assertException : function(callback, exception, re, msg){

        var exception = exception || Error;
        var error;
        try{

          this.__bJ = false;
          callback();
        } catch(ex) {

          error = ex;
        }finally{

          this.__bJ = true;
        };
        if(error == null){

          this.__bK(msg || Q, o);
        };
        error instanceof exception || this.__bK(msg || Q, M, exception, a, error);
        if(re){

          this.assertMatch(error.toString(), re, msg);
        };
      },
      assertInArray : function(value, array, msg){

        array.indexOf(value) !== -1 || this.__bK(msg || Q, bp, value, U, array, x);
      },
      assertArrayEquals : function(expected, found, msg){

        this.assertArray(expected, msg);
        this.assertArray(found, msg);
        msg = msg || bg + expected.join(bj) + bA + found.join(bj) + Y;
        if(expected.length !== found.length){

          this.fail(msg, true);
        };
        for(var i = 0;i < expected.length;i++){

          if(expected[i] !== found[i]){

            this.fail(msg, true);
          };
        };
      },
      assertKeyInMap : function(value, map, msg){

        map[value] !== undefined || this.__bK(msg || Q, bp, value, C, map, x);
      },
      assertFunction : function(value, msg){

        qx.lang.Type.isFunction(value) || this.__bK(msg || Q, bs, value, I);
      },
      assertString : function(value, msg){

        qx.lang.Type.isString(value) || this.__bK(msg || Q, F, value, I);
      },
      assertBoolean : function(value, msg){

        qx.lang.Type.isBoolean(value) || this.__bK(msg || Q, bl, value, I);
      },
      assertNumber : function(value, msg){

        (qx.lang.Type.isNumber(value) && isFinite(value)) || this.__bK(msg || Q, V, value, I);
      },
      assertPositiveNumber : function(value, msg){

        (qx.lang.Type.isNumber(value) && isFinite(value) && value >= 0) || this.__bK(msg || Q, by, value, I);
      },
      assertInteger : function(value, msg){

        (qx.lang.Type.isNumber(value) && isFinite(value) && value % 1 === 0) || this.__bK(msg || Q, bt, value, I);
      },
      assertPositiveInteger : function(value, msg){

        var condition = (qx.lang.Type.isNumber(value) && isFinite(value) && value % 1 === 0 && value >= 0);
        condition || this.__bK(msg || Q, g, value, I);
      },
      assertInRange : function(value, min, max, msg){

        (value >= min && value <= max) || this.__bK(msg || Q, qx.lang.String.format(bc, [value, min, max]));
      },
      assertObject : function(value, msg){

        var condition = value !== null && (qx.lang.Type.isObject(value) || typeof value === bC);
        condition || this.__bK(msg || Q, R, (value), I);
      },
      assertArray : function(value, msg){

        qx.lang.Type.isArray(value) || this.__bK(msg || Q, c, value, I);
      },
      assertMap : function(value, msg){

        qx.lang.Type.isObject(value) || this.__bK(msg || Q, n, value, I);
      },
      assertRegExp : function(value, msg){

        qx.lang.Type.isRegExp(value) || this.__bK(msg || Q, r, value, I);
      },
      assertType : function(value, type, msg){

        this.assertString(type, u);
        typeof (value) === type || this.__bK(msg || Q, bq, type, S, value, I);
      },
      assertInstance : function(value, clazz, msg){

        var className = clazz.classname || clazz + Q;
        value instanceof clazz || this.__bK(msg || Q, bz, className, S, value, I);
      },
      assertInterface : function(value, iface, msg){

        qx.Class.implementsInterface(value, iface) || this.__bK(msg || Q, k, value, s, iface, O);
      },
      assertCssColor : function(expected, value, msg){

        var ColorUtil = qx.Class.getByName(K);
        if(!ColorUtil){

          throw new Error(bx);
        };
        var expectedRgb = ColorUtil.stringToRgb(expected);
        try{

          var valueRgb = ColorUtil.stringToRgb(value);
        } catch(ex) {

          this.__bK(msg || Q, H, expected, d, expectedRgb.join(B), bw, value, bi);
        };
        var condition = expectedRgb[0] == valueRgb[0] && expectedRgb[1] == valueRgb[1] && expectedRgb[2] == valueRgb[2];
        condition || this.__bK(msg || Q, H, expectedRgb, d, expectedRgb.join(B), bw, value, d, valueRgb.join(B), bn);
      },
      assertElement : function(value, msg){

        !!(value && value.nodeType === 1) || this.__bK(msg || Q, q, value, O);
      },
      assertQxObject : function(value, msg){

        this.__bM(value, b) || this.__bK(msg || Q, ba, value, I);
      },
      assertQxWidget : function(value, msg){

        this.__bM(value, X) || this.__bK(msg || Q, bo, value, I);
      },
      __bM : function(object, classname){

        if(!object){

          return false;
        };
        var clazz = object.constructor;
        while(clazz){

          if(clazz.classname === classname){

            return true;
          };
          clazz = clazz.superclass;
        };
        return false;
      }
    }
  });
})();
(function(){

  var a = ": ",b = "qx.type.BaseError",c = "",d = "error";
  qx.Class.define(b, {
    extend : Error,
    construct : function(comment, failMessage){

      var inst = Error.call(this, failMessage);
      if(inst.stack){

        this.stack = inst.stack;
      };
      if(inst.stacktrace){

        this.stacktrace = inst.stacktrace;
      };
      this.__bN = comment || c;
      this.message = failMessage || qx.type.BaseError.DEFAULTMESSAGE;
    },
    statics : {
      DEFAULTMESSAGE : d
    },
    members : {
      __bO : null,
      __bN : null,
      message : null,
      getComment : function(){

        return this.__bN;
      },
      toString : function(){

        return this.__bN + (this.message ? a + this.message : c);
      }
    }
  });
})();
(function(){

  var a = "qx.core.AssertionError";
  qx.Class.define(a, {
    extend : qx.type.BaseError,
    construct : function(comment, failMessage){

      qx.type.BaseError.call(this, comment, failMessage);
      this.__bP = qx.dev.StackTrace.getStackTrace();
    },
    members : {
      __bP : null,
      getStackTrace : function(){

        return this.__bP;
      }
    }
  });
})();
(function(){

  var a = "anonymous",b = "...",c = "qx.dev.StackTrace",d = "",e = "\n",f = "?",g = "/source/class/",h = "Error created at",j = "ecmascript.error.stacktrace",k = "Backtrace:",l = "stack",m = ":",n = ".",o = "function",p = "prototype",q = "stacktrace";
  qx.Bootstrap.define(c, {
    statics : {
      FILENAME_TO_CLASSNAME : null,
      FORMAT_STACKTRACE : null,
      getStackTrace : function(){

        var trace = [];
        try{

          throw new Error();
        } catch(ex) {

          if(qx.dev.StackTrace.hasEnvironmentCheck && qx.core.Environment.get(j)){

            var errorTrace = qx.dev.StackTrace.getStackTraceFromError(ex);
            var callerTrace = qx.dev.StackTrace.getStackTraceFromCaller(arguments);
            qx.lang.Array.removeAt(errorTrace, 0);
            trace = callerTrace.length > errorTrace.length ? callerTrace : errorTrace;
            for(var i = 0;i < Math.min(callerTrace.length, errorTrace.length);i++){

              var callerCall = callerTrace[i];
              if(callerCall.indexOf(a) >= 0){

                continue;
              };
              var methodName = null;
              var callerArr = callerCall.split(n);
              var mO = /(.*?)\(/.exec(callerArr[callerArr.length - 1]);
              if(mO && mO.length == 2){

                methodName = mO[1];
                callerArr.pop();
              };
              if(callerArr[callerArr.length - 1] == p){

                callerArr.pop();
              };
              var callerClassName = callerArr.join(n);
              var errorCall = errorTrace[i];
              var errorArr = errorCall.split(m);
              var errorClassName = errorArr[0];
              var lineNumber = errorArr[1];
              var columnNumber;
              if(errorArr[2]){

                columnNumber = errorArr[2];
              };
              var className = null;
              if(qx.Class && qx.Class.getByName(errorClassName)){

                className = errorClassName;
              } else {

                className = callerClassName;
              };
              var line = className;
              if(methodName){

                line += n + methodName;
              };
              line += m + lineNumber;
              if(columnNumber){

                line += m + columnNumber;
              };
              trace[i] = line;
            };
          } else {

            trace = this.getStackTraceFromCaller(arguments);
          };
        };
        return trace;
      },
      getStackTraceFromCaller : function(args){

        var trace = [];
        var fcn = qx.lang.Function.getCaller(args);
        var knownFunction = {
        };
        while(fcn){

          var fcnName = qx.lang.Function.getName(fcn);
          trace.push(fcnName);
          try{

            fcn = fcn.caller;
          } catch(ex) {

            break;
          };
          if(!fcn){

            break;
          };
          var hash = qx.core.ObjectRegistry.toHashCode(fcn);
          if(knownFunction[hash]){

            trace.push(b);
            break;
          };
          knownFunction[hash] = fcn;
        };
        return trace;
      },
      getStackTraceFromError : function(error){

        var trace = [];
        var lineRe,hit,className,lineNumber,columnNumber,fileName,url;
        var traceProp = qx.dev.StackTrace.hasEnvironmentCheck ? qx.core.Environment.get(j) : null;
        if(traceProp === l){

          if(!error.stack){

            return trace;
          };
          lineRe = /@(.+):(\d+)$/gm;
          while((hit = lineRe.exec(error.stack)) != null){

            url = hit[1];
            lineNumber = hit[2];
            className = this.__bQ(url);
            trace.push(className + m + lineNumber);
          };
          if(trace.length > 0){

            return this.__bS(trace);
          };
          lineRe = /at (.*)/gm;
          var fileReParens = /\((.*?)(:[^\/].*)\)/;
          var fileRe = /(.*?)(:[^\/].*)/;
          while((hit = lineRe.exec(error.stack)) != null){

            var fileMatch = fileReParens.exec(hit[1]);
            if(!fileMatch){

              fileMatch = fileRe.exec(hit[1]);
            };
            if(fileMatch){

              className = this.__bQ(fileMatch[1]);
              trace.push(className + fileMatch[2]);
            } else {

              trace.push(hit[1]);
            };
          };
        } else if(traceProp === q){

          var stacktrace = error.stacktrace;
          if(!stacktrace){

            return trace;
          };
          if(stacktrace.indexOf(h) >= 0){

            stacktrace = stacktrace.split(h)[0];
          };
          lineRe = /line\ (\d+?),\ column\ (\d+?)\ in\ (?:.*?)\ in\ (.*?):[^\/]/gm;
          while((hit = lineRe.exec(stacktrace)) != null){

            lineNumber = hit[1];
            columnNumber = hit[2];
            url = hit[3];
            className = this.__bQ(url);
            trace.push(className + m + lineNumber + m + columnNumber);
          };
          if(trace.length > 0){

            return this.__bS(trace);
          };
          lineRe = /Line\ (\d+?)\ of\ linked\ script\ (.*?)$/gm;
          while((hit = lineRe.exec(stacktrace)) != null){

            lineNumber = hit[1];
            url = hit[2];
            className = this.__bQ(url);
            trace.push(className + m + lineNumber);
          };
        } else if(error.message && error.message.indexOf(k) >= 0){

          var traceString = error.message.split(k)[1].trim();
          var lines = traceString.split(e);
          for(var i = 0;i < lines.length;i++){

            var reResult = lines[i].match(/\s*Line ([0-9]+) of.* (\S.*)/);
            if(reResult && reResult.length >= 2){

              lineNumber = reResult[1];
              fileName = this.__bQ(reResult[2]);
              trace.push(fileName + m + lineNumber);
            };
          };
        } else if(error.sourceURL && error.line){

          trace.push(this.__bQ(error.sourceURL) + m + error.line);
        };;;
        return this.__bS(trace);
      },
      __bQ : function(fileName){

        if(typeof qx.dev.StackTrace.FILENAME_TO_CLASSNAME == o){

          var convertedName = qx.dev.StackTrace.FILENAME_TO_CLASSNAME(fileName);
          {
          };
          return convertedName;
        };
        return qx.dev.StackTrace.__bR(fileName);
      },
      __bR : function(fileName){

        var scriptDir = g;
        var jsPos = fileName.indexOf(scriptDir);
        var paramPos = fileName.indexOf(f);
        if(paramPos >= 0){

          fileName = fileName.substring(0, paramPos);
        };
        var className = (jsPos == -1) ? fileName : fileName.substring(jsPos + scriptDir.length).replace(/\//g, n).replace(/\.js$/, d);
        return className;
      },
      __bS : function(trace){

        if(typeof qx.dev.StackTrace.FORMAT_STACKTRACE == o){

          trace = qx.dev.StackTrace.FORMAT_STACKTRACE(trace);
          {
          };
        };
        return trace;
      }
    },
    defer : function(statics){

      statics.hasEnvironmentCheck = qx.bom.client.EcmaScript && qx.bom.client.EcmaScript.getStackTrace;
    }
  });
})();
(function(){

  var c = "-",d = "",e = "qx.core.ObjectRegistry",f = "Disposed ",g = "$$hash",h = "-0",j = " objects",k = "Could not dispose object ",m = ": ";
  qx.Bootstrap.define(e, {
    statics : {
      inShutDown : false,
      __k : {
      },
      __bT : 0,
      __bU : [],
      __bV : d,
      __bW : {
      },
      register : function(obj){

        var registry = this.__k;
        if(!registry){

          return;
        };
        var hash = obj.$$hash;
        if(hash == null){

          var cache = this.__bU;
          if(cache.length > 0 && true){

            hash = cache.pop();
          } else {

            hash = (this.__bT++) + this.__bV;
          };
          obj.$$hash = hash;
          {
          };
        };
        {
        };
        registry[hash] = obj;
      },
      unregister : function(obj){

        var hash = obj.$$hash;
        if(hash == null){

          return;
        };
        var registry = this.__k;
        if(registry && registry[hash]){

          delete registry[hash];
          this.__bU.push(hash);
        };
        try{

          delete obj.$$hash;
        } catch(ex) {

          if(obj.removeAttribute){

            obj.removeAttribute(g);
          };
        };
      },
      toHashCode : function(obj){

        {
        };
        var hash = obj.$$hash;
        if(hash != null){

          return hash;
        };
        var cache = this.__bU;
        if(cache.length > 0){

          hash = cache.pop();
        } else {

          hash = (this.__bT++) + this.__bV;
        };
        return obj.$$hash = hash;
      },
      clearHashCode : function(obj){

        {
        };
        var hash = obj.$$hash;
        if(hash != null){

          this.__bU.push(hash);
          try{

            delete obj.$$hash;
          } catch(ex) {

            if(obj.removeAttribute){

              obj.removeAttribute(g);
            };
          };
        };
      },
      fromHashCode : function(hash){

        return this.__k[hash] || null;
      },
      shutdown : function(){

        this.inShutDown = true;
        var registry = this.__k;
        var hashes = [];
        for(var hash in registry){

          hashes.push(hash);
        };
        hashes.sort(function(a, b){

          return parseInt(b, 10) - parseInt(a, 10);
        });
        var obj,i = 0,l = hashes.length;
        while(true){

          try{

            for(;i < l;i++){

              hash = hashes[i];
              obj = registry[hash];
              if(obj && obj.dispose){

                obj.dispose();
              };
            };
          } catch(ex) {

            qx.Bootstrap.error(this, k + obj.toString() + m + ex, ex);
            if(i !== l){

              i++;
              continue;
            };
          };
          break;
        };
        qx.Bootstrap.debug(this, f + l + j);
        delete this.__k;
      },
      getRegistry : function(){

        return this.__k;
      },
      getNextHash : function(){

        return this.__bT;
      },
      getPostId : function(){

        return this.__bV;
      },
      getStackTraces : function(){

        return this.__bW;
      }
    },
    defer : function(statics){

      if(window && window.top){

        var frames = window.top.frames;
        for(var i = 0;i < frames.length;i++){

          if(frames[i] === window){

            statics.__bV = c + (i + 1);
            return;
          };
        };
      };
      statics.__bV = h;
    }
  });
})();
(function(){

  var a = "\x00\b\n\f\r\t",b = "-",c = "function",d = "[null,null,null]",e = "T",f = "+",g = ",\n",h = "constructor",i = "{\n",j = '"+275760-09-13T00:00:00.000Z"',k = "true",l = "\\n",m = "false",n = '"-271821-04-20T00:00:00.000Z"',o = "json",p = 'object',q = '""',r = "qx.lang.Json",s = "{}",t = "hasOwnProperty",u = "@",v = "prototype",w = 'hasOwnProperty',x = '"',y = "toLocaleString",z = "0",A = 'function',B = "",C = '\\"',D = "\t",E = "string",F = "}",G = "\r",H = "toJSON",I = ":",J = "[\n 1,\n 2\n]",K = "\\f",L = '"1969-12-31T23:59:59.999Z"',M = "/",N = "\\b",O = "Z",P = "\\t",Q = "\b",R = "[object Number]",S = "isPrototypeOf",T = "{",U = "toString",V = "0x",W = "[1]",X = "\\r",Y = "]",ba = ",",bb = "null",bc = "\\u00",bd = "\n",be = "json-stringify",bf = "[]",bg = "1",bh = "000000",bi = "[object Boolean]",bj = "valueOf",bk = "\\\\",bl = "[object String]",bm = "json-parse",bn = "bug-string-char-index",bo = "[object Array]",bp = "$",bq = "[\n",br = '"-000001-01-01T00:00:00.000Z"',bs = "[",bt = "[null]",bu = "\\",bv = "[object Date]",bw = '{"a":[1,true,false,null,"\\u0000\\b\\n\\f\\r\\t"]}',bx = "a",by = " ",bz = ".",bA = "[object Function]",bB = "01",bC = '"\t"',bD = "propertyIsEnumerable",bE = "\f",bF = "object";
  qx.Bootstrap.define(r, {
    statics : {
      stringify : null,
      parse : null
    }
  });
  (function(window){

    var getClass = {
    }.toString,isProperty,forEach,undef;
    var isLoader = typeof define === c && define.amd,JSON3 = typeof exports == bF && exports;
    if(JSON3 || isLoader){

      if(typeof JSON == bF && JSON){

        if(JSON3){

          JSON3.stringify = JSON.stringify;
          JSON3.parse = JSON.parse;
        } else {

          JSON3 = JSON;
        };
      } else if(isLoader){

        JSON3 = window.JSON = {
        };
      };
    } else {

      JSON3 = window.JSON || (window.JSON = {
      });
    };
    var isExtended = new Date(-3509827334573292);
    try{

      isExtended = isExtended.getUTCFullYear() == -109252 && isExtended.getUTCMonth() === 0 && isExtended.getUTCDate() === 1 && isExtended.getUTCHours() == 10 && isExtended.getUTCMinutes() == 37 && isExtended.getUTCSeconds() == 6 && isExtended.getUTCMilliseconds() == 708;
    } catch(exception) {
    };
    function has(name){

      if(name == bn){

        return bx[0] != bx;
      };
      var value,serialized = bw,isAll = name == o;
      if(isAll || name == be || name == bm){

        if(name == be || isAll){

          var stringify = JSON3.stringify,stringifySupported = typeof stringify == c && isExtended;
          if(stringifySupported){

            (value = function(){

              return 1;
            }).toJSON = value;
            try{

              stringifySupported = stringify(0) === z && stringify(new Number()) === z && stringify(new String()) == q && stringify(getClass) === undef && stringify(undef) === undef && stringify() === undef && stringify(value) === bg && stringify([value]) == W && stringify([undef]) == bt && stringify(null) == bb && stringify([undef, getClass, null]) == d && stringify({
                "a" : [value, true, false, null, a]
              }) == serialized && stringify(null, value) === bg && stringify([1, 2], null, 1) == J && stringify(new Date(-8.64e15)) == n && stringify(new Date(8.64e15)) == j && stringify(new Date(-621987552e5)) == br && stringify(new Date(-1)) == L;
            } catch(exception) {

              stringifySupported = false;
            };
          };
          if(!isAll){

            return stringifySupported;
          };
        };
        if(name == bm || isAll){

          var parse = JSON3.parse;
          if(typeof parse == c){

            try{

              if(parse(z) === 0 && !parse(false)){

                value = parse(serialized);
                var parseSupported = value[bx].length == 5 && value[bx][0] === 1;
                if(parseSupported){

                  try{

                    parseSupported = !parse(bC);
                  } catch(exception) {
                  };
                  if(parseSupported){

                    try{

                      parseSupported = parse(bB) !== 1;
                    } catch(exception) {
                    };
                  };
                };
              };
            } catch(exception) {

              parseSupported = false;
            };
          };
          if(!isAll){

            return parseSupported;
          };
        };
        return stringifySupported && parseSupported;
      };
    };
    if(!has(o)){

      var functionClass = bA;
      var dateClass = bv;
      var numberClass = R;
      var stringClass = bl;
      var arrayClass = bo;
      var booleanClass = bi;
      var charIndexBuggy = has(bn);
      if(!isExtended){

        var floor = Math.floor;
        var Months = [0, 31, 59, 90, 120, 151, 181, 212, 243, 273, 304, 334];
        var getDay = function(year, month){

          return Months[month] + 365 * (year - 1970) + floor((year - 1969 + (month = +(month > 1))) / 4) - floor((year - 1901 + month) / 100) + floor((year - 1601 + month) / 400);
        };
      };
      if(!(isProperty = {
      }.hasOwnProperty)){

        isProperty = function(property){

          var members = {
          },constructor;
          if((members.__bX = null, members.__bX = {
            "toString" : 1
          }, members).toString != getClass){

            isProperty = function(property){

              var original = this.__bX,result = property in (this.__bX = null, this);
              this.__bX = original;
              return result;
            };
          } else {

            constructor = members.constructor;
            isProperty = function(property){

              var parent = (this.constructor || constructor).prototype;
              return property in this && !(property in parent && this[property] === parent[property]);
            };
          };
          members = null;
          return isProperty.call(this, property);
        };
      };
      var PrimitiveTypes = {
        'boolean' : 1,
        'number' : 1,
        'string' : 1,
        'undefined' : 1
      };
      var isHostType = function(object, property){

        var type = typeof object[property];
        return type == p ? !!object[property] : !PrimitiveTypes[type];
      };
      forEach = function(object, callback){

        var size = 0,Properties,members,property,forEach;
        (Properties = function(){

          this.valueOf = 0;
        }).prototype.valueOf = 0;
        members = new Properties();
        for(property in members){

          if(isProperty.call(members, property)){

            size++;
          };
        };
        Properties = members = null;
        if(!size){

          members = [bj, U, y, bD, S, t, h];
          forEach = function(object, callback){

            var isFunction = getClass.call(object) == functionClass,property,length;
            var hasProperty = !isFunction && typeof object.constructor != A && isHostType(object, w) ? object.hasOwnProperty : isProperty;
            for(property in object){

              if(!(isFunction && property == v) && hasProperty.call(object, property)){

                callback(property);
              };
            };
            for(length = members.length;property = members[--length];hasProperty.call(object, property) && callback(property));
          };
        } else if(size == 2){

          forEach = function(object, callback){

            var members = {
            },isFunction = getClass.call(object) == functionClass,property;
            for(property in object){

              if(!(isFunction && property == v) && !isProperty.call(members, property) && (members[property] = 1) && isProperty.call(object, property)){

                callback(property);
              };
            };
          };
        } else {

          forEach = function(object, callback){

            var isFunction = getClass.call(object) == functionClass,property,isConstructor;
            for(property in object){

              if(!(isFunction && property == v) && isProperty.call(object, property) && !(isConstructor = property === h)){

                callback(property);
              };
            };
            if(isConstructor || isProperty.call(object, (property = h))){

              callback(property);
            };
          };
        };
        return forEach(object, callback);
      };
      if(!has(be)){

        var Escapes = {
          '92' : bk,
          '34' : C,
          '8' : N,
          '12' : K,
          '10' : l,
          '13' : X,
          '9' : P
        };
        var leadingZeroes = bh;
        var toPaddedString = function(width, value){

          return (leadingZeroes + (value || 0)).slice(-width);
        };
        var unicodePrefix = bc;
        var quote = function(value){

          var result = x,index = 0,length = value.length,isLarge = length > 10 && charIndexBuggy,symbols;
          if(isLarge){

            symbols = value.split(B);
          };
          for(;index < length;index++){

            var charCode = value.charCodeAt(index);
            switch(charCode){case 8:case 9:case 10:case 12:case 13:case 34:case 92:
            result += Escapes[charCode];
            break;default:
            if(charCode < 32){

              result += unicodePrefix + toPaddedString(2, charCode.toString(16));
              break;
            };
            result += isLarge ? symbols[index] : charIndexBuggy ? value.charAt(index) : value[index];};
          };
          return result + x;
        };
        var serialize = function(property, object, callback, properties, whitespace, indentation, stack){

          var value = object[property],className,year,month,date,time,hours,minutes,seconds,milliseconds,results,element,index,length,prefix,hasMembers,result;
          try{

            value = object[property];
          } catch(exception) {
          };
          if(typeof value == bF && value){

            className = getClass.call(value);
            if(className == dateClass && !isProperty.call(value, H)){

              if(value > -1 / 0 && value < 1 / 0){

                if(getDay){

                  date = floor(value / 864e5);
                  for(year = floor(date / 365.2425) + 1970 - 1;getDay(year + 1, 0) <= date;year++);
                  for(month = floor((date - getDay(year, 0)) / 30.42);getDay(year, month + 1) <= date;month++);
                  date = 1 + date - getDay(year, month);
                  time = (value % 864e5 + 864e5) % 864e5;
                  hours = floor(time / 36e5) % 24;
                  minutes = floor(time / 6e4) % 60;
                  seconds = floor(time / 1e3) % 60;
                  milliseconds = time % 1e3;
                } else {

                  year = value.getUTCFullYear();
                  month = value.getUTCMonth();
                  date = value.getUTCDate();
                  hours = value.getUTCHours();
                  minutes = value.getUTCMinutes();
                  seconds = value.getUTCSeconds();
                  milliseconds = value.getUTCMilliseconds();
                };
                value = (year <= 0 || year >= 1e4 ? (year < 0 ? b : f) + toPaddedString(6, year < 0 ? -year : year) : toPaddedString(4, year)) + b + toPaddedString(2, month + 1) + b + toPaddedString(2, date) + e + toPaddedString(2, hours) + I + toPaddedString(2, minutes) + I + toPaddedString(2, seconds) + bz + toPaddedString(3, milliseconds) + O;
              } else {

                value = null;
              };
            } else if(typeof value.toJSON == c && ((className != numberClass && className != stringClass && className != arrayClass) || isProperty.call(value, H))){

              value = value.toJSON(property);
            };
          };
          if(callback){

            value = callback.call(object, property, value);
          };
          if(value === null){

            return bb;
          };
          className = getClass.call(value);
          if(className == booleanClass){

            return B + value;
          } else if(className == numberClass){

            return value > -1 / 0 && value < 1 / 0 ? B + value : bb;
          } else if(className == stringClass){

            return quote(B + value);
          };;
          if(typeof value == bF){

            for(length = stack.length;length--;){

              if(stack[length] === value){

                throw TypeError();
              };
            };
            stack.push(value);
            results = [];
            prefix = indentation;
            indentation += whitespace;
            if(className == arrayClass){

              for(index = 0,length = value.length;index < length;hasMembers || (hasMembers = true),index++){

                element = serialize(index, value, callback, properties, whitespace, indentation, stack);
                results.push(element === undef ? bb : element);
              };
              result = hasMembers ? (whitespace ? bq + indentation + results.join(g + indentation) + bd + prefix + Y : (bs + results.join(ba) + Y)) : bf;
            } else {

              forEach(properties || value, function(property){

                var element = serialize(property, value, callback, properties, whitespace, indentation, stack);
                if(element !== undef){

                  results.push(quote(property) + I + (whitespace ? by : B) + element);
                };
                hasMembers || (hasMembers = true);
              });
              result = hasMembers ? (whitespace ? i + indentation + results.join(g + indentation) + bd + prefix + F : (T + results.join(ba) + F)) : s;
            };
            stack.pop();
            return result;
          };
        };
        JSON3.stringify = function(source, filter, width){

          var whitespace,callback,properties;
          if(typeof filter == c || typeof filter == bF && filter){

            if(getClass.call(filter) == functionClass){

              callback = filter;
            } else if(getClass.call(filter) == arrayClass){

              properties = {
              };
              for(var index = 0,length = filter.length,value;index < length;value = filter[index++],((getClass.call(value) == stringClass || getClass.call(value) == numberClass) && (properties[value] = 1)));
            };
          };
          if(width){

            if(getClass.call(width) == numberClass){

              if((width -= width % 1) > 0){

                for(whitespace = B,width > 10 && (width = 10);whitespace.length < width;whitespace += by);
              };
            } else if(getClass.call(width) == stringClass){

              whitespace = width.length <= 10 ? width : width.slice(0, 10);
            };
          };
          return serialize(B, (value = {
          }, value[B] = source, value), callback, properties, whitespace, B, []);
        };
      };
      if(!has(bm)){

        var fromCharCode = String.fromCharCode;
        var Unescapes = {
          '92' : bu,
          '34' : x,
          '47' : M,
          '98' : Q,
          '116' : D,
          '110' : bd,
          '102' : bE,
          '114' : G
        };
        var Index,Source;
        var abort = function(){

          Index = Source = null;
          throw SyntaxError();
        };
        var lex = function(){

          var source = Source,length = source.length,value,begin,position,isSigned,charCode;
          while(Index < length){

            charCode = source.charCodeAt(Index);
            switch(charCode){case 9:case 10:case 13:case 32:
            Index++;
            break;case 123:case 125:case 91:case 93:case 58:case 44:
            value = charIndexBuggy ? source.charAt(Index) : source[Index];
            Index++;
            return value;case 34:
            for(value = u,Index++;Index < length;){

              charCode = source.charCodeAt(Index);
              if(charCode < 32){

                abort();
              } else if(charCode == 92){

                charCode = source.charCodeAt(++Index);
                switch(charCode){case 92:case 34:case 47:case 98:case 116:case 110:case 102:case 114:
                value += Unescapes[charCode];
                Index++;
                break;case 117:
                begin = ++Index;
                for(position = Index + 4;Index < position;Index++){

                  charCode = source.charCodeAt(Index);
                  if(!(charCode >= 48 && charCode <= 57 || charCode >= 97 && charCode <= 102 || charCode >= 65 && charCode <= 70)){

                    abort();
                  };
                };
                value += fromCharCode(V + source.slice(begin, Index));
                break;default:
                abort();};
              } else {

                if(charCode == 34){

                  break;
                };
                charCode = source.charCodeAt(Index);
                begin = Index;
                while(charCode >= 32 && charCode != 92 && charCode != 34){

                  charCode = source.charCodeAt(++Index);
                };
                value += source.slice(begin, Index);
              };
            };
            if(source.charCodeAt(Index) == 34){

              Index++;
              return value;
            };
            abort();default:
            begin = Index;
            if(charCode == 45){

              isSigned = true;
              charCode = source.charCodeAt(++Index);
            };
            if(charCode >= 48 && charCode <= 57){

              if(charCode == 48 && ((charCode = source.charCodeAt(Index + 1)), charCode >= 48 && charCode <= 57)){

                abort();
              };
              isSigned = false;
              for(;Index < length && ((charCode = source.charCodeAt(Index)), charCode >= 48 && charCode <= 57);Index++);
              if(source.charCodeAt(Index) == 46){

                position = ++Index;
                for(;position < length && ((charCode = source.charCodeAt(position)), charCode >= 48 && charCode <= 57);position++);
                if(position == Index){

                  abort();
                };
                Index = position;
              };
              charCode = source.charCodeAt(Index);
              if(charCode == 101 || charCode == 69){

                charCode = source.charCodeAt(++Index);
                if(charCode == 43 || charCode == 45){

                  Index++;
                };
                for(position = Index;position < length && ((charCode = source.charCodeAt(position)), charCode >= 48 && charCode <= 57);position++);
                if(position == Index){

                  abort();
                };
                Index = position;
              };
              return +source.slice(begin, Index);
            };
            if(isSigned){

              abort();
            };
            if(source.slice(Index, Index + 4) == k){

              Index += 4;
              return true;
            } else if(source.slice(Index, Index + 5) == m){

              Index += 5;
              return false;
            } else if(source.slice(Index, Index + 4) == bb){

              Index += 4;
              return null;
            };;
            abort();};
          };
          return bp;
        };
        var get = function(value){

          var results,hasMembers;
          if(value == bp){

            abort();
          };
          if(typeof value == E){

            if((charIndexBuggy ? value.charAt(0) : value[0]) == u){

              return value.slice(1);
            };
            if(value == bs){

              results = [];
              for(;;hasMembers || (hasMembers = true)){

                value = lex();
                if(value == Y){

                  break;
                };
                if(hasMembers){

                  if(value == ba){

                    value = lex();
                    if(value == Y){

                      abort();
                    };
                  } else {

                    abort();
                  };
                };
                if(value == ba){

                  abort();
                };
                results.push(get(value));
              };
              return results;
            } else if(value == T){

              results = {
              };
              for(;;hasMembers || (hasMembers = true)){

                value = lex();
                if(value == F){

                  break;
                };
                if(hasMembers){

                  if(value == ba){

                    value = lex();
                    if(value == F){

                      abort();
                    };
                  } else {

                    abort();
                  };
                };
                if(value == ba || typeof value != E || (charIndexBuggy ? value.charAt(0) : value[0]) != u || lex() != I){

                  abort();
                };
                results[value.slice(1)] = get(lex());
              };
              return results;
            };
            abort();
          };
          return value;
        };
        var update = function(source, property, callback){

          var element = walk(source, property, callback);
          if(element === undef){

            delete source[property];
          } else {

            source[property] = element;
          };
        };
        var walk = function(source, property, callback){

          var value = source[property],length;
          if(typeof value == bF && value){

            if(getClass.call(value) == arrayClass){

              for(length = value.length;length--;){

                update(value, length, callback);
              };
            } else {

              forEach(value, function(property){

                update(value, property, callback);
              });
            };
          };
          return callback.call(source, property, value);
        };
        JSON3.parse = function(source, callback){

          var result,value;
          Index = 0;
          Source = B + source;
          result = get(lex());
          if(lex() != bp){

            abort();
          };
          Index = Source = null;
          return callback && getClass.call(callback) == functionClass ? walk((value = {
          }, value[B] = result, value), B, callback) : result;
        };
      };
    };
    if(isLoader){

      define(function(){

        return JSON3;
      });
    };
  }(this));
  qx.lang.Json.stringify = window.JSON.stringify;
  qx.lang.Json.parse = window.JSON.parse;
})();
(function(){

  var a = "qx.event.type.Data",b = "qx.event.type.Event",c = "qx.data.IListData";
  qx.Interface.define(c, {
    events : {
      "change" : a,
      "changeLength" : b
    },
    members : {
      getItem : function(index){
      },
      setItem : function(index, item){
      },
      splice : function(startIndex, amount, varargs){
      },
      contains : function(item){
      },
      getLength : function(){
      },
      toArray : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.core.ValidationError";
  qx.Class.define(a, {
    extend : qx.type.BaseError
  });
})();
(function(){

  var a = "qx.util.RingBuffer";
  qx.Bootstrap.define(a, {
    extend : Object,
    construct : function(maxEntries){

      this.setMaxEntries(maxEntries || 50);
    },
    members : {
      __bY : 0,
      __ca : 0,
      __cb : false,
      __cc : 0,
      __cd : null,
      __ce : null,
      setMaxEntries : function(maxEntries){

        this.__ce = maxEntries;
        this.clear();
      },
      getMaxEntries : function(){

        return this.__ce;
      },
      addEntry : function(entry){

        this.__cd[this.__bY] = entry;
        this.__bY = this.__cf(this.__bY, 1);
        var max = this.getMaxEntries();
        if(this.__ca < max){

          this.__ca++;
        };
        if(this.__cb && (this.__cc < max)){

          this.__cc++;
        };
      },
      mark : function(){

        this.__cb = true;
        this.__cc = 0;
      },
      clearMark : function(){

        this.__cb = false;
      },
      getAllEntries : function(){

        return this.getEntries(this.getMaxEntries(), false);
      },
      getEntries : function(count, startingFromMark){

        if(count > this.__ca){

          count = this.__ca;
        };
        if(startingFromMark && this.__cb && (count > this.__cc)){

          count = this.__cc;
        };
        if(count > 0){

          var indexOfYoungestElementInHistory = this.__cf(this.__bY, -1);
          var startIndex = this.__cf(indexOfYoungestElementInHistory, -count + 1);
          var result;
          if(startIndex <= indexOfYoungestElementInHistory){

            result = this.__cd.slice(startIndex, indexOfYoungestElementInHistory + 1);
          } else {

            result = this.__cd.slice(startIndex, this.__ca).concat(this.__cd.slice(0, indexOfYoungestElementInHistory + 1));
          };
        } else {

          result = [];
        };
        return result;
      },
      clear : function(){

        this.__cd = new Array(this.getMaxEntries());
        this.__ca = 0;
        this.__cc = 0;
        this.__bY = 0;
      },
      __cf : function(idx, addMe){

        var max = this.getMaxEntries();
        var result = (idx + addMe) % max;
        if(result < 0){

          result += max;
        };
        return result;
      }
    }
  });
})();
(function(){

  var a = "qx.log.appender.RingBuffer";
  qx.Bootstrap.define(a, {
    extend : qx.util.RingBuffer,
    construct : function(maxMessages){

      this.setMaxMessages(maxMessages || 50);
    },
    members : {
      setMaxMessages : function(maxMessages){

        this.setMaxEntries(maxMessages);
      },
      getMaxMessages : function(){

        return this.getMaxEntries();
      },
      process : function(entry){

        this.addEntry(entry);
      },
      getAllLogEvents : function(){

        return this.getAllEntries();
      },
      retrieveLogEvents : function(count, startingFromMark){

        return this.getEntries(count, startingFromMark);
      },
      clearHistory : function(){

        this.clear();
      }
    }
  });
})();
(function(){

  var a = "qx.log.Logger",b = "[",c = "...(+",d = "array",e = ")",f = "info",g = "node",h = "instance",j = "string",k = "null",m = "error",n = "#",o = "class",p = ": ",q = "warn",r = "document",s = "{...(",t = "",u = "number",v = "stringify",w = "]",x = "date",y = "unknown",z = "function",A = "text[",B = "[...(",C = "boolean",D = "\n",E = ")}",F = "debug",G = ")]",H = "map",I = "undefined",J = "object";
  qx.Bootstrap.define(a, {
    statics : {
      __cg : F,
      setLevel : function(value){

        this.__cg = value;
      },
      getLevel : function(){

        return this.__cg;
      },
      setTreshold : function(value){

        this.__cj.setMaxMessages(value);
      },
      getTreshold : function(){

        return this.__cj.getMaxMessages();
      },
      __ch : {
      },
      __ci : 0,
      register : function(appender){

        if(appender.$$id){

          return;
        };
        var id = this.__ci++;
        this.__ch[id] = appender;
        appender.$$id = id;
        var levels = this.__ck;
        var entries = this.__cj.getAllLogEvents();
        for(var i = 0,l = entries.length;i < l;i++){

          if(levels[entries[i].level] >= levels[this.__cg]){

            appender.process(entries[i]);
          };
        };
      },
      unregister : function(appender){

        var id = appender.$$id;
        if(id == null){

          return;
        };
        delete this.__ch[id];
        delete appender.$$id;
      },
      debug : function(object, message){

        qx.log.Logger.__cl(F, arguments);
      },
      info : function(object, message){

        qx.log.Logger.__cl(f, arguments);
      },
      warn : function(object, message){

        qx.log.Logger.__cl(q, arguments);
      },
      error : function(object, message){

        qx.log.Logger.__cl(m, arguments);
      },
      trace : function(object){

        var trace = qx.dev.StackTrace.getStackTrace();
        qx.log.Logger.__cl(f, [(typeof object !== I ? [object].concat(trace) : trace).join(D)]);
      },
      deprecatedMethodWarning : function(fcn, msg){

        {

          var functionName;
        };
      },
      deprecatedClassWarning : function(clazz, msg){

        {

          var className;
        };
      },
      deprecatedEventWarning : function(clazz, event, msg){

        {

          var className;
        };
      },
      deprecatedMixinWarning : function(clazz, msg){

        {

          var mixinName;
        };
      },
      deprecatedConstantWarning : function(clazz, constant, msg){

        {

          var self,constantValue;
        };
      },
      deprecateMethodOverriding : function(object, baseclass, methodName, msg){

        {

          var clazz;
        };
      },
      clear : function(){

        this.__cj.clearHistory();
      },
      __cj : new qx.log.appender.RingBuffer(50),
      __ck : {
        debug : 0,
        info : 1,
        warn : 2,
        error : 3
      },
      __cl : function(level, args){

        var levels = this.__ck;
        if(levels[level] < levels[this.__cg]){

          return;
        };
        var object = args.length < 2 ? null : args[0];
        var start = object ? 1 : 0;
        var items = [];
        for(var i = start,l = args.length;i < l;i++){

          items.push(this.__cn(args[i], true));
        };
        var time = new Date;
        var entry = {
          time : time,
          offset : time - qx.Bootstrap.LOADSTART,
          level : level,
          items : items,
          win : window
        };
        if(object){

          if(object.$$hash !== undefined){

            entry.object = object.$$hash;
          } else if(object.$$type){

            entry.clazz = object;
          } else if(object.constructor){

            entry.clazz = object.constructor;
          };;
        };
        this.__cj.process(entry);
        var appender = this.__ch;
        for(var id in appender){

          appender[id].process(entry);
        };
      },
      __cm : function(value){

        if(value === undefined){

          return I;
        } else if(value === null){

          return k;
        };
        if(value.$$type){

          return o;
        };
        var type = typeof value;
        if(type === z || type == j || type === u || type === C){

          return type;
        } else if(type === J){

          if(value.nodeType){

            return g;
          } else if(value instanceof Error || (value.name && value.message)){

            return m;
          } else if(value.classname){

            return h;
          } else if(value instanceof Array){

            return d;
          } else if(value instanceof Date){

            return x;
          } else {

            return H;
          };;;;
        };
        if(value.toString){

          return v;
        };
        return y;
      },
      __cn : function(value, deep){

        var type = this.__cm(value);
        var text = y;
        var trace = [];
        switch(type){case k:case I:
        text = type;
        break;case j:case u:case C:case x:
        text = value;
        break;case g:
        if(value.nodeType === 9){

          text = r;
        } else if(value.nodeType === 3){

          text = A + value.nodeValue + w;
        } else if(value.nodeType === 1){

          text = value.nodeName.toLowerCase();
          if(value.id){

            text += n + value.id;
          };
        } else {

          text = g;
        };;
        break;case z:
        text = qx.lang.Function.getName(value) || type;
        break;case h:
        text = value.basename + b + value.$$hash + w;
        break;case o:case v:
        text = value.toString();
        break;case m:
        trace = qx.dev.StackTrace.getStackTraceFromError(value);
        text = (value.basename ? value.basename + p : t) + value.toString();
        break;case d:
        if(deep){

          text = [];
          for(var i = 0,l = value.length;i < l;i++){

            if(text.length > 20){

              text.push(c + (l - i) + e);
              break;
            };
            text.push(this.__cn(value[i], false));
          };
        } else {

          text = B + value.length + G;
        };
        break;case H:
        if(deep){

          var temp;
          var sorted = [];
          for(var key in value){

            sorted.push(key);
          };
          sorted.sort();
          text = [];
          for(var i = 0,l = sorted.length;i < l;i++){

            if(text.length > 20){

              text.push(c + (l - i) + e);
              break;
            };
            key = sorted[i];
            temp = this.__cn(value[key], false);
            temp.key = key;
            text.push(temp);
          };
        } else {

          var number = 0;
          for(var key in value){

            number++;
          };
          text = s + number + E;
        };
        break;};
        return {
          type : type,
          text : text,
          trace : trace
        };
      }
    },
    defer : function(statics){

      var logs = qx.Bootstrap.$$logs;
      for(var i = 0;i < logs.length;i++){

        statics.__cl(logs[i][0], logs[i][1]);
      };
      qx.Bootstrap.debug = statics.debug;
      qx.Bootstrap.info = statics.info;
      qx.Bootstrap.warn = statics.warn;
      qx.Bootstrap.error = statics.error;
      qx.Bootstrap.trace = statics.trace;
    }
  });
})();
(function(){

  var a = "qx.core.MProperty",b = "get",c = "reset",d = "No such property: ",e = "set";
  qx.Mixin.define(a, {
    members : {
      set : function(data, value){

        var setter = qx.core.Property.$$method.set;
        if(qx.Bootstrap.isString(data)){

          if(!this[setter[data]]){

            if(this[e + qx.Bootstrap.firstUp(data)] != undefined){

              this[e + qx.Bootstrap.firstUp(data)](value);
              return this;
            };
            throw new Error(d + data);
          };
          return this[setter[data]](value);
        } else {

          for(var prop in data){

            if(!this[setter[prop]]){

              if(this[e + qx.Bootstrap.firstUp(prop)] != undefined){

                this[e + qx.Bootstrap.firstUp(prop)](data[prop]);
                continue;
              };
              throw new Error(d + prop);
            };
            this[setter[prop]](data[prop]);
          };
          return this;
        };
      },
      get : function(prop){

        var getter = qx.core.Property.$$method.get;
        if(!this[getter[prop]]){

          if(this[b + qx.Bootstrap.firstUp(prop)] != undefined){

            return this[b + qx.Bootstrap.firstUp(prop)]();
          };
          throw new Error(d + prop);
        };
        return this[getter[prop]]();
      },
      reset : function(prop){

        var resetter = qx.core.Property.$$method.reset;
        if(!this[resetter[prop]]){

          if(this[c + qx.Bootstrap.firstUp(prop)] != undefined){

            this[c + qx.Bootstrap.firstUp(prop)]();
            return;
          };
          throw new Error(d + prop);
        };
        this[resetter[prop]]();
      }
    }
  });
})();
(function(){

  var a = "info",b = "debug",c = "warn",d = "qx.core.MLogging",e = "error";
  qx.Mixin.define(d, {
    members : {
      __co : qx.log.Logger,
      debug : function(varargs){

        this.__cp(b, arguments);
      },
      info : function(varargs){

        this.__cp(a, arguments);
      },
      warn : function(varargs){

        this.__cp(c, arguments);
      },
      error : function(varargs){

        this.__cp(e, arguments);
      },
      trace : function(){

        this.__co.trace(this);
      },
      __cp : function(level, varargs){

        var argumentsArray = qx.lang.Array.fromArguments(varargs);
        argumentsArray.unshift(this);
        this.__co[level].apply(this.__co, argumentsArray);
      }
    }
  });
})();
(function(){

  var b = "qx.dom.Node",c = "";
  qx.Bootstrap.define(b, {
    statics : {
      ELEMENT : 1,
      ATTRIBUTE : 2,
      TEXT : 3,
      CDATA_SECTION : 4,
      ENTITY_REFERENCE : 5,
      ENTITY : 6,
      PROCESSING_INSTRUCTION : 7,
      COMMENT : 8,
      DOCUMENT : 9,
      DOCUMENT_TYPE : 10,
      DOCUMENT_FRAGMENT : 11,
      NOTATION : 12,
      getDocument : function(node){

        return node.nodeType === this.DOCUMENT ? node : node.ownerDocument || node.document;
      },
      getWindow : function(node){

        if(node.nodeType == null){

          return node;
        };
        if(node.nodeType !== this.DOCUMENT){

          node = node.ownerDocument;
        };
        return node.defaultView || node.parentWindow;
      },
      getDocumentElement : function(node){

        return this.getDocument(node).documentElement;
      },
      getBodyElement : function(node){

        return this.getDocument(node).body;
      },
      isNode : function(node){

        return !!(node && node.nodeType != null);
      },
      isElement : function(node){

        return !!(node && node.nodeType === this.ELEMENT);
      },
      isDocument : function(node){

        return !!(node && node.nodeType === this.DOCUMENT);
      },
      isText : function(node){

        return !!(node && node.nodeType === this.TEXT);
      },
      isWindow : function(obj){

        return !!(obj && obj.history && obj.location && obj.document);
      },
      isNodeName : function(node, nodeName){

        if(!nodeName || !node || !node.nodeName){

          return false;
        };
        return nodeName.toLowerCase() == qx.dom.Node.getName(node);
      },
      getName : function(node){

        if(!node || !node.nodeName){

          return null;
        };
        return node.nodeName.toLowerCase();
      },
      getText : function(node){

        if(!node || !node.nodeType){

          return null;
        };
        switch(node.nodeType){case 1:
        var i,a = [],nodes = node.childNodes,length = nodes.length;
        for(i = 0;i < length;i++){

          a[i] = this.getText(nodes[i]);
        };
        return a.join(c);case 2:case 3:case 4:
        return node.nodeValue;};
        return null;
      },
      isBlockNode : function(node){

        if(!qx.dom.Node.isElement(node)){

          return false;
        };
        node = qx.dom.Node.getName(node);
        return /^(body|form|textarea|fieldset|ul|ol|dl|dt|dd|li|div|hr|p|h[1-6]|quote|pre|table|thead|tbody|tfoot|tr|td|th|iframe|address|blockquote)$/.test(node);
      }
    }
  });
})();
(function(){

  var a = "HTMLEvents",b = "engine.name",c = "",d = "mshtml",f = "qx.bom.Event",g = "return;",h = "function",j = "mouseover",k = "transitionend",m = "gecko",n = "css.transition",o = "on",p = "undefined",q = "browser.documentmode",r = "end-event";
  qx.Bootstrap.define(f, {
    statics : {
      addNativeListener : function(target, type, listener, useCapture){

        if(target.addEventListener){

          target.addEventListener(type, listener, !!useCapture);
        } else if(target.attachEvent){

          target.attachEvent(o + type, listener);
        } else if(typeof target[o + type] != p){

          target[o + type] = listener;
        } else {

          {
          };
        };;
      },
      removeNativeListener : function(target, type, listener, useCapture){

        if(target.removeEventListener){

          target.removeEventListener(type, listener, !!useCapture);
        } else if(target.detachEvent){

          try{

            target.detachEvent(o + type, listener);
          } catch(e) {

            if(e.number !== -2146828218){

              throw e;
            };
          };
        } else if(typeof target[o + type] != p){

          target[o + type] = null;
        } else {

          {
          };
        };;
      },
      getTarget : function(e){

        return e.target || e.srcElement;
      },
      getRelatedTarget : function(e){

        if(e.relatedTarget !== undefined){

          if((qx.core.Environment.get(b) == m)){

            try{

              e.relatedTarget && e.relatedTarget.nodeType;
            } catch(ex) {

              return null;
            };
          };
          return e.relatedTarget;
        } else if(e.fromElement !== undefined && e.type === j){

          return e.fromElement;
        } else if(e.toElement !== undefined){

          return e.toElement;
        } else {

          return null;
        };;
      },
      preventDefault : function(e){

        if(e.preventDefault){

          e.preventDefault();
        } else {

          try{

            e.keyCode = 0;
          } catch(ex) {
          };
          e.returnValue = false;
        };
      },
      stopPropagation : function(e){

        if(e.stopPropagation){

          e.stopPropagation();
        } else {

          e.cancelBubble = true;
        };
      },
      fire : function(target, type){

        if(document.createEvent){

          var evt = document.createEvent(a);
          evt.initEvent(type, true, true);
          return !target.dispatchEvent(evt);
        } else {

          var evt = document.createEventObject();
          return target.fireEvent(o + type, evt);
        };
      },
      supportsEvent : function(target, type){

        if(type.toLowerCase().indexOf(k) != -1 && qx.core.Environment.get(b) === d && qx.core.Environment.get(q) > 9){

          return true;
        };
        if(target != window && type.toLowerCase().indexOf(k) != -1){

          var transitionSupport = qx.core.Environment.get(n);
          return (transitionSupport && transitionSupport[r] == type);
        };
        var eventName = o + type.toLowerCase();
        var supportsEvent = (eventName in target);
        if(!supportsEvent){

          supportsEvent = typeof target[eventName] == h;
          if(!supportsEvent && target.setAttribute){

            target.setAttribute(eventName, g);
            supportsEvent = typeof target[eventName] == h;
            target.removeAttribute(eventName);
          };
        };
        return supportsEvent;
      },
      getEventName : function(target, type){

        var pref = [c].concat(qx.bom.Style.VENDOR_PREFIXES);
        for(var i = 0,l = pref.length;i < l;i++){

          var prefix = pref[i].toLowerCase();
          if(qx.bom.Event.supportsEvent(target, prefix + type)){

            return prefix ? prefix + qx.lang.String.firstUp(type) : type;
          };
        };
        return null;
      }
    }
  });
})();
(function(){

  var a = "-",b = "qx.bom.Style",c = "",d = '-',e = "Webkit",f = "ms",g = ":",h = ";",j = "Moz",k = "O",m = "string",n = "Khtml";
  qx.Bootstrap.define(b, {
    statics : {
      VENDOR_PREFIXES : [e, j, k, f, n],
      __cq : {
      },
      __cr : null,
      getPropertyName : function(propertyName){

        var style = document.documentElement.style;
        if(style[propertyName] !== undefined){

          return propertyName;
        };
        for(var i = 0,l = this.VENDOR_PREFIXES.length;i < l;i++){

          var prefixedProp = this.VENDOR_PREFIXES[i] + qx.lang.String.firstUp(propertyName);
          if(style[prefixedProp] !== undefined){

            return prefixedProp;
          };
        };
        return null;
      },
      getCssName : function(propertyName){

        var cssName = this.__cq[propertyName];
        if(!cssName){

          cssName = propertyName.replace(/[A-Z]/g, function(match){

            return (d + match.charAt(0).toLowerCase());
          });
          if((/^ms/.test(cssName))){

            cssName = a + cssName;
          };
          this.__cq[propertyName] = cssName;
        };
        return cssName;
      },
      getAppliedStyle : function(element, propertyName, value, prefixed){

        var cssProperty = qx.bom.Style.getCssName(propertyName);
        var win = qx.dom.Node.getWindow(element);
        var vendorPrefixes = (prefixed !== false) ? [null].concat(this.VENDOR_PREFIXES) : [null];
        for(var i = 0,l = vendorPrefixes.length;i < l;i++){

          var supported = false;
          var prefixedVal = vendorPrefixes[i] ? a + vendorPrefixes[i].toLowerCase() + a + value : value;
          if(qx.bom.Style.__cr){

            supported = qx.bom.Style.__cr.call(win, cssProperty, prefixedVal);
          } else {

            element.style.cssText += cssProperty + g + prefixedVal + h;
            supported = (typeof element.style[propertyName] == m && element.style[propertyName] !== c);
          };
          if(supported){

            return prefixedVal;
          };
        };
        return null;
      }
    },
    defer : function(statics){

      if(window.CSS && window.CSS.supports){

        qx.bom.Style.__cr = window.CSS.supports.bind(window.CSS);
      } else if(window.supportsCSS){

        qx.bom.Style.__cr = window.supportsCSS.bind(window);
      };
    }
  });
})();
(function(){

  var a = "rim_tabletos",b = "10.1",c = "Darwin",d = "10.3",e = "os.version",f = "10.7",g = "2003",h = ")",i = "iPhone",j = "android",k = "unix",l = "ce",m = "7",n = "SymbianOS",o = "10.5",p = "os.name",q = "10.9",r = "|",s = "MacPPC",t = "95",u = "iPod",v = "10.8",w = "\.",x = "Win64",y = "linux",z = "me",A = "10.2",B = "Macintosh",C = "Android",D = "Windows",E = "98",F = "ios",G = "vista",H = "8",I = "blackberry",J = "2000",K = "8.1",L = "(",M = "",N = "win",O = "Linux",P = "10.6",Q = "BSD",R = "10.0",S = "10.4",T = "Mac OS X",U = "iPad",V = "X11",W = "xp",X = "symbian",Y = "qx.bom.client.OperatingSystem",ba = "g",bb = "Win32",bc = "osx",bd = "webOS",be = "RIM Tablet OS",bf = "BlackBerry",bg = "nt4",bh = ".",bi = "MacIntel",bj = "webos";
  qx.Bootstrap.define(Y, {
    statics : {
      getName : function(){

        if(!navigator){

          return M;
        };
        var input = navigator.platform || M;
        var agent = navigator.userAgent || M;
        if(input.indexOf(D) != -1 || input.indexOf(bb) != -1 || input.indexOf(x) != -1){

          return N;
        } else if(input.indexOf(B) != -1 || input.indexOf(s) != -1 || input.indexOf(bi) != -1 || input.indexOf(T) != -1){

          return bc;
        } else if(agent.indexOf(be) != -1){

          return a;
        } else if(agent.indexOf(bd) != -1){

          return bj;
        } else if(input.indexOf(u) != -1 || input.indexOf(i) != -1 || input.indexOf(U) != -1){

          return F;
        } else if(agent.indexOf(C) != -1){

          return j;
        } else if(input.indexOf(O) != -1){

          return y;
        } else if(input.indexOf(V) != -1 || input.indexOf(Q) != -1 || input.indexOf(c) != -1){

          return k;
        } else if(input.indexOf(n) != -1){

          return X;
        } else if(input.indexOf(bf) != -1){

          return I;
        };;;;;;;;;
        return M;
      },
      __cs : {
        "Windows NT 6.3" : K,
        "Windows NT 6.2" : H,
        "Windows NT 6.1" : m,
        "Windows NT 6.0" : G,
        "Windows NT 5.2" : g,
        "Windows NT 5.1" : W,
        "Windows NT 5.0" : J,
        "Windows 2000" : J,
        "Windows NT 4.0" : bg,
        "Win 9x 4.90" : z,
        "Windows CE" : l,
        "Windows 98" : E,
        "Win98" : E,
        "Windows 95" : t,
        "Win95" : t,
        "Mac OS X 10_9" : q,
        "Mac OS X 10.9" : q,
        "Mac OS X 10_8" : v,
        "Mac OS X 10.8" : v,
        "Mac OS X 10_7" : f,
        "Mac OS X 10.7" : f,
        "Mac OS X 10_6" : P,
        "Mac OS X 10.6" : P,
        "Mac OS X 10_5" : o,
        "Mac OS X 10.5" : o,
        "Mac OS X 10_4" : S,
        "Mac OS X 10.4" : S,
        "Mac OS X 10_3" : d,
        "Mac OS X 10.3" : d,
        "Mac OS X 10_2" : A,
        "Mac OS X 10.2" : A,
        "Mac OS X 10_1" : b,
        "Mac OS X 10.1" : b,
        "Mac OS X 10_0" : R,
        "Mac OS X 10.0" : R
      },
      getVersion : function(){

        var version = qx.bom.client.OperatingSystem.__ct(navigator.userAgent);
        if(version == null){

          version = qx.bom.client.OperatingSystem.__cu(navigator.userAgent);
        };
        if(version != null){

          return version;
        } else {

          return M;
        };
      },
      __ct : function(userAgent){

        var str = [];
        for(var key in qx.bom.client.OperatingSystem.__cs){

          str.push(key);
        };
        var reg = new RegExp(L + str.join(r).replace(/\./g, w) + h, ba);
        var match = reg.exec(userAgent);
        if(match && match[1]){

          return qx.bom.client.OperatingSystem.__cs[match[1]];
        };
        return null;
      },
      __cu : function(userAgent){

        var android = userAgent.indexOf(C) != -1;
        var iOs = userAgent.match(/(iPad|iPhone|iPod)/i) ? true : false;
        if(android){

          var androidVersionRegExp = new RegExp(/ Android (\d+(?:\.\d+)+)/i);
          var androidMatch = androidVersionRegExp.exec(userAgent);
          if(androidMatch && androidMatch[1]){

            return androidMatch[1];
          };
        } else if(iOs){

          var iOsVersionRegExp = new RegExp(/(CPU|iPhone|iPod) OS (\d+)_(\d+)(?:_(\d+))*\s+/);
          var iOsMatch = iOsVersionRegExp.exec(userAgent);
          if(iOsMatch && iOsMatch[2] && iOsMatch[3]){

            if(iOsMatch[4]){

              return iOsMatch[2] + bh + iOsMatch[3] + bh + iOsMatch[4];
            } else {

              return iOsMatch[2] + bh + iOsMatch[3];
            };
          };
        };
        return null;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(p, statics.getName);
      qx.core.Environment.add(e, statics.getVersion);
    }
  });
})();
(function(){

  var a = "CSS1Compat",b = " OPR/",c = "msie",d = "android",e = "operamini",f = "gecko",g = "maple",h = "browser.quirksmode",i = "browser.name",j = "trident",k = "mobile chrome",l = ")(/| )([0-9]+\.[0-9])",m = "iemobile",n = "prism|Fennec|Camino|Kmeleon|Galeon|Netscape|SeaMonkey|Namoroka|Firefox",o = "IEMobile|Maxthon|MSIE|Trident",p = "opera mobi",q = "Mobile Safari",r = "Maple",s = "operamobile",t = "ie",u = "mobile safari",v = "AdobeAIR|Titanium|Fluid|Chrome|Android|Epiphany|Konqueror|iCab|OmniWeb|Maxthon|Pre|PhantomJS|Mobile Safari|Safari",w = "qx.bom.client.Browser",x = "(Maple )([0-9]+\.[0-9]+\.[0-9]*)",y = "",z = "opera mini",A = "(",B = "browser.version",C = "opera",D = "ce",E = "mshtml",F = "Opera Mini|Opera Mobi|Opera",G = "webkit",H = "browser.documentmode",I = "5.0",J = "Mobile/";
  qx.Bootstrap.define(w, {
    statics : {
      getName : function(){

        var agent = navigator.userAgent;
        var reg = new RegExp(A + qx.bom.client.Browser.__cv + l);
        var match = agent.match(reg);
        if(!match){

          return y;
        };
        var name = match[1].toLowerCase();
        var engine = qx.bom.client.Engine.getName();
        if(engine === G){

          if(name === d){

            name = k;
          } else if(agent.indexOf(q) !== -1 || agent.indexOf(J) !== -1){

            name = u;
          } else if(agent.indexOf(b) != -1){

            name = C;
          };;
        } else if(engine === E){

          if(name === c || name === j){

            name = t;
            if(qx.bom.client.OperatingSystem.getVersion() === D){

              name = m;
            };
          };
        } else if(engine === C){

          if(name === p){

            name = s;
          } else if(name === z){

            name = e;
          };
        } else if(engine === f){

          if(agent.indexOf(r) !== -1){

            name = g;
          };
        };;;
        return name;
      },
      getVersion : function(){

        var agent = navigator.userAgent;
        var reg = new RegExp(A + qx.bom.client.Browser.__cv + l);
        var match = agent.match(reg);
        if(!match){

          return y;
        };
        var name = match[1].toLowerCase();
        var version = match[3];
        if(agent.match(/Version(\/| )([0-9]+\.[0-9])/)){

          version = RegExp.$2;
        };
        if(qx.bom.client.Engine.getName() == E){

          version = qx.bom.client.Engine.getVersion();
          if(name === c && qx.bom.client.OperatingSystem.getVersion() == D){

            version = I;
          };
        };
        if(qx.bom.client.Browser.getName() == g){

          reg = new RegExp(x);
          match = agent.match(reg);
          if(!match){

            return y;
          };
          version = match[2];
        };
        if(qx.bom.client.Engine.getName() == G || qx.bom.client.Browser.getName() == C){

          if(agent.match(/OPR(\/| )([0-9]+\.[0-9])/)){

            version = RegExp.$2;
          };
        };
        return version;
      },
      getDocumentMode : function(){

        if(document.documentMode){

          return document.documentMode;
        };
        return 0;
      },
      getQuirksMode : function(){

        if(qx.bom.client.Engine.getName() == E && parseFloat(qx.bom.client.Engine.getVersion()) >= 8){

          return qx.bom.client.Engine.DOCUMENT_MODE === 5;
        } else {

          return document.compatMode !== a;
        };
      },
      __cv : {
        "webkit" : v,
        "gecko" : n,
        "mshtml" : o,
        "opera" : F
      }[qx.bom.client.Engine.getName()]
    },
    defer : function(statics){

      qx.core.Environment.add(i, statics.getName) , qx.core.Environment.add(B, statics.getVersion) , qx.core.Environment.add(H, statics.getDocumentMode) , qx.core.Environment.add(h, statics.getQuirksMode);
    }
  });
})();
(function(){

  var a = "qx.bom.client.CssTransition",b = "E",c = "transitionEnd",d = "e",e = "nd",f = "transition",g = "css.transition",h = "Trans";
  qx.Bootstrap.define(a, {
    statics : {
      getTransitionName : function(){

        return qx.bom.Style.getPropertyName(f);
      },
      getSupport : function(){

        var name = qx.bom.client.CssTransition.getTransitionName();
        if(!name){

          return null;
        };
        var eventName = qx.bom.Event.getEventName(window, c);
        eventName = eventName == c ? eventName.toLowerCase() : eventName;
        if(!eventName){

          eventName = name + (name.indexOf(h) > 0 ? b : d) + e;
        };
        return {
          name : name,
          "end-event" : eventName
        };
      }
    },
    defer : function(statics){

      qx.core.Environment.add(g, statics.getSupport);
    }
  });
})();
(function(){

  var a = "UNKNOWN_",b = "|bubble",c = "",d = "_",e = "c",f = "|",g = "__cA",h = "unload",j = "|capture",k = "DOM_",m = "__cB",n = "WIN_",o = "QX_",p = "qx.event.Manager",q = "capture",r = "DOCUMENT_";
  qx.Class.define(p, {
    extend : Object,
    construct : function(win, registration){

      this.__cw = win;
      this.__cx = qx.core.ObjectRegistry.toHashCode(win);
      this.__cy = registration;
      if(win.qx !== qx){

        var self = this;
        qx.bom.Event.addNativeListener(win, h, qx.event.GlobalError.observeMethod(function(){

          qx.bom.Event.removeNativeListener(win, h, arguments.callee);
          self.dispose();
        }));
      };
      this.__cz = {
      };
      this.__cA = {
      };
      this.__cB = {
      };
      this.__cC = {
      };
    },
    statics : {
      __cD : 0,
      getNextUniqueId : function(){

        return (this.__cD++) + c;
      }
    },
    members : {
      __cy : null,
      __cz : null,
      __cB : null,
      __cE : null,
      __cA : null,
      __cC : null,
      __cw : null,
      __cx : null,
      getWindow : function(){

        return this.__cw;
      },
      getWindowId : function(){

        return this.__cx;
      },
      getHandler : function(clazz){

        var handler = this.__cA[clazz.classname];
        if(handler){

          return handler;
        };
        return this.__cA[clazz.classname] = new clazz(this);
      },
      getDispatcher : function(clazz){

        var dispatcher = this.__cB[clazz.classname];
        if(dispatcher){

          return dispatcher;
        };
        return this.__cB[clazz.classname] = new clazz(this, this.__cy);
      },
      getListeners : function(target, type, capture){

        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(!targetMap){

          return null;
        };
        var entryKey = type + (capture ? j : b);
        var entryList = targetMap[entryKey];
        return entryList ? entryList.concat() : null;
      },
      getAllListeners : function(){

        return this.__cz;
      },
      serializeListeners : function(target){

        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        var result = [];
        if(targetMap){

          var indexOf,type,capture,entryList,entry;
          for(var entryKey in targetMap){

            indexOf = entryKey.indexOf(f);
            type = entryKey.substring(0, indexOf);
            capture = entryKey.charAt(indexOf + 1) == e;
            entryList = targetMap[entryKey];
            for(var i = 0,l = entryList.length;i < l;i++){

              entry = entryList[i];
              result.push({
                self : entry.context,
                handler : entry.handler,
                type : type,
                capture : capture
              });
            };
          };
        };
        return result;
      },
      toggleAttachedEvents : function(target, enable){

        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(targetMap){

          var indexOf,type,capture,entryList;
          for(var entryKey in targetMap){

            indexOf = entryKey.indexOf(f);
            type = entryKey.substring(0, indexOf);
            capture = entryKey.charCodeAt(indexOf + 1) === 99;
            entryList = targetMap[entryKey];
            if(enable){

              this.__cF(target, type, capture);
            } else {

              this.__cG(target, type, capture);
            };
          };
        };
      },
      hasListener : function(target, type, capture){

        {
        };
        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(!targetMap){

          return false;
        };
        var entryKey = type + (capture ? j : b);
        var entryList = targetMap[entryKey];
        return !!(entryList && entryList.length > 0);
      },
      importListeners : function(target, list){

        {
        };
        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey] = {
        };
        var clazz = qx.event.Manager;
        for(var listKey in list){

          var item = list[listKey];
          var entryKey = item.type + (item.capture ? j : b);
          var entryList = targetMap[entryKey];
          if(!entryList){

            entryList = targetMap[entryKey] = [];
            this.__cF(target, item.type, item.capture);
          };
          entryList.push({
            handler : item.listener,
            context : item.self,
            unique : item.unique || (clazz.__cD++) + c
          });
        };
      },
      addListener : function(target, type, listener, self, capture){

        {

          var msg;
        };
        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(!targetMap){

          targetMap = this.__cz[targetKey] = {
          };
        };
        var entryKey = type + (capture ? j : b);
        var entryList = targetMap[entryKey];
        if(!entryList){

          entryList = targetMap[entryKey] = [];
        };
        if(entryList.length === 0){

          this.__cF(target, type, capture);
        };
        var unique = (qx.event.Manager.__cD++) + c;
        var entry = {
          handler : listener,
          context : self,
          unique : unique
        };
        entryList.push(entry);
        return entryKey + f + unique;
      },
      findHandler : function(target, type){

        var isDomNode = false,isWindow = false,isObject = false,isDocument = false;
        var key;
        if(target.nodeType === 1){

          isDomNode = true;
          key = k + target.tagName.toLowerCase() + d + type;
        } else if(target.nodeType === 9){

          isDocument = true;
          key = r + type;
        } else if(target == this.__cw){

          isWindow = true;
          key = n + type;
        } else if(target.classname){

          isObject = true;
          key = o + target.classname + d + type;
        } else {

          key = a + target + d + type;
        };;;
        var cache = this.__cC;
        if(cache[key]){

          return cache[key];
        };
        var classes = this.__cy.getHandlers();
        var IEventHandler = qx.event.IEventHandler;
        var clazz,instance,supportedTypes,targetCheck;
        for(var i = 0,l = classes.length;i < l;i++){

          clazz = classes[i];
          supportedTypes = clazz.SUPPORTED_TYPES;
          if(supportedTypes && !supportedTypes[type]){

            continue;
          };
          targetCheck = clazz.TARGET_CHECK;
          if(targetCheck){

            var found = false;
            if(isDomNode && ((targetCheck & IEventHandler.TARGET_DOMNODE) != 0)){

              found = true;
            } else if(isWindow && ((targetCheck & IEventHandler.TARGET_WINDOW) != 0)){

              found = true;
            } else if(isObject && ((targetCheck & IEventHandler.TARGET_OBJECT) != 0)){

              found = true;
            } else if(isDocument && ((targetCheck & IEventHandler.TARGET_DOCUMENT) != 0)){

              found = true;
            };;;
            if(!found){

              continue;
            };
          };
          instance = this.getHandler(classes[i]);
          if(clazz.IGNORE_CAN_HANDLE || instance.canHandleEvent(target, type)){

            cache[key] = instance;
            return instance;
          };
        };
        return null;
      },
      __cF : function(target, type, capture){

        var handler = this.findHandler(target, type);
        if(handler){

          handler.registerEvent(target, type, capture);
          return;
        };
        {
        };
      },
      removeListener : function(target, type, listener, self, capture){

        {

          var msg;
        };
        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(!targetMap){

          return false;
        };
        var entryKey = type + (capture ? j : b);
        var entryList = targetMap[entryKey];
        if(!entryList){

          return false;
        };
        var entry;
        for(var i = 0,l = entryList.length;i < l;i++){

          entry = entryList[i];
          if(entry.handler === listener && entry.context === self){

            qx.lang.Array.removeAt(entryList, i);
            if(entryList.length == 0){

              this.__cG(target, type, capture);
            };
            return true;
          };
        };
        return false;
      },
      removeListenerById : function(target, id){

        {

          var msg;
        };
        var split = id.split(f);
        var type = split[0];
        var capture = split[1].charCodeAt(0) == 99;
        var unique = split[2];
        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(!targetMap){

          return false;
        };
        var entryKey = type + (capture ? j : b);
        var entryList = targetMap[entryKey];
        if(!entryList){

          return false;
        };
        var entry;
        for(var i = 0,l = entryList.length;i < l;i++){

          entry = entryList[i];
          if(entry.unique === unique){

            qx.lang.Array.removeAt(entryList, i);
            if(entryList.length == 0){

              this.__cG(target, type, capture);
            };
            return true;
          };
        };
        return false;
      },
      removeAllListeners : function(target){

        var targetKey = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var targetMap = this.__cz[targetKey];
        if(!targetMap){

          return false;
        };
        var split,type,capture;
        for(var entryKey in targetMap){

          if(targetMap[entryKey].length > 0){

            split = entryKey.split(f);
            type = split[0];
            capture = split[1] === q;
            this.__cG(target, type, capture);
          };
        };
        delete this.__cz[targetKey];
        return true;
      },
      deleteAllListeners : function(targetKey){

        delete this.__cz[targetKey];
      },
      __cG : function(target, type, capture){

        var handler = this.findHandler(target, type);
        if(handler){

          handler.unregisterEvent(target, type, capture);
          return;
        };
        {
        };
      },
      dispatchEvent : function(target, event){

        {

          var msg;
        };
        var type = event.getType();
        if(!event.getBubbles() && !this.hasListener(target, type)){

          qx.event.Pool.getInstance().poolObject(event);
          return true;
        };
        if(!event.getTarget()){

          event.setTarget(target);
        };
        var classes = this.__cy.getDispatchers();
        var instance;
        var dispatched = false;
        for(var i = 0,l = classes.length;i < l;i++){

          instance = this.getDispatcher(classes[i]);
          if(instance.canDispatchEvent(target, event, type)){

            instance.dispatchEvent(target, event, type);
            dispatched = true;
            break;
          };
        };
        if(!dispatched){

          {
          };
          return true;
        };
        var preventDefault = event.getDefaultPrevented();
        qx.event.Pool.getInstance().poolObject(event);
        return !preventDefault;
      },
      dispose : function(){

        this.__cy.removeManager(this);
        qx.util.DisposeUtil.disposeMap(this, g);
        qx.util.DisposeUtil.disposeMap(this, m);
        this.__cz = this.__cw = this.__cE = null;
        this.__cy = this.__cC = null;
      }
    }
  });
})();
(function(){

  var a = "qx.event.IEventHandler";
  qx.Interface.define(a, {
    statics : {
      TARGET_DOMNODE : 1,
      TARGET_WINDOW : 2,
      TARGET_OBJECT : 4,
      TARGET_DOCUMENT : 8
    },
    members : {
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      }
    }
  });
})();
(function(){

  var c = "qx.event.Registration";
  qx.Class.define(c, {
    statics : {
      __cH : {
      },
      getManager : function(target){

        if(target == null){

          {
          };
          target = window;
        } else if(target.nodeType){

          target = qx.dom.Node.getWindow(target);
        } else if(!qx.dom.Node.isWindow(target)){

          target = window;
        };;
        var hash = target.$$hash || qx.core.ObjectRegistry.toHashCode(target);
        var manager = this.__cH[hash];
        if(!manager){

          manager = new qx.event.Manager(target, this);
          this.__cH[hash] = manager;
        };
        return manager;
      },
      removeManager : function(mgr){

        var id = mgr.getWindowId();
        delete this.__cH[id];
      },
      addListener : function(target, type, listener, self, capture){

        return this.getManager(target).addListener(target, type, listener, self, capture);
      },
      removeListener : function(target, type, listener, self, capture){

        return this.getManager(target).removeListener(target, type, listener, self, capture);
      },
      removeListenerById : function(target, id){

        return this.getManager(target).removeListenerById(target, id);
      },
      removeAllListeners : function(target){

        return this.getManager(target).removeAllListeners(target);
      },
      deleteAllListeners : function(target){

        var targetKey = target.$$hash;
        if(targetKey){

          this.getManager(target).deleteAllListeners(targetKey);
        };
      },
      hasListener : function(target, type, capture){

        return this.getManager(target).hasListener(target, type, capture);
      },
      serializeListeners : function(target){

        return this.getManager(target).serializeListeners(target);
      },
      createEvent : function(type, clazz, args){

        {
        };
        if(clazz == null){

          clazz = qx.event.type.Event;
        };
        var obj = qx.event.Pool.getInstance().getObject(clazz);
        args ? obj.init.apply(obj, args) : obj.init();
        if(type){

          obj.setType(type);
        };
        return obj;
      },
      dispatchEvent : function(target, event){

        return this.getManager(target).dispatchEvent(target, event);
      },
      fireEvent : function(target, type, clazz, args){

        {

          var msg;
        };
        var evt = this.createEvent(type, clazz || null, args);
        return this.getManager(target).dispatchEvent(target, evt);
      },
      fireNonBubblingEvent : function(target, type, clazz, args){

        {
        };
        var mgr = this.getManager(target);
        if(!mgr.hasListener(target, type, false)){

          return true;
        };
        var evt = this.createEvent(type, clazz || null, args);
        return mgr.dispatchEvent(target, evt);
      },
      PRIORITY_FIRST : -32000,
      PRIORITY_NORMAL : 0,
      PRIORITY_LAST : 32000,
      __cA : [],
      addHandler : function(handler){

        {
        };
        this.__cA.push(handler);
        this.__cA.sort(function(a, b){

          return a.PRIORITY - b.PRIORITY;
        });
      },
      getHandlers : function(){

        return this.__cA;
      },
      __cB : [],
      addDispatcher : function(dispatcher, priority){

        {
        };
        this.__cB.push(dispatcher);
        this.__cB.sort(function(a, b){

          return a.PRIORITY - b.PRIORITY;
        });
      },
      getDispatchers : function(){

        return this.__cB;
      }
    }
  });
})();
(function(){

  var a = "qx.core.MEvent";
  qx.Mixin.define(a, {
    members : {
      __cI : qx.event.Registration,
      addListener : function(type, listener, self, capture){

        if(!this.$$disposed){

          return this.__cI.addListener(this, type, listener, self, capture);
        };
        return null;
      },
      addListenerOnce : function(type, listener, self, capture){

        var callback = function(e){

          this.removeListener(type, listener, this, capture);
          listener.call(self || this, e);
        };
        listener.$$wrapped_callback = callback;
        return this.addListener(type, callback, this, capture);
      },
      removeListener : function(type, listener, self, capture){

        if(!this.$$disposed){

          if(listener.$$wrapped_callback){

            var callback = listener.$$wrapped_callback;
            delete listener.$$wrapped_callback;
            listener = callback;
          };
          return this.__cI.removeListener(this, type, listener, self, capture);
        };
        return false;
      },
      removeListenerById : function(id){

        if(!this.$$disposed){

          return this.__cI.removeListenerById(this, id);
        };
        return false;
      },
      hasListener : function(type, capture){

        return this.__cI.hasListener(this, type, capture);
      },
      dispatchEvent : function(evt){

        if(!this.$$disposed){

          return this.__cI.dispatchEvent(this, evt);
        };
        return true;
      },
      fireEvent : function(type, clazz, args){

        if(!this.$$disposed){

          return this.__cI.fireEvent(this, type, clazz, args);
        };
        return true;
      },
      fireNonBubblingEvent : function(type, clazz, args){

        if(!this.$$disposed){

          return this.__cI.fireNonBubblingEvent(this, type, clazz, args);
        };
        return true;
      },
      fireDataEvent : function(type, data, oldData, cancelable){

        if(!this.$$disposed){

          if(oldData === undefined){

            oldData = null;
          };
          return this.__cI.fireNonBubblingEvent(this, type, qx.event.type.Data, [data, oldData, !!cancelable]);
        };
        return true;
      }
    }
  });
})();
(function(){

  var a = "qx.event.IEventDispatcher";
  qx.Interface.define(a, {
    members : {
      canDispatchEvent : function(target, event, type){

        this.assertInstance(event, qx.event.type.Event);
        this.assertString(type);
      },
      dispatchEvent : function(target, event, type){

        this.assertInstance(event, qx.event.type.Event);
        this.assertString(type);
      }
    }
  });
})();
(function(){

  var a = "module.events",b = "Cloning only possible with properties.",c = "qx.core.Object",d = "module.property",e = "]",f = "[",g = "Object";
  qx.Class.define(c, {
    extend : Object,
    include : qx.core.Environment.filter({
      "module.databinding" : qx.data.MBinding,
      "module.logger" : qx.core.MLogging,
      "module.events" : qx.core.MEvent,
      "module.property" : qx.core.MProperty
    }),
    construct : function(){

      qx.core.ObjectRegistry.register(this);
    },
    statics : {
      $$type : g
    },
    members : {
      __M : qx.core.Environment.get(d) ? qx.core.Property : null,
      toHashCode : function(){

        return this.$$hash;
      },
      toString : function(){

        return this.classname + f + this.$$hash + e;
      },
      base : function(args, varargs){

        {
        };
        if(arguments.length === 1){

          return args.callee.base.call(this);
        } else {

          return args.callee.base.apply(this, Array.prototype.slice.call(arguments, 1));
        };
      },
      self : function(args){

        return args.callee.self;
      },
      clone : function(){

        if(!qx.core.Environment.get(d)){

          throw new Error(b);
        };
        var clazz = this.constructor;
        var clone = new clazz;
        var props = qx.Class.getProperties(clazz);
        var user = this.__M.$$store.user;
        var setter = this.__M.$$method.set;
        var name;
        for(var i = 0,l = props.length;i < l;i++){

          name = props[i];
          if(this.hasOwnProperty(user[name])){

            clone[setter[name]](this[user[name]]);
          };
        };
        return clone;
      },
      __cJ : null,
      setUserData : function(key, value){

        if(!this.__cJ){

          this.__cJ = {
          };
        };
        this.__cJ[key] = value;
      },
      getUserData : function(key){

        if(!this.__cJ){

          return null;
        };
        var data = this.__cJ[key];
        return data === undefined ? null : data;
      },
      isDisposed : function(){

        return this.$$disposed || false;
      },
      dispose : function(){

        if(this.$$disposed){

          return;
        };
        this.$$disposed = true;
        this.$$instance = null;
        this.$$allowconstruct = null;
        {
        };
        var clazz = this.constructor;
        var mixins;
        while(clazz.superclass){

          if(clazz.$$destructor){

            clazz.$$destructor.call(this);
          };
          if(clazz.$$includes){

            mixins = clazz.$$flatIncludes;
            for(var i = 0,l = mixins.length;i < l;i++){

              if(mixins[i].$$destructor){

                mixins[i].$$destructor.call(this);
              };
            };
          };
          clazz = clazz.superclass;
        };
        {

          var key,value;
        };
      },
      _disposeObjects : function(varargs){

        qx.util.DisposeUtil.disposeObjects(this, arguments);
      },
      _disposeSingletonObjects : function(varargs){

        qx.util.DisposeUtil.disposeObjects(this, arguments, true);
      },
      _disposeArray : function(field){

        qx.util.DisposeUtil.disposeArray(this, field);
      },
      _disposeMap : function(field){

        qx.util.DisposeUtil.disposeMap(this, field);
      }
    },
    environment : {
      "qx.debug.dispose.level" : 0
    },
    destruct : function(){

      if(qx.core.Environment.get(a)){

        if(!qx.core.ObjectRegistry.inShutDown){

          qx.event.Registration.removeAllListeners(this);
        } else {

          qx.event.Registration.deleteAllListeners(this);
        };
      };
      qx.core.ObjectRegistry.unregister(this);
      this.__cJ = null;
      if(qx.core.Environment.get(d)){

        var clazz = this.constructor;
        var properties;
        var store = this.__M.$$store;
        var storeUser = store.user;
        var storeTheme = store.theme;
        var storeInherit = store.inherit;
        var storeUseinit = store.useinit;
        var storeInit = store.init;
        while(clazz){

          properties = clazz.$$properties;
          if(properties){

            for(var name in properties){

              if(properties[name].dereference){

                this[storeUser[name]] = this[storeTheme[name]] = this[storeInherit[name]] = this[storeUseinit[name]] = this[storeInit[name]] = undefined;
              };
            };
          };
          clazz = clazz.superclass;
        };
      };
    }
  });
})();
(function(){

  var a = " is a singleton! Please use disposeSingleton instead.",b = "undefined",c = "qx.util.DisposeUtil",d = " of object: ",e = "!",f = " has non disposable entries: ",g = "The map field: ",h = "The array field: ",j = "The object stored in key ",k = "Has no disposable object under key: ";
  qx.Class.define(c, {
    statics : {
      disposeObjects : function(obj, arr, disposeSingletons){

        var name;
        for(var i = 0,l = arr.length;i < l;i++){

          name = arr[i];
          if(obj[name] == null || !obj.hasOwnProperty(name)){

            continue;
          };
          if(!qx.core.ObjectRegistry.inShutDown){

            if(obj[name].dispose){

              if(!disposeSingletons && obj[name].constructor.$$instance){

                throw new Error(j + name + a);
              } else {

                obj[name].dispose();
              };
            } else {

              throw new Error(k + name + e);
            };
          };
          obj[name] = null;
        };
      },
      disposeArray : function(obj, field){

        var data = obj[field];
        if(!data){

          return;
        };
        if(qx.core.ObjectRegistry.inShutDown){

          obj[field] = null;
          return;
        };
        try{

          var entry;
          for(var i = data.length - 1;i >= 0;i--){

            entry = data[i];
            if(entry){

              entry.dispose();
            };
          };
        } catch(ex) {

          throw new Error(h + field + d + obj + f + ex);
        };
        data.length = 0;
        obj[field] = null;
      },
      disposeMap : function(obj, field){

        var data = obj[field];
        if(!data){

          return;
        };
        if(qx.core.ObjectRegistry.inShutDown){

          obj[field] = null;
          return;
        };
        try{

          var entry;
          for(var key in data){

            entry = data[key];
            if(data.hasOwnProperty(key) && entry){

              entry.dispose();
            };
          };
        } catch(ex) {

          throw new Error(g + field + d + obj + f + ex);
        };
        obj[field] = null;
      },
      disposeTriggeredBy : function(disposeMe, trigger){

        var triggerDispose = trigger.dispose;
        trigger.dispose = function(){

          triggerDispose.call(trigger);
          disposeMe.dispose();
        };
      },
      destroyContainer : function(container){

        {
        };
        var arr = [];
        this._collectContainerChildren(container, arr);
        var len = arr.length;
        for(var i = len - 1;i >= 0;i--){

          arr[i].destroy();
        };
        container.destroy();
      },
      _collectContainerChildren : function(container, arr){

        var children = container.getChildren();
        for(var i = 0;i < children.length;i++){

          var item = children[i];
          arr.push(item);
          if(this.__cK(item)){

            this._collectContainerChildren(item, arr);
          };
        };
      },
      __cK : function(obj){

        var classes = [qx.ui.container.Composite, qx.ui.container.Scroll, qx.ui.container.SlideBar, qx.ui.container.Stack];
        for(var i = 0,l = classes.length;i < l;i++){

          if(typeof classes[i] !== b && qx.Class.isSubClassOf(obj.constructor, classes[i])){

            return true;
          };
        };
        return false;
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Event";
  qx.Class.define(a, {
    extend : qx.core.Object,
    statics : {
      CAPTURING_PHASE : 1,
      AT_TARGET : 2,
      BUBBLING_PHASE : 3
    },
    members : {
      init : function(canBubble, cancelable){

        {
        };
        this._type = null;
        this._target = null;
        this._currentTarget = null;
        this._relatedTarget = null;
        this._originalTarget = null;
        this._stopPropagation = false;
        this._preventDefault = false;
        this._bubbles = !!canBubble;
        this._cancelable = !!cancelable;
        this._timeStamp = (new Date()).getTime();
        this._eventPhase = null;
        return this;
      },
      clone : function(embryo){

        if(embryo){

          var clone = embryo;
        } else {

          var clone = qx.event.Pool.getInstance().getObject(this.constructor);
        };
        clone._type = this._type;
        clone._target = this._target;
        clone._currentTarget = this._currentTarget;
        clone._relatedTarget = this._relatedTarget;
        clone._originalTarget = this._originalTarget;
        clone._stopPropagation = this._stopPropagation;
        clone._bubbles = this._bubbles;
        clone._preventDefault = this._preventDefault;
        clone._cancelable = this._cancelable;
        return clone;
      },
      stop : function(){

        if(this._bubbles){

          this.stopPropagation();
        };
        if(this._cancelable){

          this.preventDefault();
        };
      },
      stopPropagation : function(){

        {
        };
        this._stopPropagation = true;
      },
      getPropagationStopped : function(){

        return !!this._stopPropagation;
      },
      preventDefault : function(){

        {
        };
        this._preventDefault = true;
      },
      getDefaultPrevented : function(){

        return !!this._preventDefault;
      },
      getType : function(){

        return this._type;
      },
      setType : function(type){

        this._type = type;
      },
      getEventPhase : function(){

        return this._eventPhase;
      },
      setEventPhase : function(eventPhase){

        this._eventPhase = eventPhase;
      },
      getTimeStamp : function(){

        return this._timeStamp;
      },
      getTarget : function(){

        return this._target;
      },
      setTarget : function(target){

        this._target = target;
      },
      getCurrentTarget : function(){

        return this._currentTarget || this._target;
      },
      setCurrentTarget : function(currentTarget){

        this._currentTarget = currentTarget;
      },
      getRelatedTarget : function(){

        return this._relatedTarget;
      },
      setRelatedTarget : function(relatedTarget){

        this._relatedTarget = relatedTarget;
      },
      getOriginalTarget : function(){

        return this._originalTarget;
      },
      setOriginalTarget : function(originalTarget){

        this._originalTarget = originalTarget;
      },
      getBubbles : function(){

        return this._bubbles;
      },
      setBubbles : function(bubbles){

        this._bubbles = bubbles;
      },
      isCancelable : function(){

        return this._cancelable;
      },
      setCancelable : function(cancelable){

        this._cancelable = cancelable;
      }
    },
    destruct : function(){

      this._target = this._currentTarget = this._relatedTarget = this._originalTarget = null;
    }
  });
})();
(function(){

  var a = "qx.util.ObjectPool",b = "Class needs to be defined!",c = "Object is already pooled: ",d = "Integer";
  qx.Class.define(a, {
    extend : qx.core.Object,
    construct : function(size){

      qx.core.Object.call(this);
      this.__cL = {
      };
      if(size != null){

        this.setSize(size);
      };
    },
    properties : {
      size : {
        check : d,
        init : Infinity
      }
    },
    members : {
      __cL : null,
      getObject : function(clazz){

        if(this.$$disposed){

          return new clazz;
        };
        if(!clazz){

          throw new Error(b);
        };
        var obj = null;
        var pool = this.__cL[clazz.classname];
        if(pool){

          obj = pool.pop();
        };
        if(obj){

          obj.$$pooled = false;
        } else {

          obj = new clazz;
        };
        return obj;
      },
      poolObject : function(obj){

        if(!this.__cL){

          return;
        };
        var classname = obj.classname;
        var pool = this.__cL[classname];
        if(obj.$$pooled){

          throw new Error(c + obj);
        };
        if(!pool){

          this.__cL[classname] = pool = [];
        };
        if(pool.length > this.getSize()){

          if(obj.destroy){

            obj.destroy();
          } else {

            obj.dispose();
          };
          return;
        };
        obj.$$pooled = true;
        pool.push(obj);
      }
    },
    destruct : function(){

      var pool = this.__cL;
      var classname,list,i,l;
      for(classname in pool){

        list = pool[classname];
        for(i = 0,l = list.length;i < l;i++){

          list[i].dispose();
        };
      };
      delete this.__cL;
    }
  });
})();
(function(){

  var a = "singleton",b = "qx.event.Pool";
  qx.Class.define(b, {
    extend : qx.util.ObjectPool,
    type : a,
    construct : function(){

      qx.util.ObjectPool.call(this, 30);
    }
  });
})();
(function(){

  var a = "qx.event.dispatch.Direct";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventDispatcher,
    construct : function(manager){

      this._manager = manager;
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_LAST
    },
    members : {
      canDispatchEvent : function(target, event, type){

        return !event.getBubbles();
      },
      dispatchEvent : function(target, event, type){

        {

          var expectedEventClassName,expectedEventClass;
        };
        event.setEventPhase(qx.event.type.Event.AT_TARGET);
        var listeners = this._manager.getListeners(target, type, false);
        if(listeners){

          for(var i = 0,l = listeners.length;i < l;i++){

            var context = listeners[i].context || target;
            {
            };
            listeners[i].handler.call(context, event);
          };
        };
      }
    },
    defer : function(statics){

      qx.event.Registration.addDispatcher(statics);
    }
  });
})();
(function(){

  var a = "qx.event.handler.Object";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_LAST,
      SUPPORTED_TYPES : null,
      TARGET_CHECK : qx.event.IEventHandler.TARGET_OBJECT,
      IGNORE_CAN_HANDLE : false
    },
    members : {
      canHandleEvent : function(target, type){

        return qx.Class.supportsEvent(target.constructor, type);
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      }
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "qx.event.type.Data";
  qx.Class.define(a, {
    extend : qx.event.type.Event,
    members : {
      __cM : null,
      __cN : null,
      init : function(data, old, cancelable){

        qx.event.type.Event.prototype.init.call(this, false, cancelable);
        this.__cM = data;
        this.__cN = old;
        return this;
      },
      clone : function(embryo){

        var clone = qx.event.type.Event.prototype.clone.call(this, embryo);
        clone.__cM = this.__cM;
        clone.__cN = this.__cN;
        return clone;
      },
      getData : function(){

        return this.__cM;
      },
      getOldData : function(){

        return this.__cN;
      }
    },
    destruct : function(){

      this.__cM = this.__cN = null;
    }
  });
})();
(function(){

  var a = "To enable localization please include qx.locale.Manager into your build!",b = "qx.locale.MTranslation";
  qx.Mixin.define(b, {
    members : {
      tr : function(messageId, varargs){

        var nlsManager = qx.locale.Manager;
        if(nlsManager){

          return nlsManager.tr.apply(nlsManager, arguments);
        };
        throw new Error(a);
      },
      trn : function(singularMessageId, pluralMessageId, count, varargs){

        var nlsManager = qx.locale.Manager;
        if(nlsManager){

          return nlsManager.trn.apply(nlsManager, arguments);
        };
        throw new Error(a);
      },
      trc : function(hint, messageId, varargs){

        var nlsManager = qx.locale.Manager;
        if(nlsManager){

          return nlsManager.trc.apply(nlsManager, arguments);
        };
        throw new Error(a);
      },
      marktr : function(messageId){

        var nlsManager = qx.locale.Manager;
        if(nlsManager){

          return nlsManager.marktr.apply(nlsManager, arguments);
        };
        throw new Error(a);
      }
    }
  });
})();
(function(){

  var a = "qx.application.IApplication";
  qx.Interface.define(a, {
    members : {
      main : function(){
      },
      finalize : function(){
      },
      close : function(){
      },
      terminate : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.core.BaseInit",b = "engine.name",c = "Main runtime: ",d = "",f = "os.name",g = "engine.version",h = "Missing application class: ",i = "Load runtime: ",j = "ms",k = "Could not detect engine!",l = 'kirra_qooxdoo.Application',m = "Finalize runtime: ",n = "Could not detect operating system!",o = "Could not detect the version of the engine!";
  qx.Class.define(a, {
    statics : {
      __cO : null,
      getApplication : function(){

        return this.__cO || null;
      },
      ready : function(){

        if(this.__cO){

          return;
        };
        if(qx.core.Environment.get(b) == d){

          qx.log.Logger.warn(k);
        };
        if(qx.core.Environment.get(g) == d){

          qx.log.Logger.warn(o);
        };
        if(qx.core.Environment.get(f) == d){

          qx.log.Logger.warn(n);
        };
        qx.log.Logger.debug(this, i + (new Date - qx.Bootstrap.LOADSTART) + j);
        var app = l;
        var clazz = qx.Class.getByName(app);
        if(clazz){

          this.__cO = new clazz;
          var start = new Date;
          this.__cO.main();
          qx.log.Logger.debug(this, c + (new Date - start) + j);
          var start = new Date;
          this.__cO.finalize();
          qx.log.Logger.debug(this, m + (new Date - start) + j);
        } else {

          qx.log.Logger.warn(h + app);
        };
      },
      __cP : function(e){

        var app = this.__cO;
        if(app){

          app.close();
        };
      },
      __cQ : function(){

        var app = this.__cO;
        if(app){

          app.terminate();
        };
        qx.core.ObjectRegistry.shutdown();
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Native";
  qx.Class.define(a, {
    extend : qx.event.type.Event,
    members : {
      init : function(nativeEvent, target, relatedTarget, canBubble, cancelable){

        qx.event.type.Event.prototype.init.call(this, canBubble, cancelable);
        this._target = target || qx.bom.Event.getTarget(nativeEvent);
        this._relatedTarget = relatedTarget || qx.bom.Event.getRelatedTarget(nativeEvent);
        if(nativeEvent.timeStamp){

          this._timeStamp = nativeEvent.timeStamp;
        };
        this._native = nativeEvent;
        this._returnValue = null;
        return this;
      },
      clone : function(embryo){

        var clone = qx.event.type.Event.prototype.clone.call(this, embryo);
        var nativeClone = {
        };
        clone._native = this._cloneNativeEvent(this._native, nativeClone);
        clone._returnValue = this._returnValue;
        return clone;
      },
      _cloneNativeEvent : function(nativeEvent, clone){

        clone.preventDefault = (function(){
        });
        return clone;
      },
      preventDefault : function(){

        qx.event.type.Event.prototype.preventDefault.call(this);
        qx.bom.Event.preventDefault(this._native);
      },
      getNativeEvent : function(){

        return this._native;
      },
      setReturnValue : function(returnValue){

        this._returnValue = returnValue;
      },
      getReturnValue : function(){

        return this._returnValue;
      }
    },
    destruct : function(){

      this._native = this._returnValue = null;
    }
  });
})();
(function(){

  var a = "qx.event.handler.Window";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this._manager = manager;
      this._window = manager.getWindow();
      this._initWindowObserver();
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        error : 1,
        load : 1,
        beforeunload : 1,
        unload : 1,
        resize : 1,
        scroll : 1,
        beforeshutdown : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      _initWindowObserver : function(){

        this._onNativeWrapper = qx.lang.Function.listener(this._onNative, this);
        var types = qx.event.handler.Window.SUPPORTED_TYPES;
        for(var key in types){

          qx.bom.Event.addNativeListener(this._window, key, this._onNativeWrapper);
        };
      },
      _stopWindowObserver : function(){

        var types = qx.event.handler.Window.SUPPORTED_TYPES;
        for(var key in types){

          qx.bom.Event.removeNativeListener(this._window, key, this._onNativeWrapper);
        };
      },
      _onNative : qx.event.GlobalError.observeMethod(function(e){

        if(this.isDisposed()){

          return;
        };
        var win = this._window;
        try{

          var doc = win.document;
        } catch(ex) {

          return;
        };
        var html = doc.documentElement;
        var target = qx.bom.Event.getTarget(e);
        if(target == null || target === win || target === doc || target === html){

          var event = qx.event.Registration.createEvent(e.type, qx.event.type.Native, [e, win]);
          qx.event.Registration.dispatchEvent(win, event);
          var result = event.getReturnValue();
          if(result != null){

            e.returnValue = result;
            return result;
          };
        };
      })
    },
    destruct : function(){

      this._stopWindowObserver();
      this._manager = this._window = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "ready",b = "mshtml",c = "engine.name",d = "qx.event.handler.Application",f = "complete",g = "webkit",h = "gecko",i = "load",j = "unload",k = "opera",l = "left",m = "DOMContentLoaded",n = "shutdown",o = "browser.documentmode";
  qx.Class.define(d, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this._window = manager.getWindow();
      this.__cR = false;
      this.__cS = false;
      this.__cT = false;
      this.__cU = false;
      this._initObserver();
      qx.event.handler.Application.$$instance = this;
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        ready : 1,
        shutdown : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true,
      onScriptLoaded : function(){

        var inst = qx.event.handler.Application.$$instance;
        if(inst){

          inst.__cV();
        };
      }
    },
    members : {
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      __cT : null,
      __cR : null,
      __cS : null,
      __cU : null,
      __cV : function(){

        if(!this.__cT && this.__cR && qx.$$loader.scriptLoaded){

          if((qx.core.Environment.get(c) == b)){

            if(qx.event.Registration.hasListener(this._window, a)){

              this.__cT = true;
              qx.event.Registration.fireEvent(this._window, a);
            };
          } else {

            this.__cT = true;
            qx.event.Registration.fireEvent(this._window, a);
          };
        };
      },
      isApplicationReady : function(){

        return this.__cT;
      },
      _initObserver : function(){

        if(qx.$$domReady || document.readyState == f || document.readyState == a){

          this.__cR = true;
          this.__cV();
        } else {

          this._onNativeLoadWrapped = qx.lang.Function.bind(this._onNativeLoad, this);
          if(qx.core.Environment.get(c) == h || qx.core.Environment.get(c) == k || qx.core.Environment.get(c) == g || (qx.core.Environment.get(c) == b && qx.core.Environment.get(o) > 8)){

            qx.bom.Event.addNativeListener(this._window, m, this._onNativeLoadWrapped);
          } else {

            var self = this;
            var timer = function(){

              try{

                document.documentElement.doScroll(l);
                if(document.body){

                  self._onNativeLoadWrapped();
                };
              } catch(error) {

                window.setTimeout(timer, 100);
              };
            };
            timer();
          };
          qx.bom.Event.addNativeListener(this._window, i, this._onNativeLoadWrapped);
        };
        this._onNativeUnloadWrapped = qx.lang.Function.bind(this._onNativeUnload, this);
        qx.bom.Event.addNativeListener(this._window, j, this._onNativeUnloadWrapped);
      },
      _stopObserver : function(){

        if(this._onNativeLoadWrapped){

          qx.bom.Event.removeNativeListener(this._window, i, this._onNativeLoadWrapped);
        };
        qx.bom.Event.removeNativeListener(this._window, j, this._onNativeUnloadWrapped);
        this._onNativeLoadWrapped = null;
        this._onNativeUnloadWrapped = null;
      },
      _onNativeLoad : qx.event.GlobalError.observeMethod(function(){

        this.__cR = true;
        this.__cV();
      }),
      _onNativeUnload : qx.event.GlobalError.observeMethod(function(){

        if(!this.__cU){

          this.__cU = true;
          try{

            qx.event.Registration.fireEvent(this._window, n);
          } catch(e) {

            throw e;
          }finally{

            qx.core.ObjectRegistry.shutdown();
          };
        };
      })
    },
    destruct : function(){

      this._stopObserver();
      this._window = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "ready",b = "shutdown",c = "beforeunload",d = "qx.core.Init";
  qx.Class.define(d, {
    statics : {
      getApplication : qx.core.BaseInit.getApplication,
      ready : qx.core.BaseInit.ready,
      __cP : function(e){

        var app = this.getApplication();
        if(app){

          e.setReturnValue(app.close());
        };
      },
      __cQ : function(){

        var app = this.getApplication();
        if(app){

          app.terminate();
        };
      }
    },
    defer : function(statics){

      qx.event.Registration.addListener(window, a, statics.ready, statics);
      qx.event.Registration.addListener(window, b, statics.__cQ, statics);
      qx.event.Registration.addListener(window, c, statics.__cP, statics);
    }
  });
})();
(function(){

  var a = "qx.application.Mobile",b = "qx.mobile.nativescroll";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : [qx.application.IApplication],
    include : qx.locale.MTranslation,
    construct : function(){

      qx.core.Object.call(this);
    },
    members : {
      __a : null,
      main : function(){

        this.__a = this._createRootWidget();
        if(qx.core.Environment.get(b) == false){

          this.__a.setShowScrollbarY(false);
        };
      },
      getRoot : function(){

        return this.__a;
      },
      _createRootWidget : function(){

        return new qx.ui.mobile.core.Root();
      },
      finalize : function(){
      },
      close : function(){
      },
      terminate : function(){
      }
    },
    destruct : function(){

      this.__a = null;
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.core.MLayoutHandling";
  qx.Mixin.define(a, {
    members : {
      setLayout : function(layout){

        this._setLayout(layout);
      },
      getLayout : function(){

        return this._getLayout();
      }
    },
    statics : {
      remap : function(members){

        members.getLayout = members._getLayout;
        members.setLayout = members._setLayout;
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.core.MChildrenHandling";
  qx.Mixin.define(a, {
    members : {
      getChildren : function(){

        return this._getChildren();
      },
      hasChildren : function(){

        return this._hasChildren();
      },
      indexOf : function(child){

        return this._indexOf(child);
      },
      add : function(child, layoutProperties){

        this._add(child, layoutProperties);
      },
      addAt : function(child, index, options){

        this._addAt(child, index, options);
      },
      addBefore : function(child, before, layoutProperties){

        this._addBefore(child, before, layoutProperties);
      },
      addAfter : function(child, after, layoutProperties){

        this._addAfter(child, after, layoutProperties);
      },
      remove : function(child){

        this._remove(child);
      },
      removeAt : function(index){

        this._removeAt(index);
      },
      removeAll : function(){

        this._removeAll();
      }
    },
    statics : {
      remap : function(members){

        members.getChildren = members._getChildren;
        members.hasChildren = members._hasChildren;
        members.indexOf = members._indexOf;
        members.add = members._add;
        members.addAt = members._addAt;
        members.addBefore = members._addBefore;
        members.addAfter = members._addAfter;
        members.remove = members._remove;
        members.removeAt = members._removeAt;
        members.removeAll = members._removeAll;
      }
    }
  });
})();
(function(){

  var a = "ready",b = "qx.bom.Lifecycle",c = "shutdown";
  qx.Class.define(b, {
    statics : {
      onReady : function(callback, context){

        var Registration = qx.event.Registration;
        var appHandler = Registration.getManager(window).getHandler(qx.event.handler.Application);
        if(appHandler && appHandler.isApplicationReady()){

          callback.call(context);
        } else {

          Registration.addListener(window, a, callback, context);
        };
      },
      onShutdown : function(callback, context){

        qx.event.Registration.addListener(window, c, callback, context);
      }
    }
  });
})();
(function(){

  var a = "changeVisibility",b = "_applyAttribute",c = "Boolean",d = "px",e = "qx.event.type.Mouse",f = "true",g = "excluded",h = "div",j = "qx.event.type.Tap",k = "css.transform.3d",m = "readonly",n = "exclude",o = "changeId",p = "pointerEvents",q = "data-selectable",r = "qx.event.type.KeyInput",s = "visible",t = "_transformId",u = "disabled",v = "qx.event.type.MouseWheel",w = "qx.event.type.KeySequence",x = "_applyEnabled",y = "String",z = "_applyId",A = ") ",B = "changeEnabled",C = "_transform",D = "",E = "visibility",F = "_applyDefaultCssClass",G = "data-activatable",H = "qx.event.type.Focus",I = "rem",J = "false",K = "This widget has no children!",L = "deg) ",M = "undefined",N = "qx_id_",O = "none",P = "Number",Q = "translate(",R = "rotate(",S = "translate3d(",T = "qx.ui.mobile.core.Widget",U = "_applyStyle",V = 'anonymous',W = "qx.event.type.Event",X = "hidden",Y = "transform",ba = "qx.event.type.Touch",bb = "_applyVisibility",bc = "scale(",bd = ",";
  qx.Class.define(T, {
    extend : qx.core.Object,
    include : [qx.locale.MTranslation],
    construct : function(){

      qx.core.Object.call(this);
      this._setContainerElement(this._createContainerElement());
      this.__cW = [];
      this.setId(this.getId());
      this.initDefaultCssClass();
      this.initName();
      this.initAnonymous();
      this.initActivatable();
    },
    events : {
      mousemove : e,
      mouseover : e,
      mouseout : e,
      mousedown : e,
      mouseup : e,
      click : e,
      dblclick : e,
      contextmenu : e,
      beforeContextmenuOpen : e,
      mousewheel : v,
      touchstart : ba,
      touchend : ba,
      touchmove : ba,
      touchcancel : ba,
      tap : j,
      longtap : j,
      swipe : ba,
      keyup : w,
      keydown : w,
      keypress : w,
      keyinput : r,
      domupdated : W,
      appear : W,
      disappear : W,
      focus : H,
      blur : H,
      focusin : H,
      focusout : H,
      activate : H,
      deactivate : H
    },
    properties : {
      defaultCssClass : {
        check : y,
        init : null,
        nullable : true,
        apply : F
      },
      enabled : {
        init : true,
        check : c,
        nullable : false,
        event : B,
        apply : x
      },
      name : {
        check : y,
        init : null,
        nullable : true,
        apply : b
      },
      anonymous : {
        check : c,
        init : null,
        nullable : true,
        apply : U
      },
      id : {
        check : y,
        init : null,
        nullable : true,
        apply : z,
        transform : t,
        event : o
      },
      visibility : {
        check : [s, X, g],
        init : s,
        apply : bb,
        event : a
      },
      activatable : {
        check : c,
        init : false,
        apply : b
      },
      rotation : {
        check : P,
        nullable : true,
        init : null,
        apply : C
      },
      transformUnit : {
        check : [I, d],
        nullable : false,
        init : I,
        apply : C
      },
      scaleX : {
        check : P,
        nullable : true,
        init : null,
        apply : C
      },
      scaleY : {
        check : P,
        nullable : true,
        init : null,
        apply : C
      },
      translateX : {
        check : P,
        nullable : true,
        init : 0,
        apply : C
      },
      translateY : {
        check : P,
        nullable : true,
        init : 0,
        apply : C
      },
      translateZ : {
        check : P,
        nullable : true,
        init : 0,
        apply : C
      }
    },
    statics : {
      ID_PREFIX : N,
      __k : {
      },
      __cX : 0,
      __cY : null,
      onShutdown : function(){

        window.clearTimeout(qx.ui.mobile.core.Widget.__cY);
        delete qx.ui.mobile.core.Widget.__k;
      },
      getCurrentId : function(){

        return qx.ui.mobile.core.Widget.__cX;
      },
      registerWidget : function(widget){

        var id = widget.getId();
        var registry = qx.ui.mobile.core.Widget.__k;
        {
        };
        registry[id] = widget;
      },
      unregisterWidget : function(id){

        delete qx.ui.mobile.core.Widget.__k[id];
      },
      getWidgetById : function(id){

        return qx.ui.mobile.core.Widget.__k[id];
      },
      scheduleDomUpdated : function(){

        if(qx.ui.mobile.core.Widget.__cY == null){

          qx.ui.mobile.core.Widget.__cY = window.setTimeout(qx.ui.mobile.core.Widget.domUpdated, 0);
        };
      },
      domUpdated : qx.event.GlobalError.observeMethod(function(){

        var clazz = qx.ui.mobile.core.Widget;
        window.clearTimeout(clazz.__cY);
        clazz.__cY = null;
        qx.event.handler.Appear.refresh();
        qx.ui.mobile.core.DomUpdatedHandler.refresh();
      }),
      addAttributeMapping : function(property, attribute, values){

        {

          var attributeMapping;
        };
        qx.ui.mobile.core.Widget.ATTRIBUTE_MAPPING[property] = {
          attribute : attribute,
          values : values
        };
      },
      addStyleMapping : function(property, style, values){

        {

          var styleMapping;
        };
        qx.ui.mobile.core.Widget.STYLE_MAPPING[property] = {
          style : style,
          values : values
        };
      },
      ATTRIBUTE_MAPPING : {
        "selectable" : {
          attribute : q,
          values : {
            "true" : null,
            "false" : J
          }
        },
        "activatable" : {
          attribute : G,
          values : {
            "true" : f,
            "false" : null
          }
        },
        "readOnly" : {
          attribute : m
        }
      },
      STYLE_MAPPING : {
        "anonymous" : {
          style : p,
          values : {
            "true" : O,
            "false" : null
          }
        }
      }
    },
    members : {
      __da : null,
      __db : null,
      __dc : null,
      __cW : null,
      __dd : null,
      _getTagName : function(){

        return h;
      },
      _createContainerElement : function(){

        return qx.dom.Element.create(this._getTagName());
      },
      _domUpdated : function(){

        qx.ui.mobile.core.Widget.scheduleDomUpdated();
      },
      _transformId : function(value){

        if(value == null){

          var clazz = qx.ui.mobile.core.Widget;
          value = clazz.ID_PREFIX + clazz.__cX++;
        };
        return value;
      },
      _applyId : function(value, old){

        if(old != null){

          qx.ui.mobile.core.Widget.unregisterWidget(old);
        };
        var element = this.getContainerElement();
        element.id = value;
        qx.ui.mobile.core.Widget.registerWidget(this);
      },
      _applyEnabled : function(value, old){

        if(value){

          this.removeCssClass(u);
          this._setStyle(V, this.getAnonymous());
        } else {

          this.addCssClass(u);
          this._setStyle(V, true);
        };
      },
      _add : function(child, layoutProperties){

        {
        };
        this._initializeChildLayout(child, layoutProperties);
        this.getContentElement().appendChild(child.getContainerElement());
        this.__cW.push(child);
        this._domUpdated();
      },
      _addAt : function(child, index, options){

        if(child.getLayoutParent() == this){

          qx.lang.Array.remove(this.__cW, child);
        };
        var ref = this.__cW[index];
        if(ref){

          this._addBefore(child, ref, options);
        } else {

          this._add(child, options);
        };
      },
      _addBefore : function(child, beforeWidget, layoutProperties){

        {
        };
        if(child == beforeWidget){

          return;
        };
        this._initializeChildLayout(child, layoutProperties);
        this.getContentElement().insertBefore(child.getContainerElement(), beforeWidget.getContainerElement());
        qx.lang.Array.insertBefore(this.__cW, child, beforeWidget);
        this._domUpdated();
      },
      _addAfter : function(child, afterWidget, layoutProperties){

        {
        };
        if(child == afterWidget){

          return;
        };
        this._initializeChildLayout(child, layoutProperties);
        var length = this._getChildren().length;
        var index = this._indexOf(afterWidget);
        if(index == length - 1){

          this.getContentElement().appendChild(child.getContainerElement());
        } else {

          var beforeWidget = this._getChildren()[index + 1];
          this.getContentElement().insertBefore(child.getContainerElement(), beforeWidget.getContainerElement());
        };
        qx.lang.Array.insertAfter(this.__cW, child, afterWidget);
        this._domUpdated();
      },
      _remove : function(child){

        child.setLayoutParent(null);
        this._domUpdated();
      },
      _removeAt : function(index){

        if(!this.__cW){

          throw new Error(K);
        };
        var child = this.__cW[index];
        this._remove(child);
      },
      _removeAll : function(){

        var children = this.__cW.concat();
        for(var i = 0,l = children.length;i < l;i++){

          this._remove(children[i]);
        };
        return children;
      },
      _indexOf : function(child){

        var children = this.__cW;
        if(!children){

          return -1;
        };
        return children.indexOf(child);
      },
      setLayoutParent : function(parent){

        if(this.__dc === parent){

          return;
        };
        var oldParent = this.__dc;
        if(oldParent && !oldParent.$$disposed){

          this.__dc.removeChild(this);
        };
        this.__dc = parent || null;
      },
      removeChild : function(child){

        qx.lang.Array.remove(this.__cW, child);
        this.getContentElement().removeChild(child.getContainerElement());
        var layout = this._getLayout();
        if(layout){

          layout.disconnectFromChildWidget(child);
        };
      },
      getLayoutParent : function(){

        return this.__dc;
      },
      _getChildren : function(){

        return this.__cW;
      },
      _hasChildren : function(){

        return this.__cW && this.__cW.length > 0;
      },
      _setLayout : function(layout){

        {
        };
        if(this.__dd){

          this.__dd.connectToWidget(null);
          for(var i = 0;i < length;i++){

            var child = this._getChildren()[i];
            this.__dd.disconnectFromChildWidget(child);
          };
        };
        if(layout){

          layout.connectToWidget(this);
        };
        this.__dd = layout;
        this._domUpdated();
      },
      _initializeChildLayout : function(child, layoutProperties){

        child.setLayoutParent(this);
        child.setLayoutProperties(layoutProperties);
        var layout = this._getLayout();
        if(layout){

          layout.connectToChildWidget(child);
        };
      },
      _getLayout : function(){

        return this.__dd;
      },
      setLayoutProperties : function(properties){

        var parent = this.getLayoutParent();
        if(parent){

          parent.updateLayoutProperties(this, properties);
        };
      },
      updateLayoutProperties : function(widget, properties){

        var layout = this._getLayout();
        if(layout){

          layout.setLayoutProperties(widget, properties);
        };
        this._domUpdated();
      },
      updateLayout : function(widget, action, properties){

        var layout = this._getLayout();
        if(layout){

          layout.updateLayout(widget, action, properties);
        };
        this._domUpdated();
      },
      _setHtml : function(value){

        this.getContentElement().innerHTML = value || D;
        this._domUpdated();
      },
      _transform : function(){

        var propertyValue = D;
        if(this.getRotation() != null){

          propertyValue = propertyValue + R + this.getRotation() + L;
        };
        if(this.getScaleX() != null && this.getScaleY() != null){

          propertyValue = propertyValue + bc + this.getScaleX() + bd + this.getScaleY() + A;
        };
        var resolutionFactor = 1;
        if(this.getTransformUnit() == I){

          resolutionFactor = 16;
        };
        if(this.getTranslateX() != null && this.getTranslateY() != null){

          var isTransform3d = qx.core.Environment.get(k);
          if(isTransform3d && this.getTranslateZ() != null){

            propertyValue = propertyValue + S + (this.getTranslateX() / resolutionFactor) + this.getTransformUnit() + bd + (this.getTranslateY() / resolutionFactor) + this.getTransformUnit() + bd + (this.getTranslateZ() / resolutionFactor) + this.getTransformUnit() + A;
          } else {

            propertyValue = propertyValue + Q + (this.getTranslateX() / resolutionFactor) + this.getTransformUnit() + bd + (this.getTranslateY() / resolutionFactor) + this.getTransformUnit() + A;
          };
        };
        qx.bom.element.Style.set(this.getContainerElement(), Y, propertyValue);
      },
      _applyAttribute : function(value, old, attribute){

        this._setAttribute(attribute, value);
      },
      _setAttribute : function(attribute, value){

        var mapping = qx.ui.mobile.core.Widget.ATTRIBUTE_MAPPING[attribute];
        if(mapping){

          attribute = mapping.attribute || attribute;
          var values = mapping.values;
          value = values && typeof values[value] !== M ? values[value] : value;
        };
        var element = this.getContainerElement();
        if(value != null){

          qx.bom.element.Attribute.set(element, attribute, value);
        } else {

          qx.bom.element.Attribute.reset(element, attribute);
        };
        this._domUpdated();
      },
      _getAttribute : function(attribute){

        var element = this.getContainerElement();
        return qx.bom.element.Attribute.get(element, attribute);
      },
      _applyStyle : function(value, old, style){

        this._setStyle(style, value);
      },
      _setStyle : function(style, value){

        var mapping = qx.ui.mobile.core.Widget.STYLE_MAPPING[style];
        if(mapping){

          style = mapping.style || style;
          value = mapping.values[value];
        };
        var element = this.getContainerElement();
        if(value != null){

          qx.bom.element.Style.set(element, style, value);
        } else {

          qx.bom.element.Style.reset(element, style);
        };
        this._domUpdated();
      },
      _getStyle : function(style){

        var element = this.getContainerElement();
        return qx.bom.element.Style.get(element, style);
      },
      _applyDefaultCssClass : function(value, old){

        if(old){

          this.removeCssClass(old);
        };
        if(value){

          this.addCssClass(value);
        };
      },
      addCssClass : function(cssClass){

        qx.bom.element.Class.add(this.getContainerElement(), cssClass);
        this._domUpdated();
      },
      addCssClasses : function(cssClasses){

        if(cssClasses){

          qx.bom.element.Class.addClasses(this.getContainerElement(), cssClasses);
          this._domUpdated();
        };
      },
      removeCssClass : function(cssClass){

        qx.bom.element.Class.remove(this.getContainerElement(), cssClass);
        this._domUpdated();
      },
      removeCssClasses : function(cssClasses){

        if(cssClasses){

          qx.bom.element.Class.removeClasses(this.getContainerElement(), cssClasses);
          this._domUpdated();
        };
      },
      toggleCssClass : function(cssClass){

        if(this.hasCssClass(cssClass)){

          this.removeCssClass(cssClass);
        } else {

          this.addCssClass(cssClass);
        };
      },
      hasCssClass : function(cssClass){

        return qx.bom.element.Class.has(this.getContainerElement(), cssClass);
      },
      _applyVisibility : function(value, old){

        if(value == g){

          this.addCssClass(n);
        } else if(value == s){

          this.removeCssClass(n);
          this._setStyle(E, s);
        } else if(value == X){

          this._setStyle(E, X);
        };;
        this._domUpdated();
      },
      __de : function(action, properties){

        this.setVisibility(action);
        var parent = this.getLayoutParent();
        if(parent){

          parent.updateLayout(this, action, properties);
        };
      },
      show : function(properties){

        this.__de(s, properties);
      },
      hide : function(properties){

        this.__de(X, properties);
      },
      exclude : function(properties){

        this.__de(g, properties);
      },
      isVisible : function(){

        return this.getVisibility() === s;
      },
      isHidden : function(){

        return this.getVisibility() !== s;
      },
      isExcluded : function(){

        return this.getVisibility() === g;
      },
      isSeeable : function(){

        return this.getContainerElement().offsetWidth > 0;
      },
      _setContainerElement : function(element){

        this.__da = element;
      },
      getContainerElement : function(){

        return this.__da;
      },
      getContentElement : function(){

        if(!this.__db){

          this.__db = this._getContentElement();
        };
        return this.__db;
      },
      _getContentElement : function(){

        return this.getContainerElement();
      },
      destroy : function(){

        if(this.$$disposed){

          return;
        };
        var parent = this.__dc;
        if(parent){

          parent._remove(this);
        };
        this.dispose();
      }
    },
    destruct : function(){

      if(!qx.core.ObjectRegistry.inShutDown){

        qx.event.Registration.removeAllListeners(this);
        if(this.getId()){

          qx.ui.mobile.core.Widget.unregisterWidget(this.getId());
        };
      };
      this.__dc = this.__da = this.__db = null;
      if(this.__dd){

        this.__dd.dispose();
      };
      this.__dd = null;
    },
    defer : function(statics){

      qx.bom.Lifecycle.onShutdown(statics.onShutdown, statics);
    }
  });
})();
(function(){

  var a = "os.name",b = "opera",c = "engine.name",d = "qx.event.type.Dom",e = "osx";
  qx.Class.define(d, {
    extend : qx.event.type.Native,
    statics : {
      SHIFT_MASK : 1,
      CTRL_MASK : 2,
      ALT_MASK : 4,
      META_MASK : 8
    },
    members : {
      _cloneNativeEvent : function(nativeEvent, clone){

        var clone = qx.event.type.Native.prototype._cloneNativeEvent.call(this, nativeEvent, clone);
        clone.shiftKey = nativeEvent.shiftKey;
        clone.ctrlKey = nativeEvent.ctrlKey;
        clone.altKey = nativeEvent.altKey;
        clone.metaKey = nativeEvent.metaKey;
        return clone;
      },
      getModifiers : function(){

        var mask = 0;
        var evt = this._native;
        if(evt.shiftKey){

          mask |= qx.event.type.Dom.SHIFT_MASK;
        };
        if(evt.ctrlKey){

          mask |= qx.event.type.Dom.CTRL_MASK;
        };
        if(evt.altKey){

          mask |= qx.event.type.Dom.ALT_MASK;
        };
        if(evt.metaKey){

          mask |= qx.event.type.Dom.META_MASK;
        };
        return mask;
      },
      isCtrlPressed : function(){

        return this._native.ctrlKey;
      },
      isShiftPressed : function(){

        return this._native.shiftKey;
      },
      isAltPressed : function(){

        return this._native.altKey;
      },
      isMetaPressed : function(){

        return this._native.metaKey;
      },
      isCtrlOrCommandPressed : function(){

        if(qx.core.Environment.get(a) == e && qx.core.Environment.get(c) != b){

          return this._native.metaKey;
        } else {

          return this._native.ctrlKey;
        };
      }
    }
  });
})();
(function(){

  var a = "touchcancel",b = "qx.event.type.Touch",c = "touchend";
  qx.Class.define(b, {
    extend : qx.event.type.Dom,
    members : {
      _cloneNativeEvent : function(nativeEvent, clone){

        var clone = qx.event.type.Dom.prototype._cloneNativeEvent.call(this, nativeEvent, clone);
        clone.pageX = nativeEvent.pageX;
        clone.pageY = nativeEvent.pageY;
        clone.offsetX = nativeEvent.offsetX;
        clone.offsetY = nativeEvent.offsetY;
        clone.layerX = (nativeEvent.offsetX || nativeEvent.layerX);
        clone.layerY = (nativeEvent.offsetY || nativeEvent.layerY);
        clone.scale = nativeEvent.scale;
        clone.rotation = nativeEvent.rotation;
        clone.srcElement = nativeEvent.srcElement;
        clone.targetTouches = [];
        for(var i = 0;i < nativeEvent.targetTouches.length;i++){

          clone.targetTouches[i] = nativeEvent.targetTouches[i];
        };
        clone.changedTouches = [];
        for(i = 0;i < nativeEvent.changedTouches.length;i++){

          clone.changedTouches[i] = nativeEvent.changedTouches[i];
        };
        clone.touches = [];
        for(i = 0;i < nativeEvent.touches.length;i++){

          clone.touches[i] = nativeEvent.touches[i];
        };
        return clone;
      },
      stop : function(){

        this.stopPropagation();
      },
      getAllTouches : function(){

        return this._native.touches;
      },
      getTargetTouches : function(){

        return this._native.targetTouches;
      },
      getChangedTargetTouches : function(){

        return this._native.changedTouches;
      },
      isMultiTouch : function(){

        return this.__dg().length > 1;
      },
      getScale : function(){

        return this._native.scale;
      },
      getRotation : function(){

        return this._native.rotation;
      },
      getDocumentLeft : function(touchIndex){

        return this.__df(touchIndex).pageX;
      },
      getDocumentTop : function(touchIndex){

        return this.__df(touchIndex).pageY;
      },
      getScreenLeft : function(touchIndex){

        return this.__df(touchIndex).screenX;
      },
      getScreenTop : function(touchIndex){

        return this.__df(touchIndex).screenY;
      },
      getViewportLeft : function(touchIndex){

        return this.__df(touchIndex).clientX;
      },
      getViewportTop : function(touchIndex){

        return this.__df(touchIndex).clientY;
      },
      getIdentifier : function(touchIndex){

        return this.__df(touchIndex).identifier;
      },
      __df : function(touchIndex){

        touchIndex = touchIndex == null ? 0 : touchIndex;
        return this.__dg()[touchIndex];
      },
      __dg : function(){

        var touches = (this._isTouchEnd() ? this.getChangedTargetTouches() : this.getTargetTouches());
        return touches;
      },
      _isTouchEnd : function(){

        return (this.getType() == c || this.getType() == a);
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Tap";
  qx.Class.define(a, {
    extend : qx.event.type.Touch,
    members : {
      _isTouchEnd : function(){

        return true;
      }
    }
  });
})();
(function(){

  var a = "resize",b = "os.name",c = "qx.event.handler.Orientation",d = "landscape",e = "android",f = "portrait",g = "orientationchange";
  qx.Class.define(c, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__cw = manager.getWindow();
      this._initObserver();
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        orientationchange : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      __dh : null,
      __cw : null,
      __di : null,
      _currentOrientation : null,
      __dj : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      _initObserver : function(){

        this.__dj = qx.lang.Function.listener(this._onNative, this);
        this.__di = qx.bom.Event.supportsEvent(this.__cw, g) ? g : a;
        var Event = qx.bom.Event;
        Event.addNativeListener(this.__cw, this.__di, this.__dj);
      },
      _stopObserver : function(){

        var Event = qx.bom.Event;
        Event.removeNativeListener(this.__cw, this.__di, this.__dj);
      },
      _onNative : qx.event.GlobalError.observeMethod(function(domEvent){

        var detectOrientationChangeDelay = 0;
        if(qx.core.Environment.get(b) == e){

          detectOrientationChangeDelay = 300;
        };
        qx.lang.Function.delay(this._onOrientationChange, detectOrientationChangeDelay, this, domEvent);
      }),
      _onOrientationChange : function(domEvent){

        var Viewport = qx.bom.Viewport;
        var orientation = Viewport.getOrientation(domEvent.target);
        if(this._currentOrientation != orientation){

          this._currentOrientation = orientation;
          var mode = Viewport.isLandscape(domEvent.target) ? d : f;
          qx.event.Registration.fireEvent(this.__cw, g, qx.event.type.Orientation, [orientation, mode]);
        };
      }
    },
    destruct : function(){

      this._stopObserver();
      this.__dh = this.__cw = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "ios",b = "os.name",c = "undefined",d = "qx.bom.Viewport";
  qx.Bootstrap.define(d, {
    statics : {
      getWidth : function(win){

        var win = win || window;
        var doc = win.document;
        return qx.bom.Document.isStandardMode(win) ? doc.documentElement.clientWidth : doc.body.clientWidth;
      },
      getHeight : function(win){

        var win = win || window;
        var doc = win.document;
        if(qx.core.Environment.get(b) == a && window.innerHeight != doc.documentElement.clientHeight){

          return window.innerHeight;
        };
        return qx.bom.Document.isStandardMode(win) ? doc.documentElement.clientHeight : doc.body.clientHeight;
      },
      getScrollLeft : function(win){

        var win = win ? win : window;
        if(typeof win.pageXOffset !== c){

          return win.pageXOffset;
        };
        var doc = win.document;
        return doc.documentElement.scrollLeft || doc.body.scrollLeft;
      },
      getScrollTop : function(win){

        var win = win ? win : window;
        if(typeof win.pageYOffset !== c){

          return win.pageYOffset;
        };
        var doc = win.document;
        return doc.documentElement.scrollTop || doc.body.scrollTop;
      },
      __dk : function(win){

        var currentOrientation = this.getWidth(win) > this.getHeight(win) ? 90 : 0;
        var deviceOrientation = win.orientation;
        if(deviceOrientation == null || Math.abs(deviceOrientation % 180) == currentOrientation){

          return {
            "-270" : 90,
            "-180" : 180,
            "-90" : -90,
            "0" : 0,
            "90" : 90,
            "180" : 180,
            "270" : -90
          };
        } else {

          return {
            "-270" : 180,
            "-180" : -90,
            "-90" : 0,
            "0" : 90,
            "90" : 180,
            "180" : -90,
            "270" : 0
          };
        };
      },
      __dl : null,
      getOrientation : function(win){

        var win = win || window.top;
        var orientation = win.orientation;
        if(orientation == null){

          orientation = this.getWidth(win) > this.getHeight(win) ? 90 : 0;
        } else {

          if(this.__dl == null){

            this.__dl = this.__dk(win);
          };
          orientation = this.__dl[orientation];
        };
        return orientation;
      },
      isLandscape : function(win){

        return this.getWidth(win) >= this.getHeight(win);
      },
      isPortrait : function(win){

        return this.getWidth(win) < this.getHeight(win);
      }
    }
  });
})();
(function(){

  var a = "engine.name",b = "CSS1Compat",c = "position:absolute;width:0;height:0;width:1",d = "engine.version",e = "qx.bom.Document",f = "1px",g = "div";
  qx.Bootstrap.define(e, {
    statics : {
      isQuirksMode : qx.core.Environment.select(a, {
        "mshtml" : function(win){

          if(qx.core.Environment.get(d) >= 8){

            return (win || window).document.documentMode === 5;
          } else {

            return (win || window).document.compatMode !== b;
          };
        },
        "webkit" : function(win){

          if(document.compatMode === undefined){

            var el = (win || window).document.createElement(g);
            el.style.cssText = c;
            return el.style.width === f ? true : false;
          } else {

            return (win || window).document.compatMode !== b;
          };
        },
        "default" : function(win){

          return (win || window).document.compatMode !== b;
        }
      }),
      isStandardMode : function(win){

        return !this.isQuirksMode(win);
      },
      getWidth : function(win){

        var doc = (win || window).document;
        var view = qx.bom.Viewport.getWidth(win);
        var scroll = this.isStandardMode(win) ? doc.documentElement.scrollWidth : doc.body.scrollWidth;
        return Math.max(scroll, view);
      },
      getHeight : function(win){

        var doc = (win || window).document;
        var view = qx.bom.Viewport.getHeight(win);
        var scroll = this.isStandardMode(win) ? doc.documentElement.scrollHeight : doc.body.scrollHeight;
        return Math.max(scroll, view);
      }
    }
  });
})();
(function(){

  var a = "landscape",b = "qx.event.type.Orientation",c = "portrait";
  qx.Class.define(b, {
    extend : qx.event.type.Event,
    members : {
      __dm : null,
      __dn : null,
      init : function(orientation, mode){

        qx.event.type.Event.prototype.init.call(this, false, false);
        this.__dm = orientation;
        this.__dn = mode;
        return this;
      },
      clone : function(embryo){

        var clone = qx.event.type.Event.prototype.clone.call(this, embryo);
        clone.__dm = this.__dm;
        clone.__dn = this.__dn;
        return clone;
      },
      getOrientation : function(){

        return this.__dm;
      },
      isLandscape : function(){

        return this.__dn == a;
      },
      isPortrait : function(){

        return this.__dn == c;
      }
    }
  });
})();
(function(){

  var a = "os.name",b = "qx.mobile.nativescroll",c = "osx",d = "qx.nativeScrollBars",e = "event.mspointer",f = "os.scrollBarOverlayed",g = "browser.version",h = "ios",i = "qx.bom.client.Scroll";
  qx.Bootstrap.define(i, {
    statics : {
      scrollBarOverlayed : function(){

        var scrollBarWidth = qx.bom.element.Scroll.getScrollbarWidth();
        var osx = qx.bom.client.OperatingSystem.getName() === c;
        var nativeScrollBars = qx.core.Environment.get(d);
        return scrollBarWidth === 0 && osx && nativeScrollBars;
      },
      getNativeScroll : function(){

        if(qx.core.Environment.get(a) == h && parseInt(qx.core.Environment.get(g), 10) > 4){

          return true;
        };
        if(qx.core.Environment.get(e)){

          return true;
        };
        return false;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(f, statics.scrollBarOverlayed);
      qx.core.Environment.add(b, statics.getNativeScroll);
    }
  });
})();
(function(){

  var a = "borderBottomWidth",b = "visible",d = "engine.name",e = "borderTopWidth",f = "top",g = "borderLeftStyle",h = "none",i = "overflow",j = "right",k = "bottom",l = "borderLeftWidth",m = "100px",n = "-moz-scrollbars-vertical",o = "borderRightStyle",p = "hidden",q = "div",r = "left",u = "qx.bom.element.Scroll",v = "borderRightWidth",w = "scroll",x = "overflowY";
  qx.Class.define(u, {
    statics : {
      __do : null,
      getScrollbarWidth : function(){

        if(this.__do !== null){

          return this.__do;
        };
        var Style = qx.bom.element.Style;
        var getStyleSize = function(el, propertyName){

          return parseInt(Style.get(el, propertyName), 10) || 0;
        };
        var getBorderRight = function(el){

          return (Style.get(el, o) == h ? 0 : getStyleSize(el, v));
        };
        var getBorderLeft = function(el){

          return (Style.get(el, g) == h ? 0 : getStyleSize(el, l));
        };
        var getInsetRight = qx.core.Environment.select(d, {
          "mshtml" : function(el){

            if(Style.get(el, x) == p || el.clientWidth == 0){

              return getBorderRight(el);
            };
            return Math.max(0, el.offsetWidth - el.clientLeft - el.clientWidth);
          },
          "default" : function(el){

            if(el.clientWidth == 0){

              var ov = Style.get(el, i);
              var sbv = (ov == w || ov == n ? 16 : 0);
              return Math.max(0, getBorderRight(el) + sbv);
            };
            return Math.max(0, (el.offsetWidth - el.clientWidth - getBorderLeft(el)));
          }
        });
        var getScrollBarSizeRight = function(el){

          return getInsetRight(el) - getBorderRight(el);
        };
        var t = document.createElement(q);
        var s = t.style;
        s.height = s.width = m;
        s.overflow = w;
        document.body.appendChild(t);
        var c = getScrollBarSizeRight(t);
        this.__do = c;
        document.body.removeChild(t);
        return this.__do;
      },
      intoViewX : function(element, stop, align){

        var parent = element.parentNode;
        var doc = qx.dom.Node.getDocument(element);
        var body = doc.body;
        var parentLocation,parentLeft,parentRight;
        var parentOuterWidth,parentClientWidth,parentScrollWidth;
        var parentLeftBorder,parentRightBorder,parentScrollBarWidth;
        var elementLocation,elementLeft,elementRight,elementWidth;
        var leftOffset,rightOffset,scrollDiff;
        var alignLeft = align === r;
        var alignRight = align === j;
        stop = stop ? stop.parentNode : doc;
        while(parent && parent != stop){

          if(parent.scrollWidth > parent.clientWidth && (parent === body || qx.bom.element.Style.get(parent, x) != b)){

            if(parent === body){

              parentLeft = parent.scrollLeft;
              parentRight = parentLeft + qx.bom.Viewport.getWidth();
              parentOuterWidth = qx.bom.Viewport.getWidth();
              parentClientWidth = parent.clientWidth;
              parentScrollWidth = parent.scrollWidth;
              parentLeftBorder = 0;
              parentRightBorder = 0;
              parentScrollBarWidth = 0;
            } else {

              parentLocation = qx.bom.element.Location.get(parent);
              parentLeft = parentLocation.left;
              parentRight = parentLocation.right;
              parentOuterWidth = parent.offsetWidth;
              parentClientWidth = parent.clientWidth;
              parentScrollWidth = parent.scrollWidth;
              parentLeftBorder = parseInt(qx.bom.element.Style.get(parent, l), 10) || 0;
              parentRightBorder = parseInt(qx.bom.element.Style.get(parent, v), 10) || 0;
              parentScrollBarWidth = parentOuterWidth - parentClientWidth - parentLeftBorder - parentRightBorder;
            };
            elementLocation = qx.bom.element.Location.get(element);
            elementLeft = elementLocation.left;
            elementRight = elementLocation.right;
            elementWidth = element.offsetWidth;
            leftOffset = elementLeft - parentLeft - parentLeftBorder;
            rightOffset = elementRight - parentRight + parentRightBorder;
            scrollDiff = 0;
            if(alignLeft){

              scrollDiff = leftOffset;
            } else if(alignRight){

              scrollDiff = rightOffset + parentScrollBarWidth;
            } else if(leftOffset < 0 || elementWidth > parentClientWidth){

              scrollDiff = leftOffset;
            } else if(rightOffset > 0){

              scrollDiff = rightOffset + parentScrollBarWidth;
            };;;
            parent.scrollLeft += scrollDiff;
            qx.event.Registration.fireNonBubblingEvent(parent, w);
          };
          if(parent === body){

            break;
          };
          parent = parent.parentNode;
        };
      },
      intoViewY : function(element, stop, align){

        var parent = element.parentNode;
        var doc = qx.dom.Node.getDocument(element);
        var body = doc.body;
        var parentLocation,parentTop,parentBottom;
        var parentOuterHeight,parentClientHeight,parentScrollHeight;
        var parentTopBorder,parentBottomBorder,parentScrollBarHeight;
        var elementLocation,elementTop,elementBottom,elementHeight;
        var topOffset,bottomOffset,scrollDiff;
        var alignTop = align === f;
        var alignBottom = align === k;
        stop = stop ? stop.parentNode : doc;
        while(parent && parent != stop){

          if(parent.scrollHeight > parent.clientHeight && (parent === body || qx.bom.element.Style.get(parent, x) != b)){

            if(parent === body){

              parentTop = parent.scrollTop;
              parentBottom = parentTop + qx.bom.Viewport.getHeight();
              parentOuterHeight = qx.bom.Viewport.getHeight();
              parentClientHeight = parent.clientHeight;
              parentScrollHeight = parent.scrollHeight;
              parentTopBorder = 0;
              parentBottomBorder = 0;
              parentScrollBarHeight = 0;
            } else {

              parentLocation = qx.bom.element.Location.get(parent);
              parentTop = parentLocation.top;
              parentBottom = parentLocation.bottom;
              parentOuterHeight = parent.offsetHeight;
              parentClientHeight = parent.clientHeight;
              parentScrollHeight = parent.scrollHeight;
              parentTopBorder = parseInt(qx.bom.element.Style.get(parent, e), 10) || 0;
              parentBottomBorder = parseInt(qx.bom.element.Style.get(parent, a), 10) || 0;
              parentScrollBarHeight = parentOuterHeight - parentClientHeight - parentTopBorder - parentBottomBorder;
            };
            elementLocation = qx.bom.element.Location.get(element);
            elementTop = elementLocation.top;
            elementBottom = elementLocation.bottom;
            elementHeight = element.offsetHeight;
            topOffset = elementTop - parentTop - parentTopBorder;
            bottomOffset = elementBottom - parentBottom + parentBottomBorder;
            scrollDiff = 0;
            if(alignTop){

              scrollDiff = topOffset;
            } else if(alignBottom){

              scrollDiff = bottomOffset + parentScrollBarHeight;
            } else if(topOffset < 0 || elementHeight > parentClientHeight){

              scrollDiff = topOffset;
            } else if(bottomOffset > 0){

              scrollDiff = bottomOffset + parentScrollBarHeight;
            };;;
            parent.scrollTop += scrollDiff;
            qx.event.Registration.fireNonBubblingEvent(parent, w);
          };
          if(parent === body){

            break;
          };
          parent = parent.parentNode;
        };
      },
      intoView : function(element, stop, alignX, alignY){

        this.intoViewX(element, stop, alignX);
        this.intoViewY(element, stop, alignY);
      }
    }
  });
})();
(function(){

  var a = "border-box",b = "qx.bom.element.BoxSizing",c = "css.boxsizing",d = "",e = "boxSizing",f = "content-box",g = ":",h = ";";
  qx.Bootstrap.define(b, {
    statics : {
      __dp : {
        tags : {
          button : true,
          select : true
        },
        types : {
          search : true,
          button : true,
          submit : true,
          reset : true,
          checkbox : true,
          radio : true
        }
      },
      __dq : function(element){

        var map = this.__dp;
        return map.tags[element.tagName.toLowerCase()] || map.types[element.type];
      },
      compile : function(value){

        if(qx.core.Environment.get(c)){

          var prop = qx.bom.Style.getCssName(qx.core.Environment.get(c));
          return prop + g + value + h;
        } else {

          {
          };
        };
      },
      get : function(element){

        if(qx.core.Environment.get(c)){

          return qx.bom.element.Style.get(element, e, null, false) || d;
        };
        if(qx.bom.Document.isStandardMode(qx.dom.Node.getWindow(element))){

          if(!this.__dq(element)){

            return f;
          };
        };
        return a;
      },
      set : function(element, value){

        if(qx.core.Environment.get(c)){

          try{

            element.style[qx.core.Environment.get(c)] = value;
          } catch(ex) {

            {
            };
          };
        } else {

          {
          };
        };
      },
      reset : function(element){

        this.set(element, d);
      }
    }
  });
})();
(function(){

  var a = "cursor:",b = "engine.name",c = "ns-resize",d = "",e = "mshtml",f = "nw-resize",g = "n-resize",h = "engine.version",i = "nesw-resize",j = "opera",k = "browser.documentmode",l = ";",m = "nwse-resize",n = "ew-resize",o = "qx.bom.element.Cursor",p = "ne-resize",q = "e-resize",r = "browser.quirksmode",s = "cursor";
  qx.Bootstrap.define(o, {
    statics : {
      __dr : {
      },
      compile : function(cursor){

        return a + (this.__dr[cursor] || cursor) + l;
      },
      get : function(element, mode){

        return qx.bom.element.Style.get(element, s, mode, false);
      },
      set : function(element, value){

        element.style.cursor = this.__dr[value] || value;
      },
      reset : function(element){

        element.style.cursor = d;
      }
    },
    defer : function(statics){

      if(qx.core.Environment.get(b) == e && ((parseFloat(qx.core.Environment.get(h)) < 9 || qx.core.Environment.get(k) < 9) && !qx.core.Environment.get(r))){

        statics.__dr[i] = p;
        statics.__dr[m] = f;
        if(((parseFloat(qx.core.Environment.get(h)) < 8 || qx.core.Environment.get(k) < 8) && !qx.core.Environment.get(r))){

          statics.__dr[n] = q;
          statics.__dr[c] = g;
        };
      } else if(qx.core.Environment.get(b) == j && parseInt(qx.core.Environment.get(h)) < 12){

        statics.__dr[i] = p;
        statics.__dr[m] = f;
      };
    }
  });
})();
(function(){

  var a = "engine.name",b = ");",c = "",d = ")",e = "zoom:1;filter:alpha(opacity=",f = "qx.bom.element.Opacity",g = "css.opacity",h = ";",i = "opacity:",j = "alpha(opacity=",k = "opacity",l = "filter";
  qx.Bootstrap.define(f, {
    statics : {
      compile : qx.core.Environment.select(a, {
        "mshtml" : function(opacity){

          if(opacity >= 1){

            opacity = 1;
          };
          if(opacity < 0.00001){

            opacity = 0;
          };
          if(qx.core.Environment.get(g)){

            return i + opacity + h;
          } else {

            return e + (opacity * 100) + b;
          };
        },
        "default" : function(opacity){

          if(opacity >= 1){

            return c;
          };
          return i + opacity + h;
        }
      }),
      set : qx.core.Environment.select(a, {
        "mshtml" : function(element, opacity){

          if(qx.core.Environment.get(g)){

            if(opacity >= 1){

              opacity = c;
            };
            element.style.opacity = opacity;
          } else {

            var filter = qx.bom.element.Style.get(element, l, qx.bom.element.Style.COMPUTED_MODE, false);
            if(opacity >= 1){

              opacity = 1;
            };
            if(opacity < 0.00001){

              opacity = 0;
            };
            if(!element.currentStyle || !element.currentStyle.hasLayout){

              element.style.zoom = 1;
            };
            element.style.filter = filter.replace(/alpha\([^\)]*\)/gi, c) + j + opacity * 100 + d;
          };
        },
        "default" : function(element, opacity){

          if(opacity >= 1){

            opacity = c;
          };
          element.style.opacity = opacity;
        }
      }),
      reset : qx.core.Environment.select(a, {
        "mshtml" : function(element){

          if(qx.core.Environment.get(g)){

            element.style.opacity = c;
          } else {

            var filter = qx.bom.element.Style.get(element, l, qx.bom.element.Style.COMPUTED_MODE, false);
            element.style.filter = filter.replace(/alpha\([^\)]*\)/gi, c);
          };
        },
        "default" : function(element){

          element.style.opacity = c;
        }
      }),
      get : qx.core.Environment.select(a, {
        "mshtml" : function(element, mode){

          if(qx.core.Environment.get(g)){

            var opacity = qx.bom.element.Style.get(element, k, mode, false);
            if(opacity != null){

              return parseFloat(opacity);
            };
            return 1.0;
          } else {

            var filter = qx.bom.element.Style.get(element, l, mode, false);
            if(filter){

              var opacity = filter.match(/alpha\(opacity=(.*)\)/);
              if(opacity && opacity[1]){

                return parseFloat(opacity[1]) / 100;
              };
            };
            return 1.0;
          };
        },
        "default" : function(element, mode){

          var opacity = qx.bom.element.Style.get(element, k, mode, false);
          if(opacity != null){

            return parseFloat(opacity);
          };
          return 1.0;
        }
      })
    }
  });
})();
(function(){

  var a = "clip:auto;",b = "rect(",c = ")",d = ");",e = "",f = "px",g = "Could not parse clip string: ",h = "qx.bom.element.Clip",i = "string",j = "clip:rect(",k = " ",l = "clip",m = "rect(auto,auto,auto,auto)",n = "rect(auto, auto, auto, auto)",o = "auto",p = ",";
  qx.Bootstrap.define(h, {
    statics : {
      compile : function(map){

        if(!map){

          return a;
        };
        var left = map.left;
        var top = map.top;
        var width = map.width;
        var height = map.height;
        var right,bottom;
        if(left == null){

          right = (width == null ? o : width + f);
          left = o;
        } else {

          right = (width == null ? o : left + width + f);
          left = left + f;
        };
        if(top == null){

          bottom = (height == null ? o : height + f);
          top = o;
        } else {

          bottom = (height == null ? o : top + height + f);
          top = top + f;
        };
        return j + top + p + right + p + bottom + p + left + d;
      },
      get : function(element, mode){

        var clip = qx.bom.element.Style.get(element, l, mode, false);
        var left,top,width,height;
        var right,bottom;
        if(typeof clip === i && clip !== o && clip !== e){

          clip = clip.trim();
          if(/\((.*)\)/.test(clip)){

            var result = RegExp.$1;
            if(/,/.test(result)){

              var split = result.split(p);
            } else {

              var split = result.split(k);
            };
            top = split[0].trim();
            right = split[1].trim();
            bottom = split[2].trim();
            left = split[3].trim();
            if(left === o){

              left = null;
            };
            if(top === o){

              top = null;
            };
            if(right === o){

              right = null;
            };
            if(bottom === o){

              bottom = null;
            };
            if(top != null){

              top = parseInt(top, 10);
            };
            if(right != null){

              right = parseInt(right, 10);
            };
            if(bottom != null){

              bottom = parseInt(bottom, 10);
            };
            if(left != null){

              left = parseInt(left, 10);
            };
            if(right != null && left != null){

              width = right - left;
            } else if(right != null){

              width = right;
            };
            if(bottom != null && top != null){

              height = bottom - top;
            } else if(bottom != null){

              height = bottom;
            };
          } else {

            throw new Error(g + clip);
          };
        };
        return {
          left : left || null,
          top : top || null,
          width : width || null,
          height : height || null
        };
      },
      set : function(element, map){

        if(!map){

          element.style.clip = m;
          return;
        };
        var left = map.left;
        var top = map.top;
        var width = map.width;
        var height = map.height;
        var right,bottom;
        if(left == null){

          right = (width == null ? o : width + f);
          left = o;
        } else {

          right = (width == null ? o : left + width + f);
          left = left + f;
        };
        if(top == null){

          bottom = (height == null ? o : height + f);
          top = o;
        } else {

          bottom = (height == null ? o : top + height + f);
          top = top + f;
        };
        element.style.clip = b + top + p + right + p + bottom + p + left + c;
      },
      reset : function(element){

        element.style.clip = n;
      }
    }
  });
})();
(function(){

  var a = "css.float",b = "foo",c = "css.borderimage.standardsyntax",d = "borderRadius",e = "boxSizing",f = "stretch",g = 'm11',h = "content",j = "css.inlineblock",k = "css.gradient.filter",l = "css.appearance",m = "css.opacity",n = "div",o = "pointerEvents",p = "css.gradient.radial",q = "css.pointerevents",r = "input",s = "color",t = "string",u = "borderImage",v = "userSelect",w = "styleFloat",x = "css.textShadow.filter",y = "css.usermodify",z = "css.boxsizing",A = 'url("foo.png") 4 4 4 4 fill stretch',B = "css.boxmodel",C = "qx.bom.client.Css",D = "appearance",E = "placeholder",F = "-moz-none",G = "backgroundImage",H = "css.textShadow",I = "DXImageTransform.Microsoft.Shadow",J = "css.boxshadow",K = "css.alphaimageloaderneeded",L = "css.gradient.legacywebkit",M = "css.borderradius",N = "linear-gradient(0deg, #fff, #000)",O = "textShadow",P = "auto",Q = "css.borderimage",R = "foo.png",S = "rgba(1, 2, 3, 0.5)",T = "color=#666666,direction=45",U = "radial-gradient(0px 0px, cover, red 50%, blue 100%)",V = "rgba",W = "(",X = 'url("foo.png") 4 4 4 4 stretch',Y = "css.gradient.linear",ba = "DXImageTransform.Microsoft.Gradient",bb = "css.userselect",bc = "span",bd = "-webkit-gradient(linear,0% 0%,100% 100%,from(white), to(red))",be = "mshtml",bf = "css.rgba",bg = ");",bh = "4 fill",bi = 'WebKitCSSMatrix',bj = "none",bk = "startColorStr=#550000FF, endColorStr=#55FFFF00",bl = "progid:",bm = "css.placeholder",bn = "css.userselect.none",bo = "css.textoverflow",bp = "inline-block",bq = "-moz-inline-box",br = "textOverflow",bs = "userModify",bt = "boxShadow",bu = "cssFloat",bv = "border";
  qx.Bootstrap.define(C, {
    statics : {
      __ds : null,
      getBoxModel : function(){

        var content = qx.bom.client.Engine.getName() !== be || !qx.bom.client.Browser.getQuirksMode();
        return content ? h : bv;
      },
      getTextOverflow : function(){

        return qx.bom.Style.getPropertyName(br);
      },
      getPlaceholder : function(){

        var i = document.createElement(r);
        return E in i;
      },
      getAppearance : function(){

        return qx.bom.Style.getPropertyName(D);
      },
      getBorderRadius : function(){

        return qx.bom.Style.getPropertyName(d);
      },
      getBoxShadow : function(){

        return qx.bom.Style.getPropertyName(bt);
      },
      getBorderImage : function(){

        return qx.bom.Style.getPropertyName(u);
      },
      getBorderImageSyntax : function(){

        var styleName = qx.bom.client.Css.getBorderImage();
        if(!styleName){

          return null;
        };
        var el = document.createElement(n);
        if(styleName === u){

          el.style[styleName] = A;
          if(el.style.borderImageSource.indexOf(R) >= 0 && el.style.borderImageSlice.indexOf(bh) >= 0 && el.style.borderImageRepeat.indexOf(f) >= 0){

            return true;
          };
        } else {

          el.style[styleName] = X;
          if(el.style[styleName].indexOf(R) >= 0){

            return false;
          };
        };
        return null;
      },
      getUserSelect : function(){

        return qx.bom.Style.getPropertyName(v);
      },
      getUserSelectNone : function(){

        var styleProperty = qx.bom.client.Css.getUserSelect();
        if(styleProperty){

          var el = document.createElement(bc);
          el.style[styleProperty] = F;
          return el.style[styleProperty] === F ? F : bj;
        };
        return null;
      },
      getUserModify : function(){

        return qx.bom.Style.getPropertyName(bs);
      },
      getFloat : function(){

        var style = document.documentElement.style;
        return style.cssFloat !== undefined ? bu : style.styleFloat !== undefined ? w : null;
      },
      getTranslate3d : function(){

        return bi in window && g in new WebKitCSSMatrix();
      },
      getLinearGradient : function(){

        qx.bom.client.Css.__ds = false;
        var value = N;
        var el = document.createElement(n);
        var style = qx.bom.Style.getAppliedStyle(el, G, value);
        if(!style){

          value = bd;
          var style = qx.bom.Style.getAppliedStyle(el, G, value, false);
          if(style){

            qx.bom.client.Css.__ds = true;
          };
        };
        if(!style){

          return null;
        };
        var match = /(.*?)\(/.exec(style);
        return match ? match[1] : null;
      },
      getFilterGradient : function(){

        return qx.bom.client.Css.__dt(ba, bk);
      },
      getRadialGradient : function(){

        var value = U;
        var el = document.createElement(n);
        var style = qx.bom.Style.getAppliedStyle(el, G, value);
        if(!style){

          return null;
        };
        var match = /(.*?)\(/.exec(style);
        return match ? match[1] : null;
      },
      getLegacyWebkitGradient : function(){

        if(qx.bom.client.Css.__ds === null){

          qx.bom.client.Css.getLinearGradient();
        };
        return qx.bom.client.Css.__ds;
      },
      getRgba : function(){

        var el;
        try{

          el = document.createElement(n);
        } catch(ex) {

          el = document.createElement();
        };
        try{

          el.style[s] = S;
          if(el.style[s].indexOf(V) != -1){

            return true;
          };
        } catch(ex) {
        };
        return false;
      },
      getBoxSizing : function(){

        return qx.bom.Style.getPropertyName(e);
      },
      getInlineBlock : function(){

        var el = document.createElement(bc);
        el.style.display = bp;
        if(el.style.display == bp){

          return bp;
        };
        el.style.display = bq;
        if(el.style.display !== bq){

          return bq;
        };
        return null;
      },
      getOpacity : function(){

        return (typeof document.documentElement.style.opacity == t);
      },
      getTextShadow : function(){

        return !!qx.bom.Style.getPropertyName(O);
      },
      getFilterTextShadow : function(){

        return qx.bom.client.Css.__dt(I, T);
      },
      __dt : function(filterClass, initParams){

        var supported = false;
        var value = bl + filterClass + W + initParams + bg;
        var el = document.createElement(n);
        document.body.appendChild(el);
        el.style.filter = value;
        if(el.filters && el.filters.length > 0 && el.filters.item(filterClass).enabled == true){

          supported = true;
        };
        document.body.removeChild(el);
        return supported;
      },
      getAlphaImageLoaderNeeded : function(){

        return qx.bom.client.Engine.getName() == be && qx.bom.client.Browser.getDocumentMode() < 9;
      },
      getPointerEvents : function(){

        var el = document.documentElement;
        if(o in el.style){

          var initial = el.style.pointerEvents;
          el.style.pointerEvents = P;
          el.style.pointerEvents = b;
          var supported = el.style.pointerEvents == P;
          el.style.pointerEvents = initial;
          return supported;
        };
        return false;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(bo, statics.getTextOverflow);
      qx.core.Environment.add(bm, statics.getPlaceholder);
      qx.core.Environment.add(M, statics.getBorderRadius);
      qx.core.Environment.add(J, statics.getBoxShadow);
      qx.core.Environment.add(Y, statics.getLinearGradient);
      qx.core.Environment.add(k, statics.getFilterGradient);
      qx.core.Environment.add(p, statics.getRadialGradient);
      qx.core.Environment.add(L, statics.getLegacyWebkitGradient);
      qx.core.Environment.add(B, statics.getBoxModel);
      qx.core.Environment.add(bf, statics.getRgba);
      qx.core.Environment.add(Q, statics.getBorderImage);
      qx.core.Environment.add(c, statics.getBorderImageSyntax);
      qx.core.Environment.add(y, statics.getUserModify);
      qx.core.Environment.add(bb, statics.getUserSelect);
      qx.core.Environment.add(bn, statics.getUserSelectNone);
      qx.core.Environment.add(l, statics.getAppearance);
      qx.core.Environment.add(a, statics.getFloat);
      qx.core.Environment.add(z, statics.getBoxSizing);
      qx.core.Environment.add(j, statics.getInlineBlock);
      qx.core.Environment.add(m, statics.getOpacity);
      qx.core.Environment.add(H, statics.getTextShadow);
      qx.core.Environment.add(x, statics.getFilterTextShadow);
      qx.core.Environment.add(K, statics.getAlphaImageLoaderNeeded);
      qx.core.Environment.add(q, statics.getPointerEvents);
    }
  });
})();
(function(){

  var a = "qx.lang.Object";
  qx.Bootstrap.define(a, {
    statics : {
      empty : function(map){

        {
        };
        for(var key in map){

          if(map.hasOwnProperty(key)){

            delete map[key];
          };
        };
      },
      isEmpty : function(map){

        {
        };
        for(var key in map){

          return false;
        };
        return true;
      },
      getLength : qx.Bootstrap.objectGetLength,
      getValues : function(map){

        {
        };
        var arr = [];
        var keys = Object.keys(map);
        for(var i = 0,l = keys.length;i < l;i++){

          arr.push(map[keys[i]]);
        };
        return arr;
      },
      mergeWith : qx.Bootstrap.objectMergeWith,
      clone : function(source, deep){

        if(qx.lang.Type.isObject(source)){

          var clone = {
          };
          for(var key in source){

            if(deep){

              clone[key] = qx.lang.Object.clone(source[key], deep);
            } else {

              clone[key] = source[key];
            };
          };
          return clone;
        } else if(qx.lang.Type.isArray(source)){

          var clone = [];
          for(var i = 0;i < source.length;i++){

            if(deep){

              clone[i] = qx.lang.Object.clone(source[i]);
            } else {

              clone[i] = source[i];
            };
          };
          return clone;
        };
        return source;
      },
      invert : function(map){

        {
        };
        var result = {
        };
        for(var key in map){

          result[map[key].toString()] = key;
        };
        return result;
      },
      getKeyFromValue : function(map, value){

        {
        };
        for(var key in map){

          if(map.hasOwnProperty(key) && map[key] === value){

            return key;
          };
        };
        return null;
      },
      contains : function(map, value){

        {
        };
        return this.getKeyFromValue(map, value) !== null;
      },
      fromArray : function(array){

        {
        };
        var obj = {
        };
        for(var i = 0,l = array.length;i < l;i++){

          {
          };
          obj[array[i].toString()] = true;
        };
        return obj;
      }
    }
  });
})();
(function(){

  var a = "css.float",b = "px",c = "Cascaded styles are not supported in this browser!",d = "css.appearance",e = "pixelRight",f = "float",g = "css.userselect",h = "css.boxsizing",i = "css.textoverflow",j = "pixelHeight",k = ":",l = "pixelTop",m = "browser.documentmode",n = "css.borderimage",o = "pixelLeft",p = "engine.name",q = "css.usermodify",r = "mshtml",s = "qx.bom.element.Style",t = "",u = "pixelBottom",v = "pixelWidth",w = ";",x = "style";
  qx.Bootstrap.define(s, {
    statics : {
      __du : null,
      __dv : null,
      __dw : function(){

        var styleNames = {
          "appearance" : qx.core.Environment.get(d),
          "userSelect" : qx.core.Environment.get(g),
          "textOverflow" : qx.core.Environment.get(i),
          "borderImage" : qx.core.Environment.get(n),
          "float" : qx.core.Environment.get(a),
          "userModify" : qx.core.Environment.get(q),
          "boxSizing" : qx.core.Environment.get(h)
        };
        this.__dv = {
        };
        for(var key in qx.lang.Object.clone(styleNames)){

          if(!styleNames[key]){

            delete styleNames[key];
          } else {

            this.__dv[key] = key == f ? f : qx.bom.Style.getCssName(styleNames[key]);
          };
        };
        this.__du = styleNames;
      },
      __dx : function(name){

        var styleName = qx.bom.Style.getPropertyName(name);
        if(styleName){

          this.__du[name] = styleName;
        };
        return styleName;
      },
      __dy : {
        width : v,
        height : j,
        left : o,
        right : e,
        top : l,
        bottom : u
      },
      __dz : {
        clip : qx.bom.element.Clip,
        cursor : qx.bom.element.Cursor,
        opacity : qx.bom.element.Opacity,
        boxSizing : qx.bom.element.BoxSizing
      },
      compile : function(map){

        var html = [];
        var special = this.__dz;
        var cssNames = this.__dv;
        var name,value;
        for(name in map){

          value = map[name];
          if(value == null){

            continue;
          };
          name = this.__du[name] || this.__dx(name) || name;
          if(special[name]){

            html.push(special[name].compile(value));
          } else {

            if(!cssNames[name]){

              cssNames[name] = qx.bom.Style.getCssName(name);
            };
            html.push(cssNames[name], k, value, w);
          };
        };
        return html.join(t);
      },
      setCss : function(element, value){

        if(qx.core.Environment.get(p) === r && parseInt(qx.core.Environment.get(m), 10) < 8){

          element.style.cssText = value;
        } else {

          element.setAttribute(x, value);
        };
      },
      getCss : function(element){

        if(qx.core.Environment.get(p) === r && parseInt(qx.core.Environment.get(m), 10) < 8){

          return element.style.cssText.toLowerCase();
        } else {

          return element.getAttribute(x);
        };
      },
      isPropertySupported : function(propertyName){

        return (this.__dz[propertyName] || this.__du[propertyName] || propertyName in document.documentElement.style);
      },
      COMPUTED_MODE : 1,
      CASCADED_MODE : 2,
      LOCAL_MODE : 3,
      set : function(element, name, value, smart){

        {
        };
        name = this.__du[name] || this.__dx(name) || name;
        if(smart !== false && this.__dz[name]){

          this.__dz[name].set(element, value);
        } else {

          element.style[name] = value !== null ? value : t;
        };
      },
      setStyles : function(element, styles, smart){

        {
        };
        var styleNames = this.__du;
        var special = this.__dz;
        var style = element.style;
        for(var key in styles){

          var value = styles[key];
          var name = styleNames[key] || this.__dx(key) || key;
          if(value === undefined){

            if(smart !== false && special[name]){

              special[name].reset(element);
            } else {

              style[name] = t;
            };
          } else {

            if(smart !== false && special[name]){

              special[name].set(element, value);
            } else {

              style[name] = value !== null ? value : t;
            };
          };
        };
      },
      reset : function(element, name, smart){

        name = this.__du[name] || this.__dx(name) || name;
        if(smart !== false && this.__dz[name]){

          this.__dz[name].reset(element);
        } else {

          element.style[name] = t;
        };
      },
      get : function(element, name, mode, smart){

        name = this.__du[name] || this.__dx(name) || name;
        if(smart !== false && this.__dz[name]){

          return this.__dz[name].get(element, mode);
        };
        switch(mode){case this.LOCAL_MODE:
        return element.style[name] || t;case this.CASCADED_MODE:
        if(element.currentStyle){

          return element.currentStyle[name] || t;
        };
        throw new Error(c);default:
        var doc = qx.dom.Node.getDocument(element);
        var getStyle = doc.defaultView ? doc.defaultView.getComputedStyle : undefined;
        if(getStyle !== undefined){

          var computed = getStyle(element, null);
          if(computed && computed[name]){

            return computed[name];
          };
        } else {

          if(!element.currentStyle){

            return element.style[name] || t;
          };
          var currentStyle = element.currentStyle[name] || element.style[name] || t;
          if(/^-?[\.\d]+(px)?$/i.test(currentStyle)){

            return currentStyle;
          };
          var pixel = this.__dy[name];
          if(pixel && (pixel in element.style)){

            var localStyle = element.style[name];
            element.style[name] = currentStyle || 0;
            var value = element.style[pixel] + b;
            element.style[name] = localStyle;
            return value;
          };
          return currentStyle;
        };
        return element.style[name] || t;};
      }
    },
    defer : function(statics){

      statics.__dw();
    }
  });
})();
(function(){

  var a = "borderBottomWidth",b = "scroll",c = "qx.bom.element.Location",d = "engine.version",e = "paddingLeft",f = "borderRightWidth",g = "auto",h = "static",i = "borderTopWidth",j = "borderLeftWidth",k = "marginBottom",l = "marginTop",m = "overflowY",n = "marginLeft",o = "border-box",p = "padding",q = "paddingBottom",r = "paddingTop",s = "gecko",t = "marginRight",u = "browser.quirksmode",v = "mshtml",w = "engine.name",x = "position",y = "margin",z = "paddingRight",A = "BODY",B = "overflowX",C = "border",D = "browser.documentmode";
  qx.Bootstrap.define(c, {
    statics : {
      __dA : function(elem, style){

        return qx.bom.element.Style.get(elem, style, qx.bom.element.Style.COMPUTED_MODE, false);
      },
      __dB : function(elem, style){

        return parseInt(qx.bom.element.Style.get(elem, style, qx.bom.element.Style.COMPUTED_MODE, false), 10) || 0;
      },
      __dC : function(elem){

        var left = 0,top = 0;
        var win = qx.dom.Node.getWindow(elem);
        left -= qx.bom.Viewport.getScrollLeft(win);
        top -= qx.bom.Viewport.getScrollTop(win);
        return {
          left : left,
          top : top
        };
      },
      __dD : qx.core.Environment.select(w, {
        "mshtml" : function(elem){

          var doc = qx.dom.Node.getDocument(elem);
          var body = doc.body;
          var left = 0;
          var top = 0;
          left -= body.clientLeft + doc.documentElement.clientLeft;
          top -= body.clientTop + doc.documentElement.clientTop;
          if(!qx.core.Environment.get(u)){

            left += this.__dB(body, j);
            top += this.__dB(body, i);
          };
          return {
            left : left,
            top : top
          };
        },
        "webkit" : function(elem){

          var doc = qx.dom.Node.getDocument(elem);
          var body = doc.body;
          var left = body.offsetLeft;
          var top = body.offsetTop;
          if(parseFloat(qx.core.Environment.get(d)) < 530.17){

            left += this.__dB(body, j);
            top += this.__dB(body, i);
          };
          return {
            left : left,
            top : top
          };
        },
        "gecko" : function(elem){

          var body = qx.dom.Node.getDocument(elem).body;
          var left = body.offsetLeft;
          var top = body.offsetTop;
          if(parseFloat(qx.core.Environment.get(d)) < 1.9){

            left += this.__dB(body, n);
            top += this.__dB(body, l);
          };
          if(qx.bom.element.BoxSizing.get(body) !== o){

            left += this.__dB(body, j);
            top += this.__dB(body, i);
          };
          return {
            left : left,
            top : top
          };
        },
        "default" : function(elem){

          var body = qx.dom.Node.getDocument(elem).body;
          var left = body.offsetLeft;
          var top = body.offsetTop;
          return {
            left : left,
            top : top
          };
        }
      }),
      __dE : function(elem){

        var rect = elem.getBoundingClientRect();
        return {
          left : Math.round(rect.left),
          top : Math.round(rect.top)
        };
      },
      get : function(elem, mode){

        if(elem.tagName == A){

          var location = this.__dF(elem);
          var left = location.left;
          var top = location.top;
        } else {

          var body = this.__dD(elem);
          var offset = this.__dE(elem);
          var scroll = this.__dC(elem);
          var left = offset.left + body.left - scroll.left;
          var top = offset.top + body.top - scroll.top;
        };
        var right = left + elem.offsetWidth;
        var bottom = top + elem.offsetHeight;
        if(mode){

          if(mode == p || mode == b){

            var overX = qx.bom.element.Style.get(elem, B);
            if(overX == b || overX == g){

              right += elem.scrollWidth - elem.offsetWidth + this.__dB(elem, j) + this.__dB(elem, f);
            };
            var overY = qx.bom.element.Style.get(elem, m);
            if(overY == b || overY == g){

              bottom += elem.scrollHeight - elem.offsetHeight + this.__dB(elem, i) + this.__dB(elem, a);
            };
          };
          switch(mode){case p:
          left += this.__dB(elem, e);
          top += this.__dB(elem, r);
          right -= this.__dB(elem, z);
          bottom -= this.__dB(elem, q);case b:
          left -= elem.scrollLeft;
          top -= elem.scrollTop;
          right -= elem.scrollLeft;
          bottom -= elem.scrollTop;case C:
          left += this.__dB(elem, j);
          top += this.__dB(elem, i);
          right -= this.__dB(elem, f);
          bottom -= this.__dB(elem, a);
          break;case y:
          left -= this.__dB(elem, n);
          top -= this.__dB(elem, l);
          right += this.__dB(elem, t);
          bottom += this.__dB(elem, k);
          break;};
        };
        return {
          left : left,
          top : top,
          right : right,
          bottom : bottom
        };
      },
      __dF : function(body){

        var top = body.offsetTop;
        var left = body.offsetLeft;
        if(qx.core.Environment.get(w) !== v || !((parseFloat(qx.core.Environment.get(d)) < 8 || qx.core.Environment.get(D) < 8) && !qx.core.Environment.get(u))){

          top += this.__dB(body, l);
          left += this.__dB(body, n);
        };
        if(qx.core.Environment.get(w) === s){

          top += this.__dB(body, j);
          left += this.__dB(body, i);
        };
        return {
          left : left,
          top : top
        };
      },
      getLeft : function(elem, mode){

        return this.get(elem, mode).left;
      },
      getTop : function(elem, mode){

        return this.get(elem, mode).top;
      },
      getRight : function(elem, mode){

        return this.get(elem, mode).right;
      },
      getBottom : function(elem, mode){

        return this.get(elem, mode).bottom;
      },
      getRelative : function(elem1, elem2, mode1, mode2){

        var loc1 = this.get(elem1, mode1);
        var loc2 = this.get(elem2, mode2);
        return {
          left : loc1.left - loc2.left,
          top : loc1.top - loc2.top,
          right : loc1.right - loc2.right,
          bottom : loc1.bottom - loc2.bottom
        };
      },
      getPosition : function(elem){

        return this.getRelative(elem, this.getOffsetParent(elem));
      },
      getOffsetParent : function(element){

        var offsetParent = element.offsetParent || document.body;
        var Style = qx.bom.element.Style;
        while(offsetParent && (!/^body|html$/i.test(offsetParent.tagName) && Style.get(offsetParent, x) === h)){

          offsetParent = offsetParent.offsetParent;
        };
        return offsetParent;
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Swipe";
  qx.Class.define(a, {
    extend : qx.event.type.Touch,
    members : {
      _cloneNativeEvent : function(nativeEvent, clone){

        var clone = qx.event.type.Touch.prototype._cloneNativeEvent.call(this, nativeEvent, clone);
        clone.swipe = nativeEvent.swipe;
        return clone;
      },
      _isTouchEnd : function(){

        return true;
      },
      getStartTime : function(){

        return this._native.swipe.startTime;
      },
      getDuration : function(){

        return this._native.swipe.duration;
      },
      getAxis : function(){

        return this._native.swipe.axis;
      },
      getDirection : function(){

        return this._native.swipe.direction;
      },
      getVelocity : function(){

        return this._native.swipe.velocity;
      },
      getDistance : function(){

        return this._native.swipe.distance;
      }
    }
  });
})();
(function(){

  var a = "touchmove",b = "os.name",c = "MSPointerDown",d = "swipe",e = "android",f = "engine.version",g = "pointercancel",h = "qx.event.handler.TouchCore",j = "event.mspointer",k = "MSPointerCancel",l = "y",m = "pointer-events",n = "longtap",o = "pointerup",p = "touchend",q = "pointerdown",r = "MSPointerUp",s = "right",t = "engine.name",u = "x",v = "touchcancel",w = "MSPointerMove",z = "webkit",A = "none",B = "left",C = "tap",D = "pointermove",E = "down",F = "undefined",G = "up",H = "touchstart";
  qx.Bootstrap.define(h, {
    extend : Object,
    statics : {
      TAP_MAX_DISTANCE : qx.core.Environment.get(b) != e ? 10 : 40,
      SWIPE_DIRECTION : {
        x : [B, s],
        y : [G, E]
      },
      SWIPE_MIN_DISTANCE : qx.core.Environment.get(b) != e ? 11 : 41,
      SWIPE_MIN_VELOCITY : 0,
      LONGTAP_TIME : 500
    },
    construct : function(target, emitter){

      this.__dG = target;
      this.__dH = emitter;
      this._initTouchObserver();
      this.__dI = [];
    },
    members : {
      __dG : null,
      __dH : null,
      __dJ : null,
      __dK : null,
      __dL : null,
      __dM : null,
      __dN : null,
      __dO : null,
      __dP : null,
      __dQ : null,
      __dR : null,
      __dS : null,
      __dT : null,
      __dI : null,
      __dU : null,
      _initTouchObserver : function(){

        this.__dJ = qx.lang.Function.listener(this._onTouchEvent, this);
        this.__dU = [H, a, p, v];
        if(qx.core.Environment.get(j)){

          var engineVersion = parseInt(qx.core.Environment.get(f), 10);
          if(engineVersion == 10){

            this.__dU = [c, w, r, k];
          } else {

            this.__dU = [q, D, o, g];
          };
        };
        for(var i = 0;i < this.__dU.length;i++){

          qx.bom.Event.addNativeListener(this.__dG, this.__dU[i], this.__dJ);
        };
      },
      _stopTouchObserver : function(){

        for(var i = 0;i < this.__dU.length;i++){

          qx.bom.Event.removeNativeListener(this.__dG, this.__dU[i], this.__dJ);
        };
      },
      _onTouchEvent : function(domEvent){

        this._commonTouchEventHandler(domEvent);
      },
      _getScalingDistance : function(touch0, touch1){

        return (Math.sqrt(Math.pow(touch0.pageX - touch1.pageX, 2) + Math.pow(touch0.pageY - touch1.pageY, 2)));
      },
      _getRotationAngle : function(touch0, touch1){

        var x = touch0.pageX - touch1.pageX;
        var y = touch0.pageY - touch1.pageY;
        return (Math.atan2(y, x) * 180 / Math.PI);
      },
      _commonTouchEventHandler : function(domEvent, type){

        var type = type || domEvent.type;
        if(qx.core.Environment.get(j)){

          type = this._mapPointerEvent(type);
          var touches = this._detectTouchesByPointer(domEvent, type);
          domEvent.changedTouches = touches;
          domEvent.targetTouches = touches;
          domEvent.touches = touches;
        };
        if(type == H){

          this.__dK = this._getTarget(domEvent);
          this.__dP = true;
          if(domEvent.touches && domEvent.touches.length > 1){

            this.__dR = this._getScalingDistance(domEvent.touches[0], domEvent.touches[1]);
            this.__dS = this._getRotationAngle(domEvent.touches[0], domEvent.touches[1]);
          };
        };
        if(type == a){

          if(typeof domEvent.scale == F && domEvent.targetTouches.length > 1){

            var currentScalingDistance = this._getScalingDistance(domEvent.targetTouches[0], domEvent.targetTouches[1]);
            domEvent.scale = currentScalingDistance / this.__dR;
          };
          if(typeof domEvent.rotation == F && domEvent.targetTouches.length > 1){

            var currentRotation = this._getRotationAngle(domEvent.targetTouches[0], domEvent.targetTouches[1]);
            domEvent.rotation = currentRotation - this.__dS;
          };
          if(this.__dP){

            this.__dP = this._isBelowTapMaxDistance(domEvent.changedTouches[0]);
          };
        };
        this._fireEvent(domEvent, type, this.__dK);
        this.__dW(domEvent, type);
        if(qx.core.Environment.get(j)){

          if(type == p || type == v){

            delete this.__dI[domEvent.pointerId];
          };
        };
      },
      _detectTouchesByPointer : function(domEvent, type){

        var touches = [];
        if(type == H){

          this.__dI[domEvent.pointerId] = domEvent;
        } else if(type == a){

          this.__dI[domEvent.pointerId] = domEvent;
        };
        for(var pointerId in this.__dI){

          var pointer = this.__dI[pointerId];
          touches.push(pointer);
        };
        return touches;
      },
      _mapPointerEvent : function(type){

        type = type.toLowerCase();
        if(type.indexOf(q) !== -1){

          return H;
        } else if(type.indexOf(o) !== -1){

          return p;
        } else if(type.indexOf(D) !== -1){

          if(this.__dQ === true){

            return a;
          };
        } else if(type.indexOf(g) !== -1){

          return v;
        };;;
        return type;
      },
      _isBelowTapMaxDistance : function(touch){

        var deltaCoordinates = {
          x : touch.screenX - this.__dL,
          y : touch.screenY - this.__dM
        };
        var clazz = qx.event.handler.TouchCore;
        return (Math.abs(deltaCoordinates.x) <= clazz.TAP_MAX_DISTANCE && Math.abs(deltaCoordinates.y) <= clazz.TAP_MAX_DISTANCE);
      },
      _getTarget : function(domEvent){

        var target = qx.bom.Event.getTarget(domEvent);
        if(qx.core.Environment.get(t) == z){

          if(target && target.nodeType == 3){

            target = target.parentNode;
          };
        } else if(qx.core.Environment.get(j)){

          var targetForIE = this.__dV(domEvent);
          if(targetForIE){

            target = targetForIE;
          };
        };
        return target;
      },
      __dV : function(domEvent){

        var clientX = null;
        var clientY = null;
        if(domEvent && domEvent.touches && domEvent.touches.length !== 0){

          clientX = domEvent.touches[0].clientX;
          clientY = domEvent.touches[0].clientY;
        };
        var hitTargets = document.msElementsFromPoint(clientX, clientY);
        if(hitTargets){

          for(var i = 0;i < hitTargets.length;i++){

            var currentTarget = hitTargets[i];
            var pointerEvents = qx.bom.element.Style.get(currentTarget, m, 3);
            if(pointerEvents != A){

              return currentTarget;
            };
          };
        };
        return null;
      },
      _fireEvent : function(domEvent, type, target){

        if(!target){

          target = this._getTarget(domEvent);
        };
        var type = type || domEvent.type;
        if(target && target.nodeType && this.__dH){

          this.__dH.emit(type, domEvent);
        };
      },
      __dW : function(domEvent, type, target){

        if(!target){

          target = this._getTarget(domEvent);
        };
        var type = type || domEvent.type;
        if(type == H){

          this.__dX(domEvent, target);
        } else if(type == a){

          this.__dY(domEvent, target);
        } else if(type == p){

          this.__ea(domEvent, target);
        };;
      },
      __dX : function(domEvent, target){

        var touch = domEvent.changedTouches[0];
        this.__dQ = true;
        this.__dL = touch.screenX;
        this.__dM = touch.screenY;
        this.__dN = new Date().getTime();
        this.__dO = domEvent.targetTouches.length === 1;
        if(this.__dO){

          this.__dT = window.setTimeout(this.__ec.bind(this, domEvent, target), qx.event.handler.TouchCore.LONGTAP_TIME);
        } else {

          this.__ed();
        };
      },
      __dY : function(domEvent, target){

        if(this.__dO && domEvent.changedTouches.length > 1){

          this.__dO = false;
        };
        if(!this._isBelowTapMaxDistance(domEvent.changedTouches[0])){

          this.__ed();
        };
      },
      __ea : function(domEvent, target){

        this.__dQ = false;
        this.__ed();
        if(this.__dO){

          var touch = domEvent.changedTouches[0];
          var deltaCoordinates = {
            x : touch.screenX - this.__dL,
            y : touch.screenY - this.__dM
          };
          var eventType;
          if(this.__dK == target && this.__dP){

            if(qx.event && qx.event.type && qx.event.type.Tap){

              eventType = qx.event.type.Tap;
            };
            this._fireEvent(domEvent, C, target, eventType);
          } else {

            var swipe = this.__eb(domEvent, target, deltaCoordinates);
            if(swipe){

              if(qx.event && qx.event.type && qx.event.type.Swipe){

                eventType = qx.event.type.Swipe;
              };
              domEvent.swipe = swipe;
              this._fireEvent(domEvent, d, this.__dK, eventType);
            };
          };
        };
      },
      __eb : function(domEvent, target, deltaCoordinates){

        var clazz = qx.event.handler.TouchCore;
        var duration = new Date().getTime() - this.__dN;
        var axis = (Math.abs(deltaCoordinates.x) >= Math.abs(deltaCoordinates.y)) ? u : l;
        var distance = deltaCoordinates[axis];
        var direction = clazz.SWIPE_DIRECTION[axis][distance < 0 ? 0 : 1];
        var velocity = (duration !== 0) ? distance / duration : 0;
        var swipe = null;
        if(Math.abs(velocity) >= clazz.SWIPE_MIN_VELOCITY && Math.abs(distance) >= clazz.SWIPE_MIN_DISTANCE){

          swipe = {
            startTime : this.__dN,
            duration : duration,
            axis : axis,
            direction : direction,
            distance : distance,
            velocity : velocity
          };
        };
        return swipe;
      },
      __ec : function(domEvent, target){

        this._fireEvent(domEvent, n, target, qx.event.type.Tap);
        this.__dT = null;
        this.__dP = false;
      },
      __ed : function(){

        if(this.__dT){

          window.clearTimeout(this.__dT);
          this.__dT = null;
        };
      },
      dispose : function(){

        this._stopTouchObserver();
        this.__dK = this.__dG = this.__dU = this.__dI = this.__dH = this.__dR = this.__dS = null;
        this.__ed();
      }
    }
  });
})();
(function(){

  var a = "mshtml",b = "pointerEnabled",c = "onhashchange",d = "event.help",e = "event.mspointer",f = "event.touch",g = "msPointerEnabled",h = "event.hashchange",i = "onhelp",j = "documentMode",k = "qx.bom.client.Event",l = "ontouchstart";
  qx.Bootstrap.define(k, {
    statics : {
      getTouch : function(){

        return (l in window);
      },
      getMsPointer : function(){

        if(b in window.navigator){

          return window.navigator.pointerEnabled;
        } else if(g in window.navigator){

          return window.navigator.msPointerEnabled;
        };
        return false;
      },
      getHelp : function(){

        return (i in document);
      },
      getHashChange : function(){

        var engine = qx.bom.client.Engine.getName();
        var hashchange = c in window;
        return (engine !== a && hashchange) || (engine === a && j in document && document.documentMode >= 8 && hashchange);
      }
    },
    defer : function(statics){

      qx.core.Environment.add(f, statics.getTouch);
      qx.core.Environment.add(e, statics.getMsPointer);
      qx.core.Environment.add(d, statics.getHelp);
      qx.core.Environment.add(h, statics.getHashChange);
    }
  });
})();
(function(){

  var a = "qx.event.handler.UserAction";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__cw = manager.getWindow();
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        useraction : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      __dh : null,
      __cw : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      }
    },
    destruct : function(){

      this.__dh = this.__cw = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "touchmove",b = "engine.name",c = "mouseup",d = "qx.event.handler.Touch",f = "useraction",g = "touchend",h = "mshtml",i = "event.mspointer",j = "qx.mobile.nativescroll",k = "dispose",l = "event.touch",m = "touchstart",n = "mousedown",o = "mousemove";
  qx.Class.define(d, {
    extend : qx.event.handler.TouchCore,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      this.__dh = manager;
      this.__cw = manager.getWindow();
      this.__a = this.__cw.document;
      qx.event.handler.TouchCore.apply(this, [this.__a]);
      if(!qx.core.Environment.get(i)){

        this._initMouseObserver();
      };
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        touchstart : 1,
        touchmove : 1,
        touchend : 1,
        touchcancel : 1,
        tap : 1,
        longtap : 1,
        swipe : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE + qx.event.IEventHandler.TARGET_DOCUMENT,
      IGNORE_CAN_HANDLE : true,
      MOUSE_TO_TOUCH_MAPPING : {
        "mousedown" : m,
        "mousemove" : a,
        "mouseup" : g
      }
    },
    members : {
      __ee : null,
      __dh : null,
      __cw : null,
      __a : null,
      __ef : false,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      _fireEvent : function(domEvent, type, target, eventTypeClass){

        if(!target){

          target = this._getTarget(domEvent);
        };
        var type = type || domEvent.type;
        if(target && target.nodeType){

          qx.event.Registration.fireEvent(target, type, eventTypeClass || qx.event.type.Touch, [domEvent, target, null, true, true]);
        };
        qx.event.Registration.fireEvent(this.__cw, f, qx.event.type.Data, [type]);
      },
      __eg : function(domEvent){

        var type = domEvent.type;
        var eventMapping = qx.event.handler.Touch.MOUSE_TO_TOUCH_MAPPING;
        if(eventMapping[type]){

          type = eventMapping[type];
          if(type == m && this.__eh(domEvent)){

            this.__ef = true;
          } else if(type == g){

            this.__ef = false;
          };
          var touchObject = this.__ei(domEvent);
          var touchArray = (type == g ? [] : [touchObject]);
          domEvent.touches = touchArray;
          domEvent.targetTouches = touchArray;
          domEvent.changedTouches = [touchObject];
        };
        return type;
      },
      __eh : function(domEvent){

        if((qx.core.Environment.get(b) == h)){

          var buttonIndex = 1;
        } else {

          var buttonIndex = 0;
        };
        return domEvent.button == buttonIndex;
      },
      __ei : function(domEvent){

        var target = this._getTarget(domEvent);
        return {
          clientX : domEvent.clientX,
          clientY : domEvent.clientY,
          screenX : domEvent.screenX,
          screenY : domEvent.screenY,
          pageX : domEvent.pageX,
          pageY : domEvent.pageY,
          identifier : 1,
          target : target
        };
      },
      _initMouseObserver : function(){

        if(!qx.core.Environment.get(l)){

          this.__ee = qx.lang.Function.listener(this._onMouseEvent, this);
          var Event = qx.bom.Event;
          Event.addNativeListener(this.__a, n, this.__ee);
          Event.addNativeListener(this.__a, o, this.__ee);
          Event.addNativeListener(this.__a, c, this.__ee);
        };
      },
      _stopMouseObserver : function(){

        if(!qx.core.Environment.get(l)){

          var Event = qx.bom.Event;
          Event.removeNativeListener(this.__a, n, this.__ee);
          Event.removeNativeListener(this.__a, o, this.__ee);
          Event.removeNativeListener(this.__a, c, this.__ee);
        };
      },
      _onTouchEvent : qx.event.GlobalError.observeMethod(function(domEvent){

        this._commonTouchEventHandler(domEvent);
      }),
      _onMouseEvent : qx.event.GlobalError.observeMethod(function(domEvent){

        if(!qx.core.Environment.get(l)){

          if(domEvent.type == o && !this.__ef){

            return;
          };
          var type = this.__eg(domEvent);
          this._commonTouchEventHandler(domEvent, type);
        };
      }),
      dispose : function(){

        this.__ej(k);
        this._stopMouseObserver();
        this.__dh = this.__cw = this.__a = null;
      },
      __ej : function(method, args){

        qx.event.handler.TouchCore.prototype[method].apply(this, args || []);
      }
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
      if(qx.core.Environment.get(l)){

        if(qx.core.Environment.get(j) == false){

          document.addEventListener(a, function(e){

            e.preventDefault();
          });
        };
        qx.event.Registration.getManager(document).getHandler(statics);
      };
    }
  });
})();
(function(){

  var a = "abstract",b = "Missing implementation",c = "qx.event.dispatch.AbstractBubbling";
  qx.Class.define(c, {
    extend : qx.core.Object,
    implement : qx.event.IEventDispatcher,
    type : a,
    construct : function(manager){

      this._manager = manager;
    },
    members : {
      _getParent : function(target){

        throw new Error(b);
      },
      canDispatchEvent : function(target, event, type){

        return event.getBubbles();
      },
      dispatchEvent : function(target, event, type){

        var parent = target;
        var manager = this._manager;
        var captureListeners,bubbleListeners;
        var localList;
        var listener,context;
        var currentTarget;
        var targetList = [];
        captureListeners = manager.getListeners(target, type, true);
        bubbleListeners = manager.getListeners(target, type, false);
        if(captureListeners){

          targetList.push(captureListeners);
        };
        if(bubbleListeners){

          targetList.push(bubbleListeners);
        };
        var parent = this._getParent(target);
        var bubbleList = [];
        var bubbleTargets = [];
        var captureList = [];
        var captureTargets = [];
        while(parent != null){

          captureListeners = manager.getListeners(parent, type, true);
          if(captureListeners){

            captureList.push(captureListeners);
            captureTargets.push(parent);
          };
          bubbleListeners = manager.getListeners(parent, type, false);
          if(bubbleListeners){

            bubbleList.push(bubbleListeners);
            bubbleTargets.push(parent);
          };
          parent = this._getParent(parent);
        };
        event.setEventPhase(qx.event.type.Event.CAPTURING_PHASE);
        for(var i = captureList.length - 1;i >= 0;i--){

          currentTarget = captureTargets[i];
          event.setCurrentTarget(currentTarget);
          localList = captureList[i];
          for(var j = 0,jl = localList.length;j < jl;j++){

            listener = localList[j];
            context = listener.context || currentTarget;
            {
            };
            listener.handler.call(context, event);
          };
          if(event.getPropagationStopped()){

            return;
          };
        };
        event.setEventPhase(qx.event.type.Event.AT_TARGET);
        event.setCurrentTarget(target);
        for(var i = 0,il = targetList.length;i < il;i++){

          localList = targetList[i];
          for(var j = 0,jl = localList.length;j < jl;j++){

            listener = localList[j];
            context = listener.context || target;
            {
            };
            listener.handler.call(context, event);
          };
          if(event.getPropagationStopped()){

            return;
          };
        };
        event.setEventPhase(qx.event.type.Event.BUBBLING_PHASE);
        for(var i = 0,il = bubbleList.length;i < il;i++){

          currentTarget = bubbleTargets[i];
          event.setCurrentTarget(currentTarget);
          localList = bubbleList[i];
          for(var j = 0,jl = localList.length;j < jl;j++){

            listener = localList[j];
            context = listener.context || currentTarget;
            {
            };
            listener.handler.call(context, event);
          };
          if(event.getPropagationStopped()){

            return;
          };
        };
      }
    }
  });
})();
(function(){

  var a = "qx.event.dispatch.DomBubbling";
  qx.Class.define(a, {
    extend : qx.event.dispatch.AbstractBubbling,
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL
    },
    members : {
      _getParent : function(target){

        return target.parentNode;
      },
      canDispatchEvent : function(target, event, type){

        return target.nodeType !== undefined && event.getBubbles();
      }
    },
    defer : function(statics){

      qx.event.Registration.addDispatcher(statics);
    }
  });
})();
(function(){

  var a = "touchmove",b = "qx.ui.mobile.core.EventHandler",c = "touchend",d = "touchcancel",e = "data-selectable",f = "true",g = "data-activatable",h = "touchstart",j = "false",k = "active";
  qx.Class.define(b, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(){

      qx.core.Object.call(this);
      this.__dh = qx.event.Registration.getManager(window);
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_FIRST,
      SUPPORTED_TYPES : {
        mousemove : 1,
        mouseover : 1,
        mouseout : 1,
        mousedown : 1,
        mouseup : 1,
        click : 1,
        dblclick : 1,
        contextmenu : 1,
        mousewheel : 1,
        keyup : 1,
        keydown : 1,
        keypress : 1,
        keyinput : 1,
        capture : 1,
        losecapture : 1,
        focusin : 1,
        focusout : 1,
        focus : 1,
        blur : 1,
        activate : 1,
        deactivate : 1,
        appear : 1,
        disappear : 1,
        resize : 1,
        dragstart : 1,
        dragend : 1,
        dragover : 1,
        dragleave : 1,
        drop : 1,
        drag : 1,
        dragchange : 1,
        droprequest : 1,
        touchstart : 1,
        touchend : 1,
        touchmove : 1,
        touchcancel : 1,
        tap : 1,
        longtap : 1,
        swipe : 1
      },
      IGNORE_CAN_HANDLE : false,
      __ek : null,
      __el : null,
      __em : null,
      __en : null,
      __eo : null,
      __ep : function(domEvent){

        var EventHandler = qx.ui.mobile.core.EventHandler;
        EventHandler.__el = qx.bom.Viewport.getScrollLeft();
        EventHandler.__em = qx.bom.Viewport.getScrollTop();
        var touch = domEvent.getChangedTargetTouches()[0];
        EventHandler.__en = touch.screenY;
        EventHandler.__et();
        var target = domEvent.getTarget();
        while(target && target.parentNode && target.parentNode.nodeType == 1 && qx.bom.element.Attribute.get(target, g) != f){

          target = target.parentNode;
        };
        EventHandler.__ek = target;
        EventHandler.__eq = window.setTimeout(function(){

          EventHandler.__eq = null;
          var target = EventHandler.__ek;
          if(target && (qx.bom.element.Attribute.get(target, e) != j)){

            qx.bom.element.Class.add(target, k);
          };
        }, 100);
      },
      __er : function(domEvent){

        qx.ui.mobile.core.EventHandler.__eu();
      },
      __es : function(domEvent){

        var EventHandler = qx.ui.mobile.core.EventHandler;
        var touch = domEvent.getChangedTargetTouches()[0];
        var deltaY = touch.screenY - EventHandler.__en;
        if(EventHandler.__ek && Math.abs(deltaY) >= qx.event.handler.TouchCore.TAP_MAX_DISTANCE){

          EventHandler.__eu();
        };
        if(EventHandler.__ek && (EventHandler.__el != qx.bom.Viewport.getScrollLeft() || EventHandler.__em != qx.bom.Viewport.getScrollTop())){

          EventHandler.__eu();
        };
      },
      __et : function(){

        var EventHandler = qx.ui.mobile.core.EventHandler;
        if(EventHandler.__eq){

          window.clearTimeout(EventHandler.__eq);
          EventHandler.__eq = null;
        };
      },
      __eu : function(){

        var EventHandler = qx.ui.mobile.core.EventHandler;
        EventHandler.__et();
        var activeTarget = EventHandler.__ek;
        if(activeTarget){

          qx.bom.element.Class.remove(activeTarget, k);
        };
        EventHandler.__ek = null;
      }
    },
    members : {
      __dh : null,
      canHandleEvent : function(target, type){

        return target instanceof qx.ui.mobile.core.Widget;
      },
      registerEvent : function(target, type, capture){

        var element = target.getContainerElement();
        qx.event.Registration.addListener(element, type, this._dispatchEvent, this, capture);
      },
      unregisterEvent : function(target, type, capture){

        var element = target.getContainerElement();
        qx.event.Registration.removeListener(element, type, this._dispatchEvent, this, capture);
      },
      _dispatchEvent : function(domEvent){

        var domTarget = domEvent.getTarget();
        if(!domTarget || domTarget.id == null){

          return;
        };
        var widgetTarget = qx.ui.mobile.core.Widget.getWidgetById(domTarget.id);
        if(domEvent.getRelatedTarget){

          var domRelatedTarget = domEvent.getRelatedTarget();
          if(domRelatedTarget && domRelatedTarget.id){

            var widgetRelatedTarget = qx.ui.mobile.core.Widget.getWidgetById(domRelatedTarget.id);
          };
        };
        var currentTarget = domEvent.getCurrentTarget();
        var currentWidget = qx.ui.mobile.core.Widget.getWidgetById(currentTarget.id);
        if(!currentWidget){

          return;
        };
        var capture = domEvent.getEventPhase() == qx.event.type.Event.CAPTURING_PHASE;
        var type = domEvent.getType();
        var listeners = this.__dh.getListeners(currentWidget, type, capture);
        if(!listeners || listeners.length === 0){

          return;
        };
        var widgetEvent = qx.event.Pool.getInstance().getObject(domEvent.constructor);
        domEvent.clone(widgetEvent);
        widgetEvent.setTarget(widgetTarget);
        widgetEvent.setRelatedTarget(widgetRelatedTarget || null);
        widgetEvent.setCurrentTarget(currentWidget);
        var orig = domEvent.getOriginalTarget();
        if(orig && orig.id){

          var widgetOriginalTarget = qx.ui.mobile.core.Widget.getWidgetById(orig.id);
          widgetEvent.setOriginalTarget(widgetOriginalTarget);
        } else {

          widgetEvent.setOriginalTarget(domTarget);
        };
        for(var i = 0,l = listeners.length;i < l;i++){

          var context = listeners[i].context || currentWidget;
          listeners[i].handler.call(context, widgetEvent);
        };
        if(widgetEvent.getPropagationStopped()){

          domEvent.stopPropagation();
        };
        if(widgetEvent.getDefaultPrevented()){

          domEvent.preventDefault();
        };
        qx.event.Pool.getInstance().poolObject(widgetEvent);
      }
    },
    destruct : function(){

      this.__dh = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
      qx.event.Registration.addListener(document, h, statics.__ep);
      qx.event.Registration.addListener(document, c, statics.__er);
      qx.event.Registration.addListener(document, d, statics.__er);
      qx.event.Registration.addListener(document, a, statics.__es);
    }
  });
})();
(function(){

  var b = "function",c = "html.video.h264",d = "html.element.contains",f = 'video/ogg; codecs="theora, vorbis"',g = "qxtest",h = "html.console",i = "html.xul",j = "http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul",k = "html.video.ogg",l = "http://www.w3.org/TR/SVG11/feature#BasicStructure",m = "html.storage.local",n = "div",o = "qx.bom.client.Html",p = "getSelection",q = 'audio',r = 'video/mp4; codecs="avc1.42E01E, mp4a.40.2"',s = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw==",t = "html.audio",u = "video",w = "url(#default#VML)",x = "head",y = "audio",z = "audio/mpeg",A = "org.w3c.dom.svg",B = "html.classlist",C = "html.svg",D = "html.video",E = "html.geolocation",F = "DOMTokenList",G = "html.storage.session",H = "1.1",I = "html.history.state",J = "object",K = "html.image.naturaldimensions",L = "html.audio.aif",M = "audio/x-wav",N = '<v:shape id="vml_flag1" adj="1" />',O = "html.node.isequalnode",P = "html.canvas",Q = "audio/ogg",R = "",S = "html.storage.userdata",T = "number",U = "html.element.compareDocumentPosition",V = "audio/x-aiff",W = "html.audio.au",X = "img",Y = "html.selection",ba = "selection",bb = "html.xpath",bc = "$qx_check",bd = "test",be = 'video',bf = "span",bg = "html.element.textcontent",bh = "geolocation",bi = "html.audio.mp3",bj = "html.vml",bk = "undefined",bl = "html.audio.ogg",bm = "none",bn = "label",bo = 'video/webm; codecs="vp8, vorbis"',bp = "html.dataurl",bq = "html.webworker",br = "html.dataset",bs = "1.0",bt = "html.audio.wav",bu = "html.filereader",bv = "audio/basic",bw = "display",bx = "html.video.webm",by = "#default#userdata";
  qx.Bootstrap.define(o, {
    statics : {
      getWebWorker : function(){

        return window.Worker != null;
      },
      getFileReader : function(){

        return window.FileReader != null;
      },
      getGeoLocation : function(){

        return bh in navigator;
      },
      getAudio : function(){

        return !!document.createElement(q).canPlayType;
      },
      getAudioOgg : function(){

        if(!qx.bom.client.Html.getAudio()){

          return R;
        };
        var a = document.createElement(y);
        return a.canPlayType(Q);
      },
      getAudioMp3 : function(){

        if(!qx.bom.client.Html.getAudio()){

          return R;
        };
        var a = document.createElement(y);
        return a.canPlayType(z);
      },
      getAudioWav : function(){

        if(!qx.bom.client.Html.getAudio()){

          return R;
        };
        var a = document.createElement(y);
        return a.canPlayType(M);
      },
      getAudioAu : function(){

        if(!qx.bom.client.Html.getAudio()){

          return R;
        };
        var a = document.createElement(y);
        return a.canPlayType(bv);
      },
      getAudioAif : function(){

        if(!qx.bom.client.Html.getAudio()){

          return R;
        };
        var a = document.createElement(y);
        return a.canPlayType(V);
      },
      getVideo : function(){

        return !!document.createElement(be).canPlayType;
      },
      getVideoOgg : function(){

        if(!qx.bom.client.Html.getVideo()){

          return R;
        };
        var v = document.createElement(u);
        return v.canPlayType(f);
      },
      getVideoH264 : function(){

        if(!qx.bom.client.Html.getVideo()){

          return R;
        };
        var v = document.createElement(u);
        return v.canPlayType(r);
      },
      getVideoWebm : function(){

        if(!qx.bom.client.Html.getVideo()){

          return R;
        };
        var v = document.createElement(u);
        return v.canPlayType(bo);
      },
      getLocalStorage : function(){

        try{

          var has = window.localStorage != null;
          if(has){

            window.sessionStorage.setItem(bc, bd);
            window.sessionStorage.removeItem(bc);
          };
          return has;
        } catch(exc) {

          return false;
        };
      },
      getSessionStorage : function(){

        try{

          var has = window.sessionStorage != null;
          if(has){

            window.sessionStorage.setItem(bc, bd);
            window.sessionStorage.removeItem(bc);
          };
          return has;
        } catch(exc) {

          return false;
        };
      },
      getUserDataStorage : function(){

        var el = document.createElement(n);
        el.style[bw] = bm;
        document.getElementsByTagName(x)[0].appendChild(el);
        var supported = false;
        try{

          el.addBehavior(by);
          el.load(g);
          supported = true;
        } catch(e) {
        };
        document.getElementsByTagName(x)[0].removeChild(el);
        return supported;
      },
      getClassList : function(){

        return !!(document.documentElement.classList && qx.Bootstrap.getClass(document.documentElement.classList) === F);
      },
      getXPath : function(){

        return !!document.evaluate;
      },
      getXul : function(){

        try{

          document.createElementNS(j, bn);
          return true;
        } catch(e) {

          return false;
        };
      },
      getSvg : function(){

        return document.implementation && document.implementation.hasFeature && (document.implementation.hasFeature(A, bs) || document.implementation.hasFeature(l, H));
      },
      getVml : function(){

        var el = document.createElement(n);
        document.body.appendChild(el);
        el.innerHTML = N;
        el.firstChild.style.behavior = w;
        var hasVml = typeof el.firstChild.adj == J;
        document.body.removeChild(el);
        return hasVml;
      },
      getCanvas : function(){

        return !!window.CanvasRenderingContext2D;
      },
      getDataUrl : function(callback){

        var data = new Image();
        data.onload = data.onerror = function(){

          window.setTimeout(function(){

            callback.call(null, (data.width == 1 && data.height == 1));
          }, 0);
        };
        data.src = s;
      },
      getDataset : function(){

        return !!document.documentElement.dataset;
      },
      getContains : function(){

        return (typeof document.documentElement.contains !== bk);
      },
      getCompareDocumentPosition : function(){

        return (typeof document.documentElement.compareDocumentPosition === b);
      },
      getTextContent : function(){

        var el = document.createElement(bf);
        return (typeof el.textContent !== bk);
      },
      getConsole : function(){

        return typeof window.console !== bk;
      },
      getNaturalDimensions : function(){

        var img = document.createElement(X);
        return typeof img.naturalHeight === T && typeof img.naturalWidth === T;
      },
      getHistoryState : function(){

        return (typeof window.onpopstate !== bk && typeof window.history.replaceState !== bk && typeof window.history.pushState !== bk);
      },
      getSelection : function(){

        if(typeof window.getSelection === b){

          return p;
        };
        if(typeof document.selection === J){

          return ba;
        };
        return null;
      },
      getIsEqualNode : function(){

        return typeof document.documentElement.isEqualNode === b;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(bq, statics.getWebWorker);
      qx.core.Environment.add(bu, statics.getFileReader);
      qx.core.Environment.add(E, statics.getGeoLocation);
      qx.core.Environment.add(t, statics.getAudio);
      qx.core.Environment.add(bl, statics.getAudioOgg);
      qx.core.Environment.add(bi, statics.getAudioMp3);
      qx.core.Environment.add(bt, statics.getAudioWav);
      qx.core.Environment.add(W, statics.getAudioAu);
      qx.core.Environment.add(L, statics.getAudioAif);
      qx.core.Environment.add(D, statics.getVideo);
      qx.core.Environment.add(k, statics.getVideoOgg);
      qx.core.Environment.add(c, statics.getVideoH264);
      qx.core.Environment.add(bx, statics.getVideoWebm);
      qx.core.Environment.add(m, statics.getLocalStorage);
      qx.core.Environment.add(G, statics.getSessionStorage);
      qx.core.Environment.add(S, statics.getUserDataStorage);
      qx.core.Environment.add(B, statics.getClassList);
      qx.core.Environment.add(bb, statics.getXPath);
      qx.core.Environment.add(i, statics.getXul);
      qx.core.Environment.add(P, statics.getCanvas);
      qx.core.Environment.add(C, statics.getSvg);
      qx.core.Environment.add(bj, statics.getVml);
      qx.core.Environment.add(br, statics.getDataset);
      qx.core.Environment.addAsync(bp, statics.getDataUrl);
      qx.core.Environment.add(d, statics.getContains);
      qx.core.Environment.add(U, statics.getCompareDocumentPosition);
      qx.core.Environment.add(bg, statics.getTextContent);
      qx.core.Environment.add(h, statics.getConsole);
      qx.core.Environment.add(K, statics.getNaturalDimensions);
      qx.core.Environment.add(I, statics.getHistoryState);
      qx.core.Environment.add(Y, statics.getSelection);
      qx.core.Environment.add(O, statics.getIsEqualNode);
    }
  });
})();
(function(){

  var a = "readOnly",b = "accessKey",c = "qx.bom.element.Attribute",d = "rowSpan",e = "vAlign",f = "className",g = "textContent",h = "'",i = "htmlFor",j = "longDesc",k = "cellSpacing",l = "frameBorder",m = "='",n = "",o = "useMap",p = "innerText",q = "innerHTML",r = "tabIndex",s = "dateTime",t = "maxLength",u = "html.element.textcontent",v = "mshtml",w = "engine.name",x = "cellPadding",y = "browser.documentmode",z = "colSpan",A = "undefined";
  qx.Bootstrap.define(c, {
    statics : {
      __ev : {
        names : {
          "class" : f,
          "for" : i,
          html : q,
          text : qx.core.Environment.get(u) ? g : p,
          colspan : z,
          rowspan : d,
          valign : e,
          datetime : s,
          accesskey : b,
          tabindex : r,
          maxlength : t,
          readonly : a,
          longdesc : j,
          cellpadding : x,
          cellspacing : k,
          frameborder : l,
          usemap : o
        },
        runtime : {
          "html" : 1,
          "text" : 1
        },
        bools : {
          compact : 1,
          nowrap : 1,
          ismap : 1,
          declare : 1,
          noshade : 1,
          checked : 1,
          disabled : 1,
          readOnly : 1,
          multiple : 1,
          selected : 1,
          noresize : 1,
          defer : 1,
          allowTransparency : 1
        },
        property : {
          $$html : 1,
          $$widget : 1,
          disabled : 1,
          checked : 1,
          readOnly : 1,
          multiple : 1,
          selected : 1,
          value : 1,
          maxLength : 1,
          className : 1,
          innerHTML : 1,
          innerText : 1,
          textContent : 1,
          htmlFor : 1,
          tabIndex : 1
        },
        qxProperties : {
          $$widget : 1,
          $$html : 1
        },
        propertyDefault : {
          disabled : false,
          checked : false,
          readOnly : false,
          multiple : false,
          selected : false,
          value : n,
          className : n,
          innerHTML : n,
          innerText : n,
          textContent : n,
          htmlFor : n,
          tabIndex : 0,
          maxLength : qx.core.Environment.select(w, {
            "mshtml" : 2147483647,
            "webkit" : 524288,
            "default" : -1
          })
        },
        removeableProperties : {
          disabled : 1,
          multiple : 1,
          maxLength : 1
        },
        original : {
          href : 1,
          src : 1,
          type : 1
        }
      },
      compile : function(map){

        var html = [];
        var runtime = this.__ev.runtime;
        for(var key in map){

          if(!runtime[key]){

            html.push(key, m, map[key], h);
          };
        };
        return html.join(n);
      },
      get : function(element, name){

        var hints = this.__ev;
        var value;
        name = hints.names[name] || name;
        if(qx.core.Environment.get(w) == v && parseInt(qx.core.Environment.get(y), 10) < 8 && hints.original[name]){

          value = element.getAttribute(name, 2);
        } else if(hints.property[name]){

          value = element[name];
          if(typeof hints.propertyDefault[name] !== A && value == hints.propertyDefault[name]){

            if(typeof hints.bools[name] === A){

              return null;
            } else {

              return value;
            };
          };
        } else {

          value = element.getAttribute(name);
        };
        if(hints.bools[name]){

          return !!value;
        };
        return value;
      },
      set : function(element, name, value){

        if(typeof value === A){

          return;
        };
        var hints = this.__ev;
        name = hints.names[name] || name;
        if(hints.bools[name]){

          value = !!value;
        };
        if(hints.property[name] && (!(element[name] === undefined) || hints.qxProperties[name])){

          if(value == null){

            if(hints.removeableProperties[name]){

              element.removeAttribute(name);
              return;
            } else if(typeof hints.propertyDefault[name] !== A){

              value = hints.propertyDefault[name];
            };
          };
          element[name] = value;
        } else {

          if(value === true){

            element.setAttribute(name, name);
          } else if(value === false || value === null){

            element.removeAttribute(name);
          } else {

            element.setAttribute(name, value);
          };
        };
      },
      reset : function(element, name){

        this.set(element, name, null);
      }
    }
  });
})();
(function(){

  var a = '',b = "g",c = "(^|\\s)",d = 'function',e = "(\\s|$)",f = "",g = "\\b|\\b",h = "qx.bom.element.Class",j = 'SVGAnimatedString',k = "html.classlist",m = "default",n = " ",o = 'object',p = "$2",q = "native",r = "\\b",s = 'undefined';
  qx.Bootstrap.define(h, {
    statics : {
      __ew : /\s+/g,
      __ex : /^\s+|\s+$/g,
      add : {
        "native" : function(element, name){

          element.classList.add(name);
          return name;
        },
        "default" : function(element, name){

          if(!this.has(element, name)){

            element.className += (element.className ? n : f) + name;
          };
          return name;
        }
      }[qx.core.Environment.get(k) ? q : m],
      addClasses : {
        "native" : function(element, classes){

          for(var i = 0;i < classes.length;i++){

            element.classList.add(classes[i]);
          };
          return element.className;
        },
        "default" : function(element, classes){

          var keys = {
          };
          var result;
          var old = element.className;
          if(old){

            result = old.split(this.__ew);
            for(var i = 0,l = result.length;i < l;i++){

              keys[result[i]] = true;
            };
            for(var i = 0,l = classes.length;i < l;i++){

              if(!keys[classes[i]]){

                result.push(classes[i]);
              };
            };
          } else {

            result = classes;
          };
          return element.className = result.join(n);
        }
      }[qx.core.Environment.get(k) ? q : m],
      get : function(element){

        var className = element.className;
        if(typeof className.split !== d){

          if(typeof className === o){

            if(qx.Bootstrap.getClass(className) == j){

              className = className.baseVal;
            } else {

              {
              };
              className = a;
            };
          };
          if(typeof className === s){

            {
            };
            className = a;
          };
        };
        return className;
      },
      has : {
        "native" : function(element, name){

          return element.classList.contains(name);
        },
        "default" : function(element, name){

          var regexp = new RegExp(c + name + e);
          return regexp.test(element.className);
        }
      }[qx.core.Environment.get(k) ? q : m],
      remove : {
        "native" : function(element, name){

          element.classList.remove(name);
          return name;
        },
        "default" : function(element, name){

          var regexp = new RegExp(c + name + e);
          element.className = element.className.replace(regexp, p);
          return name;
        }
      }[qx.core.Environment.get(k) ? q : m],
      removeClasses : {
        "native" : function(element, classes){

          for(var i = 0;i < classes.length;i++){

            element.classList.remove(classes[i]);
          };
          return element.className;
        },
        "default" : function(element, classes){

          var reg = new RegExp(r + classes.join(g) + r, b);
          return element.className = element.className.replace(reg, f).replace(this.__ex, f).replace(this.__ew, n);
        }
      }[qx.core.Environment.get(k) ? q : m],
      replace : function(element, oldName, newName){

        if(!this.has(element, oldName)){

          return f;
        };
        this.remove(element, oldName);
        return this.add(element, newName);
      },
      toggle : {
        "native" : function(element, name, toggle){

          if(toggle === undefined){

            element.classList.toggle(name);
          } else {

            toggle ? this.add(element, name) : this.remove(element, name);
          };
          return name;
        },
        "default" : function(element, name, toggle){

          if(toggle == null){

            toggle = !this.has(element, name);
          };
          toggle ? this.add(element, name) : this.remove(element, name);
          return name;
        }
      }[qx.core.Environment.get(k) ? q : m]
    }
  });
})();
(function(){

  var a = "qx.event.handler.Appear",b = "engine.name",c = "mshtml",d = "disappear",e = "appear",f = "browser.documentmode";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__ey = {
      };
      qx.event.handler.Appear.__ez[this.$$hash] = this;
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        appear : true,
        disappear : true
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : true,
      __ez : {
      },
      refresh : function(){

        var all = this.__ez;
        for(var hash in all){

          all[hash].refresh();
        };
      }
    },
    members : {
      __dh : null,
      __ey : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){

        var hash = qx.core.ObjectRegistry.toHashCode(target) + type;
        var targets = this.__ey;
        if(targets && !targets[hash]){

          targets[hash] = target;
          target.$$displayed = target.offsetWidth > 0;
        };
      },
      unregisterEvent : function(target, type, capture){

        var hash = qx.core.ObjectRegistry.toHashCode(target) + type;
        var targets = this.__ey;
        if(!targets){

          return;
        };
        if(targets[hash]){

          delete targets[hash];
        };
      },
      refresh : function(){

        var targets = this.__ey;
        var elem;
        var legacyIe = qx.core.Environment.get(b) == c && qx.core.Environment.get(f) < 9;
        for(var hash in targets){

          elem = targets[hash];
          var displayed = elem.offsetWidth > 0;
          if(!displayed && legacyIe){

            displayed = elem.offsetWidth > 0;
          };
          if((!!elem.$$displayed) !== displayed){

            elem.$$displayed = displayed;
            var evt = qx.event.Registration.createEvent(displayed ? e : d);
            this.__dh.dispatchEvent(elem, evt);
          };
        };
      }
    },
    destruct : function(){

      this.__dh = this.__ey = null;
      delete qx.event.handler.Appear.__ez[this.$$hash];
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.core.DomUpdatedHandler",b = "domupdated";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__ey = {
      };
      qx.ui.mobile.core.DomUpdatedHandler.__ez[this.$$hash] = this;
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        domupdated : 1
      },
      IGNORE_CAN_HANDLE : false,
      __ez : {
      },
      refresh : function(){

        var all = this.__ez;
        for(var hash in all){

          all[hash].refresh();
        };
      }
    },
    members : {
      __dh : null,
      __ey : null,
      canHandleEvent : function(target, type){

        return target instanceof qx.ui.mobile.core.Widget;
      },
      registerEvent : function(target, type, capture){

        var hash = target.$$hash;
        var targets = this.__ey;
        if(targets && !targets[hash]){

          targets[hash] = target;
        };
      },
      unregisterEvent : function(target, type, capture){

        var hash = target.$$hash;
        var targets = this.__ey;
        if(!targets){

          return;
        };
        if(targets[hash]){

          delete targets[hash];
        };
      },
      refresh : function(){

        var targets = this.__ey;
        var target;
        for(var hash in targets){

          target = targets[hash];
          if(target && !target.$$disposed && target.isSeeable()){

            var evt = qx.event.Registration.createEvent(b);
            this.__dh.dispatchEvent(target, evt);
          };
        };
      }
    },
    destruct : function(){

      this.__dh = this.__ey = null;
      delete qx.ui.mobile.core.DomUpdatedHandler.__ez[this.$$hash];
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "engine.name",b = "",c = "none",d = "qx.dom.Element",e = "webkit",f = "The tag name is missing!",g = "div";
  qx.Bootstrap.define(d, {
    statics : {
      hasChild : function(parent, child){

        return child.parentNode === parent;
      },
      hasChildren : function(element){

        return !!element.firstChild;
      },
      hasChildElements : function(element){

        element = element.firstChild;
        while(element){

          if(element.nodeType === 1){

            return true;
          };
          element = element.nextSibling;
        };
        return false;
      },
      getParentElement : function(element){

        return element.parentNode;
      },
      isInDom : function(element, win){

        if(!win){

          win = window;
        };
        var domElements = win.document.getElementsByTagName(element.nodeName);
        for(var i = 0,l = domElements.length;i < l;i++){

          if(domElements[i] === element){

            return true;
          };
        };
        return false;
      },
      insertAt : function(node, parent, index){

        var ref = parent.childNodes[index];
        if(ref){

          parent.insertBefore(node, ref);
        } else {

          parent.appendChild(node);
        };
        return true;
      },
      insertBegin : function(node, parent){

        if(parent.firstChild){

          this.insertBefore(node, parent.firstChild);
        } else {

          parent.appendChild(node);
        };
        return true;
      },
      insertEnd : function(node, parent){

        parent.appendChild(node);
        return true;
      },
      insertBefore : function(node, ref){

        ref.parentNode.insertBefore(node, ref);
        return true;
      },
      insertAfter : function(node, ref){

        var parent = ref.parentNode;
        if(ref == parent.lastChild){

          parent.appendChild(node);
        } else {

          return this.insertBefore(node, ref.nextSibling);
        };
        return true;
      },
      remove : function(node){

        if(!node.parentNode){

          return false;
        };
        node.parentNode.removeChild(node);
        return true;
      },
      removeChild : function(node, parent){

        if(node.parentNode !== parent){

          return false;
        };
        parent.removeChild(node);
        return true;
      },
      removeChildAt : function(index, parent){

        var child = parent.childNodes[index];
        if(!child){

          return false;
        };
        parent.removeChild(child);
        return true;
      },
      replaceChild : function(newNode, oldNode){

        if(!oldNode.parentNode){

          return false;
        };
        oldNode.parentNode.replaceChild(newNode, oldNode);
        return true;
      },
      replaceAt : function(newNode, index, parent){

        var oldNode = parent.childNodes[index];
        if(!oldNode){

          return false;
        };
        parent.replaceChild(newNode, oldNode);
        return true;
      },
      __eA : {
      },
      getHelperElement : function(win){

        if(!win){

          win = window;
        };
        var key = win.location.href;
        if(!qx.dom.Element.__eA[key]){

          var helper = qx.dom.Element.__eA[key] = win.document.createElement(g);
          if(qx.core.Environment.get(a) == e){

            helper.style.display = c;
            win.document.body.appendChild(helper);
          };
        };
        return qx.dom.Element.__eA[key];
      },
      create : function(name, attributes, win){

        if(!win){

          win = window;
        };
        if(!name){

          throw new Error(f);
        };
        var element = win.document.createElement(name);
        for(var key in attributes){

          qx.bom.element.Attribute.set(element, key, attributes[key]);
        };
        return element;
      },
      empty : function(element){

        return element.innerHTML = b;
      }
    }
  });
})();
(function(){

  var a = "css.transform.3d",b = "backfaceVisibility",c = "transformStyle",d = "css.transform",e = "transformOrigin",f = "qx.bom.client.CssTransform",g = "transform",h = "perspective",i = "perspectiveOrigin";
  qx.Bootstrap.define(f, {
    statics : {
      getSupport : function(){

        var name = qx.bom.client.CssTransform.getName();
        if(name != null){

          return {
            "name" : name,
            "style" : qx.bom.client.CssTransform.getStyle(),
            "origin" : qx.bom.client.CssTransform.getOrigin(),
            "3d" : qx.bom.client.CssTransform.get3D(),
            "perspective" : qx.bom.client.CssTransform.getPerspective(),
            "perspective-origin" : qx.bom.client.CssTransform.getPerspectiveOrigin(),
            "backface-visibility" : qx.bom.client.CssTransform.getBackFaceVisibility()
          };
        };
        return null;
      },
      getStyle : function(){

        return qx.bom.Style.getPropertyName(c);
      },
      getPerspective : function(){

        return qx.bom.Style.getPropertyName(h);
      },
      getPerspectiveOrigin : function(){

        return qx.bom.Style.getPropertyName(i);
      },
      getBackFaceVisibility : function(){

        return qx.bom.Style.getPropertyName(b);
      },
      getOrigin : function(){

        return qx.bom.Style.getPropertyName(e);
      },
      getName : function(){

        return qx.bom.Style.getPropertyName(g);
      },
      get3D : function(){

        return qx.bom.client.CssTransform.getPerspective() != null;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(d, statics.getSupport);
      qx.core.Environment.add(a, statics.get3D);
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.container.Composite";
  qx.Class.define(a, {
    extend : qx.ui.mobile.core.Widget,
    include : [qx.ui.mobile.core.MChildrenHandling, qx.ui.mobile.core.MLayoutHandling],
    construct : function(layout){

      qx.ui.mobile.core.Widget.call(this);
      if(layout){

        this.setLayout(layout);
      };
    },
    defer : function(statics, members){

      qx.ui.mobile.core.MChildrenHandling.remap(members);
      qx.ui.mobile.core.MLayoutHandling.remap(members);
    }
  });
})();
(function(){

  var a = "os.name",b = "Boolean",c = "px",d = "portrait",e = "os.version",f = "v",g = "auto",h = "landscape",i = "",j = "fontSize",k = "overflow-y",l = "orientationchange",m = "qx.event.type.Event",n = "ios",o = "qx.ui.mobile.core.Root",p = "root",q = "changeAppScale",r = "_applyShowScrollbarY",s = "mobile",t = "none",u = "%",v = "hidden",w = "translate3d(0,0,0)",x = "scroll";
  qx.Class.define(o, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(root, layout){

      this.__a = root || document.body;
      qx.ui.mobile.container.Composite.call(this, layout || new qx.ui.mobile.layout.VBox());
      this.addCssClass(s);
      this.addCssClass(qx.core.Environment.get(a));
      this.addCssClass(f + qx.core.Environment.get(e).charAt(0));
      qx.event.Registration.addListener(window, l, this._onOrientationChange, this);
      if(qx.core.Environment.get(a) == n && window.innerHeight != document.documentElement.clientHeight){

        qx.event.Registration.addListener(window, l, this.__eB, this);
        qx.event.Registration.addListener(window, x, this.__eB, this);
        this.__eB();
        document.body.style.webkitTransform = w;
      };
      this._onOrientationChange();
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : p
      },
      showScrollbarY : {
        check : b,
        init : true,
        apply : r
      }
    },
    events : {
      "changeAppScale" : m
    },
    members : {
      __a : null,
      _createContainerElement : function(){

        return this.__a;
      },
      _applyShowScrollbarY : function(value, old){

        this._setStyle(k, value ? g : v);
      },
      getAppScale : function(){

        var pixelRatio = parseFloat(qx.bom.client.Device.getDevicePixelRatio().toFixed(2));
        var fontScale = this.getFontScale();
        if(!isNaN(pixelRatio * fontScale)){

          return parseFloat((pixelRatio * fontScale).toFixed(2));
        } else {

          return null;
        };
      },
      getFontScale : function(){

        var fontScale = null;
        var appScale = 1;
        var fontSize = document.documentElement.style.fontSize;
        if(fontSize.indexOf(u) !== -1){

          appScale = (parseInt(fontSize, 10) / 100);
        };
        fontSize = qx.bom.element.Style.get(document.documentElement, j);
        if(fontSize.indexOf(c) !== -1){

          fontSize = parseFloat(fontSize);
          if(fontSize > 15 && fontSize < 17){

            fontSize = 16;
          };
          if(appScale !== 1){

            fontSize = Math.round(fontSize / appScale);
          };
          fontScale = (fontSize / 16);
          fontScale *= appScale;
          fontScale = parseFloat(fontScale.toFixed(3));
        };
        return fontScale;
      },
      setFontScale : function(value){

        {
        };
        var docElement = document.documentElement;
        docElement.style.fontSize = value * 100 + u;
        setTimeout(function(){

          docElement.style.display = t;
          docElement.clientWidth = docElement.clientWidth;
          docElement.style.display = i;
        }, 0);
        this.fireEvent(q);
      },
      getWidth : function(){

        return qx.bom.element.Dimension.getWidth(this.__a);
      },
      getHeight : function(){

        return qx.bom.element.Dimension.getHeight(this.__a);
      },
      __eB : function(){

        document.documentElement.style.height = window.innerHeight + c;
        if(document.body.scrollTop !== 0){

          window.scrollTo(0, 0);
        };
      },
      _onOrientationChange : function(evt){

        var isPortrait = null;
        if(evt){

          isPortrait = evt.isPortrait();
        } else {

          isPortrait = qx.bom.Viewport.isPortrait();
        };
        if(isPortrait){

          this.addCssClass(d);
          this.removeCssClass(h);
        } else {

          this.addCssClass(h);
          this.removeCssClass(d);
        };
      }
    },
    destruct : function(){

      this.__a = null;
      qx.event.Registration.removeListener(window, l, this._onOrientationChange, this);
      if(qx.core.Environment.get(a) == n){

        qx.event.Registration.removeListener(window, l, this.__eB, this);
        qx.event.Registration.removeListener(window, x, this.__eB, this);
      };
    }
  });
})();
(function(){

  var a = "abstract",b = "The layout does not support the ",c = "qx.ui.mobile.layout.Abstract",d = "updateLayout",e = " property",f = "qx.event.type.Data";
  qx.Class.define(c, {
    extend : qx.core.Object,
    type : a,
    events : {
      updateLayout : f
    },
    members : {
      _widget : null,
      __eC : null,
      __eD : null,
      _getCssClasses : function(){

        {
        };
      },
      _getSupportedChildLayoutProperties : function(){

        return null;
      },
      _setLayoutProperty : function(widget, property, value){

        {
        };
      },
      setLayoutProperties : function(widget, properties){

        if(properties == null){

          return;
        };
        var supportedChildLayoutProperties = this._getSupportedChildLayoutProperties();
        if(!supportedChildLayoutProperties){

          return;
        };
        for(var property in properties){

          if(!supportedChildLayoutProperties[property]){

            throw new Error(b + property + e);
          };
          var value = properties[property];
          this._setLayoutProperty(widget, property, value);
          this._addPropertyToChildLayoutCache(widget, property, value);
        };
      },
      connectToWidget : function(widget){

        if(this._widget){

          this._widget.removeCssClasses(this._getCssClasses());
        };
        this._widget = widget;
        if(widget){

          widget.addCssClasses(this._getCssClasses());
          if(this.__eC){

            for(var property in this.__eC){

              this.reset(property);
              this.set(property, this.__eC[property]);
            };
          };
        } else {

          this.__eC = null;
        };
      },
      connectToChildWidget : function(widget){
      },
      disconnectFromChildWidget : function(widget){
      },
      updateLayout : function(widget, action, properties){

        this.fireDataEvent(d, {
          widget : widget,
          action : action,
          properties : properties
        });
      },
      _addCachedProperty : function(property, value){

        if(!this.__eC){

          this.__eC = {
          };
        };
        this.__eC[property] = value;
      },
      _getChildLayoutPropertyValue : function(widget, property){

        var cache = this.__eE(widget);
        return cache[property];
      },
      _addPropertyToChildLayoutCache : function(widget, property, value){

        var cache = this.__eE(widget);
        if(value == null){

          delete cache[property];
        } else {

          cache[property] = value;
        };
      },
      __eE : function(widget){

        if(!this.__eD){

          this.__eD = {
          };
        };
        var cache = this.__eD;
        var hash = widget.toHashCode();
        if(!cache[hash]){

          cache[hash] = {
          };
        };
        return cache[hash];
      }
    },
    destruct : function(){

      this._widget = null;
    }
  });
})();
(function(){

  var a = "abstract",b = "_applyLayoutChange",c = "flex",d = "middle",e = "bottom",f = "boxAlignEnd",g = "boxReverse",h = "boxAlignStart",j = "center",k = "boxPackStart",l = "boxAlignCenter",m = "Boolean",n = "boxPackEnd",o = "top",p = "left",q = "right",r = "boxFlex",s = "boxPackCenter",t = "qx.ui.mobile.layout.AbstractBox";
  qx.Class.define(t, {
    extend : qx.ui.mobile.layout.Abstract,
    type : a,
    construct : function(alignX, alignY, reversed){

      qx.ui.mobile.layout.Abstract.call(this);
      if(alignX){

        this.setAlignX(alignX);
      };
      if(alignY){

        this.setAlignY(alignY);
      };
      if(reversed){

        this.setReversed(reversed);
      };
    },
    properties : {
      alignX : {
        check : [p, j, q],
        nullable : true,
        init : null,
        apply : b
      },
      alignY : {
        check : [o, d, e],
        nullable : true,
        init : null,
        apply : b
      },
      reversed : {
        check : m,
        nullable : true,
        init : null,
        apply : b
      }
    },
    statics : {
      PROPERTY_CSS_MAPPING : {
        "alignX" : {
          "hbox" : {
            "left" : k,
            "center" : s,
            "right" : n
          },
          "vbox" : {
            "left" : h,
            "center" : l,
            "right" : f
          }
        },
        "alignY" : {
          "hbox" : {
            "top" : h,
            "middle" : l,
            "bottom" : f
          },
          "vbox" : {
            "top" : k,
            "middle" : s,
            "bottom" : n
          }
        },
        "reversed" : {
          "hbox" : {
            "true" : g,
            "false" : null
          },
          "vbox" : {
            "true" : g,
            "false" : null
          }
        }
      },
      SUPPORTED_CHILD_LAYOUT_PROPERTIES : {
        "flex" : 1
      }
    },
    members : {
      _getSupportedChildLayoutProperties : function(){

        return qx.ui.mobile.layout.AbstractBox.SUPPORTED_CHILD_LAYOUT_PROPERTIES;
      },
      _setLayoutProperty : function(widget, property, value){

        if(property == c){

          var old = this._getChildLayoutPropertyValue(widget, property);
          if(old != null){

            widget.removeCssClass(r + value);
          };
          widget.addCssClass(r + value);
        };
      },
      connectToWidget : function(widget){

        if(this._widget){

          this.resetAlignX();
          this.resetAlignY();
          this.resetReversed();
        };
        qx.ui.mobile.layout.Abstract.prototype.connectToWidget.call(this, widget);
      },
      disconnectFromChildWidget : function(widget){

        qx.ui.mobile.layout.Abstract.prototype.disconnectFromChildWidget.call(this);
        for(var i = 0;i <= 6;i++){

          widget.removeCssClass(r + i);
        };
      },
      _applyLayoutChange : function(value, old, property){

        if(this._widget){

          var layoutCss = this._getCssClasses()[0];
          var CSS_MAPPING = qx.ui.mobile.layout.AbstractBox.PROPERTY_CSS_MAPPING[property][layoutCss];
          if(old){

            var oldCssClass = CSS_MAPPING[old];
            if(oldCssClass){

              this._widget.removeCssClass(oldCssClass);
            };
          };
          if(value){

            var cssClass = CSS_MAPPING[value];
            if(cssClass){

              this._widget.addCssClass(cssClass);
            };
          };
        } else {

          if(value){

            this._addCachedProperty(property, value);
          };
        };
      }
    }
  });
})();
(function(){

  var a = "vbox",b = "qx.ui.mobile.layout.VBox";
  qx.Class.define(b, {
    extend : qx.ui.mobile.layout.AbstractBox,
    members : {
      _getCssClasses : function(){

        return [a];
      }
    }
  });
})();
(function(){

  var a = "ipod",b = "pc",c = "ps3",d = ")",e = "iPhone",f = "psp",g = "wii",h = "xbox",i = "\.",j = "ipad",k = "ds",l = "(",m = "mobile",n = "device.type",o = "tablet",p = "ontouchstart",q = "g",r = "|",s = "qx.bom.client.Device",t = "desktop",u = "device.name",v = "device.touch",w = "undefined",x = "device.pixelRatio";
  qx.Bootstrap.define(s, {
    statics : {
      __cs : {
        "iPod" : a,
        "iPad" : j,
        "iPhone" : e,
        "PSP" : f,
        "PLAYSTATION 3" : c,
        "Nintendo Wii" : g,
        "Nintendo DS" : k,
        "XBOX" : h,
        "Xbox" : h
      },
      getName : function(){

        var str = [];
        for(var key in this.__cs){

          str.push(key);
        };
        var reg = new RegExp(l + str.join(r).replace(/\./g, i) + d, q);
        var match = reg.exec(navigator.userAgent);
        if(match && match[1]){

          return qx.bom.client.Device.__cs[match[1]];
        };
        return b;
      },
      getType : function(){

        return qx.bom.client.Device.detectDeviceType(navigator.userAgent);
      },
      detectDeviceType : function(userAgentString){

        if(qx.bom.client.Device.detectTabletDevice(userAgentString)){

          return o;
        } else if(qx.bom.client.Device.detectMobileDevice(userAgentString)){

          return m;
        };
        return t;
      },
      detectMobileDevice : function(userAgentString){

        return /android.+mobile|ip(hone|od)|bada\/|blackberry|BB10|maemo|opera m(ob|in)i|fennec|NetFront|phone|psp|symbian|IEMobile|windows (ce|phone)|xda/i.test(userAgentString);
      },
      detectTabletDevice : function(userAgentString){

        var isIE10Tablet = (/MSIE 10/i.test(userAgentString)) && (/ARM/i.test(userAgentString)) && !(/windows phone/i.test(userAgentString));
        var isCommonTablet = (!(/Fennec|HTC.Magic|Nexus|android.+mobile|Tablet PC/i.test(userAgentString)) && (/Android|ipad|tablet|playbook|silk|kindle|psp/i.test(userAgentString)));
        return isIE10Tablet || isCommonTablet;
      },
      getDevicePixelRatio : function(){

        if(typeof window.devicePixelRatio !== w){

          return window.devicePixelRatio;
        };
        return 1;
      },
      getTouch : function(){

        return ((p in window) || window.navigator.maxTouchPoints > 0 || window.navigator.msMaxTouchPoints > 0);
      }
    },
    defer : function(statics){

      qx.core.Environment.add(u, statics.getName);
      qx.core.Environment.add(v, statics.getTouch);
      qx.core.Environment.add(n, statics.getType);
      qx.core.Environment.add(x, statics.getDevicePixelRatio);
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "qx.bom.element.Dimension",d = "0px",e = "paddingRight",f = "engine.version",g = "paddingLeft",h = "opera",i = "paddingBottom",j = "paddingTop",k = "overflowX",l = "overflowY";
  qx.Bootstrap.define(c, {
    statics : {
      getWidth : function(element){

        var rect = element.getBoundingClientRect();
        return Math.round(rect.right) - Math.round(rect.left);
      },
      getHeight : function(element){

        var rect = element.getBoundingClientRect();
        return Math.round(rect.bottom) - Math.round(rect.top);
      },
      getSize : function(element){

        return {
          width : this.getWidth(element),
          height : this.getHeight(element)
        };
      },
      __eF : {
        visible : true,
        hidden : true
      },
      getContentWidth : function(element){

        var Style = qx.bom.element.Style;
        var overflowX = qx.bom.element.Style.get(element, k);
        var paddingLeft = parseInt(Style.get(element, g) || d, 10);
        var paddingRight = parseInt(Style.get(element, e) || d, 10);
        if(this.__eF[overflowX]){

          var contentWidth = element.clientWidth;
          if((qx.core.Environment.get(b) == h) || qx.dom.Node.isBlockNode(element)){

            contentWidth = contentWidth - paddingLeft - paddingRight;
          };
          if(qx.core.Environment.get(b) == a){

            if(contentWidth === 0 && element.offsetHeight === 0){

              return element.offsetWidth;
            };
          };
          return contentWidth;
        } else {

          if(element.clientWidth >= element.scrollWidth){

            return Math.max(element.clientWidth, element.scrollWidth) - paddingLeft - paddingRight;
          } else {

            var width = element.scrollWidth - paddingLeft;
            if(qx.core.Environment.get(b) == a && qx.core.Environment.get(f) >= 6){

              width -= paddingRight;
            };
            return width;
          };
        };
      },
      getContentHeight : function(element){

        var Style = qx.bom.element.Style;
        var overflowY = qx.bom.element.Style.get(element, l);
        var paddingTop = parseInt(Style.get(element, j) || d, 10);
        var paddingBottom = parseInt(Style.get(element, i) || d, 10);
        if(this.__eF[overflowY]){

          return element.clientHeight - paddingTop - paddingBottom;
        } else {

          if(element.clientHeight >= element.scrollHeight){

            return Math.max(element.clientHeight, element.scrollHeight) - paddingTop - paddingBottom;
          } else {

            var height = element.scrollHeight - paddingTop;
            if(qx.core.Environment.get(b) == a && qx.core.Environment.get(f) == 6){

              height -= paddingBottom;
            };
            return height;
          };
        };
      },
      getContentSize : function(element){

        return {
          width : this.getContentWidth(element),
          height : this.getContentHeight(element)
        };
      }
    }
  });
})();
(function(){

  var a = "/(objectId)/actions/(actionName)/parameters/(parameterName)/domain/",b = "Missing entity or extentUri",c = "loadEnd",d = ")",f = "success",g = "Missing instance or instance URI",h = "/actions/(actionName)",j = "Unauthorized",k = "(actionName)",l = "application/json",m = "kirra_qooxdoo.Repository",n = "PUT",o = ":",q = "Login required",r = "(objectId)/actions/(actionName)",s = "entity.",t = "yyyy/MM/dd",u = "://",v = "Please wait",w = "Missing entityFullName: ",x = "",y = "Entity not found: ",z = "(",A = "/(objectId)/relationships/(relationshipName)/domain/",B = "tap",C = "The server seems in trouble (",D = "Ok",E = "Not found",F = "DELETE",G = "POST",H = "/(objectId)/relationships/(relationshipName)/",I = "statusError",J = "_entityList",K = 'GET',L = 'string',M = "Content-Type",N = "_application",O = "Missing objectId";
  qx.Class.define(m, {
    extend : qx.core.Object,
    construct : function(applicationUri){

      qx.core.Object.call(this);
      this._parsedApplicationUri = qx.util.Uri.parseUri(applicationUri);
      this._applicationUri = applicationUri;
      var busyIndicator = new qx.ui.mobile.dialog.BusyIndicator(v);
      this._busyPopup = new qx.ui.mobile.dialog.Popup(busyIndicator);
    },
    members : {
      _level : 0,
      _application : {
      },
      _entityList : [],
      _applicationUri : null,
      _parsedApplicationUri : null,
      _busyPopup : null,
      loadApplication : function(callback){

        this.load(this._applicationUri, callback, N);
      },
      listRelationshipDomain : function(entity, objectId, relationship, callback){

        var domainUri = entity.relationshipDomainUriTemplate || (entity.extentUri + A);
        this.tryToLoad(this.resolve(domainUri, {
          objectId : objectId,
          relationshipName : relationship.name
        }), callback);
      },
      listRelatedInstances : function(entity, objectId, relationship, callback){

        var relatedInstancesUri = entity.relatedInstancesUriTemplate || (entity.extentUri + H);
        this.load(this.resolve(relatedInstancesUri, {
          objectId : objectId,
          relationshipName : relationship.name
        }), callback);
      },
      listParameterDomain : function(entity, objectId, action, parameter, callback){

        var domainUri = entity.instanceActionParameterDomainUriTemplate || (entity.extentUri + a);
        this.tryToLoad(this.resolve(domainUri, {
          objectId : objectId,
          actionName : action.name,
          parameterName : parameter.name
        }), callback);
      },
      loadEntities : function(callback, retry){

        var me = this;
        if(!me._application.entities){

          if(retry === false)return;
          this.loadApplication(function(){

            me.loadEntities(callback, false);
          });
          return;
        };
        this.load(me._application.entities, callback, J);
      },
      loadEntity : function(entityFullName, callback, retry){

        if(!entityFullName)throw Error(w + entityFullName);
        for(var i in this._entityList){

          if(this._entityList[i].fullName == entityFullName){

            var entityUri = this._entityList[i].uri;
            this.load(entityUri, callback, s + entityFullName);
            return;
          };
        };
        if(retry === false)throw Error(y + entityFullName);
        var me = this;
        this.loadEntities(function(){

          me.loadEntity(entityFullName, callback, false);
        });
      },
      resolve : function(template, values){

        var resolved = template;
        for(var p in values){

          resolved = resolved.replace(z + p + d, values[p]);
        };
        return resolved;
      },
      loadInstances : function(entityFullName, callback, retry){

        if(!entityFullName)throw Error(w + entityFullName);
        for(var i in this._entityList){

          if(this._entityList[i].fullName == entityFullName){

            var extentUri = this._entityList[i].extentUri;
            this.load(extentUri, function(instances){

              callback(instances);
            });
            return;
          };
        };
        if(retry === false)throw Error(y + entityFullName);
        var me = this;
        this.loadEntities(function(){

          me.loadInstances(entityFullName, callback, false);
        });
      },
      loadInstance : function(entity, objectId, callback){

        if(!entity || !entity.extentUri)throw Error(b);
        if(!objectId)throw Error(O);
        var instanceUri = entity.instanceUriTemplate || (entity.extentUri + objectId);
        this.load(this.resolve(instanceUri, {
          objectId : objectId
        }), callback);
      },
      saveInstance : function(entity, instance, callback){

        if(!instance || !instance.uri)throw Error(g);
        if(instance.objectId){

          this.put(instance.uri, instance, callback);
        } else {

          this.post(entity.extentUri, instance, callback);
        };
      },
      deleteInstance : function(instance, callback){

        if(!instance || !instance.uri)throw Error(g);
        this.remove(instance.uri, callback);
      },
      sendAction : function(entity, objectId, operation, arguments, callback){

        var me = this;
        var instanceActionUri = entity.instanceActionUriTemplate || (entity.extentUri + r);
        this.post(this.resolve(instanceActionUri, {
          objectId : objectId,
          actionName : operation.name
        }), arguments || {
        }, function(){

          me.loadInstance(entity, objectId, callback);
        });
      },
      sendStaticAction : function(entity, operation, arguments, callback){

        var me = this;
        var entityActionUri = entity.entityActionUriTemplate || (entity.uri + h);
        this.post(entityActionUri.replace(k, operation.name), arguments || {
        }, callback);
      },
      tryToLoad : function(uri, callback, slotName){

        if(uri === null || uri === undefined){

          callback([]);
          return;
        };
        return this.load(uri, callback, slotName);
      },
      load : function(uri, callback, slotName){

        var req = this.buildRequest(uri);
        this.sendRequest(req, callback, slotName);
      },
      remove : function(uri, callback){

        var req = this.buildRequest(uri, F);
        this.sendRequest(req, callback);
      },
      post : function(uri, data, callback){

        var req = this.buildRequest(uri, G);
        req.setRequestHeader(M, l);
        req.setRequestData(qx.util.Serializer.toJson(data, (function(){
        }), new qx.util.format.DateFormat(t)));
        this.sendRequest(req, callback);
      },
      put : function(uri, data, callback){

        var req = this.buildRequest(uri, n);
        req.setRequestHeader(M, l);
        req.setRequestData(qx.util.Serializer.toJson(data, (function(){
        }), new qx.util.format.DateFormat(t)));
        this.sendRequest(req, callback);
      },
      sendRequest : function(req, callback, slotName){

        var me = this;
        if(me._level == 0)me._busyPopup.toggleVisibility();
        me._level = me._level + 1;
        req.addListener(f, function(e){

          var req = e.getTarget();
          var response = undefined;
          if(req.getStatus() !== 204){

            response = req.getResponse();
            if(slotName)me[slotName] = response;
            if(typeof (response) === L)response = JSON.parse(response);
          };
          if(callback)callback(response);
        }, this);
        req.addListener(I, function(e){

          var req = e.getTarget();
          var message;
          if(req.getStatus() === 400){

            var response = req.getResponse();
            message = response.message;
          } else if(req.getStatus() == 401){

            message = q;
          } else if(req.getStatus() == 403){

            message = j;
          } else if(req.getStatus() == 404){

            message = E;
          } else {

            message = C + req.getStatus() + d;
          };;;
          var okButton = new qx.ui.mobile.form.Button(D);
          var messagePopup = new qx.ui.mobile.dialog.Popup(okButton);
          messagePopup.setTitle(message);
          messagePopup.show();
          okButton.addListener(B, function(){

            messagePopup.hide();
          });
        }, this);
        req.addListener(c, function(e){

          me._level = me._level - 1;
          if(me._level === 0)me._busyPopup.hide();
        }, this);
        req.send();
      },
      buildRequest : function(uri, method){

        var parsedUri = qx.util.Uri.parseUri(uri);
        if(!parsedUri.protocol){

          uri = this._parsedApplicationUri.protocol + u + this._parsedApplicationUri.host + (this._parsedApplicationUri.port ? (o + this._parsedApplicationUri.port) : x) + this._parsedApplicationUri.directory + uri;
        };
        return new qx.io.request.Xhr(uri, method || K);
      }
    }
  });
})();
(function(){

  var a = "file",b = "+",c = "strict",d = "anchor",e = "div",f = "query",g = "source",h = "password",j = "host",k = "protocol",l = "user",n = "directory",p = "loose",q = "relative",r = "queryKey",s = "qx.util.Uri",t = "",u = "path",v = "authority",w = '">0</a>',x = "&",y = "port",z = '<a href="',A = "userInfo",B = "?",C = "=";
  qx.Bootstrap.define(s, {
    statics : {
      parseUri : function(str, strict){

        var options = {
          key : [g, k, v, A, l, h, j, y, q, u, n, a, f, d],
          q : {
            name : r,
            parser : /(?:^|&)([^&=]*)=?([^&]*)/g
          },
          parser : {
            strict : /^(?:([^:\/?#]+):)?(?:\/\/((?:(([^:@]*)(?::([^:@]*))?)?@)?([^:\/?#]*)(?::(\d*))?))?((((?:[^?#\/]*\/)*)([^?#]*))(?:\?([^#]*))?(?:#(.*))?)/,
            loose : /^(?:(?![^:@]+:[^:@\/]*@)([^:\/?#.]+):)?(?:\/\/)?((?:(([^:@]*)(?::([^:@]*))?)?@)?([^:\/?#]*)(?::(\d*))?)(((\/(?:[^?#](?![^?#\/]*\.[^?#\/.]+(?:[?#]|$)))*\/?)?([^?#\/]*))(?:\?([^#]*))?(?:#(.*))?)/
          }
        };
        var o = options,m = options.parser[strict ? c : p].exec(str),uri = {
        },i = 14;
        while(i--){

          uri[o.key[i]] = m[i] || t;
        };
        uri[o.q.name] = {
        };
        uri[o.key[12]].replace(o.q.parser, function($0, $1, $2){

          if($1){

            uri[o.q.name][$1] = $2;
          };
        });
        return uri;
      },
      appendParamsToUrl : function(url, params){

        if(params === undefined){

          return url;
        };
        {
        };
        if(qx.lang.Type.isObject(params)){

          params = qx.util.Uri.toParameter(params);
        };
        if(!params){

          return url;
        };
        return url += /\?/.test(url) ? x + params : B + params;
      },
      toParameter : function(obj, post){

        var key,parts = [];
        for(key in obj){

          if(obj.hasOwnProperty(key)){

            var value = obj[key];
            if(value instanceof Array){

              for(var i = 0;i < value.length;i++){

                this.__eG(key, value[i], parts, post);
              };
            } else {

              this.__eG(key, value, parts, post);
            };
          };
        };
        return parts.join(x);
      },
      __eG : function(key, value, parts, post){

        var encode = window.encodeURIComponent;
        if(post){

          parts.push(encode(key).replace(/%20/g, b) + C + encode(value).replace(/%20/g, b));
        } else {

          parts.push(encode(key) + C + encode(value));
        };
      },
      getAbsolute : function(uri){

        var div = document.createElement(e);
        div.innerHTML = z + uri + w;
        return div.firstChild.href;
      }
    }
  });
})();
(function(){

  var a = "__eI",b = 'px',c = "bottom",d = "changeIcon",e = "changeLabel",f = "both",g = "Integer",h = "_applyIconPosition",i = 'left',j = "__eJ",k = "qx.ui.mobile.basic.Atom",l = 'icon',m = 'bottom',n = "String",o = 'top',p = 'both',q = 'margin',r = "icon",s = "__eH",t = "box-centered",u = "top",v = "right",w = 'label',x = "_applyIcon",y = "label",z = "_applyShow",A = 'right',B = "left",C = "_applyLabel",D = "_applyGap",E = "atom";
  qx.Class.define(k, {
    extend : qx.ui.mobile.core.Widget,
    construct : function(label, icon){

      qx.ui.mobile.core.Widget.call(this);
      this.__eM(label, icon);
      this.__eK(this.getIconPosition(), 4);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : E
      },
      label : {
        apply : C,
        nullable : true,
        check : n,
        event : e
      },
      icon : {
        check : n,
        apply : x,
        nullable : true,
        event : d
      },
      gap : {
        check : g,
        nullable : false,
        apply : D,
        init : 4
      },
      show : {
        init : f,
        check : [f, y, r],
        inheritable : true,
        apply : z
      },
      iconPosition : {
        init : B,
        check : [u, v, c, B],
        apply : h
      }
    },
    members : {
      __eH : null,
      __eI : null,
      __eJ : null,
      _applyIconPosition : function(value, old){

        var targetLayout;
        var verticalLayout = [u, c].indexOf(value) != -1;
        if(verticalLayout){

          targetLayout = new qx.ui.mobile.layout.VBox();
        } else {

          targetLayout = new qx.ui.mobile.layout.HBox();
        };
        var isReverse = [v, c].indexOf(value) != -1;
        targetLayout.setReversed(isReverse);
        this.__eJ.setLayout(targetLayout);
        this.__eK(old, null);
        this.__eK(value, this.getGap());
        this._domUpdated();
      },
      _applyShow : function(value, old){

        if(this.__eH){

          if(value === p || value === w){

            this.__eH.show();
          } else if(value === l){

            this.__eH.exclude();
          };
        };
        if(this.__eI){

          if(value === p || value === l){

            this.__eI.show();
          } else if(value === w){

            this.__eI.exclude();
          };
        };
      },
      _applyGap : function(value, old){

        this.__eK(this.getIconPosition(), value);
      },
      __eK : function(iconPosition, value){

        if(this.__eI){

          var newMarginPosition = this.__eL(iconPosition);
          var newPropKey = q + qx.lang.String.firstUp(newMarginPosition);
          if(value){

            this.__eI._setStyle(newPropKey, value + b);
          } else {

            this.__eI._setStyle(newPropKey, null);
          };
        };
      },
      __eL : function(position){

        var opposedPosition = i;
        switch(position){case o:
        opposedPosition = m;
        break;case m:
        opposedPosition = o;
        break;case i:
        opposedPosition = A;
        break;};
        return opposedPosition;
      },
      _applyLabel : function(value, old){

        if(this.__eH){

          this.__eH.setValue(value);
        } else {

          this.__eH = this._createLabelWidget(value);
        };
      },
      _applyIcon : function(value, old){

        if(this.__eI){

          this.__eI.setSource(value);
        } else {

          this.__eI = this._createIconWidget(value);
        };
      },
      getIconWidget : function(){

        return this.__eI;
      },
      getLabelWidget : function(){

        return this.__eH;
      },
      _createIconWidget : function(iconUrl){

        var iconWidget = new qx.ui.mobile.basic.Image(iconUrl);
        iconWidget.setAnonymous(true);
        return iconWidget;
      },
      _createLabelWidget : function(label){

        var labelWidget = new qx.ui.mobile.basic.Label(label);
        labelWidget.setAnonymous(true);
        labelWidget.setWrap(false);
        return labelWidget;
      },
      __eM : function(label, icon){

        this.__eH = this._createLabelWidget(label);
        if(label){

          this.setLabel(label);
        };
        this.__eI = this._createIconWidget(icon);
        if(icon){

          this.setIcon(icon);
        } else {

          this.__eI.exclude();
        };
        var layout;
        var verticalLayout = [u, c].indexOf(this.getIconPosition()) != -1;
        var hasNoLabel = !this.__eH;
        if(verticalLayout || hasNoLabel){

          layout = new qx.ui.mobile.layout.VBox();
        } else {

          layout = new qx.ui.mobile.layout.HBox();
        };
        this.__eJ = new qx.ui.mobile.container.Composite(layout);
        this.__eJ.addCssClass(t);
        this.__eJ.setAnonymous(true);
        if(this.__eI){

          this.__eJ.add(this.__eI, {
            flex : 0
          });
        };
        if(this.__eH){

          this.__eH.addCssClass(t);
          this.__eJ.add(this.__eH, {
            flex : 0
          });
        };
        if(this.getShow() === l && this.__eH){

          this.__eH.exclude();
        };
        if(this.getShow() === w && this.__eI){

          this.__eI.exclude();
        };
        this._add(this.__eJ);
      }
    },
    destruct : function(){

      this._disposeObjects(s, a, j);
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.layout.HBox",b = "hbox";
  qx.Class.define(a, {
    extend : qx.ui.mobile.layout.AbstractBox,
    members : {
      _getCssClasses : function(){

        return [b];
      }
    }
  });
})();
(function(){

  var a = "Image could not be loaded: ",b = "height",c = "url(",d = ")",e = 'data:',f = "true",g = "100%",h = "loaded",j = "3",l = "src",m = "backgroundSize",n = "backgroundImage",o = "backgroundRepeat",p = "qx.event.type.Event",q = '.',r = "loadingFailed",s = "1.5",t = "qx.ui.mobile.basic.Image",u = "String",v = "backgroundPosition",w = "width",x = "draggable",y = "_applySource",z = "img",A = "@",B = "rem",C = "changeAppScale",D = "false",E = "50% 50%",F = "2",G = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7",H = "no-repeat",I = "x";
  qx.Class.define(t, {
    extend : qx.ui.mobile.core.Widget,
    construct : function(source){

      qx.ui.mobile.core.Widget.call(this);
      if(qx.ui.mobile.basic.Image.ROOT === null){

        qx.ui.mobile.basic.Image.ROOT = qx.core.Init.getApplication().getRoot();
      };
      if(source){

        this.setSource(source);
      } else {

        this.initSource();
      };
      qx.ui.mobile.basic.Image.ROOT.addListener(C, this._onChangeAppScale, this);
    },
    events : {
      loadingFailed : p,
      loaded : p
    },
    statics : {
      PIXEL_RATIOS : null,
      ROOT : null
    },
    properties : {
      source : {
        check : u,
        nullable : true,
        init : null,
        apply : y
      }
    },
    members : {
      _getTagName : function(){

        return z;
      },
      _applySource : function(value, old){

        var source = value;
        if(source && source.indexOf(e) != 0){

          var resourceManager = qx.util.ResourceManager.getInstance();
          this._setStyle(w, resourceManager.getImageWidth(source) / 16 + B);
          this._setStyle(b, resourceManager.getImageHeight(source) / 16 + B);
          var foundHighResolutionSource = this._findHighResolutionSource(source);
          source = resourceManager.toUri(source);
          var ImageLoader = qx.io.ImageLoader;
          if(!ImageLoader.isFailed(source) && !ImageLoader.isLoaded(source)){

            ImageLoader.load(source, this.__eN, this);
          };
          if(foundHighResolutionSource == false){

            this._setSource(source);
          };
        } else {

          this._setSource(source);
        };
      },
      _onChangeAppScale : function(){

        this._applySource(this.getSource());
      },
      _findHighResolutionSource : function(source){

        var pixelRatioCandidates = qx.ui.mobile.basic.Image.PIXEL_RATIOS;
        var factor = qx.ui.mobile.basic.Image.ROOT.getAppScale();
        if(factor <= 1){

          return false;
        };
        var i = pixelRatioCandidates.length;
        while(i > 0 && factor > pixelRatioCandidates[--i]){
        };
        var imgSrc;
        for(var k = i;k >= 0;k--){

          imgSrc = this._getHighResolutionSource(source, pixelRatioCandidates[k]);
          if(imgSrc){

            this._createHighResolutionOverlay(imgSrc);
            return true;
          };
        };
        for(var k = i + 1;k < pixelRatioCandidates.length;k++){

          imgSrc = this._getHighResolutionSource(source, pixelRatioCandidates[k]);
          if(imgSrc){

            this._createHighResolutionOverlay(imgSrc);
            return true;
          };
        };
        return false;
      },
      _getHighResolutionSource : function(source, pixelRatio){

        var fileExtIndex = source.lastIndexOf(q);
        if(fileExtIndex > -1){

          var pixelRatioIdentifier = A + pixelRatio + I;
          var candidate = source.slice(0, fileExtIndex) + pixelRatioIdentifier + source.slice(fileExtIndex);
          if(qx.util.ResourceManager.getInstance().has(candidate)){

            return candidate;
          };
        };
        return null;
      },
      _createHighResolutionOverlay : function(highResSource){

        this._setSource(G);
        this._setStyle(n, c + qx.util.ResourceManager.getInstance().toUri(highResSource) + d);
        this._setStyle(m, g);
        this._setStyle(o, H);
        this._setStyle(v, E);
      },
      __eN : function(source, imageInfo){

        if(this.$$disposed === true){

          return;
        };
        if(imageInfo.failed){

          this.warn(a + source);
          this.fireEvent(r);
        } else if(imageInfo.aborted){

          return;
        } else {

          this.fireEvent(h);
        };
        this._domUpdated();
      },
      _setSource : function(source){

        this._setAttribute(l, source);
      },
      setDraggable : function(isDraggable){

        if(isDraggable){

          this._setAttribute(x, f);
        } else {

          this._setAttribute(x, D);
        };
      }
    },
    defer : function(statics){

      statics.PIXEL_RATIOS = [j, F, s];
    },
    destruct : function(){

      qx.ui.mobile.basic.Image.ROOT.removeListener(C, this._onChangeAppScale, this);
    }
  });
})();
(function(){

  var a = "singleton",b = "qx.util.LibraryManager";
  qx.Class.define(b, {
    extend : qx.core.Object,
    type : a,
    statics : {
      __eO : qx.$$libraries || {
      }
    },
    members : {
      has : function(namespace){

        return !!this.self(arguments).__eO[namespace];
      },
      get : function(namespace, key){

        return this.self(arguments).__eO[namespace][key] ? this.self(arguments).__eO[namespace][key] : null;
      },
      set : function(namespace, key, value){

        this.self(arguments).__eO[namespace][key] = value;
      }
    }
  });
})();
(function(){

  var a = "Microsoft.XMLHTTP",b = "xhr",c = "io.ssl",d = "io.xhr",e = "",f = "file:",g = "https:",h = "webkit",i = "gecko",j = "activex",k = "opera",l = ".",m = "io.maxrequests",n = "qx.bom.client.Transport";
  qx.Bootstrap.define(n, {
    statics : {
      getMaxConcurrentRequestCount : function(){

        var maxConcurrentRequestCount;
        var versionParts = qx.bom.client.Engine.getVersion().split(l);
        var versionMain = 0;
        var versionMajor = 0;
        var versionMinor = 0;
        if(versionParts[0]){

          versionMain = versionParts[0];
        };
        if(versionParts[1]){

          versionMajor = versionParts[1];
        };
        if(versionParts[2]){

          versionMinor = versionParts[2];
        };
        if(window.maxConnectionsPerServer){

          maxConcurrentRequestCount = window.maxConnectionsPerServer;
        } else if(qx.bom.client.Engine.getName() == k){

          maxConcurrentRequestCount = 8;
        } else if(qx.bom.client.Engine.getName() == h){

          maxConcurrentRequestCount = 4;
        } else if(qx.bom.client.Engine.getName() == i && ((versionMain > 1) || ((versionMain == 1) && (versionMajor > 9)) || ((versionMain == 1) && (versionMajor == 9) && (versionMinor >= 1)))){

          maxConcurrentRequestCount = 6;
        } else {

          maxConcurrentRequestCount = 2;
        };;;
        return maxConcurrentRequestCount;
      },
      getSsl : function(){

        return window.location.protocol === g;
      },
      getXmlHttpRequest : function(){

        var supports = window.ActiveXObject ? (function(){

          if(window.location.protocol !== f){

            try{

              new window.XMLHttpRequest();
              return b;
            } catch(noXhr) {
            };
          };
          try{

            new window.ActiveXObject(a);
            return j;
          } catch(noActiveX) {
          };
        })() : (function(){

          try{

            new window.XMLHttpRequest();
            return b;
          } catch(noXhr) {
          };
        })();
        return supports || e;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(m, statics.getMaxConcurrentRequestCount);
      qx.core.Environment.add(c, statics.getSsl);
      qx.core.Environment.add(d, statics.getXmlHttpRequest);
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "//",d = "io.ssl",e = "",f = "encoding",g = "?",h = "data",i = "string",j = "type",k = "data:image/",l = ";",m = "/",n = "resourceUri",o = "qx.util.ResourceManager",p = "singleton",q = ",";
  qx.Class.define(o, {
    extend : qx.core.Object,
    type : p,
    construct : function(){

      qx.core.Object.call(this);
    },
    statics : {
      __k : qx.$$resources || {
      },
      __eP : {
      }
    },
    members : {
      has : function(id){

        return !!this.self(arguments).__k[id];
      },
      getData : function(id){

        return this.self(arguments).__k[id] || null;
      },
      getImageWidth : function(id){

        var entry = this.self(arguments).__k[id];
        return entry ? entry[0] : null;
      },
      getImageHeight : function(id){

        var entry = this.self(arguments).__k[id];
        return entry ? entry[1] : null;
      },
      getImageFormat : function(id){

        var entry = this.self(arguments).__k[id];
        return entry ? entry[2] : null;
      },
      getCombinedFormat : function(id){

        var clippedtype = e;
        var entry = this.self(arguments).__k[id];
        var isclipped = entry && entry.length > 4 && typeof (entry[4]) == i && this.constructor.__k[entry[4]];
        if(isclipped){

          var combId = entry[4];
          var combImg = this.constructor.__k[combId];
          clippedtype = combImg[2];
        };
        return clippedtype;
      },
      toUri : function(id){

        if(id == null){

          return id;
        };
        var entry = this.self(arguments).__k[id];
        if(!entry){

          return id;
        };
        if(typeof entry === i){

          var lib = entry;
        } else {

          var lib = entry[3];
          if(!lib){

            return id;
          };
        };
        var urlPrefix = e;
        if((qx.core.Environment.get(b) == a) && qx.core.Environment.get(d)){

          urlPrefix = this.self(arguments).__eP[lib];
        };
        return urlPrefix + qx.util.LibraryManager.getInstance().get(lib, n) + m + id;
      },
      toDataUri : function(resid){

        var resentry = this.constructor.__k[resid];
        var combined = this.constructor.__k[resentry[4]];
        var uri;
        if(combined){

          var resstruct = combined[4][resid];
          uri = k + resstruct[j] + l + resstruct[f] + q + resstruct[h];
        } else {

          uri = this.toUri(resid);
        };
        return uri;
      }
    },
    defer : function(statics){

      if((qx.core.Environment.get(b) == a)){

        if(qx.core.Environment.get(d)){

          for(var lib in qx.$$libraries){

            var resourceUri;
            if(qx.util.LibraryManager.getInstance().get(lib, n)){

              resourceUri = qx.util.LibraryManager.getInstance().get(lib, n);
            } else {

              statics.__eP[lib] = e;
              continue;
            };
            if(resourceUri.match(/^\/\//) != null){

              statics.__eP[lib] = window.location.protocol;
            } else if(resourceUri.match(/^\//) != null){

              statics.__eP[lib] = window.location.protocol + c + window.location.host;
            } else if(resourceUri.match(/^\.\//) != null){

              var url = document.URL;
              statics.__eP[lib] = url.substring(0, url.lastIndexOf(m) + 1);
            } else if(resourceUri.match(/^http/) != null){

              statics.__eP[lib] = e;
            } else {

              var index = window.location.href.indexOf(g);
              var href;
              if(index == -1){

                href = window.location.href;
              } else {

                href = window.location.href.substring(0, index);
              };
              statics.__eP[lib] = href.substring(0, href.lastIndexOf(m) + 1);
            };;;
          };
        };
      };
    }
  });
})();
(function(){

  var a = "load",b = "html.image.naturaldimensions",c = "qx.io.ImageLoader";
  qx.Bootstrap.define(c, {
    statics : {
      __cM : {
      },
      __eQ : {
        width : null,
        height : null
      },
      __eR : /\.(png|gif|jpg|jpeg|bmp)\b/i,
      __eS : /^data:image\/(png|gif|jpg|jpeg|bmp)\b/i,
      isLoaded : function(source){

        var entry = this.__cM[source];
        return !!(entry && entry.loaded);
      },
      isFailed : function(source){

        var entry = this.__cM[source];
        return !!(entry && entry.failed);
      },
      isLoading : function(source){

        var entry = this.__cM[source];
        return !!(entry && entry.loading);
      },
      getFormat : function(source){

        var entry = this.__cM[source];
        if(!entry || !entry.format){

          var result = this.__eS.exec(source);
          if(result != null){

            var width = (entry && qx.lang.Type.isNumber(entry.width) ? entry.width : this.__eQ.width);
            var height = (entry && qx.lang.Type.isNumber(entry.height) ? entry.height : this.__eQ.height);
            entry = {
              loaded : true,
              format : result[1],
              width : width,
              height : height
            };
          };
        };
        return entry ? entry.format : null;
      },
      getSize : function(source){

        var entry = this.__cM[source];
        return entry ? {
          width : entry.width,
          height : entry.height
        } : this.__eQ;
      },
      getWidth : function(source){

        var entry = this.__cM[source];
        return entry ? entry.width : null;
      },
      getHeight : function(source){

        var entry = this.__cM[source];
        return entry ? entry.height : null;
      },
      load : function(source, callback, context){

        var entry = this.__cM[source];
        if(!entry){

          entry = this.__cM[source] = {
          };
        };
        if(callback && !context){

          context = window;
        };
        if(entry.loaded || entry.loading || entry.failed){

          if(callback){

            if(entry.loading){

              entry.callbacks.push(callback, context);
            } else {

              callback.call(context, source, entry);
            };
          };
        } else {

          entry.loading = true;
          entry.callbacks = [];
          if(callback){

            entry.callbacks.push(callback, context);
          };
          var el = new Image();
          var boundCallback = qx.lang.Function.listener(this.__eT, this, el, source);
          el.onload = boundCallback;
          el.onerror = boundCallback;
          el.src = source;
          entry.element = el;
        };
      },
      abort : function(source){

        var entry = this.__cM[source];
        if(entry && !entry.loaded){

          entry.aborted = true;
          var callbacks = entry.callbacks;
          var element = entry.element;
          element.onload = element.onerror = null;
          delete entry.callbacks;
          delete entry.element;
          delete entry.loading;
          for(var i = 0,l = callbacks.length;i < l;i += 2){

            callbacks[i].call(callbacks[i + 1], source, entry);
          };
        };
        this.__cM[source] = null;
      },
      __eT : qx.event.GlobalError.observeMethod(function(event, element, source){

        var entry = this.__cM[source];
        var isImageAvailable = function(imgElem){

          return (imgElem && imgElem.height !== 0);
        };
        if(event.type === a && isImageAvailable(element)){

          entry.loaded = true;
          entry.width = this.__eU(element);
          entry.height = this.__eV(element);
          var result = this.__eR.exec(source);
          if(result != null){

            entry.format = result[1];
          };
        } else {

          entry.failed = true;
        };
        element.onload = element.onerror = null;
        var callbacks = entry.callbacks;
        delete entry.loading;
        delete entry.callbacks;
        delete entry.element;
        for(var i = 0,l = callbacks.length;i < l;i += 2){

          callbacks[i].call(callbacks[i + 1], source, entry);
        };
      }),
      __eU : function(element){

        return qx.core.Environment.get(b) ? element.naturalWidth : element.width;
      },
      __eV : function(element){

        return qx.core.Environment.get(b) ? element.naturalHeight : element.height;
      },
      dispose : function(){

        this.__cM = {
        };
      }
    }
  });
})();
(function(){

  var a = "Boolean",b = "no-wrap",c = "changeLocale",d = "label",f = "_applyValue",g = "qx.ui.mobile.basic.Label",h = "_applyWrap",i = "qx.dynlocale",j = "changeValue",k = "String";
  qx.Class.define(g, {
    extend : qx.ui.mobile.core.Widget,
    construct : function(value){

      qx.ui.mobile.core.Widget.call(this);
      if(value){

        this.setValue(value);
      };
      this.initWrap();
      if(qx.core.Environment.get(i)){

        qx.locale.Manager.getInstance().addListener(c, this._onChangeLocale, this);
      };
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : d
      },
      value : {
        nullable : true,
        init : null,
        check : k,
        apply : f,
        event : j
      },
      anonymous : {
        refine : true,
        init : true
      },
      wrap : {
        check : a,
        init : true,
        apply : h
      }
    },
    members : {
      _applyValue : function(value, old){

        this._setHtml(value);
      },
      _applyWrap : function(value, old){

        if(value){

          this.removeCssClass(b);
        } else {

          this.addCssClass(b);
        };
      },
      _onChangeLocale : qx.core.Environment.select(i, {
        "true" : function(e){

          var content = this.getValue();
          if(content && content.translate){

            this.setValue(content.translate());
          };
        },
        "false" : null
      })
    },
    destruct : function(){

      if(qx.core.Environment.get(i)){

        qx.locale.Manager.getInstance().removeListener(c, this._onChangeLocale, this);
      };
    }
  });
})();
(function(){

  var a = 'indexOf',b = 'slice',c = 'concat',d = 'toLocaleLowerCase',e = "qx.type.BaseString",f = "",g = 'trim',h = 'match',j = 'toLocaleUpperCase',k = 'search',m = 'replace',n = 'toLowerCase',o = 'charCodeAt',p = 'split',q = 'substring',r = 'lastIndexOf',s = 'substr',t = 'toUpperCase',u = 'charAt';
  qx.Class.define(e, {
    extend : Object,
    construct : function(txt){

      var txt = txt || f;
      this.__eW = txt;
      this.length = txt.length;
    },
    members : {
      $$isString : true,
      length : 0,
      __eW : null,
      toString : function(){

        return this.__eW;
      },
      charAt : null,
      valueOf : null,
      charCodeAt : null,
      concat : null,
      indexOf : null,
      lastIndexOf : null,
      match : null,
      replace : null,
      search : null,
      slice : null,
      split : null,
      substr : null,
      substring : null,
      toLowerCase : null,
      toUpperCase : null,
      toHashCode : function(){

        return qx.core.ObjectRegistry.toHashCode(this);
      },
      toLocaleLowerCase : null,
      toLocaleUpperCase : null,
      base : function(args, varags){

        return qx.core.Object.prototype.base.apply(this, arguments);
      }
    },
    defer : function(statics, members){

      {
      };
      var mappedFunctions = [u, o, c, a, r, h, m, k, b, p, s, q, n, t, d, j, g];
      members.valueOf = members.toString;
      if(new statics(f).valueOf() == null){

        delete members.valueOf;
      };
      for(var i = 0,l = mappedFunctions.length;i < l;i++){

        members[mappedFunctions[i]] = String.prototype[mappedFunctions[i]];
      };
    }
  });
})();
(function(){

  var a = "qx.locale.LocalizedString";
  qx.Class.define(a, {
    extend : qx.type.BaseString,
    construct : function(translation, messageId, args){

      qx.type.BaseString.call(this, translation);
      this.__eX = messageId;
      this.__eY = args;
    },
    members : {
      __eX : null,
      __eY : null,
      translate : function(){

        return qx.locale.Manager.getInstance().translate(this.__eX, this.__eY);
      }
    }
  });
})();
(function(){

  var a = "locale",b = "_applyLocale",c = "",d = "changeLocale",e = "_",f = "C",g = "locale.variant",h = "qx.dynlocale",j = "qx.locale.Manager",k = "String",l = "singleton";
  qx.Class.define(j, {
    type : l,
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__fa = qx.$$translations || {
      };
      this.__fb = qx.$$locales || {
      };
      var locale = qx.core.Environment.get(a);
      var variant = qx.core.Environment.get(g);
      if(variant !== c){

        locale += e + variant;
      };
      this.__fc = locale;
      this.setLocale(locale || this.__fd);
    },
    statics : {
      tr : function(messageId, varargs){

        var args = qx.lang.Array.fromArguments(arguments);
        args.splice(0, 1);
        return qx.locale.Manager.getInstance().translate(messageId, args);
      },
      trn : function(singularMessageId, pluralMessageId, count, varargs){

        var args = qx.lang.Array.fromArguments(arguments);
        args.splice(0, 3);
        if(count != 1){

          return qx.locale.Manager.getInstance().translate(pluralMessageId, args);
        } else {

          return qx.locale.Manager.getInstance().translate(singularMessageId, args);
        };
      },
      trc : function(hint, messageId, varargs){

        var args = qx.lang.Array.fromArguments(arguments);
        args.splice(0, 2);
        return qx.locale.Manager.getInstance().translate(messageId, args);
      },
      marktr : function(messageId){

        return messageId;
      }
    },
    properties : {
      locale : {
        check : k,
        nullable : true,
        apply : b,
        event : d
      }
    },
    members : {
      __fd : f,
      __fe : null,
      __ff : null,
      __fa : null,
      __fb : null,
      __fc : null,
      getLanguage : function(){

        return this.__ff;
      },
      getTerritory : function(){

        return this.getLocale().split(e)[1] || c;
      },
      getAvailableLocales : function(includeNonloaded){

        var locales = [];
        for(var locale in this.__fb){

          if(locale != this.__fd){

            if(this.__fb[locale] === null && !includeNonloaded){

              continue;
            };
            locales.push(locale);
          };
        };
        return locales;
      },
      __fg : function(locale){

        var language;
        if(locale == null){

          return null;
        };
        var pos = locale.indexOf(e);
        if(pos == -1){

          language = locale;
        } else {

          language = locale.substring(0, pos);
        };
        return language;
      },
      _applyLocale : function(value, old){

        {
        };
        this.__fe = value;
        this.__ff = this.__fg(value);
      },
      addTranslation : function(languageCode, translationMap){

        var catalog = this.__fa;
        if(catalog[languageCode]){

          for(var key in translationMap){

            catalog[languageCode][key] = translationMap[key];
          };
        } else {

          catalog[languageCode] = translationMap;
        };
      },
      addLocale : function(localeCode, localeMap){

        var catalog = this.__fb;
        if(catalog[localeCode]){

          for(var key in localeMap){

            catalog[localeCode][key] = localeMap[key];
          };
        } else {

          catalog[localeCode] = localeMap;
        };
      },
      translate : function(messageId, args, locale){

        var catalog = this.__fa;
        return this.__fh(catalog, messageId, args, locale);
      },
      localize : function(messageId, args, locale){

        var catalog = this.__fb;
        return this.__fh(catalog, messageId, args, locale);
      },
      __fh : function(catalog, messageId, args, locale){

        {
        };
        var txt;
        if(!catalog){

          return messageId;
        };
        if(locale){

          var language = this.__fg(locale);
        } else {

          locale = this.__fe;
          language = this.__ff;
        };
        if(!txt && catalog[locale]){

          txt = catalog[locale][messageId];
        };
        if(!txt && catalog[language]){

          txt = catalog[language][messageId];
        };
        if(!txt && catalog[this.__fd]){

          txt = catalog[this.__fd][messageId];
        };
        if(!txt){

          txt = messageId;
        };
        if(args.length > 0){

          var translatedArgs = [];
          for(var i = 0;i < args.length;i++){

            var arg = args[i];
            if(arg && arg.translate){

              translatedArgs[i] = arg.translate();
            } else {

              translatedArgs[i] = arg;
            };
          };
          txt = qx.lang.String.format(txt, translatedArgs);
        };
        if(qx.core.Environment.get(h)){

          txt = new qx.locale.LocalizedString(txt, messageId, args);
        };
        return txt;
      }
    },
    destruct : function(){

      this.__fa = this.__fb = null;
    }
  });
})();
(function(){

  var a = "qx.bom.client.Locale",b = "-",c = "locale",d = "",e = "android",f = "locale.variant";
  qx.Bootstrap.define(a, {
    statics : {
      getLocale : function(){

        var locale = qx.bom.client.Locale.__fi();
        var index = locale.indexOf(b);
        if(index != -1){

          locale = locale.substr(0, index);
        };
        return locale;
      },
      getVariant : function(){

        var locale = qx.bom.client.Locale.__fi();
        var variant = d;
        var index = locale.indexOf(b);
        if(index != -1){

          variant = locale.substr(index + 1);
        };
        return variant;
      },
      __fi : function(){

        var locale = (navigator.userLanguage || navigator.language || d);
        if(qx.bom.client.OperatingSystem.getName() == e){

          var match = /(\w{2})-(\w{2})/i.exec(navigator.userAgent);
          if(match){

            locale = match[0];
          };
        };
        return locale.toLowerCase();
      }
    },
    defer : function(statics){

      qx.core.Environment.add(c, statics.getLocale);
      qx.core.Environment.add(f, statics.getVariant);
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.dialog.BusyIndicator",b = "spinner",c = "359deg",d = "_applySpinnerClass",e = "spinnerContainer",f = "infinite",g = "disappear",h = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABQAAAAUCAQAAAAngNWGAAAAAXNSR0IArs4c6QAAAAJiS0dEAP+Hj8y/AAAACXBIWXMAAA7DAAAOwwHHb6hkAAAAB3RJTUUH2wsJDS8ybObCaQAAABBJREFUKM9jYBgFo2AUkAIAAzQAATnIy0MAAAAASUVORK5CYII=',i = "0deg",j = "appear",k = "linear",l = "String",m = "center center";
  qx.Class.define(a, {
    extend : qx.ui.mobile.basic.Atom,
    construct : function(label){

      qx.ui.mobile.basic.Atom.call(this, label, h);
      this.addListener(j, this._onAppear, this);
      this.addListener(g, this._onDisappear, this);
      this.__fj = {
        duration : 1000,
        timing : k,
        origin : m,
        repeat : f,
        keyFrames : {
          '0' : {
            rotate : i
          },
          '100' : {
            rotate : c
          }
        }
      };
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : e
      },
      spinnerClass : {
        apply : d,
        nullable : false,
        check : l,
        init : b
      }
    },
    members : {
      __fj : null,
      __fk : null,
      _onAppear : function(){

        this.__fk = qx.bom.element.Animation.animate(this.getIconWidget().getContainerElement(), this.__fj);
      },
      _onDisappear : function(){

        this.__fk.stop();
      },
      _createIconWidget : function(iconUrl){

        var iconWidget = qx.ui.mobile.basic.Atom.prototype._createIconWidget.call(this, iconUrl);
        iconWidget.addCssClass(this.getSpinnerClass());
        return iconWidget;
      },
      _applySpinnerClass : function(value, old){

        if(old){

          this.getIconWidget().removeCssClass(old);
        };
        if(value){

          this.getIconWidget().addCssClass(value);
        };
      }
    },
    destruct : function(){

      this.removeListener(j, this._onAppear, this);
      this.removeListener(g, this._onDisappear, this);
      if(this.__fk){

        this.__fk.stop();
      };
      this.__fj = this.__fk = null;
    }
  });
})();
(function(){

  var a = "css.animation",b = "translate",c = "rotate",d = "skew",e = "scale",f = "qx.bom.element.Animation";
  qx.Bootstrap.define(f, {
    statics : {
      animate : function(el, desc, duration){

        var onlyCssKeys = qx.bom.element.Animation.__fl(el, desc.keyFrames);
        if(qx.core.Environment.get(a) && onlyCssKeys){

          return qx.bom.element.AnimationCss.animate(el, desc, duration);
        } else {

          return qx.bom.element.AnimationJs.animate(el, desc, duration);
        };
      },
      animateReverse : function(el, desc, duration){

        var onlyCssKeys = qx.bom.element.Animation.__fl(el, desc.keyFrames);
        if(qx.core.Environment.get(a) && onlyCssKeys){

          return qx.bom.element.AnimationCss.animateReverse(el, desc, duration);
        } else {

          return qx.bom.element.AnimationJs.animateReverse(el, desc, duration);
        };
      },
      __fl : function(el, keyFrames){

        var keys = [];
        for(var nr in keyFrames){

          var frame = keyFrames[nr];
          for(var key in frame){

            if(keys.indexOf(key) == -1){

              keys.push(key);
            };
          };
        };
        var transformKeys = [e, c, d, b];
        for(var i = 0;i < keys.length;i++){

          var key = qx.lang.String.camelCase(keys[i]);
          if(!(key in el.style)){

            if(transformKeys.indexOf(keys[i]) != -1){

              continue;
            };
            if(qx.bom.Style.getPropertyName(key)){

              continue;
            };
            return false;
          };
        };
        return true;
      }
    }
  });
})();
(function(){

  var a = "stylesheet",b = "head",c = "html.stylesheet.addimport",d = "html.stylesheet.insertrule",e = "}",f = "html.stylesheet.createstylesheet",g = '@import "',h = "text/css",j = "{",k = '";',l = "html.stylesheet.removeimport",m = "html.stylesheet.deleterule",n = "qx.bom.Stylesheet",o = "link",p = "style";
  qx.Bootstrap.define(n, {
    statics : {
      includeFile : function(href, doc){

        if(!doc){

          doc = document;
        };
        var el = doc.createElement(o);
        el.type = h;
        el.rel = a;
        el.href = href;
        var head = doc.getElementsByTagName(b)[0];
        head.appendChild(el);
      },
      createElement : function(text){

        if(qx.core.Environment.get(f)){

          var sheet = document.createStyleSheet();
          if(text){

            sheet.cssText = text;
          };
          return sheet;
        } else {

          var elem = document.createElement(p);
          elem.type = h;
          if(text){

            elem.appendChild(document.createTextNode(text));
          };
          document.getElementsByTagName(b)[0].appendChild(elem);
          return elem.sheet;
        };
      },
      addRule : function(sheet, selector, entry){

        {

          var msg;
        };
        if(qx.core.Environment.get(d)){

          sheet.insertRule(selector + j + entry + e, sheet.cssRules.length);
        } else {

          sheet.addRule(selector, entry);
        };
      },
      removeRule : function(sheet, selector){

        if(qx.core.Environment.get(m)){

          var rules = sheet.cssRules;
          var len = rules.length;
          for(var i = len - 1;i >= 0;--i){

            if(rules[i].selectorText == selector){

              sheet.deleteRule(i);
            };
          };
        } else {

          var rules = sheet.rules;
          var len = rules.length;
          for(var i = len - 1;i >= 0;--i){

            if(rules[i].selectorText == selector){

              sheet.removeRule(i);
            };
          };
        };
      },
      removeSheet : function(sheet){

        var owner = sheet.ownerNode ? sheet.ownerNode : sheet.owningElement;
        qx.dom.Element.removeChild(owner, owner.parentNode);
      },
      removeAllRules : function(sheet){

        if(qx.core.Environment.get(m)){

          var rules = sheet.cssRules;
          var len = rules.length;
          for(var i = len - 1;i >= 0;i--){

            sheet.deleteRule(i);
          };
        } else {

          var rules = sheet.rules;
          var len = rules.length;
          for(var i = len - 1;i >= 0;i--){

            sheet.removeRule(i);
          };
        };
      },
      addImport : function(sheet, url){

        if(qx.core.Environment.get(c)){

          sheet.addImport(url);
        } else {

          sheet.insertRule(g + url + k, sheet.cssRules.length);
        };
      },
      removeImport : function(sheet, url){

        if(qx.core.Environment.get(l)){

          var imports = sheet.imports;
          var len = imports.length;
          for(var i = len - 1;i >= 0;i--){

            if(imports[i].href == url || imports[i].href == qx.util.Uri.getAbsolute(url)){

              sheet.removeImport(i);
            };
          };
        } else {

          var rules = sheet.cssRules;
          var len = rules.length;
          for(var i = len - 1;i >= 0;i--){

            if(rules[i].href == url){

              sheet.deleteRule(i);
            };
          };
        };
      },
      removeAllImports : function(sheet){

        if(qx.core.Environment.get(l)){

          var imports = sheet.imports;
          var len = imports.length;
          for(var i = len - 1;i >= 0;i--){

            sheet.removeImport(i);
          };
        } else {

          var rules = sheet.cssRules;
          var len = rules.length;
          for(var i = len - 1;i >= 0;i--){

            if(rules[i].type == rules[i].IMPORT_RULE){

              sheet.deleteRule(i);
            };
          };
        };
      }
    }
  });
})();
(function(){

  var a = "qx.bom.client.Stylesheet",b = "html.stylesheet.deleterule",c = "html.stylesheet.insertrule",d = "function",e = "html.stylesheet.createstylesheet",f = "html.stylesheet.addimport",g = "html.stylesheet.removeimport",h = "object";
  qx.Bootstrap.define(a, {
    statics : {
      __fm : function(){

        if(!qx.bom.client.Stylesheet.__fn){

          qx.bom.client.Stylesheet.__fn = qx.bom.Stylesheet.createElement();
        };
        return qx.bom.client.Stylesheet.__fn;
      },
      getCreateStyleSheet : function(){

        return typeof document.createStyleSheet === h;
      },
      getInsertRule : function(){

        return typeof qx.bom.client.Stylesheet.__fm().insertRule === d;
      },
      getDeleteRule : function(){

        return typeof qx.bom.client.Stylesheet.__fm().deleteRule === d;
      },
      getAddImport : function(){

        return (typeof qx.bom.client.Stylesheet.__fm().addImport === h);
      },
      getRemoveImport : function(){

        return (typeof qx.bom.client.Stylesheet.__fm().removeImport === h);
      }
    },
    defer : function(statics){

      qx.core.Environment.add(e, statics.getCreateStyleSheet);
      qx.core.Environment.add(c, statics.getInsertRule);
      qx.core.Environment.add(b, statics.getDeleteRule);
      qx.core.Environment.add(f, statics.getAddImport);
      qx.core.Environment.add(g, statics.getRemoveImport);
    }
  });
})();
(function(){

  var a = "oAnimationStart",b = "animationend",c = "MSAnimationStart",d = "oRequestAnimationFrame",f = "AnimationFillMode",g = "MSAnimationEnd",h = "requestAnimationFrame",j = "mozRequestAnimationFrame",k = "webkitAnimationEnd",l = "css.animation.requestframe",m = "AnimationPlayState",n = "",o = "MSAnimationIteration",p = "animation",q = "oAnimationEnd",r = "@",s = "animationiteration",t = "webkitRequestAnimationFrame",u = " name",v = "qx.bom.client.CssAnimation",w = "css.animation",x = "oAnimationIteration",y = "webkitAnimationIteration",z = "-keyframes",A = "msRequestAnimationFrame",B = "@keyframes",C = "animationstart",D = "webkitAnimationStart";
  qx.Bootstrap.define(v, {
    statics : {
      getSupport : function(){

        var name = qx.bom.client.CssAnimation.getName();
        if(name != null){

          return {
            "name" : name,
            "play-state" : qx.bom.client.CssAnimation.getPlayState(),
            "start-event" : qx.bom.client.CssAnimation.getAnimationStart(),
            "iteration-event" : qx.bom.client.CssAnimation.getAnimationIteration(),
            "end-event" : qx.bom.client.CssAnimation.getAnimationEnd(),
            "fill-mode" : qx.bom.client.CssAnimation.getFillMode(),
            "keyframes" : qx.bom.client.CssAnimation.getKeyFrames()
          };
        };
        return null;
      },
      getFillMode : function(){

        return qx.bom.Style.getPropertyName(f);
      },
      getPlayState : function(){

        return qx.bom.Style.getPropertyName(m);
      },
      getName : function(){

        return qx.bom.Style.getPropertyName(p);
      },
      getAnimationStart : function(){

        var mapping = {
          "msAnimation" : c,
          "WebkitAnimation" : D,
          "MozAnimation" : C,
          "OAnimation" : a,
          "animation" : C
        };
        return mapping[this.getName()];
      },
      getAnimationIteration : function(){

        var mapping = {
          "msAnimation" : o,
          "WebkitAnimation" : y,
          "MozAnimation" : s,
          "OAnimation" : x,
          "animation" : s
        };
        return mapping[this.getName()];
      },
      getAnimationEnd : function(){

        var mapping = {
          "msAnimation" : g,
          "WebkitAnimation" : k,
          "MozAnimation" : b,
          "OAnimation" : q,
          "animation" : b
        };
        return mapping[this.getName()];
      },
      getKeyFrames : function(){

        var prefixes = qx.bom.Style.VENDOR_PREFIXES;
        var keyFrames = [];
        for(var i = 0;i < prefixes.length;i++){

          var key = r + qx.bom.Style.getCssName(prefixes[i]) + z;
          keyFrames.push(key);
        };
        keyFrames.unshift(B);
        var sheet = qx.bom.Stylesheet.createElement();
        for(var i = 0;i < keyFrames.length;i++){

          try{

            qx.bom.Stylesheet.addRule(sheet, keyFrames[i] + u, n);
            return keyFrames[i];
          } catch(e) {
          };
        };
        return null;
      },
      getRequestAnimationFrame : function(){

        var choices = [h, A, t, j, d];
        for(var i = 0;i < choices.length;i++){

          if(window[choices[i]] != undefined){

            return choices[i];
          };
        };
        return null;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(w, statics.getSupport);
      qx.core.Environment.add(l, statics.getRequestAnimationFrame);
    }
  });
})();
(function(){

  var a = "fill-mode",b = "repeat",c = "timing",d = "start",f = "end",g = "Anni",h = "alternate",i = "keep",j = ":",k = "} ",l = "name",m = "iteration-event",n = "",o = "origin",p = "forwards",q = "start-event",r = "iteration",s = "end-event",t = "css.animation",u = "ms ",v = "% {",w = " ",x = "linear",y = ";",z = "qx.bom.element.AnimationCss",A = "keyframes";
  qx.Bootstrap.define(z, {
    statics : {
      __fo : null,
      __fp : g,
      __ci : 0,
      __fq : {
      },
      __fr : {
        "scale" : true,
        "rotate" : true,
        "skew" : true,
        "translate" : true
      },
      __fs : qx.core.Environment.get(t),
      animateReverse : function(el, desc, duration){

        return this._animate(el, desc, duration, true);
      },
      animate : function(el, desc, duration){

        return this._animate(el, desc, duration, false);
      },
      _animate : function(el, desc, duration, reverse){

        this.__fx(desc);
        {
        };
        var keep = desc.keep;
        if(keep != null && (reverse || (desc.alternate && desc.repeat % 2 == 0))){

          keep = 100 - keep;
        };
        if(!this.__fo){

          this.__fo = qx.bom.Stylesheet.createElement();
        };
        var keyFrames = desc.keyFrames;
        if(duration == undefined){

          duration = desc.duration;
        };
        if(this.__fs != null){

          var name = this.__fz(keyFrames, reverse);
          var style = name + w + duration + u + desc.repeat + w + desc.timing + w + (desc.delay ? desc.delay + u : n) + (desc.alternate ? h : n);
          qx.bom.Event.addNativeListener(el, this.__fs[q], this.__ft);
          qx.bom.Event.addNativeListener(el, this.__fs[m], this.__fu);
          qx.bom.Event.addNativeListener(el, this.__fs[s], this.__fv);
          {
          };
          el.style[qx.lang.String.camelCase(this.__fs[l])] = style;
          if(keep && keep == 100 && this.__fs[a]){

            el.style[this.__fs[a]] = p;
          };
        };
        var animation = new qx.bom.element.AnimationHandle();
        animation.desc = desc;
        animation.el = el;
        animation.keep = keep;
        el.$$animation = animation;
        if(desc.origin != null){

          qx.bom.element.Transform.setOrigin(el, desc.origin);
        };
        if(this.__fs == null){

          window.setTimeout(function(){

            qx.bom.element.AnimationCss.__fv({
              target : el
            });
          }, 0);
        };
        return animation;
      },
      __ft : function(e){

        e.target.$$animation.emit(d, e.target);
      },
      __fu : function(e){

        if(e.target != null && e.target.$$animation != null){

          e.target.$$animation.emit(r, e.target);
        };
      },
      __fv : function(e){

        var el = e.target;
        var animation = el.$$animation;
        if(!animation){

          return;
        };
        var desc = animation.desc;
        if(qx.bom.element.AnimationCss.__fs != null){

          var key = qx.lang.String.camelCase(qx.bom.element.AnimationCss.__fs[l]);
          el.style[key] = n;
          qx.bom.Event.removeNativeListener(el, qx.bom.element.AnimationCss.__fs[l], qx.bom.element.AnimationCss.__fv);
        };
        if(desc.origin != null){

          qx.bom.element.Transform.setOrigin(el, n);
        };
        qx.bom.element.AnimationCss.__fw(el, desc.keyFrames[animation.keep]);
        el.$$animation = null;
        animation.el = null;
        animation.ended = true;
        animation.emit(f, el);
      },
      __fw : function(el, endFrame){

        var transforms;
        for(var style in endFrame){

          if(style in qx.bom.element.AnimationCss.__fr){

            if(!transforms){

              transforms = {
              };
            };
            transforms[style] = endFrame[style];
          } else {

            el.style[qx.lang.String.camelCase(style)] = endFrame[style];
          };
        };
        if(transforms){

          qx.bom.element.Transform.transform(el, transforms);
        };
      },
      __fx : function(desc){

        if(!desc.hasOwnProperty(h)){

          desc.alternate = false;
        };
        if(!desc.hasOwnProperty(i)){

          desc.keep = null;
        };
        if(!desc.hasOwnProperty(b)){

          desc.repeat = 1;
        };
        if(!desc.hasOwnProperty(c)){

          desc.timing = x;
        };
        if(!desc.hasOwnProperty(o)){

          desc.origin = null;
        };
      },
      __fy : null,
      __fz : function(frames, reverse){

        var rule = n;
        for(var position in frames){

          rule += (reverse ? -(position - 100) : position) + v;
          var frame = frames[position];
          var transforms;
          for(var style in frame){

            if(style in this.__fr){

              if(!transforms){

                transforms = {
                };
              };
              transforms[style] = frame[style];
            } else {

              var propName = qx.bom.Style.getPropertyName(style);
              var prefixed = (propName !== null) ? qx.bom.Style.getCssName(propName) : n;
              rule += (prefixed || style) + j + frame[style] + y;
            };
          };
          if(transforms){

            rule += qx.bom.element.Transform.getCss(transforms);
          };
          rule += k;
        };
        if(this.__fq[rule]){

          return this.__fq[rule];
        };
        var name = this.__fp + this.__ci++;
        var selector = this.__fs[A] + w + name;
        qx.bom.Stylesheet.addRule(this.__fo, selector, rule);
        this.__fq[rule] = name;
        return name;
      }
    }
  });
})();
(function(){

  var a = "qx.event.Emitter",b = "*";
  qx.Bootstrap.define(a, {
    extend : Object,
    statics : {
      __fA : []
    },
    members : {
      __fB : null,
      __fC : null,
      on : function(name, listener, ctx){

        var id = qx.event.Emitter.__fA.length;
        this.__fD(name).push({
          listener : listener,
          ctx : ctx,
          id : id
        });
        qx.event.Emitter.__fA.push({
          name : name,
          listener : listener,
          ctx : ctx
        });
        return id;
      },
      once : function(name, listener, ctx){

        var id = qx.event.Emitter.__fA.length;
        this.__fD(name).push({
          listener : listener,
          ctx : ctx,
          once : true,
          id : id
        });
        qx.event.Emitter.__fA.push({
          name : name,
          listener : listener,
          ctx : ctx
        });
        return id;
      },
      off : function(name, listener, ctx){

        var storage = this.__fD(name);
        for(var i = storage.length - 1;i >= 0;i--){

          var entry = storage[i];
          if(entry.listener == listener && entry.ctx == ctx){

            storage.splice(i, 1);
            qx.event.Emitter.__fA[entry.id] = null;
            return entry.id;
          };
        };
        return null;
      },
      offById : function(id){

        var entry = qx.event.Emitter.__fA[id];
        if(entry){

          this.off(entry.name, entry.listener, entry.ctx);
        };
        return null;
      },
      addListener : function(name, listener, ctx){

        return this.on(name, listener, ctx);
      },
      addListenerOnce : function(name, listener, ctx){

        return this.once(name, listener, ctx);
      },
      removeListener : function(name, listener, ctx){

        this.off(name, listener, ctx);
      },
      removeListenerById : function(id){

        this.offById(id);
      },
      emit : function(name, data){

        var storage = this.__fD(name);
        for(var i = 0;i < storage.length;i++){

          var entry = storage[i];
          entry.listener.call(entry.ctx, data);
          if(entry.once){

            storage.splice(i, 1);
            i--;
          };
        };
        storage = this.__fD(b);
        for(var i = storage.length - 1;i >= 0;i--){

          var entry = storage[i];
          entry.listener.call(entry.ctx, data);
        };
      },
      getListeners : function(){

        return this.__fB;
      },
      __fD : function(name){

        if(this.__fB == null){

          this.__fB = {
          };
        };
        if(this.__fB[name] == null){

          this.__fB[name] = [];
        };
        return this.__fB[name];
      }
    }
  });
})();
(function(){

  var a = "css.animation",b = "Element",c = "",d = "qx.bom.element.AnimationHandle",e = "play-state",f = "paused",g = "running";
  qx.Bootstrap.define(d, {
    extend : qx.event.Emitter,
    construct : function(){

      var css = qx.core.Environment.get(a);
      this.__fE = css && css[e];
      this.__fF = true;
    },
    events : {
      "start" : b,
      "end" : b,
      "iteration" : b
    },
    members : {
      __fE : null,
      __fF : false,
      __fG : false,
      isPlaying : function(){

        return this.__fF;
      },
      isEnded : function(){

        return this.__fG;
      },
      isPaused : function(){

        return this.el.style[this.__fE] == f;
      },
      pause : function(){

        if(this.el){

          this.el.style[this.__fE] = f;
          this.el.$$animation.__fF = false;
          if(this.animationId && qx.bom.element.AnimationJs){

            qx.bom.element.AnimationJs.pause(this);
          };
        };
      },
      play : function(){

        if(this.el){

          this.el.style[this.__fE] = g;
          this.el.$$animation.__fF = true;
          if(this.i != undefined && qx.bom.element.AnimationJs){

            qx.bom.element.AnimationJs.play(this);
          };
        };
      },
      stop : function(){

        if(this.el && qx.core.Environment.get(a) && !this.jsAnimation){

          this.el.style[this.__fE] = c;
          this.el.style[qx.core.Environment.get(a).name] = c;
          this.el.$$animation.__fF = false;
          this.el.$$animation.__fG = true;
        } else if(this.jsAnimation){

          this.stopped = true;
          qx.bom.element.AnimationJs.stop(this);
        };
      }
    }
  });
})();
(function(){

  var c = "cm",d = "mm",e = "0",f = "pt",g = "pc",h = "",k = "%",l = "em",m = "qx.bom.element.AnimationJs",n = "infinite",o = "#",p = "in",q = "px",r = "start",s = "end",t = "ex",u = ";",v = "undefined",w = "iteration",y = "string",z = ":";
  qx.Bootstrap.define(m, {
    statics : {
      __fH : 30,
      __fI : [k, p, c, d, l, t, f, g, q],
      __fr : {
        "scale" : true,
        "rotate" : true,
        "skew" : true,
        "translate" : true
      },
      animate : function(el, desc, duration){

        return this._animate(el, desc, duration, false);
      },
      animateReverse : function(el, desc, duration){

        return this._animate(el, desc, duration, true);
      },
      _animate : function(el, desc, duration, reverse){

        if(el.$$animation){

          return el.$$animation;
        };
        desc = qx.lang.Object.clone(desc, true);
        if(duration == undefined){

          duration = desc.duration;
        };
        var keyFrames = desc.keyFrames;
        var keys = this.__fR(keyFrames);
        var stepTime = this.__fQ(duration, keys);
        var steps = parseInt(duration / stepTime, 10);
        this.__fJ(keyFrames, el);
        var delta = this.__fL(steps, stepTime, keys, keyFrames, duration, desc.timing);
        var handle = new qx.bom.element.AnimationHandle();
        handle.jsAnimation = true;
        if(reverse){

          delta.reverse();
          handle.reverse = true;
        };
        handle.desc = desc;
        handle.el = el;
        handle.delta = delta;
        handle.stepTime = stepTime;
        handle.steps = steps;
        el.$$animation = handle;
        handle.i = 0;
        handle.initValues = {
        };
        handle.repeatSteps = this.__fO(steps, desc.repeat);
        var delay = desc.delay || 0;
        var self = this;
        handle.delayId = window.setTimeout(function(){

          handle.delayId = null;
          self.play(handle);
        }, delay);
        return handle;
      },
      __fJ : function(keyFrames, el){

        var units = {
        };
        for(var percent in keyFrames){

          for(var name in keyFrames[percent]){

            var prefixed = qx.bom.Style.getPropertyName(name);
            if(prefixed && prefixed != name){

              var prefixedName = qx.bom.Style.getCssName(prefixed);
              keyFrames[percent][prefixedName] = keyFrames[percent][name];
              delete keyFrames[percent][name];
              name = prefixedName;
            };
            if(units[name] == undefined){

              var item = keyFrames[percent][name];
              if(typeof item == y){

                units[name] = this.__fM(item);
              } else {

                units[name] = h;
              };
            };
          };
        };
        for(var percent in keyFrames){

          var frame = keyFrames[percent];
          for(var name in units){

            if(frame[name] == undefined){

              if(name in el.style){

                if(window.getComputedStyle){

                  frame[name] = getComputedStyle(el, null)[name];
                } else {

                  frame[name] = el.style[name];
                };
              } else {

                frame[name] = el[name];
              };
              if(frame[name] === h && this.__fI.indexOf(units[name]) != -1){

                frame[name] = e + units[name];
              };
            };
          };
        };
      },
      __fK : function(frame){

        frame = qx.lang.Object.clone(frame);
        var transforms;
        for(var name in frame){

          if(name in this.__fr){

            if(!transforms){

              transforms = {
              };
            };
            transforms[name] = frame[name];
            delete frame[name];
          };
        };
        if(transforms){

          var transformStyle = qx.bom.element.Transform.getCss(transforms).split(z);
          if(transformStyle.length > 1){

            frame[transformStyle[0]] = transformStyle[1].replace(u, h);
          };
        };
        return frame;
      },
      __fL : function(steps, stepTime, keys, keyFrames, duration, timing){

        var delta = new Array(steps);
        var keyIndex = 1;
        delta[0] = this.__fK(keyFrames[0]);
        var last = keyFrames[0];
        var next = keyFrames[keys[keyIndex]];
        var stepsToNext = Math.floor(keys[keyIndex] / (stepTime / duration * 100));
        var calculationIndex = 1;
        for(var i = 1;i < delta.length;i++){

          if(i * stepTime / duration * 100 > keys[keyIndex]){

            last = next;
            keyIndex++;
            next = keyFrames[keys[keyIndex]];
            stepsToNext = Math.floor(keys[keyIndex] / (stepTime / duration * 100)) - stepsToNext;
            calculationIndex = 1;
          };
          delta[i] = {
          };
          var transforms;
          for(var name in next){

            var nItem = next[name] + h;
            if(name in this.__fr){

              if(!transforms){

                transforms = {
                };
              };
              if(qx.Bootstrap.isArray(last[name])){

                if(!qx.Bootstrap.isArray(next[name])){

                  next[name] = [next[name]];
                };
                transforms[name] = [];
                for(var j = 0;j < next[name].length;j++){

                  var item = next[name][j] + h;
                  var x = calculationIndex / stepsToNext;
                  transforms[name][j] = this.__fN(item, last[name], timing, x);
                };
              } else {

                var x = calculationIndex / stepsToNext;
                transforms[name] = this.__fN(nItem, last[name], timing, x);
              };
            } else if(nItem.charAt(0) == o){

              var value0 = qx.util.ColorUtil.cssStringToRgb(last[name]);
              var value1 = qx.util.ColorUtil.cssStringToRgb(nItem);
              var stepValue = [];
              for(var j = 0;j < value0.length;j++){

                var range = value0[j] - value1[j];
                var x = calculationIndex / stepsToNext;
                var timingX = qx.bom.AnimationFrame.calculateTiming(timing, x);
                stepValue[j] = parseInt(value0[j] - range * timingX, 10);
              };
              delta[i][name] = qx.util.ColorUtil.rgbToHexString(stepValue);
            } else if(!isNaN(parseFloat(nItem))){

              var x = calculationIndex / stepsToNext;
              delta[i][name] = this.__fN(nItem, last[name], timing, x);
            } else {

              delta[i][name] = last[name] + h;
            };;
          };
          if(transforms){

            var transformStyle = qx.bom.element.Transform.getCss(transforms).split(z);
            if(transformStyle.length > 1){

              delta[i][transformStyle[0]] = transformStyle[1].replace(u, h);
            };
          };
          calculationIndex++;
        };
        delta[delta.length - 1] = this.__fK(keyFrames[100]);
        return delta;
      },
      __fM : function(item){

        return item.substring((parseFloat(item) + h).length, item.length);
      },
      __fN : function(nextItem, lastItem, timing, x){

        var range = parseFloat(nextItem) - parseFloat(lastItem);
        return (parseFloat(lastItem) + range * qx.bom.AnimationFrame.calculateTiming(timing, x)) + this.__fM(nextItem);
      },
      play : function(handle){

        handle.emit(r, handle.el);
        var id = window.setInterval(function(){

          handle.repeatSteps--;
          var values = handle.delta[handle.i % handle.steps];
          if(handle.i === 0){

            for(var name in values){

              if(handle.initValues[name] === undefined){

                if(handle.el[name] !== undefined){

                  handle.initValues[name] = handle.el[name];
                } else if(qx.bom.element.Style){

                  handle.initValues[name] = qx.bom.element.Style.get(handle.el, qx.lang.String.camelCase(name));
                } else {

                  handle.initValues[name] = handle.el.style[qx.lang.String.camelCase(name)];
                };
              };
            };
          };
          qx.bom.element.AnimationJs.__fP(handle.el, values);
          handle.i++;
          if(handle.i % handle.steps == 0){

            handle.emit(w, handle.el);
            if(handle.desc.alternate){

              handle.delta.reverse();
            };
          };
          if(handle.repeatSteps < 0){

            qx.bom.element.AnimationJs.stop(handle);
          };
        }, handle.stepTime);
        handle.animationId = id;
        return handle;
      },
      pause : function(handle){

        window.clearInterval(handle.animationId);
        handle.animationId = null;
        return handle;
      },
      stop : function(handle){

        var desc = handle.desc;
        var el = handle.el;
        var initValues = handle.initValues;
        if(handle.animationId){

          window.clearInterval(handle.animationId);
        };
        if(handle.delayId){

          window.clearTimeout(handle.delayId);
        };
        if(el == undefined){

          return handle;
        };
        var keep = desc.keep;
        if(keep != undefined && !handle.stopped){

          if(handle.reverse || (desc.alternate && desc.repeat && desc.repeat % 2 == 0)){

            keep = 100 - keep;
          };
          this.__fP(el, desc.keyFrames[keep]);
        } else {

          this.__fP(el, initValues);
        };
        el.$$animation = null;
        handle.el = null;
        handle.ended = true;
        handle.animationId = null;
        handle.emit(s, el);
        return handle;
      },
      __fO : function(steps, repeat){

        if(repeat == undefined){

          return steps;
        };
        if(repeat == n){

          return Number.MAX_VALUE;
        };
        return steps * repeat;
      },
      __fP : function(el, styles){

        for(var key in styles){

          if(styles[key] === undefined){

            continue;
          };
          if(typeof el.style[key] === v && key in el){

            el[key] = styles[key];
            continue;
          };
          var name = qx.bom.Style.getPropertyName(key) || key;
          if(qx.bom.element.Style){

            qx.bom.element.Style.set(el, name, styles[key]);
          } else {

            el.style[name] = styles[key];
          };
        };
      },
      __fQ : function(duration, keys){

        var minDiff = 100;
        for(var i = 0;i < keys.length - 1;i++){

          minDiff = Math.min(minDiff, keys[i + 1] - keys[i]);
        };
        var stepTime = duration * minDiff / 100;
        while(stepTime > this.__fH){

          stepTime = stepTime / 2;
        };
        return Math.round(stepTime);
      },
      __fR : function(keyFrames){

        var keys = Object.keys(keyFrames);
        for(var i = 0;i < keys.length;i++){

          keys[i] = parseInt(keys[i], 10);
        };
        keys.sort(function(a, b){

          return a - b;
        });
        return keys;
      }
    }
  });
})();
(function(){

  var a = "backface-visibility",b = "name",c = "perspective",d = "css.transform.3d",e = "visible",f = "",g = ") ",h = "px",j = "css.transform",k = "(",l = " ",m = "qx.bom.element.Transform",n = "hidden",o = "Z",p = ";",q = "perspective-origin",r = "X",s = "Y",t = "origin",u = "style",v = ":";
  qx.Bootstrap.define(m, {
    statics : {
      __fS : [r, s, o],
      __fT : qx.core.Environment.get(j),
      transform : function(el, transforms){

        var transformCss = this.__fU(transforms);
        if(this.__fT != null){

          var style = this.__fT[b];
          el.style[style] = transformCss;
        };
      },
      translate : function(el, value){

        this.transform(el, {
          translate : value
        });
      },
      scale : function(el, value){

        this.transform(el, {
          scale : value
        });
      },
      rotate : function(el, value){

        this.transform(el, {
          rotate : value
        });
      },
      skew : function(el, value){

        this.transform(el, {
          skew : value
        });
      },
      getCss : function(transforms){

        var transformCss = this.__fU(transforms);
        if(this.__fT != null){

          var style = this.__fT[b];
          return qx.bom.Style.getCssName(style) + v + transformCss + p;
        };
        return f;
      },
      setOrigin : function(el, value){

        if(this.__fT != null){

          el.style[this.__fT[t]] = value;
        };
      },
      getOrigin : function(el){

        if(this.__fT != null){

          return el.style[this.__fT[t]];
        };
        return f;
      },
      setStyle : function(el, value){

        if(this.__fT != null){

          el.style[this.__fT[u]] = value;
        };
      },
      getStyle : function(el){

        if(this.__fT != null){

          return el.style[this.__fT[u]];
        };
        return f;
      },
      setPerspective : function(el, value){

        if(this.__fT != null){

          el.style[this.__fT[c]] = value + h;
        };
      },
      getPerspective : function(el){

        if(this.__fT != null){

          return el.style[this.__fT[c]];
        };
        return f;
      },
      setPerspectiveOrigin : function(el, value){

        if(this.__fT != null){

          el.style[this.__fT[q]] = value;
        };
      },
      getPerspectiveOrigin : function(el){

        if(this.__fT != null){

          var value = el.style[this.__fT[q]];
          if(value != f){

            return value;
          } else {

            var valueX = el.style[this.__fT[q] + r];
            var valueY = el.style[this.__fT[q] + s];
            if(valueX != f){

              return valueX + l + valueY;
            };
          };
        };
        return f;
      },
      setBackfaceVisibility : function(el, value){

        if(this.__fT != null){

          el.style[this.__fT[a]] = value ? e : n;
        };
      },
      getBackfaceVisibility : function(el){

        if(this.__fT != null){

          return el.style[this.__fT[a]] == e;
        };
        return true;
      },
      __fU : function(transforms){

        var value = f;
        for(var func in transforms){

          var params = transforms[func];
          if(qx.Bootstrap.isArray(params)){

            for(var i = 0;i < params.length;i++){

              if(params[i] == undefined || (i == 2 && !qx.core.Environment.get(d))){

                continue;
              };
              value += func + this.__fS[i] + k;
              value += params[i];
              value += g;
            };
          } else {

            value += func + k + transforms[func] + g;
          };
        };
        return value;
      }
    }
  });
})();
(function(){

  var a = "Could not parse color: ",c = "",d = "Invalid hex value: ",e = "Could not convert system colors to RGB: ",h = "(",j = ")",k = "#",l = "a",m = "Invalid hex3 value: ",n = "qx.theme.manager.Color",o = "qx.util.ColorUtil",q = "Invalid hex6 value: ",s = "rgb",u = ",";
  qx.Bootstrap.define(o, {
    statics : {
      REGEXP : {
        hex3 : /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/,
        hex6 : /^#([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/,
        rgb : /^rgb\(\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*,\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*,\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*\)$/,
        rgba : /^rgba\(\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*,\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*,\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*,\s*([0-9]{1,3}\.{0,1}[0-9]*)\s*\)$/
      },
      SYSTEM : {
        activeborder : true,
        activecaption : true,
        appworkspace : true,
        background : true,
        buttonface : true,
        buttonhighlight : true,
        buttonshadow : true,
        buttontext : true,
        captiontext : true,
        graytext : true,
        highlight : true,
        highlighttext : true,
        inactiveborder : true,
        inactivecaption : true,
        inactivecaptiontext : true,
        infobackground : true,
        infotext : true,
        menu : true,
        menutext : true,
        scrollbar : true,
        threeddarkshadow : true,
        threedface : true,
        threedhighlight : true,
        threedlightshadow : true,
        threedshadow : true,
        window : true,
        windowframe : true,
        windowtext : true
      },
      NAMED : {
        black : [0, 0, 0],
        silver : [192, 192, 192],
        gray : [128, 128, 128],
        white : [255, 255, 255],
        maroon : [128, 0, 0],
        red : [255, 0, 0],
        purple : [128, 0, 128],
        fuchsia : [255, 0, 255],
        green : [0, 128, 0],
        lime : [0, 255, 0],
        olive : [128, 128, 0],
        yellow : [255, 255, 0],
        navy : [0, 0, 128],
        blue : [0, 0, 255],
        teal : [0, 128, 128],
        aqua : [0, 255, 255],
        transparent : [-1, -1, -1],
        magenta : [255, 0, 255],
        orange : [255, 165, 0],
        brown : [165, 42, 42]
      },
      isNamedColor : function(value){

        return this.NAMED[value] !== undefined;
      },
      isSystemColor : function(value){

        return this.SYSTEM[value] !== undefined;
      },
      supportsThemes : function(){

        if(qx.Class){

          return qx.Class.isDefined(n);
        };
        return false;
      },
      isThemedColor : function(value){

        if(!this.supportsThemes()){

          return false;
        };
        if(qx.theme && qx.theme.manager && qx.theme.manager.Color){

          return qx.theme.manager.Color.getInstance().isDynamic(value);
        };
        return false;
      },
      stringToRgb : function(str){

        if(this.supportsThemes() && this.isThemedColor(str)){

          str = qx.theme.manager.Color.getInstance().resolveDynamic(str);
        };
        if(this.isNamedColor(str)){

          return this.NAMED[str].concat();
        } else if(this.isSystemColor(str)){

          throw new Error(e + str);
        } else if(this.isRgbaString(str)){

          return this.__fW(str);
        } else if(this.isRgbString(str)){

          return this.__fV();
        } else if(this.isHex3String(str)){

          return this.__fX();
        } else if(this.isHex6String(str)){

          return this.__fY();
        };;;;;
        throw new Error(a + str);
      },
      cssStringToRgb : function(str){

        if(this.isNamedColor(str)){

          return this.NAMED[str];
        } else if(this.isSystemColor(str)){

          throw new Error(e + str);
        } else if(this.isRgbString(str)){

          return this.__fV();
        } else if(this.isRgbaString(str)){

          return this.__fW();
        } else if(this.isHex3String(str)){

          return this.__fX();
        } else if(this.isHex6String(str)){

          return this.__fY();
        };;;;;
        throw new Error(a + str);
      },
      stringToRgbString : function(str){

        return this.rgbToRgbString(this.stringToRgb(str));
      },
      rgbToRgbString : function(rgb){

        return s + (rgb[3] ? l : c) + h + rgb.join(u) + j;
      },
      rgbToHexString : function(rgb){

        return (k + qx.lang.String.pad(rgb[0].toString(16).toUpperCase(), 2) + qx.lang.String.pad(rgb[1].toString(16).toUpperCase(), 2) + qx.lang.String.pad(rgb[2].toString(16).toUpperCase(), 2));
      },
      isValidPropertyValue : function(str){

        return (this.isThemedColor(str) || this.isNamedColor(str) || this.isHex3String(str) || this.isHex6String(str) || this.isRgbString(str) || this.isRgbaString(str));
      },
      isCssString : function(str){

        return (this.isSystemColor(str) || this.isNamedColor(str) || this.isHex3String(str) || this.isHex6String(str) || this.isRgbString(str) || this.isRgbaString(str));
      },
      isHex3String : function(str){

        return this.REGEXP.hex3.test(str);
      },
      isHex6String : function(str){

        return this.REGEXP.hex6.test(str);
      },
      isRgbString : function(str){

        return this.REGEXP.rgb.test(str);
      },
      isRgbaString : function(str){

        return this.REGEXP.rgba.test(str);
      },
      __fV : function(){

        var red = parseInt(RegExp.$1, 10);
        var green = parseInt(RegExp.$2, 10);
        var blue = parseInt(RegExp.$3, 10);
        return [red, green, blue];
      },
      __fW : function(){

        var red = parseInt(RegExp.$1, 10);
        var green = parseInt(RegExp.$2, 10);
        var blue = parseInt(RegExp.$3, 10);
        var alpha = parseInt(RegExp.$4, 10);
        if(red === 0 && green === 0 & blue === 0 && alpha === 0){

          return [-1, -1, -1];
        };
        return [red, green, blue];
      },
      __fX : function(){

        var red = parseInt(RegExp.$1, 16) * 17;
        var green = parseInt(RegExp.$2, 16) * 17;
        var blue = parseInt(RegExp.$3, 16) * 17;
        return [red, green, blue];
      },
      __fY : function(){

        var red = (parseInt(RegExp.$1, 16) * 16) + parseInt(RegExp.$2, 16);
        var green = (parseInt(RegExp.$3, 16) * 16) + parseInt(RegExp.$4, 16);
        var blue = (parseInt(RegExp.$5, 16) * 16) + parseInt(RegExp.$6, 16);
        return [red, green, blue];
      },
      hex3StringToRgb : function(value){

        if(this.isHex3String(value)){

          return this.__fX(value);
        };
        throw new Error(m + value);
      },
      hex3StringToHex6String : function(value){

        if(this.isHex3String(value)){

          return this.rgbToHexString(this.hex3StringToRgb(value));
        };
        return value;
      },
      hex6StringToRgb : function(value){

        if(this.isHex6String(value)){

          return this.__fY(value);
        };
        throw new Error(q + value);
      },
      hexStringToRgb : function(value){

        if(this.isHex3String(value)){

          return this.__fX(value);
        };
        if(this.isHex6String(value)){

          return this.__fY(value);
        };
        throw new Error(d + value);
      },
      rgbToHsb : function(rgb){

        var hue,saturation,brightness;
        var red = rgb[0];
        var green = rgb[1];
        var blue = rgb[2];
        var cmax = (red > green) ? red : green;
        if(blue > cmax){

          cmax = blue;
        };
        var cmin = (red < green) ? red : green;
        if(blue < cmin){

          cmin = blue;
        };
        brightness = cmax / 255.0;
        if(cmax != 0){

          saturation = (cmax - cmin) / cmax;
        } else {

          saturation = 0;
        };
        if(saturation == 0){

          hue = 0;
        } else {

          var redc = (cmax - red) / (cmax - cmin);
          var greenc = (cmax - green) / (cmax - cmin);
          var bluec = (cmax - blue) / (cmax - cmin);
          if(red == cmax){

            hue = bluec - greenc;
          } else if(green == cmax){

            hue = 2.0 + redc - bluec;
          } else {

            hue = 4.0 + greenc - redc;
          };
          hue = hue / 6.0;
          if(hue < 0){

            hue = hue + 1.0;
          };
        };
        return [Math.round(hue * 360), Math.round(saturation * 100), Math.round(brightness * 100)];
      },
      hsbToRgb : function(hsb){

        var i,f,p,r,t;
        var hue = hsb[0] / 360;
        var saturation = hsb[1] / 100;
        var brightness = hsb[2] / 100;
        if(hue >= 1.0){

          hue %= 1.0;
        };
        if(saturation > 1.0){

          saturation = 1.0;
        };
        if(brightness > 1.0){

          brightness = 1.0;
        };
        var tov = Math.floor(255 * brightness);
        var rgb = {
        };
        if(saturation == 0.0){

          rgb.red = rgb.green = rgb.blue = tov;
        } else {

          hue *= 6.0;
          i = Math.floor(hue);
          f = hue - i;
          p = Math.floor(tov * (1.0 - saturation));
          r = Math.floor(tov * (1.0 - (saturation * f)));
          t = Math.floor(tov * (1.0 - (saturation * (1.0 - f))));
          switch(i){case 0:
          rgb.red = tov;
          rgb.green = t;
          rgb.blue = p;
          break;case 1:
          rgb.red = r;
          rgb.green = tov;
          rgb.blue = p;
          break;case 2:
          rgb.red = p;
          rgb.green = tov;
          rgb.blue = t;
          break;case 3:
          rgb.red = p;
          rgb.green = r;
          rgb.blue = tov;
          break;case 4:
          rgb.red = t;
          rgb.green = p;
          rgb.blue = tov;
          break;case 5:
          rgb.red = tov;
          rgb.green = p;
          rgb.blue = r;
          break;};
        };
        return [rgb.red, rgb.green, rgb.blue];
      },
      randomColor : function(){

        var r = Math.round(Math.random() * 255);
        var g = Math.round(Math.random() * 255);
        var b = Math.round(Math.random() * 255);
        return this.rgbToRgbString([r, g, b]);
      }
    }
  });
})();
(function(){

  var b = "ease-in-out",c = "Number",d = "css.animation.requestframe",e = "qx.bom.AnimationFrame",f = "frame",g = "end",h = "linear",j = "ease-in",k = "ease-out";
  qx.Bootstrap.define(e, {
    extend : qx.event.Emitter,
    events : {
      "end" : undefined,
      "frame" : c
    },
    members : {
      __ga : false,
      startSequence : function(duration){

        this.__ga = false;
        var start = +(new Date());
        var clb = function(time){

          if(this.__ga){

            this.id = null;
            return;
          };
          if(time >= start + duration){

            this.emit(g);
            this.id = null;
          } else {

            var timePassed = Math.max(time - start, 0);
            this.emit(f, timePassed);
            this.id = qx.bom.AnimationFrame.request(clb, this);
          };
        };
        this.id = qx.bom.AnimationFrame.request(clb, this);
      },
      cancelSequence : function(){

        this.__ga = true;
      }
    },
    statics : {
      TIMEOUT : 30,
      calculateTiming : function(func, x){

        if(func == j){

          var a = [3.1223e-7, 0.0757, 1.2646, -0.167, -0.4387, 0.2654];
        } else if(func == k){

          var a = [-7.0198e-8, 1.652, -0.551, -0.0458, 0.1255, -0.1807];
        } else if(func == h){

          return x;
        } else if(func == b){

          var a = [2.482e-7, -0.2289, 3.3466, -1.0857, -1.7354, 0.7034];
        } else {

          var a = [-0.0021, 0.2472, 9.8054, -21.6869, 17.7611, -5.1226];
        };;;
        var y = 0;
        for(var i = 0;i < a.length;i++){

          y += a[i] * Math.pow(x, i);
        };
        return y;
      },
      request : function(callback, context){

        var req = qx.core.Environment.get(d);
        var clb = function(time){

          if(time < 1e10){

            time = this.__gb + time;
          };
          time = time || +(new Date());
          callback.call(context, time);
        };
        if(req){

          return window[req](clb);
        } else {

          return window.setTimeout(function(){

            clb();
          }, qx.bom.AnimationFrame.TIMEOUT);
        };
      }
    },
    defer : function(statics){

      statics.__gb = window.performance && performance.timing && performance.timing.navigationStart;
      if(!statics.__gb){

        statics.__gb = Date.now();
      };
    }
  });
})();
(function(){

  var a = "Boolean",b = "px",c = 'anchor',d = "keep-align",e = "changeIcon",f = "qx.ui.mobile.dialog.Popup",g = "0px",h = "bottom-left",i = 'left',j = 'bottom',k = "middle",l = "String",m = 'top',n = "resize",o = "__eJ",p = "popup-content",q = "touchstart",r = "top",s = "changeTitle",t = 'popup-title',u = "50%",v = "anchor-target",w = "popup",x = "_applyIcon",y = 'right',z = "left",A = "tap",B = "_applyTitle",C = "absolute";
  qx.Class.define(f, {
    extend : qx.ui.mobile.core.Widget,
    statics : {
      ROOT : null
    },
    construct : function(widget, anchor){

      qx.ui.mobile.core.Widget.call(this);
      this.exclude();
      if(qx.ui.mobile.dialog.Popup.ROOT == null){

        qx.ui.mobile.dialog.Popup.ROOT = qx.core.Init.getApplication().getRoot();
      };
      qx.ui.mobile.dialog.Popup.ROOT.add(this);
      this.__gc = anchor;
      if(widget){

        this._initializeChild(widget);
      };
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : w
      },
      title : {
        apply : B,
        nullable : true,
        check : l,
        event : s
      },
      icon : {
        check : l,
        apply : x,
        nullable : true,
        event : e
      },
      modal : {
        init : false,
        check : a,
        nullable : false
      },
      hideOnBlockerClick : {
        check : a,
        init : false
      }
    },
    members : {
      __gd : false,
      __eJ : null,
      __ge : null,
      __gc : null,
      __gf : null,
      __gg : null,
      __gh : null,
      _updatePosition : function(){

        this.removeCssClasses([m, j, i, y, c]);
        if(this.__gc){

          this.addCssClass(c);
          var rootHeight = qx.ui.mobile.dialog.Popup.ROOT.getHeight();
          var rootWidth = qx.ui.mobile.dialog.Popup.ROOT.getWidth();
          var rootPosition = qx.bom.element.Location.get(qx.ui.mobile.dialog.Popup.ROOT.getContainerElement());
          var anchorPosition = qx.bom.element.Location.get(this.__gc.getContainerElement());
          var popupDimension = qx.bom.element.Dimension.getSize(this.getContainerElement());
          this.__gh = popupDimension;
          var computedPopupPosition = qx.util.placement.Placement.compute(popupDimension, {
            width : rootPosition.left + rootWidth,
            height : rootPosition.top + rootHeight
          }, anchorPosition, {
            left : 0,
            right : 0,
            top : 0,
            bottom : 0
          }, h, d, d);
          this._resetPosition();
          var isTop = anchorPosition.top > computedPopupPosition.top;
          var isLeft = anchorPosition.left > computedPopupPosition.left;
          computedPopupPosition.top = computedPopupPosition.top - rootPosition.top;
          computedPopupPosition.left = computedPopupPosition.left - rootPosition.left;
          var isOutsideViewPort = computedPopupPosition.top < 0 || computedPopupPosition.left < 0 || computedPopupPosition.left + popupDimension.width > rootWidth || computedPopupPosition.top + popupDimension.height > rootHeight;
          if(isOutsideViewPort){

            this._positionToCenter();
          } else {

            if(isTop){

              this.addCssClass(j);
            } else {

              this.addCssClass(m);
            };
            if(isLeft){

              this.addCssClass(y);
            } else {

              this.addCssClass(i);
            };
            this.placeTo(computedPopupPosition.left, computedPopupPosition.top);
          };
        } else if(this.__eJ){

          this._positionToCenter();
        };
      },
      show : function(){

        if(!this.__gd){

          this.__gi();
          this.placeTo(-1000, -1000);
          qx.ui.mobile.core.Widget.prototype.show.call(this);
          this._updatePosition();
        };
        this.__gd = true;
        if(this.getModal() === true){

          qx.ui.mobile.core.Blocker.getInstance().show();
          if(this.getHideOnBlockerClick()){

            qx.ui.mobile.core.Blocker.getInstance().addListener(A, this.hide, this);
          };
        };
      },
      hide : function(){

        if(this.__gd){

          this.__gj();
          this.exclude();
        };
        this.__gd = false;
        if(this.getModal()){

          qx.ui.mobile.core.Blocker.getInstance().hide();
        };
        qx.ui.mobile.core.Blocker.getInstance().removeListener(A, this.hide, this);
      },
      hideWithDelay : function(delay){

        if(delay){

          qx.lang.Function.delay(this.hide, delay, this);
        } else {

          this.hide();
        };
      },
      isShown : function(){

        return this.__gd;
      },
      toggleVisibility : function(){

        if(this.__gd == true){

          this.hide();
        } else {

          this.show();
        };
      },
      placeTo : function(left, top){

        this._setStyle(z, left + b);
        this._setStyle(r, top + b);
      },
      _trackUserTouch : function(evt){

        var clientX = evt.getAllTouches()[0].clientX;
        var clientY = evt.getAllTouches()[0].clientY;
        var popupLocation = qx.bom.element.Location.get(this.getContainerElement());
        var isOutsideWidget = clientX < popupLocation.left || clientX > popupLocation.left + this.__gh.width || clientY > popupLocation.top + this.__gh.height || clientY < popupLocation.top;
        if(isOutsideWidget){

          this.hide();
        };
      },
      _preventTouch : function(evt){

        var originalTargetWidget = qx.ui.mobile.core.Widget.getWidgetById(evt.getOriginalTarget().id);
        if(!(originalTargetWidget instanceof qx.ui.mobile.form.Input) && !(originalTargetWidget instanceof qx.ui.mobile.form.TextArea)){

          evt.preventDefault();
        };
      },
      _positionToCenter : function(){

        var container = this.getContainerElement();
        container.style.position = C;
        container.style.marginLeft = -(container.offsetWidth / 2) + b;
        container.style.marginTop = -(container.offsetHeight / 2) + b;
        container.style.left = u;
        container.style.top = u;
      },
      _resetPosition : function(){

        var container = this.getContainerElement();
        container.style.left = g;
        container.style.top = g;
        container.style.marginLeft = null;
        container.style.marginTop = null;
      },
      __gi : function(){

        qx.event.Registration.addListener(window, n, this._updatePosition, this);
        if(this.__gc){

          this.__gc.addCssClass(v);
          qx.ui.mobile.dialog.Popup.ROOT.addListener(q, this._trackUserTouch, this);
        };
        this.addListener(q, this._preventTouch, this);
      },
      __gj : function(){

        qx.event.Registration.removeListener(window, n, this._updatePosition, this);
        if(this.__gc){

          this.__gc.removeCssClass(v);
          qx.ui.mobile.dialog.Popup.ROOT.removeListener(q, this._trackUserTouch, this);
        };
        this.removeListener(q, this._preventTouch, this);
      },
      _initializeChild : function(widget){

        if(this.__eJ == null){

          this.__eJ = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.VBox().set({
            alignY : k
          }));
          this.__eJ.setDefaultCssClass(p);
          this._add(this.__eJ);
        };
        if(this._createTitleWidget()){

          this.__eJ.remove(this._createTitleWidget());
          this.__eJ.add(this._createTitleWidget());
        };
        this.__eJ.add(widget, {
          flex : 1
        });
        this.__gf = widget;
      },
      _createTitleWidget : function(){

        if(this.__gg){

          return this.__gg;
        };
        if(this.getTitle() || this.getIcon()){

          this.__gg = new qx.ui.mobile.basic.Atom(this.getTitle(), this.getIcon());
          this.__gg.addCssClass(t);
          return this.__gg;
        } else {

          return null;
        };
      },
      _applyTitle : function(value, old){

        if(value){

          if(this.__gg){

            this.__gg.setLabel(value);
          } else {

            this.__gg = new qx.ui.mobile.basic.Atom(value, this.getIcon());
            this.__gg.addCssClass(t);
            if(this.__gf){

              this.__eJ.addBefore(this._createTitleWidget(), this.__gf);
            } else {

              if(this.__eJ){

                this.__eJ.add(this._createTitleWidget());
              };
            };
          };
        };
      },
      _applyIcon : function(value, old){

        if(value){

          if(this.__gg){

            this.__gg.setIcon(value);
          } else {

            this.__gg = new qx.ui.mobile.basic.Atom(this.getTitle(), value);
            this.__gg.addCssClass(t);
            if(this.__gf){

              this.__eJ.addBefore(this._createTitleWidget(), this.__gf);
            } else {

              if(this.__eJ){

                this.__eJ.add(this._createTitleWidget());
              };
            };
          };
        };
      },
      add : function(widget){

        this.removeWidget();
        this._initializeChild(widget);
      },
      setAnchor : function(widget){

        this.__gc = widget;
        this._updatePosition();
      },
      getTitleWidget : function(){

        return this.__gg;
      },
      removeWidget : function(){

        if(this.__gf){

          this.__eJ.remove(this.__gf);
          return this.__gf;
        } else {

          {
          };
          return null;
        };
      },
      _getBlocker : function(){

        return qx.ui.mobile.core.Blocker.getInstance();
      }
    },
    destruct : function(){

      this.__gj();
      this._disposeObjects(o);
      this.__gd = this.__ge = this._anchor = this.__gf = this.__gh = null;
    }
  });
})();
(function(){

  var a = "-",b = "align-start",c = "best-fit",d = "qx.util.placement.Placement",e = "middle",f = "bottom",g = "keep-align",h = "align-end",i = "align-center",j = "Invalid 'mode' argument!'",k = "center",l = "edge-start",m = "Class",n = '__gk',o = "direct",p = "top",q = "left",r = "right",s = "edge-end";
  qx.Class.define(d, {
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__gk = qx.util.placement.DirectAxis;
    },
    properties : {
      axisX : {
        check : m
      },
      axisY : {
        check : m
      },
      edge : {
        check : [p, r, f, q],
        init : p
      },
      align : {
        check : [p, r, f, q, k, e],
        init : r
      }
    },
    statics : {
      __gl : null,
      compute : function(size, area, target, offsets, position, modeX, modeY){

        this.__gl = this.__gl || new qx.util.placement.Placement();
        var splitted = position.split(a);
        var edge = splitted[0];
        var align = splitted[1];
        {

          var expected;
        };
        this.__gl.set({
          axisX : this.__gp(modeX),
          axisY : this.__gp(modeY),
          edge : edge,
          align : align
        });
        return this.__gl.compute(size, area, target, offsets);
      },
      __gm : null,
      __gn : null,
      __go : null,
      __gp : function(mode){

        switch(mode){case o:
        this.__gm = this.__gm || qx.util.placement.DirectAxis;
        return this.__gm;case g:
        this.__gn = this.__gn || qx.util.placement.KeepAlignAxis;
        return this.__gn;case c:
        this.__go = this.__go || qx.util.placement.BestFitAxis;
        return this.__go;default:
        throw new Error(j);};
      }
    },
    members : {
      __gk : null,
      compute : function(size, area, target, offsets){

        {
        };
        var axisX = this.getAxisX() || this.__gk;
        var left = axisX.computeStart(size.width, {
          start : target.left,
          end : target.right
        }, {
          start : offsets.left,
          end : offsets.right
        }, area.width, this.__gq());
        var axisY = this.getAxisY() || this.__gk;
        var top = axisY.computeStart(size.height, {
          start : target.top,
          end : target.bottom
        }, {
          start : offsets.top,
          end : offsets.bottom
        }, area.height, this.__gr());
        return {
          left : left,
          top : top
        };
      },
      __gq : function(){

        var edge = this.getEdge();
        var align = this.getAlign();
        if(edge == q){

          return l;
        } else if(edge == r){

          return s;
        } else if(align == q){

          return b;
        } else if(align == k){

          return i;
        } else if(align == r){

          return h;
        };;;;
      },
      __gr : function(){

        var edge = this.getEdge();
        var align = this.getAlign();
        if(edge == p){

          return l;
        } else if(edge == f){

          return s;
        } else if(align == p){

          return b;
        } else if(align == e){

          return i;
        } else if(align == f){

          return h;
        };;;;
      }
    },
    destruct : function(){

      this._disposeObjects(n);
    }
  });
})();
(function(){

  var a = "align-start",b = "align-end",c = "qx.util.placement.AbstractAxis",d = "edge-start",e = "align-center",f = "abstract method call!",g = "edge-end";
  qx.Bootstrap.define(c, {
    extend : Object,
    statics : {
      computeStart : function(size, target, offsets, areaSize, position){

        throw new Error(f);
      },
      _moveToEdgeAndAlign : function(size, target, offsets, position){

        switch(position){case d:
        return target.start - offsets.end - size;case g:
        return target.end + offsets.start;case a:
        return target.start + offsets.start;case e:
        return target.start + parseInt((target.end - target.start - size) / 2, 10) + offsets.start;case b:
        return target.end - offsets.end - size;};
      },
      _isInRange : function(start, size, areaSize){

        return start >= 0 && start + size <= areaSize;
      }
    }
  });
})();
(function(){

  var a = "qx.util.placement.DirectAxis";
  qx.Bootstrap.define(a, {
    statics : {
      _moveToEdgeAndAlign : qx.util.placement.AbstractAxis._moveToEdgeAndAlign,
      computeStart : function(size, target, offsets, areaSize, position){

        return this._moveToEdgeAndAlign(size, target, offsets, position);
      }
    }
  });
})();
(function(){

  var a = "qx.util.placement.KeepAlignAxis",b = "edge-start",c = "edge-end";
  qx.Bootstrap.define(a, {
    statics : {
      _moveToEdgeAndAlign : qx.util.placement.AbstractAxis._moveToEdgeAndAlign,
      _isInRange : qx.util.placement.AbstractAxis._isInRange,
      computeStart : function(size, target, offsets, areaSize, position){

        var start = this._moveToEdgeAndAlign(size, target, offsets, position);
        var range1End,range2Start;
        if(this._isInRange(start, size, areaSize)){

          return start;
        };
        if(position == b || position == c){

          range1End = target.start - offsets.end;
          range2Start = target.end + offsets.start;
        } else {

          range1End = target.end - offsets.end;
          range2Start = target.start + offsets.start;
        };
        if(range1End > areaSize - range2Start){

          start = range1End - size;
        } else {

          start = range2Start;
        };
        return start;
      }
    }
  });
})();
(function(){

  var a = "qx.util.placement.BestFitAxis";
  qx.Bootstrap.define(a, {
    statics : {
      _isInRange : qx.util.placement.AbstractAxis._isInRange,
      _moveToEdgeAndAlign : qx.util.placement.AbstractAxis._moveToEdgeAndAlign,
      computeStart : function(size, target, offsets, areaSize, position){

        var start = this._moveToEdgeAndAlign(size, target, offsets, position);
        if(this._isInRange(start, size, areaSize)){

          return start;
        };
        if(start < 0){

          start = Math.min(0, areaSize - size);
        };
        if(start + size > areaSize){

          start = Math.max(0, areaSize - size);
        };
        return start;
      }
    }
  });
})();
(function(){

  var a = "resize",b = "touchmove",c = "px",d = "blocker",e = "qx.ui.mobile.core.Blocker",f = "touchstart",g = "scroll",h = "singleton";
  qx.Class.define(e, {
    extend : qx.ui.mobile.core.Widget,
    type : h,
    statics : {
      ROOT : null
    },
    construct : function(){

      qx.ui.mobile.core.Widget.call(this);
      if(qx.ui.mobile.core.Blocker.ROOT == null){

        qx.ui.mobile.core.Blocker.ROOT = qx.core.Init.getApplication().getRoot();
      };
      this.forceHide();
      qx.ui.mobile.core.Blocker.ROOT.add(this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : d
      }
    },
    members : {
      __gs : 0,
      show : function(){

        if(this.__gs == 0){

          this._updateSize();
          this.__gi();
          qx.ui.mobile.core.Widget.prototype.show.call(this);
        };
        this.__gs++;
      },
      hide : function(){

        this.__gs--;
        if(this.__gs <= 0){

          this.__gs = 0;
          this.__gj();
          this.exclude();
        };
      },
      forceHide : function(){

        this.__gs = 0;
        this.hide();
      },
      isShown : function(){

        return this.__gs > 0;
      },
      _updateSize : function(){

        if(qx.ui.mobile.core.Blocker.ROOT == this.getLayoutParent()){

          this.getContainerElement().style.top = qx.bom.Viewport.getScrollTop() + c;
          this.getContainerElement().style.left = qx.bom.Viewport.getScrollLeft() + c;
          this.getContainerElement().style.width = qx.bom.Viewport.getWidth() + c;
          this.getContainerElement().style.height = qx.bom.Viewport.getHeight() + c;
        } else if(this.getLayoutParent() != null){

          var dimension = qx.bom.element.Dimension.getSize(this.getLayoutParent().getContainerElement());
          this.getContainerElement().style.width = dimension.width + c;
          this.getContainerElement().style.height = dimension.height + c;
        };
      },
      _onTouch : function(evt){

        evt.preventDefault();
      },
      _onScroll : function(evt){

        this._updateSize();
      },
      __gi : function(){

        qx.event.Registration.addListener(window, a, this._updateSize, this);
        qx.event.Registration.addListener(window, g, this._onScroll, this);
        this.addListener(f, this._onTouch, this);
        this.addListener(b, this._onTouch, this);
      },
      __gj : function(){

        qx.event.Registration.removeListener(window, a, this._updateSize, this);
        qx.event.Registration.removeListener(window, g, this._onScroll, this);
        this.removeListener(f, this._onTouch, this);
        this.removeListener(b, this._onTouch, this);
      }
    },
    destruct : function(){

      qx.ui.mobile.core.Blocker.ROOT.remove(this);
      this.__gj();
    }
  });
})();
(function(){

  var a = "qx.ui.form.IForm",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeEnabled" : b,
      "changeValid" : b,
      "changeInvalidMessage" : b,
      "changeRequired" : b
    },
    members : {
      setEnabled : function(enabled){

        return arguments.length == 1;
      },
      getEnabled : function(){
      },
      setRequired : function(required){

        return arguments.length == 1;
      },
      getRequired : function(){
      },
      setValid : function(valid){

        return arguments.length == 1;
      },
      getValid : function(){
      },
      setInvalidMessage : function(message){

        return arguments.length == 1;
      },
      getInvalidMessage : function(){
      },
      setRequiredInvalidMessage : function(message){

        return arguments.length == 1;
      },
      getRequiredInvalidMessage : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.form.MState";
  qx.Mixin.define(a, {
    members : {
      __gt : null,
      addState : function(state){

        if(this.__gt === null){

          this.__gt = {
          };
        };
        this.__gt[state] = true;
        this.addCssClass(state);
      },
      hasState : function(state){

        return this.__gt !== null && this.__gt[state];
      },
      removeState : function(state){

        if(this.hasState(state)){

          delete this.__gt[state];
          this.removeCssClass(state);
        };
      },
      replaceState : function(oldState, newState){

        if(this.hasState(oldState)){

          delete this.__gt[oldState];
          this.__gt[newState] = true;
          this.removeCssClass(oldState);
          this.addCssClass(newState);
        } else {

          this.addState(newState);
        };
      }
    }
  });
})();
(function(){

  var a = "qx.ui.form.IModel",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeModel" : b
    },
    members : {
      setModel : function(value){
      },
      getModel : function(){
      },
      resetModel : function(){
      }
    }
  });
})();
(function(){

  var a = "",b = "qx.ui.form.MForm",c = "Boolean",d = "_applyValid",f = "changeLocale",g = "changeRequired",h = "changeValid",i = "qx.dynlocale",j = "changeInvalidMessage",k = "String",l = "invalid";
  qx.Mixin.define(b, {
    construct : function(){

      if(qx.core.Environment.get(i)){

        qx.locale.Manager.getInstance().addListener(f, this.__gu, this);
      };
    },
    properties : {
      valid : {
        check : c,
        init : true,
        apply : d,
        event : h
      },
      required : {
        check : c,
        init : false,
        event : g
      },
      invalidMessage : {
        check : k,
        init : a,
        event : j
      },
      requiredInvalidMessage : {
        check : k,
        nullable : true,
        event : j
      }
    },
    members : {
      _applyValid : function(value, old){

        value ? this.removeState(l) : this.addState(l);
      },
      __gu : qx.core.Environment.select(i, {
        "true" : function(e){

          var invalidMessage = this.getInvalidMessage();
          if(invalidMessage && invalidMessage.translate){

            this.setInvalidMessage(invalidMessage.translate());
          };
          var requiredInvalidMessage = this.getRequiredInvalidMessage();
          if(requiredInvalidMessage && requiredInvalidMessage.translate){

            this.setRequiredInvalidMessage(requiredInvalidMessage.translate());
          };
        },
        "false" : null
      })
    },
    destruct : function(){

      if(qx.core.Environment.get(i)){

        qx.locale.Manager.getInstance().removeListener(f, this.__gu, this);
      };
    }
  });
})();
(function(){

  var a = "changeModel",b = "_applyModel",c = "qx.ui.form.MModelProperty";
  qx.Mixin.define(c, {
    properties : {
      model : {
        nullable : true,
        event : a,
        apply : b,
        dereference : true
      }
    },
    members : {
      _applyModel : function(value, old){
      }
    }
  });
})();
(function(){

  var a = "os.name",b = "blur",c = "abstract",d = "focus",e = "qx.ui.mobile.form.Input",f = "type",g = "ios",h = "input";
  qx.Class.define(e, {
    extend : qx.ui.mobile.core.Widget,
    include : [qx.ui.form.MForm, qx.ui.form.MModelProperty, qx.ui.mobile.form.MState],
    implement : [qx.ui.form.IForm, qx.ui.form.IModel],
    type : c,
    construct : function(){

      qx.ui.mobile.core.Widget.call(this);
      this._setAttribute(f, this._getType());
      if(qx.core.Environment.get(a) == g){

        this.addListener(b, this._onBlur, this);
        this.addListener(d, this._onFocus, this);
      };
    },
    statics : {
      SCROLL_TIMER_ID : null
    },
    members : {
      _getTagName : function(){

        return h;
      },
      _getType : function(){

        {
        };
      },
      _onFocus : function(){

        clearTimeout(qx.ui.mobile.form.Input.SCROLL_TIMER_ID);
      },
      _onBlur : function(){

        qx.ui.mobile.form.Input.SCROLL_TIMER_ID = setTimeout(function(){

          window.scrollTo(0, 0);
        }, 150);
      }
    },
    destruct : function(){

      if(qx.core.Environment.get(a) == g){

        this.removeListener(d, this._onFocus, this);
        this.removeListener(b, this._onBlur, this);
      };
    }
  });
})();
(function(){

  var a = "text",b = "blur",c = "engine.version",d = "keydown",f = "radio",g = "textarea",h = "password",j = "propertychange",k = "select-multiple",m = "change",n = "input",p = "value",q = "select",r = "browser.documentmode",s = "browser.version",t = "opera",u = "keyup",v = "mshtml",w = "engine.name",x = "keypress",y = "checkbox",z = "qx.event.handler.Input",A = "checked";
  qx.Class.define(z, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(){

      qx.core.Object.call(this);
      this._onChangeCheckedWrapper = qx.lang.Function.listener(this._onChangeChecked, this);
      this._onChangeValueWrapper = qx.lang.Function.listener(this._onChangeValue, this);
      this._onInputWrapper = qx.lang.Function.listener(this._onInput, this);
      this._onPropertyWrapper = qx.lang.Function.listener(this._onProperty, this);
      if((qx.core.Environment.get(w) == t)){

        this._onKeyDownWrapper = qx.lang.Function.listener(this._onKeyDown, this);
        this._onKeyUpWrapper = qx.lang.Function.listener(this._onKeyUp, this);
        this._onBlurWrapper = qx.lang.Function.listener(this._onBlur, this);
      };
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        input : 1,
        change : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : false
    },
    members : {
      __gv : false,
      __gw : null,
      __gx : null,
      __gy : null,
      canHandleEvent : function(target, type){

        var lower = target.tagName.toLowerCase();
        if(type === n && (lower === n || lower === g)){

          return true;
        };
        if(type === m && (lower === n || lower === g || lower === q)){

          return true;
        };
        return false;
      },
      registerEvent : function(target, type, capture){

        if(qx.core.Environment.get(w) == v && (qx.core.Environment.get(c) < 9 || (qx.core.Environment.get(c) >= 9 && qx.core.Environment.get(r) < 9))){

          if(!target.__gz){

            var tag = target.tagName.toLowerCase();
            var elementType = target.type;
            if(elementType === a || elementType === h || tag === g || elementType === y || elementType === f){

              qx.bom.Event.addNativeListener(target, j, this._onPropertyWrapper);
            };
            if(elementType !== y && elementType !== f){

              qx.bom.Event.addNativeListener(target, m, this._onChangeValueWrapper);
            };
            if(elementType === a || elementType === h){

              this._onKeyPressWrapped = qx.lang.Function.listener(this._onKeyPress, this, target);
              qx.bom.Event.addNativeListener(target, x, this._onKeyPressWrapped);
            };
            target.__gz = true;
          };
        } else {

          if(type === n){

            this.__gA(target);
          } else if(type === m){

            if(target.type === f || target.type === y){

              qx.bom.Event.addNativeListener(target, m, this._onChangeCheckedWrapper);
            } else {

              qx.bom.Event.addNativeListener(target, m, this._onChangeValueWrapper);
            };
            if((qx.core.Environment.get(w) == t) || (qx.core.Environment.get(w) == v)){

              if(target.type === a || target.type === h){

                this._onKeyPressWrapped = qx.lang.Function.listener(this._onKeyPress, this, target);
                qx.bom.Event.addNativeListener(target, x, this._onKeyPressWrapped);
              };
            };
          };
        };
      },
      __gA : qx.core.Environment.select(w, {
        "mshtml" : function(target){

          if(qx.core.Environment.get(c) >= 9 && qx.core.Environment.get(r) >= 9){

            qx.bom.Event.addNativeListener(target, n, this._onInputWrapper);
            if(target.type === a || target.type === h || target.type === g){

              this._inputFixWrapper = qx.lang.Function.listener(this._inputFix, this, target);
              qx.bom.Event.addNativeListener(target, u, this._inputFixWrapper);
            };
          };
        },
        "webkit" : function(target){

          var tag = target.tagName.toLowerCase();
          if(parseFloat(qx.core.Environment.get(c)) < 532 && tag == g){

            qx.bom.Event.addNativeListener(target, x, this._onInputWrapper);
          };
          qx.bom.Event.addNativeListener(target, n, this._onInputWrapper);
        },
        "opera" : function(target){

          qx.bom.Event.addNativeListener(target, u, this._onKeyUpWrapper);
          qx.bom.Event.addNativeListener(target, d, this._onKeyDownWrapper);
          qx.bom.Event.addNativeListener(target, b, this._onBlurWrapper);
          qx.bom.Event.addNativeListener(target, n, this._onInputWrapper);
        },
        "default" : function(target){

          qx.bom.Event.addNativeListener(target, n, this._onInputWrapper);
        }
      }),
      unregisterEvent : function(target, type){

        if(qx.core.Environment.get(w) == v && qx.core.Environment.get(c) < 9 && qx.core.Environment.get(r) < 9){

          if(target.__gz){

            var tag = target.tagName.toLowerCase();
            var elementType = target.type;
            if(elementType === a || elementType === h || tag === g || elementType === y || elementType === f){

              qx.bom.Event.removeNativeListener(target, j, this._onPropertyWrapper);
            };
            if(elementType !== y && elementType !== f){

              qx.bom.Event.removeNativeListener(target, m, this._onChangeValueWrapper);
            };
            if(elementType === a || elementType === h){

              qx.bom.Event.removeNativeListener(target, x, this._onKeyPressWrapped);
            };
            try{

              delete target.__gz;
            } catch(ex) {

              target.__gz = null;
            };
          };
        } else {

          if(type === n){

            this.__gB(target);
          } else if(type === m){

            if(target.type === f || target.type === y){

              qx.bom.Event.removeNativeListener(target, m, this._onChangeCheckedWrapper);
            } else {

              qx.bom.Event.removeNativeListener(target, m, this._onChangeValueWrapper);
            };
          };
          if((qx.core.Environment.get(w) == t) || (qx.core.Environment.get(w) == v)){

            if(target.type === a || target.type === h){

              qx.bom.Event.removeNativeListener(target, x, this._onKeyPressWrapped);
            };
          };
        };
      },
      __gB : qx.core.Environment.select(w, {
        "mshtml" : function(target){

          if(qx.core.Environment.get(c) >= 9 && qx.core.Environment.get(r) >= 9){

            qx.bom.Event.removeNativeListener(target, n, this._onInputWrapper);
            if(target.type === a || target.type === h || target.type === g){

              qx.bom.Event.removeNativeListener(target, u, this._inputFixWrapper);
            };
          };
        },
        "webkit" : function(target){

          var tag = target.tagName.toLowerCase();
          if(parseFloat(qx.core.Environment.get(c)) < 532 && tag == g){

            qx.bom.Event.removeNativeListener(target, x, this._onInputWrapper);
          };
          qx.bom.Event.removeNativeListener(target, n, this._onInputWrapper);
        },
        "opera" : function(target){

          qx.bom.Event.removeNativeListener(target, u, this._onKeyUpWrapper);
          qx.bom.Event.removeNativeListener(target, d, this._onKeyDownWrapper);
          qx.bom.Event.removeNativeListener(target, b, this._onBlurWrapper);
          qx.bom.Event.removeNativeListener(target, n, this._onInputWrapper);
        },
        "default" : function(target){

          qx.bom.Event.removeNativeListener(target, n, this._onInputWrapper);
        }
      }),
      _onKeyPress : qx.core.Environment.select(w, {
        "mshtml|opera" : function(e, target){

          if(e.keyCode === 13){

            if(target.value !== this.__gx){

              this.__gx = target.value;
              qx.event.Registration.fireEvent(target, m, qx.event.type.Data, [target.value]);
            };
          };
        },
        "default" : null
      }),
      _inputFix : qx.core.Environment.select(w, {
        "mshtml" : function(e, target){

          if(e.keyCode === 46 || e.keyCode === 8){

            if(target.value !== this.__gy){

              this.__gy = target.value;
              qx.event.Registration.fireEvent(target, n, qx.event.type.Data, [target.value]);
            };
          };
        },
        "default" : null
      }),
      _onKeyDown : qx.core.Environment.select(w, {
        "opera" : function(e){

          if(e.keyCode === 13){

            this.__gv = true;
          };
        },
        "default" : null
      }),
      _onKeyUp : qx.core.Environment.select(w, {
        "opera" : function(e){

          if(e.keyCode === 13){

            this.__gv = false;
          };
        },
        "default" : null
      }),
      _onBlur : qx.core.Environment.select(w, {
        "opera" : function(e){

          if(this.__gw && qx.core.Environment.get(s) < 10.6){

            window.clearTimeout(this.__gw);
          };
        },
        "default" : null
      }),
      _onInput : qx.event.GlobalError.observeMethod(function(e){

        var target = qx.bom.Event.getTarget(e);
        var tag = target.tagName.toLowerCase();
        if(!this.__gv || tag !== n){

          if((qx.core.Environment.get(w) == t) && qx.core.Environment.get(s) < 10.6){

            this.__gw = window.setTimeout(function(){

              qx.event.Registration.fireEvent(target, n, qx.event.type.Data, [target.value]);
            }, 0);
          } else {

            qx.event.Registration.fireEvent(target, n, qx.event.type.Data, [target.value]);
          };
        };
      }),
      _onChangeValue : qx.event.GlobalError.observeMethod(function(e){

        var target = qx.bom.Event.getTarget(e);
        var data = target.value;
        if(target.type === k){

          var data = [];
          for(var i = 0,o = target.options,l = o.length;i < l;i++){

            if(o[i].selected){

              data.push(o[i].value);
            };
          };
        };
        qx.event.Registration.fireEvent(target, m, qx.event.type.Data, [data]);
      }),
      _onChangeChecked : qx.event.GlobalError.observeMethod(function(e){

        var target = qx.bom.Event.getTarget(e);
        if(target.type === f){

          if(target.checked){

            qx.event.Registration.fireEvent(target, m, qx.event.type.Data, [target.value]);
          };
        } else {

          qx.event.Registration.fireEvent(target, m, qx.event.type.Data, [target.checked]);
        };
      }),
      _onProperty : qx.core.Environment.select(w, {
        "mshtml" : qx.event.GlobalError.observeMethod(function(e){

          var target = qx.bom.Event.getTarget(e);
          var prop = e.propertyName;
          if(prop === p && (target.type === a || target.type === h || target.tagName.toLowerCase() === g)){

            if(!target.$$inValueSet){

              qx.event.Registration.fireEvent(target, n, qx.event.type.Data, [target.value]);
            };
          } else if(prop === A){

            if(target.type === y){

              qx.event.Registration.fireEvent(target, m, qx.event.type.Data, [target.checked]);
            } else if(target.checked){

              qx.event.Registration.fireEvent(target, m, qx.event.type.Data, [target.value]);
            };
          };
        }),
        "default" : function(){
        }
      })
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "os.name",b = "qx.ui.mobile.form.MText",c = "Boolean",d = "_applyMaxLength",e = "placeholder",f = "android",g = "maxlength",h = "_applyPlaceholder",i = " ",j = "_applyAttribute",k = "String",l = "PositiveInteger";
  qx.Mixin.define(b, {
    construct : function(value){

      this.initMaxLength();
      this.initPlaceholder();
      this.initReadOnly();
    },
    properties : {
      maxLength : {
        check : l,
        nullable : true,
        init : null,
        apply : d
      },
      placeholder : {
        check : k,
        nullable : true,
        init : null,
        apply : h
      },
      readOnly : {
        check : c,
        nullable : true,
        init : null,
        apply : j
      }
    },
    members : {
      _applyMaxLength : function(value, old){

        this._setAttribute(g, value);
      },
      _applyPlaceholder : function(value, old){

        if(qx.core.Environment.get(a) == f && value !== null){

          value = i + value;
        };
        this._setAttribute(e, value);
      },
      focus : function(){

        if(this.isReadOnly() || this.getEnabled() == false){

          return;
        };
        var targetElement = this.getContainerElement();
        if(targetElement){

          qx.bom.Element.focus(targetElement);
        };
      },
      blur : function(){

        var targetElement = this.getContainerElement();
        if(targetElement){

          qx.bom.Element.blur(targetElement);
        };
      }
    }
  });
})();
(function(){

  var a = "-",b = "qx.event.handler.Element",c = "load",d = "iframe";
  qx.Class.define(b, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this._manager = manager;
      this._registeredEvents = {
      };
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        abort : true,
        load : true,
        scroll : true,
        select : true,
        reset : true,
        submit : true
      },
      CANCELABLE : {
        selectstart : true
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : false
    },
    members : {
      canHandleEvent : function(target, type){

        if(type === c){

          return target.tagName.toLowerCase() !== d;
        } else {

          return true;
        };
      },
      registerEvent : function(target, type, capture){

        var elementId = qx.core.ObjectRegistry.toHashCode(target);
        var eventId = elementId + a + type;
        var listener = qx.lang.Function.listener(this._onNative, this, eventId);
        qx.bom.Event.addNativeListener(target, type, listener);
        this._registeredEvents[eventId] = {
          element : target,
          type : type,
          listener : listener
        };
      },
      unregisterEvent : function(target, type, capture){

        var events = this._registeredEvents;
        if(!events){

          return;
        };
        var elementId = qx.core.ObjectRegistry.toHashCode(target);
        var eventId = elementId + a + type;
        var eventData = this._registeredEvents[eventId];
        if(eventData){

          qx.bom.Event.removeNativeListener(target, type, eventData.listener);
        };
        delete this._registeredEvents[eventId];
      },
      _onNative : qx.event.GlobalError.observeMethod(function(nativeEvent, eventId){

        var events = this._registeredEvents;
        if(!events){

          return;
        };
        var eventData = events[eventId];
        var isCancelable = this.constructor.CANCELABLE[eventData.type];
        qx.event.Registration.fireNonBubblingEvent(eventData.element, eventData.type, qx.event.type.Native, [nativeEvent, undefined, undefined, undefined, isCancelable]);
      })
    },
    destruct : function(){

      var entry;
      var events = this._registeredEvents;
      for(var id in events){

        entry = events[id];
        qx.bom.Event.removeNativeListener(entry.element, entry.type, entry.listener);
      };
      this._manager = this._registeredEvents = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "os.name",b = "mousedown",c = "mouseout",d = "gecko",e = "dblclick",f = "useraction",g = "mousewheel",h = "event.touch",j = "mousemove",k = "ios",l = "mouseover",m = "qx.event.handler.Mouse",n = "click",o = "on",p = "engine.version",q = "engine.name",r = "mouseup",s = "contextmenu",t = "qx.emulatemouse",u = "webkit",v = "DOMMouseScroll",w = "device.touch",x = "event.mspointer";
  qx.Class.define(m, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__cw = manager.getWindow();
      this.__a = this.__cw.document;
      if(!(qx.core.Environment.get(h) && qx.event.handler.MouseEmulation.ON)){

        this._initButtonObserver();
        this._initMoveObserver();
        this._initWheelObserver();
      };
      if(qx.core.Environment.get(t)){

        qx.event.handler.MouseEmulation;
      };
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        mousemove : 1,
        mouseover : 1,
        mouseout : 1,
        mousedown : 1,
        mouseup : 1,
        click : 1,
        dblclick : 1,
        contextmenu : 1,
        mousewheel : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE + qx.event.IEventHandler.TARGET_DOCUMENT + qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      __gC : null,
      __gD : null,
      __gE : null,
      __gF : null,
      __gG : null,
      __dh : null,
      __cw : null,
      __a : null,
      __gH : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : qx.core.Environment.get(a) === k ? function(target, type, capture){

        target[o + type] = (function(){

          return null;
        });
      } : (function(){

        return null;
      }),
      unregisterEvent : qx.core.Environment.get(a) === k ? function(target, type, capture){

        target[o + type] = undefined;
      } : (function(){

        return null;
      }),
      __gI : function(domEvent, type, target){

        if(!target){

          target = qx.bom.Event.getTarget(domEvent);
        };
        if(target && target.nodeType){

          qx.event.Registration.fireEvent(target, type || domEvent.type, type == g ? qx.event.type.MouseWheel : qx.event.type.Mouse, [domEvent, target, null, true, true]);
        };
        qx.event.Registration.fireEvent(this.__cw, f, qx.event.type.Data, [type || domEvent.type]);
      },
      __gJ : function(){

        var targets = [this.__cw, this.__a, this.__a.body];
        var target = this.__cw;
        var type = v;
        for(var i = 0;i < targets.length;i++){

          if(qx.bom.Event.supportsEvent(targets[i], g)){

            type = g;
            target = targets[i];
            break;
          };
        };
        return {
          type : type,
          target : target
        };
      },
      preventNextClick : function(){

        this.__gH = true;
      },
      _initButtonObserver : function(){

        this.__gC = qx.lang.Function.listener(this._onButtonEvent, this);
        var Event = qx.bom.Event;
        Event.addNativeListener(this.__a, b, this.__gC);
        Event.addNativeListener(this.__a, r, this.__gC);
        if(!(qx.event.handler.MouseEmulation.ON && qx.core.Environment.get(x) && qx.core.Environment.get(w))){

          Event.addNativeListener(this.__a, n, this.__gC);
        };
        Event.addNativeListener(this.__a, e, this.__gC);
        Event.addNativeListener(this.__a, s, this.__gC);
      },
      _initMoveObserver : function(){

        this.__gD = qx.lang.Function.listener(this._onMoveEvent, this);
        var Event = qx.bom.Event;
        Event.addNativeListener(this.__a, j, this.__gD);
        Event.addNativeListener(this.__a, l, this.__gD);
        Event.addNativeListener(this.__a, c, this.__gD);
      },
      _initWheelObserver : function(){

        this.__gE = qx.lang.Function.listener(this._onWheelEvent, this);
        var data = this.__gJ();
        qx.bom.Event.addNativeListener(data.target, data.type, this.__gE);
      },
      _stopButtonObserver : function(){

        var Event = qx.bom.Event;
        Event.removeNativeListener(this.__a, b, this.__gC);
        Event.removeNativeListener(this.__a, r, this.__gC);
        if(!(qx.event.handler.MouseEmulation.ON && qx.core.Environment.get(x) && qx.core.Environment.get(w))){

          Event.removeNativeListener(this.__a, n, this.__gC);
        };
        Event.removeNativeListener(this.__a, e, this.__gC);
        Event.removeNativeListener(this.__a, s, this.__gC);
      },
      _stopMoveObserver : function(){

        var Event = qx.bom.Event;
        Event.removeNativeListener(this.__a, j, this.__gD);
        Event.removeNativeListener(this.__a, l, this.__gD);
        Event.removeNativeListener(this.__a, c, this.__gD);
      },
      _stopWheelObserver : function(){

        var data = this.__gJ();
        qx.bom.Event.removeNativeListener(data.target, data.type, this.__gE);
      },
      _onMoveEvent : qx.event.GlobalError.observeMethod(function(domEvent){

        this.__gI(domEvent);
      }),
      _onButtonEvent : qx.event.GlobalError.observeMethod(function(domEvent){

        var type = domEvent.type;
        var target = qx.bom.Event.getTarget(domEvent);
        if(type == n && this.__gH){

          delete this.__gH;
          return;
        };
        if(qx.core.Environment.get(q) == d || qx.core.Environment.get(q) == u){

          if(target && target.nodeType == 3){

            target = target.parentNode;
          };
        };
        var isDrag = qx.event.handler.DragDrop && this.__dh.getHandler(qx.event.handler.DragDrop).isSessionActive();
        if(isDrag && type == n){

          return;
        };
        if(this.__gK){

          this.__gK(domEvent, type, target);
        };
        if(this.__gM){

          this.__gM(domEvent, type, target);
        };
        this.__gI(domEvent, type, target);
        if(this.__gL){

          this.__gL(domEvent, type, target);
        };
        if(this.__gN && !isDrag){

          this.__gN(domEvent, type, target);
        };
        this.__gF = type;
      }),
      _onWheelEvent : qx.event.GlobalError.observeMethod(function(domEvent){

        this.__gI(domEvent, g);
      }),
      __gK : qx.core.Environment.select(q, {
        "webkit" : function(domEvent, type, target){

          if(parseFloat(qx.core.Environment.get(p)) < 530){

            if(type == s){

              this.__gI(domEvent, r, target);
            };
          };
        },
        "default" : null
      }),
      __gL : qx.core.Environment.select(q, {
        "opera" : function(domEvent, type, target){

          if(type == r && domEvent.button == 2){

            this.__gI(domEvent, s, target);
          };
        },
        "default" : null
      }),
      __gM : qx.core.Environment.select(q, {
        "mshtml" : function(domEvent, type, target){

          if(domEvent.target !== undefined){

            return;
          };
          if(type == r && this.__gF == n){

            this.__gI(domEvent, b, target);
          } else if(type == e){

            this.__gI(domEvent, n, target);
          };
        },
        "default" : null
      }),
      __gN : qx.core.Environment.select(q, {
        "mshtml" : null,
        "default" : function(domEvent, type, target){

          switch(type){case b:
          this.__gG = target;
          break;case r:
          if(target !== this.__gG){

            var commonParent = qx.dom.Hierarchy.getCommonParent(target, this.__gG);
            if(commonParent){

              this.__gI(domEvent, n, commonParent);
            };
          };};
        }
      })
    },
    destruct : function(){

      if(!(qx.core.Environment.get(h) && qx.event.handler.MouseEmulation.ON)){

        this._stopButtonObserver();
        this._stopMoveObserver();
        this._stopWheelObserver();
      };
      this.__dh = this.__cw = this.__a = this.__gG = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "touchmove",b = "os.name",c = "mouseup",d = "mousedown",f = "touchend",g = "win",h = "none",i = "qx.event.handler.MouseEmulation",j = "event.mspointer",k = "qx.emulatemouse",l = "longtap",m = "click",n = "event.touch",o = "touchstart",p = "contextmenu",q = "tap",r = "device.touch",s = "mousewheel",t = "touch",u = "mousemove";
  qx.Class.define(i, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__cw = manager.getWindow();
      this.__a = this.__cw.document;
      if(qx.event.handler.MouseEmulation.ON){

        this._initObserver();
        document.documentElement.style.msTouchAction = h;
      };
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_FIRST,
      SUPPORTED_TYPES : {
        mousedown : 1,
        mouseup : 1,
        mousemove : 1,
        click : 1,
        contextmenu : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE + qx.event.IEventHandler.TARGET_DOCUMENT + qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true,
      ON : qx.core.Environment.get(k) && ((qx.core.Environment.get(j) && qx.core.Environment.get(r)) || (qx.core.Environment.get(n) && qx.core.Environment.get(b) !== g))
    },
    members : {
      __dh : null,
      __cw : null,
      __a : null,
      __gO : null,
      __gP : null,
      __gQ : null,
      __gR : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      __gI : function(evt, type, target){

        var mouseEvent = type == s ? new qx.event.type.MouseWheel() : new qx.event.type.Mouse();
        mouseEvent.init(evt, target, null, true, true);
        mouseEvent.setType(type);
        return qx.event.Registration.getManager(target).dispatchEvent(target, mouseEvent);
      },
      __gS : function(deltaX, deltaY, finger, target){

        var wheelEvent = this.__gY(target, finger);
        wheelEvent.wheelDelta = deltaX;
        wheelEvent.wheelDeltaY = deltaY;
        wheelEvent.wheelDeltaX = deltaX;
        this.__gI(wheelEvent, s, target);
      },
      __gT : function(deltaX, deltaY, finger, target, time){

        this.__gQ = null;
        this.__gR = null;
        if(deltaX == 0 && deltaY == 0){

          return;
        };
        var change = parseInt((time || 20) / 10);
        if(deltaX > 0){

          deltaX = Math.max(0, deltaX - change);
        } else {

          deltaX = Math.min(0, deltaX + change);
        };
        if(deltaY > 0){

          deltaY = Math.max(0, deltaY - change);
        } else {

          deltaY = Math.min(0, deltaY + change);
        };
        var start = +(new Date());
        this.__gR = qx.bom.AnimationFrame.request(qx.lang.Function.bind(function(deltaX, deltaY, finger, target, time){

          this.__gT(deltaX, deltaY, finger, target, time - start);
        }, this, deltaX, deltaY, finger, target));
        this.__gS(deltaX, deltaY, finger, target);
      },
      __gU : function(nativeEvent){

        var endPos = {
          x : nativeEvent.screenX,
          y : nativeEvent.screenY
        };
        var moved = false;
        var offset = 20;
        if(Math.abs(endPos.x - this.__gO.x) > offset){

          moved = true;
        };
        if(Math.abs(endPos.y - this.__gO.y) > offset){

          moved = true;
        };
        return moved;
      },
      _initObserver : function(){

        qx.event.Registration.addListener(this.__a, o, this.__ep, this);
        qx.event.Registration.addListener(this.__a, a, this.__es, this);
        qx.event.Registration.addListener(this.__a, f, this.__er, this);
        qx.event.Registration.addListener(this.__a, q, this.__gW, this);
        qx.event.Registration.addListener(this.__a, l, this.__gX, this);
        qx.bom.Event.addNativeListener(this.__cw, a, this.__gV);
      },
      _stopObserver : function(){

        qx.event.Registration.removeListener(this.__a, o, this.__ep, this);
        qx.event.Registration.removeListener(this.__a, a, this.__es, this);
        qx.event.Registration.removeListener(this.__a, f, this.__er, this);
        qx.event.Registration.removeListener(this.__a, q, this.__gW, this);
        qx.event.Registration.removeListener(this.__a, l, this.__gX, this);
        qx.bom.Event.removeNativeListener(this.__cw, a, this.__gV);
      },
      __gV : function(e){

        var node = e.target;
        while(node){

          if(node.style && node.style.WebkitOverflowScrolling == t){

            return;
          };
          node = node.parentNode;
        };
        e.preventDefault();
      },
      __ep : function(e){

        var target = e.getTarget();
        var nativeEvent = this.__gY(target, e.getAllTouches()[0]);
        if(qx.core.Environment.get(n)){

          if(!this.__gI(nativeEvent, d, target)){

            e.preventDefault();
          };
        };
        this.__gP = {
          x : nativeEvent.screenX,
          y : nativeEvent.screenY
        };
        this.__gO = {
          x : nativeEvent.screenX,
          y : nativeEvent.screenY
        };
        if(this.__gR && window.cancelAnimationFrame){

          window.cancelAnimationFrame(this.__gR);
          this.__gR = null;
        };
      },
      __es : function(e){

        var target = e.getTarget();
        var nativeEvent = this.__gY(target, e.getChangedTargetTouches()[0]);
        if(qx.core.Environment.get(n)){

          if(!this.__gI(nativeEvent, u, target)){

            e.preventDefault();
          };
        };
        var deltaY = -parseInt(this.__gP.y - nativeEvent.screenY);
        var deltaX = -parseInt(this.__gP.x - nativeEvent.screenX);
        this.__gP = {
          x : nativeEvent.screenX,
          y : nativeEvent.screenY
        };
        if(e.getNativeEvent().pointerType != 4){

          var finger = e.getChangedTargetTouches()[0];
          this.__gS(deltaX, deltaY, finger, target);
          if(this.__gQ){

            clearTimeout(this.__gQ);
            this.__gQ = null;
          };
          this.__gQ = setTimeout(qx.lang.Function.bind(function(deltaX, deltaY, finger, target){

            this.__gT(deltaX, deltaY, finger, target);
          }, this, deltaX, deltaY, finger, target), 100);
        };
      },
      __er : function(e){

        var target = e.getTarget();
        var nativeEvent = this.__gY(target, e.getChangedTargetTouches()[0]);
        if(qx.core.Environment.get(n)){

          if(!this.__gI(nativeEvent, c, target)){

            e.preventDefault();
          };
        };
      },
      __gW : function(e){

        var target = e.getTarget();
        var nativeEvent = this.__gY(target, e.getChangedTargetTouches()[0]);
        if(!this.__gU(nativeEvent)){

          this.__gI(nativeEvent, m, target);
        };
      },
      __gX : function(e){

        var target = e.getTarget();
        var nativeEvent = this.__gY(target, e.getChangedTargetTouches()[0]);
        this.__gI(nativeEvent, p, target);
      },
      __gY : function(target, finger){

        var nativeEvent = {
        };
        nativeEvent.button = 0;
        nativeEvent.wheelDelta = 0;
        nativeEvent.wheelDeltaX = 0;
        nativeEvent.wheelDeltaY = 0;
        nativeEvent.wheelX = 0;
        nativeEvent.wheelY = 0;
        nativeEvent.target = target;
        nativeEvent.clientX = finger.clientX;
        nativeEvent.clientY = finger.clientY;
        nativeEvent.pageX = finger.pageX;
        nativeEvent.pageY = finger.pageY;
        nativeEvent.screenX = finger.screenX;
        nativeEvent.screenY = finger.screenY;
        nativeEvent.shiftKey = false;
        nativeEvent.ctrlKey = false;
        nativeEvent.altKey = false;
        nativeEvent.metaKey = false;
        return nativeEvent;
      }
    },
    destruct : function(){

      if(qx.event.handler.MouseEmulation.ON){

        this._stopObserver();
      };
      this.__dh = this.__cw = this.__a = null;
    },
    defer : function(statics){

      if(statics.ON){

        qx.event.Registration.addHandler(statics);
      };
    }
  });
})();
(function(){

  var a = "click",b = "middle",c = "none",d = "contextmenu",e = "qx.event.type.Mouse",f = "browser.documentmode",g = "left",h = "right",i = "browser.name",j = "ie";
  qx.Class.define(e, {
    extend : qx.event.type.Dom,
    members : {
      _cloneNativeEvent : function(nativeEvent, clone){

        var clone = qx.event.type.Dom.prototype._cloneNativeEvent.call(this, nativeEvent, clone);
        clone.button = nativeEvent.button;
        clone.clientX = Math.round(nativeEvent.clientX);
        clone.clientY = Math.round(nativeEvent.clientY);
        clone.pageX = Math.round(nativeEvent.pageX);
        clone.pageY = Math.round(nativeEvent.pageY);
        clone.screenX = Math.round(nativeEvent.screenX);
        clone.screenY = Math.round(nativeEvent.screenY);
        clone.wheelDelta = nativeEvent.wheelDelta;
        clone.wheelDeltaX = nativeEvent.wheelDeltaX;
        clone.wheelDeltaY = nativeEvent.wheelDeltaY;
        clone.detail = nativeEvent.detail;
        clone.axis = nativeEvent.axis;
        clone.wheelX = nativeEvent.wheelX;
        clone.wheelY = nativeEvent.wheelY;
        clone.HORIZONTAL_AXIS = nativeEvent.HORIZONTAL_AXIS;
        clone.srcElement = nativeEvent.srcElement;
        clone.target = nativeEvent.target;
        return clone;
      },
      __ha : {
        '0' : g,
        '2' : h,
        '1' : b
      },
      __hb : {
        '1' : g,
        '2' : h,
        '4' : b
      },
      stop : function(){

        this.stopPropagation();
      },
      getButton : function(){

        switch(this._type){case d:
        return h;case a:
        if(qx.core.Environment.get(i) === j && qx.core.Environment.get(f) < 9){

          return g;
        };default:
        if(this._native.target !== undefined){

          return this.__ha[this._native.button] || c;
        } else {

          return this.__hb[this._native.button] || c;
        };};
      },
      isLeftPressed : function(){

        return this.getButton() === g;
      },
      isMiddlePressed : function(){

        return this.getButton() === b;
      },
      isRightPressed : function(){

        return this.getButton() === h;
      },
      getRelatedTarget : function(){

        return this._relatedTarget;
      },
      getViewportLeft : function(){

        return Math.round(this._native.clientX);
      },
      getViewportTop : function(){

        return Math.round(this._native.clientY);
      },
      getDocumentLeft : function(){

        if(this._native.pageX !== undefined){

          return Math.round(this._native.pageX);
        } else {

          var win = qx.dom.Node.getWindow(this._native.srcElement);
          return Math.round(this._native.clientX) + qx.bom.Viewport.getScrollLeft(win);
        };
      },
      getDocumentTop : function(){

        if(this._native.pageY !== undefined){

          return Math.round(this._native.pageY);
        } else {

          var win = qx.dom.Node.getWindow(this._native.srcElement);
          return Math.round(this._native.clientY) + qx.bom.Viewport.getScrollTop(win);
        };
      },
      getScreenLeft : function(){

        return Math.round(this._native.screenX);
      },
      getScreenTop : function(){

        return Math.round(this._native.screenY);
      }
    }
  });
})();
(function(){

  var a = "engine.name",b = "x",c = "osx",d = "win",f = "os.name",g = "qx.dynamicmousewheel",h = "engine.version",i = "chrome",j = "qx.event.type.MouseWheel",k = "browser.name",l = "y";
  qx.Class.define(j, {
    extend : qx.event.type.Mouse,
    statics : {
      MAXSCROLL : null,
      MINSCROLL : null,
      FACTOR : 1
    },
    members : {
      stop : function(){

        this.stopPropagation();
        this.preventDefault();
      },
      __hc : function(delta){

        var absDelta = Math.abs(delta);
        if(qx.event.type.MouseWheel.MINSCROLL == null || qx.event.type.MouseWheel.MINSCROLL > absDelta){

          qx.event.type.MouseWheel.MINSCROLL = absDelta;
          this.__hd();
        };
        if(qx.event.type.MouseWheel.MAXSCROLL == null || qx.event.type.MouseWheel.MAXSCROLL < absDelta){

          qx.event.type.MouseWheel.MAXSCROLL = absDelta;
          this.__hd();
        };
        if(qx.event.type.MouseWheel.MAXSCROLL === absDelta && qx.event.type.MouseWheel.MINSCROLL === absDelta){

          return 2 * (delta / absDelta);
        };
        var range = qx.event.type.MouseWheel.MAXSCROLL - qx.event.type.MouseWheel.MINSCROLL;
        var ret = (delta / range) * Math.log(range) * qx.event.type.MouseWheel.FACTOR;
        return ret < 0 ? Math.min(ret, -1) : Math.max(ret, 1);
      },
      __hd : function(){

        var max = qx.event.type.MouseWheel.MAXSCROLL || 0;
        var min = qx.event.type.MouseWheel.MINSCROLL || max;
        if(max <= min){

          return;
        };
        var range = max - min;
        var maxRet = (max / range) * Math.log(range);
        if(maxRet == 0){

          maxRet = 1;
        };
        qx.event.type.MouseWheel.FACTOR = 6 / maxRet;
      },
      getWheelDelta : function(axis){

        var e = this._native;
        if(axis === undefined){

          if(delta === undefined){

            var delta = -e.wheelDelta;
            if(e.wheelDelta === undefined){

              delta = e.detail;
            };
          };
          return this.__he(delta);
        };
        if(axis === b){

          var x = 0;
          if(e.wheelDelta !== undefined){

            if(e.wheelDeltaX !== undefined){

              x = e.wheelDeltaX ? this.__he(-e.wheelDeltaX) : 0;
            };
          } else {

            if(e.axis && e.axis == e.HORIZONTAL_AXIS){

              x = this.__he(e.detail);
            };
          };
          return x;
        };
        if(axis === l){

          var y = 0;
          if(e.wheelDelta !== undefined){

            if(e.wheelDeltaY !== undefined){

              y = e.wheelDeltaY ? this.__he(-e.wheelDeltaY) : 0;
            } else {

              y = this.__he(-e.wheelDelta);
            };
          } else {

            if(!(e.axis && e.axis == e.HORIZONTAL_AXIS)){

              y = this.__he(e.detail);
            };
          };
          return y;
        };
        return 0;
      },
      __he : function(delta){

        if(qx.event.handler.MouseEmulation.ON){

          return delta;
        } else if(qx.core.Environment.get(g)){

          return this.__hc(delta);
        } else {

          var handler = qx.core.Environment.select(a, {
            "default" : function(){

              return delta / 40;
            },
            "gecko" : function(){

              return delta;
            },
            "webkit" : function(){

              if(qx.core.Environment.get(k) == i){

                if(qx.core.Environment.get(f) == c){

                  return delta / 60;
                } else {

                  return delta / 120;
                };
              } else {

                if(qx.core.Environment.get(f) == d){

                  var factor = 120;
                  if(parseFloat(qx.core.Environment.get(h)) == 533.16){

                    factor = 1200;
                  };
                } else {

                  factor = 40;
                  if(parseFloat(qx.core.Environment.get(h)) == 533.16 || parseFloat(qx.core.Environment.get(h)) == 533.17 || parseFloat(qx.core.Environment.get(h)) == 533.18){

                    factor = 1200;
                  };
                };
                return delta / factor;
              };
            }
          });
          return handler.call(this);
        };
      }
    }
  });
})();
(function(){

  var a = "qx.dom.Hierarchy",b = "previousSibling",c = "html.element.contains",d = "html.element.compareDocumentPosition",e = "nextSibling",f = "parentNode",g = "*";
  qx.Bootstrap.define(a, {
    statics : {
      getNodeIndex : function(node){

        var index = 0;
        while(node && (node = node.previousSibling)){

          index++;
        };
        return index;
      },
      getElementIndex : function(element){

        var index = 0;
        var type = qx.dom.Node.ELEMENT;
        while(element && (element = element.previousSibling)){

          if(element.nodeType == type){

            index++;
          };
        };
        return index;
      },
      getNextElementSibling : function(element){

        while(element && (element = element.nextSibling) && !qx.dom.Node.isElement(element)){

          continue;
        };
        return element || null;
      },
      getPreviousElementSibling : function(element){

        while(element && (element = element.previousSibling) && !qx.dom.Node.isElement(element)){

          continue;
        };
        return element || null;
      },
      contains : function(element, target){

        if(qx.core.Environment.get(c)){

          if(qx.dom.Node.isDocument(element)){

            var doc = qx.dom.Node.getDocument(target);
            return element && doc == element;
          } else if(qx.dom.Node.isDocument(target)){

            return false;
          } else {

            return element.contains(target);
          };
        } else if(qx.core.Environment.get(d)){

          return !!(element.compareDocumentPosition(target) & 16);
        } else {

          while(target){

            if(element == target){

              return true;
            };
            target = target.parentNode;
          };
          return false;
        };
      },
      isRendered : function(element){

        var doc = element.ownerDocument || element.document;
        if(qx.core.Environment.get(c)){

          if(!element.parentNode || !element.offsetParent){

            return false;
          };
          return doc.body.contains(element);
        } else if(qx.core.Environment.get(d)){

          return !!(doc.compareDocumentPosition(element) & 16);
        } else {

          while(element){

            if(element == doc.body){

              return true;
            };
            element = element.parentNode;
          };
          return false;
        };
      },
      isDescendantOf : function(element, ancestor){

        return this.contains(ancestor, element);
      },
      getCommonParent : function(element1, element2){

        if(element1 === element2){

          return element1;
        };
        if(qx.core.Environment.get(c)){

          while(element1 && qx.dom.Node.isElement(element1)){

            if(element1.contains(element2)){

              return element1;
            };
            element1 = element1.parentNode;
          };
          return null;
        } else {

          var known = [];
          while(element1 || element2){

            if(element1){

              if(qx.lang.Array.contains(known, element1)){

                return element1;
              };
              known.push(element1);
              element1 = element1.parentNode;
            };
            if(element2){

              if(qx.lang.Array.contains(known, element2)){

                return element2;
              };
              known.push(element2);
              element2 = element2.parentNode;
            };
          };
          return null;
        };
      },
      getAncestors : function(element){

        return this._recursivelyCollect(element, f);
      },
      getChildElements : function(element){

        element = element.firstChild;
        if(!element){

          return [];
        };
        var arr = this.getNextSiblings(element);
        if(element.nodeType === 1){

          arr.unshift(element);
        };
        return arr;
      },
      getDescendants : function(element){

        return qx.lang.Array.fromCollection(element.getElementsByTagName(g));
      },
      getFirstDescendant : function(element){

        element = element.firstChild;
        while(element && element.nodeType != 1){

          element = element.nextSibling;
        };
        return element;
      },
      getLastDescendant : function(element){

        element = element.lastChild;
        while(element && element.nodeType != 1){

          element = element.previousSibling;
        };
        return element;
      },
      getPreviousSiblings : function(element){

        return this._recursivelyCollect(element, b);
      },
      getNextSiblings : function(element){

        return this._recursivelyCollect(element, e);
      },
      _recursivelyCollect : function(element, property){

        var list = [];
        while(element = element[property]){

          if(element.nodeType == 1){

            list.push(element);
          };
        };
        return list;
      },
      getSiblings : function(element){

        return this.getPreviousSiblings(element).reverse().concat(this.getNextSiblings(element));
      },
      isEmpty : function(element){

        element = element.firstChild;
        while(element){

          if(element.nodeType === qx.dom.Node.ELEMENT || element.nodeType === qx.dom.Node.TEXT){

            return false;
          };
          element = element.nextSibling;
        };
        return true;
      },
      cleanWhitespace : function(element){

        var node = element.firstChild;
        while(node){

          var nextNode = node.nextSibling;
          if(node.nodeType == 3 && !/\S/.test(node.nodeValue)){

            element.removeChild(node);
          };
          node = nextNode;
        };
      }
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "keypress",d = "useraction",e = "win",f = "text",g = "keyinput",h = "os.name",i = "webkit",j = "input",k = "gecko",l = "off",m = "keydown",n = "autoComplete",o = "keyup",p = "qx.event.handler.Keyboard";
  qx.Class.define(p, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__cw = manager.getWindow();
      if((qx.core.Environment.get(b) == k)){

        this.__a = this.__cw;
      } else {

        this.__a = this.__cw.document.documentElement;
      };
      this.__hf = {
      };
      this._initKeyObserver();
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        keyup : 1,
        keydown : 1,
        keypress : 1,
        keyinput : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      __hg : null,
      __dh : null,
      __cw : null,
      __a : null,
      __hf : null,
      __hh : null,
      __hi : null,
      __hj : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      _fireInputEvent : function(domEvent, charCode){

        var target = this.__hk();
        if(target && target.offsetWidth != 0){

          var event = qx.event.Registration.createEvent(g, qx.event.type.KeyInput, [domEvent, target, charCode]);
          this.__dh.dispatchEvent(target, event);
        };
        if(this.__cw){

          qx.event.Registration.fireEvent(this.__cw, d, qx.event.type.Data, [g]);
        };
      },
      _fireSequenceEvent : function(domEvent, type, keyIdentifier){

        var target = this.__hk();
        var keyCode = domEvent.keyCode;
        var event = qx.event.Registration.createEvent(type, qx.event.type.KeySequence, [domEvent, target, keyIdentifier]);
        this.__dh.dispatchEvent(target, event);
        if(qx.core.Environment.get(b) == a || qx.core.Environment.get(b) == i){

          if(type == m && event.getDefaultPrevented()){

            if(!qx.event.util.Keyboard.isNonPrintableKeyCode(keyCode) && !this._emulateKeyPress[keyCode]){

              this._fireSequenceEvent(domEvent, c, keyIdentifier);
            };
          };
        };
        if(this.__cw){

          qx.event.Registration.fireEvent(this.__cw, d, qx.event.type.Data, [type]);
        };
      },
      __hk : function(){

        var focusHandler = this.__dh.getHandler(qx.event.handler.Focus);
        var target = focusHandler.getActive();
        if(!target || target.offsetWidth == 0){

          target = focusHandler.getFocus();
        };
        if(!target || target.offsetWidth == 0){

          target = this.__dh.getWindow().document.body;
        };
        return target;
      },
      _initKeyObserver : function(){

        this.__hg = qx.lang.Function.listener(this.__hl, this);
        this.__hj = qx.lang.Function.listener(this.__hn, this);
        var Event = qx.bom.Event;
        Event.addNativeListener(this.__a, o, this.__hg);
        Event.addNativeListener(this.__a, m, this.__hg);
        Event.addNativeListener(this.__a, c, this.__hj);
      },
      _stopKeyObserver : function(){

        var Event = qx.bom.Event;
        Event.removeNativeListener(this.__a, o, this.__hg);
        Event.removeNativeListener(this.__a, m, this.__hg);
        Event.removeNativeListener(this.__a, c, this.__hj);
        for(var key in (this.__hi || {
        })){

          var listener = this.__hi[key];
          Event.removeNativeListener(listener.target, c, listener.callback);
        };
        delete (this.__hi);
      },
      __hl : qx.event.GlobalError.observeMethod(qx.core.Environment.select(b, {
        "mshtml" : function(domEvent){

          domEvent = window.event || domEvent;
          var keyCode = domEvent.keyCode;
          var charCode = 0;
          var type = domEvent.type;
          if(!(this.__hf[keyCode] == m && type == m)){

            this._idealKeyHandler(keyCode, charCode, type, domEvent);
          };
          if(type == m){

            if(qx.event.util.Keyboard.isNonPrintableKeyCode(keyCode) || this._emulateKeyPress[keyCode]){

              this._idealKeyHandler(keyCode, charCode, c, domEvent);
            };
          };
          this.__hf[keyCode] = type;
        },
        "gecko" : function(domEvent){

          var charCode = 0;
          var keyCode = domEvent.keyCode;
          var type = domEvent.type;
          var kbUtil = qx.event.util.Keyboard;
          if(qx.core.Environment.get(h) == e){

            var keyIdentifier = keyCode ? kbUtil.keyCodeToIdentifier(keyCode) : kbUtil.charCodeToIdentifier(charCode);
            if(!(this.__hf[keyIdentifier] == m && type == m)){

              this._idealKeyHandler(keyCode, charCode, type, domEvent);
            };
            this.__hf[keyIdentifier] = type;
          } else {

            this._idealKeyHandler(keyCode, charCode, type, domEvent);
          };
          this.__hm(domEvent.target, type, keyCode);
        },
        "webkit" : function(domEvent){

          var keyCode = 0;
          var charCode = 0;
          var type = domEvent.type;
          keyCode = domEvent.keyCode;
          this._idealKeyHandler(keyCode, charCode, type, domEvent);
          if(type == m){

            if(qx.event.util.Keyboard.isNonPrintableKeyCode(keyCode) || this._emulateKeyPress[keyCode]){

              this._idealKeyHandler(keyCode, charCode, c, domEvent);
            };
          };
          this.__hf[keyCode] = type;
        },
        "opera" : function(domEvent){

          this.__hh = domEvent.keyCode;
          this._idealKeyHandler(domEvent.keyCode, 0, domEvent.type, domEvent);
        }
      })),
      __hm : qx.core.Environment.select(b, {
        "gecko" : function(target, type, keyCode){

          if(type === m && (keyCode == 33 || keyCode == 34 || keyCode == 38 || keyCode == 40) && target.type == f && target.tagName.toLowerCase() === j && target.getAttribute(n) !== l){

            if(!this.__hi){

              this.__hi = {
              };
            };
            var hash = qx.core.ObjectRegistry.toHashCode(target);
            if(this.__hi[hash]){

              return;
            };
            var self = this;
            this.__hi[hash] = {
              target : target,
              callback : function(domEvent){

                qx.bom.Event.stopPropagation(domEvent);
                self.__hn(domEvent);
              }
            };
            var listener = qx.event.GlobalError.observeMethod(this.__hi[hash].callback);
            qx.bom.Event.addNativeListener(target, c, listener);
          };
        },
        "default" : null
      }),
      __hn : qx.event.GlobalError.observeMethod(qx.core.Environment.select(b, {
        "mshtml" : function(domEvent){

          domEvent = window.event || domEvent;
          if(this._charCode2KeyCode[domEvent.keyCode]){

            this._idealKeyHandler(this._charCode2KeyCode[domEvent.keyCode], 0, domEvent.type, domEvent);
          } else {

            this._idealKeyHandler(0, domEvent.keyCode, domEvent.type, domEvent);
          };
        },
        "gecko" : function(domEvent){

          var charCode = domEvent.charCode;
          var type = domEvent.type;
          this._idealKeyHandler(domEvent.keyCode, charCode, type, domEvent);
        },
        "webkit" : function(domEvent){

          if(this._charCode2KeyCode[domEvent.keyCode]){

            this._idealKeyHandler(this._charCode2KeyCode[domEvent.keyCode], 0, domEvent.type, domEvent);
          } else {

            this._idealKeyHandler(0, domEvent.keyCode, domEvent.type, domEvent);
          };
        },
        "opera" : function(domEvent){

          var keyCode = domEvent.keyCode;
          var type = domEvent.type;
          if(keyCode != this.__hh){

            this._idealKeyHandler(0, this.__hh, type, domEvent);
          } else {

            if(qx.event.util.Keyboard.keyCodeToIdentifierMap[domEvent.keyCode]){

              this._idealKeyHandler(domEvent.keyCode, 0, domEvent.type, domEvent);
            } else {

              this._idealKeyHandler(0, domEvent.keyCode, domEvent.type, domEvent);
            };
          };
        }
      })),
      _idealKeyHandler : function(keyCode, charCode, eventType, domEvent){

        var keyIdentifier;
        if(keyCode || (!keyCode && !charCode)){

          keyIdentifier = qx.event.util.Keyboard.keyCodeToIdentifier(keyCode);
          this._fireSequenceEvent(domEvent, eventType, keyIdentifier);
        } else {

          keyIdentifier = qx.event.util.Keyboard.charCodeToIdentifier(charCode);
          this._fireSequenceEvent(domEvent, c, keyIdentifier);
          this._fireInputEvent(domEvent, charCode);
        };
      },
      _emulateKeyPress : qx.core.Environment.select(b, {
        "mshtml" : {
          '8' : true,
          '9' : true
        },
        "webkit" : {
          '8' : true,
          '9' : true,
          '27' : true
        },
        "default" : {
        }
      }),
      _identifierToKeyCode : function(keyIdentifier){

        return qx.event.util.Keyboard.identifierToKeyCodeMap[keyIdentifier] || keyIdentifier.charCodeAt(0);
      }
    },
    destruct : function(){

      this._stopKeyObserver();
      this.__hh = this.__dh = this.__cw = this.__a = this.__hf = null;
    },
    defer : function(statics, members){

      qx.event.Registration.addHandler(statics);
      if((qx.core.Environment.get(b) == a) || qx.core.Environment.get(b) == i){

        members._charCode2KeyCode = {
          '13' : 13,
          '27' : 27
        };
      };
    }
  });
})();
(function(){

  var a = "qx.event.type.KeyInput";
  qx.Class.define(a, {
    extend : qx.event.type.Dom,
    members : {
      init : function(domEvent, target, charCode){

        qx.event.type.Dom.prototype.init.call(this, domEvent, target, null, true, true);
        this._charCode = charCode;
        return this;
      },
      clone : function(embryo){

        var clone = qx.event.type.Dom.prototype.clone.call(this, embryo);
        clone._charCode = this._charCode;
        return clone;
      },
      getCharCode : function(){

        return this._charCode;
      },
      getChar : function(){

        return String.fromCharCode(this._charCode);
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.KeySequence";
  qx.Class.define(a, {
    extend : qx.event.type.Dom,
    members : {
      init : function(domEvent, target, identifier){

        qx.event.type.Dom.prototype.init.call(this, domEvent, target, null, true, true);
        this._keyCode = domEvent.keyCode;
        this._identifier = identifier;
        return this;
      },
      clone : function(embryo){

        var clone = qx.event.type.Dom.prototype.clone.call(this, embryo);
        clone._keyCode = this._keyCode;
        clone._identifier = this._identifier;
        return clone;
      },
      getKeyIdentifier : function(){

        return this._identifier;
      },
      getKeyCode : function(){

        return this._keyCode;
      },
      isPrintable : function(){

        return qx.event.util.Keyboard.isPrintableKeyIdentifier(this._identifier);
      }
    }
  });
})();
(function(){

  var a = "-",b = "PageUp",c = "Escape",d = "Enter",e = "+",f = "PrintScreen",g = "os.name",h = "7",i = "A",j = "Space",k = "Left",l = "5",m = "F5",n = "Down",o = "Up",p = "3",q = "Meta",r = "F11",s = "0",t = "F6",u = "PageDown",v = "osx",w = "CapsLock",x = "Insert",y = "F8",z = "Scroll",A = "Control",B = "Tab",C = "Shift",D = "End",E = "Pause",F = "Unidentified",G = "/",H = "8",I = "Z",J = "*",K = "cmd",L = "F1",M = "F4",N = "Home",O = "qx.event.util.Keyboard",P = "F2",Q = "6",R = "F7",S = "Apps",T = "4",U = "F12",V = "Alt",W = "2",X = "NumLock",Y = "Delete",ba = "1",bb = "Win",bc = "Backspace",bd = "F9",be = "F10",bf = "Right",bg = "F3",bh = "9",bi = ",";
  qx.Bootstrap.define(O, {
    statics : {
      specialCharCodeMap : {
        '8' : bc,
        '9' : B,
        '13' : d,
        '27' : c,
        '32' : j
      },
      numpadToCharCode : {
        '96' : s.charCodeAt(0),
        '97' : ba.charCodeAt(0),
        '98' : W.charCodeAt(0),
        '99' : p.charCodeAt(0),
        '100' : T.charCodeAt(0),
        '101' : l.charCodeAt(0),
        '102' : Q.charCodeAt(0),
        '103' : h.charCodeAt(0),
        '104' : H.charCodeAt(0),
        '105' : bh.charCodeAt(0),
        '106' : J.charCodeAt(0),
        '107' : e.charCodeAt(0),
        '109' : a.charCodeAt(0),
        '110' : bi.charCodeAt(0),
        '111' : G.charCodeAt(0)
      },
      keyCodeToIdentifierMap : {
        '16' : C,
        '17' : A,
        '18' : V,
        '20' : w,
        '224' : q,
        '37' : k,
        '38' : o,
        '39' : bf,
        '40' : n,
        '33' : b,
        '34' : u,
        '35' : D,
        '36' : N,
        '45' : x,
        '46' : Y,
        '112' : L,
        '113' : P,
        '114' : bg,
        '115' : M,
        '116' : m,
        '117' : t,
        '118' : R,
        '119' : y,
        '120' : bd,
        '121' : be,
        '122' : r,
        '123' : U,
        '144' : X,
        '44' : f,
        '145' : z,
        '19' : E,
        '91' : qx.core.Environment.get(g) == v ? K : bb,
        '92' : bb,
        '93' : qx.core.Environment.get(g) == v ? K : S
      },
      charCodeA : i.charCodeAt(0),
      charCodeZ : I.charCodeAt(0),
      charCode0 : s.charCodeAt(0),
      charCode9 : bh.charCodeAt(0),
      keyCodeToIdentifier : function(keyCode){

        if(this.isIdentifiableKeyCode(keyCode)){

          var numPadKeyCode = this.numpadToCharCode[keyCode];
          if(numPadKeyCode){

            return String.fromCharCode(numPadKeyCode);
          };
          return (this.keyCodeToIdentifierMap[keyCode] || this.specialCharCodeMap[keyCode] || String.fromCharCode(keyCode));
        } else {

          return F;
        };
      },
      charCodeToIdentifier : function(charCode){

        return this.specialCharCodeMap[charCode] || String.fromCharCode(charCode).toUpperCase();
      },
      isIdentifiableKeyCode : function(keyCode){

        if(keyCode >= this.charCodeA && keyCode <= this.charCodeZ){

          return true;
        };
        if(keyCode >= this.charCode0 && keyCode <= this.charCode9){

          return true;
        };
        if(this.specialCharCodeMap[keyCode]){

          return true;
        };
        if(this.numpadToCharCode[keyCode]){

          return true;
        };
        if(this.isNonPrintableKeyCode(keyCode)){

          return true;
        };
        return false;
      },
      isNonPrintableKeyCode : function(keyCode){

        return this.keyCodeToIdentifierMap[keyCode] ? true : false;
      },
      isValidKeyIdentifier : function(keyIdentifier){

        if(this.identifierToKeyCodeMap[keyIdentifier]){

          return true;
        };
        if(keyIdentifier.length != 1){

          return false;
        };
        if(keyIdentifier >= s && keyIdentifier <= bh){

          return true;
        };
        if(keyIdentifier >= i && keyIdentifier <= I){

          return true;
        };
        switch(keyIdentifier){case e:case a:case J:case G:
        return true;default:
        return false;};
      },
      isPrintableKeyIdentifier : function(keyIdentifier){

        if(keyIdentifier === j){

          return true;
        } else {

          return this.identifierToKeyCodeMap[keyIdentifier] ? false : true;
        };
      }
    },
    defer : function(statics, members){

      if(!statics.identifierToKeyCodeMap){

        statics.identifierToKeyCodeMap = {
        };
        for(var key in statics.keyCodeToIdentifierMap){

          statics.identifierToKeyCodeMap[statics.keyCodeToIdentifierMap[key]] = parseInt(key, 10);
        };
        for(var key in statics.specialCharCodeMap){

          statics.identifierToKeyCodeMap[statics.specialCharCodeMap[key]] = parseInt(key, 10);
        };
      };
    }
  });
})();
(function(){

  var a = "selectstart",b = "blur",c = "mousedown",d = "focus",e = "qx.event.handler.Focus",f = "_applyFocus",g = "DOMFocusIn",h = "deactivate",i = "textarea",j = "touchend",k = "_applyActive",l = 'character',m = "input",n = "event.touch",o = "",p = "qxSelectable",q = "tabIndex",r = "off",s = "touchstart",t = "activate",u = "focusin",v = "mshtml",w = "engine.name",x = "mouseup",y = "DOMFocusOut",z = "focusout",A = "qxKeepFocus",B = "on",C = "qxKeepActive",D = "draggesture";
  qx.Class.define(e, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this._manager = manager;
      this._window = manager.getWindow();
      this._document = this._window.document;
      this._root = this._document.documentElement;
      this._body = this._document.body;
      var useTouch = qx.core.Environment.get(n) && qx.event.handler.MouseEmulation.ON;
      this.__ho = useTouch ? s : c;
      this.__hp = useTouch ? j : x;
      this._initObserver();
    },
    properties : {
      active : {
        apply : k,
        nullable : true
      },
      focus : {
        apply : f,
        nullable : true
      }
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        focus : 1,
        blur : 1,
        focusin : 1,
        focusout : 1,
        activate : 1,
        deactivate : 1
      },
      IGNORE_CAN_HANDLE : true,
      FOCUSABLE_ELEMENTS : qx.core.Environment.select(w, {
        "mshtml|gecko" : {
          a : 1,
          body : 1,
          button : 1,
          frame : 1,
          iframe : 1,
          img : 1,
          input : 1,
          object : 1,
          select : 1,
          textarea : 1
        },
        "opera|webkit" : {
          button : 1,
          input : 1,
          select : 1,
          textarea : 1
        }
      })
    },
    members : {
      __hq : null,
      __hr : null,
      __hs : null,
      __ht : null,
      __hu : null,
      __hv : null,
      __hw : null,
      __hx : null,
      __hy : null,
      __hz : null,
      __ho : o,
      __hp : o,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      focus : function(element){

        if((qx.core.Environment.get(w) == v)){

          window.setTimeout(function(){

            try{

              element.focus();
              var selection = qx.bom.Selection.get(element);
              if(selection.length == 0){

                var textRange = element.createTextRange();
                textRange.moveStart(l, element.value.length);
                textRange.collapse();
                textRange.select();
              };
            } catch(ex) {
            };
          }, 0);
        } else {

          try{

            element.focus();
          } catch(ex) {
          };
        };
        this.setFocus(element);
        this.setActive(element);
      },
      activate : function(element){

        this.setActive(element);
      },
      blur : function(element){

        try{

          element.blur();
        } catch(ex) {
        };
        if(this.getActive() === element){

          this.resetActive();
        };
        if(this.getFocus() === element){

          this.resetFocus();
        };
      },
      deactivate : function(element){

        if(this.getActive() === element){

          this.resetActive();
        };
      },
      tryActivate : function(element){

        var active = this.__hN(element);
        if(active){

          this.setActive(active);
        };
      },
      __gI : function(target, related, type, bubbles){

        var Registration = qx.event.Registration;
        var evt = Registration.createEvent(type, qx.event.type.Focus, [target, related, bubbles]);
        Registration.dispatchEvent(target, evt);
      },
      _windowFocused : true,
      __hA : function(){

        if(this._windowFocused){

          this._windowFocused = false;
          this.__gI(this._window, null, b, false);
        };
      },
      __hB : function(){

        if(!this._windowFocused){

          this._windowFocused = true;
          this.__gI(this._window, null, d, false);
        };
      },
      _initObserver : qx.core.Environment.select(w, {
        "gecko" : function(){

          this.__hq = qx.lang.Function.listener(this.__hH, this);
          this.__hr = qx.lang.Function.listener(this.__hI, this);
          this.__hs = qx.lang.Function.listener(this.__hG, this);
          this.__ht = qx.lang.Function.listener(this.__hF, this);
          this.__hu = qx.lang.Function.listener(this.__hC, this);
          qx.bom.Event.addNativeListener(this._document, this.__ho, this.__hq, true);
          qx.bom.Event.addNativeListener(this._document, this.__hp, this.__hr, true);
          qx.bom.Event.addNativeListener(this._window, d, this.__hs, true);
          qx.bom.Event.addNativeListener(this._window, b, this.__ht, true);
          qx.bom.Event.addNativeListener(this._window, D, this.__hu, true);
        },
        "mshtml" : function(){

          this.__hq = qx.lang.Function.listener(this.__hH, this);
          this.__hr = qx.lang.Function.listener(this.__hI, this);
          this.__hw = qx.lang.Function.listener(this.__hD, this);
          this.__hx = qx.lang.Function.listener(this.__hE, this);
          this.__hv = qx.lang.Function.listener(this.__hK, this);
          qx.bom.Event.addNativeListener(this._document, this.__ho, this.__hq);
          qx.bom.Event.addNativeListener(this._document, this.__hp, this.__hr);
          qx.bom.Event.addNativeListener(this._document, u, this.__hw);
          qx.bom.Event.addNativeListener(this._document, z, this.__hx);
          qx.bom.Event.addNativeListener(this._document, a, this.__hv);
        },
        "webkit" : function(){

          this.__hq = qx.lang.Function.listener(this.__hH, this);
          this.__hr = qx.lang.Function.listener(this.__hI, this);
          this.__hx = qx.lang.Function.listener(this.__hE, this);
          this.__hs = qx.lang.Function.listener(this.__hG, this);
          this.__ht = qx.lang.Function.listener(this.__hF, this);
          this.__hv = qx.lang.Function.listener(this.__hK, this);
          qx.bom.Event.addNativeListener(this._document, this.__ho, this.__hq, true);
          qx.bom.Event.addNativeListener(this._document, this.__hp, this.__hr, true);
          qx.bom.Event.addNativeListener(this._document, a, this.__hv, false);
          qx.bom.Event.addNativeListener(this._window, y, this.__hx, true);
          qx.bom.Event.addNativeListener(this._window, d, this.__hs, true);
          qx.bom.Event.addNativeListener(this._window, b, this.__ht, true);
        },
        "opera" : function(){

          this.__hq = qx.lang.Function.listener(this.__hH, this);
          this.__hr = qx.lang.Function.listener(this.__hI, this);
          this.__hw = qx.lang.Function.listener(this.__hD, this);
          this.__hx = qx.lang.Function.listener(this.__hE, this);
          qx.bom.Event.addNativeListener(this._document, this.__ho, this.__hq, true);
          qx.bom.Event.addNativeListener(this._document, this.__hp, this.__hr, true);
          qx.bom.Event.addNativeListener(this._window, g, this.__hw, true);
          qx.bom.Event.addNativeListener(this._window, y, this.__hx, true);
        }
      }),
      _stopObserver : qx.core.Environment.select(w, {
        "gecko" : function(){

          qx.bom.Event.removeNativeListener(this._document, this.__ho, this.__hq, true);
          qx.bom.Event.removeNativeListener(this._document, this.__hp, this.__hr, true);
          qx.bom.Event.removeNativeListener(this._window, d, this.__hs, true);
          qx.bom.Event.removeNativeListener(this._window, b, this.__ht, true);
          qx.bom.Event.removeNativeListener(this._window, D, this.__hu, true);
        },
        "mshtml" : function(){

          qx.bom.Event.removeNativeListener(this._document, this.__ho, this.__hq);
          qx.bom.Event.removeNativeListener(this._document, this.__hp, this.__hr);
          qx.bom.Event.removeNativeListener(this._document, u, this.__hw);
          qx.bom.Event.removeNativeListener(this._document, z, this.__hx);
          qx.bom.Event.removeNativeListener(this._document, a, this.__hv);
        },
        "webkit" : function(){

          qx.bom.Event.removeNativeListener(this._document, this.__ho, this.__hq, true);
          qx.bom.Event.removeNativeListener(this._document, this.__hp, this.__hr, true);
          qx.bom.Event.removeNativeListener(this._document, a, this.__hv, false);
          qx.bom.Event.removeNativeListener(this._window, y, this.__hx, true);
          qx.bom.Event.removeNativeListener(this._window, d, this.__hs, true);
          qx.bom.Event.removeNativeListener(this._window, b, this.__ht, true);
        },
        "opera" : function(){

          qx.bom.Event.removeNativeListener(this._document, this.__ho, this.__hq, true);
          qx.bom.Event.removeNativeListener(this._document, this.__hp, this.__hr, true);
          qx.bom.Event.removeNativeListener(this._window, g, this.__hw, true);
          qx.bom.Event.removeNativeListener(this._window, y, this.__hx, true);
        }
      }),
      __hC : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "gecko" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(!this.__hO(target)){

            qx.bom.Event.preventDefault(domEvent);
          };
        },
        "default" : null
      })),
      __hD : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "mshtml" : function(domEvent){

          this.__hB();
          var target = qx.bom.Event.getTarget(domEvent);
          var focusTarget = this.__hM(target);
          if(focusTarget){

            this.setFocus(focusTarget);
          };
          this.tryActivate(target);
        },
        "opera" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target == this._document || target == this._window){

            this.__hB();
            if(this.__hy){

              this.setFocus(this.__hy);
              delete this.__hy;
            };
            if(this.__hz){

              this.setActive(this.__hz);
              delete this.__hz;
            };
          } else {

            this.setFocus(target);
            this.tryActivate(target);
            if(!this.__hO(target)){

              target.selectionStart = 0;
              target.selectionEnd = 0;
            };
          };
        },
        "default" : null
      })),
      __hE : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "mshtml" : function(domEvent){

          var relatedTarget = qx.bom.Event.getRelatedTarget(domEvent);
          if(relatedTarget == null){

            this.__hA();
            this.resetFocus();
            this.resetActive();
          };
        },
        "webkit" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target === this.getFocus()){

            this.resetFocus();
          };
          if(target === this.getActive()){

            this.resetActive();
          };
        },
        "opera" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target == this._document){

            this.__hA();
            this.__hy = this.getFocus();
            this.__hz = this.getActive();
            this.resetFocus();
            this.resetActive();
          } else {

            if(target === this.getFocus()){

              this.resetFocus();
            };
            if(target === this.getActive()){

              this.resetActive();
            };
          };
        },
        "default" : null
      })),
      __hF : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "gecko" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target === this._window || target === this._document){

            this.__hA();
            this.resetActive();
            this.resetFocus();
          };
        },
        "webkit" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target === this._window || target === this._document){

            this.__hA();
            this.__hy = this.getFocus();
            this.__hz = this.getActive();
            this.resetActive();
            this.resetFocus();
          };
        },
        "default" : null
      })),
      __hG : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "gecko" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target === this._window || target === this._document){

            this.__hB();
            target = this._body;
          };
          this.setFocus(target);
          this.tryActivate(target);
        },
        "webkit" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target === this._window || target === this._document){

            this.__hB();
            if(this.__hy){

              this.setFocus(this.__hy);
              delete this.__hy;
            };
            if(this.__hz){

              this.setActive(this.__hz);
              delete this.__hz;
            };
          } else {

            this.setFocus(target);
            this.tryActivate(target);
          };
        },
        "default" : null
      })),
      __hH : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "mshtml" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          var focusTarget = this.__hM(target);
          if(focusTarget){

            if(!this.__hO(target)){

              target.unselectable = B;
              try{

                document.selection.empty();
              } catch(ex) {
              };
              try{

                focusTarget.focus();
              } catch(ex) {
              };
            };
          } else {

            qx.bom.Event.preventDefault(domEvent);
            if(!this.__hO(target)){

              target.unselectable = B;
            };
          };
        },
        "webkit|gecko" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          var focusTarget = this.__hM(target);
          if(focusTarget){

            this.setFocus(focusTarget);
            if(qx.core.Environment.get(n) && qx.event.handler.MouseEmulation.ON){

              qx.event.GlobalError.observeMethod(window.setTimeout(function(){

                try{

                  if(document.activeElement == focusTarget){

                    focusTarget.blur();
                  };
                  focusTarget.focus();
                } catch(ex) {
                };
              }, 200));
            };
          } else {

            qx.bom.Event.preventDefault(domEvent);
          };
        },
        "opera" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          var focusTarget = this.__hM(target);
          if(!this.__hO(target)){

            qx.bom.Event.preventDefault(domEvent);
            if(focusTarget){

              var current = this.getFocus();
              if(current && current.selectionEnd){

                current.selectionStart = 0;
                current.selectionEnd = 0;
                current.blur();
              };
              if(focusTarget){

                this.setFocus(focusTarget);
              };
            };
          } else if(focusTarget){

            this.setFocus(focusTarget);
          };
        },
        "default" : null
      })),
      __hI : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "mshtml" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(target.unselectable){

            target.unselectable = r;
          };
          this.tryActivate(this.__hJ(target));
        },
        "gecko" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          while(target && target.offsetWidth === undefined){

            target = target.parentNode;
          };
          if(target){

            this.tryActivate(target);
          };
        },
        "webkit|opera" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          this.tryActivate(this.__hJ(target));
        },
        "default" : null
      })),
      __hJ : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "mshtml|webkit" : function(target){

          var focusedElement = this.getFocus();
          if(focusedElement && target != focusedElement && (focusedElement.nodeName.toLowerCase() === m || focusedElement.nodeName.toLowerCase() === i)){

            target = focusedElement;
          };
          return target;
        },
        "default" : function(target){

          return target;
        }
      })),
      __hK : qx.event.GlobalError.observeMethod(qx.core.Environment.select(w, {
        "mshtml|webkit" : function(domEvent){

          var target = qx.bom.Event.getTarget(domEvent);
          if(!this.__hO(target)){

            qx.bom.Event.preventDefault(domEvent);
          };
        },
        "default" : null
      })),
      __hL : function(el){

        var index = qx.bom.element.Attribute.get(el, q);
        if(index >= 1){

          return true;
        };
        var focusable = qx.event.handler.Focus.FOCUSABLE_ELEMENTS;
        if(index >= 0 && focusable[el.tagName]){

          return true;
        };
        return false;
      },
      __hM : function(el){

        while(el && el.nodeType === 1){

          if(el.getAttribute(A) == B){

            return null;
          };
          if(this.__hL(el)){

            return el;
          };
          el = el.parentNode;
        };
        return this._body;
      },
      __hN : function(el){

        var orig = el;
        while(el && el.nodeType === 1){

          if(el.getAttribute(C) == B){

            return null;
          };
          el = el.parentNode;
        };
        return orig;
      },
      __hO : function(node){

        while(node && node.nodeType === 1){

          var attr = node.getAttribute(p);
          if(attr != null){

            return attr === B;
          };
          node = node.parentNode;
        };
        return true;
      },
      _applyActive : function(value, old){

        if(old){

          this.__gI(old, value, h, true);
        };
        if(value){

          this.__gI(value, old, t, true);
        };
      },
      _applyFocus : function(value, old){

        if(old){

          this.__gI(old, value, z, true);
        };
        if(value){

          this.__gI(value, old, u, true);
        };
        if(old){

          this.__gI(old, value, b, false);
        };
        if(value){

          this.__gI(value, old, d, false);
        };
      }
    },
    destruct : function(){

      this._stopObserver();
      this._manager = this._window = this._document = this._root = this._body = this.__hP = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
      var focusable = statics.FOCUSABLE_ELEMENTS;
      for(var entry in focusable){

        focusable[entry.toUpperCase()] = 1;
      };
    }
  });
})();
(function(){

  var a = "engine.name",b = "qx.bom.Selection",c = "character",d = "button",e = 'character',f = "#text",g = "webkit",h = "input",i = "gecko",j = "EndToEnd",k = "opera",l = "StartToStart",m = "html.selection",n = "textarea",o = "body";
  qx.Bootstrap.define(b, {
    statics : {
      getSelectionObject : qx.core.Environment.select(m, {
        "selection" : function(documentNode){

          return documentNode.selection;
        },
        "default" : function(documentNode){

          return qx.dom.Node.getWindow(documentNode).getSelection();
        }
      }),
      get : qx.core.Environment.select(m, {
        "selection" : function(node){

          var rng = qx.bom.Range.get(qx.dom.Node.getDocument(node));
          return rng.text;
        },
        "default" : function(node){

          if(this.__hQ(node)){

            return node.value.substring(node.selectionStart, node.selectionEnd);
          } else {

            return this.getSelectionObject(qx.dom.Node.getDocument(node)).toString();
          };
        }
      }),
      getLength : qx.core.Environment.select(m, {
        "selection" : function(node){

          var selectedValue = this.get(node);
          var split = qx.util.StringSplit.split(selectedValue, /\r\n/);
          return selectedValue.length - (split.length - 1);
        },
        "default" : function(node){

          if(qx.core.Environment.get(a) == k){

            var selectedValue,selectedLength,split;
            if(this.__hQ(node)){

              var start = node.selectionStart;
              var end = node.selectionEnd;
              selectedValue = node.value.substring(start, end);
              selectedLength = end - start;
            } else {

              selectedValue = qx.bom.Selection.get(node);
              selectedLength = selectedValue.length;
            };
            split = qx.util.StringSplit.split(selectedValue, /\r\n/);
            return selectedLength - (split.length - 1);
          };
          if(this.__hQ(node)){

            return node.selectionEnd - node.selectionStart;
          } else {

            return this.get(node).length;
          };
        }
      }),
      getStart : qx.core.Environment.select(m, {
        "selection" : function(node){

          if(this.__hQ(node)){

            var documentRange = qx.bom.Range.get();
            if(!node.contains(documentRange.parentElement())){

              return -1;
            };
            var range = qx.bom.Range.get(node);
            var len = node.value.length;
            range.moveToBookmark(documentRange.getBookmark());
            range.moveEnd(e, len);
            return len - range.text.length;
          } else {

            var range = qx.bom.Range.get(node);
            var parentElement = range.parentElement();
            var elementRange = qx.bom.Range.get();
            try{

              elementRange.moveToElementText(parentElement);
            } catch(ex) {

              return 0;
            };
            var bodyRange = qx.bom.Range.get(qx.dom.Node.getBodyElement(node));
            bodyRange.setEndPoint(l, range);
            bodyRange.setEndPoint(j, elementRange);
            if(elementRange.compareEndPoints(l, bodyRange) == 0){

              return 0;
            };
            var moved;
            var steps = 0;
            while(true){

              moved = bodyRange.moveStart(c, -1);
              if(elementRange.compareEndPoints(l, bodyRange) == 0){

                break;
              };
              if(moved == 0){

                break;
              } else {

                steps++;
              };
            };
            return ++steps;
          };
        },
        "default" : function(node){

          if(qx.core.Environment.get(a) === i || qx.core.Environment.get(a) === g){

            if(this.__hQ(node)){

              return node.selectionStart;
            } else {

              var documentElement = qx.dom.Node.getDocument(node);
              var documentSelection = this.getSelectionObject(documentElement);
              if(documentSelection.anchorOffset < documentSelection.focusOffset){

                return documentSelection.anchorOffset;
              } else {

                return documentSelection.focusOffset;
              };
            };
          };
          if(this.__hQ(node)){

            return node.selectionStart;
          } else {

            return qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node)).anchorOffset;
          };
        }
      }),
      getEnd : qx.core.Environment.select(m, {
        "selection" : function(node){

          if(this.__hQ(node)){

            var documentRange = qx.bom.Range.get();
            if(!node.contains(documentRange.parentElement())){

              return -1;
            };
            var range = qx.bom.Range.get(node);
            var len = node.value.length;
            range.moveToBookmark(documentRange.getBookmark());
            range.moveStart(e, -len);
            return range.text.length;
          } else {

            var range = qx.bom.Range.get(node);
            var parentElement = range.parentElement();
            var elementRange = qx.bom.Range.get();
            try{

              elementRange.moveToElementText(parentElement);
            } catch(ex) {

              return 0;
            };
            var len = elementRange.text.length;
            var bodyRange = qx.bom.Range.get(qx.dom.Node.getBodyElement(node));
            bodyRange.setEndPoint(j, range);
            bodyRange.setEndPoint(l, elementRange);
            if(elementRange.compareEndPoints(j, bodyRange) == 0){

              return len - 1;
            };
            var moved;
            var steps = 0;
            while(true){

              moved = bodyRange.moveEnd(c, 1);
              if(elementRange.compareEndPoints(j, bodyRange) == 0){

                break;
              };
              if(moved == 0){

                break;
              } else {

                steps++;
              };
            };
            return len - (++steps);
          };
        },
        "default" : function(node){

          if(qx.core.Environment.get(a) === i || qx.core.Environment.get(a) === g){

            if(this.__hQ(node)){

              return node.selectionEnd;
            } else {

              var documentElement = qx.dom.Node.getDocument(node);
              var documentSelection = this.getSelectionObject(documentElement);
              if(documentSelection.focusOffset > documentSelection.anchorOffset){

                return documentSelection.focusOffset;
              } else {

                return documentSelection.anchorOffset;
              };
            };
          };
          if(this.__hQ(node)){

            return node.selectionEnd;
          } else {

            return qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node)).focusOffset;
          };
        }
      }),
      __hQ : function(node){

        return qx.dom.Node.isElement(node) && (node.nodeName.toLowerCase() == h || node.nodeName.toLowerCase() == n);
      },
      set : qx.core.Environment.select(m, {
        "selection" : function(node, start, end){

          var rng;
          if(qx.dom.Node.isDocument(node)){

            node = node.body;
          };
          if(qx.dom.Node.isElement(node) || qx.dom.Node.isText(node)){

            switch(node.nodeName.toLowerCase()){case h:case n:case d:
            if(end === undefined){

              end = node.value.length;
            };
            if(start >= 0 && start <= node.value.length && end >= 0 && end <= node.value.length){

              rng = qx.bom.Range.get(node);
              rng.collapse(true);
              rng.moveStart(c, start);
              rng.moveEnd(c, end - start);
              rng.select();
              return true;
            };
            break;case f:
            if(end === undefined){

              end = node.nodeValue.length;
            };
            if(start >= 0 && start <= node.nodeValue.length && end >= 0 && end <= node.nodeValue.length){

              rng = qx.bom.Range.get(qx.dom.Node.getBodyElement(node));
              rng.moveToElementText(node.parentNode);
              rng.collapse(true);
              rng.moveStart(c, start);
              rng.moveEnd(c, end - start);
              rng.select();
              return true;
            };
            break;default:
            if(end === undefined){

              end = node.childNodes.length - 1;
            };
            if(node.childNodes[start] && node.childNodes[end]){

              rng = qx.bom.Range.get(qx.dom.Node.getBodyElement(node));
              rng.moveToElementText(node.childNodes[start]);
              rng.collapse(true);
              var newRng = qx.bom.Range.get(qx.dom.Node.getBodyElement(node));
              newRng.moveToElementText(node.childNodes[end]);
              rng.setEndPoint(j, newRng);
              rng.select();
              return true;
            };};
          };
          return false;
        },
        "default" : function(node, start, end){

          var nodeName = node.nodeName.toLowerCase();
          if(qx.dom.Node.isElement(node) && (nodeName == h || nodeName == n)){

            if(end === undefined){

              end = node.value.length;
            };
            if(start >= 0 && start <= node.value.length && end >= 0 && end <= node.value.length){

              node.focus();
              node.select();
              node.setSelectionRange(start, end);
              return true;
            };
          } else {

            var validBoundaries = false;
            var sel = qx.dom.Node.getWindow(node).getSelection();
            var rng = qx.bom.Range.get(node);
            if(qx.dom.Node.isText(node)){

              if(end === undefined){

                end = node.length;
              };
              if(start >= 0 && start < node.length && end >= 0 && end <= node.length){

                validBoundaries = true;
              };
            } else if(qx.dom.Node.isElement(node)){

              if(end === undefined){

                end = node.childNodes.length - 1;
              };
              if(start >= 0 && node.childNodes[start] && end >= 0 && node.childNodes[end]){

                validBoundaries = true;
              };
            } else if(qx.dom.Node.isDocument(node)){

              node = node.body;
              if(end === undefined){

                end = node.childNodes.length - 1;
              };
              if(start >= 0 && node.childNodes[start] && end >= 0 && node.childNodes[end]){

                validBoundaries = true;
              };
            };;
            if(validBoundaries){

              if(!sel.isCollapsed){

                sel.collapseToStart();
              };
              rng.setStart(node, start);
              if(qx.dom.Node.isText(node)){

                rng.setEnd(node, end);
              } else {

                rng.setEndAfter(node.childNodes[end]);
              };
              if(sel.rangeCount > 0){

                sel.removeAllRanges();
              };
              sel.addRange(rng);
              return true;
            };
          };
          return false;
        }
      }),
      setAll : function(node){

        return qx.bom.Selection.set(node, 0);
      },
      clear : qx.core.Environment.select(m, {
        "selection" : function(node){

          var sel = qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node));
          var rng = qx.bom.Range.get(node);
          var parent = rng.parentElement();
          var documentRange = qx.bom.Range.get(qx.dom.Node.getDocument(node));
          if(parent == documentRange.parentElement() && parent == node){

            sel.empty();
          };
        },
        "default" : function(node){

          var sel = qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node));
          var nodeName = node.nodeName.toLowerCase();
          if(qx.dom.Node.isElement(node) && (nodeName == h || nodeName == n)){

            node.setSelectionRange(0, 0);
            if(qx.bom.Element && qx.bom.Element.blur){

              qx.bom.Element.blur(node);
            };
          } else if(qx.dom.Node.isDocument(node) || nodeName == o){

            sel.collapse(node.body ? node.body : node, 0);
          } else {

            var rng = qx.bom.Range.get(node);
            if(!rng.collapsed){

              var compareNode;
              var commonAncestor = rng.commonAncestorContainer;
              if(qx.dom.Node.isElement(node) && qx.dom.Node.isText(commonAncestor)){

                compareNode = commonAncestor.parentNode;
              } else {

                compareNode = commonAncestor;
              };
              if(compareNode == node){

                sel.collapse(node, 0);
              };
            };
          };
        }
      })
    }
  });
})();
(function(){

  var a = "qx.bom.Range",b = "text",c = "password",d = "file",e = "submit",f = "reset",g = "textarea",h = "input",i = "hidden",j = "html.selection",k = "button",l = "body";
  qx.Bootstrap.define(a, {
    statics : {
      get : qx.core.Environment.select(j, {
        "selection" : function(node){

          if(qx.dom.Node.isElement(node)){

            switch(node.nodeName.toLowerCase()){case h:
            switch(node.type){case b:case c:case i:case k:case f:case d:case e:
            return node.createTextRange();default:
            return qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node)).createRange();};
            break;case g:case l:case k:
            return node.createTextRange();default:
            return qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node)).createRange();};
          } else {

            if(node == null){

              node = window;
            };
            return qx.bom.Selection.getSelectionObject(qx.dom.Node.getDocument(node)).createRange();
          };
        },
        "default" : function(node){

          var doc = qx.dom.Node.getDocument(node);
          var sel = qx.bom.Selection.getSelectionObject(doc);
          if(sel.rangeCount > 0){

            return sel.getRangeAt(0);
          } else {

            return doc.createRange();
          };
        }
      })
    }
  });
})();
(function(){

  var a = "m",b = "g",c = "^",d = "",e = "qx.util.StringSplit",f = "i",g = "$(?!\\s)",h = "[object RegExp]",j = "y";
  qx.Bootstrap.define(e, {
    statics : {
      split : function(str, separator, limit){

        if(Object.prototype.toString.call(separator) !== h){

          return String.prototype.split.call(str, separator, limit);
        };
        var output = [],lastLastIndex = 0,flags = (separator.ignoreCase ? f : d) + (separator.multiline ? a : d) + (separator.sticky ? j : d),separator = RegExp(separator.source, flags + b),separator2,match,lastIndex,lastLength,compliantExecNpcg = /()??/.exec(d)[1] === undefined;
        str = str + d;
        if(!compliantExecNpcg){

          separator2 = RegExp(c + separator.source + g, flags);
        };
        if(limit === undefined || +limit < 0){

          limit = Infinity;
        } else {

          limit = Math.floor(+limit);
          if(!limit){

            return [];
          };
        };
        while(match = separator.exec(str)){

          lastIndex = match.index + match[0].length;
          if(lastIndex > lastLastIndex){

            output.push(str.slice(lastLastIndex, match.index));
            if(!compliantExecNpcg && match.length > 1){

              match[0].replace(separator2, function(){

                for(var i = 1;i < arguments.length - 2;i++){

                  if(arguments[i] === undefined){

                    match[i] = undefined;
                  };
                };
              });
            };
            if(match.length > 1 && match.index < str.length){

              Array.prototype.push.apply(output, match.slice(1));
            };
            lastLength = match[0].length;
            lastLastIndex = lastIndex;
            if(output.length >= limit){

              break;
            };
          };
          if(separator.lastIndex === match.index){

            separator.lastIndex++;
          };
        };
        if(lastLastIndex === str.length){

          if(lastLength || !separator.test(d)){

            output.push(d);
          };
        } else {

          output.push(str.slice(lastLastIndex));
        };
        return output.length > limit ? output.slice(0, limit) : output;
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Focus";
  qx.Class.define(a, {
    extend : qx.event.type.Event,
    members : {
      init : function(target, relatedTarget, canBubble){

        qx.event.type.Event.prototype.init.call(this, canBubble, false);
        this._target = target;
        this._relatedTarget = relatedTarget;
        return this;
      }
    }
  });
})();
(function(){

  var a = "qx.event.handler.Capture";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        capture : true,
        losecapture : true
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      }
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "blur",b = "mousedown",c = "qxDraggable",d = "mouseout",f = "Escape",g = "drag",h = "keydown",i = "Unsupported data type: ",j = "drop",k = "qxDroppable",l = "qx.event.handler.DragDrop",m = "This method must not be used outside the drop event listener!",n = "Control",o = "Shift",p = "!",q = "mousemove",r = "alias",s = "droprequest",t = "copy",u = "dragstart",v = "move",w = "mouseover",x = "dragchange",y = "Alt",z = "keyup",A = "mouseup",B = "keypress",C = "dragleave",D = "dragend",E = "dragover",F = "left",G = "Please use a droprequest listener to the drag source to fill the manager with data!",H = "on";
  qx.Class.define(l, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__a = manager.getWindow().document.documentElement;
      this.__dh.addListener(this.__a, b, this._onMouseDown, this);
      this.__ic();
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        dragstart : 1,
        dragend : 1,
        dragover : 1,
        dragleave : 1,
        drop : 1,
        drag : 1,
        dragchange : 1,
        droprequest : 1
      },
      IGNORE_CAN_HANDLE : true,
      ALLOWED_BUTTONS : [F]
    },
    members : {
      __dh : null,
      __a : null,
      __hR : null,
      __hS : null,
      __hT : null,
      __hU : null,
      __hV : null,
      __d : null,
      __hW : null,
      __hX : null,
      __hY : false,
      __ia : 0,
      __ib : 0,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      addType : function(type){

        this.__hT[type] = true;
      },
      addAction : function(action){

        this.__hU[action] = true;
      },
      supportsType : function(type){

        return !!this.__hT[type];
      },
      supportsAction : function(type){

        return !!this.__hU[type];
      },
      getData : function(type){

        if(!this.__ii || !this.__hR){

          throw new Error(m);
        };
        if(!this.__hT[type]){

          throw new Error(i + type + p);
        };
        if(!this.__d[type]){

          this.__hW = type;
          this.__gI(s, this.__hS, this.__hR, false);
        };
        if(!this.__d[type]){

          throw new Error(G);
        };
        return this.__d[type] || null;
      },
      getCurrentAction : function(){

        return this.__hX;
      },
      addData : function(type, data){

        this.__d[type] = data;
      },
      getCurrentType : function(){

        return this.__hW;
      },
      isSessionActive : function(){

        return this.__hY;
      },
      __ic : function(){

        this.__hT = {
        };
        this.__hU = {
        };
        this.__hV = {
        };
        this.__d = {
        };
      },
      __id : function(){

        if(this.__hS == null){

          return;
        };
        var actions = this.__hU;
        var keys = this.__hV;
        var current = null;
        if(this.__ii){

          if(keys.Shift && keys.Control && actions.alias){

            current = r;
          } else if(keys.Shift && keys.Alt && actions.copy){

            current = t;
          } else if(keys.Shift && actions.move){

            current = v;
          } else if(keys.Alt && actions.alias){

            current = r;
          } else if(keys.Control && actions.copy){

            current = t;
          } else if(actions.move){

            current = v;
          } else if(actions.copy){

            current = t;
          } else if(actions.alias){

            current = r;
          };;;;;;;
        };
        var old = this.__hX;
        if(current != old){

          if(this.__hR){

            this.__hX = current;
            this.__ie = this.__gI(x, this.__hR, this.__hS, true);
            if(!this.__ie){

              current = null;
            };
          };
          if(current != old){

            this.__hX = current;
            this.__gI(x, this.__hS, this.__hR, false);
          };
        };
      },
      __gI : function(type, target, relatedTarget, cancelable, original){

        var Registration = qx.event.Registration;
        var dragEvent = Registration.createEvent(type, qx.event.type.Drag, [cancelable, original]);
        if(target !== relatedTarget){

          dragEvent.setRelatedTarget(relatedTarget);
        };
        return Registration.dispatchEvent(target, dragEvent);
      },
      __if : function(elem){

        while(elem && elem.nodeType == 1){

          if(elem.getAttribute(c) == H){

            return elem;
          };
          elem = elem.parentNode;
        };
        return null;
      },
      __ig : function(elem){

        while(elem && elem.nodeType == 1){

          if(elem.getAttribute(k) == H){

            return elem;
          };
          elem = elem.parentNode;
        };
        return null;
      },
      __ih : function(){

        this.__hS = null;
        this.__dh.removeListener(this.__a, q, this._onMouseMove, this, true);
        this.__dh.removeListener(this.__a, A, this._onMouseUp, this, true);
        qx.event.Registration.removeListener(window, a, this._onWindowBlur, this);
        this.__ic();
      },
      clearSession : function(){

        if(this.__hY){

          this.__dh.removeListener(this.__a, w, this._onMouseOver, this, true);
          this.__dh.removeListener(this.__a, d, this._onMouseOut, this, true);
          this.__dh.removeListener(this.__a, h, this._onKeyDown, this, true);
          this.__dh.removeListener(this.__a, z, this._onKeyUp, this, true);
          this.__dh.removeListener(this.__a, B, this._onKeyPress, this, true);
          this.__gI(D, this.__hS, this.__hR, false);
          this.__hY = false;
        };
        this.__ii = false;
        this.__hR = null;
        this.__ih();
      },
      __ii : false,
      __ie : false,
      _onWindowBlur : function(e){

        this.clearSession();
      },
      _onKeyDown : function(e){

        var iden = e.getKeyIdentifier();
        switch(iden){case y:case n:case o:
        if(!this.__hV[iden]){

          this.__hV[iden] = true;
          this.__id();
        };};
      },
      _onKeyUp : function(e){

        var iden = e.getKeyIdentifier();
        switch(iden){case y:case n:case o:
        if(this.__hV[iden]){

          this.__hV[iden] = false;
          this.__id();
        };};
      },
      _onKeyPress : function(e){

        var iden = e.getKeyIdentifier();
        switch(iden){case f:
        this.clearSession();};
      },
      _onMouseDown : function(e){

        var isButtonOk = qx.event.handler.DragDrop.ALLOWED_BUTTONS.indexOf(e.getButton()) !== -1;
        if(this.__hY || !isButtonOk){

          return;
        };
        var dragable = this.__if(e.getTarget());
        if(dragable){

          this.__ia = e.getDocumentLeft();
          this.__ib = e.getDocumentTop();
          this.__hS = dragable;
          this.__dh.addListener(this.__a, q, this._onMouseMove, this, true);
          this.__dh.addListener(this.__a, A, this._onMouseUp, this, true);
          qx.event.Registration.addListener(window, a, this._onWindowBlur, this);
        };
      },
      _onMouseUp : function(e){

        if(this.__ii && this.__ie){

          this.__gI(j, this.__hR, this.__hS, false, e);
        };
        if(this.__hY && e.getTarget() == this.__hS){

          e.stopPropagation();
          this.__gH();
        };
        this.clearSession();
      },
      _onMouseMove : function(e){

        if(this.__hY){

          if(!this.__gI(g, this.__hS, this.__hR, true, e)){

            this.clearSession();
          };
        } else {

          if(Math.abs(e.getDocumentLeft() - this.__ia) > 3 || Math.abs(e.getDocumentTop() - this.__ib) > 3){

            if(this.__gI(u, this.__hS, this.__hR, true, e)){

              this.__hY = true;
              this.__dh.addListener(this.__a, w, this._onMouseOver, this, true);
              this.__dh.addListener(this.__a, d, this._onMouseOut, this, true);
              this.__dh.addListener(this.__a, h, this._onKeyDown, this, true);
              this.__dh.addListener(this.__a, z, this._onKeyUp, this, true);
              this.__dh.addListener(this.__a, B, this._onKeyPress, this, true);
              var keys = this.__hV;
              keys.Control = e.isCtrlPressed();
              keys.Shift = e.isShiftPressed();
              keys.Alt = e.isAltPressed();
              this.__id();
            } else {

              this.__gI(D, this.__hS, this.__hR, false);
              this.__ih();
            };
          };
        };
      },
      _onMouseOver : function(e){

        var target = e.getTarget();
        var cursor = qx.ui.core.DragDropCursor.getInstance();
        var cursorEl = cursor.getContentElement().getDomElement();
        if(target === cursorEl){

          return;
        };
        var dropable = this.__ig(target);
        if(dropable && dropable != this.__hR){

          this.__ii = this.__gI(E, dropable, this.__hS, true, e);
          this.__hR = dropable;
          this.__id();
        };
      },
      _onMouseOut : function(e){

        var cursor = qx.ui.core.DragDropCursor.getInstance();
        var cursorEl = cursor.getContentElement().getDomElement();
        if(e.getTarget() === cursorEl){

          return;
        };
        if(e.getRelatedTarget() === cursorEl){

          return;
        };
        var dropable = this.__ig(e.getTarget());
        var newDropable = this.__ig(e.getRelatedTarget());
        if(dropable && dropable !== newDropable && dropable == this.__hR){

          this.__gI(C, this.__hR, newDropable, false, e);
          this.__hR = null;
          this.__ii = false;
          qx.event.Timer.once(this.__id, this, 0);
        };
      },
      __gH : function(){

        var mouseHandler = qx.event.Registration.getManager(window).getHandler(qx.event.handler.Mouse);
        mouseHandler.preventNextClick();
      }
    },
    destruct : function(){

      this.__hS = this.__hR = this.__dh = this.__a = this.__hT = this.__hU = this.__hV = this.__d = null;
    },
    defer : function(statics){

      if(!qx.event.handler.MouseEmulation.ON){

        qx.event.Registration.addHandler(statics);
      };
    }
  });
})();
(function(){

  var a = "qx.event.type.Drag";
  qx.Class.define(a, {
    extend : qx.event.type.Event,
    members : {
      init : function(cancelable, originalEvent){

        qx.event.type.Event.prototype.init.call(this, true, cancelable);
        if(originalEvent){

          this._native = originalEvent.getNativeEvent() || null;
          this._originalTarget = originalEvent.getTarget() || null;
        } else {

          this._native = null;
          this._originalTarget = null;
        };
        return this;
      },
      clone : function(embryo){

        var clone = qx.event.type.Event.prototype.clone.call(this, embryo);
        clone._native = this._native;
        return clone;
      },
      getDocumentLeft : function(){

        if(this._native == null){

          return 0;
        };
        if(this._native.pageX !== undefined){

          return Math.round(this._native.pageX);
        } else {

          var win = qx.dom.Node.getWindow(this._native.srcElement);
          return Math.round(this._native.clientX) + qx.bom.Viewport.getScrollLeft(win);
        };
      },
      getDocumentTop : function(){

        if(this._native == null){

          return 0;
        };
        if(this._native.pageY !== undefined){

          return Math.round(this._native.pageY);
        } else {

          var win = qx.dom.Node.getWindow(this._native.srcElement);
          return Math.round(this._native.clientY) + qx.bom.Viewport.getScrollTop(win);
        };
      },
      getManager : function(){

        return qx.event.Registration.getManager(this.getTarget()).getHandler(qx.event.handler.DragDrop);
      },
      addType : function(type){

        this.getManager().addType(type);
      },
      addAction : function(action){

        this.getManager().addAction(action);
      },
      supportsType : function(type){

        return this.getManager().supportsType(type);
      },
      supportsAction : function(action){

        return this.getManager().supportsAction(action);
      },
      addData : function(type, data){

        this.getManager().addData(type, data);
      },
      getData : function(type){

        return this.getManager().getData(type);
      },
      getCurrentType : function(){

        return this.getManager().getCurrentType();
      },
      getCurrentAction : function(){

        return this.getManager().getCurrentAction();
      },
      stopSession : function(){

        this.getManager().clearSession();
      }
    }
  });
})();
(function(){

  var a = "best-fit",b = "placementRight",c = "Boolean",d = "bottom-right",e = "' ",f = "widget",g = "placementLeft",h = "qx.ui.core.MPlacement",i = "left-top",j = "Integer",k = "left-middle",l = "right-middle",m = "top-center",n = "[qx.ui.core.MPlacement.setMoveDirection()], the value was '",o = "offsetRight",p = "mouse",q = "interval",r = "keep-align",s = "bottom-left",t = "direct",u = "shorthand",v = "Invalid value for the parameter 'direction' ",w = "offsetLeft",x = "top-left",y = "appear",z = "offsetBottom",A = "top",B = "top-right",C = "offsetTop",D = "but 'top' or 'left' are allowed.",E = "right-bottom",F = "disappear",G = "right-top",H = "bottom-center",I = "left-bottom",J = "left";
  qx.Mixin.define(h, {
    statics : {
      __ij : null,
      __ik : J,
      setVisibleElement : function(elem){

        this.__ij = elem;
      },
      getVisibleElement : function(){

        return this.__ij;
      },
      setMoveDirection : function(direction){

        if(direction === A || direction === J){

          this.__ik = direction;
        } else {

          throw new Error(v + n + direction + e + D);
        };
      },
      getMoveDirection : function(){

        return this.__ik;
      }
    },
    properties : {
      position : {
        check : [x, m, B, s, H, d, i, k, I, G, l, E],
        init : s,
        themeable : true
      },
      placeMethod : {
        check : [f, p],
        init : p,
        themeable : true
      },
      domMove : {
        check : c,
        init : false
      },
      placementModeX : {
        check : [t, r, a],
        init : r,
        themeable : true
      },
      placementModeY : {
        check : [t, r, a],
        init : r,
        themeable : true
      },
      offsetLeft : {
        check : j,
        init : 0,
        themeable : true
      },
      offsetTop : {
        check : j,
        init : 0,
        themeable : true
      },
      offsetRight : {
        check : j,
        init : 0,
        themeable : true
      },
      offsetBottom : {
        check : j,
        init : 0,
        themeable : true
      },
      offset : {
        group : [C, o, z, w],
        mode : u,
        themeable : true
      }
    },
    members : {
      __il : null,
      __im : null,
      __in : null,
      getLayoutLocation : function(widget){

        var insets,bounds,left,top;
        bounds = widget.getBounds();
        if(!bounds){

          return null;
        };
        left = bounds.left;
        top = bounds.top;
        var size = bounds;
        widget = widget.getLayoutParent();
        while(widget && !widget.isRootWidget()){

          bounds = widget.getBounds();
          left += bounds.left;
          top += bounds.top;
          insets = widget.getInsets();
          left += insets.left;
          top += insets.top;
          widget = widget.getLayoutParent();
        };
        if(widget.isRootWidget()){

          var rootCoords = widget.getContentLocation();
          if(rootCoords){

            left += rootCoords.left;
            top += rootCoords.top;
          };
        };
        return {
          left : left,
          top : top,
          right : left + size.width,
          bottom : top + size.height
        };
      },
      moveTo : function(left, top){

        var visible = qx.ui.core.MPlacement.getVisibleElement();
        if(visible){

          var bounds = this.getBounds();
          var elemLocation = visible.getContentLocation();
          if(bounds && elemLocation){

            var bottom = top + bounds.height;
            var right = left + bounds.width;
            if((right > elemLocation.left && left < elemLocation.right) && (bottom > elemLocation.top && top < elemLocation.bottom)){

              var direction = qx.ui.core.MPlacement.getMoveDirection();
              if(direction === J){

                left = Math.max(elemLocation.left - bounds.width, 0);
              } else {

                top = Math.max(elemLocation.top - bounds.height, 0);
              };
            };
          };
        };
        if(this.getDomMove()){

          this.setDomPosition(left, top);
        } else {

          this.setLayoutProperties({
            left : left,
            top : top
          });
        };
      },
      placeToWidget : function(target, liveupdate){

        if(liveupdate){

          this.__io();
          this.__il = qx.lang.Function.bind(this.placeToWidget, this, target, false);
          qx.event.Idle.getInstance().addListener(q, this.__il);
          this.__in = function(){

            this.__io();
          };
          this.addListener(F, this.__in, this);
        };
        var coords = target.getContentLocation() || this.getLayoutLocation(target);
        if(coords != null){

          this._place(coords);
          return true;
        } else {

          return false;
        };
      },
      __io : function(){

        if(this.__il){

          qx.event.Idle.getInstance().removeListener(q, this.__il);
          this.__il = null;
        };
        if(this.__in){

          this.removeListener(F, this.__in, this);
          this.__in = null;
        };
      },
      placeToMouse : function(event){

        var left = Math.round(event.getDocumentLeft());
        var top = Math.round(event.getDocumentTop());
        var coords = {
          left : left,
          top : top,
          right : left,
          bottom : top
        };
        this._place(coords);
      },
      placeToElement : function(elem, liveupdate){

        var location = qx.bom.element.Location.get(elem);
        var coords = {
          left : location.left,
          top : location.top,
          right : location.left + elem.offsetWidth,
          bottom : location.top + elem.offsetHeight
        };
        if(liveupdate){

          this.__il = qx.lang.Function.bind(this.placeToElement, this, elem, false);
          qx.event.Idle.getInstance().addListener(q, this.__il);
          this.addListener(F, function(){

            if(this.__il){

              qx.event.Idle.getInstance().removeListener(q, this.__il);
              this.__il = null;
            };
          }, this);
        };
        this._place(coords);
      },
      placeToPoint : function(point){

        var coords = {
          left : point.left,
          top : point.top,
          right : point.left,
          bottom : point.top
        };
        this._place(coords);
      },
      _getPlacementOffsets : function(){

        return {
          left : this.getOffsetLeft(),
          top : this.getOffsetTop(),
          right : this.getOffsetRight(),
          bottom : this.getOffsetBottom()
        };
      },
      __ip : function(callback){

        var size = null;
        if(this._computePlacementSize){

          var size = this._computePlacementSize();
        } else if(this.isVisible()){

          var size = this.getBounds();
        };
        if(size == null){

          this.addListenerOnce(y, function(){

            this.__ip(callback);
          }, this);
        } else {

          callback.call(this, size);
        };
      },
      _place : function(coords){

        this.__ip(function(size){

          var result = qx.util.placement.Placement.compute(size, this.getLayoutParent().getBounds(), coords, this._getPlacementOffsets(), this.getPosition(), this.getPlacementModeX(), this.getPlacementModeY());
          this.removeState(g);
          this.removeState(b);
          this.addState(coords.left < result.left ? b : g);
          this.moveTo(result.left, result.top);
        });
      }
    },
    destruct : function(){

      this.__io();
    }
  });
})();
(function(){

  var a = "Number",b = "interval",c = "_applyTimeoutInterval",d = "qx.event.type.Event",e = "qx.event.Idle",f = "singleton";
  qx.Class.define(e, {
    extend : qx.core.Object,
    type : f,
    construct : function(){

      qx.core.Object.call(this);
      var timer = new qx.event.Timer(this.getTimeoutInterval());
      timer.addListener(b, this._onInterval, this);
      timer.start();
      this.__eo = timer;
    },
    events : {
      "interval" : d
    },
    properties : {
      timeoutInterval : {
        check : a,
        init : 100,
        apply : c
      }
    },
    members : {
      __eo : null,
      _applyTimeoutInterval : function(value){

        this.__eo.setInterval(value);
      },
      _onInterval : function(){

        this.fireEvent(b);
      }
    },
    destruct : function(){

      if(this.__eo){

        this.__eo.stop();
      };
      this.__eo = null;
    }
  });
})();
(function(){

  var a = "qx.event.Timer",b = "_applyInterval",c = "_applyEnabled",d = "Boolean",f = "interval",g = "qx.event.type.Event",h = "Integer";
  qx.Class.define(a, {
    extend : qx.core.Object,
    construct : function(interval){

      qx.core.Object.call(this);
      this.setEnabled(false);
      if(interval != null){

        this.setInterval(interval);
      };
      var self = this;
      this.__iq = function(){

        self._oninterval.call(self);
      };
    },
    events : {
      "interval" : g
    },
    statics : {
      once : function(func, obj, timeout){

        {
        };
        var timer = new qx.event.Timer(timeout);
        timer.__ir = func;
        timer.addListener(f, function(e){

          timer.stop();
          func.call(obj, e);
          timer.dispose();
          obj = null;
        }, obj);
        timer.start();
        return timer;
      }
    },
    properties : {
      enabled : {
        init : true,
        check : d,
        apply : c
      },
      interval : {
        check : h,
        init : 1000,
        apply : b
      }
    },
    members : {
      __is : null,
      __iq : null,
      _applyInterval : function(value, old){

        if(this.getEnabled()){

          this.restart();
        };
      },
      _applyEnabled : function(value, old){

        if(old){

          window.clearInterval(this.__is);
          this.__is = null;
        } else if(value){

          this.__is = window.setInterval(this.__iq, this.getInterval());
        };
      },
      start : function(){

        this.setEnabled(true);
      },
      startWith : function(interval){

        this.setInterval(interval);
        this.start();
      },
      stop : function(){

        this.setEnabled(false);
      },
      restart : function(){

        this.stop();
        this.start();
      },
      restartWith : function(interval){

        this.stop();
        this.startWith(interval);
      },
      _oninterval : qx.event.GlobalError.observeMethod(function(){

        if(this.$$disposed){

          return;
        };
        if(this.getEnabled()){

          this.fireEvent(f);
        };
      })
    },
    destruct : function(){

      if(this.__is){

        window.clearInterval(this.__is);
      };
      this.__is = this.__iq = null;
    }
  });
})();
(function(){

  var a = "changeWidth",b = "Boolean",c = "allowShrinkY",d = "_applyAlign",e = "_applyStretching",f = "bottom",g = "Integer",h = "changeTheme",i = "_applyDimension",j = "baseline",k = "marginBottom",l = "qx.ui.core.LayoutItem",m = "center",n = "marginTop",o = "allowGrowX",p = "shorthand",q = "middle",r = "marginLeft",s = "qx.dyntheme",t = "allowShrinkX",u = "top",v = "right",w = "marginRight",x = "abstract",y = "_applyMargin",z = "allowGrowY",A = "left",B = "changeHeight";
  qx.Class.define(l, {
    type : x,
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      if(qx.core.Environment.get(s)){

        qx.theme.manager.Appearance.getInstance().addListener(h, this._onChangeTheme, this);
        qx.theme.manager.Color.getInstance().addListener(h, this._onChangeTheme, this);
      };
    },
    properties : {
      minWidth : {
        check : g,
        nullable : true,
        apply : i,
        init : null,
        themeable : true
      },
      width : {
        check : g,
        event : a,
        nullable : true,
        apply : i,
        init : null,
        themeable : true
      },
      maxWidth : {
        check : g,
        nullable : true,
        apply : i,
        init : null,
        themeable : true
      },
      minHeight : {
        check : g,
        nullable : true,
        apply : i,
        init : null,
        themeable : true
      },
      height : {
        check : g,
        event : B,
        nullable : true,
        apply : i,
        init : null,
        themeable : true
      },
      maxHeight : {
        check : g,
        nullable : true,
        apply : i,
        init : null,
        themeable : true
      },
      allowGrowX : {
        check : b,
        apply : e,
        init : true,
        themeable : true
      },
      allowShrinkX : {
        check : b,
        apply : e,
        init : true,
        themeable : true
      },
      allowGrowY : {
        check : b,
        apply : e,
        init : true,
        themeable : true
      },
      allowShrinkY : {
        check : b,
        apply : e,
        init : true,
        themeable : true
      },
      allowStretchX : {
        group : [o, t],
        mode : p,
        themeable : true
      },
      allowStretchY : {
        group : [z, c],
        mode : p,
        themeable : true
      },
      marginTop : {
        check : g,
        init : 0,
        apply : y,
        themeable : true
      },
      marginRight : {
        check : g,
        init : 0,
        apply : y,
        themeable : true
      },
      marginBottom : {
        check : g,
        init : 0,
        apply : y,
        themeable : true
      },
      marginLeft : {
        check : g,
        init : 0,
        apply : y,
        themeable : true
      },
      margin : {
        group : [n, w, k, r],
        mode : p,
        themeable : true
      },
      alignX : {
        check : [A, m, v],
        nullable : true,
        apply : d,
        themeable : true
      },
      alignY : {
        check : [u, q, f, j],
        nullable : true,
        apply : d,
        themeable : true
      }
    },
    members : {
      _onChangeTheme : qx.core.Environment.select(s, {
        "true" : function(){

          var props = qx.util.PropertyUtil.getAllProperties(this.constructor);
          for(var name in props){

            var desc = props[name];
            if(desc.themeable){

              var userValue = qx.util.PropertyUtil.getUserValue(this, name);
              if(userValue == null){

                qx.util.PropertyUtil.resetThemed(this, name);
              };
            };
          };
        },
        "false" : null
      }),
      __it : null,
      __iu : null,
      __iv : null,
      __iw : null,
      __ix : null,
      __iy : null,
      __iz : null,
      getBounds : function(){

        return this.__iy || this.__iu || null;
      },
      clearSeparators : function(){
      },
      renderSeparator : function(separator, bounds){
      },
      renderLayout : function(left, top, width, height){

        {

          var msg;
        };
        var flowHeight = null;
        if(this.getHeight() == null && this._hasHeightForWidth()){

          var flowHeight = this._getHeightForWidth(width);
        };
        if(flowHeight != null && flowHeight !== this.__it){

          this.__it = flowHeight;
          qx.ui.core.queue.Layout.add(this);
          return null;
        };
        var computed = this.__iu;
        if(!computed){

          computed = this.__iu = {
          };
        };
        var changes = {
        };
        if(left !== computed.left || top !== computed.top){

          changes.position = true;
          computed.left = left;
          computed.top = top;
        };
        if(width !== computed.width || height !== computed.height){

          changes.size = true;
          computed.width = width;
          computed.height = height;
        };
        if(this.__iv){

          changes.local = true;
          delete this.__iv;
        };
        if(this.__ix){

          changes.margin = true;
          delete this.__ix;
        };
        return changes;
      },
      isExcluded : function(){

        return false;
      },
      hasValidLayout : function(){

        return !this.__iv;
      },
      scheduleLayoutUpdate : function(){

        qx.ui.core.queue.Layout.add(this);
      },
      invalidateLayoutCache : function(){

        this.__iv = true;
        this.__iw = null;
      },
      getSizeHint : function(compute){

        var hint = this.__iw;
        if(hint){

          return hint;
        };
        if(compute === false){

          return null;
        };
        hint = this.__iw = this._computeSizeHint();
        if(this._hasHeightForWidth() && this.__it && this.getHeight() == null){

          hint.height = this.__it;
        };
        if(hint.minWidth > hint.width){

          hint.width = hint.minWidth;
        };
        if(hint.maxWidth < hint.width){

          hint.width = hint.maxWidth;
        };
        if(!this.getAllowGrowX()){

          hint.maxWidth = hint.width;
        };
        if(!this.getAllowShrinkX()){

          hint.minWidth = hint.width;
        };
        if(hint.minHeight > hint.height){

          hint.height = hint.minHeight;
        };
        if(hint.maxHeight < hint.height){

          hint.height = hint.maxHeight;
        };
        if(!this.getAllowGrowY()){

          hint.maxHeight = hint.height;
        };
        if(!this.getAllowShrinkY()){

          hint.minHeight = hint.height;
        };
        return hint;
      },
      _computeSizeHint : function(){

        var minWidth = this.getMinWidth() || 0;
        var minHeight = this.getMinHeight() || 0;
        var width = this.getWidth() || minWidth;
        var height = this.getHeight() || minHeight;
        var maxWidth = this.getMaxWidth() || Infinity;
        var maxHeight = this.getMaxHeight() || Infinity;
        return {
          minWidth : minWidth,
          width : width,
          maxWidth : maxWidth,
          minHeight : minHeight,
          height : height,
          maxHeight : maxHeight
        };
      },
      _hasHeightForWidth : function(){

        var layout = this._getLayout();
        if(layout){

          return layout.hasHeightForWidth();
        };
        return false;
      },
      _getHeightForWidth : function(width){

        var layout = this._getLayout();
        if(layout && layout.hasHeightForWidth()){

          return layout.getHeightForWidth(width);
        };
        return null;
      },
      _getLayout : function(){

        return null;
      },
      _applyMargin : function(){

        this.__ix = true;
        var parent = this.$$parent;
        if(parent){

          parent.updateLayoutProperties();
        };
      },
      _applyAlign : function(){

        var parent = this.$$parent;
        if(parent){

          parent.updateLayoutProperties();
        };
      },
      _applyDimension : function(){

        qx.ui.core.queue.Layout.add(this);
      },
      _applyStretching : function(){

        qx.ui.core.queue.Layout.add(this);
      },
      hasUserBounds : function(){

        return !!this.__iy;
      },
      setUserBounds : function(left, top, width, height){

        this.__iy = {
          left : left,
          top : top,
          width : width,
          height : height
        };
        qx.ui.core.queue.Layout.add(this);
      },
      resetUserBounds : function(){

        delete this.__iy;
        qx.ui.core.queue.Layout.add(this);
      },
      __iA : {
      },
      setLayoutProperties : function(props){

        if(props == null){

          return;
        };
        var storage = this.__iz;
        if(!storage){

          storage = this.__iz = {
          };
        };
        var parent = this.getLayoutParent();
        if(parent){

          parent.updateLayoutProperties(props);
        };
        for(var key in props){

          if(props[key] == null){

            delete storage[key];
          } else {

            storage[key] = props[key];
          };
        };
      },
      getLayoutProperties : function(){

        return this.__iz || this.__iA;
      },
      clearLayoutProperties : function(){

        delete this.__iz;
      },
      updateLayoutProperties : function(props){

        var layout = this._getLayout();
        if(layout){

          {

            var key;
          };
          layout.invalidateChildrenCache();
        };
        qx.ui.core.queue.Layout.add(this);
      },
      getApplicationRoot : function(){

        return qx.core.Init.getApplication().getRoot();
      },
      getLayoutParent : function(){

        return this.$$parent || null;
      },
      setLayoutParent : function(parent){

        if(this.$$parent === parent){

          return;
        };
        this.$$parent = parent || null;
        qx.ui.core.queue.Visibility.add(this);
      },
      isRootWidget : function(){

        return false;
      },
      _getRoot : function(){

        var parent = this;
        while(parent){

          if(parent.isRootWidget()){

            return parent;
          };
          parent = parent.$$parent;
        };
        return null;
      },
      clone : function(){

        var clone = qx.core.Object.prototype.clone.call(this);
        var props = this.__iz;
        if(props){

          clone.__iz = qx.lang.Object.clone(props);
        };
        return clone;
      }
    },
    destruct : function(){

      if(qx.core.Environment.get(s)){

        qx.theme.manager.Appearance.getInstance().removeListener(h, this._onChangeTheme, this);
        qx.theme.manager.Color.getInstance().removeListener(h, this._onChangeTheme, this);
      };
      this.$$parent = this.$$subparent = this.__iz = this.__iu = this.__iy = this.__iw = null;
    }
  });
})();
(function(){

  var a = "Missing appearance: ",b = "_applyTheme",c = "string",d = "qx.theme.manager.Appearance",e = ":",f = "Theme",g = "changeTheme",h = "/",j = "singleton";
  qx.Class.define(d, {
    type : j,
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__iB = {
      };
      this.__iC = {
      };
    },
    properties : {
      theme : {
        check : f,
        nullable : true,
        event : g,
        apply : b
      }
    },
    members : {
      __iD : {
      },
      __iB : null,
      __iC : null,
      _applyTheme : function(){

        this.__iC = {
        };
        this.__iB = {
        };
      },
      __iE : function(id, theme, defaultId, chain){

        var db = theme.appearances;
        var entry = db[id];
        if(!entry){

          var divider = h;
          var end = [];
          var splitted = id.split(divider);
          var chainCopy = qx.lang.Array.clone(splitted);
          var alias;
          while(!entry && splitted.length > 0){

            end.unshift(splitted.pop());
            var baseid = splitted.join(divider);
            entry = db[baseid];
            if(entry){

              alias = entry.alias || entry;
              if(typeof alias === c){

                var mapped = alias + divider + end.join(divider);
                return this.__iE(mapped, theme, defaultId, chainCopy);
              };
            };
          };
          for(var i = 0;i < end.length - 1;i++){

            end.shift();
            var subId = end.join(divider);
            var resolved = this.__iE(subId, theme, null, chainCopy);
            if(resolved){

              return resolved;
            };
          };
          if(defaultId != null){

            return this.__iE(defaultId, theme, null, chainCopy);
          };
          {
          };
          return null;
        } else if(typeof entry === c){

          return this.__iE(entry, theme, defaultId, chainCopy);
        } else if(entry.include && !entry.style){

          return this.__iE(entry.include, theme, defaultId, chainCopy);
        };;
        return id;
      },
      styleFrom : function(id, states, theme, defaultId){

        if(!theme){

          theme = this.getTheme();
        };
        var aliasMap = this.__iC;
        var resolved = aliasMap[id];
        if(!resolved){

          resolved = aliasMap[id] = this.__iE(id, theme, defaultId);
        };
        var entry = theme.appearances[resolved];
        if(!entry){

          this.warn(a + id);
          return null;
        };
        if(!entry.style){

          return null;
        };
        var unique = resolved;
        if(states){

          var bits = entry.$$bits;
          if(!bits){

            bits = entry.$$bits = {
            };
            entry.$$length = 0;
          };
          var sum = 0;
          for(var state in states){

            if(!states[state]){

              continue;
            };
            if(bits[state] == null){

              bits[state] = 1 << entry.$$length++;
            };
            sum += bits[state];
          };
          if(sum > 0){

            unique += e + sum;
          };
        };
        var cache = this.__iB;
        if(cache[unique] !== undefined){

          return cache[unique];
        };
        if(!states){

          states = this.__iD;
        };
        var result;
        if(entry.include || entry.base){

          var incl;
          if(entry.include){

            incl = this.styleFrom(entry.include, states, theme, defaultId);
          };
          var local = entry.style(states, incl);
          result = {
          };
          if(entry.base){

            var base = this.styleFrom(resolved, states, entry.base, defaultId);
            if(entry.include){

              for(var baseIncludeKey in base){

                if(!incl.hasOwnProperty(baseIncludeKey) && !local.hasOwnProperty(baseIncludeKey)){

                  result[baseIncludeKey] = base[baseIncludeKey];
                };
              };
            } else {

              for(var baseKey in base){

                if(!local.hasOwnProperty(baseKey)){

                  result[baseKey] = base[baseKey];
                };
              };
            };
          };
          if(entry.include){

            for(var includeKey in incl){

              if(!local.hasOwnProperty(includeKey)){

                result[includeKey] = incl[includeKey];
              };
            };
          };
          for(var localKey in local){

            result[localKey] = local[localKey];
          };
        } else {

          result = entry.style(states);
        };
        return cache[unique] = result || null;
      }
    },
    destruct : function(){

      this.__iB = this.__iC = null;
    }
  });
})();
(function(){

  var a = "qx.util.ValueManager",b = "abstract";
  qx.Class.define(a, {
    type : b,
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this._dynamic = {
      };
    },
    members : {
      _dynamic : null,
      resolveDynamic : function(value){

        return this._dynamic[value];
      },
      isDynamic : function(value){

        return !!this._dynamic[value];
      },
      resolve : function(value){

        if(value && this._dynamic[value]){

          return this._dynamic[value];
        };
        return value;
      },
      _setDynamic : function(value){

        this._dynamic = value;
      },
      _getDynamic : function(){

        return this._dynamic;
      }
    },
    destruct : function(){

      this._dynamic = null;
    }
  });
})();
(function(){

  var a = "Could not parse color: ",b = "_applyTheme",c = "qx.theme.manager.Color",d = "Theme",e = "changeTheme",f = "string",g = "singleton";
  qx.Class.define(c, {
    type : g,
    extend : qx.util.ValueManager,
    properties : {
      theme : {
        check : d,
        nullable : true,
        apply : b,
        event : e
      }
    },
    members : {
      _applyTheme : function(value){

        var dest = {
        };
        if(value){

          var colors = value.colors;
          for(var name in colors){

            dest[name] = this.__iF(colors, name);
          };
        };
        this._setDynamic(dest);
      },
      __iF : function(colors, name){

        var color = colors[name];
        if(typeof color === f){

          if(!qx.util.ColorUtil.isCssString(color)){

            if(colors[color] != undefined){

              return this.__iF(colors, color);
            };
            throw new Error(a + color);
          };
          return color;
        } else if(color instanceof Array){

          return qx.util.ColorUtil.rgbToRgbString(color);
        };
        throw new Error(a + color);
      },
      resolve : function(value){

        var cache = this._dynamic;
        var resolved = cache[value];
        if(resolved){

          return resolved;
        };
        var theme = this.getTheme();
        if(theme !== null && theme.colors[value]){

          return cache[value] = theme.colors[value];
        };
        return value;
      },
      isDynamic : function(value){

        var cache = this._dynamic;
        if(value && (cache[value] !== undefined)){

          return true;
        };
        var theme = this.getTheme();
        if(theme !== null && value && (theme.colors[value] !== undefined)){

          cache[value] = theme.colors[value];
          return true;
        };
        return false;
      }
    }
  });
})();
(function(){

  var a = "$$theme_",b = "$$user_",c = "qx.util.PropertyUtil",d = "$$init_";
  qx.Class.define(c, {
    statics : {
      getProperties : function(clazz){

        return clazz.$$properties;
      },
      getAllProperties : function(clazz){

        var properties = {
        };
        var superclass = clazz;
        while(superclass != qx.core.Object){

          var currentProperties = this.getProperties(superclass);
          for(var property in currentProperties){

            properties[property] = currentProperties[property];
          };
          superclass = superclass.superclass;
        };
        return properties;
      },
      getUserValue : function(object, propertyName){

        return object[b + propertyName];
      },
      setUserValue : function(object, propertyName, value){

        object[b + propertyName] = value;
      },
      deleteUserValue : function(object, propertyName){

        delete (object[b + propertyName]);
      },
      getInitValue : function(object, propertyName){

        return object[d + propertyName];
      },
      setInitValue : function(object, propertyName, value){

        object[d + propertyName] = value;
      },
      deleteInitValue : function(object, propertyName){

        delete (object[d + propertyName]);
      },
      getThemeValue : function(object, propertyName){

        return object[a + propertyName];
      },
      setThemeValue : function(object, propertyName, value){

        object[a + propertyName] = value;
      },
      deleteThemeValue : function(object, propertyName){

        delete (object[a + propertyName]);
      },
      setThemed : function(object, propertyName, value){

        var styler = qx.core.Property.$$method.setThemed;
        object[styler[propertyName]](value);
      },
      resetThemed : function(object, propertyName){

        var unstyler = qx.core.Property.$$method.resetThemed;
        object[unstyler[propertyName]]();
      }
    }
  });
})();
(function(){

  var a = "qx.ui.core.queue.Layout",b = "layout";
  qx.Class.define(a, {
    statics : {
      __iG : {
      },
      __iH : {
      },
      remove : function(widget){

        delete this.__iG[widget.$$hash];
      },
      add : function(widget){

        this.__iG[widget.$$hash] = widget;
        qx.ui.core.queue.Manager.scheduleFlush(b);
      },
      isScheduled : function(widget){

        return !!this.__iG[widget.$$hash];
      },
      flush : function(){

        var queue = this.__iJ();
        for(var i = queue.length - 1;i >= 0;i--){

          var widget = queue[i];
          if(widget.hasValidLayout()){

            continue;
          };
          if(widget.isRootWidget() && !widget.hasUserBounds()){

            var hint = widget.getSizeHint();
            widget.renderLayout(0, 0, hint.width, hint.height);
          } else {

            var bounds = widget.getBounds();
            widget.renderLayout(bounds.left, bounds.top, bounds.width, bounds.height);
          };
        };
      },
      getNestingLevel : function(widget){

        var cache = this.__iH;
        var level = 0;
        var parent = widget;
        while(true){

          if(cache[parent.$$hash] != null){

            level += cache[parent.$$hash];
            break;
          };
          if(!parent.$$parent){

            break;
          };
          parent = parent.$$parent;
          level += 1;
        };
        var leveldown = level;
        while(widget && widget !== parent){

          cache[widget.$$hash] = leveldown--;
          widget = widget.$$parent;
        };
        return level;
      },
      __iI : function(){

        var VisibilityQueue = qx.ui.core.queue.Visibility;
        this.__iH = {
        };
        var levels = [];
        var queue = this.__iG;
        var widget,level;
        for(var hash in queue){

          widget = queue[hash];
          if(VisibilityQueue.isVisible(widget)){

            level = this.getNestingLevel(widget);
            if(!levels[level]){

              levels[level] = {
              };
            };
            levels[level][hash] = widget;
            delete queue[hash];
          };
        };
        return levels;
      },
      __iJ : function(){

        var sortedQueue = [];
        var levels = this.__iI();
        for(var level = levels.length - 1;level >= 0;level--){

          if(!levels[level]){

            continue;
          };
          for(var hash in levels[level]){

            var widget = levels[level][hash];
            if(level == 0 || widget.isRootWidget() || widget.hasUserBounds()){

              sortedQueue.push(widget);
              widget.invalidateLayoutCache();
              continue;
            };
            var oldSizeHint = widget.getSizeHint(false);
            if(oldSizeHint){

              widget.invalidateLayoutCache();
              var newSizeHint = widget.getSizeHint();
              var hintChanged = (!widget.getBounds() || oldSizeHint.minWidth !== newSizeHint.minWidth || oldSizeHint.width !== newSizeHint.width || oldSizeHint.maxWidth !== newSizeHint.maxWidth || oldSizeHint.minHeight !== newSizeHint.minHeight || oldSizeHint.height !== newSizeHint.height || oldSizeHint.maxHeight !== newSizeHint.maxHeight);
            } else {

              hintChanged = true;
            };
            if(hintChanged){

              var parent = widget.getLayoutParent();
              if(!levels[level - 1]){

                levels[level - 1] = {
                };
              };
              levels[level - 1][parent.$$hash] = parent;
            } else {

              sortedQueue.push(widget);
            };
          };
        };
        return sortedQueue;
      }
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "pop.push.reverse.shift.sort.splice.unshift.join.slice",d = "number",e = "qx.type.BaseArray",f = ".";
  qx.Bootstrap.define(e, {
    extend : Array,
    construct : function(length_or_items){
    },
    members : {
      toArray : null,
      valueOf : null,
      pop : null,
      push : null,
      reverse : null,
      shift : null,
      sort : null,
      splice : null,
      unshift : null,
      concat : null,
      join : null,
      slice : null,
      toString : null,
      indexOf : null,
      lastIndexOf : null,
      forEach : null,
      filter : null,
      map : null,
      some : null,
      every : null
    }
  });
  (function(){

    function createStackConstructor(stack){

      if((qx.core.Environment.get(b) == a)){

        Stack.prototype = {
          length : 0,
          $$isArray : true
        };
        var args = c.split(f);
        for(var length = args.length;length;){

          Stack.prototype[args[--length]] = Array.prototype[args[length]];
        };
      };
      var slice = Array.prototype.slice;
      Stack.prototype.concat = function(){

        var constructor = this.slice(0);
        for(var i = 0,length = arguments.length;i < length;i++){

          var copy;
          if(arguments[i] instanceof Stack){

            copy = slice.call(arguments[i], 0);
          } else if(arguments[i] instanceof Array){

            copy = arguments[i];
          } else {

            copy = [arguments[i]];
          };
          constructor.push.apply(constructor, copy);
        };
        return constructor;
      };
      Stack.prototype.toString = function(){

        return slice.call(this, 0).toString();
      };
      Stack.prototype.toLocaleString = function(){

        return slice.call(this, 0).toLocaleString();
      };
      Stack.prototype.constructor = Stack;
      Stack.prototype.indexOf = Array.prototype.indexOf;
      Stack.prototype.lastIndexOf = Array.prototype.lastIndexOf;
      Stack.prototype.forEach = Array.prototype.forEach;
      Stack.prototype.some = Array.prototype.some;
      Stack.prototype.every = Array.prototype.every;
      var filter = Array.prototype.filter;
      var map = Array.prototype.map;
      Stack.prototype.filter = function(){

        var ret = new this.constructor;
        ret.push.apply(ret, filter.apply(this, arguments));
        return ret;
      };
      Stack.prototype.map = function(){

        var ret = new this.constructor;
        ret.push.apply(ret, map.apply(this, arguments));
        return ret;
      };
      Stack.prototype.slice = function(){

        var ret = new this.constructor;
        ret.push.apply(ret, Array.prototype.slice.apply(this, arguments));
        return ret;
      };
      Stack.prototype.splice = function(){

        var ret = new this.constructor;
        ret.push.apply(ret, Array.prototype.splice.apply(this, arguments));
        return ret;
      };
      Stack.prototype.toArray = function(){

        return Array.prototype.slice.call(this, 0);
      };
      Stack.prototype.valueOf = function(){

        return this.length;
      };
      return Stack;
    };
    function Stack(length){

      if(arguments.length === 1 && typeof length === d){

        this.length = -1 < length && length === length >> .5 ? length : this.push(length);
      } else if(arguments.length){

        this.push.apply(this, arguments);
      };
    };
    function PseudoArray(){
    };
    PseudoArray.prototype = [];
    Stack.prototype = new PseudoArray;
    Stack.prototype.length = 0;
    qx.type.BaseArray = createStackConstructor(Stack);
  })();
})();
(function(){

  var a = "qxWeb",b = "data-qx-class";
  qx.Bootstrap.define(a, {
    extend : qx.type.BaseArray,
    statics : {
      __iK : [],
      $$qx : qx,
      $init : function(arg, clazz){

        var clean = [];
        for(var i = 0;i < arg.length;i++){

          var isNode = !!(arg[i] && (arg[i].nodeType === 1 || arg[i].nodeType === 9));
          if(isNode){

            clean.push(arg[i]);
            continue;
          };
          var isWindow = !!(arg[i] && arg[i].history && arg[i].location && arg[i].document);
          if(isWindow){

            clean.push(arg[i]);
          };
        };
        if(arg[0] && arg[0].getAttribute && arg[0].getAttribute(b)){

          clazz = qx.Bootstrap.getByName(arg[0].getAttribute(b)) || clazz;
        };
        var col = qx.lang.Array.cast(clean, clazz);
        for(var i = 0;i < qxWeb.__iK.length;i++){

          qxWeb.__iK[i].call(col);
        };
        return col;
      },
      $attach : function(module){

        for(var name in module){

          {
          };
          qxWeb.prototype[name] = module[name];
        };
      },
      $attachStatic : function(module){

        for(var name in module){

          {
          };
          qxWeb[name] = module[name];
        };
      },
      $attachInit : function(init){

        this.__iK.push(init);
      },
      define : function(name, config){

        if(config == undefined){

          config = name;
          name = null;
        };
        return qx.Bootstrap.define.call(qx.Bootstrap, name, config);
      }
    },
    construct : function(selector, context){

      if(!selector && this instanceof qxWeb){

        return this;
      };
      if(qx.Bootstrap.isString(selector)){

        if(context instanceof qxWeb){

          context = context[0];
        };
        selector = qx.bom.Selector.query(selector, context);
      } else if(!(qx.Bootstrap.isArray(selector))){

        selector = [selector];
      };
      return qxWeb.$init(selector, qxWeb);
    },
    members : {
      filter : function(selector){

        if(qx.lang.Type.isFunction(selector)){

          return qxWeb.$init(Array.prototype.filter.call(this, selector), this.constructor);
        };
        return qxWeb.$init(qx.bom.Selector.matches(selector, this), this.constructor);
      },
      unique : function(){

        var unique = qx.lang.Array.unique(this);
        return qxWeb.$init(unique, this.constructor);
      },
      slice : function(begin, end){

        if(end !== undefined){

          return qxWeb.$init(Array.prototype.slice.call(this, begin, end), this.constructor);
        };
        return qxWeb.$init(Array.prototype.slice.call(this, begin), this.constructor);
      },
      splice : function(index, howMany, varargs){

        return qxWeb.$init(Array.prototype.splice.apply(this, arguments), this.constructor);
      },
      map : function(callback, thisarg){

        return qxWeb.$init(Array.prototype.map.apply(this, arguments), this.constructor);
      },
      concat : function(varargs){

        var clone = Array.prototype.slice.call(this, 0);
        for(var i = 0;i < arguments.length;i++){

          if(arguments[i] instanceof qxWeb){

            clone = clone.concat(Array.prototype.slice.call(arguments[i], 0));
          } else {

            clone.push(arguments[i]);
          };
        };
        return qxWeb.$init(clone, this.constructor);
      },
      indexOf : function(elem){

        if(!elem){

          return -1;
        };
        if(qx.Bootstrap.isArray(elem)){

          elem = elem[0];
        };
        for(var i = 0,l = this.length;i < l;i++){

          if(this[i] === elem){

            return i;
          };
        };
        return -1;
      },
      debug : function(){

        debugger;
        return this;
      },
      _forEachElement : function(func, ctx){

        for(var i = 0,l = this.length;i < l;i++){

          if(this[i] && this[i].nodeType === 1){

            func.apply(ctx || this, [this[i], i, this]);
          };
        };
        return this;
      },
      _forEachElementWrapped : function(func, ctx){

        this._forEachElement(function(item, idx, arr){

          func.apply(this, [qxWeb(item), idx, arr]);
        }, ctx);
        return this;
      }
    },
    defer : function(statics){

      if(window.q == undefined){

        q = statics;
      };
    }
  });
})();
(function(){

  var c = "-",d = "*(?:checked|disabled|ismap|multiple|readonly|selected|value)",f = "(^|",g = "'] ",h = ":active",k = ":disabled",l = "div",n = ")(?:\\((?:(['\"])((?:\\\\.|[^\\\\])*?)\\2|([^()[\\]]*|(?:(?:",o = "[selected]",p = "'></div>",q = "[test!='']:sizzle",r = "nth",s = "*(?:",t = "*(?:(['\"])((?:\\\\.|[^\\\\])*?)\\3|(",u = "<a name='",v = "option",w = "image",x = "*([\\x20\\t\\r\\n\\f>+~])",y = "~=",z = "previousSibling",A = "*(even|odd|(([+-]|)(\\d*)n|)",B = "only",C = "*",D = "+|((?:^|[^\\\\])(?:\\\\.)*)",E = "i",F = "='$1']",G = "@",H = "w#",I = "^=",J = "*\\)|)",K = "+$",L = "=",M = ":focus",N = "id",O = "first",P = "'></a><div name='",Q = "$=",R = "reset",S = "string",T = "[\\x20\\t\\r\\n\\f]",U = "*(?:([+-]|)",V = "*((?:-\\d)?\\d*)",W = "#",X = "input",Y = "type",ba = "parentNode",bb = "(",bc = "w",bd = "password",be = "even",bf = "TAG",bg = "*[>+~]|",bh = "*\\]",bi = "*(?:\"\"|'')",bj = "*\\)|)(?=[^-]|$)",bk = "unsupported pseudo: ",bm = "w*",bn = " ",bo = "*,",bq = "text",br = "^",bs = ")",bt = ":(",bu = "[test^='']",bv = "radio",bw = "sizcache",bx = "button",by = "0",bz = "^(",bA = "<input type='hidden'/>",bB = "odd",bC = "class",bD = "*(\\d+)|))",bE = "<p test=''></p>",bF = "|=",bG = "\\[",bH = "<div class='hidden e'></div><div class='hidden'></div>",bI = "g",bJ = "submit",bK = "!=",bL = "<select><option selected=''></option></select>",bM = "e",bN = "checkbox",bO = "*=",bP = "|",bQ = ".",bR = "<select></select>",bS = "object",bT = "$1",bU = "file",bV = "eq",bW = "qx.bom.Selector",bX = "CHILD",bY = "|$)",ca = ",",cb = ":(even|odd|eq|gt|lt|nth|first|last)(?:\\(",cc = ")['\"]?\\]",cd = "<a href='#'></a>",ce = "empty",cf = ":enabled",cg = "[id='",ch = "(?:\\\\.|[-\\w]|[^\\x00-\\xa0])+",ci = "^#(",cj = "[*^$]=",ck = "^:(only|nth|first|last)-child(?:\\(",cl = "*(",cm = "^\\.(",cn = "",co = "href",cp = "multiple",cq = ")|[^:]|\\\\.)*|.*))\\)|)",cr = ")|)|)",cs = "POS",ct = "boolean",cu = "Syntax error, unrecognized expression: ",cv = "([*^$|!~]?=)",cw = "^\\[name=['\"]?(",cx = "\\$&",cy = ":checked",cz = "undefined",cA = "ID",cB = "last",cC = "HTML";
  qx.Bootstrap.define(bW, {
    statics : {
      query : null,
      matches : null
    }
  });
  (function(window, undefined){

    var cachedruns,assertGetIdNotName,Expr,getText,isXML,contains,compile,sortOrder,hasDuplicate,outermostContext,baseHasDuplicate = true,strundefined = cz,expando = (bw + Math.random()).replace(bQ, cn),Token = String,document = window.document,docElem = document.documentElement,dirruns = 0,done = 0,pop = [].pop,push = [].push,slice = [].slice,indexOf = [].indexOf || function(elem){

      var i = 0,len = this.length;
      for(;i < len;i++){

        if(this[i] === elem){

          return i;
        };
      };
      return -1;
    },markFunction = function(fn, value){

      fn[expando] = value == null || value;
      return fn;
    },createCache = function(){

      var cache = {
      },keys = [];
      return markFunction(function(key, value){

        if(keys.push(key) > Expr.cacheLength){

          delete cache[keys.shift()];
        };
        return (cache[key] = value);
      }, cache);
    },classCache = createCache(),tokenCache = createCache(),compilerCache = createCache(),whitespace = T,characterEncoding = ch,identifier = characterEncoding.replace(bc, H),operators = cv,attributes = bG + whitespace + cl + characterEncoding + bs + whitespace + s + operators + whitespace + t + identifier + cr + whitespace + bh,pseudos = bt + characterEncoding + n + attributes + cq,pos = cb + whitespace + V + whitespace + bj,rtrim = new RegExp(br + whitespace + D + whitespace + K, bI),rcomma = new RegExp(br + whitespace + bo + whitespace + C),rcombinators = new RegExp(br + whitespace + x + whitespace + C),rpseudo = new RegExp(pseudos),rquickExpr = /^(?:#([\w\-]+)|(\w+)|\.([\w\-]+))$/,rnot = /^:not/,rsibling = /[\x20\t\r\n\f]*[+~]/,rendsWithNot = /:not\($/,rheader = /h\d/i,rinputs = /input|select|textarea|button/i,rbackslash = /\\(?!\\)/g,matchExpr = {
      "ID" : new RegExp(ci + characterEncoding + bs),
      "CLASS" : new RegExp(cm + characterEncoding + bs),
      "NAME" : new RegExp(cw + characterEncoding + cc),
      "TAG" : new RegExp(bz + characterEncoding.replace(bc, bm) + bs),
      "ATTR" : new RegExp(br + attributes),
      "PSEUDO" : new RegExp(br + pseudos),
      "POS" : new RegExp(pos, E),
      "CHILD" : new RegExp(ck + whitespace + A + whitespace + U + whitespace + bD + whitespace + J, E),
      "needsContext" : new RegExp(br + whitespace + bg + pos, E)
    },assert = function(fn){

      var div = document.createElement(l);
      try{

        return fn(div);
      } catch(e) {

        return false;
      }finally{

        div = null;
      };
    },assertTagNameNoComments = assert(function(div){

      div.appendChild(document.createComment(cn));
      return !div.getElementsByTagName(C).length;
    }),assertHrefNotNormalized = assert(function(div){

      div.innerHTML = cd;
      return div.firstChild && typeof div.firstChild.getAttribute !== strundefined && div.firstChild.getAttribute(co) === W;
    }),assertAttributes = assert(function(div){

      div.innerHTML = bR;
      var type = typeof div.lastChild.getAttribute(cp);
      return type !== ct && type !== S;
    }),assertUsableClassName = assert(function(div){

      div.innerHTML = bH;
      if(!div.getElementsByClassName || !div.getElementsByClassName(bM).length){

        return false;
      };
      div.lastChild.className = bM;
      return div.getElementsByClassName(bM).length === 2;
    }),assertUsableName = assert(function(div){

      div.id = expando + 0;
      div.innerHTML = u + expando + P + expando + p;
      docElem.insertBefore(div, docElem.firstChild);
      var pass = document.getElementsByName && document.getElementsByName(expando).length === 2 + document.getElementsByName(expando + 0).length;
      assertGetIdNotName = !document.getElementById(expando);
      docElem.removeChild(div);
      return pass;
    });
    try{

      slice.call(docElem.childNodes, 0)[0].nodeType;
    } catch(e) {

      slice = function(i){

        var elem,results = [];
        for(;(elem = this[i]);i++){

          results.push(elem);
        };
        return results;
      };
    };
    function Sizzle(selector, context, results, seed){

      results = results || [];
      context = context || document;
      var match,elem,xml,m,nodeType = context.nodeType;
      if(!selector || typeof selector !== S){

        return results;
      };
      if(nodeType !== 1 && nodeType !== 9){

        return [];
      };
      xml = isXML(context);
      if(!xml && !seed){

        if((match = rquickExpr.exec(selector))){

          if((m = match[1])){

            if(nodeType === 9){

              elem = context.getElementById(m);
              if(elem && elem.parentNode){

                if(elem.id === m){

                  results.push(elem);
                  return results;
                };
              } else {

                return results;
              };
            } else {

              if(context.ownerDocument && (elem = context.ownerDocument.getElementById(m)) && contains(context, elem) && elem.id === m){

                results.push(elem);
                return results;
              };
            };
          } else if(match[2]){

            push.apply(results, slice.call(context.getElementsByTagName(selector), 0));
            return results;
          } else if((m = match[3]) && assertUsableClassName && context.getElementsByClassName){

            push.apply(results, slice.call(context.getElementsByClassName(m), 0));
            return results;
          };;
        };
      };
      return select(selector.replace(rtrim, bT), context, results, seed, xml);
    };
    Sizzle.matches = function(expr, elements){

      return Sizzle(expr, null, null, elements);
    };
    Sizzle.matchesSelector = function(elem, expr){

      return Sizzle(expr, null, null, [elem]).length > 0;
    };
    function createInputPseudo(type){

      return function(elem){

        var name = elem.nodeName.toLowerCase();
        return name === X && elem.type === type;
      };
    };
    function createButtonPseudo(type){

      return function(elem){

        var name = elem.nodeName.toLowerCase();
        return (name === X || name === bx) && elem.type === type;
      };
    };
    function createPositionalPseudo(fn){

      return markFunction(function(argument){

        argument = +argument;
        return markFunction(function(seed, matches){

          var j,matchIndexes = fn([], seed.length, argument),i = matchIndexes.length;
          while(i--){

            if(seed[(j = matchIndexes[i])]){

              seed[j] = !(matches[j] = seed[j]);
            };
          };
        });
      });
    };
    getText = Sizzle.getText = function(elem){

      var node,ret = cn,i = 0,nodeType = elem.nodeType;
      if(nodeType){

        if(nodeType === 1 || nodeType === 9 || nodeType === 11){

          if(typeof elem.textContent === S){

            return elem.textContent;
          } else {

            for(elem = elem.firstChild;elem;elem = elem.nextSibling){

              ret += getText(elem);
            };
          };
        } else if(nodeType === 3 || nodeType === 4){

          return elem.nodeValue;
        };
      } else {

        for(;(node = elem[i]);i++){

          ret += getText(node);
        };
      };
      return ret;
    };
    isXML = Sizzle.isXML = function(elem){

      var documentElement = elem && (elem.ownerDocument || elem).documentElement;
      return documentElement ? documentElement.nodeName !== cC : false;
    };
    contains = Sizzle.contains = docElem.contains ? function(a, b){

      var adown = a.nodeType === 9 ? a.documentElement : a,bup = b && b.parentNode;
      return a === bup || !!(bup && bup.nodeType === 1 && adown.contains && adown.contains(bup));
    } : docElem.compareDocumentPosition ? function(a, b){

      return b && !!(a.compareDocumentPosition(b) & 16);
    } : function(a, b){

      while((b = b.parentNode)){

        if(b === a){

          return true;
        };
      };
      return false;
    };
    Sizzle.attr = function(elem, name){

      var val,xml = isXML(elem);
      if(!xml){

        name = name.toLowerCase();
      };
      if((val = Expr.attrHandle[name])){

        return val(elem);
      };
      if(xml || assertAttributes){

        return elem.getAttribute(name);
      };
      val = elem.getAttributeNode(name);
      return val ? typeof elem[name] === ct ? elem[name] ? name : null : val.specified ? val.value : null : null;
    };
    Expr = Sizzle.selectors = {
      cacheLength : 50,
      createPseudo : markFunction,
      match : matchExpr,
      attrHandle : assertHrefNotNormalized ? {
      } : {
        "href" : function(elem){

          return elem.getAttribute(co, 2);
        },
        "type" : function(elem){

          return elem.getAttribute(Y);
        }
      },
      find : {
        "ID" : assertGetIdNotName ? function(id, context, xml){

          if(typeof context.getElementById !== strundefined && !xml){

            var m = context.getElementById(id);
            return m && m.parentNode ? [m] : [];
          };
        } : function(id, context, xml){

          if(typeof context.getElementById !== strundefined && !xml){

            var m = context.getElementById(id);
            return m ? m.id === id || typeof m.getAttributeNode !== strundefined && m.getAttributeNode(N).value === id ? [m] : undefined : [];
          };
        },
        "TAG" : assertTagNameNoComments ? function(tag, context){

          if(typeof context.getElementsByTagName !== strundefined){

            return context.getElementsByTagName(tag);
          };
        } : function(tag, context){

          var results = context.getElementsByTagName(tag);
          if(tag === C){

            var elem,tmp = [],i = 0;
            for(;(elem = results[i]);i++){

              if(elem.nodeType === 1){

                tmp.push(elem);
              };
            };
            return tmp;
          };
          return results;
        },
        "NAME" : assertUsableName && function(tag, context){

          if(typeof context.getElementsByName !== strundefined){

            return context.getElementsByName(name);
          };
        },
        "CLASS" : assertUsableClassName && function(className, context, xml){

          if(typeof context.getElementsByClassName !== strundefined && !xml){

            return context.getElementsByClassName(className);
          };
        }
      },
      relative : {
        ">" : {
          dir : ba,
          first : true
        },
        " " : {
          dir : ba
        },
        "+" : {
          dir : z,
          first : true
        },
        "~" : {
          dir : z
        }
      },
      preFilter : {
        "ATTR" : function(match){

          match[1] = match[1].replace(rbackslash, cn);
          match[3] = (match[4] || match[5] || cn).replace(rbackslash, cn);
          if(match[2] === y){

            match[3] = bn + match[3] + bn;
          };
          return match.slice(0, 4);
        },
        "CHILD" : function(match){

          match[1] = match[1].toLowerCase();
          if(match[1] === r){

            if(!match[2]){

              Sizzle.error(match[0]);
            };
            match[3] = +(match[3] ? match[4] + (match[5] || 1) : 2 * (match[2] === be || match[2] === bB));
            match[4] = +((match[6] + match[7]) || match[2] === bB);
          } else if(match[2]){

            Sizzle.error(match[0]);
          };
          return match;
        },
        "PSEUDO" : function(match){

          var unquoted,excess;
          if(matchExpr[bX].test(match[0])){

            return null;
          };
          if(match[3]){

            match[2] = match[3];
          } else if((unquoted = match[4])){

            if(rpseudo.test(unquoted) && (excess = tokenize(unquoted, true)) && (excess = unquoted.indexOf(bs, unquoted.length - excess) - unquoted.length)){

              unquoted = unquoted.slice(0, excess);
              match[0] = match[0].slice(0, excess);
            };
            match[2] = unquoted;
          };
          return match.slice(0, 3);
        }
      },
      filter : {
        "ID" : assertGetIdNotName ? function(id){

          id = id.replace(rbackslash, cn);
          return function(elem){

            return elem.getAttribute(N) === id;
          };
        } : function(id){

          id = id.replace(rbackslash, cn);
          return function(elem){

            var node = typeof elem.getAttributeNode !== strundefined && elem.getAttributeNode(N);
            return node && node.value === id;
          };
        },
        "TAG" : function(nodeName){

          if(nodeName === C){

            return function(){

              return true;
            };
          };
          nodeName = nodeName.replace(rbackslash, cn).toLowerCase();
          return function(elem){

            return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;
          };
        },
        "CLASS" : function(className){

          var pattern = classCache[expando][className];
          if(!pattern){

            pattern = classCache(className, new RegExp(f + whitespace + bs + className + bb + whitespace + bY));
          };
          return function(elem){

            return pattern.test(elem.className || (typeof elem.getAttribute !== strundefined && elem.getAttribute(bC)) || cn);
          };
        },
        "ATTR" : function(name, operator, check){

          return function(elem, context){

            var result = Sizzle.attr(elem, name);
            if(result == null){

              return operator === bK;
            };
            if(!operator){

              return true;
            };
            result += cn;
            return operator === L ? result === check : operator === bK ? result !== check : operator === I ? check && result.indexOf(check) === 0 : operator === bO ? check && result.indexOf(check) > -1 : operator === Q ? check && result.substr(result.length - check.length) === check : operator === y ? (bn + result + bn).indexOf(check) > -1 : operator === bF ? result === check || result.substr(0, check.length + 1) === check + c : false;
          };
        },
        "CHILD" : function(type, argument, first, last){

          if(type === r){

            return function(elem){

              var node,diff,parent = elem.parentNode;
              if(first === 1 && last === 0){

                return true;
              };
              if(parent){

                diff = 0;
                for(node = parent.firstChild;node;node = node.nextSibling){

                  if(node.nodeType === 1){

                    diff++;
                    if(elem === node){

                      break;
                    };
                  };
                };
              };
              diff -= last;
              return diff === first || (diff % first === 0 && diff / first >= 0);
            };
          };
          return function(elem){

            var node = elem;
            switch(type){case B:case O:
            while((node = node.previousSibling)){

              if(node.nodeType === 1){

                return false;
              };
            };
            if(type === O){

              return true;
            };
            node = elem;case cB:
            while((node = node.nextSibling)){

              if(node.nodeType === 1){

                return false;
              };
            };
            return true;};
          };
        },
        "PSEUDO" : function(pseudo, argument){

          var args,fn = Expr.pseudos[pseudo] || Expr.setFilters[pseudo.toLowerCase()] || Sizzle.error(bk + pseudo);
          if(fn[expando]){

            return fn(argument);
          };
          if(fn.length > 1){

            args = [pseudo, pseudo, cn, argument];
            return Expr.setFilters.hasOwnProperty(pseudo.toLowerCase()) ? markFunction(function(seed, matches){

              var idx,matched = fn(seed, argument),i = matched.length;
              while(i--){

                idx = indexOf.call(seed, matched[i]);
                seed[idx] = !(matches[idx] = matched[i]);
              };
            }) : function(elem){

              return fn(elem, 0, args);
            };
          };
          return fn;
        }
      },
      pseudos : {
        "not" : markFunction(function(selector){

          var input = [],results = [],matcher = compile(selector.replace(rtrim, bT));
          return matcher[expando] ? markFunction(function(seed, matches, context, xml){

            var elem,unmatched = matcher(seed, null, xml, []),i = seed.length;
            while(i--){

              if((elem = unmatched[i])){

                seed[i] = !(matches[i] = elem);
              };
            };
          }) : function(elem, context, xml){

            input[0] = elem;
            matcher(input, null, xml, results);
            return !results.pop();
          };
        }),
        "has" : markFunction(function(selector){

          return function(elem){

            return Sizzle(selector, elem).length > 0;
          };
        }),
        "contains" : markFunction(function(text){

          return function(elem){

            return (elem.textContent || elem.innerText || getText(elem)).indexOf(text) > -1;
          };
        }),
        "enabled" : function(elem){

          return elem.disabled === false;
        },
        "disabled" : function(elem){

          return elem.disabled === true;
        },
        "checked" : function(elem){

          var nodeName = elem.nodeName.toLowerCase();
          return (nodeName === X && !!elem.checked) || (nodeName === v && !!elem.selected);
        },
        "selected" : function(elem){

          if(elem.parentNode){

            elem.parentNode.selectedIndex;
          };
          return elem.selected === true;
        },
        "parent" : function(elem){

          return !Expr.pseudos[ce](elem);
        },
        "empty" : function(elem){

          var nodeType;
          elem = elem.firstChild;
          while(elem){

            if(elem.nodeName > G || (nodeType = elem.nodeType) === 3 || nodeType === 4){

              return false;
            };
            elem = elem.nextSibling;
          };
          return true;
        },
        "header" : function(elem){

          return rheader.test(elem.nodeName);
        },
        "text" : function(elem){

          var type,attr;
          return elem.nodeName.toLowerCase() === X && (type = elem.type) === bq && ((attr = elem.getAttribute(Y)) == null || attr.toLowerCase() === type);
        },
        "radio" : createInputPseudo(bv),
        "checkbox" : createInputPseudo(bN),
        "file" : createInputPseudo(bU),
        "password" : createInputPseudo(bd),
        "image" : createInputPseudo(w),
        "submit" : createButtonPseudo(bJ),
        "reset" : createButtonPseudo(R),
        "button" : function(elem){

          var name = elem.nodeName.toLowerCase();
          return name === X && elem.type === bx || name === bx;
        },
        "input" : function(elem){

          return rinputs.test(elem.nodeName);
        },
        "focus" : function(elem){

          var doc = elem.ownerDocument;
          return elem === doc.activeElement && (!doc.hasFocus || doc.hasFocus()) && !!(elem.type || elem.href);
        },
        "active" : function(elem){

          return elem === elem.ownerDocument.activeElement;
        },
        "first" : createPositionalPseudo(function(matchIndexes, length, argument){

          return [0];
        }),
        "last" : createPositionalPseudo(function(matchIndexes, length, argument){

          return [length - 1];
        }),
        "eq" : createPositionalPseudo(function(matchIndexes, length, argument){

          return [argument < 0 ? argument + length : argument];
        }),
        "even" : createPositionalPseudo(function(matchIndexes, length, argument){

          for(var i = 0;i < length;i += 2){

            matchIndexes.push(i);
          };
          return matchIndexes;
        }),
        "odd" : createPositionalPseudo(function(matchIndexes, length, argument){

          for(var i = 1;i < length;i += 2){

            matchIndexes.push(i);
          };
          return matchIndexes;
        }),
        "lt" : createPositionalPseudo(function(matchIndexes, length, argument){

          for(var i = argument < 0 ? argument + length : argument;--i >= 0;){

            matchIndexes.push(i);
          };
          return matchIndexes;
        }),
        "gt" : createPositionalPseudo(function(matchIndexes, length, argument){

          for(var i = argument < 0 ? argument + length : argument;++i < length;){

            matchIndexes.push(i);
          };
          return matchIndexes;
        })
      }
    };
    function siblingCheck(a, b, ret){

      if(a === b){

        return ret;
      };
      var cur = a.nextSibling;
      while(cur){

        if(cur === b){

          return -1;
        };
        cur = cur.nextSibling;
      };
      return 1;
    };
    sortOrder = docElem.compareDocumentPosition ? function(a, b){

      if(a === b){

        hasDuplicate = true;
        return 0;
      };
      return (!a.compareDocumentPosition || !b.compareDocumentPosition ? a.compareDocumentPosition : a.compareDocumentPosition(b) & 4) ? -1 : 1;
    } : function(a, b){

      if(a === b){

        hasDuplicate = true;
        return 0;
      } else if(a.sourceIndex && b.sourceIndex){

        return a.sourceIndex - b.sourceIndex;
      };
      var al,bl,ap = [],bp = [],aup = a.parentNode,bup = b.parentNode,cur = aup;
      if(aup === bup){

        return siblingCheck(a, b);
      } else if(!aup){

        return -1;
      } else if(!bup){

        return 1;
      };;
      while(cur){

        ap.unshift(cur);
        cur = cur.parentNode;
      };
      cur = bup;
      while(cur){

        bp.unshift(cur);
        cur = cur.parentNode;
      };
      al = ap.length;
      bl = bp.length;
      for(var i = 0;i < al && i < bl;i++){

        if(ap[i] !== bp[i]){

          return siblingCheck(ap[i], bp[i]);
        };
      };
      return i === al ? siblingCheck(a, bp[i], -1) : siblingCheck(ap[i], b, 1);
    };
    [0, 0].sort(sortOrder);
    baseHasDuplicate = !hasDuplicate;
    Sizzle.uniqueSort = function(results){

      var elem,i = 1;
      hasDuplicate = baseHasDuplicate;
      results.sort(sortOrder);
      if(hasDuplicate){

        for(;(elem = results[i]);i++){

          if(elem === results[i - 1]){

            results.splice(i--, 1);
          };
        };
      };
      return results;
    };
    Sizzle.error = function(msg){

      throw new Error(cu + msg);
    };
    function tokenize(selector, parseOnly){

      var matched,match,tokens,type,soFar,groups,preFilters,cached = tokenCache[expando][selector];
      if(cached){

        return parseOnly ? 0 : cached.slice(0);
      };
      soFar = selector;
      groups = [];
      preFilters = Expr.preFilter;
      while(soFar){

        if(!matched || (match = rcomma.exec(soFar))){

          if(match){

            soFar = soFar.slice(match[0].length);
          };
          groups.push(tokens = []);
        };
        matched = false;
        if((match = rcombinators.exec(soFar))){

          tokens.push(matched = new Token(match.shift()));
          soFar = soFar.slice(matched.length);
          matched.type = match[0].replace(rtrim, bn);
        };
        for(type in Expr.filter){

          if((match = matchExpr[type].exec(soFar)) && (!preFilters[type] || (match = preFilters[type](match, document, true)))){

            tokens.push(matched = new Token(match.shift()));
            soFar = soFar.slice(matched.length);
            matched.type = type;
            matched.matches = match;
          };
        };
        if(!matched){

          break;
        };
      };
      return parseOnly ? soFar.length : soFar ? Sizzle.error(selector) : tokenCache(selector, groups).slice(0);
    };
    function addCombinator(matcher, combinator, base){

      var dir = combinator.dir,checkNonElements = base && combinator.dir === ba,doneName = done++;
      return combinator.first ? function(elem, context, xml){

        while((elem = elem[dir])){

          if(checkNonElements || elem.nodeType === 1){

            return matcher(elem, context, xml);
          };
        };
      } : function(elem, context, xml){

        if(!xml){

          var cache,dirkey = dirruns + bn + doneName + bn,cachedkey = dirkey + cachedruns;
          while((elem = elem[dir])){

            if(checkNonElements || elem.nodeType === 1){

              if((cache = elem[expando]) === cachedkey){

                return elem.sizset;
              } else if(typeof cache === S && cache.indexOf(dirkey) === 0){

                if(elem.sizset){

                  return elem;
                };
              } else {

                elem[expando] = cachedkey;
                if(matcher(elem, context, xml)){

                  elem.sizset = true;
                  return elem;
                };
                elem.sizset = false;
              };
            };
          };
        } else {

          while((elem = elem[dir])){

            if(checkNonElements || elem.nodeType === 1){

              if(matcher(elem, context, xml)){

                return elem;
              };
            };
          };
        };
      };
    };
    function elementMatcher(matchers){

      return matchers.length > 1 ? function(elem, context, xml){

        var i = matchers.length;
        while(i--){

          if(!matchers[i](elem, context, xml)){

            return false;
          };
        };
        return true;
      } : matchers[0];
    };
    function condense(unmatched, map, filter, context, xml){

      var elem,newUnmatched = [],i = 0,len = unmatched.length,mapped = map != null;
      for(;i < len;i++){

        if((elem = unmatched[i])){

          if(!filter || filter(elem, context, xml)){

            newUnmatched.push(elem);
            if(mapped){

              map.push(i);
            };
          };
        };
      };
      return newUnmatched;
    };
    function setMatcher(preFilter, selector, matcher, postFilter, postFinder, postSelector){

      if(postFilter && !postFilter[expando]){

        postFilter = setMatcher(postFilter);
      };
      if(postFinder && !postFinder[expando]){

        postFinder = setMatcher(postFinder, postSelector);
      };
      return markFunction(function(seed, results, context, xml){

        if(seed && postFinder){

          return;
        };
        var i,elem,postFilterIn,preMap = [],postMap = [],preexisting = results.length,elems = seed || multipleContexts(selector || C, context.nodeType ? [context] : context, [], seed),matcherIn = preFilter && (seed || !selector) ? condense(elems, preMap, preFilter, context, xml) : elems,matcherOut = matcher ? postFinder || (seed ? preFilter : preexisting || postFilter) ? [] : results : matcherIn;
        if(matcher){

          matcher(matcherIn, matcherOut, context, xml);
        };
        if(postFilter){

          postFilterIn = condense(matcherOut, postMap);
          postFilter(postFilterIn, [], context, xml);
          i = postFilterIn.length;
          while(i--){

            if((elem = postFilterIn[i])){

              matcherOut[postMap[i]] = !(matcherIn[postMap[i]] = elem);
            };
          };
        };
        if(seed){

          i = preFilter && matcherOut.length;
          while(i--){

            if((elem = matcherOut[i])){

              seed[preMap[i]] = !(results[preMap[i]] = elem);
            };
          };
        } else {

          matcherOut = condense(matcherOut === results ? matcherOut.splice(preexisting, matcherOut.length) : matcherOut);
          if(postFinder){

            postFinder(null, results, matcherOut, xml);
          } else {

            push.apply(results, matcherOut);
          };
        };
      });
    };
    function matcherFromTokens(tokens){

      var checkContext,matcher,j,len = tokens.length,leadingRelative = Expr.relative[tokens[0].type],implicitRelative = leadingRelative || Expr.relative[bn],i = leadingRelative ? 1 : 0,matchContext = addCombinator(function(elem){

        return elem === checkContext;
      }, implicitRelative, true),matchAnyContext = addCombinator(function(elem){

        return indexOf.call(checkContext, elem) > -1;
      }, implicitRelative, true),matchers = [function(elem, context, xml){

        return (!leadingRelative && (xml || context !== outermostContext)) || ((checkContext = context).nodeType ? matchContext(elem, context, xml) : matchAnyContext(elem, context, xml));
      }];
      for(;i < len;i++){

        if((matcher = Expr.relative[tokens[i].type])){

          matchers = [addCombinator(elementMatcher(matchers), matcher)];
        } else {

          matcher = Expr.filter[tokens[i].type].apply(null, tokens[i].matches);
          if(matcher[expando]){

            j = ++i;
            for(;j < len;j++){

              if(Expr.relative[tokens[j].type]){

                break;
              };
            };
            return setMatcher(i > 1 && elementMatcher(matchers), i > 1 && tokens.slice(0, i - 1).join(cn).replace(rtrim, bT), matcher, i < j && matcherFromTokens(tokens.slice(i, j)), j < len && matcherFromTokens((tokens = tokens.slice(j))), j < len && tokens.join(cn));
          };
          matchers.push(matcher);
        };
      };
      return elementMatcher(matchers);
    };
    function matcherFromGroupMatchers(elementMatchers, setMatchers){

      var bySet = setMatchers.length > 0,byElement = elementMatchers.length > 0,superMatcher = function(seed, context, xml, results, expandContext){

        var elem,j,matcher,setMatched = [],matchedCount = 0,i = by,unmatched = seed && [],outermost = expandContext != null,contextBackup = outermostContext,elems = seed || byElement && Expr.find[bf](C, expandContext && context.parentNode || context),dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.E);
        if(outermost){

          outermostContext = context !== document && context;
          cachedruns = superMatcher.el;
        };
        for(;(elem = elems[i]) != null;i++){

          if(byElement && elem){

            for(j = 0;(matcher = elementMatchers[j]);j++){

              if(matcher(elem, context, xml)){

                results.push(elem);
                break;
              };
            };
            if(outermost){

              dirruns = dirrunsUnique;
              cachedruns = ++superMatcher.el;
            };
          };
          if(bySet){

            if((elem = !matcher && elem)){

              matchedCount--;
            };
            if(seed){

              unmatched.push(elem);
            };
          };
        };
        matchedCount += i;
        if(bySet && i !== matchedCount){

          for(j = 0;(matcher = setMatchers[j]);j++){

            matcher(unmatched, setMatched, context, xml);
          };
          if(seed){

            if(matchedCount > 0){

              while(i--){

                if(!(unmatched[i] || setMatched[i])){

                  setMatched[i] = pop.call(results);
                };
              };
            };
            setMatched = condense(setMatched);
          };
          push.apply(results, setMatched);
          if(outermost && !seed && setMatched.length > 0 && (matchedCount + setMatchers.length) > 1){

            Sizzle.uniqueSort(results);
          };
        };
        if(outermost){

          dirruns = dirrunsUnique;
          outermostContext = contextBackup;
        };
        return unmatched;
      };
      superMatcher.el = 0;
      return bySet ? markFunction(superMatcher) : superMatcher;
    };
    compile = Sizzle.compile = function(selector, group){

      var i,setMatchers = [],elementMatchers = [],cached = compilerCache[expando][selector];
      if(!cached){

        if(!group){

          group = tokenize(selector);
        };
        i = group.length;
        while(i--){

          cached = matcherFromTokens(group[i]);
          if(cached[expando]){

            setMatchers.push(cached);
          } else {

            elementMatchers.push(cached);
          };
        };
        cached = compilerCache(selector, matcherFromGroupMatchers(elementMatchers, setMatchers));
      };
      return cached;
    };
    function multipleContexts(selector, contexts, results, seed){

      var i = 0,len = contexts.length;
      for(;i < len;i++){

        Sizzle(selector, contexts[i], results, seed);
      };
      return results;
    };
    function select(selector, context, results, seed, xml){

      var i,tokens,token,type,find,match = tokenize(selector),j = match.length;
      if(!seed){

        if(match.length === 1){

          tokens = match[0] = match[0].slice(0);
          if(tokens.length > 2 && (token = tokens[0]).type === cA && context.nodeType === 9 && !xml && Expr.relative[tokens[1].type]){

            context = Expr.find[cA](token.matches[0].replace(rbackslash, cn), context, xml)[0];
            if(!context){

              return results;
            };
            selector = selector.slice(tokens.shift().length);
          };
          for(i = matchExpr[cs].test(selector) ? -1 : tokens.length - 1;i >= 0;i--){

            token = tokens[i];
            if(Expr.relative[(type = token.type)]){

              break;
            };
            if((find = Expr.find[type])){

              if((seed = find(token.matches[0].replace(rbackslash, cn), rsibling.test(tokens[0].type) && context.parentNode || context, xml))){

                tokens.splice(i, 1);
                selector = seed.length && tokens.join(cn);
                if(!selector){

                  push.apply(results, slice.call(seed, 0));
                  return results;
                };
                break;
              };
            };
          };
        };
      };
      compile(selector, match)(seed, context, xml, results, rsibling.test(selector));
      return results;
    };
    if(document.querySelectorAll){

      (function(){

        var disconnectedMatch,oldSelect = select,rescape = /'|\\/g,rattributeQuotes = /\=[\x20\t\r\n\f]*([^'"\]]*)[\x20\t\r\n\f]*\]/g,rbuggyQSA = [M],rbuggyMatches = [h, M],matches = docElem.matchesSelector || docElem.mozMatchesSelector || docElem.webkitMatchesSelector || docElem.oMatchesSelector || docElem.msMatchesSelector;
        assert(function(div){

          div.innerHTML = bL;
          if(!div.querySelectorAll(o).length){

            rbuggyQSA.push(bG + whitespace + d);
          };
          if(!div.querySelectorAll(cy).length){

            rbuggyQSA.push(cy);
          };
        });
        assert(function(div){

          div.innerHTML = bE;
          if(div.querySelectorAll(bu).length){

            rbuggyQSA.push(cj + whitespace + bi);
          };
          div.innerHTML = bA;
          if(!div.querySelectorAll(cf).length){

            rbuggyQSA.push(cf, k);
          };
        });
        rbuggyQSA = new RegExp(rbuggyQSA.join(bP));
        select = function(selector, context, results, seed, xml){

          if(!seed && !xml && (!rbuggyQSA || !rbuggyQSA.test(selector))){

            var groups,i,old = true,nid = expando,newContext = context,newSelector = context.nodeType === 9 && selector;
            if(context.nodeType === 1 && context.nodeName.toLowerCase() !== bS){

              groups = tokenize(selector);
              if((old = context.getAttribute(N))){

                nid = old.replace(rescape, cx);
              } else {

                context.setAttribute(N, nid);
              };
              nid = cg + nid + g;
              i = groups.length;
              while(i--){

                groups[i] = nid + groups[i].join(cn);
              };
              newContext = rsibling.test(selector) && context.parentNode || context;
              newSelector = groups.join(ca);
            };
            if(newSelector){

              try{

                push.apply(results, slice.call(newContext.querySelectorAll(newSelector), 0));
                return results;
              } catch(qsaError) {
              }finally{

                if(!old){

                  context.removeAttribute(N);
                };
              };
            };
          };
          return oldSelect(selector, context, results, seed, xml);
        };
        if(matches){

          assert(function(div){

            disconnectedMatch = matches.call(div, l);
            try{

              matches.call(div, q);
              rbuggyMatches.push(bK, pseudos);
            } catch(e) {
            };
          });
          rbuggyMatches = new RegExp(rbuggyMatches.join(bP));
          Sizzle.matchesSelector = function(elem, expr){

            expr = expr.replace(rattributeQuotes, F);
            if(!isXML(elem) && !rbuggyMatches.test(expr) && (!rbuggyQSA || !rbuggyQSA.test(expr))){

              try{

                var ret = matches.call(elem, expr);
                if(ret || disconnectedMatch || elem.document && elem.document.nodeType !== 11){

                  return ret;
                };
              } catch(e) {
              };
            };
            return Sizzle(expr, null, null, [elem]).length > 0;
          };
        };
      })();
    };
    Expr.pseudos[r] = Expr.pseudos[bV];
    function setFilters(){
    };
    Expr.filters = setFilters.prototype = Expr.pseudos;
    Expr.setFilters = new setFilters();
    qx.bom.Selector.query = function(selector, context){

      return Sizzle(selector, context);
    };
    qx.bom.Selector.matches = function(selector, set){

      return Sizzle(selector, null, null, set);
    };
  })(window);
})();
(function(){

  var a = "",b = "block",c = "none",d = "hidden",e = "absolute",f = "qx.module.Css",g = "display";
  qx.Bootstrap.define(f, {
    statics : {
      setStyle : function(name, value){

        if(/\w-\w/.test(name)){

          name = qx.lang.String.camelCase(name);
        };
        this._forEachElement(function(item){

          qx.bom.element.Style.set(item, name, value);
        });
        return this;
      },
      getStyle : function(name){

        if(this[0] && qx.dom.Node.isElement(this[0])){

          if(/\w-\w/.test(name)){

            name = qx.lang.String.camelCase(name);
          };
          return qx.bom.element.Style.get(this[0], name);
        };
        return null;
      },
      setStyles : function(styles){

        for(var name in styles){

          this.setStyle(name, styles[name]);
        };
        return this;
      },
      getStyles : function(names){

        var styles = {
        };
        for(var i = 0;i < names.length;i++){

          styles[names[i]] = this.getStyle(names[i]);
        };
        return styles;
      },
      addClass : function(name){

        this._forEachElement(function(item){

          qx.bom.element.Class.add(item, name);
        });
        return this;
      },
      addClasses : function(names){

        this._forEachElement(function(item){

          qx.bom.element.Class.addClasses(item, names);
        });
        return this;
      },
      removeClass : function(name){

        this._forEachElement(function(item){

          qx.bom.element.Class.remove(item, name);
        });
        return this;
      },
      removeClasses : function(names){

        this._forEachElement(function(item){

          qx.bom.element.Class.removeClasses(item, names);
        });
        return this;
      },
      hasClass : function(name){

        if(!this[0] || !qx.dom.Node.isElement(this[0])){

          return false;
        };
        return qx.bom.element.Class.has(this[0], name);
      },
      getClass : function(){

        if(!this[0] || !qx.dom.Node.isElement(this[0])){

          return a;
        };
        return qx.bom.element.Class.get(this[0]);
      },
      toggleClass : function(name){

        var bCls = qx.bom.element.Class;
        this._forEachElement(function(item){

          bCls.has(item, name) ? bCls.remove(item, name) : bCls.add(item, name);
        });
        return this;
      },
      toggleClasses : function(names){

        for(var i = 0,l = names.length;i < l;i++){

          this.toggleClass(names[i]);
        };
        return this;
      },
      replaceClass : function(oldName, newName){

        this._forEachElement(function(item){

          qx.bom.element.Class.replace(item, oldName, newName);
        });
        return this;
      },
      getHeight : function(force){

        var elem = this[0];
        if(elem){

          if(qx.dom.Node.isElement(elem)){

            var elementHeight;
            if(force){

              var stylesToSwap = {
                display : b,
                position : e,
                visibility : d
              };
              elementHeight = qx.module.Css.__iN(elem, stylesToSwap, qx.module.Css.getHeight, this);
            } else {

              elementHeight = qx.bom.element.Dimension.getHeight(elem);
            };
            return elementHeight;
          } else if(qx.dom.Node.isDocument(elem)){

            return qx.bom.Document.getHeight(qx.dom.Node.getWindow(elem));
          } else if(qx.dom.Node.isWindow(elem)){

            return qx.bom.Viewport.getHeight(elem);
          };;
        };
        return null;
      },
      getWidth : function(force){

        var elem = this[0];
        if(elem){

          if(qx.dom.Node.isElement(elem)){

            var elementWidth;
            if(force){

              var stylesToSwap = {
                display : b,
                position : e,
                visibility : d
              };
              elementWidth = qx.module.Css.__iN(elem, stylesToSwap, qx.module.Css.getWidth, this);
            } else {

              elementWidth = qx.bom.element.Dimension.getWidth(elem);
            };
            return elementWidth;
          } else if(qx.dom.Node.isDocument(elem)){

            return qx.bom.Document.getWidth(qx.dom.Node.getWindow(elem));
          } else if(qx.dom.Node.isWindow(elem)){

            return qx.bom.Viewport.getWidth(elem);
          };;
        };
        return null;
      },
      getOffset : function(mode){

        var elem = this[0];
        if(elem && qx.dom.Node.isElement(elem)){

          return qx.bom.element.Location.get(elem, mode);
        };
        return null;
      },
      getContentHeight : function(force){

        var obj = this[0];
        if(qx.dom.Node.isElement(obj)){

          var contentHeight;
          if(force){

            var stylesToSwap = {
              position : e,
              visibility : d,
              display : b
            };
            contentHeight = qx.module.Css.__iN(obj, stylesToSwap, qx.module.Css.getContentHeight, this);
          } else {

            contentHeight = qx.bom.element.Dimension.getContentHeight(obj);
          };
          return contentHeight;
        };
        return null;
      },
      getContentWidth : function(force){

        var obj = this[0];
        if(qx.dom.Node.isElement(obj)){

          var contentWidth;
          if(force){

            var stylesToSwap = {
              position : e,
              visibility : d,
              display : b
            };
            contentWidth = qx.module.Css.__iN(obj, stylesToSwap, qx.module.Css.getContentWidth, this);
          } else {

            contentWidth = qx.bom.element.Dimension.getContentWidth(obj);
          };
          return contentWidth;
        };
        return null;
      },
      getPosition : function(){

        var obj = this[0];
        if(qx.dom.Node.isElement(obj)){

          return qx.bom.element.Location.getPosition(obj);
        };
        return null;
      },
      includeStylesheet : function(uri, doc){

        qx.bom.Stylesheet.includeFile(uri, doc);
      },
      hide : function(){

        this._forEachElementWrapped(function(item){

          var prevStyle = item.getStyle(g);
          if(prevStyle !== c){

            item[0].$$qPrevDisp = prevStyle;
            item.setStyle(g, c);
          };
        });
        return this;
      },
      show : function(){

        this._forEachElementWrapped(function(item){

          var currentVal = item.getStyle(g);
          var prevVal = item[0].$$qPrevDisp;
          var newVal;
          if(currentVal == c){

            if(prevVal && prevVal != c){

              newVal = prevVal;
            } else {

              var doc = qxWeb.getDocument(item[0]);
              newVal = qx.module.Css.__iM(item[0].tagName, doc);
            };
            item.setStyle(g, newVal);
            item[0].$$qPrevDisp = c;
          };
        });
        return this;
      },
      __iL : {
      },
      __iM : function(tagName, doc){

        var defaults = qx.module.Css.__iL;
        if(!defaults[tagName]){

          var docu = doc || document;
          var tempEl = qxWeb(docu.createElement(tagName)).appendTo(doc.body);
          defaults[tagName] = tempEl.getStyle(g);
          tempEl.remove();
        };
        return defaults[tagName] || a;
      },
      __iN : function(element, styles, callback, context){

        var currentValues = {
        };
        for(var styleProperty in styles){

          currentValues[styleProperty] = element.style[styleProperty];
          element.style[styleProperty] = styles[styleProperty];
        };
        var value = callback.call(context);
        for(var styleProperty in currentValues){

          element.style[styleProperty] = currentValues[styleProperty];
        };
        return value;
      }
    },
    defer : function(statics){

      qxWeb.$attach({
        "setStyle" : statics.setStyle,
        "getStyle" : statics.getStyle,
        "setStyles" : statics.setStyles,
        "getStyles" : statics.getStyles,
        "addClass" : statics.addClass,
        "addClasses" : statics.addClasses,
        "removeClass" : statics.removeClass,
        "removeClasses" : statics.removeClasses,
        "hasClass" : statics.hasClass,
        "getClass" : statics.getClass,
        "toggleClass" : statics.toggleClass,
        "toggleClasses" : statics.toggleClasses,
        "replaceClass" : statics.replaceClass,
        "getHeight" : statics.getHeight,
        "getWidth" : statics.getWidth,
        "getOffset" : statics.getOffset,
        "getContentHeight" : statics.getContentHeight,
        "getContentWidth" : statics.getContentWidth,
        "getPosition" : statics.getPosition,
        "hide" : statics.hide,
        "show" : statics.show
      });
      qxWeb.$attachStatic({
        "includeStylesheet" : statics.includeStylesheet
      });
    }
  });
})();
(function(){

  var a = "qx.module.Polyfill";
  qx.Bootstrap.define(a, {
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "*",d = "mouseover",e = "mouseout",f = "load",g = "left",h = "qx.module.Event",n = "undefined",o = "DOMContentLoaded",p = "browser.documentmode",q = "complete";
  qx.Bootstrap.define(h, {
    statics : {
      __iO : {
      },
      __iP : {
        on : {
        },
        off : {
        }
      },
      on : function(type, listener, context, useCapture){

        for(var i = 0;i < this.length;i++){

          var el = this[i];
          var ctx = context || qxWeb(el);
          var hooks = qx.module.Event.__iP.on;
          var typeHooks = hooks[c] || [];
          if(hooks[type]){

            typeHooks = typeHooks.concat(hooks[type]);
          };
          for(var j = 0,m = typeHooks.length;j < m;j++){

            typeHooks[j](el, type, listener, context);
          };
          var bound = function(event){

            var registry = qx.module.Event.__iO;
            var normalizations = registry[c] || [];
            if(registry[type]){

              normalizations = normalizations.concat(registry[type]);
            };
            for(var x = 0,y = normalizations.length;x < y;x++){

              event = normalizations[x](event, el, type);
            };
            listener.apply(this, [event]);
          }.bind(ctx);
          bound.original = listener;
          if(qx.bom.Event.supportsEvent(el, type)){

            qx.bom.Event.addNativeListener(el, type, bound, useCapture);
          };
          if(!el.__dH){

            el.__dH = new qx.event.Emitter();
          };
          var id = el.__dH.on(type, bound, ctx);
          if(!el.__fB){

            el.__fB = {
            };
          };
          if(!el.__fB[type]){

            el.__fB[type] = {
            };
          };
          el.__fB[type][id] = bound;
          if(!context){

            if(!el.__iQ){

              el.__iQ = {
              };
            };
            el.__iQ[id] = ctx;
          };
        };
        return this;
      },
      off : function(type, listener, context, useCapture){

        var removeAll = (listener === null && context === null);
        for(var j = 0;j < this.length;j++){

          var el = this[j];
          if(!el.__fB){

            continue;
          };
          var types = [];
          if(type !== null){

            types.push(type);
          } else {

            for(var listenerType in el.__fB){

              types.push(listenerType);
            };
          };
          for(var i = 0,l = types.length;i < l;i++){

            for(var id in el.__fB[types[i]]){

              var storedListener = el.__fB[types[i]][id];
              if(removeAll || storedListener == listener || storedListener.original == listener){

                var hasStoredContext = typeof el.__iQ !== n && el.__iQ[id];
                var storedContext;
                if(!context && hasStoredContext){

                  storedContext = el.__iQ[id];
                };
                el.__dH.off(types[i], storedListener, storedContext || context);
                if(removeAll || storedListener.original == listener){

                  qx.bom.Event.removeNativeListener(el, types[i], storedListener, useCapture);
                };
                delete el.__fB[types[i]][id];
                if(hasStoredContext){

                  delete el.__iQ[id];
                };
              };
            };
            var hooks = qx.module.Event.__iP.off;
            var typeHooks = hooks[c] || [];
            if(hooks[type]){

              typeHooks = typeHooks.concat(hooks[type]);
            };
            for(var k = 0,m = typeHooks.length;k < m;k++){

              typeHooks[k](el, type, listener, context);
            };
          };
        };
        return this;
      },
      allOff : function(type){

        return this.off(type || null, null, null);
      },
      emit : function(type, data){

        for(var j = 0;j < this.length;j++){

          var el = this[j];
          if(el.__dH){

            el.__dH.emit(type, data);
          };
        };
        return this;
      },
      once : function(type, listener, context){

        var self = this;
        var wrappedListener = function(data){

          self.off(type, wrappedListener, context);
          listener.call(this, data);
        };
        this.on(type, wrappedListener, context);
        return this;
      },
      hasListener : function(type, listener, context){

        if(!this[0] || !this[0].__dH || !this[0].__dH.getListeners()[type]){

          return false;
        };
        if(listener){

          var attachedListeners = this[0].__dH.getListeners()[type];
          for(var i = 0;i < attachedListeners.length;i++){

            var hasListener = false;
            if(attachedListeners[i].listener == listener){

              hasListener = true;
            };
            if(attachedListeners[i].listener.original && attachedListeners[i].listener.original == listener){

              hasListener = true;
            };
            if(hasListener){

              if(context !== undefined){

                if(attachedListeners[i].ctx === context){

                  return true;
                };
              } else {

                return true;
              };
            };
          };
          return false;
        };
        return this[0].__dH.getListeners()[type].length > 0;
      },
      copyEventsTo : function(target){

        var source = this.concat();
        var targetCopy = target.concat();
        for(var i = source.length - 1;i >= 0;i--){

          var descendants = source[i].getElementsByTagName(c);
          for(var j = 0;j < descendants.length;j++){

            source.push(descendants[j]);
          };
        };
        for(var i = targetCopy.length - 1;i >= 0;i--){

          var descendants = targetCopy[i].getElementsByTagName(c);
          for(var j = 0;j < descendants.length;j++){

            targetCopy.push(descendants[j]);
          };
        };
        targetCopy.forEach(function(el){

          el.__dH = null;
        });
        for(var i = 0;i < source.length;i++){

          var el = source[i];
          if(!el.__dH){

            continue;
          };
          var storage = el.__dH.getListeners();
          for(var name in storage){

            for(var j = storage[name].length - 1;j >= 0;j--){

              var listener = storage[name][j].listener;
              if(listener.original){

                listener = listener.original;
              };
              qxWeb(targetCopy[i]).on(name, listener, storage[name][j].ctx);
            };
          };
        };
      },
      __cT : false,
      ready : function(callback){

        if(document.readyState === q){

          window.setTimeout(callback, 1);
          return;
        };
        var onWindowLoad = function(){

          qx.module.Event.__cT = true;
          callback();
        };
        qxWeb(window).on(f, onWindowLoad);
        var wrappedCallback = function(){

          qxWeb(window).off(f, onWindowLoad);
          callback();
        };
        if(qxWeb.env.get(b) !== a || qxWeb.env.get(p) > 8){

          qx.bom.Event.addNativeListener(document, o, wrappedCallback);
        } else {

          var timer = function(){

            if(qx.module.Event.__cT){

              return;
            };
            try{

              document.documentElement.doScroll(g);
              if(document.body){

                wrappedCallback();
              };
            } catch(error) {

              window.setTimeout(timer, 100);
            };
          };
          timer();
        };
      },
      hover : function(callbackIn, callbackOut){

        this.on(d, callbackIn, this);
        if(qx.lang.Type.isFunction(callbackOut)){

          this.on(e, callbackOut, this);
        };
        return this;
      },
      $registerNormalization : function(types, normalizer){

        if(!qx.lang.Type.isArray(types)){

          types = [types];
        };
        var registry = qx.module.Event.__iO;
        for(var i = 0,l = types.length;i < l;i++){

          var type = types[i];
          if(qx.lang.Type.isFunction(normalizer)){

            if(!registry[type]){

              registry[type] = [];
            };
            registry[type].push(normalizer);
          };
        };
      },
      $unregisterNormalization : function(types, normalizer){

        if(!qx.lang.Type.isArray(types)){

          types = [types];
        };
        var registry = qx.module.Event.__iO;
        for(var i = 0,l = types.length;i < l;i++){

          var type = types[i];
          if(registry[type]){

            qx.lang.Array.remove(registry[type], normalizer);
          };
        };
      },
      $getRegistry : function(){

        return qx.module.Event.__iO;
      },
      $registerEventHook : function(types, registerHook, unregisterHook){

        if(!qx.lang.Type.isArray(types)){

          types = [types];
        };
        var onHooks = qx.module.Event.__iP.on;
        for(var i = 0,l = types.length;i < l;i++){

          var type = types[i];
          if(qx.lang.Type.isFunction(registerHook)){

            if(!onHooks[type]){

              onHooks[type] = [];
            };
            onHooks[type].push(registerHook);
          };
        };
        if(!unregisterHook){

          return;
        };
        var offHooks = qx.module.Event.__iP.off;
        for(var i = 0,l = types.length;i < l;i++){

          var type = types[i];
          if(qx.lang.Type.isFunction(unregisterHook)){

            if(!offHooks[type]){

              offHooks[type] = [];
            };
            offHooks[type].push(unregisterHook);
          };
        };
      },
      $unregisterEventHook : function(types, registerHook, unregisterHook){

        if(!qx.lang.Type.isArray(types)){

          types = [types];
        };
        var onHooks = qx.module.Event.__iP.on;
        for(var i = 0,l = types.length;i < l;i++){

          var type = types[i];
          if(onHooks[type]){

            qx.lang.Array.remove(onHooks[type], registerHook);
          };
        };
        if(!unregisterHook){

          return;
        };
        var offHooks = qx.module.Event.__iP.off;
        for(var i = 0,l = types.length;i < l;i++){

          var type = types[i];
          if(offHooks[type]){

            qx.lang.Array.remove(offHooks[type], unregisterHook);
          };
        };
      },
      $getHookRegistry : function(){

        return qx.module.Event.__iP;
      }
    },
    defer : function(statics){

      qxWeb.$attach({
        "on" : statics.on,
        "off" : statics.off,
        "allOff" : statics.allOff,
        "once" : statics.once,
        "emit" : statics.emit,
        "hasListener" : statics.hasListener,
        "copyEventsTo" : statics.copyEventsTo,
        "hover" : statics.hover
      });
      qxWeb.$attachStatic({
        "ready" : statics.ready,
        "$registerEventNormalization" : statics.$registerNormalization,
        "$unregisterEventNormalization" : statics.$unregisterNormalization,
        "$getEventNormalizationRegistry" : statics.$getRegistry,
        "$registerEventHook" : statics.$registerEventHook,
        "$unregisterEventHook" : statics.$unregisterEventHook,
        "$getEventHookRegistry" : statics.$getHookRegistry
      });
    }
  });
})();
(function(){

  var a = "start",b = "animationEnd",c = "",d = "none",e = "qx.module.Animation",f = "animationIteration",g = "end",h = "animationStart",j = "ease-in",k = "iteration",l = "ease-out",m = "display";
  qx.Bootstrap.define(e, {
    events : {
      "animationStart" : undefined,
      "animationIteration" : undefined,
      "animationEnd" : undefined
    },
    statics : {
      getAnimationHandles : function(){

        var animationHandles = [];
        for(var i = 0;i < this.length;i++){

          animationHandles[i] = this[i].$$animation;
        };
        return animationHandles;
      },
      _fadeOut : {
        duration : 700,
        timing : l,
        keep : 100,
        keyFrames : {
          '0' : {
            opacity : 1
          },
          '100' : {
            opacity : 0,
            display : d
          }
        }
      },
      _fadeIn : {
        duration : 700,
        timing : j,
        keep : 100,
        keyFrames : {
          '0' : {
            opacity : 0
          },
          '100' : {
            opacity : 1
          }
        }
      },
      animate : function(desc, duration){

        qx.module.Animation._animate.bind(this)(desc, duration, false);
        return this;
      },
      animateReverse : function(desc, duration){

        qx.module.Animation._animate.bind(this)(desc, duration, true);
        return this;
      },
      _animate : function(desc, duration, reverse){

        this._forEachElement(function(el, i){

          if(el.$$animation){

            el.$$animation.stop();
          };
          var handle;
          if(reverse){

            handle = qx.bom.element.Animation.animateReverse(el, desc, duration);
          } else {

            handle = qx.bom.element.Animation.animate(el, desc, duration);
          };
          var self = this;
          if(i == 0){

            handle.on(a, function(){

              self.emit(h);
            }, handle);
            handle.on(k, function(){

              self.emit(f);
            }, handle);
          };
          handle.on(g, function(){

            for(var i = 0;i < self.length;i++){

              if(self[i].$$animation){

                return;
              };
            };
            self.emit(b);
          }, el);
        });
      },
      play : function(){

        for(var i = 0;i < this.length;i++){

          var handle = this[i].$$animation;
          if(handle){

            handle.play();
          };
        };
        return this;
      },
      pause : function(){

        for(var i = 0;i < this.length;i++){

          var handle = this[i].$$animation;
          if(handle){

            handle.pause();
          };
        };
        return this;
      },
      stop : function(){

        for(var i = 0;i < this.length;i++){

          var handle = this[i].$$animation;
          if(handle){

            handle.stop();
          };
        };
        return this;
      },
      isPlaying : function(){

        for(var i = 0;i < this.length;i++){

          var handle = this[i].$$animation;
          if(handle && handle.isPlaying()){

            return true;
          };
        };
        return false;
      },
      isEnded : function(){

        for(var i = 0;i < this.length;i++){

          var handle = this[i].$$animation;
          if(handle && !handle.isEnded()){

            return false;
          };
        };
        return true;
      },
      fadeIn : function(duration){

        this.setStyle(m, c);
        return this.animate(qx.module.Animation._fadeIn, duration);
      },
      fadeOut : function(duration){

        return this.animate(qx.module.Animation._fadeOut, duration);
      }
    },
    defer : function(statics){

      qxWeb.$attach({
        "animate" : statics.animate,
        "animateReverse" : statics.animateReverse,
        "fadeIn" : statics.fadeIn,
        "fadeOut" : statics.fadeOut,
        "play" : statics.play,
        "pause" : statics.pause,
        "stop" : statics.stop,
        "isEnded" : statics.isEnded,
        "isPlaying" : statics.isPlaying,
        "getAnimationHandles" : statics.getAnimationHandles
      });
    }
  });
})();
(function(){

  var a = "qx.util.DeferredCallManager",b = "singleton";
  qx.Class.define(a, {
    extend : qx.core.Object,
    type : b,
    construct : function(){

      this.__iR = {
      };
      this.__iS = qx.lang.Function.bind(this.__iW, this);
      this.__iT = false;
    },
    members : {
      __iU : null,
      __iV : null,
      __iR : null,
      __iT : null,
      __iS : null,
      schedule : function(deferredCall){

        if(this.__iU == null){

          this.__iU = window.setTimeout(this.__iS, 0);
        };
        var callKey = deferredCall.toHashCode();
        if(this.__iV && this.__iV[callKey]){

          return;
        };
        this.__iR[callKey] = deferredCall;
        this.__iT = true;
      },
      cancel : function(deferredCall){

        var callKey = deferredCall.toHashCode();
        if(this.__iV && this.__iV[callKey]){

          this.__iV[callKey] = null;
          return;
        };
        delete this.__iR[callKey];
        if(qx.lang.Object.isEmpty(this.__iR) && this.__iU != null){

          window.clearTimeout(this.__iU);
          this.__iU = null;
        };
      },
      __iW : qx.event.GlobalError.observeMethod(function(){

        this.__iU = null;
        while(this.__iT){

          this.__iV = qx.lang.Object.clone(this.__iR);
          this.__iR = {
          };
          this.__iT = false;
          for(var key in this.__iV){

            var call = this.__iV[key];
            if(call){

              this.__iV[key] = null;
              call.call();
            };
          };
        };
        this.__iV = null;
      })
    },
    destruct : function(){

      if(this.__iU != null){

        window.clearTimeout(this.__iU);
      };
      this.__iS = this.__iR = null;
    }
  });
})();
(function(){

  var a = "qx.util.DeferredCall";
  qx.Class.define(a, {
    extend : qx.core.Object,
    construct : function(callback, context){

      qx.core.Object.call(this);
      this.__bz = callback;
      this.__bB = context || null;
      this.__dh = qx.util.DeferredCallManager.getInstance();
    },
    members : {
      __bz : null,
      __bB : null,
      __dh : null,
      cancel : function(){

        this.__dh.cancel(this);
      },
      schedule : function(){

        this.__dh.schedule(this);
      },
      call : function(){

        {

          var context;
        };
        this.__bB ? this.__bz.apply(this.__bB) : this.__bz();
      }
    },
    destruct : function(){

      this.cancel();
      this.__bB = this.__bz = this.__dh = null;
    }
  });
})();
(function(){

  var a = "Child is already in: ",b = "text",c = "qx.html.Element",d = "|capture|",f = "focus",g = "blur",h = "div",j = "class",k = "deactivate",m = "__cW",n = "css.userselect",o = "animationEnd",p = "capture",q = "visible",r = "Root elements could not be inserted into other ones.",s = "Has no children!",t = "|bubble|",u = "releaseCapture",v = "Could not move to same index!",w = "element",z = "",A = "qxSelectable",B = "tabIndex",C = "off",D = "on",E = "qx.html.Iframe",F = "activate",G = "Has no parent to remove from.",H = "mshtml",I = "engine.name",J = "none",K = "Has no child: ",L = "scroll",M = " ",N = "hidden",O = "Has no child at this position!",P = "css.userselect.none",Q = "Could not overwrite existing element!";
  qx.Class.define(c, {
    extend : qx.core.Object,
    construct : function(tagName, styles, attributes){

      qx.core.Object.call(this);
      this.__iX = tagName || h;
      this.__iY = styles || null;
      this.__ja = attributes || null;
    },
    statics : {
      DEBUG : false,
      _modified : {
      },
      _visibility : {
      },
      _scroll : {
      },
      _actions : [],
      __jb : {
      },
      __jc : null,
      __jd : null,
      _scheduleFlush : function(job){

        qx.html.Element.__jC.schedule();
      },
      flush : function(){

        var obj;
        {
        };
        var focusHandler = this.__je();
        var focusedDomElement = focusHandler.getFocus();
        if(focusedDomElement && this.__jg(focusedDomElement)){

          focusHandler.blur(focusedDomElement);
        };
        var activeDomElement = focusHandler.getActive();
        if(activeDomElement && this.__jg(activeDomElement)){

          qx.bom.Element.deactivate(activeDomElement);
        };
        var captureDomElement = this.__jf();
        if(captureDomElement && this.__jg(captureDomElement)){

          qx.bom.Element.releaseCapture(captureDomElement);
        };
        var later = [];
        var modified = this._modified;
        for(var hc in modified){

          obj = modified[hc];
          if(obj.__jv() || obj.classname == E){

            if(obj.__jh && qx.dom.Hierarchy.isRendered(obj.__jh)){

              later.push(obj);
            } else {

              {
              };
              obj.__ju();
            };
            delete modified[hc];
          };
        };
        for(var i = 0,l = later.length;i < l;i++){

          obj = later[i];
          {
          };
          obj.__ju();
        };
        var visibility = this._visibility;
        for(var hc in visibility){

          obj = visibility[hc];
          var element = obj.__jh;
          if(!element){

            delete visibility[hc];
            continue;
          };
          {
          };
          if(!obj.$$disposed){

            element.style.display = obj.__ij ? z : J;
            if((qx.core.Environment.get(I) == H)){

              if(!(document.documentMode >= 8)){

                element.style.visibility = obj.__ij ? q : N;
              };
            };
          };
          delete visibility[hc];
        };
        var scroll = this._scroll;
        for(var hc in scroll){

          obj = scroll[hc];
          var elem = obj.__jh;
          if(elem && elem.offsetWidth){

            var done = true;
            if(obj.__jl != null){

              obj.__jh.scrollLeft = obj.__jl;
              delete obj.__jl;
            };
            if(obj.__jm != null){

              obj.__jh.scrollTop = obj.__jm;
              delete obj.__jm;
            };
            var intoViewX = obj.__jj;
            if(intoViewX != null){

              var child = intoViewX.element.getDomElement();
              if(child && child.offsetWidth){

                qx.bom.element.Scroll.intoViewX(child, elem, intoViewX.align);
                delete obj.__jj;
              } else {

                done = false;
              };
            };
            var intoViewY = obj.__jk;
            if(intoViewY != null){

              var child = intoViewY.element.getDomElement();
              if(child && child.offsetWidth){

                qx.bom.element.Scroll.intoViewY(child, elem, intoViewY.align);
                delete obj.__jk;
              } else {

                done = false;
              };
            };
            if(done){

              delete scroll[hc];
            };
          };
        };
        var activityEndActions = {
          "releaseCapture" : 1,
          "blur" : 1,
          "deactivate" : 1
        };
        for(var i = 0;i < this._actions.length;i++){

          var action = this._actions[i];
          var element = action.element.__jh;
          if(!element || !activityEndActions[action.type] && !action.element.__jv()){

            continue;
          };
          var args = action.args;
          args.unshift(element);
          qx.bom.Element[action.type].apply(qx.bom.Element, args);
        };
        this._actions = [];
        for(var hc in this.__jb){

          var selection = this.__jb[hc];
          var elem = selection.element.__jh;
          if(elem){

            qx.bom.Selection.set(elem, selection.start, selection.end);
            delete this.__jb[hc];
          };
        };
        qx.event.handler.Appear.refresh();
      },
      __je : function(){

        if(!this.__jc){

          var eventManager = qx.event.Registration.getManager(window);
          this.__jc = eventManager.getHandler(qx.event.handler.Focus);
        };
        return this.__jc;
      },
      __jf : function(){

        if(!this.__jd){

          var eventManager = qx.event.Registration.getManager(window);
          this.__jd = eventManager.getDispatcher(qx.event.dispatch.MouseCapture);
        };
        return this.__jd.getCaptureElement();
      },
      __jg : function(domElement){

        var element = qx.core.ObjectRegistry.fromHashCode(domElement.$$element);
        return element && !element.__jv();
      }
    },
    members : {
      __iX : null,
      __jh : null,
      __a : false,
      __ji : true,
      __ij : true,
      __jj : null,
      __jk : null,
      __jl : null,
      __jm : null,
      __jn : null,
      __jo : null,
      __jp : null,
      __iY : null,
      __ja : null,
      __jq : null,
      __jr : null,
      __cW : null,
      __js : null,
      __jt : null,
      _scheduleChildrenUpdate : function(){

        if(this.__js){

          return;
        };
        this.__js = true;
        qx.html.Element._modified[this.$$hash] = this;
        qx.html.Element._scheduleFlush(w);
      },
      _createDomElement : function(){

        return qx.dom.Element.create(this.__iX);
      },
      __ju : function(){

        {
        };
        var length;
        var children = this.__cW;
        if(children){

          length = children.length;
          var child;
          for(var i = 0;i < length;i++){

            child = children[i];
            if(child.__ij && child.__ji && !child.__jh){

              child.__ju();
            };
          };
        };
        if(!this.__jh){

          this.__jh = this._createDomElement();
          this.__jh.$$element = this.$$hash;
          this._copyData(false);
          if(children && length > 0){

            this._insertChildren();
          };
        } else {

          this._syncData();
          if(this.__js){

            this._syncChildren();
          };
        };
        delete this.__js;
      },
      _insertChildren : function(){

        var children = this.__cW;
        var length = children.length;
        var child;
        if(length > 2){

          var domElement = document.createDocumentFragment();
          for(var i = 0;i < length;i++){

            child = children[i];
            if(child.__jh && child.__ji){

              domElement.appendChild(child.__jh);
            };
          };
          this.__jh.appendChild(domElement);
        } else {

          var domElement = this.__jh;
          for(var i = 0;i < length;i++){

            child = children[i];
            if(child.__jh && child.__ji){

              domElement.appendChild(child.__jh);
            };
          };
        };
      },
      _syncChildren : function(){

        var ObjectRegistry = qx.core.ObjectRegistry;
        var dataChildren = this.__cW;
        var dataLength = dataChildren.length;
        var dataChild;
        var dataEl;
        var domParent = this.__jh;
        var domChildren = domParent.childNodes;
        var domPos = 0;
        var domEl;
        {

          var domOperations;
        };
        for(var i = domChildren.length - 1;i >= 0;i--){

          domEl = domChildren[i];
          dataEl = ObjectRegistry.fromHashCode(domEl.$$element);
          if(!dataEl || !dataEl.__ji || dataEl.__jt !== this){

            domParent.removeChild(domEl);
            {
            };
          };
        };
        for(var i = 0;i < dataLength;i++){

          dataChild = dataChildren[i];
          if(dataChild.__ji){

            dataEl = dataChild.__jh;
            domEl = domChildren[domPos];
            if(!dataEl){

              continue;
            };
            if(dataEl != domEl){

              if(domEl){

                domParent.insertBefore(dataEl, domEl);
              } else {

                domParent.appendChild(dataEl);
              };
              {
              };
            };
            domPos++;
          };
        };
        {
        };
      },
      _copyData : function(fromMarkup){

        var elem = this.__jh;
        var data = this.__ja;
        if(data){

          var Attribute = qx.bom.element.Attribute;
          for(var key in data){

            Attribute.set(elem, key, data[key]);
          };
        };
        var data = this.__iY;
        if(data){

          var Style = qx.bom.element.Style;
          if(fromMarkup){

            Style.setStyles(elem, data);
          } else {

            Style.setCss(elem, Style.compile(data));
          };
        };
        var data = this.__jq;
        if(data){

          for(var key in data){

            this._applyProperty(key, data[key]);
          };
        };
        var data = this.__jr;
        if(data){

          qx.event.Registration.getManager(elem).importListeners(elem, data);
          delete this.__jr;
        };
      },
      _syncData : function(){

        var elem = this.__jh;
        var Attribute = qx.bom.element.Attribute;
        var Style = qx.bom.element.Style;
        var jobs = this.__jo;
        if(jobs){

          var data = this.__ja;
          if(data){

            var value;
            for(var key in jobs){

              value = data[key];
              if(value !== undefined){

                Attribute.set(elem, key, value);
              } else {

                Attribute.reset(elem, key);
              };
            };
          };
          this.__jo = null;
        };
        var jobs = this.__jn;
        if(jobs){

          var data = this.__iY;
          if(data){

            var styles = {
            };
            for(var key in jobs){

              styles[key] = data[key];
            };
            Style.setStyles(elem, styles);
          };
          this.__jn = null;
        };
        var jobs = this.__jp;
        if(jobs){

          var data = this.__jq;
          if(data){

            var value;
            for(var key in jobs){

              this._applyProperty(key, data[key]);
            };
          };
          this.__jp = null;
        };
      },
      __jv : function(){

        var pa = this;
        while(pa){

          if(pa.__a){

            return true;
          };
          if(!pa.__ji || !pa.__ij){

            return false;
          };
          pa = pa.__jt;
        };
        return false;
      },
      __jw : function(child){

        if(child.__jt === this){

          throw new Error(a + child);
        };
        if(child.__a){

          throw new Error(r);
        };
        if(child.__jt){

          child.__jt.remove(child);
        };
        child.__jt = this;
        if(!this.__cW){

          this.__cW = [];
        };
        if(this.__jh){

          this._scheduleChildrenUpdate();
        };
      },
      __jx : function(child){

        if(child.__jt !== this){

          throw new Error(K + child);
        };
        if(this.__jh){

          this._scheduleChildrenUpdate();
        };
        delete child.__jt;
      },
      __jy : function(child){

        if(child.__jt !== this){

          throw new Error(K + child);
        };
        if(this.__jh){

          this._scheduleChildrenUpdate();
        };
      },
      getChildren : function(){

        return this.__cW || null;
      },
      getChild : function(index){

        var children = this.__cW;
        return children && children[index] || null;
      },
      hasChildren : function(){

        var children = this.__cW;
        return children && children[0] !== undefined;
      },
      indexOf : function(child){

        var children = this.__cW;
        return children ? children.indexOf(child) : -1;
      },
      hasChild : function(child){

        var children = this.__cW;
        return children && children.indexOf(child) !== -1;
      },
      add : function(varargs){

        if(arguments[1]){

          for(var i = 0,l = arguments.length;i < l;i++){

            this.__jw(arguments[i]);
          };
          this.__cW.push.apply(this.__cW, arguments);
        } else {

          this.__jw(varargs);
          this.__cW.push(varargs);
        };
        return this;
      },
      addAt : function(child, index){

        this.__jw(child);
        qx.lang.Array.insertAt(this.__cW, child, index);
        return this;
      },
      remove : function(childs){

        var children = this.__cW;
        if(!children){

          return this;
        };
        if(arguments[1]){

          var child;
          for(var i = 0,l = arguments.length;i < l;i++){

            child = arguments[i];
            this.__jx(child);
            qx.lang.Array.remove(children, child);
          };
        } else {

          this.__jx(childs);
          qx.lang.Array.remove(children, childs);
        };
        return this;
      },
      removeAt : function(index){

        var children = this.__cW;
        if(!children){

          throw new Error(s);
        };
        var child = children[index];
        if(!child){

          throw new Error(O);
        };
        this.__jx(child);
        qx.lang.Array.removeAt(this.__cW, index);
        return this;
      },
      removeAll : function(){

        var children = this.__cW;
        if(children){

          for(var i = 0,l = children.length;i < l;i++){

            this.__jx(children[i]);
          };
          children.length = 0;
        };
        return this;
      },
      getParent : function(){

        return this.__jt || null;
      },
      insertInto : function(parent, index){

        parent.__jw(this);
        if(index == null){

          parent.__cW.push(this);
        } else {

          qx.lang.Array.insertAt(this.__cW, this, index);
        };
        return this;
      },
      insertBefore : function(rel){

        var parent = rel.__jt;
        parent.__jw(this);
        qx.lang.Array.insertBefore(parent.__cW, this, rel);
        return this;
      },
      insertAfter : function(rel){

        var parent = rel.__jt;
        parent.__jw(this);
        qx.lang.Array.insertAfter(parent.__cW, this, rel);
        return this;
      },
      moveTo : function(index){

        var parent = this.__jt;
        parent.__jy(this);
        var oldIndex = parent.__cW.indexOf(this);
        if(oldIndex === index){

          throw new Error(v);
        } else if(oldIndex < index){

          index--;
        };
        qx.lang.Array.removeAt(parent.__cW, oldIndex);
        qx.lang.Array.insertAt(parent.__cW, this, index);
        return this;
      },
      moveBefore : function(rel){

        var parent = this.__jt;
        return this.moveTo(parent.__cW.indexOf(rel));
      },
      moveAfter : function(rel){

        var parent = this.__jt;
        return this.moveTo(parent.__cW.indexOf(rel) + 1);
      },
      free : function(){

        var parent = this.__jt;
        if(!parent){

          throw new Error(G);
        };
        if(!parent.__cW){

          return this;
        };
        parent.__jx(this);
        qx.lang.Array.remove(parent.__cW, this);
        return this;
      },
      getDomElement : function(){

        return this.__jh || null;
      },
      getNodeName : function(){

        return this.__iX;
      },
      setNodeName : function(name){

        this.__iX = name;
      },
      setRoot : function(root){

        this.__a = root;
      },
      useMarkup : function(html){

        if(this.__jh){

          throw new Error(Q);
        };
        if(qx.core.Environment.get(I) == H){

          var helper = document.createElement(h);
        } else {

          var helper = qx.dom.Element.getHelperElement();
        };
        helper.innerHTML = html;
        this.useElement(helper.firstChild);
        return this.__jh;
      },
      useElement : function(elem){

        if(this.__jh){

          throw new Error(Q);
        };
        this.__jh = elem;
        this.__jh.$$element = this.$$hash;
        this._copyData(true);
      },
      isFocusable : function(){

        var tabIndex = this.getAttribute(B);
        if(tabIndex >= 1){

          return true;
        };
        var focusable = qx.event.handler.Focus.FOCUSABLE_ELEMENTS;
        if(tabIndex >= 0 && focusable[this.__iX]){

          return true;
        };
        return false;
      },
      setSelectable : function(value){

        this.setAttribute(A, value ? D : C);
        var userSelect = qx.core.Environment.get(n);
        if(userSelect){

          this.setStyle(userSelect, value ? b : qx.core.Environment.get(P));
        };
      },
      isNativelyFocusable : function(){

        return !!qx.event.handler.Focus.FOCUSABLE_ELEMENTS[this.__iX];
      },
      include : function(){

        if(this.__ji){

          return this;
        };
        delete this.__ji;
        if(this.__jt){

          this.__jt._scheduleChildrenUpdate();
        };
        return this;
      },
      exclude : function(){

        if(!this.__ji){

          return this;
        };
        this.__ji = false;
        if(this.__jt){

          this.__jt._scheduleChildrenUpdate();
        };
        return this;
      },
      isIncluded : function(){

        return this.__ji === true;
      },
      fadeIn : function(duration){

        var col = qxWeb(this.__jh);
        if(col.isPlaying()){

          col.stop();
        };
        if(!this.__jh){

          this.__ju();
          col.push(this.__jh);
        };
        if(this.__jh){

          col.fadeIn(duration);
          return col.getAnimationHandles()[0];
        };
      },
      fadeOut : function(duration){

        var col = qxWeb(this.__jh);
        if(col.isPlaying()){

          col.stop();
        };
        if(this.__jh){

          col.fadeOut(duration).once(o, function(){

            this.hide();
            qx.html.Element.flush();
          }, this);
          return col.getAnimationHandles()[0];
        };
      },
      show : function(){

        if(this.__ij){

          return this;
        };
        if(this.__jh){

          qx.html.Element._visibility[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        if(this.__jt){

          this.__jt._scheduleChildrenUpdate();
        };
        delete this.__ij;
        return this;
      },
      hide : function(){

        if(!this.__ij){

          return this;
        };
        if(this.__jh){

          qx.html.Element._visibility[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        this.__ij = false;
        return this;
      },
      isVisible : function(){

        return this.__ij === true;
      },
      scrollChildIntoViewX : function(elem, align, direct){

        var thisEl = this.__jh;
        var childEl = elem.getDomElement();
        if(direct !== false && thisEl && thisEl.offsetWidth && childEl && childEl.offsetWidth){

          qx.bom.element.Scroll.intoViewX(childEl, thisEl, align);
        } else {

          this.__jj = {
            element : elem,
            align : align
          };
          qx.html.Element._scroll[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        delete this.__jl;
      },
      scrollChildIntoViewY : function(elem, align, direct){

        var thisEl = this.__jh;
        var childEl = elem.getDomElement();
        if(direct !== false && thisEl && thisEl.offsetWidth && childEl && childEl.offsetWidth){

          qx.bom.element.Scroll.intoViewY(childEl, thisEl, align);
        } else {

          this.__jk = {
            element : elem,
            align : align
          };
          qx.html.Element._scroll[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        delete this.__jm;
      },
      scrollToX : function(x, lazy){

        var thisEl = this.__jh;
        if(lazy !== true && thisEl && thisEl.offsetWidth){

          thisEl.scrollLeft = x;
          delete this.__jl;
        } else {

          this.__jl = x;
          qx.html.Element._scroll[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        delete this.__jj;
      },
      getScrollX : function(){

        var thisEl = this.__jh;
        if(thisEl){

          return thisEl.scrollLeft;
        };
        return this.__jl || 0;
      },
      scrollToY : function(y, lazy){

        var thisEl = this.__jh;
        if(lazy !== true && thisEl && thisEl.offsetWidth){

          thisEl.scrollTop = y;
          delete this.__jm;
        } else {

          this.__jm = y;
          qx.html.Element._scroll[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        delete this.__jk;
      },
      getScrollY : function(){

        var thisEl = this.__jh;
        if(thisEl){

          return thisEl.scrollTop;
        };
        return this.__jm || 0;
      },
      disableScrolling : function(){

        this.enableScrolling();
        this.scrollToX(0);
        this.scrollToY(0);
        this.addListener(L, this.__jA, this);
      },
      enableScrolling : function(){

        this.removeListener(L, this.__jA, this);
      },
      __jz : null,
      __jA : function(e){

        if(!this.__jz){

          this.__jz = true;
          this.__jh.scrollTop = 0;
          this.__jh.scrollLeft = 0;
          delete this.__jz;
        };
      },
      getTextSelection : function(){

        var el = this.__jh;
        if(el){

          return qx.bom.Selection.get(el);
        };
        return null;
      },
      getTextSelectionLength : function(){

        var el = this.__jh;
        if(el){

          return qx.bom.Selection.getLength(el);
        };
        return null;
      },
      getTextSelectionStart : function(){

        var el = this.__jh;
        if(el){

          return qx.bom.Selection.getStart(el);
        };
        return null;
      },
      getTextSelectionEnd : function(){

        var el = this.__jh;
        if(el){

          return qx.bom.Selection.getEnd(el);
        };
        return null;
      },
      setTextSelection : function(start, end){

        var el = this.__jh;
        if(el){

          qx.bom.Selection.set(el, start, end);
          return;
        };
        qx.html.Element.__jb[this.toHashCode()] = {
          element : this,
          start : start,
          end : end
        };
        qx.html.Element._scheduleFlush(w);
      },
      clearTextSelection : function(){

        var el = this.__jh;
        if(el){

          qx.bom.Selection.clear(el);
        };
        delete qx.html.Element.__jb[this.toHashCode()];
      },
      __jB : function(action, args){

        var actions = qx.html.Element._actions;
        actions.push({
          type : action,
          element : this,
          args : args || []
        });
        qx.html.Element._scheduleFlush(w);
      },
      focus : function(){

        this.__jB(f);
      },
      blur : function(){

        this.__jB(g);
      },
      activate : function(){

        this.__jB(F);
      },
      deactivate : function(){

        this.__jB(k);
      },
      capture : function(containerCapture){

        this.__jB(p, [containerCapture !== false]);
      },
      releaseCapture : function(){

        this.__jB(u);
      },
      setStyle : function(key, value, direct){

        if(!this.__iY){

          this.__iY = {
          };
        };
        if(this.__iY[key] == value){

          return this;
        };
        if(value == null){

          delete this.__iY[key];
        } else {

          this.__iY[key] = value;
        };
        if(this.__jh){

          if(direct){

            qx.bom.element.Style.set(this.__jh, key, value);
            return this;
          };
          if(!this.__jn){

            this.__jn = {
            };
          };
          this.__jn[key] = true;
          qx.html.Element._modified[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        return this;
      },
      setStyles : function(map, direct){

        var Style = qx.bom.element.Style;
        if(!this.__iY){

          this.__iY = {
          };
        };
        if(this.__jh){

          if(!this.__jn){

            this.__jn = {
            };
          };
          for(var key in map){

            var value = map[key];
            if(this.__iY[key] == value){

              continue;
            };
            if(value == null){

              delete this.__iY[key];
            } else {

              this.__iY[key] = value;
            };
            if(direct){

              Style.set(this.__jh, key, value);
              continue;
            };
            this.__jn[key] = true;
          };
          qx.html.Element._modified[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        } else {

          for(var key in map){

            var value = map[key];
            if(this.__iY[key] == value){

              continue;
            };
            if(value == null){

              delete this.__iY[key];
            } else {

              this.__iY[key] = value;
            };
          };
        };
        return this;
      },
      removeStyle : function(key, direct){

        this.setStyle(key, null, direct);
        return this;
      },
      getStyle : function(key){

        return this.__iY ? this.__iY[key] : null;
      },
      getAllStyles : function(){

        return this.__iY || null;
      },
      setAttribute : function(key, value, direct){

        if(!this.__ja){

          this.__ja = {
          };
        };
        if(this.__ja[key] == value){

          return this;
        };
        if(value == null){

          delete this.__ja[key];
        } else {

          this.__ja[key] = value;
        };
        if(this.__jh){

          if(direct){

            qx.bom.element.Attribute.set(this.__jh, key, value);
            return this;
          };
          if(!this.__jo){

            this.__jo = {
            };
          };
          this.__jo[key] = true;
          qx.html.Element._modified[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        return this;
      },
      setAttributes : function(map, direct){

        for(var key in map){

          this.setAttribute(key, map[key], direct);
        };
        return this;
      },
      removeAttribute : function(key, direct){

        return this.setAttribute(key, null, direct);
      },
      getAttribute : function(key){

        return this.__ja ? this.__ja[key] : null;
      },
      addClass : function(name){

        var value = ((this.getAttribute(j) || z) + M + name).trim();
        this.setAttribute(j, value);
      },
      removeClass : function(name){

        var currentClass = this.getAttribute(j);
        if(currentClass){

          this.setAttribute(j, (currentClass.replace(name, z)).trim());
        };
      },
      _applyProperty : function(name, value){
      },
      _setProperty : function(key, value, direct){

        if(!this.__jq){

          this.__jq = {
          };
        };
        if(this.__jq[key] == value){

          return this;
        };
        if(value == null){

          delete this.__jq[key];
        } else {

          this.__jq[key] = value;
        };
        if(this.__jh){

          if(direct){

            this._applyProperty(key, value);
            return this;
          };
          if(!this.__jp){

            this.__jp = {
            };
          };
          this.__jp[key] = true;
          qx.html.Element._modified[this.$$hash] = this;
          qx.html.Element._scheduleFlush(w);
        };
        return this;
      },
      _removeProperty : function(key, direct){

        return this._setProperty(key, null, direct);
      },
      _getProperty : function(key){

        var db = this.__jq;
        if(!db){

          return null;
        };
        var value = db[key];
        return value == null ? null : value;
      },
      addListener : function(type, listener, self, capture){

        if(this.$$disposed){

          return null;
        };
        {

          var msg;
        };
        if(this.__jh){

          return qx.event.Registration.addListener(this.__jh, type, listener, self, capture);
        };
        if(!this.__jr){

          this.__jr = {
          };
        };
        if(capture == null){

          capture = false;
        };
        var unique = qx.event.Manager.getNextUniqueId();
        var id = type + (capture ? d : t) + unique;
        this.__jr[id] = {
          type : type,
          listener : listener,
          self : self,
          capture : capture,
          unique : unique
        };
        return id;
      },
      removeListener : function(type, listener, self, capture){

        if(this.$$disposed){

          return null;
        };
        {

          var msg;
        };
        if(this.__jh){

          qx.event.Registration.removeListener(this.__jh, type, listener, self, capture);
        } else {

          var values = this.__jr;
          var entry;
          if(capture == null){

            capture = false;
          };
          for(var key in values){

            entry = values[key];
            if(entry.listener === listener && entry.self === self && entry.capture === capture && entry.type === type){

              delete values[key];
              break;
            };
          };
        };
        return this;
      },
      removeListenerById : function(id){

        if(this.$$disposed){

          return null;
        };
        if(this.__jh){

          qx.event.Registration.removeListenerById(this.__jh, id);
        } else {

          delete this.__jr[id];
        };
        return this;
      },
      hasListener : function(type, capture){

        if(this.$$disposed){

          return false;
        };
        if(this.__jh){

          return qx.event.Registration.hasListener(this.__jh, type, capture);
        };
        var values = this.__jr;
        var entry;
        if(capture == null){

          capture = false;
        };
        for(var key in values){

          entry = values[key];
          if(entry.capture === capture && entry.type === type){

            return true;
          };
        };
        return false;
      },
      getListeners : function(){

        if(this.$$disposed){

          return null;
        };
        if(this.__jh){

          return qx.event.Registration.getManager(this.__jh).serializeListeners(this.__jh);
        };
        var listeners = [];
        for(var id in this.__jr){

          var listenerData = this.__jr[id];
          listeners.push({
            type : listenerData.type,
            handler : listenerData.listener,
            self : listenerData.self,
            capture : listenerData.capture
          });
        };
        return listeners;
      }
    },
    defer : function(statics){

      statics.__jC = new qx.util.DeferredCall(statics.flush, statics);
    },
    destruct : function(){

      var el = this.__jh;
      if(el){

        qx.event.Registration.getManager(el).removeAllListeners(el);
        el.$$element = z;
      };
      if(!qx.core.ObjectRegistry.inShutDown){

        var parent = this.__jt;
        if(parent && !parent.$$disposed){

          parent.remove(this);
        };
      };
      this._disposeArray(m);
      this.__ja = this.__iY = this.__jr = this.__jq = this.__jo = this.__jn = this.__jp = this.__jh = this.__jt = this.__jj = this.__jk = null;
    }
  });
})();
(function(){

  var a = "offline",b = "qx.event.handler.Offline",c = "online";
  qx.Class.define(b, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__dh = manager;
      this.__cw = manager.getWindow();
      this._initObserver();
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        online : true,
        offline : true
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_WINDOW,
      IGNORE_CAN_HANDLE : true
    },
    members : {
      __dh : null,
      __cw : null,
      __dj : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      },
      _initObserver : function(){

        this.__dj = qx.lang.Function.listener(this._onNative, this);
        qx.bom.Event.addNativeListener(this.__cw, a, this.__dj);
        qx.bom.Event.addNativeListener(this.__cw, c, this.__dj);
      },
      _stopObserver : function(){

        qx.bom.Event.removeNativeListener(this.__cw, a, this.__dj);
        qx.bom.Event.removeNativeListener(this.__cw, c, this.__dj);
      },
      _onNative : qx.event.GlobalError.observeMethod(function(domEvent){

        qx.event.Registration.fireEvent(this.__cw, domEvent.type, qx.event.type.Event, []);
      }),
      isOnline : function(){

        return !!this.__cw.navigator.onLine;
      }
    },
    destruct : function(){

      this.__dh = null;
      this._stopObserver();
      delete qx.event.handler.Appear.__instances[this.$$hash];
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "qx.bom.Element";
  qx.Class.define(c, {
    statics : {
      addListener : function(element, type, listener, self, capture){

        return qx.event.Registration.addListener(element, type, listener, self, capture);
      },
      removeListener : function(element, type, listener, self, capture){

        return qx.event.Registration.removeListener(element, type, listener, self, capture);
      },
      removeListenerById : function(target, id){

        return qx.event.Registration.removeListenerById(target, id);
      },
      hasListener : function(element, type, capture){

        return qx.event.Registration.hasListener(element, type, capture);
      },
      focus : function(element){

        qx.event.Registration.getManager(element).getHandler(qx.event.handler.Focus).focus(element);
      },
      blur : function(element){

        qx.event.Registration.getManager(element).getHandler(qx.event.handler.Focus).blur(element);
      },
      activate : function(element){

        qx.event.Registration.getManager(element).getHandler(qx.event.handler.Focus).activate(element);
      },
      deactivate : function(element){

        qx.event.Registration.getManager(element).getHandler(qx.event.handler.Focus).deactivate(element);
      },
      capture : function(element, containerCapture){

        qx.event.Registration.getManager(element).getDispatcher(qx.event.dispatch.MouseCapture).activateCapture(element, containerCapture);
      },
      releaseCapture : function(element){

        qx.event.Registration.getManager(element).getDispatcher(qx.event.dispatch.MouseCapture).releaseCapture(element);
      },
      clone : function(element, events){

        var clone;
        if(events || ((qx.core.Environment.get(b) == a) && !qx.xml.Document.isXmlDocument(element))){

          var mgr = qx.event.Registration.getManager(element);
          var all = qx.dom.Hierarchy.getDescendants(element);
          all.push(element);
        };
        if((qx.core.Environment.get(b) == a)){

          for(var i = 0,l = all.length;i < l;i++){

            mgr.toggleAttachedEvents(all[i], false);
          };
        };
        var clone = element.cloneNode(true);
        if((qx.core.Environment.get(b) == a)){

          for(var i = 0,l = all.length;i < l;i++){

            mgr.toggleAttachedEvents(all[i], true);
          };
        };
        if(events === true){

          var cloneAll = qx.dom.Hierarchy.getDescendants(clone);
          cloneAll.push(clone);
          var eventList,cloneElem,origElem,eventEntry;
          for(var i = 0,il = all.length;i < il;i++){

            origElem = all[i];
            eventList = mgr.serializeListeners(origElem);
            if(eventList.length > 0){

              cloneElem = cloneAll[i];
              for(var j = 0,jl = eventList.length;j < jl;j++){

                eventEntry = eventList[j];
                mgr.addListener(cloneElem, eventEntry.type, eventEntry.handler, eventEntry.self, eventEntry.capture);
              };
            };
          };
        };
        return clone;
      }
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "blur",d = "losecapture",e = "focus",f = "click",g = "qx.event.dispatch.MouseCapture",h = "capture",i = "scroll";
  qx.Class.define(g, {
    extend : qx.event.dispatch.AbstractBubbling,
    construct : function(manager, registration){

      qx.event.dispatch.AbstractBubbling.call(this, manager);
      this.__cw = manager.getWindow();
      this.__cy = registration;
      manager.addListener(this.__cw, c, this.releaseCapture, this);
      manager.addListener(this.__cw, e, this.releaseCapture, this);
      manager.addListener(this.__cw, i, this.releaseCapture, this);
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_FIRST
    },
    members : {
      __cy : null,
      __jD : null,
      __jE : true,
      __cw : null,
      _getParent : function(target){

        return target.parentNode;
      },
      canDispatchEvent : function(target, event, type){

        return !!(this.__jD && this.__jF[type]);
      },
      dispatchEvent : function(target, event, type){

        if(!qx.event.handler.MouseEmulation.ON){

          if(type == f){

            event.stopPropagation();
            this.releaseCapture();
            return;
          };
        };
        if(this.__jE || !qx.dom.Hierarchy.contains(this.__jD, target)){

          target = this.__jD;
        };
        qx.event.dispatch.AbstractBubbling.prototype.dispatchEvent.call(this, target, event, type);
      },
      __jF : {
        "mouseup" : 1,
        "mousedown" : 1,
        "click" : 1,
        "dblclick" : 1,
        "mousemove" : 1,
        "mouseout" : 1,
        "mouseover" : 1
      },
      activateCapture : function(element, containerCapture){

        var containerCapture = containerCapture !== false;
        if(this.__jD === element && this.__jE == containerCapture){

          return;
        };
        if(this.__jD){

          this.releaseCapture();
        };
        this.nativeSetCapture(element, containerCapture);
        if(this.hasNativeCapture){

          var self = this;
          qx.bom.Event.addNativeListener(element, d, function(){

            qx.bom.Event.removeNativeListener(element, d, arguments.callee);
            self.releaseCapture();
          });
        };
        this.__jE = containerCapture;
        this.__jD = element;
        this.__cy.fireEvent(element, h, qx.event.type.Event, [true, false]);
      },
      getCaptureElement : function(){

        return this.__jD;
      },
      releaseCapture : function(){

        var element = this.__jD;
        if(!element){

          return;
        };
        this.__jD = null;
        this.__cy.fireEvent(element, d, qx.event.type.Event, [true, false]);
        this.nativeReleaseCapture(element);
      },
      hasNativeCapture : qx.core.Environment.get(b) == a,
      nativeSetCapture : qx.core.Environment.select(b, {
        "mshtml" : function(element, containerCapture){

          element.setCapture(containerCapture !== false);
        },
        "default" : (function(){
        })
      }),
      nativeReleaseCapture : qx.core.Environment.select(b, {
        "mshtml" : function(element){

          element.releaseCapture();
        },
        "default" : (function(){
        })
      })
    },
    destruct : function(){

      this.__jD = this.__cw = this.__cy = null;
    },
    defer : function(statics){

      qx.event.Registration.addDispatcher(statics);
    }
  });
})();
(function(){

  var a = "function",b = "plugin.silverlight.version",c = "Silverlight",d = "Skype.Detection",f = "QuickTimeCheckObject.QuickTimeCheck.1",g = "Adobe Acrobat",h = "plugin.windowsmedia",k = "QuickTime",l = "plugin.silverlight",m = "pdf",n = "wmv",o = "qx.bom.client.Plugin",p = "application/x-skype",q = "plugin.divx",r = "Chrome PDF Viewer",s = "divx",t = "Windows Media",u = "",v = "mshtml",w = "skype.click2call",x = "plugin.skype",y = "plugin.gears",z = "plugin.quicktime",A = "plugin.windowsmedia.version",B = "quicktime",C = "DivX Web Player",D = "AgControl.AgControl",E = "Microsoft.XMLHTTP",F = "silverlight",G = "plugin.pdf",H = "plugin.pdf.version",I = "MSXML2.DOMDocument.6.0",J = "WMPlayer.OCX.7",K = "AcroPDF.PDF",L = "plugin.activex",M = "plugin.quicktime.version",N = "plugin.divx.version",O = "npdivx.DivXBrowserPlugin.1",P = "object";
  qx.Bootstrap.define(o, {
    statics : {
      getGears : function(){

        return !!(window.google && window.google.gears);
      },
      getActiveX : function(){

        if(typeof window.ActiveXObject === a){

          return true;
        };
        try{

          return (typeof (new window.ActiveXObject(E)) === P || typeof (new window.ActiveXObject(I)) === P);
        } catch(ex) {

          return false;
        };
      },
      getSkype : function(){

        if(qx.bom.client.Plugin.getActiveX()){

          try{

            new ActiveXObject(d);
            return true;
          } catch(e) {
          };
        };
        var mimeTypes = navigator.mimeTypes;
        if(mimeTypes){

          if(p in mimeTypes){

            return true;
          };
          for(var i = 0;i < mimeTypes.length;i++){

            var desc = mimeTypes[i];
            if(desc.type.indexOf(w) != -1){

              return true;
            };
          };
        };
        return false;
      },
      __jG : {
        quicktime : {
          plugin : [k],
          control : f
        },
        wmv : {
          plugin : [t],
          control : J
        },
        divx : {
          plugin : [C],
          control : O
        },
        silverlight : {
          plugin : [c],
          control : D
        },
        pdf : {
          plugin : [r, g],
          control : K
        }
      },
      getQuicktimeVersion : function(){

        var entry = qx.bom.client.Plugin.__jG[B];
        return qx.bom.client.Plugin.__jH(entry.control, entry.plugin);
      },
      getWindowsMediaVersion : function(){

        var entry = qx.bom.client.Plugin.__jG[n];
        return qx.bom.client.Plugin.__jH(entry.control, entry.plugin);
      },
      getDivXVersion : function(){

        var entry = qx.bom.client.Plugin.__jG[s];
        return qx.bom.client.Plugin.__jH(entry.control, entry.plugin);
      },
      getSilverlightVersion : function(){

        var entry = qx.bom.client.Plugin.__jG[F];
        return qx.bom.client.Plugin.__jH(entry.control, entry.plugin);
      },
      getPdfVersion : function(){

        var entry = qx.bom.client.Plugin.__jG[m];
        return qx.bom.client.Plugin.__jH(entry.control, entry.plugin);
      },
      getQuicktime : function(){

        var entry = qx.bom.client.Plugin.__jG[B];
        return qx.bom.client.Plugin.__jI(entry.control, entry.plugin);
      },
      getWindowsMedia : function(){

        var entry = qx.bom.client.Plugin.__jG[n];
        return qx.bom.client.Plugin.__jI(entry.control, entry.plugin);
      },
      getDivX : function(){

        var entry = qx.bom.client.Plugin.__jG[s];
        return qx.bom.client.Plugin.__jI(entry.control, entry.plugin);
      },
      getSilverlight : function(){

        var entry = qx.bom.client.Plugin.__jG[F];
        return qx.bom.client.Plugin.__jI(entry.control, entry.plugin);
      },
      getPdf : function(){

        var entry = qx.bom.client.Plugin.__jG[m];
        return qx.bom.client.Plugin.__jI(entry.control, entry.plugin);
      },
      __jH : function(activeXName, pluginNames){

        var available = qx.bom.client.Plugin.__jI(activeXName, pluginNames);
        if(!available){

          return u;
        };
        if(qx.bom.client.Engine.getName() == v){

          var obj = new ActiveXObject(activeXName);
          try{

            var version = obj.versionInfo;
            if(version != undefined){

              return version;
            };
            version = obj.version;
            if(version != undefined){

              return version;
            };
            version = obj.settings.version;
            if(version != undefined){

              return version;
            };
          } catch(ex) {

            return u;
          };
          return u;
        } else {

          var plugins = navigator.plugins;
          var verreg = /([0-9]\.[0-9])/g;
          for(var i = 0;i < plugins.length;i++){

            var plugin = plugins[i];
            for(var j = 0;j < pluginNames.length;j++){

              if(plugin.name.indexOf(pluginNames[j]) !== -1){

                if(verreg.test(plugin.name) || verreg.test(plugin.description)){

                  return RegExp.$1;
                };
              };
            };
          };
          return u;
        };
      },
      __jI : function(activeXName, pluginNames){

        if(qx.bom.client.Engine.getName() == v){

          var control = window.ActiveXObject;
          if(!control){

            return false;
          };
          try{

            new ActiveXObject(activeXName);
          } catch(ex) {

            return false;
          };
          return true;
        } else {

          var plugins = navigator.plugins;
          if(!plugins){

            return false;
          };
          var name;
          for(var i = 0;i < plugins.length;i++){

            name = plugins[i].name;
            for(var j = 0;j < pluginNames.length;j++){

              if(name.indexOf(pluginNames[j]) !== -1){

                return true;
              };
            };
          };
          return false;
        };
      }
    },
    defer : function(statics){

      qx.core.Environment.add(y, statics.getGears);
      qx.core.Environment.add(z, statics.getQuicktime);
      qx.core.Environment.add(M, statics.getQuicktimeVersion);
      qx.core.Environment.add(h, statics.getWindowsMedia);
      qx.core.Environment.add(A, statics.getWindowsMediaVersion);
      qx.core.Environment.add(q, statics.getDivX);
      qx.core.Environment.add(N, statics.getDivXVersion);
      qx.core.Environment.add(l, statics.getSilverlight);
      qx.core.Environment.add(b, statics.getSilverlightVersion);
      qx.core.Environment.add(G, statics.getPdf);
      qx.core.Environment.add(H, statics.getPdfVersion);
      qx.core.Environment.add(L, statics.getActiveX);
      qx.core.Environment.add(x, statics.getSkype);
    }
  });
})();
(function(){

  var a = '<\?xml version="1.0" encoding="utf-8"?>\n<',b = "MSXML2.DOMDocument.3.0",c = "qx.xml.Document",d = "",e = " />",f = "xml.domparser",g = "SelectionLanguage",h = "'",j = "MSXML2.XMLHTTP.3.0",k = "plugin.activex",m = "No XML implementation available!",n = "MSXML2.XMLHTTP.6.0",o = "xml.implementation",p = " xmlns='",q = "text/xml",r = "XPath",s = "MSXML2.DOMDocument.6.0",t = "HTML";
  qx.Bootstrap.define(c, {
    statics : {
      DOMDOC : null,
      XMLHTTP : null,
      isXmlDocument : function(elem){

        if(elem.nodeType === 9){

          return elem.documentElement.nodeName !== t;
        } else if(elem.ownerDocument){

          return this.isXmlDocument(elem.ownerDocument);
        } else {

          return false;
        };
      },
      create : function(namespaceUri, qualifiedName){

        if(qx.core.Environment.get(k)){

          var obj = new ActiveXObject(this.DOMDOC);
          if(this.DOMDOC == b){

            obj.setProperty(g, r);
          };
          if(qualifiedName){

            var str = a;
            str += qualifiedName;
            if(namespaceUri){

              str += p + namespaceUri + h;
            };
            str += e;
            obj.loadXML(str);
          };
          return obj;
        };
        if(qx.core.Environment.get(o)){

          return document.implementation.createDocument(namespaceUri || d, qualifiedName || d, null);
        };
        throw new Error(m);
      },
      fromString : function(str){

        if(qx.core.Environment.get(k)){

          var dom = qx.xml.Document.create();
          dom.loadXML(str);
          return dom;
        };
        if(qx.core.Environment.get(f)){

          var parser = new DOMParser();
          return parser.parseFromString(str, q);
        };
        throw new Error(m);
      }
    },
    defer : function(statics){

      if(qx.core.Environment.get(k)){

        var domDoc = [s, b];
        var httpReq = [n, j];
        for(var i = 0,l = domDoc.length;i < l;i++){

          try{

            new ActiveXObject(domDoc[i]);
            new ActiveXObject(httpReq[i]);
          } catch(ex) {

            continue;
          };
          statics.DOMDOC = domDoc[i];
          statics.XMLHTTP = httpReq[i];
          break;
        };
      };
    }
  });
})();
(function(){

  var a = "function",b = "xml.implementation",c = "xml.attributens",d = "xml.selectnodes",e = "<a></a>",f = "xml.getqualifieditem",g = "SelectionLanguage",h = "xml.getelementsbytagnamens",i = "qx.bom.client.Xml",j = "xml.domproperties",k = "xml.selectsinglenode",l = "1.0",m = "xml.createnode",n = "xml.domparser",o = "getProperty",p = "undefined",q = "XML",r = "string",s = "xml.createelementns";
  qx.Bootstrap.define(i, {
    statics : {
      getImplementation : function(){

        return document.implementation && document.implementation.hasFeature && document.implementation.hasFeature(q, l);
      },
      getDomParser : function(){

        return typeof window.DOMParser !== p;
      },
      getSelectSingleNode : function(){

        return typeof qx.xml.Document.create().selectSingleNode !== p;
      },
      getSelectNodes : function(){

        return typeof qx.xml.Document.create().selectNodes !== p;
      },
      getElementsByTagNameNS : function(){

        return typeof qx.xml.Document.create().getElementsByTagNameNS !== p;
      },
      getDomProperties : function(){

        var doc = qx.xml.Document.create();
        return (o in doc && typeof doc.getProperty(g) === r);
      },
      getAttributeNS : function(){

        var docElem = qx.xml.Document.fromString(e).documentElement;
        return typeof docElem.getAttributeNS === a && typeof docElem.setAttributeNS === a;
      },
      getCreateElementNS : function(){

        return typeof qx.xml.Document.create().createElementNS === a;
      },
      getCreateNode : function(){

        return typeof qx.xml.Document.create().createNode !== p;
      },
      getQualifiedItem : function(){

        var docElem = qx.xml.Document.fromString(e).documentElement;
        return typeof docElem.attributes.getQualifiedItem !== p;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(b, statics.getImplementation);
      qx.core.Environment.add(n, statics.getDomParser);
      qx.core.Environment.add(k, statics.getSelectSingleNode);
      qx.core.Environment.add(d, statics.getSelectNodes);
      qx.core.Environment.add(h, statics.getElementsByTagNameNS);
      qx.core.Environment.add(j, statics.getDomProperties);
      qx.core.Environment.add(c, statics.getAttributeNS);
      qx.core.Environment.add(s, statics.getCreateElementNS);
      qx.core.Environment.add(m, statics.getCreateNode);
      qx.core.Environment.add(f, statics.getQualifiedItem);
    }
  });
})();
(function(){

  var a = "useraction",b = " due to exceptions in user code. The application has to be reloaded!",c = "event.touch",d = "qx.ui.core.queue.Manager",f = " times in a row",g = "Fatal Error: Flush terminated ";
  qx.Class.define(d, {
    statics : {
      __jJ : false,
      __ga : false,
      __jK : {
      },
      __jL : 0,
      MAX_RETRIES : 10,
      scheduleFlush : function(job){

        var self = qx.ui.core.queue.Manager;
        self.__jK[job] = true;
        if(!self.__jJ){

          self.__ga = false;
          qx.bom.AnimationFrame.request(function(){

            if(self.__ga){

              self.__ga = false;
              return;
            };
            self.flush();
          }, self);
          self.__jJ = true;
        };
      },
      flush : function(){

        var self = qx.ui.core.queue.Manager;
        if(self.__jM){

          return;
        };
        self.__jM = true;
        self.__ga = true;
        var jobs = self.__jK;
        self.__jN(function(){

          while(jobs.visibility || jobs.widget || jobs.appearance || jobs.layout || jobs.element){

            if(jobs.widget){

              delete jobs.widget;
              {

                qx.ui.core.queue.Widget.flush();
              };
            };
            if(jobs.visibility){

              delete jobs.visibility;
              {

                qx.ui.core.queue.Visibility.flush();
              };
            };
            if(jobs.appearance){

              delete jobs.appearance;
              {

                qx.ui.core.queue.Appearance.flush();
              };
            };
            if(jobs.widget || jobs.visibility || jobs.appearance){

              continue;
            };
            if(jobs.layout){

              delete jobs.layout;
              {

                qx.ui.core.queue.Layout.flush();
              };
            };
            if(jobs.widget || jobs.visibility || jobs.appearance || jobs.layout){

              continue;
            };
            if(jobs.element){

              delete jobs.element;
              qx.html.Element.flush();
            };
          };
        }, function(){

          self.__jJ = false;
        });
        self.__jN(function(){

          if(jobs.dispose){

            delete jobs.dispose;
            {

              qx.ui.core.queue.Dispose.flush();
            };
          };
        }, function(){

          self.__jM = false;
        });
        self.__jL = 0;
      },
      __jN : function(callback, finallyCode){

        var self = qx.ui.core.queue.Manager;
        try{

          callback();
        } catch(e) {

          {
          };
          self.__jJ = false;
          self.__jM = false;
          self.__jL += 1;
          if(self.__jL <= self.MAX_RETRIES){

            self.scheduleFlush();
          } else {

            throw new Error(g + (self.__jL - 1) + f + b);
          };
          throw e;
        }finally{

          finallyCode();
        };
      },
      __jO : function(e){

        qx.ui.core.queue.Manager.flush();
      }
    },
    defer : function(statics){

      qx.html.Element._scheduleFlush = statics.scheduleFlush;
      qx.event.Registration.addListener(window, a, qx.core.Environment.get(c) ? statics.__jO : statics.flush);
    }
  });
})();
(function(){

  var a = "qx.ui.core.queue.Widget",b = "widget",c = "$$default";
  qx.Class.define(a, {
    statics : {
      __iG : [],
      __jK : {
      },
      remove : function(widget, job){

        var queue = this.__iG;
        if(!qx.lang.Array.contains(queue, widget)){

          return;
        };
        var hash = widget.$$hash;
        if(job == null){

          qx.lang.Array.remove(queue, widget);
          delete this.__jK[hash];
          return;
        };
        if(this.__jK[hash]){

          delete this.__jK[hash][job];
          if(qx.lang.Object.getLength(this.__jK[hash]) == 0){

            qx.lang.Array.remove(queue, widget);
          };
        };
      },
      add : function(widget, job){

        var queue = this.__iG;
        if(!qx.lang.Array.contains(queue, widget)){

          queue.unshift(widget);
        };
        if(job == null){

          job = c;
        };
        var hash = widget.$$hash;
        if(!this.__jK[hash]){

          this.__jK[hash] = {
          };
        };
        this.__jK[hash][job] = true;
        qx.ui.core.queue.Manager.scheduleFlush(b);
      },
      flush : function(){

        var queue = this.__iG;
        var obj,jobs;
        for(var i = queue.length - 1;i >= 0;i--){

          obj = queue[i];
          jobs = this.__jK[obj.$$hash];
          queue.splice(i, 1);
          obj.syncWidget(jobs);
        };
        if(queue.length != 0){

          return;
        };
        this.__iG = [];
        this.__jK = {
        };
      }
    }
  });
})();
(function(){

  var a = "qx.ui.core.queue.Visibility",b = "visibility";
  qx.Class.define(a, {
    statics : {
      __iG : [],
      __cM : {
      },
      remove : function(widget){

        delete this.__cM[widget.$$hash];
        qx.lang.Array.remove(this.__iG, widget);
      },
      isVisible : function(widget){

        return this.__cM[widget.$$hash] || false;
      },
      __jP : function(widget){

        var data = this.__cM;
        var hash = widget.$$hash;
        var visible;
        if(widget.isExcluded()){

          visible = false;
        } else {

          var parent = widget.$$parent;
          if(parent){

            visible = this.__jP(parent);
          } else {

            visible = widget.isRootWidget();
          };
        };
        return data[hash] = visible;
      },
      add : function(widget){

        var queue = this.__iG;
        if(qx.lang.Array.contains(queue, widget)){

          return;
        };
        queue.unshift(widget);
        qx.ui.core.queue.Manager.scheduleFlush(b);
      },
      flush : function(){

        var queue = this.__iG;
        var data = this.__cM;
        for(var i = queue.length - 1;i >= 0;i--){

          var hash = queue[i].$$hash;
          if(data[hash] != null){

            queue[i].addChildrenToQueue(queue);
          };
        };
        var oldData = {
        };
        for(var i = queue.length - 1;i >= 0;i--){

          var hash = queue[i].$$hash;
          oldData[hash] = data[hash];
          data[hash] = null;
        };
        for(var i = queue.length - 1;i >= 0;i--){

          var widget = queue[i];
          var hash = widget.$$hash;
          queue.splice(i, 1);
          if(data[hash] == null){

            this.__jP(widget);
          };
          if(data[hash] && data[hash] != oldData[hash]){

            widget.checkAppearanceNeeds();
          };
        };
        this.__iG = [];
      }
    }
  });
})();
(function(){

  var a = "appearance",b = "qx.ui.core.queue.Appearance";
  qx.Class.define(b, {
    statics : {
      __iG : [],
      remove : function(widget){

        qx.lang.Array.remove(this.__iG, widget);
      },
      add : function(widget){

        var queue = this.__iG;
        if(qx.lang.Array.contains(queue, widget)){

          return;
        };
        queue.unshift(widget);
        qx.ui.core.queue.Manager.scheduleFlush(a);
      },
      has : function(widget){

        return qx.lang.Array.contains(this.__iG, widget);
      },
      flush : function(){

        var Visibility = qx.ui.core.queue.Visibility;
        var queue = this.__iG;
        var obj;
        for(var i = queue.length - 1;i >= 0;i--){

          obj = queue[i];
          queue.splice(i, 1);
          if(Visibility.isVisible(obj)){

            obj.syncAppearance();
          } else {

            obj.$$stateChanges = true;
          };
        };
      }
    }
  });
})();
(function(){

  var a = "dispose",b = "qx.ui.core.queue.Dispose";
  qx.Class.define(b, {
    statics : {
      __iG : [],
      add : function(widget){

        var queue = this.__iG;
        if(qx.lang.Array.contains(queue, widget)){

          return;
        };
        queue.unshift(widget);
        qx.ui.core.queue.Manager.scheduleFlush(a);
      },
      isEmpty : function(){

        return this.__iG.length == 0;
      },
      flush : function(){

        var queue = this.__iG;
        for(var i = queue.length - 1;i >= 0;i--){

          var widget = queue[i];
          queue.splice(i, 1);
          widget.dispose();
        };
        if(queue.length != 0){

          return;
        };
        this.__iG = [];
      }
    }
  });
})();
(function(){

  var a = "backgroundColor",b = "drag",c = "_applyNativeContextMenu",d = "div",f = "_applyBackgroundColor",g = "qx.event.type.Data",h = "_applyFocusable",j = " requires a layout, but no one was defined!",k = "qx.event.type.KeyInput",m = "focused",n = "disabled",o = "move",p = "__db",q = "createChildControl",r = "Unsupported control: ",s = "dragstart",t = "__jV",u = "Font",v = "qx.dynlocale",w = "dragchange",x = "_applyEnabled",y = "_applySelectable",z = "Number",A = "_applyKeepActive",B = "dragend",C = "_applyVisibility",D = "Child control '",E = "__dd",F = "qxDraggable",G = "syncAppearance",H = "paddingLeft",I = "' of widget ",J = "qx.event.type.Mouse",K = "_applyPadding",L = "#",M = "At least one child in control ",N = "visible",O = "qx.event.type.Event",P = "qx.event.type.MouseWheel",Q = "_applyCursor",R = "changeVisibility",S = "_applyDraggable",T = "resize",U = "Decorator",V = "Remove Error: ",W = "zIndex",X = "changeTextColor",Y = "$$widget",ba = "changeContextMenu",bb = "on",bc = "paddingTop",bd = "opacity",be = "This widget has no children!",bf = "changeSelectable",bg = "none",bh = "outline",bi = "hidden",bj = "_applyAppearance",bk = "hovered",bl = "_applyOpacity",bm = "Boolean",bn = "px",bo = "qx.ui.core.Widget",bp = "default",bq = "TabIndex property must be between 1 and 32000",br = "_applyFont",bs = "cursor",bt = "qxDroppable",bu = "' already created!",bv = "changeZIndex",bw = ": ",bx = "Color",by = "changeEnabled",bz = "Abstract method call: _getContentHeightForWidth()!",bA = "changeFont",bB = "qx.event.type.Focus",bC = "_applyDecorator",bD = "_applyZIndex",bE = "_applyTextColor",bF = "Widget is not focusable!",bG = "qx.ui.menu.Menu",bH = "engine.name",bI = "qx.event.type.Drag",bJ = "qx.event.type.KeySequence",bK = "excluded",bL = "DOM element is not yet created!",bM = "_applyToolTipText",bN = "Exception while creating child control '",bO = "_applyDroppable",bP = " is not a child of this widget!",bQ = "widget",bR = "changeDecorator",bS = "qx.event.type.Tap",bT = "Integer",bU = "_applyTabIndex",bV = "changeAppearance",bW = "shorthand",bX = "/",bY = "String",ca = "border-box",cb = "",cc = "_applyContextMenu",cd = "changeToolTipText",ce = "padding",cf = "tabIndex",cg = "paddingBottom",ch = "beforeContextmenuOpen",ci = "changeNativeContextMenu",cj = "undefined",ck = "qx.ui.tooltip.ToolTip",cl = "contextmenu",cm = "__jT",cn = "_applyKeepFocus",co = "paddingRight",cp = "changeBackgroundColor",cq = "changeLocale",cr = "qxKeepFocus",cs = "opera",ct = "qx.event.type.Touch",cu = "qxKeepActive",cv = "absolute";
  qx.Class.define(bo, {
    extend : qx.ui.core.LayoutItem,
    include : [qx.locale.MTranslation],
    construct : function(){

      qx.ui.core.LayoutItem.call(this);
      this.__db = this.__jU();
      this.initFocusable();
      this.initSelectable();
      this.initNativeContextMenu();
    },
    events : {
      appear : O,
      disappear : O,
      createChildControl : g,
      resize : g,
      move : g,
      syncAppearance : g,
      mousemove : J,
      mouseover : J,
      mouseout : J,
      mousedown : J,
      mouseup : J,
      click : J,
      dblclick : J,
      contextmenu : J,
      beforeContextmenuOpen : g,
      mousewheel : P,
      touchstart : ct,
      touchend : ct,
      touchmove : ct,
      touchcancel : ct,
      tap : bS,
      longtap : bS,
      swipe : ct,
      keyup : bJ,
      keydown : bJ,
      keypress : bJ,
      keyinput : k,
      focus : bB,
      blur : bB,
      focusin : bB,
      focusout : bB,
      activate : bB,
      deactivate : bB,
      capture : O,
      losecapture : O,
      drop : bI,
      dragleave : bI,
      dragover : bI,
      drag : bI,
      dragstart : bI,
      dragend : bI,
      dragchange : bI,
      droprequest : bI
    },
    properties : {
      paddingTop : {
        check : bT,
        init : 0,
        apply : K,
        themeable : true
      },
      paddingRight : {
        check : bT,
        init : 0,
        apply : K,
        themeable : true
      },
      paddingBottom : {
        check : bT,
        init : 0,
        apply : K,
        themeable : true
      },
      paddingLeft : {
        check : bT,
        init : 0,
        apply : K,
        themeable : true
      },
      padding : {
        group : [bc, co, cg, H],
        mode : bW,
        themeable : true
      },
      zIndex : {
        nullable : true,
        init : 10,
        apply : bD,
        event : bv,
        check : bT,
        themeable : true
      },
      decorator : {
        nullable : true,
        init : null,
        apply : bC,
        event : bR,
        check : U,
        themeable : true
      },
      backgroundColor : {
        nullable : true,
        check : bx,
        apply : f,
        event : cp,
        themeable : true
      },
      textColor : {
        nullable : true,
        check : bx,
        apply : bE,
        event : X,
        themeable : true,
        inheritable : true
      },
      font : {
        nullable : true,
        apply : br,
        check : u,
        event : bA,
        themeable : true,
        inheritable : true,
        dereference : true
      },
      opacity : {
        check : z,
        apply : bl,
        themeable : true,
        nullable : true,
        init : null
      },
      cursor : {
        check : bY,
        apply : Q,
        themeable : true,
        inheritable : true,
        nullable : true,
        init : null
      },
      toolTip : {
        check : ck,
        nullable : true
      },
      toolTipText : {
        check : bY,
        nullable : true,
        event : cd,
        apply : bM
      },
      toolTipIcon : {
        check : bY,
        nullable : true,
        event : cd
      },
      blockToolTip : {
        check : bm,
        init : false
      },
      visibility : {
        check : [N, bi, bK],
        init : N,
        apply : C,
        event : R
      },
      enabled : {
        init : true,
        check : bm,
        inheritable : true,
        apply : x,
        event : by
      },
      anonymous : {
        init : false,
        check : bm
      },
      tabIndex : {
        check : bT,
        nullable : true,
        apply : bU
      },
      focusable : {
        check : bm,
        init : false,
        apply : h
      },
      keepFocus : {
        check : bm,
        init : false,
        apply : cn
      },
      keepActive : {
        check : bm,
        init : false,
        apply : A
      },
      draggable : {
        check : bm,
        init : false,
        apply : S
      },
      droppable : {
        check : bm,
        init : false,
        apply : bO
      },
      selectable : {
        check : bm,
        init : false,
        event : bf,
        apply : y
      },
      contextMenu : {
        check : bG,
        apply : cc,
        nullable : true,
        event : ba
      },
      nativeContextMenu : {
        check : bm,
        init : false,
        themeable : true,
        event : ci,
        apply : c
      },
      appearance : {
        check : bY,
        init : bQ,
        apply : bj,
        event : bV
      }
    },
    statics : {
      DEBUG : false,
      getWidgetByElement : function(element, considerAnonymousState){

        while(element){

          var widgetKey = element.$$widget;
          if(widgetKey != null){

            var widget = qx.core.ObjectRegistry.fromHashCode(widgetKey);
            if(!considerAnonymousState || !widget.getAnonymous()){

              return widget;
            };
          };
          try{

            element = element.parentNode;
          } catch(e) {

            return null;
          };
        };
        return null;
      },
      contains : function(parent, child){

        while(child){

          if(parent == child){

            return true;
          };
          child = child.getLayoutParent();
        };
        return false;
      },
      __jQ : new qx.util.ObjectPool()
    },
    members : {
      __db : null,
      __jR : null,
      __jS : null,
      __dd : null,
      _getLayout : function(){

        return this.__dd;
      },
      _setLayout : function(layout){

        {
        };
        if(this.__dd){

          this.__dd.connectToWidget(null);
        };
        if(layout){

          layout.connectToWidget(this);
        };
        this.__dd = layout;
        qx.ui.core.queue.Layout.add(this);
      },
      setLayoutParent : function(parent){

        if(this.$$parent === parent){

          return;
        };
        var content = this.getContentElement();
        if(this.$$parent && !this.$$parent.$$disposed){

          this.$$parent.getContentElement().remove(content);
        };
        this.$$parent = parent || null;
        if(parent && !parent.$$disposed){

          this.$$parent.getContentElement().add(content);
        };
        this.$$refreshInheritables();
        qx.ui.core.queue.Visibility.add(this);
      },
      _updateInsets : null,
      renderLayout : function(left, top, width, height){

        var changes = qx.ui.core.LayoutItem.prototype.renderLayout.call(this, left, top, width, height);
        if(!changes){

          return null;
        };
        if(qx.lang.Object.isEmpty(changes) && !this._updateInsets){

          return null;
        };
        var content = this.getContentElement();
        var inner = changes.size || this._updateInsets;
        var pixel = bn;
        var contentStyles = {
        };
        if(changes.position){

          contentStyles.left = left + pixel;
          contentStyles.top = top + pixel;
        };
        if(inner || changes.margin){

          contentStyles.width = width + pixel;
          contentStyles.height = height + pixel;
        };
        if(Object.keys(contentStyles).length > 0){

          content.setStyles(contentStyles);
        };
        if(inner || changes.local || changes.margin){

          if(this.__dd && this.hasLayoutChildren()){

            var inset = this.getInsets();
            var innerWidth = width - inset.left - inset.right;
            var innerHeight = height - inset.top - inset.bottom;
            var decorator = this.getDecorator();
            var decoratorPadding = {
              left : 0,
              right : 0,
              top : 0,
              bottom : 0
            };
            if(decorator){

              decorator = qx.theme.manager.Decoration.getInstance().resolve(decorator);
              decoratorPadding = decorator.getPadding();
            };
            var padding = {
              top : this.getPaddingTop() + decoratorPadding.top,
              right : this.getPaddingRight() + decoratorPadding.right,
              bottom : this.getPaddingBottom() + decoratorPadding.bottom,
              left : this.getPaddingLeft() + decoratorPadding.left
            };
            this.__dd.renderLayout(innerWidth, innerHeight, padding);
          } else if(this.hasLayoutChildren()){

            throw new Error(M + this._findTopControl() + j);
          };
        };
        if(changes.position && this.hasListener(o)){

          this.fireDataEvent(o, this.getBounds());
        };
        if(changes.size && this.hasListener(T)){

          this.fireDataEvent(T, this.getBounds());
        };
        delete this._updateInsets;
        return changes;
      },
      __jT : null,
      clearSeparators : function(){

        var reg = this.__jT;
        if(!reg){

          return;
        };
        var pool = qx.ui.core.Widget.__jQ;
        var content = this.getContentElement();
        var widget;
        for(var i = 0,l = reg.length;i < l;i++){

          widget = reg[i];
          pool.poolObject(widget);
          content.remove(widget.getContentElement());
        };
        reg.length = 0;
      },
      renderSeparator : function(separator, bounds){

        var widget = qx.ui.core.Widget.__jQ.getObject(qx.ui.core.Widget);
        widget.set({
          decorator : separator
        });
        var elem = widget.getContentElement();
        this.getContentElement().add(elem);
        var domEl = elem.getDomElement();
        if(domEl){

          domEl.style.top = bounds.top + bn;
          domEl.style.left = bounds.left + bn;
          domEl.style.width = bounds.width + bn;
          domEl.style.height = bounds.height + bn;
        } else {

          elem.setStyles({
            left : bounds.left + bn,
            top : bounds.top + bn,
            width : bounds.width + bn,
            height : bounds.height + bn
          });
        };
        if(!this.__jT){

          this.__jT = [];
        };
        this.__jT.push(widget);
      },
      _computeSizeHint : function(){

        var width = this.getWidth();
        var minWidth = this.getMinWidth();
        var maxWidth = this.getMaxWidth();
        var height = this.getHeight();
        var minHeight = this.getMinHeight();
        var maxHeight = this.getMaxHeight();
        {
        };
        var contentHint = this._getContentHint();
        var insets = this.getInsets();
        var insetX = insets.left + insets.right;
        var insetY = insets.top + insets.bottom;
        if(width == null){

          width = contentHint.width + insetX;
        };
        if(height == null){

          height = contentHint.height + insetY;
        };
        if(minWidth == null){

          minWidth = insetX;
          if(contentHint.minWidth != null){

            minWidth += contentHint.minWidth;
            if(minWidth > maxWidth && maxWidth != null){

              minWidth = maxWidth;
            };
          };
        };
        if(minHeight == null){

          minHeight = insetY;
          if(contentHint.minHeight != null){

            minHeight += contentHint.minHeight;
            if(minHeight > maxHeight && maxHeight != null){

              minHeight = maxHeight;
            };
          };
        };
        if(maxWidth == null){

          if(contentHint.maxWidth == null){

            maxWidth = Infinity;
          } else {

            maxWidth = contentHint.maxWidth + insetX;
            if(maxWidth < minWidth && minWidth != null){

              maxWidth = minWidth;
            };
          };
        };
        if(maxHeight == null){

          if(contentHint.maxHeight == null){

            maxHeight = Infinity;
          } else {

            maxHeight = contentHint.maxHeight + insetY;
            if(maxHeight < minHeight && minHeight != null){

              maxHeight = minHeight;
            };
          };
        };
        return {
          width : width,
          minWidth : minWidth,
          maxWidth : maxWidth,
          height : height,
          minHeight : minHeight,
          maxHeight : maxHeight
        };
      },
      invalidateLayoutCache : function(){

        qx.ui.core.LayoutItem.prototype.invalidateLayoutCache.call(this);
        if(this.__dd){

          this.__dd.invalidateLayoutCache();
        };
      },
      _getContentHint : function(){

        var layout = this.__dd;
        if(layout){

          if(this.hasLayoutChildren()){

            var hint = layout.getSizeHint();
            {

              var msg;
            };
            return hint;
          } else {

            return {
              width : 0,
              height : 0
            };
          };
        } else {

          return {
            width : 100,
            height : 50
          };
        };
      },
      _getHeightForWidth : function(width){

        var insets = this.getInsets();
        var insetX = insets.left + insets.right;
        var insetY = insets.top + insets.bottom;
        var contentWidth = width - insetX;
        var layout = this._getLayout();
        if(layout && layout.hasHeightForWidth()){

          var contentHeight = layout.getHeightForWidth(width);
        } else {

          contentHeight = this._getContentHeightForWidth(contentWidth);
        };
        var height = contentHeight + insetY;
        return height;
      },
      _getContentHeightForWidth : function(width){

        throw new Error(bz);
      },
      getInsets : function(){

        var top = this.getPaddingTop();
        var right = this.getPaddingRight();
        var bottom = this.getPaddingBottom();
        var left = this.getPaddingLeft();
        if(this.getDecorator()){

          var decorator = qx.theme.manager.Decoration.getInstance().resolve(this.getDecorator());
          var inset = decorator.getInsets();
          {
          };
          top += inset.top;
          right += inset.right;
          bottom += inset.bottom;
          left += inset.left;
        };
        return {
          "top" : top,
          "right" : right,
          "bottom" : bottom,
          "left" : left
        };
      },
      getInnerSize : function(){

        var computed = this.getBounds();
        if(!computed){

          return null;
        };
        var insets = this.getInsets();
        return {
          width : computed.width - insets.left - insets.right,
          height : computed.height - insets.top - insets.bottom
        };
      },
      fadeOut : function(duration){

        return this.getContentElement().fadeOut(duration);
      },
      fadeIn : function(duration){

        return this.getContentElement().fadeIn(duration);
      },
      show : function(){

        this.setVisibility(N);
      },
      hide : function(){

        this.setVisibility(bi);
      },
      exclude : function(){

        this.setVisibility(bK);
      },
      isVisible : function(){

        return this.getVisibility() === N;
      },
      isHidden : function(){

        return this.getVisibility() !== N;
      },
      isExcluded : function(){

        return this.getVisibility() === bK;
      },
      isSeeable : function(){

        qx.ui.core.queue.Manager.flush();
        var element = this.getContentElement().getDomElement();
        if(element){

          return element.offsetWidth > 0;
        };
        return false;
      },
      __jU : function(){

        var el = this._createContentElement();
        el.setAttribute(Y, this.toHashCode());
        {
        };
        var styles = {
          "zIndex" : 10,
          "boxSizing" : ca
        };
        if(!qx.ui.root.Inline || !(this instanceof qx.ui.root.Inline)){

          styles.position = cv;
        };
        el.setStyles(styles);
        return el;
      },
      _createContentElement : function(){

        return new qx.html.Element(d, {
          overflowX : bi,
          overflowY : bi
        });
      },
      getContentElement : function(){

        return this.__db;
      },
      __jV : null,
      getLayoutChildren : function(){

        var children = this.__jV;
        if(!children){

          return this.__jW;
        };
        var layoutChildren;
        for(var i = 0,l = children.length;i < l;i++){

          var child = children[i];
          if(child.hasUserBounds() || child.isExcluded()){

            if(layoutChildren == null){

              layoutChildren = children.concat();
            };
            qx.lang.Array.remove(layoutChildren, child);
          };
        };
        return layoutChildren || children;
      },
      scheduleLayoutUpdate : function(){

        qx.ui.core.queue.Layout.add(this);
      },
      invalidateLayoutChildren : function(){

        var layout = this.__dd;
        if(layout){

          layout.invalidateChildrenCache();
        };
        qx.ui.core.queue.Layout.add(this);
      },
      hasLayoutChildren : function(){

        var children = this.__jV;
        if(!children){

          return false;
        };
        var child;
        for(var i = 0,l = children.length;i < l;i++){

          child = children[i];
          if(!child.hasUserBounds() && !child.isExcluded()){

            return true;
          };
        };
        return false;
      },
      getChildrenContainer : function(){

        return this;
      },
      __jW : [],
      _getChildren : function(){

        return this.__jV || this.__jW;
      },
      _indexOf : function(child){

        var children = this.__jV;
        if(!children){

          return -1;
        };
        return children.indexOf(child);
      },
      _hasChildren : function(){

        var children = this.__jV;
        return children != null && (!!children[0]);
      },
      addChildrenToQueue : function(queue){

        var children = this.__jV;
        if(!children){

          return;
        };
        var child;
        for(var i = 0,l = children.length;i < l;i++){

          child = children[i];
          queue.push(child);
          child.addChildrenToQueue(queue);
        };
      },
      _add : function(child, options){

        {
        };
        if(child.getLayoutParent() == this){

          qx.lang.Array.remove(this.__jV, child);
        };
        if(this.__jV){

          this.__jV.push(child);
        } else {

          this.__jV = [child];
        };
        this.__jX(child, options);
      },
      _addAt : function(child, index, options){

        if(!this.__jV){

          this.__jV = [];
        };
        if(child.getLayoutParent() == this){

          qx.lang.Array.remove(this.__jV, child);
        };
        var ref = this.__jV[index];
        if(ref === child){

          child.setLayoutProperties(options);
        };
        if(ref){

          qx.lang.Array.insertBefore(this.__jV, child, ref);
        } else {

          this.__jV.push(child);
        };
        this.__jX(child, options);
      },
      _addBefore : function(child, before, options){

        {
        };
        if(child == before){

          return;
        };
        if(!this.__jV){

          this.__jV = [];
        };
        if(child.getLayoutParent() == this){

          qx.lang.Array.remove(this.__jV, child);
        };
        qx.lang.Array.insertBefore(this.__jV, child, before);
        this.__jX(child, options);
      },
      _addAfter : function(child, after, options){

        {
        };
        if(child == after){

          return;
        };
        if(!this.__jV){

          this.__jV = [];
        };
        if(child.getLayoutParent() == this){

          qx.lang.Array.remove(this.__jV, child);
        };
        qx.lang.Array.insertAfter(this.__jV, child, after);
        this.__jX(child, options);
      },
      _remove : function(child){

        if(!this.__jV){

          throw new Error(be);
        };
        qx.lang.Array.remove(this.__jV, child);
        this.__jY(child);
      },
      _removeAt : function(index){

        if(!this.__jV){

          throw new Error(be);
        };
        var child = this.__jV[index];
        qx.lang.Array.removeAt(this.__jV, index);
        this.__jY(child);
        return child;
      },
      _removeAll : function(){

        if(!this.__jV){

          return [];
        };
        var children = this.__jV.concat();
        this.__jV.length = 0;
        for(var i = children.length - 1;i >= 0;i--){

          this.__jY(children[i]);
        };
        qx.ui.core.queue.Layout.add(this);
        return children;
      },
      _afterAddChild : null,
      _afterRemoveChild : null,
      __jX : function(child, options){

        {
        };
        var parent = child.getLayoutParent();
        if(parent && parent != this){

          parent._remove(child);
        };
        child.setLayoutParent(this);
        if(options){

          child.setLayoutProperties(options);
        } else {

          this.updateLayoutProperties();
        };
        if(this._afterAddChild){

          this._afterAddChild(child);
        };
      },
      __jY : function(child){

        {
        };
        if(child.getLayoutParent() !== this){

          throw new Error(V + child + bP);
        };
        child.setLayoutParent(null);
        if(this.__dd){

          this.__dd.invalidateChildrenCache();
        };
        qx.ui.core.queue.Layout.add(this);
        if(this._afterRemoveChild){

          this._afterRemoveChild(child);
        };
      },
      capture : function(capture){

        this.getContentElement().capture(capture);
      },
      releaseCapture : function(){

        this.getContentElement().releaseCapture();
      },
      _applyPadding : function(value, old, name){

        this._updateInsets = true;
        qx.ui.core.queue.Layout.add(this);
        this.__ka(name, value);
      },
      __ka : function(style, value){

        var content = this.getContentElement();
        var decorator = this.getDecorator();
        decorator = qx.theme.manager.Decoration.getInstance().resolve(decorator);
        if(decorator){

          var direction = qx.Bootstrap.firstLow(style.replace(ce, cb));
          value += decorator.getPadding()[direction] || 0;
        };
        content.setStyle(style, value + bn);
      },
      _applyDecorator : function(value, old){

        var content = this.getContentElement();
        if(old){

          old = qx.theme.manager.Decoration.getInstance().getCssClassName(old);
          content.removeClass(old);
        };
        if(value){

          value = qx.theme.manager.Decoration.getInstance().addCssClass(value);
          content.addClass(value);
        };
      },
      _applyToolTipText : function(value, old){

        if(qx.core.Environment.get(v)){

          if(this.__jS){

            return;
          };
          var manager = qx.locale.Manager.getInstance();
          this.__jS = manager.addListener(cq, function(){

            var toolTipText = this.getToolTipText();
            if(toolTipText && toolTipText.translate){

              this.setToolTipText(toolTipText.translate());
            };
          }, this);
        };
      },
      _applyTextColor : function(value, old){
      },
      _applyZIndex : function(value, old){

        this.getContentElement().setStyle(W, value == null ? 0 : value);
      },
      _applyVisibility : function(value, old){

        var content = this.getContentElement();
        if(value === N){

          content.show();
        } else {

          content.hide();
        };
        var parent = this.$$parent;
        if(parent && (old == null || value == null || old === bK || value === bK)){

          parent.invalidateLayoutChildren();
        };
        qx.ui.core.queue.Visibility.add(this);
      },
      _applyOpacity : function(value, old){

        this.getContentElement().setStyle(bd, value == 1 ? null : value);
      },
      _applyCursor : function(value, old){

        if(value == null && !this.isSelectable()){

          value = bp;
        };
        this.getContentElement().setStyle(bs, value, qx.core.Environment.get(bH) == cs);
      },
      _applyBackgroundColor : function(value, old){

        var color = this.getBackgroundColor();
        var content = this.getContentElement();
        var resolved = qx.theme.manager.Color.getInstance().resolve(color);
        content.setStyle(a, resolved);
      },
      _applyFont : function(value, old){
      },
      _onChangeTheme : function(){

        qx.ui.core.LayoutItem.prototype._onChangeTheme.call(this);
        this.updateAppearance();
        var value = this.getDecorator();
        this._applyDecorator(null, value);
        this._applyDecorator(value);
        value = this.getFont();
        if(qx.lang.Type.isString(value)){

          this._applyFont(value, value);
        };
        value = this.getTextColor();
        if(qx.lang.Type.isString(value)){

          this._applyTextColor(value, value);
        };
        value = this.getBackgroundColor();
        if(qx.lang.Type.isString(value)){

          this._applyBackgroundColor(value, value);
        };
      },
      __gt : null,
      $$stateChanges : null,
      _forwardStates : null,
      hasState : function(state){

        var states = this.__gt;
        return !!states && !!states[state];
      },
      addState : function(state){

        var states = this.__gt;
        if(!states){

          states = this.__gt = {
          };
        };
        if(states[state]){

          return;
        };
        this.__gt[state] = true;
        if(state === bk){

          this.syncAppearance();
        } else if(!qx.ui.core.queue.Visibility.isVisible(this)){

          this.$$stateChanges = true;
        } else {

          qx.ui.core.queue.Appearance.add(this);
        };
        var forward = this._forwardStates;
        var controls = this.__kd;
        if(forward && forward[state] && controls){

          var control;
          for(var id in controls){

            control = controls[id];
            if(control instanceof qx.ui.core.Widget){

              controls[id].addState(state);
            };
          };
        };
      },
      removeState : function(state){

        var states = this.__gt;
        if(!states || !states[state]){

          return;
        };
        delete this.__gt[state];
        if(state === bk){

          this.syncAppearance();
        } else if(!qx.ui.core.queue.Visibility.isVisible(this)){

          this.$$stateChanges = true;
        } else {

          qx.ui.core.queue.Appearance.add(this);
        };
        var forward = this._forwardStates;
        var controls = this.__kd;
        if(forward && forward[state] && controls){

          for(var id in controls){

            var control = controls[id];
            if(control instanceof qx.ui.core.Widget){

              control.removeState(state);
            };
          };
        };
      },
      replaceState : function(old, value){

        var states = this.__gt;
        if(!states){

          states = this.__gt = {
          };
        };
        if(!states[value]){

          states[value] = true;
        };
        if(states[old]){

          delete states[old];
        };
        if(!qx.ui.core.queue.Visibility.isVisible(this)){

          this.$$stateChanges = true;
        } else {

          qx.ui.core.queue.Appearance.add(this);
        };
        var forward = this._forwardStates;
        var controls = this.__kd;
        if(forward && forward[value] && controls){

          for(var id in controls){

            var control = controls[id];
            if(control instanceof qx.ui.core.Widget){

              control.replaceState(old, value);
            };
          };
        };
      },
      __kb : null,
      __kc : null,
      syncAppearance : function(){

        var states = this.__gt;
        var selector = this.__kb;
        var manager = qx.theme.manager.Appearance.getInstance();
        var styler = qx.core.Property.$$method.setThemed;
        var unstyler = qx.core.Property.$$method.resetThemed;
        if(this.__kc){

          delete this.__kc;
          if(selector){

            var oldData = manager.styleFrom(selector, states, null, this.getAppearance());
            selector = null;
          };
        };
        if(!selector){

          var obj = this;
          var id = [];
          do {

            id.push(obj.$$subcontrol || obj.getAppearance());
          }while((obj = obj.$$subparent));
          selector = id.reverse().join(bX).replace(/#[0-9]+/g, cb);
          this.__kb = selector;
        };
        var newData = manager.styleFrom(selector, states, null, this.getAppearance());
        if(newData){

          if(oldData){

            for(var prop in oldData){

              if(newData[prop] === undefined){

                this[unstyler[prop]]();
              };
            };
          };
          {

            var prop;
          };
          for(var prop in newData){

            newData[prop] === undefined ? this[unstyler[prop]]() : this[styler[prop]](newData[prop]);
          };
        } else if(oldData){

          for(var prop in oldData){

            this[unstyler[prop]]();
          };
        };
        this.fireDataEvent(G, this.__gt);
      },
      _applyAppearance : function(value, old){

        this.updateAppearance();
      },
      checkAppearanceNeeds : function(){

        if(!this.__jR){

          qx.ui.core.queue.Appearance.add(this);
          this.__jR = true;
        } else if(this.$$stateChanges){

          qx.ui.core.queue.Appearance.add(this);
          delete this.$$stateChanges;
        };
      },
      updateAppearance : function(){

        this.__kc = true;
        qx.ui.core.queue.Appearance.add(this);
        var controls = this.__kd;
        if(controls){

          var obj;
          for(var id in controls){

            obj = controls[id];
            if(obj instanceof qx.ui.core.Widget){

              obj.updateAppearance();
            };
          };
        };
      },
      syncWidget : function(jobs){
      },
      getEventTarget : function(){

        var target = this;
        while(target.getAnonymous()){

          target = target.getLayoutParent();
          if(!target){

            return null;
          };
        };
        return target;
      },
      getFocusTarget : function(){

        var target = this;
        if(!target.getEnabled()){

          return null;
        };
        while(target.getAnonymous() || !target.getFocusable()){

          target = target.getLayoutParent();
          if(!target || !target.getEnabled()){

            return null;
          };
        };
        return target;
      },
      getFocusElement : function(){

        return this.getContentElement();
      },
      isTabable : function(){

        return (!!this.getContentElement().getDomElement()) && this.isFocusable();
      },
      _applyFocusable : function(value, old){

        var target = this.getFocusElement();
        if(value){

          var tabIndex = this.getTabIndex();
          if(tabIndex == null){

            tabIndex = 1;
          };
          target.setAttribute(cf, tabIndex);
          target.setStyle(bh, bg);
        } else {

          if(target.isNativelyFocusable()){

            target.setAttribute(cf, -1);
          } else if(old){

            target.setAttribute(cf, null);
          };
        };
      },
      _applyKeepFocus : function(value){

        var target = this.getFocusElement();
        target.setAttribute(cr, value ? bb : null);
      },
      _applyKeepActive : function(value){

        var target = this.getContentElement();
        target.setAttribute(cu, value ? bb : null);
      },
      _applyTabIndex : function(value){

        if(value == null){

          value = 1;
        } else if(value < 1 || value > 32000){

          throw new Error(bq);
        };
        if(this.getFocusable() && value != null){

          this.getFocusElement().setAttribute(cf, value);
        };
      },
      _applySelectable : function(value, old){

        if(old !== null){

          this._applyCursor(this.getCursor());
        };
        this.getContentElement().setSelectable(value);
      },
      _applyEnabled : function(value, old){

        if(value === false){

          this.addState(n);
          this.removeState(bk);
          if(this.isFocusable()){

            this.removeState(m);
            this._applyFocusable(false, true);
          };
          if(this.isDraggable()){

            this._applyDraggable(false, true);
          };
          if(this.isDroppable()){

            this._applyDroppable(false, true);
          };
        } else {

          this.removeState(n);
          if(this.isFocusable()){

            this._applyFocusable(true, false);
          };
          if(this.isDraggable()){

            this._applyDraggable(true, false);
          };
          if(this.isDroppable()){

            this._applyDroppable(true, false);
          };
        };
      },
      _applyNativeContextMenu : function(value, old, name){
      },
      _applyContextMenu : function(value, old){

        if(old){

          old.removeState(cl);
          if(old.getOpener() == this){

            old.resetOpener();
          };
          if(!value){

            this.removeListener(cl, this._onContextMenuOpen);
            old.removeListener(R, this._onBeforeContextMenuOpen, this);
          };
        };
        if(value){

          value.setOpener(this);
          value.addState(cl);
          if(!old){

            this.addListener(cl, this._onContextMenuOpen);
            value.addListener(R, this._onBeforeContextMenuOpen, this);
          };
        };
      },
      _onContextMenuOpen : function(e){

        this.getContextMenu().openAtMouse(e);
        e.stop();
      },
      _onBeforeContextMenuOpen : function(e){

        if(e.getData() == N && this.hasListener(ch)){

          this.fireDataEvent(ch, e);
        };
      },
      _onStopEvent : function(e){

        e.stopPropagation();
      },
      _getDragDropCursor : function(){

        return qx.ui.core.DragDropCursor.getInstance();
      },
      _applyDraggable : function(value, old){

        if(qx.event.handler.MouseEmulation.ON){

          return;
        };
        if(!this.isEnabled() && value === true){

          value = false;
        };
        this._getDragDropCursor();
        if(value){

          this.addListener(s, this._onDragStart);
          this.addListener(b, this._onDrag);
          this.addListener(B, this._onDragEnd);
          this.addListener(w, this._onDragChange);
        } else {

          this.removeListener(s, this._onDragStart);
          this.removeListener(b, this._onDrag);
          this.removeListener(B, this._onDragEnd);
          this.removeListener(w, this._onDragChange);
        };
        this.getContentElement().setAttribute(F, value ? bb : null);
      },
      _applyDroppable : function(value, old){

        if(!this.isEnabled() && value === true){

          value = false;
        };
        this.getContentElement().setAttribute(bt, value ? bb : null);
      },
      _onDragStart : function(e){

        this._getDragDropCursor().placeToMouse(e);
        this.getApplicationRoot().setGlobalCursor(bp);
      },
      _onDrag : function(e){

        this._getDragDropCursor().placeToMouse(e);
      },
      _onDragEnd : function(e){

        this._getDragDropCursor().moveTo(-1000, -1000);
        this.getApplicationRoot().resetGlobalCursor();
      },
      _onDragChange : function(e){

        var cursor = this._getDragDropCursor();
        var action = e.getCurrentAction();
        action ? cursor.setAction(action) : cursor.resetAction();
      },
      visualizeFocus : function(){

        this.addState(m);
      },
      visualizeBlur : function(){

        this.removeState(m);
      },
      scrollChildIntoView : function(child, alignX, alignY, direct){

        direct = typeof direct == cj ? true : direct;
        var Layout = qx.ui.core.queue.Layout;
        var parent;
        if(direct){

          direct = !Layout.isScheduled(child);
          parent = child.getLayoutParent();
          if(direct && parent){

            direct = !Layout.isScheduled(parent);
            if(direct){

              parent.getChildren().forEach(function(sibling){

                direct = direct && !Layout.isScheduled(sibling);
              });
            };
          };
        };
        this.scrollChildIntoViewX(child, alignX, direct);
        this.scrollChildIntoViewY(child, alignY, direct);
      },
      scrollChildIntoViewX : function(child, align, direct){

        this.getContentElement().scrollChildIntoViewX(child.getContentElement(), align, direct);
      },
      scrollChildIntoViewY : function(child, align, direct){

        this.getContentElement().scrollChildIntoViewY(child.getContentElement(), align, direct);
      },
      focus : function(){

        if(this.isFocusable()){

          this.getFocusElement().focus();
        } else {

          throw new Error(bF);
        };
      },
      blur : function(){

        if(this.isFocusable()){

          this.getFocusElement().blur();
        } else {

          throw new Error(bF);
        };
      },
      activate : function(){

        this.getContentElement().activate();
      },
      deactivate : function(){

        this.getContentElement().deactivate();
      },
      tabFocus : function(){

        this.getFocusElement().focus();
      },
      hasChildControl : function(id){

        if(!this.__kd){

          return false;
        };
        return !!this.__kd[id];
      },
      __kd : null,
      _getCreatedChildControls : function(){

        return this.__kd;
      },
      getChildControl : function(id, notcreate){

        if(!this.__kd){

          if(notcreate){

            return null;
          };
          this.__kd = {
          };
        };
        var control = this.__kd[id];
        if(control){

          return control;
        };
        if(notcreate === true){

          return null;
        };
        return this._createChildControl(id);
      },
      _showChildControl : function(id){

        var control = this.getChildControl(id);
        control.show();
        return control;
      },
      _excludeChildControl : function(id){

        var control = this.getChildControl(id, true);
        if(control){

          control.exclude();
        };
      },
      _isChildControlVisible : function(id){

        var control = this.getChildControl(id, true);
        if(control){

          return control.isVisible();
        };
        return false;
      },
      _releaseChildControl : function(id){

        var control = this.getChildControl(id, false);
        if(!control){

          throw new Error(r + id);
        };
        delete control.$$subcontrol;
        delete control.$$subparent;
        var states = this.__gt;
        var forward = this._forwardStates;
        if(states && forward && control instanceof qx.ui.core.Widget){

          for(var state in states){

            if(forward[state]){

              control.removeState(state);
            };
          };
        };
        delete this.__kd[id];
        return control;
      },
      _createChildControl : function(id){

        if(!this.__kd){

          this.__kd = {
          };
        } else if(this.__kd[id]){

          throw new Error(D + id + bu);
        };
        var pos = id.indexOf(L);
        try{

          if(pos == -1){

            var control = this._createChildControlImpl(id);
          } else {

            var control = this._createChildControlImpl(id.substring(0, pos), id.substring(pos + 1, id.length));
          };
        } catch(exc) {

          exc.message = bN + id + I + this.toString() + bw + exc.message;
          throw exc;
        };
        if(!control){

          throw new Error(r + id);
        };
        control.$$subcontrol = id;
        control.$$subparent = this;
        var states = this.__gt;
        var forward = this._forwardStates;
        if(states && forward && control instanceof qx.ui.core.Widget){

          for(var state in states){

            if(forward[state]){

              control.addState(state);
            };
          };
        };
        this.fireDataEvent(q, control);
        return this.__kd[id] = control;
      },
      _createChildControlImpl : function(id, hash){

        return null;
      },
      _disposeChildControls : function(){

        var controls = this.__kd;
        if(!controls){

          return;
        };
        var Widget = qx.ui.core.Widget;
        for(var id in controls){

          var control = controls[id];
          if(!Widget.contains(this, control)){

            control.destroy();
          } else {

            control.dispose();
          };
        };
        delete this.__kd;
      },
      _findTopControl : function(){

        var obj = this;
        while(obj){

          if(!obj.$$subparent){

            return obj;
          };
          obj = obj.$$subparent;
        };
        return null;
      },
      getContentLocation : function(mode){

        var domEl = this.getContentElement().getDomElement();
        return domEl ? qx.bom.element.Location.get(domEl, mode) : null;
      },
      setDomLeft : function(value){

        var domEl = this.getContentElement().getDomElement();
        if(domEl){

          domEl.style.left = value + bn;
        } else {

          throw new Error(bL);
        };
      },
      setDomTop : function(value){

        var domEl = this.getContentElement().getDomElement();
        if(domEl){

          domEl.style.top = value + bn;
        } else {

          throw new Error(bL);
        };
      },
      setDomPosition : function(left, top){

        var domEl = this.getContentElement().getDomElement();
        if(domEl){

          domEl.style.left = left + bn;
          domEl.style.top = top + bn;
        } else {

          throw new Error(bL);
        };
      },
      destroy : function(){

        if(this.$$disposed){

          return;
        };
        var parent = this.$$parent;
        if(parent){

          parent._remove(this);
        };
        qx.ui.core.queue.Dispose.add(this);
      },
      clone : function(){

        var clone = qx.ui.core.LayoutItem.prototype.clone.call(this);
        if(this.getChildren){

          var children = this.getChildren();
          for(var i = 0,l = children.length;i < l;i++){

            clone.add(children[i].clone());
          };
        };
        return clone;
      }
    },
    destruct : function(){

      if(!qx.core.ObjectRegistry.inShutDown){

        if(qx.core.Environment.get(v)){

          if(this.__jS){

            qx.locale.Manager.getInstance().removeListenerById(this.__jS);
          };
        };
        var contentEl = this.getContentElement();
        if(contentEl){

          contentEl.setAttribute(Y, null, true);
        };
        this._disposeChildControls();
        qx.ui.core.queue.Appearance.remove(this);
        qx.ui.core.queue.Layout.remove(this);
        qx.ui.core.queue.Visibility.remove(this);
        qx.ui.core.queue.Widget.remove(this);
      };
      if(this.getContextMenu()){

        this.setContextMenu(null);
      };
      if(!qx.core.ObjectRegistry.inShutDown){

        this.clearSeparators();
        this.__jT = null;
      } else {

        this._disposeArray(cm);
      };
      this._disposeArray(t);
      this.__gt = this.__kd = null;
      this._disposeObjects(E, p);
    }
  });
})();
(function(){

  var a = "blur",b = "activate",c = "focus",d = "qx.ui.core.EventHandler";
  qx.Class.define(d, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(){

      qx.core.Object.call(this);
      this.__dh = qx.event.Registration.getManager(window);
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_FIRST,
      SUPPORTED_TYPES : {
        mousemove : 1,
        mouseover : 1,
        mouseout : 1,
        mousedown : 1,
        mouseup : 1,
        click : 1,
        dblclick : 1,
        contextmenu : 1,
        mousewheel : 1,
        keyup : 1,
        keydown : 1,
        keypress : 1,
        keyinput : 1,
        capture : 1,
        losecapture : 1,
        focusin : 1,
        focusout : 1,
        focus : 1,
        blur : 1,
        activate : 1,
        deactivate : 1,
        appear : 1,
        disappear : 1,
        dragstart : 1,
        dragend : 1,
        dragover : 1,
        dragleave : 1,
        drop : 1,
        drag : 1,
        dragchange : 1,
        droprequest : 1,
        touchstart : 1,
        touchend : 1,
        touchmove : 1,
        touchcancel : 1,
        tap : 1,
        longtap : 1,
        swipe : 1
      },
      IGNORE_CAN_HANDLE : false
    },
    members : {
      __dh : null,
      __ke : {
        focusin : 1,
        focusout : 1,
        focus : 1,
        blur : 1
      },
      __kf : {
        mouseover : 1,
        mouseout : 1,
        appear : 1,
        disappear : 1
      },
      canHandleEvent : function(target, type){

        return target instanceof qx.ui.core.Widget;
      },
      _dispatchEvent : function(domEvent){

        var domTarget = domEvent.getTarget();
        var widgetTarget = qx.ui.core.Widget.getWidgetByElement(domTarget);
        var targetChanged = false;
        while(widgetTarget && widgetTarget.isAnonymous()){

          var targetChanged = true;
          widgetTarget = widgetTarget.getLayoutParent();
        };
        if(widgetTarget && targetChanged && domEvent.getType() == b){

          widgetTarget.getContentElement().activate();
        };
        if(this.__ke[domEvent.getType()]){

          widgetTarget = widgetTarget && widgetTarget.getFocusTarget();
          if(!widgetTarget){

            return;
          };
        };
        if(domEvent.getRelatedTarget){

          var domRelatedTarget = domEvent.getRelatedTarget();
          var widgetRelatedTarget = qx.ui.core.Widget.getWidgetByElement(domRelatedTarget);
          while(widgetRelatedTarget && widgetRelatedTarget.isAnonymous()){

            widgetRelatedTarget = widgetRelatedTarget.getLayoutParent();
          };
          if(widgetRelatedTarget){

            if(this.__ke[domEvent.getType()]){

              widgetRelatedTarget = widgetRelatedTarget.getFocusTarget();
            };
            if(widgetRelatedTarget === widgetTarget){

              return;
            };
          };
        };
        var currentTarget = domEvent.getCurrentTarget();
        var currentWidget = qx.ui.core.Widget.getWidgetByElement(currentTarget);
        if(!currentWidget || currentWidget.isAnonymous()){

          return;
        };
        if(this.__ke[domEvent.getType()]){

          currentWidget = currentWidget.getFocusTarget();
        };
        var type = domEvent.getType();
        if(!currentWidget || !(currentWidget.isEnabled() || this.__kf[type])){

          return;
        };
        var capture = domEvent.getEventPhase() == qx.event.type.Event.CAPTURING_PHASE;
        var listeners = this.__dh.getListeners(currentWidget, type, capture);
        if(!listeners || listeners.length === 0){

          return;
        };
        var widgetEvent = qx.event.Pool.getInstance().getObject(domEvent.constructor);
        domEvent.clone(widgetEvent);
        widgetEvent.setTarget(widgetTarget);
        widgetEvent.setRelatedTarget(widgetRelatedTarget || null);
        widgetEvent.setCurrentTarget(currentWidget);
        var orig = domEvent.getOriginalTarget();
        if(orig){

          var widgetOriginalTarget = qx.ui.core.Widget.getWidgetByElement(orig);
          while(widgetOriginalTarget && widgetOriginalTarget.isAnonymous()){

            widgetOriginalTarget = widgetOriginalTarget.getLayoutParent();
          };
          widgetEvent.setOriginalTarget(widgetOriginalTarget);
        } else {

          widgetEvent.setOriginalTarget(domTarget);
        };
        for(var i = 0,l = listeners.length;i < l;i++){

          var context = listeners[i].context || currentWidget;
          listeners[i].handler.call(context, widgetEvent);
        };
        if(widgetEvent.getPropagationStopped()){

          domEvent.stopPropagation();
        };
        if(widgetEvent.getDefaultPrevented()){

          domEvent.preventDefault();
        };
        qx.event.Pool.getInstance().poolObject(widgetEvent);
      },
      registerEvent : function(target, type, capture){

        var elem;
        if(type === c || type === a){

          elem = target.getFocusElement();
        } else {

          elem = target.getContentElement();
        };
        if(elem){

          elem.addListener(type, this._dispatchEvent, this, capture);
        };
      },
      unregisterEvent : function(target, type, capture){

        var elem;
        if(type === c || type === a){

          elem = target.getFocusElement();
        } else {

          elem = target.getContentElement();
        };
        if(elem){

          elem.removeListener(type, this._dispatchEvent, this, capture);
        };
      }
    },
    destruct : function(){

      this.__dh = null;
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "mshtml",b = "engine.name",c = "__kh",d = "_applyTheme",e = "",f = "'.",g = "qx-",h = "Unable to resolve decorator '",j = "singleton",k = ";",l = "qx.theme.manager.Decoration",m = ".",n = "Theme",o = "object",p = "changeTheme",q = "string",r = "browser.documentmode",s = ":";
  qx.Class.define(l, {
    type : j,
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__fq = [];
      this.__kg = (qx.core.Environment.get(b) == a && qx.core.Environment.get(r) < 9);
    },
    properties : {
      theme : {
        check : n,
        nullable : true,
        apply : d,
        event : p
      }
    },
    members : {
      __kh : null,
      __fq : null,
      __kg : false,
      getCssClassName : function(value){

        if(qx.lang.Type.isString(value)){

          return g + value;
        } else {

          return g + value.toHashCode();
        };
      },
      addCssClass : function(value){

        var sheet = qx.ui.style.Stylesheet.getInstance();
        var instance = value;
        value = this.getCssClassName(value);
        var selector = m + value;
        if(sheet.hasRule(selector)){

          return value;
        };
        if(qx.lang.Type.isString(instance)){

          instance = this.resolve(instance);
        };
        if(!instance){

          throw new Error(h + value + f);
        };
        var css = e;
        var styles = instance.getStyles(true);
        for(var key in styles){

          if(qx.Bootstrap.isObject(styles[key])){

            var innerCss = e;
            var innerStyles = styles[key];
            var inner = false;
            for(var innerKey in innerStyles){

              inner = true;
              innerCss += innerKey + s + innerStyles[innerKey] + k;
            };
            var innerSelector = this.__kg ? selector : selector + (inner ? s : e);
            this.__fq.push(innerSelector + key);
            sheet.addRule(innerSelector + key, innerCss);
            continue;
          };
          css += key + s + styles[key] + k;
        };
        if(css){

          sheet.addRule(selector, css);
          this.__fq.push(selector);
        };
        return value;
      },
      resolve : function(value){

        if(!value){

          return null;
        };
        if(typeof value === o){

          return value;
        };
        var theme = this.getTheme();
        if(!theme){

          return null;
        };
        var cache = this.__kh;
        if(!cache){

          cache = this.__kh = {
          };
        };
        var resolved = cache[value];
        if(resolved){

          return resolved;
        };
        var entry = qx.lang.Object.clone(theme.decorations[value], true);
        if(!entry){

          return null;
        };
        if(!entry.style){

          entry.style = {
          };
        };
        var currentEntry = entry;
        while(currentEntry.include){

          currentEntry = theme.decorations[currentEntry.include];
          if(!entry.decorator && currentEntry.decorator){

            entry.decorator = qx.lang.Object.clone(currentEntry.decorator);
          };
          if(currentEntry.style){

            for(var key in currentEntry.style){

              if(entry.style[key] == undefined){

                entry.style[key] = qx.lang.Object.clone(currentEntry.style[key], true);
              };
            };
          };
        };
        return cache[value] = (new qx.ui.decoration.Decorator()).set(entry.style);
      },
      isValidPropertyValue : function(value){

        if(typeof value === q){

          return this.isDynamic(value);
        } else if(typeof value === o){

          var clazz = value.constructor;
          return qx.Class.hasInterface(clazz, qx.ui.decoration.IDecorator);
        };
        return false;
      },
      isDynamic : function(value){

        if(!value){

          return false;
        };
        var theme = this.getTheme();
        if(!theme){

          return false;
        };
        return !!theme.decorations[value];
      },
      isCached : function(decorator){

        return !this.__kh ? false : qx.lang.Object.contains(this.__kh, decorator);
      },
      _applyTheme : function(value, old){

        var aliasManager = qx.util.AliasManager.getInstance();
        for(var i = 0;i < this.__fq.length;i++){

          var selector = this.__fq[i];
          qx.ui.style.Stylesheet.getInstance().removeRule(selector);
        };
        this.__fq = [];
        if(old){

          for(var alias in old.aliases){

            aliasManager.remove(alias);
          };
        };
        if(value){

          for(var alias in value.aliases){

            aliasManager.add(alias, value.aliases[alias]);
          };
        };
        this._disposeMap(c);
        this.__kh = {
        };
      }
    },
    destruct : function(){

      this._disposeMap(c);
    }
  });
})();
(function(){

  var a = "qx.ui.style.Stylesheet",b = "singleton";
  qx.Class.define(a, {
    type : b,
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__fo = qx.bom.Stylesheet.createElement();
      this.__fq = [];
    },
    members : {
      __fq : null,
      __fo : null,
      addRule : function(selector, css){

        if(this.hasRule(selector)){

          return;
        };
        qx.bom.Stylesheet.addRule(this.__fo, selector, css);
        this.__fq.push(selector);
      },
      hasRule : function(selector){

        return this.__fq.indexOf(selector) != -1;
      },
      removeRule : function(selector){

        delete this.__fq[this.__fq.indexOf(selector)];
        qx.bom.Stylesheet.removeRule(this.__fo, selector);
      }
    },
    destruct : function(){

      qx.bom.Stylesheet.removeSheet(this.__fo);
    }
  });
})();
(function(){

  var a = "qx.ui.decoration.MBackgroundColor",b = 'custom',c = "background-color",d = "Color",e = "_applyBackgroundColor";
  qx.Mixin.define(a, {
    properties : {
      backgroundColor : {
        check : d,
        nullable : true,
        apply : e
      }
    },
    members : {
      _styleBackgroundColor : function(styles){

        var bgcolor = this.getBackgroundColor();
        if(bgcolor && b){

          bgcolor = qx.theme.manager.Color.getInstance().resolve(bgcolor);
        };
        if(bgcolor){

          styles[c] = bgcolor;
        };
      },
      _applyBackgroundColor : function(){

        {
        };
      }
    }
  });
})();
(function(){

  var a = "qx.ui.decoration.IDecorator";
  qx.Interface.define(a, {
    members : {
      getStyles : function(){
      },
      getPadding : function(){
      },
      getInsets : function(){
      }
    }
  });
})();
(function(){

  var a = "abstract",b = "Abstract method called.",c = "qx.ui.decoration.Abstract";
  qx.Class.define(c, {
    extend : qx.core.Object,
    implement : [qx.ui.decoration.IDecorator],
    type : a,
    members : {
      __ki : null,
      _getDefaultInsets : function(){

        throw new Error(b);
      },
      _isInitialized : function(){

        throw new Error(b);
      },
      _resetInsets : function(){

        this.__ki = null;
      },
      getInsets : function(){

        if(this.__ki){

          return this.__ki;
        };
        return this._getDefaultInsets();
      }
    },
    destruct : function(){

      this.__ki = null;
    }
  });
})();
(function(){

  var a = "double",b = "px ",c = "widthTop",d = "inset",e = "solid",f = "dotted",g = "styleRight",h = "styleBottom",i = "_applyWidth",j = "border-top",k = "border-left",l = "ridge",m = "border-right",n = "qx.ui.decoration.MSingleBorder",o = "shorthand",p = "Color",q = "widthBottom",r = "outset",s = "widthLeft",t = "",u = "border-bottom",v = "styleTop",w = "colorBottom",x = "groove",y = "styleLeft",z = "widthRight",A = "dashed",B = "Number",C = "colorLeft",D = "colorRight",E = "colorTop",F = "_applyStyle",G = " ",H = "absolute";
  qx.Mixin.define(n, {
    properties : {
      widthTop : {
        check : B,
        init : 0,
        apply : i
      },
      widthRight : {
        check : B,
        init : 0,
        apply : i
      },
      widthBottom : {
        check : B,
        init : 0,
        apply : i
      },
      widthLeft : {
        check : B,
        init : 0,
        apply : i
      },
      styleTop : {
        nullable : true,
        check : [e, f, A, a, d, r, l, x],
        init : e,
        apply : F
      },
      styleRight : {
        nullable : true,
        check : [e, f, A, a, d, r, l, x],
        init : e,
        apply : F
      },
      styleBottom : {
        nullable : true,
        check : [e, f, A, a, d, r, l, x],
        init : e,
        apply : F
      },
      styleLeft : {
        nullable : true,
        check : [e, f, A, a, d, r, l, x],
        init : e,
        apply : F
      },
      colorTop : {
        nullable : true,
        check : p,
        apply : F
      },
      colorRight : {
        nullable : true,
        check : p,
        apply : F
      },
      colorBottom : {
        nullable : true,
        check : p,
        apply : F
      },
      colorLeft : {
        nullable : true,
        check : p,
        apply : F
      },
      left : {
        group : [s, y, C]
      },
      right : {
        group : [z, g, D]
      },
      top : {
        group : [c, v, E]
      },
      bottom : {
        group : [q, h, w]
      },
      width : {
        group : [c, z, q, s],
        mode : o
      },
      style : {
        group : [v, g, h, y],
        mode : o
      },
      color : {
        group : [E, D, w, C],
        mode : o
      }
    },
    members : {
      _styleBorder : function(styles){

        {

          var Color = qx.theme.manager.Color.getInstance();
          var colorTop = Color.resolve(this.getColorTop());
          var colorRight = Color.resolve(this.getColorRight());
          var colorBottom = Color.resolve(this.getColorBottom());
          var colorLeft = Color.resolve(this.getColorLeft());
        };
        var width = this.getWidthTop();
        if(width > 0){

          styles[j] = width + b + this.getStyleTop() + G + (colorTop || t);
        };
        var width = this.getWidthRight();
        if(width > 0){

          styles[m] = width + b + this.getStyleRight() + G + (colorRight || t);
        };
        var width = this.getWidthBottom();
        if(width > 0){

          styles[u] = width + b + this.getStyleBottom() + G + (colorBottom || t);
        };
        var width = this.getWidthLeft();
        if(width > 0){

          styles[k] = width + b + this.getStyleLeft() + G + (colorLeft || t);
        };
        {
        };
        styles.position = H;
      },
      _getDefaultInsetsForBorder : function(){

        return {
          top : this.getWidthTop(),
          right : this.getWidthRight(),
          bottom : this.getWidthBottom(),
          left : this.getWidthLeft()
        };
      },
      _applyWidth : function(){

        this._applyStyle();
        this._resetInsets();
      },
      _applyStyle : function(){

        {
        };
      }
    }
  });
})();
(function(){

  var a = ', url(',b = "repeat",c = "backgroundPositionX",d = "backgroundPositionY",e = "px",f = "background-position",g = " ",h = "background-repeat",i = "no-repeat",j = ')',k = "scale",l = "_applyBackgroundPosition",m = 'url(',n = "repeat-x",o = "background-image",p = "100% 100%",q = "repeat-y",r = "qx.ui.decoration.MBackgroundImage",s = "background-size",t = "String",u = "_applyBackgroundImage";
  qx.Mixin.define(r, {
    properties : {
      backgroundImage : {
        check : t,
        nullable : true,
        apply : u
      },
      backgroundRepeat : {
        check : [b, n, q, i, k],
        init : b,
        apply : u
      },
      backgroundPositionX : {
        nullable : true,
        apply : l
      },
      backgroundPositionY : {
        nullable : true,
        apply : l
      },
      backgroundPosition : {
        group : [d, c]
      }
    },
    members : {
      _styleBackgroundImage : function(styles){

        var image = this.getBackgroundImage();
        if(!image){

          return;
        };
        var id = qx.util.AliasManager.getInstance().resolve(image);
        var source = qx.util.ResourceManager.getInstance().toUri(id);
        if(styles[o]){

          styles[o] += a + source + j;
        } else {

          styles[o] = m + source + j;
        };
        var repeat = this.getBackgroundRepeat();
        if(repeat === k){

          styles[s] = p;
        } else {

          styles[h] = repeat;
        };
        var top = this.getBackgroundPositionY() || 0;
        var left = this.getBackgroundPositionX() || 0;
        if(!isNaN(top)){

          top += e;
        };
        if(!isNaN(left)){

          left += e;
        };
        styles[f] = left + g + top;
        {
        };
      },
      _applyBackgroundImage : function(){

        {
        };
      },
      _applyBackgroundPosition : function(){

        {
        };
      }
    }
  });
})();
(function(){

  var a = "0",b = "qx/static",c = "http://",d = "https://",e = "file://",f = "qx.util.AliasManager",g = "singleton",h = ".",i = "/",j = "static";
  qx.Class.define(f, {
    type : g,
    extend : qx.util.ValueManager,
    construct : function(){

      qx.util.ValueManager.call(this);
      this.__kj = {
      };
      this.add(j, b);
    },
    members : {
      __kj : null,
      _preprocess : function(value){

        var dynamics = this._getDynamic();
        if(dynamics[value] === false){

          return value;
        } else if(dynamics[value] === undefined){

          if(value.charAt(0) === i || value.charAt(0) === h || value.indexOf(c) === 0 || value.indexOf(d) === a || value.indexOf(e) === 0){

            dynamics[value] = false;
            return value;
          };
          if(this.__kj[value]){

            return this.__kj[value];
          };
          var alias = value.substring(0, value.indexOf(i));
          var resolved = this.__kj[alias];
          if(resolved !== undefined){

            dynamics[value] = resolved + value.substring(alias.length);
          };
        };
        return value;
      },
      add : function(alias, base){

        this.__kj[alias] = base;
        var dynamics = this._getDynamic();
        for(var path in dynamics){

          if(path.substring(0, path.indexOf(i)) === alias){

            dynamics[path] = base + path.substring(alias.length);
          };
        };
      },
      remove : function(alias){

        delete this.__kj[alias];
      },
      resolve : function(path){

        var dynamic = this._getDynamic();
        if(path != null){

          path = this._preprocess(path);
        };
        return dynamic[path] || path;
      },
      getAliases : function(){

        var res = {
        };
        for(var key in this.__kj){

          res[key] = this.__kj[key];
        };
        return res;
      }
    },
    destruct : function(){

      this.__kj = null;
    }
  });
})();
(function(){

  var a = "innerWidthRight",b = "innerColorBottom",c = "css.borderradius",d = "px ",e = '""',f = "_applyDoubleBorder",g = "border-top",h = "inset 0 -",i = "css.boxsizing",j = "innerWidthTop",k = "100%",l = "border-left",m = "innerColorRight",n = "css.boxshadow",o = "innerColorTop",p = "innerColorLeft",q = "inset ",r = "shorthand",s = "inset -",t = "Color",u = "border-box",v = "qx.ui.decoration.MDoubleBorder",w = "border-bottom",x = ":before",y = "inset 0 ",z = "px solid ",A = "innerWidthBottom",B = "css.rgba",C = "inherit",D = "Number",E = "innerWidthLeft",F = "px 0 ",G = "inset 0 0 0 ",H = "border-right",I = " ",J = ",",K = "absolute";
  qx.Mixin.define(v, {
    include : [qx.ui.decoration.MSingleBorder, qx.ui.decoration.MBackgroundImage],
    construct : function(){

      this._getDefaultInsetsForBorder = this.__km;
      this._styleBorder = this.__kk;
    },
    properties : {
      innerWidthTop : {
        check : D,
        init : 0,
        apply : f
      },
      innerWidthRight : {
        check : D,
        init : 0,
        apply : f
      },
      innerWidthBottom : {
        check : D,
        init : 0,
        apply : f
      },
      innerWidthLeft : {
        check : D,
        init : 0,
        apply : f
      },
      innerWidth : {
        group : [j, a, A, E],
        mode : r
      },
      innerColorTop : {
        nullable : true,
        check : t,
        apply : f
      },
      innerColorRight : {
        nullable : true,
        check : t,
        apply : f
      },
      innerColorBottom : {
        nullable : true,
        check : t,
        apply : f
      },
      innerColorLeft : {
        nullable : true,
        check : t,
        apply : f
      },
      innerColor : {
        group : [o, m, b, p],
        mode : r
      },
      innerOpacity : {
        check : D,
        init : 1,
        apply : f
      }
    },
    members : {
      __kk : function(styles){

        var propName = qx.core.Environment.get(n);
        var color,innerColor,innerWidth;
        {

          var Color = qx.theme.manager.Color.getInstance();
          color = {
            top : Color.resolve(this.getColorTop()),
            right : Color.resolve(this.getColorRight()),
            bottom : Color.resolve(this.getColorBottom()),
            left : Color.resolve(this.getColorLeft())
          };
          innerColor = {
            top : Color.resolve(this.getInnerColorTop()),
            right : Color.resolve(this.getInnerColorRight()),
            bottom : Color.resolve(this.getInnerColorBottom()),
            left : Color.resolve(this.getInnerColorLeft())
          };
        };
        innerWidth = {
          top : this.getInnerWidthTop(),
          right : this.getInnerWidthRight(),
          bottom : this.getInnerWidthBottom(),
          left : this.getInnerWidthLeft()
        };
        var width = this.getWidthTop();
        if(width > 0){

          styles[g] = width + d + this.getStyleTop() + I + color.top;
        };
        width = this.getWidthRight();
        if(width > 0){

          styles[H] = width + d + this.getStyleRight() + I + color.right;
        };
        width = this.getWidthBottom();
        if(width > 0){

          styles[w] = width + d + this.getStyleBottom() + I + color.bottom;
        };
        width = this.getWidthLeft();
        if(width > 0){

          styles[l] = width + d + this.getStyleLeft() + I + color.left;
        };
        var innerOpacity = this.getInnerOpacity();
        if(innerOpacity < 1){

          this.__kl(innerColor, innerOpacity);
        };
        if(innerWidth.top > 0 || innerWidth.right > 0 || innerWidth.bottom > 0 || innerWidth.left > 0){

          var borderTop = (innerWidth.top || 0) + z + innerColor.top;
          var borderRight = (innerWidth.right || 0) + z + innerColor.right;
          var borderBottom = (innerWidth.bottom || 0) + z + innerColor.bottom;
          var borderLeft = (innerWidth.left || 0) + z + innerColor.left;
          styles[x] = {
            "width" : k,
            "height" : k,
            "position" : K,
            "content" : e,
            "border-top" : borderTop,
            "border-right" : borderRight,
            "border-bottom" : borderBottom,
            "border-left" : borderLeft,
            "left" : 0,
            "top" : 0
          };
          var boxSizingKey = qx.bom.Style.getCssName(qx.core.Environment.get(i));
          styles[x][boxSizingKey] = u;
          var borderRadiusKey = qx.core.Environment.get(c);
          if(borderRadiusKey){

            borderRadiusKey = qx.bom.Style.getCssName(borderRadiusKey);
            styles[x][borderRadiusKey] = C;
          };
          var shadowStyle = [];
          if(innerColor.top && innerWidth.top && innerColor.top == innerColor.bottom && innerColor.top == innerColor.right && innerColor.top == innerColor.left && innerWidth.top == innerWidth.bottom && innerWidth.top == innerWidth.right && innerWidth.top == innerWidth.left){

            shadowStyle.push(G + innerWidth.top + d + innerColor.top);
          } else {

            if(innerColor.top){

              shadowStyle.push(y + (innerWidth.top || 0) + d + innerColor.top);
            };
            if(innerColor.right){

              shadowStyle.push(s + (innerWidth.right || 0) + F + innerColor.right);
            };
            if(innerColor.bottom){

              shadowStyle.push(h + (innerWidth.bottom || 0) + d + innerColor.bottom);
            };
            if(innerColor.left){

              shadowStyle.push(q + (innerWidth.left || 0) + F + innerColor.left);
            };
          };
          if(shadowStyle.length > 0 && propName){

            propName = qx.bom.Style.getCssName(propName);
            if(!styles[propName]){

              styles[propName] = shadowStyle.join(J);
            } else {

              styles[propName] += J + shadowStyle.join(J);
            };
          };
        };
      },
      __kl : function(innerColor, innerOpacity){

        if(!qx.core.Environment.get(B)){

          {
          };
          return;
        };
        for(var edge in innerColor){

          var rgb = qx.util.ColorUtil.stringToRgb(innerColor[edge]);
          rgb.push(innerOpacity);
          var rgbString = qx.util.ColorUtil.rgbToRgbString(rgb);
          innerColor[edge] = rgbString;
        };
      },
      _applyDoubleBorder : function(){

        {
        };
      },
      __km : function(){

        return {
          top : this.getWidthTop() + this.getInnerWidthTop(),
          right : this.getWidthRight() + this.getInnerWidthRight(),
          bottom : this.getWidthBottom() + this.getInnerWidthBottom(),
          left : this.getWidthLeft() + this.getInnerWidthLeft()
        };
      }
    }
  });
})();
(function(){

  var a = "radiusTopRight",b = "radiusTopLeft",c = "px",d = "-webkit-border-bottom-left-radius",e = "-webkit-background-clip",f = "radiusBottomRight",g = "Integer",h = "-webkit-border-bottom-right-radius",i = "background-clip",j = "border-top-left-radius",k = "border-top-right-radius",l = "border-bottom-left-radius",m = "radiusBottomLeft",n = "-webkit-border-top-left-radius",o = "shorthand",p = "-moz-border-radius-bottomright",q = "padding-box",r = "border-bottom-right-radius",s = "qx.ui.decoration.MBorderRadius",t = "-moz-border-radius-topright",u = "engine.name",v = "_applyBorderRadius",w = "-webkit-border-top-right-radius",x = "webkit",y = "-moz-border-radius-topleft",z = "-moz-border-radius-bottomleft";
  qx.Mixin.define(s, {
    properties : {
      radiusTopLeft : {
        nullable : true,
        check : g,
        apply : v
      },
      radiusTopRight : {
        nullable : true,
        check : g,
        apply : v
      },
      radiusBottomLeft : {
        nullable : true,
        check : g,
        apply : v
      },
      radiusBottomRight : {
        nullable : true,
        check : g,
        apply : v
      },
      radius : {
        group : [b, a, f, m],
        mode : o
      }
    },
    members : {
      _styleBorderRadius : function(styles){

        styles[e] = q;
        styles[i] = q;
        var hasRadius = false;
        var radius = this.getRadiusTopLeft();
        if(radius > 0){

          hasRadius = true;
          styles[y] = radius + c;
          styles[n] = radius + c;
          styles[j] = radius + c;
        };
        radius = this.getRadiusTopRight();
        if(radius > 0){

          hasRadius = true;
          styles[t] = radius + c;
          styles[w] = radius + c;
          styles[k] = radius + c;
        };
        radius = this.getRadiusBottomLeft();
        if(radius > 0){

          hasRadius = true;
          styles[z] = radius + c;
          styles[d] = radius + c;
          styles[l] = radius + c;
        };
        radius = this.getRadiusBottomRight();
        if(radius > 0){

          hasRadius = true;
          styles[p] = radius + c;
          styles[h] = radius + c;
          styles[r] = radius + c;
        };
        if(hasRadius && qx.core.Environment.get(u) == x){

          styles[e] = q;
        } else {

          styles[i] = q;
        };
      },
      _applyBorderRadius : function(){

        {
        };
      }
    }
  });
})();
(function(){

  var a = "border-width",b = "css.borderimage.standardsyntax",c = "repeat",d = "Boolean",e = "-l",f = "stretch",g = "px ",h = "sliceBottom",i = "-t",j = "Integer",k = "solid",l = "borderImage",m = "-r",n = "border-style",o = "sliceLeft",p = "-b",q = "sliceRight",r = "px",s = "repeatX",t = " fill",u = "String",v = "vertical",w = "",x = "transparent",y = "round",z = '") ',A = "shorthand",B = "qx.ui.decoration.MBorderImage",C = "sliceTop",D = "horizontal",E = "_applyBorderImage",F = "border-color",G = 'url("',H = " ",I = "grid",J = "repeatY";
  qx.Mixin.define(B, {
    properties : {
      borderImage : {
        check : u,
        nullable : true,
        apply : E
      },
      sliceTop : {
        check : j,
        nullable : true,
        init : null,
        apply : E
      },
      sliceRight : {
        check : j,
        nullable : true,
        init : null,
        apply : E
      },
      sliceBottom : {
        check : j,
        nullable : true,
        init : null,
        apply : E
      },
      sliceLeft : {
        check : j,
        nullable : true,
        init : null,
        apply : E
      },
      slice : {
        group : [C, q, h, o],
        mode : A
      },
      repeatX : {
        check : [f, c, y],
        init : f,
        apply : E
      },
      repeatY : {
        check : [f, c, y],
        init : f,
        apply : E
      },
      repeat : {
        group : [s, J],
        mode : A
      },
      fill : {
        check : d,
        init : true,
        apply : E
      },
      borderImageMode : {
        check : [D, v, I],
        init : I
      }
    },
    members : {
      _styleBorderImage : function(styles){

        if(!this.getBorderImage()){

          return;
        };
        var resolvedImage = qx.util.AliasManager.getInstance().resolve(this.getBorderImage());
        var source = qx.util.ResourceManager.getInstance().toUri(resolvedImage);
        var computedSlices = this._getDefaultInsetsForBorderImage();
        var slice = [computedSlices.top, computedSlices.right, computedSlices.bottom, computedSlices.left];
        var repeat = [this.getRepeatX(), this.getRepeatY()].join(H);
        var fill = this.getFill() && qx.core.Environment.get(b) ? t : w;
        var styleName = qx.bom.Style.getPropertyName(l);
        if(styleName){

          var cssName = qx.bom.Style.getCssName(styleName);
          styles[cssName] = G + source + z + slice.join(H) + fill + H + repeat;
        };
        styles[n] = k;
        styles[F] = x;
        styles[a] = slice.join(g) + r;
      },
      _getDefaultInsetsForBorderImage : function(){

        if(!this.getBorderImage()){

          return {
            top : 0,
            right : 0,
            bottom : 0,
            left : 0
          };
        };
        var resolvedImage = qx.util.AliasManager.getInstance().resolve(this.getBorderImage());
        var computedSlices = this.__kn(resolvedImage);
        return {
          top : this.getSliceTop() || computedSlices[0],
          right : this.getSliceRight() || computedSlices[1],
          bottom : this.getSliceBottom() || computedSlices[2],
          left : this.getSliceLeft() || computedSlices[3]
        };
      },
      _applyBorderImage : function(){

        {
        };
      },
      __kn : function(baseImage){

        var mode = this.getBorderImageMode();
        var topSlice = 0;
        var rightSlice = 0;
        var bottomSlice = 0;
        var leftSlice = 0;
        var split = /(.*)(\.[a-z]+)$/.exec(baseImage);
        var prefix = split[1];
        var ext = split[2];
        var ResourceManager = qx.util.ResourceManager.getInstance();
        if(mode == I || mode == v){

          topSlice = ResourceManager.getImageHeight(prefix + i + ext);
          bottomSlice = ResourceManager.getImageHeight(prefix + p + ext);
        };
        if(mode == I || mode == D){

          rightSlice = ResourceManager.getImageWidth(prefix + m + ext);
          leftSlice = ResourceManager.getImageWidth(prefix + e + ext);
        };
        return [topSlice, rightSlice, bottomSlice, leftSlice];
      }
    }
  });
})();
(function(){

  var a = " 0",b = "</div>",c = "),to(",d = "px",e = "css.borderradius",f = "from(",g = ")",h = "background-image",i = "background",j = "<div style='width: 100%; height: 100%; position: absolute;",k = "filter",l = "background-size",m = "', ",n = "'></div>",o = "0",p = "_applyLinearBackgroundGradient",q = "-webkit-gradient(linear,",r = "startColorPosition",s = "background-color",t = "deg, ",u = "url(",v = "css.gradient.legacywebkit",w = "EndColorStr='#FF",x = "startColor",y = "shorthand",z = "100% 100%",A = "Color",B = '<div style=\"position: absolute; width: 100%; height: 100%; filter:progid:DXImageTransform.Microsoft.Gradient(GradientType=',C = "MBoxShadow",D = "StartColorStr='#FF",E = "vertical",F = "",G = "transparent",H = "qx.ui.decoration.MLinearBackgroundGradient",I = "(",J = "endColorPosition",K = "canvas",L = "';)\">",M = "css.gradient.linear",N = "';)",O = "endColor",P = ", ",Q = "css.gradient.filter",R = "horizontal",S = "Number",T = '2d',U = "%",V = " ",W = "white",X = "linear-gradient",Y = 'progid:DXImageTransform.Microsoft.Gradient(GradientType=',ba = ",";
  qx.Mixin.define(H, {
    properties : {
      startColor : {
        check : A,
        nullable : true,
        apply : p
      },
      endColor : {
        check : A,
        nullable : true,
        apply : p
      },
      orientation : {
        check : [R, E],
        init : E,
        apply : p
      },
      startColorPosition : {
        check : S,
        init : 0,
        apply : p
      },
      endColorPosition : {
        check : S,
        init : 100,
        apply : p
      },
      colorPositionUnit : {
        check : [d, U],
        init : U,
        apply : p
      },
      gradientStart : {
        group : [x, r],
        mode : y
      },
      gradientEnd : {
        group : [O, J],
        mode : y
      }
    },
    members : {
      __ko : null,
      _styleLinearBackgroundGradient : function(styles){

        var colors = this.__kp();
        var startColor = colors.start;
        var endColor = colors.end;
        var value;
        if(!startColor || !endColor){

          return;
        };
        var unit = this.getColorPositionUnit();
        if(qx.core.Environment.get(v)){

          unit = unit === d ? F : unit;
          if(this.getOrientation() == R){

            var startPos = this.getStartColorPosition() + unit + a + unit;
            var endPos = this.getEndColorPosition() + unit + a + unit;
          } else {

            var startPos = o + unit + V + this.getStartColorPosition() + unit;
            var endPos = o + unit + V + this.getEndColorPosition() + unit;
          };
          var color = f + startColor + c + endColor + g;
          value = q + startPos + ba + endPos + ba + color + g;
          styles[i] = value;
        } else if(qx.core.Environment.get(Q) && !qx.core.Environment.get(M) && qx.core.Environment.get(e)){

          if(!this.__ko){

            this.__ko = document.createElement(K);
          };
          var isVertical = this.getOrientation() == E;
          var colors = this.__kp();
          var height = isVertical ? 200 : 1;
          var width = isVertical ? 1 : 200;
          this.__ko.width = width;
          this.__ko.height = height;
          var ctx = this.__ko.getContext(T);
          if(isVertical){

            var lingrad = ctx.createLinearGradient(0, 0, 0, height);
          } else {

            var lingrad = ctx.createLinearGradient(0, 0, width, 0);
          };
          lingrad.addColorStop(this.getStartColorPosition() / 100, colors.start);
          lingrad.addColorStop(this.getEndColorPosition() / 100, colors.end);
          ctx.fillStyle = lingrad;
          ctx.fillRect(0, 0, width, height);
          var value = u + this.__ko.toDataURL() + g;
          styles[h] = value;
          styles[l] = z;
        } else if(qx.core.Environment.get(Q) && !qx.core.Environment.get(M)){

          var colors = this.__kp();
          var type = this.getOrientation() == R ? 1 : 0;
          var startColor = colors.start;
          var endColor = colors.end;
          if(!qx.util.ColorUtil.isHex6String(startColor)){

            startColor = qx.util.ColorUtil.stringToRgb(startColor);
            startColor = qx.util.ColorUtil.rgbToHexString(startColor);
          };
          if(!qx.util.ColorUtil.isHex6String(endColor)){

            endColor = qx.util.ColorUtil.stringToRgb(endColor);
            endColor = qx.util.ColorUtil.rgbToHexString(endColor);
          };
          startColor = startColor.substring(1, startColor.length);
          endColor = endColor.substring(1, endColor.length);
          value = Y + type + P + D + startColor + m + w + endColor + N;
          if(styles[k]){

            styles[k] += P + value;
          } else {

            styles[k] = value;
          };
          if(!styles[s] || styles[s] == G){

            styles[s] = W;
          };
        } else {

          var deg = this.getOrientation() == R ? 0 : 270;
          var start = startColor + V + this.getStartColorPosition() + unit;
          var end = endColor + V + this.getEndColorPosition() + unit;
          var prefixedName = qx.core.Environment.get(M);
          if(prefixedName === X){

            deg = this.getOrientation() == R ? deg + 90 : deg - 90;
          };
          value = prefixedName + I + deg + t + start + ba + end + g;
          if(styles[h]){

            styles[h] += P + value;
          } else {

            styles[h] = value;
          };
        };;
      },
      __kp : function(){

        {

          var Color = qx.theme.manager.Color.getInstance();
          var startColor = Color.resolve(this.getStartColor());
          var endColor = Color.resolve(this.getEndColor());
        };
        return {
          start : startColor,
          end : endColor
        };
      },
      _getContent : function(){

        if(qx.core.Environment.get(Q) && !qx.core.Environment.get(M)){

          var colors = this.__kp();
          var type = this.getOrientation() == R ? 1 : 0;
          var startColor = qx.util.ColorUtil.hex3StringToHex6String(colors.start);
          var endColor = qx.util.ColorUtil.hex3StringToHex6String(colors.end);
          startColor = startColor.substring(1, startColor.length);
          endColor = endColor.substring(1, endColor.length);
          var shadow = F;
          if(this.classname.indexOf(C) != -1){

            var styles = {
            };
            this._styleBoxShadow(styles);
            shadow = j + qx.bom.element.Style.compile(styles) + n;
          };
          return B + type + P + D + startColor + m + w + endColor + L + shadow + b;
        };
        return F;
      },
      _applyLinearBackgroundGradient : function(){

        {
        };
      }
    }
  });
})();
(function(){

  var a = "_applyBoxShadow",b = "shadowHorizontalLength",c = "Boolean",d = "",e = "px ",f = "css.boxshadow",g = "shadowVerticalLength",h = "inset ",i = "shorthand",j = "qx.ui.decoration.MBoxShadow",k = "Integer",l = "Color",m = ",";
  qx.Mixin.define(j, {
    properties : {
      shadowHorizontalLength : {
        nullable : true,
        check : k,
        apply : a
      },
      shadowVerticalLength : {
        nullable : true,
        check : k,
        apply : a
      },
      shadowBlurRadius : {
        nullable : true,
        check : k,
        apply : a
      },
      shadowSpreadRadius : {
        nullable : true,
        check : k,
        apply : a
      },
      shadowColor : {
        nullable : true,
        check : l,
        apply : a
      },
      inset : {
        init : false,
        check : c,
        apply : a
      },
      shadowLength : {
        group : [b, g],
        mode : i
      }
    },
    members : {
      _styleBoxShadow : function(styles){

        var propName = qx.core.Environment.get(f);
        if(!propName || this.getShadowVerticalLength() == null && this.getShadowHorizontalLength() == null){

          return;
        };
        {

          var Color = qx.theme.manager.Color.getInstance();
          var color = Color.resolve(this.getShadowColor());
        };
        if(color != null){

          var vLength = this.getShadowVerticalLength() || 0;
          var hLength = this.getShadowHorizontalLength() || 0;
          var blur = this.getShadowBlurRadius() || 0;
          var spread = this.getShadowSpreadRadius() || 0;
          var inset = this.getInset() ? h : d;
          var value = inset + hLength + e + vLength + e + blur + e + spread + e + color;
          propName = qx.bom.Style.getCssName(propName);
          if(!styles[propName]){

            styles[propName] = value;
          } else {

            styles[propName] += m + value;
          };
        };
      },
      _applyBoxShadow : function(){

        {
        };
      }
    }
  });
})();
(function(){

  var a = "qx.ui.decoration.Decorator",b = "_style",c = "_getDefaultInsetsFor",d = "bottom",e = "top",f = "left",g = "right";
  qx.Class.define(a, {
    extend : qx.ui.decoration.Abstract,
    implement : [qx.ui.decoration.IDecorator],
    include : [qx.ui.decoration.MBackgroundColor, qx.ui.decoration.MBorderRadius, qx.ui.decoration.MBoxShadow, qx.ui.decoration.MDoubleBorder, qx.ui.decoration.MLinearBackgroundGradient, qx.ui.decoration.MBorderImage],
    members : {
      __kq : false,
      getPadding : function(){

        var insets = this.getInset();
        var slices = this._getDefaultInsetsForBorderImage();
        var borderTop = insets.top - (slices.top ? slices.top : this.getWidthTop());
        var borderRight = insets.right - (slices.right ? slices.right : this.getWidthRight());
        var borderBottom = insets.bottom - (slices.bottom ? slices.bottom : this.getWidthBottom());
        var borderLeft = insets.left - (slices.left ? slices.left : this.getWidthLeft());
        return {
          top : insets.top ? borderTop : this.getInnerWidthTop(),
          right : insets.right ? borderRight : this.getInnerWidthRight(),
          bottom : insets.bottom ? borderBottom : this.getInnerWidthBottom(),
          left : insets.left ? borderLeft : this.getInnerWidthLeft()
        };
      },
      getStyles : function(css){

        if(css){

          return this._getStyles();
        };
        var jsStyles = {
        };
        var cssStyles = this._getStyles();
        for(var property in cssStyles){

          jsStyles[qx.lang.String.camelCase(property)] = cssStyles[property];
        };
        return jsStyles;
      },
      _getStyles : function(){

        var styles = {
        };
        for(var name in this){

          if(name.indexOf(b) == 0 && this[name] instanceof Function){

            this[name](styles);
          };
        };
        this.__kq = true;
        return styles;
      },
      _getDefaultInsets : function(){

        var directions = [e, g, d, f];
        var defaultInsets = {
        };
        for(var name in this){

          if(name.indexOf(c) == 0 && this[name] instanceof Function){

            var currentInsets = this[name]();
            for(var i = 0;i < directions.length;i++){

              var direction = directions[i];
              if(defaultInsets[direction] == undefined){

                defaultInsets[direction] = currentInsets[direction];
              };
              if(currentInsets[direction] > defaultInsets[direction]){

                defaultInsets[direction] = currentInsets[direction];
              };
            };
          };
        };
        if(defaultInsets[e] != undefined){

          return defaultInsets;
        };
        return {
          top : 0,
          right : 0,
          bottom : 0,
          left : 0
        };
      },
      _isInitialized : function(){

        return this.__kq;
      }
    }
  });
})();
(function(){

  var a = "Image could not be loaded: ",b = "Boolean",c = "px",d = ".png",e = "background-image",f = "engine.version",g = "scale",h = "changeSource",i = "div",j = "nonScaled",k = "qx.ui.basic.Image",l = "loaded",m = "0 0",n = ", no-repeat",o = "replacement",p = "backgroundImage",q = "backgroundRepeat",r = "-disabled.$1",s = "class",t = "qx.event.type.Event",u = "loadingFailed",v = "css.alphaimageloaderneeded",w = "String",x = "browser.documentmode",y = "backgroundPosition",z = "border-box",A = "__kr",B = "left",C = "_applySource",D = "$$widget",E = "top",F = "scaled",G = ", ",H = "image",I = "mshtml",J = "engine.name",K = ", 0 0",L = "_applyScale",M = "position",N = "img",O = "no-repeat",P = "background-position",Q = "hidden",R = "alphaScaled",S = ",",T = "absolute";
  qx.Class.define(k, {
    extend : qx.ui.core.Widget,
    construct : function(source){

      this.__kr = {
      };
      qx.ui.core.Widget.call(this);
      if(source){

        this.setSource(source);
      };
    },
    properties : {
      source : {
        check : w,
        init : null,
        nullable : true,
        event : h,
        apply : C,
        themeable : true
      },
      scale : {
        check : b,
        init : false,
        themeable : true,
        apply : L
      },
      appearance : {
        refine : true,
        init : H
      },
      allowShrinkX : {
        refine : true,
        init : false
      },
      allowShrinkY : {
        refine : true,
        init : false
      },
      allowGrowX : {
        refine : true,
        init : false
      },
      allowGrowY : {
        refine : true,
        init : false
      }
    },
    events : {
      loadingFailed : t,
      loaded : t
    },
    members : {
      __ks : null,
      __kt : null,
      __dn : null,
      __kr : null,
      __ku : null,
      __kv : null,
      _onChangeTheme : function(){

        qx.ui.core.Widget.prototype._onChangeTheme.call(this);
        this._styleSource();
      },
      getContentElement : function(){

        return this.__kz();
      },
      _createContentElement : function(){

        return this.__kz();
      },
      _getContentHint : function(){

        return {
          width : this.__ks || 0,
          height : this.__kt || 0
        };
      },
      _applyDecorator : function(value, old){

        qx.ui.core.Widget.prototype._applyDecorator.call(this, value, old);
        var source = this.getSource();
        source = qx.util.AliasManager.getInstance().resolve(source);
        var el = this.getContentElement();
        if(this.__kv){

          el = el.getChild(0);
        };
        this.__kF(el, source);
      },
      _applyPadding : function(value, old, name){

        qx.ui.core.Widget.prototype._applyPadding.call(this, value, old, name);
        var element = this.getContentElement();
        if(this.__kv){

          element.getChild(0).setStyles({
            top : this.getPaddingTop() || 0,
            left : this.getPaddingLeft() || 0
          });
        } else {

          element.setPadding(this.getPaddingLeft() || 0, this.getPaddingTop() || 0);
        };
      },
      renderLayout : function(left, top, width, height){

        qx.ui.core.Widget.prototype.renderLayout.call(this, left, top, width, height);
        var element = this.getContentElement();
        if(this.__kv){

          element.getChild(0).setStyles({
            width : width - (this.getPaddingLeft() || 0) - (this.getPaddingRight() || 0),
            height : height - (this.getPaddingTop() || 0) - (this.getPaddingBottom() || 0),
            top : this.getPaddingTop() || 0,
            left : this.getPaddingLeft() || 0
          });
        };
      },
      _applyEnabled : function(value, old){

        qx.ui.core.Widget.prototype._applyEnabled.call(this, value, old);
        if(this.getSource()){

          this._styleSource();
        };
      },
      _applySource : function(value){

        this._styleSource();
      },
      _applyScale : function(value){

        this._styleSource();
      },
      __kw : function(mode){

        this.__dn = mode;
      },
      __kx : function(){

        if(this.__dn == null){

          var source = this.getSource();
          var isPng = false;
          if(source != null){

            isPng = qx.lang.String.endsWith(source, d);
          };
          if(this.getScale() && isPng && qx.core.Environment.get(v)){

            this.__dn = R;
          } else if(this.getScale()){

            this.__dn = F;
          } else {

            this.__dn = j;
          };
        };
        return this.__dn;
      },
      __ky : function(mode){

        var scale;
        var tagName;
        if(mode == R){

          scale = true;
          tagName = i;
        } else if(mode == j){

          scale = false;
          tagName = i;
        } else {

          scale = true;
          tagName = N;
        };
        var element = new qx.html.Image(tagName);
        element.setAttribute(D, this.toHashCode());
        element.setScale(scale);
        element.setStyles({
          "overflowX" : Q,
          "overflowY" : Q,
          "boxSizing" : z
        });
        if(qx.core.Environment.get(v)){

          var wrapper = this.__kv = new qx.html.Element(i);
          wrapper.setAttribute(D, this.toHashCode());
          wrapper.setStyle(M, T);
          wrapper.add(element);
          return wrapper;
        };
        return element;
      },
      __kz : function(){

        if(this.$$disposed){

          return null;
        };
        var mode = this.__kx();
        if(this.__kr[mode] == null){

          this.__kr[mode] = this.__ky(mode);
        };
        var element = this.__kr[mode];
        if(!this.__ku){

          this.__ku = element;
        };
        return element;
      },
      _styleSource : function(){

        var source = qx.util.AliasManager.getInstance().resolve(this.getSource());
        var element = this.getContentElement();
        if(this.__kv){

          element = element.getChild(0);
        };
        if(!source){

          element.resetSource();
          return;
        };
        this.__kA(source);
        if((qx.core.Environment.get(J) == I) && (parseInt(qx.core.Environment.get(f), 10) < 9 || qx.core.Environment.get(x) < 9)){

          var repeat = this.getScale() ? g : O;
          element.tagNameHint = qx.bom.element.Decoration.getTagName(repeat, source);
        };
        var contentEl = this.__ku;
        if(this.__kv){

          contentEl = contentEl.getChild(0);
        };
        if(qx.util.ResourceManager.getInstance().has(source)){

          this.__kC(contentEl, source);
        } else if(qx.io.ImageLoader.isLoaded(source)){

          this.__kD(contentEl, source);
        } else {

          this.__kE(contentEl, source);
        };
      },
      __kA : qx.core.Environment.select(J, {
        "mshtml" : function(source){

          var alphaImageLoader = qx.core.Environment.get(v);
          var isPng = qx.lang.String.endsWith(source, d);
          if(alphaImageLoader && isPng){

            if(this.getScale() && this.__kx() != R){

              this.__kw(R);
            } else if(!this.getScale() && this.__kx() != j){

              this.__kw(j);
            };
          } else {

            if(this.getScale() && this.__kx() != F){

              this.__kw(F);
            } else if(!this.getScale() && this.__kx() != j){

              this.__kw(j);
            };
          };
          this.__kB(this.__kz());
        },
        "default" : function(source){

          if(this.getScale() && this.__kx() != F){

            this.__kw(F);
          } else if(!this.getScale() && this.__kx(j)){

            this.__kw(j);
          };
          this.__kB(this.__kz());
        }
      }),
      __kB : function(elementToAdd){

        var currentContentElement = this.__ku;
        if(currentContentElement != elementToAdd){

          if(currentContentElement != null){

            var pixel = c;
            var styles = {
            };
            var bounds = this.getBounds();
            if(bounds != null){

              styles.width = bounds.width + pixel;
              styles.height = bounds.height + pixel;
            };
            var insets = this.getInsets();
            styles.left = parseInt(currentContentElement.getStyle(B) || insets.left) + pixel;
            styles.top = parseInt(currentContentElement.getStyle(E) || insets.top) + pixel;
            styles.zIndex = 10;
            var newEl = this.__kv ? elementToAdd.getChild(0) : elementToAdd;
            newEl.setStyles(styles, true);
            newEl.setSelectable(this.getSelectable());
            if(!currentContentElement.isVisible()){

              elementToAdd.hide();
            };
            if(!currentContentElement.isIncluded()){

              elementToAdd.exclude();
            };
            var container = currentContentElement.getParent();
            if(container){

              var index = container.getChildren().indexOf(currentContentElement);
              container.removeAt(index);
              container.addAt(elementToAdd, index);
            };
            var hint = newEl.getNodeName();
            newEl.setSource(null);
            var currentEl = this.__kv ? this.__ku.getChild(0) : this.__ku;
            newEl.tagNameHint = hint;
            newEl.setAttribute(s, currentEl.getAttribute(s));
            qx.html.Element.flush();
            var currentDomEl = currentEl.getDomElement();
            var newDomEl = elementToAdd.getDomElement();
            var listeners = currentContentElement.getListeners() || [];
            listeners.forEach(function(listenerData){

              elementToAdd.addListener(listenerData.type, listenerData.handler, listenerData.self, listenerData.capture);
            });
            if(currentDomEl && newDomEl){

              var currentHash = currentDomEl.$$hash;
              currentDomEl.$$hash = newDomEl.$$hash;
              newDomEl.$$hash = currentHash;
            };
            this.__ku = elementToAdd;
          };
        };
      },
      __kC : function(el, source){

        var ResourceManager = qx.util.ResourceManager.getInstance();
        if(!this.getEnabled()){

          var disabled = source.replace(/\.([a-z]+)$/, r);
          if(ResourceManager.has(disabled)){

            source = disabled;
            this.addState(o);
          } else {

            this.removeState(o);
          };
        };
        if(el.getSource() === source){

          return;
        };
        this.__kF(el, source);
        this.__kG(ResourceManager.getImageWidth(source), ResourceManager.getImageHeight(source));
      },
      __kD : function(el, source){

        var ImageLoader = qx.io.ImageLoader;
        this.__kF(el, source);
        var width = ImageLoader.getWidth(source);
        var height = ImageLoader.getHeight(source);
        this.__kG(width, height);
      },
      __kE : function(el, source){

        var ImageLoader = qx.io.ImageLoader;
        {

          var sourceLC,startsWith,self;
        };
        if(!ImageLoader.isFailed(source)){

          ImageLoader.load(source, this.__eN, this);
        } else {

          if(el != null){

            el.resetSource();
          };
        };
      },
      __kF : function(el, source){

        if(el.getNodeName() == i){

          var dec = qx.theme.manager.Decoration.getInstance().resolve(this.getDecorator());
          if(dec){

            var hasGradient = (dec.getStartColor() && dec.getEndColor());
            var hasBackground = dec.getBackgroundImage();
            if(hasGradient || hasBackground){

              var repeat = this.getScale() ? g : O;
              var attr = qx.bom.element.Decoration.getAttributes(source, repeat);
              var decStyle = dec.getStyles(true);
              var combinedStyles = {
                "backgroundImage" : attr.style.backgroundImage,
                "backgroundPosition" : (attr.style.backgroundPosition || m),
                "backgroundRepeat" : (attr.style.backgroundRepeat || O)
              };
              if(hasBackground){

                combinedStyles[y] += S + decStyle[P] || m;
                combinedStyles[q] += G + dec.getBackgroundRepeat();
              };
              if(hasGradient){

                combinedStyles[y] += K;
                combinedStyles[q] += n;
              };
              combinedStyles[p] += S + decStyle[e];
              el.setStyles(combinedStyles);
              return;
            };
          } else {

            el.setSource(null);
          };
        };
        el.setSource(source);
      },
      __eN : function(source, imageInfo){

        if(this.$$disposed === true){

          return;
        };
        if(source !== qx.util.AliasManager.getInstance().resolve(this.getSource())){

          return;
        };
        if(imageInfo.failed){

          this.warn(a + source);
          this.fireEvent(u);
        } else if(imageInfo.aborted){

          return;
        } else {

          this.fireEvent(l);
        };
        this._styleSource();
      },
      __kG : function(width, height){

        if(width !== this.__ks || height !== this.__kt){

          this.__ks = width;
          this.__kt = height;
          qx.ui.core.queue.Layout.add(this);
        };
      }
    },
    destruct : function(){

      delete this.__ku;
      this._disposeMap(A);
    }
  });
})();
(function(){

  var a = "source",b = "engine.name",c = "",d = "mshtml",e = "px",f = "px ",g = "no-repeat",h = "backgroundImage",i = "scale",j = "webkit",k = "div",l = "qx.html.Image",m = "qx/static/blank.gif",n = "backgroundPosition";
  qx.Class.define(l, {
    extend : qx.html.Element,
    members : {
      __kH : null,
      __kI : null,
      tagNameHint : null,
      setPadding : function(paddingLeft, paddingTop){

        this.__kI = paddingLeft;
        this.__kH = paddingTop;
        if(this.getNodeName() == k){

          this.setStyle(n, paddingLeft + f + paddingTop + e);
        };
      },
      _applyProperty : function(name, value){

        qx.html.Element.prototype._applyProperty.call(this, name, value);
        if(name === a){

          var elem = this.getDomElement();
          var styles = this.getAllStyles();
          if(this.getNodeName() == k && this.getStyle(h)){

            styles.backgroundRepeat = null;
          };
          var source = this._getProperty(a);
          var scale = this._getProperty(i);
          var repeat = scale ? i : g;
          if(source != null){

            source = source || null;
            styles.paddingTop = this.__kH;
            styles.paddingLeft = this.__kI;
            qx.bom.element.Decoration.update(elem, source, repeat, styles);
          };
        };
      },
      _removeProperty : function(key, direct){

        if(key == a){

          this._setProperty(key, c, direct);
        } else {

          this._setProperty(key, null, direct);
        };
      },
      _createDomElement : function(){

        var scale = this._getProperty(i);
        var repeat = scale ? i : g;
        if((qx.core.Environment.get(b) == d)){

          var source = this._getProperty(a);
          if(this.tagNameHint != null){

            this.setNodeName(this.tagNameHint);
          } else {

            this.setNodeName(qx.bom.element.Decoration.getTagName(repeat, source));
          };
        } else {

          this.setNodeName(qx.bom.element.Decoration.getTagName(repeat));
        };
        return qx.html.Element.prototype._createDomElement.call(this);
      },
      _copyData : function(fromMarkup){

        return qx.html.Element.prototype._copyData.call(this, true);
      },
      setSource : function(value){

        this._setProperty(a, value);
        return this;
      },
      getSource : function(){

        return this._getProperty(a);
      },
      resetSource : function(){

        if((qx.core.Environment.get(b) == j)){

          this._setProperty(a, m);
        } else {

          this._removeProperty(a, true);
        };
        return this;
      },
      setScale : function(value){

        this._setProperty(i, value);
        return this;
      },
      getScale : function(){

        return this._getProperty(i);
      }
    }
  });
})();
(function(){

  var a = "qx/icon",b = "repeat",c = "px",d = ".png",f = "crop",g = "px ",h = "background-image",i = "scale",j = "no-repeat",k = "div",l = "Potential clipped image candidate: ",m = "progid:DXImageTransform.Microsoft.AlphaImageLoader(src='",n = '<div style="',o = "scale-x",p = "css.alphaimageloaderneeded",q = "repeat-y",r = '<img src="',s = "qx.bom.element.Decoration",t = "Image modification not possible because elements could not be replaced at runtime anymore!",u = "', sizingMethod='",v = "",w = '"/>',x = "png",y = "img",z = "')",A = '"></div>',B = "mshtml",C = "engine.name",D = '" style="',E = "none",F = "b64",G = "webkit",H = " ",I = "repeat-x",J = "background-repeat",K = "DXImageTransform.Microsoft.AlphaImageLoader",L = "qx/static/blank.gif",M = "scale-y",N = "absolute";
  qx.Class.define(s, {
    statics : {
      DEBUG : false,
      __kJ : {
      },
      __kK : qx.core.Environment.select(C, {
        "mshtml" : {
          "scale-x" : true,
          "scale-y" : true,
          "scale" : true,
          "no-repeat" : true
        },
        "default" : null
      }),
      __kL : {
        "scale-x" : y,
        "scale-y" : y,
        "scale" : y,
        "repeat" : k,
        "no-repeat" : k,
        "repeat-x" : k,
        "repeat-y" : k
      },
      update : function(element, source, repeat, style){

        var tag = this.getTagName(repeat, source);
        if(tag != element.tagName.toLowerCase()){

          throw new Error(t);
        };
        var ret = this.getAttributes(source, repeat, style);
        if(tag === y){

          element.src = ret.src || qx.util.ResourceManager.getInstance().toUri(L);
        };
        if(element.style.backgroundPosition != v && ret.style.backgroundPosition === undefined){

          ret.style.backgroundPosition = null;
        };
        if(element.style.clip != v && ret.style.clip === undefined){

          ret.style.clip = null;
        };
        qx.bom.element.Style.setStyles(element, ret.style);
        if(qx.core.Environment.get(p)){

          try{

            element.filters[K].apply();
          } catch(e) {
          };
        };
      },
      create : function(source, repeat, style){

        var tag = this.getTagName(repeat, source);
        var ret = this.getAttributes(source, repeat, style);
        var css = qx.bom.element.Style.compile(ret.style);
        if(tag === y){

          return r + ret.src + D + css + w;
        } else {

          return n + css + A;
        };
      },
      getTagName : function(repeat, source){

        if(source && qx.core.Environment.get(p) && this.__kK[repeat] && qx.lang.String.endsWith(source, d)){

          return k;
        };
        return this.__kL[repeat];
      },
      getAttributes : function(source, repeat, style){

        if(!style){

          style = {
          };
        };
        if(!style.position){

          style.position = N;
        };
        if((qx.core.Environment.get(C) == B)){

          style.fontSize = 0;
          style.lineHeight = 0;
        } else if((qx.core.Environment.get(C) == G)){

          style.WebkitUserDrag = E;
        };
        var format = qx.util.ResourceManager.getInstance().getImageFormat(source) || qx.io.ImageLoader.getFormat(source);
        {
        };
        var result;
        if(qx.core.Environment.get(p) && this.__kK[repeat] && format === x){

          var dimension = this.__kN(source);
          this.__kM(style, dimension.width, dimension.height);
          result = this.processAlphaFix(style, repeat, source);
        } else {

          delete style.clip;
          if(repeat === i){

            result = this.__kO(style, repeat, source);
          } else if(repeat === o || repeat === M){

            result = this.__kP(style, repeat, source);
          } else {

            result = this.__kS(style, repeat, source);
          };
        };
        return result;
      },
      __kM : function(style, width, height){

        if(style.width == null && width != null){

          style.width = width + c;
        };
        if(style.height == null && height != null){

          style.height = height + c;
        };
      },
      __kN : function(source){

        var width = qx.util.ResourceManager.getInstance().getImageWidth(source) || qx.io.ImageLoader.getWidth(source);
        var height = qx.util.ResourceManager.getInstance().getImageHeight(source) || qx.io.ImageLoader.getHeight(source);
        return {
          width : width,
          height : height
        };
      },
      processAlphaFix : function(style, repeat, source){

        if(repeat == b || repeat == I || repeat == q){

          return style;
        };
        var sizingMethod = repeat == j ? f : i;
        var filter = m + qx.util.ResourceManager.getInstance().toUri(source) + u + sizingMethod + z;
        style.filter = filter;
        style.backgroundImage = style.backgroundRepeat = v;
        delete style[h];
        delete style[J];
        return {
          style : style
        };
      },
      __kO : function(style, repeat, source){

        var uri = qx.util.ResourceManager.getInstance().toUri(source);
        var dimension = this.__kN(source);
        this.__kM(style, dimension.width, dimension.height);
        return {
          src : uri,
          style : style
        };
      },
      __kP : function(style, repeat, sourceid){

        var ResourceManager = qx.util.ResourceManager.getInstance();
        var clipped = ResourceManager.getCombinedFormat(sourceid);
        var dimension = this.__kN(sourceid);
        var uri;
        if(clipped){

          var data = ResourceManager.getData(sourceid);
          var combinedid = data[4];
          if(clipped == F){

            uri = ResourceManager.toDataUri(sourceid);
          } else {

            uri = ResourceManager.toUri(combinedid);
          };
          if(repeat === o){

            style = this.__kQ(style, data, dimension.height);
          } else {

            style = this.__kR(style, data, dimension.width);
          };
          return {
            src : uri,
            style : style
          };
        } else {

          {
          };
          if(repeat == o){

            style.height = dimension.height == null ? null : dimension.height + c;
          } else if(repeat == M){

            style.width = dimension.width == null ? null : dimension.width + c;
          };
          uri = ResourceManager.toUri(sourceid);
          return {
            src : uri,
            style : style
          };
        };
      },
      __kQ : function(style, data, height){

        var imageHeight = qx.util.ResourceManager.getInstance().getImageHeight(data[4]);
        style.clip = {
          top : -data[6],
          height : height
        };
        style.height = imageHeight + c;
        if(style.top != null){

          style.top = (parseInt(style.top, 10) + data[6]) + c;
        } else if(style.bottom != null){

          style.bottom = (parseInt(style.bottom, 10) + height - imageHeight - data[6]) + c;
        };
        return style;
      },
      __kR : function(style, data, width){

        var imageWidth = qx.util.ResourceManager.getInstance().getImageWidth(data[4]);
        style.clip = {
          left : -data[5],
          width : width
        };
        style.width = imageWidth + c;
        if(style.left != null){

          style.left = (parseInt(style.left, 10) + data[5]) + c;
        } else if(style.right != null){

          style.right = (parseInt(style.right, 10) + width - imageWidth - data[5]) + c;
        };
        return style;
      },
      __kS : function(style, repeat, sourceid){

        var ResourceManager = qx.util.ResourceManager.getInstance();
        var clipped = ResourceManager.getCombinedFormat(sourceid);
        var dimension = this.__kN(sourceid);
        if(clipped && repeat !== b){

          var data = ResourceManager.getData(sourceid);
          var combinedid = data[4];
          if(clipped == F){

            var uri = ResourceManager.toDataUri(sourceid);
            var offx = 0;
            var offy = 0;
          } else {

            var uri = ResourceManager.toUri(combinedid);
            var offx = data[5];
            var offy = data[6];
            if(style.paddingTop || style.paddingLeft || style.paddingRight || style.paddingBottom){

              var top = style.paddingTop || 0;
              var left = style.paddingLeft || 0;
              offx += style.paddingLeft || 0;
              offy += style.paddingTop || 0;
              style.clip = {
                left : left,
                top : top,
                width : dimension.width,
                height : dimension.height
              };
            };
          };
          var bg = qx.bom.element.Background.getStyles(uri, repeat, offx, offy);
          for(var key in bg){

            style[key] = bg[key];
          };
          if(dimension.width != null && style.width == null && (repeat == q || repeat === j)){

            style.width = dimension.width + c;
          };
          if(dimension.height != null && style.height == null && (repeat == I || repeat === j)){

            style.height = dimension.height + c;
          };
          return {
            style : style
          };
        } else {

          var top = style.paddingTop || 0;
          var left = style.paddingLeft || 0;
          style.backgroundPosition = left + g + top + c;
          {
          };
          this.__kM(style, dimension.width, dimension.height);
          this.__kT(style, sourceid, repeat);
          return {
            style : style
          };
        };
      },
      __kT : function(style, source, repeat){

        var top = null;
        var left = null;
        if(style.backgroundPosition){

          var backgroundPosition = style.backgroundPosition.split(H);
          left = parseInt(backgroundPosition[0], 10);
          if(isNaN(left)){

            left = backgroundPosition[0];
          };
          top = parseInt(backgroundPosition[1], 10);
          if(isNaN(top)){

            top = backgroundPosition[1];
          };
        };
        var bg = qx.bom.element.Background.getStyles(source, repeat, left, top);
        for(var key in bg){

          style[key] = bg[key];
        };
        if(style.filter){

          style.filter = v;
        };
      },
      __kU : function(source){

        if(this.DEBUG && qx.util.ResourceManager.getInstance().has(source) && source.indexOf(a) == -1){

          if(!this.__kJ[source]){

            qx.log.Logger.debug(l + source);
            this.__kJ[source] = true;
          };
        };
      }
    }
  });
})();
(function(){

  var a = "')",b = "gecko",c = "background-image:url(",d = "0",e = ");",f = "",g = "px",h = "number",i = ")",j = "background-repeat:",k = "engine.version",l = "data:",m = " ",n = "qx.bom.element.Background",o = ";",p = "url(",q = "background-position:",r = "base64",s = "url('",t = "engine.name",u = "'";
  qx.Class.define(n, {
    statics : {
      __kV : [c, null, e, q, null, o, j, null, o],
      __kW : {
        backgroundImage : null,
        backgroundPosition : null,
        backgroundRepeat : null
      },
      __kX : function(left, top){

        var engine = qx.core.Environment.get(t);
        var version = qx.core.Environment.get(k);
        if(engine == b && version < 1.9 && left == top && typeof left == h){

          top += 0.01;
        };
        if(left){

          var leftCss = (typeof left == h) ? left + g : left;
        } else {

          leftCss = d;
        };
        if(top){

          var topCss = (typeof top == h) ? top + g : top;
        } else {

          topCss = d;
        };
        return leftCss + m + topCss;
      },
      __kY : function(url){

        var String = qx.lang.String;
        var firstPartOfUrl = url.substr(0, 50);
        return String.startsWith(firstPartOfUrl, l) && String.contains(firstPartOfUrl, r);
      },
      compile : function(source, repeat, left, top){

        var position = this.__kX(left, top);
        var backgroundImageUrl = qx.util.ResourceManager.getInstance().toUri(source);
        if(this.__kY(backgroundImageUrl)){

          backgroundImageUrl = u + backgroundImageUrl + u;
        };
        var tmpl = this.__kV;
        tmpl[1] = backgroundImageUrl;
        tmpl[4] = position;
        tmpl[7] = repeat;
        return tmpl.join(f);
      },
      getStyles : function(source, repeat, left, top){

        if(!source){

          return this.__kW;
        };
        var position = this.__kX(left, top);
        var backgroundImageUrl = qx.util.ResourceManager.getInstance().toUri(source);
        var backgroundImageCssString;
        if(this.__kY(backgroundImageUrl)){

          backgroundImageCssString = s + backgroundImageUrl + a;
        } else {

          backgroundImageCssString = p + backgroundImageUrl + i;
        };
        var map = {
          backgroundPosition : position,
          backgroundImage : backgroundImageCssString
        };
        if(repeat != null){

          map.backgroundRepeat = repeat;
        };
        return map;
      },
      set : function(element, source, repeat, left, top){

        var styles = this.getStyles(source, repeat, left, top);
        for(var prop in styles){

          element.style[prop] = styles[prop];
        };
      }
    }
  });
})();
(function(){

  var a = "dragdrop-cursor",b = "_applyAction",c = "alias",d = "qx.ui.core.DragDropCursor",e = "move",f = "singleton",g = "copy";
  qx.Class.define(d, {
    extend : qx.ui.basic.Image,
    include : qx.ui.core.MPlacement,
    type : f,
    construct : function(){

      qx.ui.basic.Image.call(this);
      this.setZIndex(1e8);
      this.setDomMove(true);
      var root = this.getApplicationRoot();
      root.add(this, {
        left : -1000,
        top : -1000
      });
    },
    properties : {
      appearance : {
        refine : true,
        init : a
      },
      action : {
        check : [c, g, e],
        apply : b,
        nullable : true
      }
    },
    members : {
      _applyAction : function(value, old){

        if(old){

          this.removeState(old);
        };
        if(value){

          this.addState(value);
        };
      }
    }
  });
})();
(function(){

  var a = "Boolean",b = "",c = 'boolean',d = "change",e = "event.mspointer",f = "input",g = "qx.ui.mobile.form.MValue",h = "value",i = "changeValue",j = 'number',k = "qx.event.type.Data";
  qx.Mixin.define(g, {
    construct : function(value){

      if(value){

        this.setValue(value);
      };
      qx.event.Registration.addListener(this.getContentElement(), d, this._onChangeContent, this);
      qx.event.Registration.addListener(this.getContentElement(), f, this._onInput, this);
    },
    events : {
      "input" : k,
      "changeValue" : k
    },
    properties : {
      liveUpdate : {
        check : a,
        init : false
      }
    },
    members : {
      __gx : null,
      _convertValue : function(value){

        if(typeof value === c){

          return value;
        } else if(typeof value === j){

          return value;
        } else {

          return value || b;
        };
      },
      setValue : function(value){

        value = this._convertValue(value);
        if(this.__gx != value){

          if(this._setValue){

            this._setValue(value);
          } else {

            this._setAttribute(h, value);
          };
          this.__la(value);
        };
      },
      getValue : function(){

        return this._convertValue(this._getValue ? this._getValue() : this._getAttribute(h));
      },
      resetValue : function(){

        this.setValue(null);
      },
      _onChangeContent : function(evt){

        this.__la(this._convertValue(evt.getData()));
      },
      _onInput : function(evt){

        this.fireDataEvent(f, evt.getData(), true);
        if(this.getLiveUpdate()){

          var caretPosition = this._getCaretPosition();
          this.setValue(evt.getData());
          if(qx.core.Environment.get(e)){

            setTimeout(function(){

              this._setCaretPosition(caretPosition);
            }.bind(this), 0);
          } else {

            this._setCaretPosition(caretPosition);
          };
        };
      },
      _getCaretPosition : function(){

        var val = this.getContentElement().value;
        var position = val.slice(0, this.getContentElement().selectionStart).length;
        return position;
      },
      _setCaretPosition : function(position){

        var element = this.getContentElement();
        if(element.setSelectionRange){

          element.focus();
          element.setSelectionRange(position, position);
        };
      },
      __la : function(value){

        if(this.__gx != value){

          this.__gx = value;
          this.fireDataEvent(i, value);
        };
      }
    }
  });
})();
(function(){

  var a = "text-area",b = "textarea",c = "qx.ui.mobile.form.TextArea";
  qx.Class.define(c, {
    extend : qx.ui.mobile.core.Widget,
    include : [qx.ui.mobile.form.MValue, qx.ui.mobile.form.MText, qx.ui.form.MForm, qx.ui.form.MModelProperty, qx.ui.mobile.form.MState],
    implement : [qx.ui.form.IForm, qx.ui.form.IModel],
    construct : function(value){

      qx.ui.mobile.core.Widget.call(this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : a
      }
    },
    members : {
      _getTagName : function(){

        return b;
      }
    }
  });
})();
(function(){

  var a = "qx.util.Serializer",b = '\\\\',c = '\\f',d = '"',e = "null",f = '\\"',g = "}",h = "get",j = "{",k = '\\r',l = "",m = '\\t',n = "]",o = "Class",p = "Interface",q = "[",r = "Mixin",s = '":',t = "&",u = '\\b',v = "=",w = '\\n',x = ",";
  qx.Class.define(a, {
    statics : {
      toUriParameter : function(object, qxSerializer, dateFormat){

        var result = l;
        var properties = qx.util.PropertyUtil.getAllProperties(object.constructor);
        for(var name in properties){

          if(properties[name].group != undefined){

            continue;
          };
          var value = object[h + qx.lang.String.firstUp(name)]();
          if(qx.lang.Type.isArray(value)){

            var isdataArray = qx.data && qx.data.IListData && qx.Class.hasInterface(value && value.constructor, qx.data.IListData);
            for(var i = 0;i < value.length;i++){

              var valueAtI = isdataArray ? value.getItem(i) : value[i];
              result += this.__lb(name, valueAtI, qxSerializer);
            };
          } else if(qx.lang.Type.isDate(value) && dateFormat != null){

            result += this.__lb(name, dateFormat.format(value), qxSerializer);
          } else {

            result += this.__lb(name, value, qxSerializer);
          };
        };
        return result.substring(0, result.length - 1);
      },
      __lb : function(name, value, qxSerializer){

        if(value && value.$$type == o){

          value = value.classname;
        };
        if(value && (value.$$type == p || value.$$type == r)){

          value = value.name;
        };
        if(value instanceof qx.core.Object && qxSerializer != null){

          var encValue = encodeURIComponent(qxSerializer(value));
          if(encValue === undefined){

            var encValue = encodeURIComponent(value);
          };
        } else {

          var encValue = encodeURIComponent(value);
        };
        return encodeURIComponent(name) + v + encValue + t;
      },
      toNativeObject : function(object, qxSerializer, dateFormat){

        var result;
        if(object == null){

          return null;
        };
        if(qx.data && qx.data.IListData && qx.Class.hasInterface(object.constructor, qx.data.IListData)){

          result = [];
          for(var i = 0;i < object.getLength();i++){

            result.push(qx.util.Serializer.toNativeObject(object.getItem(i), qxSerializer, dateFormat));
          };
          return result;
        };
        if(qx.lang.Type.isArray(object)){

          result = [];
          for(var i = 0;i < object.length;i++){

            result.push(qx.util.Serializer.toNativeObject(object[i], qxSerializer, dateFormat));
          };
          return result;
        };
        if(object.$$type == o){

          return object.classname;
        };
        if(object.$$type == p || object.$$type == r){

          return object.name;
        };
        if(object instanceof qx.core.Object){

          if(qxSerializer != null){

            var returnValue = qxSerializer(object);
            if(returnValue != undefined){

              return returnValue;
            };
          };
          result = {
          };
          var properties = qx.util.PropertyUtil.getAllProperties(object.constructor);
          for(var name in properties){

            if(properties[name].group != undefined){

              continue;
            };
            var value = object[h + qx.lang.String.firstUp(name)]();
            result[name] = qx.util.Serializer.toNativeObject(value, qxSerializer, dateFormat);
          };
          return result;
        };
        if(qx.lang.Type.isDate(object) && dateFormat != null){

          return dateFormat.format(object);
        };
        if(qx.locale && qx.locale.LocalizedString && object instanceof qx.locale.LocalizedString){

          return object.toString();
        };
        if(qx.lang.Type.isObject(object)){

          result = {
          };
          for(var key in object){

            result[key] = qx.util.Serializer.toNativeObject(object[key], qxSerializer, dateFormat);
          };
          return result;
        };
        return object;
      },
      toJson : function(object, qxSerializer, dateFormat){

        var result = l;
        if(object == null){

          return e;
        };
        if(qx.data && qx.data.IListData && qx.Class.hasInterface(object.constructor, qx.data.IListData)){

          result += q;
          for(var i = 0;i < object.getLength();i++){

            result += qx.util.Serializer.toJson(object.getItem(i), qxSerializer, dateFormat) + x;
          };
          if(result != q){

            result = result.substring(0, result.length - 1);
          };
          return result + n;
        };
        if(qx.lang.Type.isArray(object)){

          result += q;
          for(var i = 0;i < object.length;i++){

            result += qx.util.Serializer.toJson(object[i], qxSerializer, dateFormat) + x;
          };
          if(result != q){

            result = result.substring(0, result.length - 1);
          };
          return result + n;
        };
        if(object.$$type == o){

          return d + object.classname + d;
        };
        if(object.$$type == p || object.$$type == r){

          return d + object.name + d;
        };
        if(object instanceof qx.core.Object){

          if(qxSerializer != null){

            var returnValue = qxSerializer(object);
            if(returnValue != undefined){

              return d + returnValue + d;
            };
          };
          result += j;
          var properties = qx.util.PropertyUtil.getAllProperties(object.constructor);
          for(var name in properties){

            if(properties[name].group != undefined){

              continue;
            };
            var value = object[h + qx.lang.String.firstUp(name)]();
            result += d + name + s + qx.util.Serializer.toJson(value, qxSerializer, dateFormat) + x;
          };
          if(result != j){

            result = result.substring(0, result.length - 1);
          };
          return result + g;
        };
        if(qx.locale && qx.locale.LocalizedString && object instanceof qx.locale.LocalizedString){

          object = object.toString();
        };
        if(qx.lang.Type.isDate(object) && dateFormat != null){

          return d + dateFormat.format(object) + d;
        };
        if(qx.lang.Type.isObject(object)){

          result += j;
          for(var key in object){

            result += d + key + s + qx.util.Serializer.toJson(object[key], qxSerializer, dateFormat) + x;
          };
          if(result != j){

            result = result.substring(0, result.length - 1);
          };
          return result + g;
        };
        if(qx.lang.Type.isString(object)){

          object = object.replace(/([\\])/g, b);
          object = object.replace(/(["])/g, f);
          object = object.replace(/([\r])/g, k);
          object = object.replace(/([\f])/g, c);
          object = object.replace(/([\n])/g, w);
          object = object.replace(/([\t])/g, m);
          object = object.replace(/([\b])/g, u);
          return d + object + d;
        };
        if(qx.lang.Type.isDate(object) || qx.lang.Type.isRegExp(object)){

          return d + object + d;
        };
        return object + l;
      }
    }
  });
})();
(function(){

  var a = "qx.util.format.IFormat";
  qx.Interface.define(a, {
    members : {
      format : function(obj){
      },
      parse : function(str){
      }
    }
  });
})();
(function(){

  var a = "-",b = "QQQ",c = 'W',d = "qqqq",e = "h",f = "KK",g = 'L',h = " is not supported",m = "Z",n = "Date string '",o = "GGGGG",p = 'abbreviated',q = "Y+",r = ". Wildcard ",s = "eeeee",t = "(\\d\\d?)",u = "LL",v = "abbreviated",w = "EEEE",x = "^",y = ":",z = 'y',A = "quoted_literal",B = "weekOfYear",C = "locale",D = "SSS",E = 'a',F = "day",G = "(-*",H = "qqq",I = '3rd quarter',J = "K",K = 'q',L = "pm",M = "Error parsing date '",N = 'Anno Domini',O = 'G',P = "a",Q = '2nd quarter',R = "yyyy-MM-dd'T'HH:mm:ss",S = "GMT",T = "HH:mm:ss",U = "dd",V = "+",W = "qx.util.format.DateFormat",X = 'weekDay',Y = "am",ba = "eee",bb = "quarter",bc = "ee",bd = "q",be = "QQQQ",bf = "HHmmss",bg = "ww",bh = "H",bi = 'Y+',bj = "wide",bk = 'wide',bl = "(\\d\\d\\d?)",bm = "(GMT[\\+\\-]\\d\\d:\\d\\d)",bn = "_applyLocale",bo = "Q",bp = "c",bq = "(",br = "weekOfMonth",bs = "ccc",bt = "long",bu = "Malformed date format: ",bv = "EEEEE",bw = 'Y',bx = "EE",by = "GGG",bz = '-',bA = "w",bB = "mm",bC = 'h',bD = "S",bE = 's',bF = 'A',bG = "yyyy-MM-dd",bH = "G",bI = 'z',bJ = "GG",bK = ")",bL = "kk",bM = "ss",bN = "([\\+\\-]\\d\\d\\d\\d)",bO = "default",bP = 'BC',bQ = 'H',bR = 'S',bS = '1st quarter',bT = "stand-alone",bU = "0",bV = "MMMM",bW = 'c',bX = "d",bY = 'Q',ca = 'Q1',cb = "eeee",cc = "QQ",cd = "LLLLL",ce = "HH",cf = 'k',cg = "m",ch = "DDD",ci = "D",cj = 'D',ck = "LLLL",cl = "(\\d\\d*?)",cm = 'e',cn = "': the value for day or month is too large",co = "literal",cp = 'K',cq = "L",cr = "hh",cs = "ms",ct = "(\\d\\d?\\d?)",cu = "cccc",cv = "e",cw = 'y+',cx = "|",cy = "W",cz = "dayOfYear",cA = "GGGG",cB = 'Q2',cC = 'B',cD = "MM",cE = 'Q4',cF = "y+",cG = 'narrow',cH = "ccccc",cI = "E",cJ = "wildcard",cK = '',cL = "' does not match the date format: ",cM = "short",cN = 'd',cO = "unkown",cP = "narrow",cQ = "\\d?",cR = "'",cS = "k",cT = 'Before Christ',cU = 'Z',cV = 'm',cW = " ",cX = "yyyy-MM-dd'T'HH:mm:ss'Z'",cY = "String",da = 'Q3',db = "z",dc = "DD",dd = "min",de = "",df = 'M',dg = "sec",dh = 'isoUtcDateTime',di = '4th quarter',dj = "(\\d?)",dk = "SS",dl = "MMM",dm = "s",dn = "M",dp = 'w',dq = "EEE",dr = "$",ds = "?",dt = 'AD',du = "\\d",dv = "+?",dw = "hour",dx = "MMMMM",dy = 'E',dz = 'month',dA = "LLL",dB = "qq",dC = "format";
  qx.Class.define(W, {
    extend : qx.core.Object,
    implement : qx.util.format.IFormat,
    construct : function(format, locale){

      qx.core.Object.call(this);
      if(!locale){

        this.__fe = qx.locale.Manager.getInstance().getLocale();
        this.__lc = qx.locale.Manager.getInstance().bind(C, this, C);
      } else {

        this.__fe = locale;
        this.setLocale(locale);
      };
      this.__ld = this.__fe;
      if(format != null){

        this.__le = format.toString();
        if(this.__le in qx.util.format.DateFormat.ISO_MASKS){

          if(this.__le === dh){

            this.__lf = true;
          };
          this.__le = qx.util.format.DateFormat.ISO_MASKS[this.__le];
        };
      } else {

        this.__le = qx.locale.Date.getDateFormat(bt, this.__fe) + cW + qx.locale.Date.getDateTimeFormat(bf, T, this.__fe);
      };
    },
    properties : {
      locale : {
        apply : bn,
        nullable : true,
        check : cY
      }
    },
    statics : {
      getDateInstance : function(){

        var DateFormat = qx.util.format.DateFormat;
        var format = qx.locale.Date.getDateFormat(cM) + de;
        if(DateFormat._dateInstance == null || DateFormat._dateInstance.__le != format){

          DateFormat._dateInstance = new DateFormat(format);
        };
        return DateFormat._dateInstance;
      },
      getDateTimeInstance : function(){

        var DateFormat = qx.util.format.DateFormat;
        var format = qx.locale.Date.getDateFormat(bt) + cW + qx.locale.Date.getDateTimeFormat(bf, T);
        if(DateFormat._dateTimeInstance == null || DateFormat._dateTimeInstance.__le != format){

          DateFormat._dateTimeInstance = new DateFormat(format);
        };
        return DateFormat._dateTimeInstance;
      },
      ASSUME_YEAR_2000_THRESHOLD : 30,
      ISO_MASKS : {
        isoDate : bG,
        isoTime : T,
        isoDateTime : R,
        isoUtcDateTime : cX
      },
      AM_MARKER : Y,
      PM_MARKER : L
    },
    members : {
      __lc : null,
      __fe : null,
      __ld : null,
      __le : null,
      __lg : null,
      __lh : null,
      __li : null,
      __lf : null,
      __lj : function(number, minSize){

        var str = de + (number < 0 ? (-1 * number) : number);
        while(str.length < minSize){

          str = bU + str;
        };
        return number < 0 ? a + str : str;
      },
      __lk : function(date){

        var helpDate = new Date(date.getTime());
        var day = helpDate.getDate();
        while(helpDate.getMonth() != 0){

          helpDate.setDate(-1);
          day += helpDate.getDate() + 1;
        };
        return day;
      },
      __ll : function(date){

        return new Date(date.getTime() + (3 - ((date.getDay() + 6) % 7)) * 86400000);
      },
      __lm : function(date){

        var thursdayDate = this.__ll(date);
        var weekYear = thursdayDate.getFullYear();
        var thursdayWeek1 = this.__ll(new Date(weekYear, 0, 4));
        return Math.floor(1.5 + (thursdayDate.getTime() - thursdayWeek1.getTime()) / 86400000 / 7);
      },
      __ln : function(date){

        var thursdayDate = this.__ll(date);
        var thursdayWeek1 = this.__ll(new Date(date.getFullYear(), date.getMonth(), 4));
        return Math.floor(1.5 + (thursdayDate.getTime() - thursdayWeek1.getTime()) / 86400000 / 7);
      },
      __lo : function(date){

        var thursdayDate = this.__ll(date);
        return thursdayDate.getFullYear();
      },
      __lp : function(year){

        var februaryDate = new Date(year, 2, 1);
        februaryDate.setDate(-1);
        return februaryDate.getDate() + 1 === 29;
      },
      __lq : function(dayOfYear, year){

        var month = 0;
        var day = 0;
        if(!year){

          year = 1971;
        };
        var dayCounter = 0;
        for(var i = 1;i <= 12;i++){

          var tempDate = new Date(year, i, 1);
          tempDate.setDate(-1);
          var days = tempDate.getDate() + 1;
          dayCounter += days;
          if(dayCounter < dayOfYear){

            month++;
            day += days;
          } else {

            day = dayOfYear - (dayCounter - days);
            break;
          };
        };
        return {
          month : month,
          day : day
        };
      },
      __lr : function(weekYear, month, dayOfMonth){

        var year;
        switch(month){case 11:
        year = weekYear - 1;
        if(weekYear != this.__lo(new Date(year, month, dayOfMonth))){

          year = weekYear;
        };
        break;case 0:
        year = weekYear + 1;
        if(weekYear != this.__lo(new Date(year, month, dayOfMonth))){

          year = weekYear;
        };
        break;default:
        year = weekYear;};
        return year;
      },
      _applyLocale : function(value, old){

        this.__fe = value === null ? this.setLocale(this.__ld) : value;
      },
      format : function(date){

        if(date == null){

          return null;
        };
        if(isNaN(date.getTime())){

          {
          };
          return null;
        };
        if(this.__lf){

          date = new Date(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate(), date.getUTCHours(), date.getUTCMinutes(), date.getUTCSeconds(), date.getUTCMilliseconds());
        };
        var locale = this.__fe;
        var fullYear = date.getFullYear();
        var month = date.getMonth();
        var dayOfMonth = date.getDate();
        var dayOfWeek = date.getDay();
        var hours = date.getHours();
        var minutes = date.getMinutes();
        var seconds = date.getSeconds();
        var ms = date.getMilliseconds();
        var timezoneOffset = date.getTimezoneOffset();
        var timezoneSign = timezoneOffset > 0 ? 1 : -1;
        var timezoneHours = Math.floor(Math.abs(timezoneOffset) / 60);
        var timezoneMinutes = Math.abs(timezoneOffset) % 60;
        this.__ls();
        var output = de;
        for(var i = 0;i < this.__li.length;i++){

          var currAtom = this.__li[i];
          if(currAtom.type == co){

            output += currAtom.text;
          } else {

            var wildcardChar = currAtom.character;
            var wildcardSize = currAtom.size;
            var replacement = ds;
            switch(wildcardChar){case z:
            if(wildcardSize == 2){

              replacement = this.__lj(fullYear % 100, 2);
            } else {

              var year = Math.abs(fullYear);
              replacement = year + de;
              if(wildcardSize > replacement.length){

                for(var j = replacement.length;j < wildcardSize;j++){

                  replacement = bU + replacement;
                };
              };
              if(fullYear < 0){

                replacement = a + replacement;
              };
            };
            break;case bw:
            replacement = this.__lo(date) + de;
            var year = replacement.replace(bz, cK);
            if(wildcardSize > replacement.length){

              for(var j = year.length;j < wildcardSize;j++){

                year = bU + year;
              };
            };
            replacement = replacement.indexOf(a) != -1 ? a + year : year;
            break;case O:
            if(wildcardSize >= 1 && wildcardSize <= 3){

              replacement = fullYear > 0 ? dt : bP;
            } else if(wildcardSize == 4){

              replacement = fullYear > 0 ? N : cT;
            } else if(wildcardSize == 5){

              replacement = fullYear > 0 ? bF : cC;
            };;
            break;case bY:
            if(wildcardSize == 1 || wildcardSize == 2){

              replacement = this.__lj(parseInt(month / 4) + 1, wildcardSize);
            };
            if(wildcardSize == 3){

              replacement = bY + (parseInt(month / 4) + 1);
            };
            break;case K:
            if(wildcardSize == 1 || wildcardSize == 2){

              replacement = this.__lj(parseInt(month / 4) + 1, wildcardSize);
            };
            if(wildcardSize == 3){

              replacement = bY + (parseInt(month / 4) + 1);
            };
            break;case cj:
            replacement = this.__lj(this.__lk(date), wildcardSize);
            break;case cN:
            replacement = this.__lj(dayOfMonth, wildcardSize);
            break;case dp:
            replacement = this.__lj(this.__lm(date), wildcardSize);
            break;case c:
            replacement = this.__ln(date);
            break;case dy:
            if(wildcardSize >= 1 && wildcardSize <= 3){

              replacement = qx.locale.Date.getDayName(v, dayOfWeek, locale, dC, true);
            } else if(wildcardSize == 4){

              replacement = qx.locale.Date.getDayName(bj, dayOfWeek, locale, dC, true);
            } else if(wildcardSize == 5){

              replacement = qx.locale.Date.getDayName(cP, dayOfWeek, locale, dC, true);
            };;
            break;case cm:
            var startOfWeek = qx.locale.Date.getWeekStart(locale);
            var localeDayOfWeek = 1 + ((dayOfWeek - startOfWeek >= 0) ? (dayOfWeek - startOfWeek) : 7 + (dayOfWeek - startOfWeek));
            if(wildcardSize >= 1 && wildcardSize <= 2){

              replacement = this.__lj(localeDayOfWeek, wildcardSize);
            } else if(wildcardSize == 3){

              replacement = qx.locale.Date.getDayName(v, dayOfWeek, locale, dC, true);
            } else if(wildcardSize == 4){

              replacement = qx.locale.Date.getDayName(bj, dayOfWeek, locale, dC, true);
            } else if(wildcardSize == 5){

              replacement = qx.locale.Date.getDayName(cP, dayOfWeek, locale, dC, true);
            };;;
            break;case bW:
            var startOfWeek = qx.locale.Date.getWeekStart(locale);
            var localeDayOfWeek = 1 + ((dayOfWeek - startOfWeek >= 0) ? (dayOfWeek - startOfWeek) : 7 + (dayOfWeek - startOfWeek));
            if(wildcardSize == 1){

              replacement = cK + localeDayOfWeek;
            } else if(wildcardSize == 3){

              replacement = qx.locale.Date.getDayName(v, dayOfWeek, locale, bT, true);
            } else if(wildcardSize == 4){

              replacement = qx.locale.Date.getDayName(bj, dayOfWeek, locale, bT, true);
            } else if(wildcardSize == 5){

              replacement = qx.locale.Date.getDayName(cP, dayOfWeek, locale, bT, true);
            };;;
            break;case df:
            if(wildcardSize == 1 || wildcardSize == 2){

              replacement = this.__lj(month + 1, wildcardSize);
            } else if(wildcardSize == 3){

              replacement = qx.locale.Date.getMonthName(v, month, locale, dC, true);
            } else if(wildcardSize == 4){

              replacement = qx.locale.Date.getMonthName(bj, month, locale, dC, true);
            } else if(wildcardSize == 5){

              replacement = qx.locale.Date.getMonthName(cP, month, locale, dC, true);
            };;;
            break;case g:
            if(wildcardSize == 1 || wildcardSize == 2){

              replacement = this.__lj(month + 1, wildcardSize);
            } else if(wildcardSize == 3){

              replacement = qx.locale.Date.getMonthName(v, month, locale, bT, true);
            } else if(wildcardSize == 4){

              replacement = qx.locale.Date.getMonthName(bj, month, locale, bT, true);
            } else if(wildcardSize == 5){

              replacement = qx.locale.Date.getMonthName(cP, month, locale, bT, true);
            };;;
            break;case E:
            replacement = (hours < 12) ? qx.locale.Date.getAmMarker(locale) : qx.locale.Date.getPmMarker(locale);
            break;case bQ:
            replacement = this.__lj(hours, wildcardSize);
            break;case cf:
            replacement = this.__lj((hours == 0) ? 24 : hours, wildcardSize);
            break;case cp:
            replacement = this.__lj(hours % 12, wildcardSize);
            break;case bC:
            replacement = this.__lj(((hours % 12) == 0) ? 12 : (hours % 12), wildcardSize);
            break;case cV:
            replacement = this.__lj(minutes, wildcardSize);
            break;case bE:
            replacement = this.__lj(seconds, wildcardSize);
            break;case bR:
            replacement = this.__lj(ms, 3);
            if(wildcardSize < replacement.length){

              replacement = replacement.substr(0, wildcardSize);
            } else {

              while(wildcardSize > replacement.length){

                replacement += bU;
              };
            };
            break;case bI:
            if(wildcardSize >= 1 && wildcardSize <= 4){

              replacement = S + ((timezoneSign > 0) ? a : V) + this.__lj(Math.abs(timezoneHours), 2) + y + this.__lj(timezoneMinutes, 2);
            };
            break;case cU:
            if(wildcardSize >= 1 && wildcardSize <= 3){

              replacement = ((timezoneSign > 0) ? a : V) + this.__lj(Math.abs(timezoneHours), 2) + this.__lj(timezoneMinutes, 2);
            } else {

              replacement = S + ((timezoneSign > 0) ? a : V) + this.__lj(Math.abs(timezoneHours), 2) + y + this.__lj(timezoneMinutes, 2);
            };
            break;};
            output += replacement;
          };
        };
        return output;
      },
      parse : function(dateStr){

        this.__lt();
        var hit = this.__lg.regex.exec(dateStr);
        if(hit == null){

          throw new Error(n + dateStr + cL + this.__le);
        };
        var dateValues = {
          era : 1,
          year : 1970,
          quarter : 1,
          month : 0,
          day : 1,
          dayOfYear : 1,
          hour : 0,
          ispm : false,
          weekDay : 4,
          weekYear : 1970,
          weekOfMonth : 1,
          weekOfYear : 1,
          min : 0,
          sec : 0,
          ms : 0
        };
        var currGroup = 1;
        var applyWeekYearAfterRule = false;
        var applyDayOfYearAfterRule = false;
        for(var i = 0;i < this.__lg.usedRules.length;i++){

          var rule = this.__lg.usedRules[i];
          var value = hit[currGroup];
          if(rule.field != null){

            dateValues[rule.field] = parseInt(value, 10);
          } else {

            rule.manipulator(dateValues, value, rule.pattern);
          };
          if(rule.pattern == q){

            var yearRuleApplied = false;
            for(var k = 0;k < this.__lg.usedRules.length;k++){

              if(this.__lg.usedRules[k].pattern == cw){

                yearRuleApplied = true;
                break;
              };
            };
            if(!yearRuleApplied){

              applyWeekYearAfterRule = true;
            };
          };
          if(rule.pattern.indexOf(ci) != -1){

            var dayRuleApplied = false;
            for(var k = 0;k < this.__lg.usedRules.length;k++){

              if(this.__lg.usedRules[k].pattern.indexOf(bX) != -1){

                dayRuleApplied = true;
                break;
              };
            };
            if(!dayRuleApplied){

              applyDayOfYearAfterRule = true;
            };
          };
          currGroup += (rule.groups == null) ? 1 : rule.groups;
        };
        if(applyWeekYearAfterRule){

          dateValues.year = this.__lr(dateValues.weekYear, dateValues.month, dateValues.day);
        };
        if(applyDayOfYearAfterRule){

          var dayAndMonth = this.__lq(dateValues.dayOfYear, dateValues.year);
          dateValues.month = dayAndMonth.month;
          dateValues.day = dayAndMonth.day;
        };
        if(dateValues.era < 0 && (dateValues.year * dateValues.era < 0)){

          dateValues.year = dateValues.year * dateValues.era;
        };
        var date = new Date(dateValues.year, dateValues.month, dateValues.day, (dateValues.ispm) ? (dateValues.hour + 12) : dateValues.hour, dateValues.min, dateValues.sec, dateValues.ms);
        if(this.__lf){

          date = new Date(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate(), date.getUTCHours(), date.getUTCMinutes(), date.getUTCSeconds(), date.getUTCMilliseconds());
        };
        if(dateValues.month != date.getMonth() || dateValues.year != date.getFullYear()){

          throw new Error(M + dateStr + cn);
        };
        return date;
      },
      __ls : function(){

        if(this.__li != null){

          return;
        };
        this.__li = [];
        var currWildcardChar;
        var currWildcardSize = 0;
        var currLiteral = de;
        var format = this.__le;
        var state = bO;
        var i = 0;
        while(i < format.length){

          var currChar = format.charAt(i);
          switch(state){case A:
          if(currChar == cR){

            if(i + 1 >= format.length){

              i++;
              break;
            };
            var lookAhead = format.charAt(i + 1);
            if(lookAhead == cR){

              currLiteral += currChar;
              i++;
            } else {

              i++;
              state = cO;
            };
          } else {

            currLiteral += currChar;
            i++;
          };
          break;case cJ:
          if(currChar == currWildcardChar){

            currWildcardSize++;
            i++;
          } else {

            this.__li.push({
              type : cJ,
              character : currWildcardChar,
              size : currWildcardSize
            });
            currWildcardChar = null;
            currWildcardSize = 0;
            state = bO;
          };
          break;default:
          if((currChar >= E && currChar <= bI) || (currChar >= bF && currChar <= cU)){

            currWildcardChar = currChar;
            state = cJ;
          } else if(currChar == cR){

            if(i + 1 >= format.length){

              currLiteral += currChar;
              i++;
              break;
            };
            var lookAhead = format.charAt(i + 1);
            if(lookAhead == cR){

              currLiteral += currChar;
              i++;
            };
            i++;
            state = A;
          } else {

            state = bO;
          };
          if(state != bO){

            if(currLiteral.length > 0){

              this.__li.push({
                type : co,
                text : currLiteral
              });
              currLiteral = de;
            };
          } else {

            currLiteral += currChar;
            i++;
          };
          break;};
        };
        if(currWildcardChar != null){

          this.__li.push({
            type : cJ,
            character : currWildcardChar,
            size : currWildcardSize
          });
        } else if(currLiteral.length > 0){

          this.__li.push({
            type : co,
            text : currLiteral
          });
        };
      },
      __lt : function(){

        if(this.__lg != null){

          return;
        };
        var format = this.__le;
        this.__lv();
        this.__ls();
        var usedRules = [];
        var pattern = x;
        for(var atomIdx = 0;atomIdx < this.__li.length;atomIdx++){

          var currAtom = this.__li[atomIdx];
          if(currAtom.type == co){

            pattern += qx.lang.String.escapeRegexpChars(currAtom.text);
          } else {

            var wildcardChar = currAtom.character;
            var wildcardSize = currAtom.size;
            var wildcardRule;
            for(var ruleIdx = 0;ruleIdx < this.__lh.length;ruleIdx++){

              var rule = this.__lh[ruleIdx];
              if(this.__lu(rule, wildcardChar, wildcardSize)){

                wildcardRule = rule;
                break;
              };
            };
            if(wildcardRule == null){

              var wildcardStr = de;
              for(var i = 0;i < wildcardSize;i++){

                wildcardStr += wildcardChar;
              };
              throw new Error(bu + format + r + wildcardStr + h);
            } else {

              usedRules.push(wildcardRule);
              pattern += wildcardRule.regex;
            };
          };
        };
        pattern += dr;
        var regex;
        try{

          regex = new RegExp(pattern);
        } catch(exc) {

          throw new Error(bu + format);
        };
        this.__lg = {
          regex : regex,
          "usedRules" : usedRules,
          pattern : pattern
        };
      },
      __lu : function(rule, wildcardChar, wildcardSize){

        if(wildcardChar === z && rule.pattern === cw){

          rule.regex = rule.regexFunc(wildcardSize);
          return true;
        } else if(wildcardChar === bw && rule.pattern === bi){

          rule.regex = rule.regexFunc(wildcardSize);
          return true;
        } else {

          return wildcardChar == rule.pattern.charAt(0) && wildcardSize == rule.pattern.length;
        };
      },
      __lv : function(){

        var DateFormat = qx.util.format.DateFormat;
        var LString = qx.lang.String;
        if(this.__lh != null){

          return;
        };
        var rules = this.__lh = [];
        var amMarker = qx.locale.Date.getAmMarker(this.__fe).toString() || DateFormat.AM_MARKER;
        var pmMarker = qx.locale.Date.getPmMarker(this.__fe).toString() || DateFormat.PM_MARKER;
        var locale = this.__fe;
        var yearManipulator = function(dateValues, value){

          value = parseInt(value, 10);
          if(value > 0){

            if(value < DateFormat.ASSUME_YEAR_2000_THRESHOLD){

              value += 2000;
            } else if(value < 100){

              value += 1900;
            };
          };
          dateValues.year = value;
        };
        var weekYearManipulator = function(dateValues, value){

          value = parseInt(value, 10);
          if(value > 0){

            if(value < DateFormat.ASSUME_YEAR_2000_THRESHOLD){

              value += 2000;
            } else if(value < 100){

              value += 1900;
            };
          };
          dateValues.weekYear = value;
        };
        var monthManipulator = function(dateValues, value){

          dateValues.month = parseInt(value, 10) - 1;
        };
        var localWeekDayManipulator = function(dateValues, value){

          var startOfWeek = qx.locale.Date.getWeekStart(locale);
          var dayOfWeek = (parseInt(value, 10) - 1 + startOfWeek) <= 6 ? parseInt(value, 10) - 1 + startOfWeek : (parseInt(value, 10) - 1 + startOfWeek) - 7;
          dateValues.weekDay = dayOfWeek;
        };
        var ampmManipulator = function(dateValues, value){

          var pmMarker = qx.locale.Date.getPmMarker(locale).toString() || DateFormat.PM_MARKER;
          dateValues.ispm = (value == pmMarker);
        };
        var noZeroHourManipulator = function(dateValues, value){

          dateValues.hour = parseInt(value, 10) % 24;
        };
        var noZeroAmPmHourManipulator = function(dateValues, value){

          dateValues.hour = parseInt(value, 10) % 12;
        };
        var ignoreManipulator = function(dateValues, value){

          return;
        };
        var narrowEraNames = [bF, cC];
        var narrowEraNameManipulator = function(dateValues, value){

          dateValues.era = value == bF ? 1 : -1;
        };
        var abbrevEraNames = [dt, bP];
        var abbrevEraNameManipulator = function(dateValues, value){

          dateValues.era = value == dt ? 1 : -1;
        };
        var fullEraNames = [N, cT];
        var fullEraNameManipulator = function(dateValues, value){

          dateValues.era = value == N ? 1 : -1;
        };
        var abbrevQuarterNames = [ca, cB, da, cE];
        var abbrevQuarterManipulator = function(dateValues, value){

          dateValues.quarter = abbrevQuarterNames.indexOf(value);
        };
        var fullQuarterNames = [bS, Q, I, di];
        var fullQuarterManipulator = function(dateValues, value){

          dateValues.quarter = fullQuarterNames.indexOf(value);
        };
        var cache = {
        };
        var dateNamesManipulator = function(pattern){

          var monthPatternLetters = [g, df];
          var dayPatternLetters = [bW, cm, dy];
          var firstLetterInPattern = pattern.charAt(0);
          var isMonth = monthPatternLetters.indexOf(firstLetterInPattern) >= 0;
          var getContext = function(){

            var letters = isMonth ? monthPatternLetters : dayPatternLetters;
            var context = firstLetterInPattern === letters[0] ? bT : dC;
            var patternLength = pattern.length;
            var lengthName = p;
            switch(patternLength){case 4:
            lengthName = bk;
            break;case 5:
            lengthName = cG;
            break;default:
            lengthName = p;};
            return [context, lengthName];
          };
          if(!cache[pattern]){

            cache[pattern] = {
            };
            var context = getContext();
            var func = isMonth ? qx.locale.Date.getMonthNames : qx.locale.Date.getDayNames;
            var names = func.call(qx.locale.Date, context[1], locale, context[0], true);
            for(var i = 0,l = names.length;i < l;i++){

              names[i] = LString.escapeRegexpChars(names[i].toString());
            };
            cache[pattern].data = names;
            cache[pattern].func = function(dateValues, value){

              value = LString.escapeRegexpChars(value);
              dateValues[isMonth ? dz : X] = names.indexOf(value);
            };
          };
          return cache[pattern];
        };
        rules.push({
          pattern : cF,
          regexFunc : function(yNumber){

            var regex = G;
            for(var i = 0;i < yNumber;i++){

              regex += du;
              if(i === yNumber - 1 && i !== 1){

                regex += dv;
              };
            };
            regex += bK;
            return regex;
          },
          manipulator : yearManipulator
        });
        rules.push({
          pattern : q,
          regexFunc : function(yNumber){

            var regex = G;
            for(var i = 0;i < yNumber;i++){

              regex += du;
              if(i === yNumber - 1){

                regex += dv;
              };
            };
            regex += bK;
            return regex;
          },
          manipulator : weekYearManipulator
        });
        rules.push({
          pattern : bH,
          regex : bq + abbrevEraNames.join(cx) + bK,
          manipulator : abbrevEraNameManipulator
        });
        rules.push({
          pattern : bJ,
          regex : bq + abbrevEraNames.join(cx) + bK,
          manipulator : abbrevEraNameManipulator
        });
        rules.push({
          pattern : by,
          regex : bq + abbrevEraNames.join(cx) + bK,
          manipulator : abbrevEraNameManipulator
        });
        rules.push({
          pattern : cA,
          regex : bq + fullEraNames.join(cx) + bK,
          manipulator : fullEraNameManipulator
        });
        rules.push({
          pattern : o,
          regex : bq + narrowEraNames.join(cx) + bK,
          manipulator : narrowEraNameManipulator
        });
        rules.push({
          pattern : bo,
          regex : cl,
          field : bb
        });
        rules.push({
          pattern : cc,
          regex : t,
          field : bb
        });
        rules.push({
          pattern : b,
          regex : bq + abbrevQuarterNames.join(cx) + bK,
          manipulator : abbrevQuarterManipulator
        });
        rules.push({
          pattern : be,
          regex : bq + fullQuarterNames.join(cx) + bK,
          manipulator : fullQuarterManipulator
        });
        rules.push({
          pattern : bd,
          regex : cl,
          field : bb
        });
        rules.push({
          pattern : dB,
          regex : t,
          field : bb
        });
        rules.push({
          pattern : H,
          regex : bq + abbrevQuarterNames.join(cx) + bK,
          manipulator : abbrevQuarterManipulator
        });
        rules.push({
          pattern : d,
          regex : bq + fullQuarterNames.join(cx) + bK,
          manipulator : fullQuarterManipulator
        });
        rules.push({
          pattern : dn,
          regex : cl,
          manipulator : monthManipulator
        });
        rules.push({
          pattern : cD,
          regex : t,
          manipulator : monthManipulator
        });
        rules.push({
          pattern : dl,
          regex : bq + dateNamesManipulator(dl).data.join(cx) + bK,
          manipulator : dateNamesManipulator(dl).func
        });
        rules.push({
          pattern : bV,
          regex : bq + dateNamesManipulator(bV).data.join(cx) + bK,
          manipulator : dateNamesManipulator(bV).func
        });
        rules.push({
          pattern : dx,
          regex : bq + dateNamesManipulator(dx).data.join(cx) + bK,
          manipulator : dateNamesManipulator(dx).func
        });
        rules.push({
          pattern : cq,
          regex : cl,
          manipulator : monthManipulator
        });
        rules.push({
          pattern : u,
          regex : t,
          manipulator : monthManipulator
        });
        rules.push({
          pattern : dA,
          regex : bq + dateNamesManipulator(dA).data.join(cx) + bK,
          manipulator : dateNamesManipulator(dA).func
        });
        rules.push({
          pattern : ck,
          regex : bq + dateNamesManipulator(ck).data.join(cx) + bK,
          manipulator : dateNamesManipulator(ck).func
        });
        rules.push({
          pattern : cd,
          regex : bq + dateNamesManipulator(cd).data.join(cx) + bK,
          manipulator : dateNamesManipulator(cd).func
        });
        rules.push({
          pattern : U,
          regex : t,
          field : F
        });
        rules.push({
          pattern : bX,
          regex : cl,
          field : F
        });
        rules.push({
          pattern : ci,
          regex : dj,
          field : cz
        });
        rules.push({
          pattern : dc,
          regex : t,
          field : cz
        });
        rules.push({
          pattern : ch,
          regex : bl,
          field : cz
        });
        rules.push({
          pattern : cI,
          regex : bq + dateNamesManipulator(cI).data.join(cx) + bK,
          manipulator : dateNamesManipulator(cI).func
        });
        rules.push({
          pattern : bx,
          regex : bq + dateNamesManipulator(bx).data.join(cx) + bK,
          manipulator : dateNamesManipulator(bx).func
        });
        rules.push({
          pattern : dq,
          regex : bq + dateNamesManipulator(dq).data.join(cx) + bK,
          manipulator : dateNamesManipulator(dq).func
        });
        rules.push({
          pattern : w,
          regex : bq + dateNamesManipulator(w).data.join(cx) + bK,
          manipulator : dateNamesManipulator(w).func
        });
        rules.push({
          pattern : bv,
          regex : bq + dateNamesManipulator(bv).data.join(cx) + bK,
          manipulator : dateNamesManipulator(bv).func
        });
        rules.push({
          pattern : cv,
          regex : dj,
          manipulator : localWeekDayManipulator
        });
        rules.push({
          pattern : bc,
          regex : t,
          manipulator : localWeekDayManipulator
        });
        rules.push({
          pattern : ba,
          regex : bq + dateNamesManipulator(ba).data.join(cx) + bK,
          manipulator : dateNamesManipulator(ba).func
        });
        rules.push({
          pattern : cb,
          regex : bq + dateNamesManipulator(cb).data.join(cx) + bK,
          manipulator : dateNamesManipulator(cb).func
        });
        rules.push({
          pattern : s,
          regex : bq + dateNamesManipulator(s).data.join(cx) + bK,
          manipulator : dateNamesManipulator(s).func
        });
        rules.push({
          pattern : bp,
          regex : cQ,
          manipulator : localWeekDayManipulator
        });
        rules.push({
          pattern : bs,
          regex : bq + dateNamesManipulator(bs).data.join(cx) + bK,
          manipulator : dateNamesManipulator(bs).func
        });
        rules.push({
          pattern : cu,
          regex : bq + dateNamesManipulator(cu).data.join(cx) + bK,
          manipulator : dateNamesManipulator(cu).func
        });
        rules.push({
          pattern : cH,
          regex : bq + dateNamesManipulator(cH).data.join(cx) + bK,
          manipulator : dateNamesManipulator(cH).func
        });
        rules.push({
          pattern : P,
          regex : bq + amMarker + cx + pmMarker + bK,
          manipulator : ampmManipulator
        });
        rules.push({
          pattern : cy,
          regex : dj,
          field : br
        });
        rules.push({
          pattern : bA,
          regex : dj,
          field : B
        });
        rules.push({
          pattern : bg,
          regex : t,
          field : B
        });
        rules.push({
          pattern : ce,
          regex : t,
          field : dw
        });
        rules.push({
          pattern : bh,
          regex : t,
          field : dw
        });
        rules.push({
          pattern : bL,
          regex : t,
          manipulator : noZeroHourManipulator
        });
        rules.push({
          pattern : cS,
          regex : t,
          manipulator : noZeroHourManipulator
        });
        rules.push({
          pattern : f,
          regex : t,
          field : dw
        });
        rules.push({
          pattern : J,
          regex : t,
          field : dw
        });
        rules.push({
          pattern : cr,
          regex : t,
          manipulator : noZeroAmPmHourManipulator
        });
        rules.push({
          pattern : e,
          regex : t,
          manipulator : noZeroAmPmHourManipulator
        });
        rules.push({
          pattern : bB,
          regex : t,
          field : dd
        });
        rules.push({
          pattern : cg,
          regex : t,
          field : dd
        });
        rules.push({
          pattern : bM,
          regex : t,
          field : dg
        });
        rules.push({
          pattern : dm,
          regex : t,
          field : dg
        });
        rules.push({
          pattern : D,
          regex : ct,
          field : cs
        });
        rules.push({
          pattern : dk,
          regex : ct,
          field : cs
        });
        rules.push({
          pattern : bD,
          regex : ct,
          field : cs
        });
        rules.push({
          pattern : m,
          regex : bN,
          manipulator : ignoreManipulator
        });
        rules.push({
          pattern : db,
          regex : bm,
          manipulator : ignoreManipulator
        });
      }
    },
    destruct : function(){

      if(this.__lc != null){

        qx.locale.Manager.getInstance().removeBinding(this.__lc);
      };
      this.__li = this.__lg = this.__lh = null;
    }
  });
})();
(function(){

  var a = "format",b = 'format',c = "short",d = "HH:mm",e = '_',f = "HHmmsszz",g = "HHmm",h = "HHmmss",j = "thu",k = "This case should never happen.",l = "sat",m = "cldr_date_format_",n = "HH:mm:ss zz",o = "full",p = "cldr_pm",q = "cldr_day_",r = "_",s = "cldr_month_",t = "long",u = "wed",v = "medium",w = "fri",x = "cldr_am",y = "tue",z = "qx.locale.Date",A = "mon",B = "cldr_date_time_format_",C = "sun",D = "cldr_time_format_",E = "HH:mm:ss",F = 'stand-alone';
  qx.Class.define(z, {
    statics : {
      __lw : qx.locale.Manager.getInstance(),
      getAmMarker : function(locale){

        return this.__lw.localize(x, [], locale);
      },
      getPmMarker : function(locale){

        return this.__lw.localize(p, [], locale);
      },
      getDayNames : function(length, locale, context, withFallback){

        var context = context ? context : a;
        {
        };
        var days = [C, A, y, u, j, w, l];
        var names = [];
        for(var i = 0;i < days.length;i++){

          var key = q + context + r + length + r + days[i];
          names.push(withFallback ? this.__lx(context, context === b ? F : b, key, locale) : this.__lw.localize(key, [], locale));
        };
        return names;
      },
      getDayName : function(length, day, locale, context, withFallback){

        var context = context ? context : a;
        {
        };
        var days = [C, A, y, u, j, w, l];
        var key = q + context + r + length + r + days[day];
        return withFallback ? this.__lx(context, context === b ? F : b, key, locale) : this.__lw.localize(key, [], locale);
      },
      getMonthNames : function(length, locale, context, withFallback){

        var context = context ? context : a;
        {
        };
        var names = [];
        for(var i = 0;i < 12;i++){

          var key = s + context + r + length + r + (i + 1);
          names.push(withFallback ? this.__lx(context, context === b ? F : b, key, locale) : this.__lw.localize(key, [], locale));
        };
        return names;
      },
      getMonthName : function(length, month, locale, context, withFallback){

        var context = context ? context : a;
        {
        };
        var key = s + context + r + length + r + (month + 1);
        return withFallback ? this.__lx(context, context === b ? F : b, key, locale) : this.__lw.localize(key, [], locale);
      },
      getDateFormat : function(size, locale){

        {
        };
        var key = m + size;
        return this.__lw.localize(key, [], locale);
      },
      getDateTimeFormat : function(canonical, fallback, locale){

        var key = B + canonical;
        var localizedFormat = this.__lw.localize(key, [], locale);
        if(localizedFormat == key){

          localizedFormat = fallback;
        };
        return localizedFormat;
      },
      getTimeFormat : function(size, locale){

        {
        };
        var key = D + size;
        var localizedFormat = this.__lw.localize(key, [], locale);
        if(localizedFormat != key){

          return localizedFormat;
        };
        switch(size){case c:case v:
        return qx.locale.Date.getDateTimeFormat(g, d);case t:
        return qx.locale.Date.getDateTimeFormat(h, E);case o:
        return qx.locale.Date.getDateTimeFormat(f, n);default:
        throw new Error(k);};
      },
      getWeekStart : function(locale){

        var weekStart = {
          "MV" : 5,
          "AE" : 6,
          "AF" : 6,
          "BH" : 6,
          "DJ" : 6,
          "DZ" : 6,
          "EG" : 6,
          "ER" : 6,
          "ET" : 6,
          "IQ" : 6,
          "IR" : 6,
          "JO" : 6,
          "KE" : 6,
          "KW" : 6,
          "LB" : 6,
          "LY" : 6,
          "MA" : 6,
          "OM" : 6,
          "QA" : 6,
          "SA" : 6,
          "SD" : 6,
          "SO" : 6,
          "TN" : 6,
          "YE" : 6,
          "AS" : 0,
          "AU" : 0,
          "AZ" : 0,
          "BW" : 0,
          "CA" : 0,
          "CN" : 0,
          "FO" : 0,
          "GE" : 0,
          "GL" : 0,
          "GU" : 0,
          "HK" : 0,
          "IE" : 0,
          "IL" : 0,
          "IS" : 0,
          "JM" : 0,
          "JP" : 0,
          "KG" : 0,
          "KR" : 0,
          "LA" : 0,
          "MH" : 0,
          "MN" : 0,
          "MO" : 0,
          "MP" : 0,
          "MT" : 0,
          "NZ" : 0,
          "PH" : 0,
          "PK" : 0,
          "SG" : 0,
          "TH" : 0,
          "TT" : 0,
          "TW" : 0,
          "UM" : 0,
          "US" : 0,
          "UZ" : 0,
          "VI" : 0,
          "ZA" : 0,
          "ZW" : 0,
          "MW" : 0,
          "NG" : 0,
          "TJ" : 0
        };
        var territory = qx.locale.Date._getTerritory(locale);
        return weekStart[territory] != null ? weekStart[territory] : 1;
      },
      getWeekendStart : function(locale){

        var weekendStart = {
          "EG" : 5,
          "IL" : 5,
          "SY" : 5,
          "IN" : 0,
          "AE" : 4,
          "BH" : 4,
          "DZ" : 4,
          "IQ" : 4,
          "JO" : 4,
          "KW" : 4,
          "LB" : 4,
          "LY" : 4,
          "MA" : 4,
          "OM" : 4,
          "QA" : 4,
          "SA" : 4,
          "SD" : 4,
          "TN" : 4,
          "YE" : 4
        };
        var territory = qx.locale.Date._getTerritory(locale);
        return weekendStart[territory] != null ? weekendStart[territory] : 6;
      },
      getWeekendEnd : function(locale){

        var weekendEnd = {
          "AE" : 5,
          "BH" : 5,
          "DZ" : 5,
          "IQ" : 5,
          "JO" : 5,
          "KW" : 5,
          "LB" : 5,
          "LY" : 5,
          "MA" : 5,
          "OM" : 5,
          "QA" : 5,
          "SA" : 5,
          "SD" : 5,
          "TN" : 5,
          "YE" : 5,
          "AF" : 5,
          "IR" : 5,
          "EG" : 6,
          "IL" : 6,
          "SY" : 6
        };
        var territory = qx.locale.Date._getTerritory(locale);
        return weekendEnd[territory] != null ? weekendEnd[territory] : 0;
      },
      isWeekend : function(day, locale){

        var weekendStart = qx.locale.Date.getWeekendStart(locale);
        var weekendEnd = qx.locale.Date.getWeekendEnd(locale);
        if(weekendEnd > weekendStart){

          return ((day >= weekendStart) && (day <= weekendEnd));
        } else {

          return ((day >= weekendStart) || (day <= weekendEnd));
        };
      },
      _getTerritory : function(locale){

        if(locale){

          var territory = locale.split(r)[1] || locale;
        } else {

          territory = this.__lw.getTerritory() || this.__lw.getLanguage();
        };
        return territory.toUpperCase();
      },
      __lx : function(context, fallbackContext, key, locale){

        var localizedString = this.__lw.localize(key, [], locale);
        if(localizedString == key){

          var newKey = key.replace(e + context + e, e + fallbackContext + e);
          return this.__lw.localize(newKey, [], locale);
        } else {

          return localizedString;
        };
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.form.Button",b = "button";
  qx.Class.define(a, {
    extend : qx.ui.mobile.basic.Atom,
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      },
      activatable : {
        refine : true,
        init : true
      }
    },
    members : {
      setValue : function(value){

        this.setLabel(value);
      },
      getValue : function(){

        return this.getLabel();
      }
    }
  });
})();
(function(){

  var a = "loadEnd",b = "qx.io.request.AbstractRequest",c = "changePhase",d = "GET",f = "sent",g = "qx.event.type.Data",h = "qx.io.request.authentication.IAuthentication",i = "error",j = "fail",k = "loading",l = "load",m = "qx.event.type.Event",n = "abort",o = "success",p = "String",q = "",r = "opened",s = "POST",t = "statusError",u = "readyStateChange",v = "Abstract method call",w = "abstract",x = "unsent",y = "changeResponse",z = "Number",A = "Content-Type",B = "timeout",C = "undefined";
  qx.Class.define(b, {
    type : w,
    extend : qx.core.Object,
    construct : function(url){

      qx.core.Object.call(this);
      if(url !== undefined){

        this.setUrl(url);
      };
      this.__ly = {
      };
      var transport = this._transport = this._createTransport();
      this._setPhase(x);
      this.__lz = qx.lang.Function.bind(this._onReadyStateChange, this);
      this.__lA = qx.lang.Function.bind(this._onLoad, this);
      this.__lB = qx.lang.Function.bind(this._onLoadEnd, this);
      this.__lC = qx.lang.Function.bind(this._onAbort, this);
      this.__lD = qx.lang.Function.bind(this._onTimeout, this);
      this.__lE = qx.lang.Function.bind(this._onError, this);
      transport.onreadystatechange = this.__lz;
      transport.onload = this.__lA;
      transport.onloadend = this.__lB;
      transport.onabort = this.__lC;
      transport.ontimeout = this.__lD;
      transport.onerror = this.__lE;
    },
    events : {
      "readyStateChange" : m,
      "success" : m,
      "load" : m,
      "loadEnd" : m,
      "abort" : m,
      "timeout" : m,
      "error" : m,
      "statusError" : m,
      "fail" : m,
      "changeResponse" : g,
      "changePhase" : g
    },
    properties : {
      url : {
        check : p
      },
      timeout : {
        check : z,
        nullable : true,
        init : 0
      },
      requestData : {
        check : function(value){

          return qx.lang.Type.isString(value) || qx.Class.isSubClassOf(value.constructor, qx.core.Object) || qx.lang.Type.isObject(value);
        },
        nullable : true
      },
      authentication : {
        check : h,
        nullable : true
      }
    },
    members : {
      __lz : null,
      __lA : null,
      __lB : null,
      __lC : null,
      __lD : null,
      __lE : null,
      __lF : null,
      __lG : null,
      __lH : null,
      __ly : null,
      __lI : null,
      _transport : null,
      _createTransport : function(){

        throw new Error(v);
      },
      _getConfiguredUrl : function(){
      },
      _getConfiguredRequestHeaders : function(){
      },
      _getParsedResponse : function(){

        throw new Error(v);
      },
      _getMethod : function(){

        return d;
      },
      _isAsync : function(){

        return true;
      },
      send : function(){

        var transport = this._transport,url,method,async,serializedData;
        url = this._getConfiguredUrl();
        if(/\#/.test(url)){

          url = url.replace(/\#.*/, q);
        };
        transport.timeout = this.getTimeout();
        method = this._getMethod();
        async = this._isAsync();
        {
        };
        transport.open(method, url, async);
        this._setPhase(r);
        serializedData = this._serializeData(this.getRequestData());
        this._setRequestHeaders();
        {
        };
        method == d ? transport.send() : transport.send(serializedData);
        this._setPhase(f);
      },
      abort : function(){

        {
        };
        this.__lG = true;
        this.__lH = n;
        this._transport.abort();
      },
      _setRequestHeaders : function(){

        var transport = this._transport,requestHeaders = this._getAllRequestHeaders();
        for(var key in requestHeaders){

          transport.setRequestHeader(key, requestHeaders[key]);
        };
      },
      _getAllRequestHeaders : function(){

        var requestHeaders = {
        };
        qx.lang.Object.mergeWith(requestHeaders, this._getConfiguredRequestHeaders());
        qx.lang.Object.mergeWith(requestHeaders, this.__lJ());
        qx.lang.Object.mergeWith(requestHeaders, this.__lI);
        qx.lang.Object.mergeWith(requestHeaders, this.__ly);
        return requestHeaders;
      },
      __lJ : function(){

        var auth = this.getAuthentication(),headers = {
        };
        if(auth){

          auth.getAuthHeaders().forEach(function(header){

            headers[header.key] = header.value;
          });
          return headers;
        };
      },
      setRequestHeader : function(key, value){

        this.__ly[key] = value;
      },
      getRequestHeader : function(key){

        return this.__ly[key];
      },
      removeRequestHeader : function(key){

        if(this.__ly[key]){

          delete this.__ly[key];
        };
      },
      getTransport : function(){

        return this._transport;
      },
      getReadyState : function(){

        return this._transport.readyState;
      },
      getPhase : function(){

        return this.__lH;
      },
      getStatus : function(){

        return this._transport.status;
      },
      getStatusText : function(){

        return this._transport.statusText;
      },
      getResponseText : function(){

        return this._transport.responseText;
      },
      getAllResponseHeaders : function(){

        return this._transport.getAllResponseHeaders();
      },
      getResponseHeader : function(key){

        return this._transport.getResponseHeader(key);
      },
      overrideResponseContentType : function(contentType){

        return this._transport.overrideMimeType(contentType);
      },
      getResponseContentType : function(){

        return this.getResponseHeader(A);
      },
      isDone : function(){

        return this.getReadyState() === 4;
      },
      getResponse : function(){

        return this.__lF;
      },
      _setResponse : function(response){

        var oldResponse = response;
        if(this.__lF !== response){

          this.__lF = response;
          this.fireEvent(y, qx.event.type.Data, [this.__lF, oldResponse]);
        };
      },
      _onReadyStateChange : function(){

        var readyState = this.getReadyState();
        {
        };
        this.fireEvent(u);
        if(this.__lG){

          return;
        };
        if(readyState === 3){

          this._setPhase(k);
        };
        if(this.isDone()){

          this.__lK();
        };
      },
      __lK : function(){

        {
        };
        this._setPhase(l);
        if(qx.util.Request.isSuccessful(this.getStatus())){

          {
          };
          this._setResponse(this._getParsedResponse());
          this._fireStatefulEvent(o);
        } else {

          try{

            this._setResponse(this._getParsedResponse());
          } catch(e) {
          };
          if(this.getStatus() !== 0){

            this._fireStatefulEvent(t);
            this.fireEvent(j);
          };
        };
      },
      _onLoad : function(){

        this.fireEvent(l);
      },
      _onLoadEnd : function(){

        this.fireEvent(a);
      },
      _onAbort : function(){

        this._fireStatefulEvent(n);
      },
      _onTimeout : function(){

        this._fireStatefulEvent(B);
        this.fireEvent(j);
      },
      _onError : function(){

        this.fireEvent(i);
        this.fireEvent(j);
      },
      _fireStatefulEvent : function(evt){

        {
        };
        this._setPhase(evt);
        this.fireEvent(evt);
      },
      _setPhase : function(phase){

        var previousPhase = this.__lH;
        {
        };
        this.__lH = phase;
        this.fireDataEvent(c, phase, previousPhase);
      },
      _serializeData : function(data){

        var isPost = typeof this.getMethod !== C && this.getMethod() == s,isJson = /application\/.*\+?json/.test(this.getRequestHeader(A));
        if(!data){

          return null;
        };
        if(qx.lang.Type.isString(data)){

          return data;
        };
        if(qx.Class.isSubClassOf(data.constructor, qx.core.Object)){

          return qx.util.Serializer.toUriParameter(data);
        };
        if(isJson && (qx.lang.Type.isObject(data) || qx.lang.Type.isArray(data))){

          return qx.lang.Json.stringify(data);
        };
        if(qx.lang.Type.isObject(data)){

          return qx.util.Uri.toParameter(data, isPost);
        };
      }
    },
    environment : {
      "qx.debug.io" : false
    },
    destruct : function(){

      var transport = this._transport,noop = function(){
      };
      if(this._transport){

        transport.onreadystatechange = transport.onload = transport.onloadend = transport.onabort = transport.ontimeout = transport.onerror = noop;
        transport.dispose();
      };
    }
  });
})();
(function(){

  var a = "HEAD",b = "CONNECT",c = "OPTIONS",d = "PUT",e = "GET",f = "PATCH",g = "//",h = "DELETE",i = "POST",j = "TRACE",k = "qx.util.Request";
  qx.Bootstrap.define(k, {
    statics : {
      isCrossDomain : function(url){

        var result = qx.util.Uri.parseUri(url),location = window.location;
        if(!location){

          return false;
        };
        var protocol = location.protocol;
        if(!(url.indexOf(g) !== -1)){

          return false;
        };
        if(protocol.substr(0, protocol.length - 1) == result.protocol && location.host === result.host && location.port === result.port){

          return false;
        };
        return true;
      },
      isSuccessful : function(status){

        return (status >= 200 && status < 300 || status === 304);
      },
      isMethod : function(method){

        var knownMethods = [e, i, d, h, a, c, j, b, f];
        return (knownMethods.indexOf(method) !== -1) ? true : false;
      },
      methodAllowsRequestBody : function(method){

        return !((/^(GET|HEAD)$/).test(method));
      }
    }
  });
})();
(function(){

  var a = "null",b = "XMLHttpRequest",c = "Boolean",d = "X-Requested-With",e = "",f = "application/x-www-form-urlencoded",g = "Cache-Control",h = "Content-Type",i = "qx.event.type.Event",j = "GET",k = "qx.io.request.Xhr",l = "Accept",m = "String";
  qx.Class.define(k, {
    extend : qx.io.request.AbstractRequest,
    construct : function(url, method){

      if(method !== undefined){

        this.setMethod(method);
      };
      qx.io.request.AbstractRequest.call(this, url);
      this._parser = this._createResponseParser();
    },
    events : {
      "readyStateChange" : i,
      "success" : i,
      "load" : i,
      "statusError" : i
    },
    properties : {
      method : {
        init : j
      },
      async : {
        check : c,
        init : true
      },
      accept : {
        check : m,
        nullable : true
      },
      cache : {
        check : function(value){

          return qx.lang.Type.isBoolean(value) || qx.lang.Type.isString(value);
        },
        init : true
      }
    },
    members : {
      _parser : null,
      _createTransport : function(){

        return new qx.bom.request.Xhr();
      },
      _getConfiguredUrl : function(){

        var url = this.getUrl(),serializedData;
        if(this.getMethod() === j && this.getRequestData()){

          serializedData = this._serializeData(this.getRequestData());
          url = qx.util.Uri.appendParamsToUrl(url, serializedData);
        };
        if(this.getCache() === false){

          url = qx.util.Uri.appendParamsToUrl(url, {
            nocache : new Date().valueOf()
          });
        };
        return url;
      },
      _getConfiguredRequestHeaders : function(){

        var headers = {
        },isAllowsBody = qx.util.Request.methodAllowsRequestBody(this.getMethod());
        if(!qx.util.Request.isCrossDomain(this.getUrl())){

          headers[d] = b;
        };
        if(qx.lang.Type.isString(this.getCache())){

          headers[g] = this.getCache();
        };
        if(this.getRequestData() !== a && isAllowsBody){

          headers[h] = f;
        };
        if(this.getAccept()){

          {
          };
          headers[l] = this.getAccept();
        };
        return headers;
      },
      _getMethod : function(){

        return this.getMethod();
      },
      _isAsync : function(){

        return this.isAsync();
      },
      _createResponseParser : function(){

        return new qx.util.ResponseParser();
      },
      _getParsedResponse : function(){

        var response = this._transport.responseText,contentType = this.getResponseContentType() || e;
        return this._parser.parse(response, contentType);
      },
      setParser : function(parser){

        return this._parser.setParser(parser);
      }
    }
  });
})();
(function(){

  var a = "activex",b = "No XHR support available.",c = "If-None-Match",d = "xhr",f = "If-Modified-Since",g = "engine.version",h = "onunload",i = "GET",j = "-1",k = "qx.debug.io",l = "error",m = "loadend",n = "load",o = "abort",p = "browser.documentmode",q = "",r = "engine.name",s = "Microsoft.XMLHTTP",t = "Already disposed",u = "browser.version",v = "opera",w = "qx.bom.request.Xhr",x = "Not enough arguments",y = "gecko",z = "If-Match",A = "mshtml",B = "readystatechange",C = "Microsoft.XMLDOM",D = "file:",E = "If-Range",F = "Content-Type",G = "io.xhr",H = "on",I = "timeout",J = "undefined",K = "Native XHR object doesn't support overrideMimeType.";
  qx.Bootstrap.define(w, {
    extend : Object,
    construct : function(){

      var boundFunc = qx.Bootstrap.bind(this.__lY, this);
      if(qx.event && qx.event.GlobalError && qx.event.GlobalError.observeMethod){

        this.__lL = qx.event.GlobalError.observeMethod(boundFunc);
      } else {

        this.__lL = boundFunc;
      };
      this.__lM = qx.Bootstrap.bind(this.__lX, this);
      this.__lD = qx.Bootstrap.bind(this.__md, this);
      this.__lW();
      this._emitter = new qx.event.Emitter();
      if(window.attachEvent){

        this.__lN = qx.Bootstrap.bind(this.__mg, this);
        window.attachEvent(h, this.__lN);
      };
    },
    statics : {
      UNSENT : 0,
      OPENED : 1,
      HEADERS_RECEIVED : 2,
      LOADING : 3,
      DONE : 4
    },
    events : {
      "readystatechange" : w,
      "error" : w,
      "loadend" : w,
      "timeout" : w,
      "abort" : w,
      "load" : w
    },
    members : {
      readyState : 0,
      responseText : q,
      responseXML : null,
      status : 0,
      statusText : q,
      timeout : 0,
      open : function(method, url, async, user, password){

        this.__mi();
        if(typeof url === J){

          throw new Error(x);
        } else if(typeof method === J){

          method = i;
        };
        this.__lG = false;
        this.__lO = false;
        this.__lP = false;
        this.__lQ = url;
        if(typeof async == J){

          async = true;
        };
        this.__lR = async;
        if(!this.__mh() && this.readyState > qx.bom.request.Xhr.UNSENT){

          this.dispose();
          this.__lW();
        };
        this.__lT.onreadystatechange = this.__lL;
        try{

          {
          };
          this.__lT.open(method, url, async, user, password);
        } catch(OpenError) {

          if(!qx.util.Request.isCrossDomain(url)){

            throw OpenError;
          };
          if(!this.__lR){

            this.__lS = OpenError;
          };
          if(this.__lR){

            if(window.XDomainRequest){

              this.readyState = 4;
              this.__lT = new XDomainRequest();
              this.__lT.onerror = qx.Bootstrap.bind(function(){

                this._emit(B);
                this._emit(l);
                this._emit(m);
              }, this);
              {
              };
              this.__lT.open(method, url, async, user, password);
              return;
            };
            window.setTimeout(qx.Bootstrap.bind(function(){

              if(this.__lV){

                return;
              };
              this.readyState = 4;
              this._emit(B);
              this._emit(l);
              this._emit(m);
            }, this));
          };
        };
        if(qx.core.Environment.get(r) === A && qx.core.Environment.get(p) < 9 && this.__lT.readyState > 0){

          this.__lT.setRequestHeader(f, j);
        };
        if(qx.core.Environment.get(r) === y && parseInt(qx.core.Environment.get(g), 10) < 2 && !this.__lR){

          this.readyState = qx.bom.request.Xhr.OPENED;
          this._emit(B);
        };
      },
      setRequestHeader : function(key, value){

        this.__mi();
        if(key == z || key == f || key == c || key == E){

          this.__lP = true;
        };
        this.__lT.setRequestHeader(key, value);
        return this;
      },
      send : function(data){

        this.__mi();
        if(!this.__lR && this.__lS){

          throw this.__lS;
        };
        if(qx.core.Environment.get(r) === v && this.timeout === 0){

          this.timeout = 10000;
        };
        if(this.timeout > 0){

          this.__lU = window.setTimeout(this.__lD, this.timeout);
        };
        data = typeof data == J ? null : data;
        try{

          {
          };
          this.__lT.send(data);
        } catch(SendError) {

          if(!this.__lR){

            throw SendError;
          };
          if(this._getProtocol() === D){

            this.readyState = 2;
            this.__ma();
            var that = this;
            window.setTimeout(function(){

              if(that.__lV){

                return;
              };
              that.readyState = 3;
              that.__ma();
              that.readyState = 4;
              that.__ma();
            });
          };
        };
        if(qx.core.Environment.get(r) === y && !this.__lR){

          this.__lY();
        };
        this.__lO = true;
        return this;
      },
      abort : function(){

        this.__mi();
        this.__lG = true;
        this.__lT.abort();
        if(this.__lT){

          this.readyState = this.__lT.readyState;
        };
        return this;
      },
      _emit : function(event){

        if(this[H + event]){

          this[H + event]();
        };
        this._emitter.emit(event, this);
      },
      onreadystatechange : function(){
      },
      onload : function(){
      },
      onloadend : function(){
      },
      onerror : function(){
      },
      onabort : function(){
      },
      ontimeout : function(){
      },
      on : function(name, listener, ctx){

        this._emitter.on(name, listener, ctx);
        return this;
      },
      getResponseHeader : function(header){

        this.__mi();
        return this.__lT.getResponseHeader(header);
      },
      getAllResponseHeaders : function(){

        this.__mi();
        return this.__lT.getAllResponseHeaders();
      },
      overrideMimeType : function(mimeType){

        this.__mi();
        if(this.__lT.overrideMimeType){

          this.__lT.overrideMimeType(mimeType);
        } else {

          throw new Error(K);
        };
        return this;
      },
      getRequest : function(){

        return this.__lT;
      },
      dispose : function(){

        if(this.__lV){

          return false;
        };
        window.clearTimeout(this.__lU);
        if(window.detachEvent){

          window.detachEvent(h, this.__lN);
        };
        try{

          this.__lT.onreadystatechange;
        } catch(PropertiesNotAccessable) {

          return false;
        };
        var noop = function(){
        };
        this.__lT.onreadystatechange = noop;
        this.__lT.onload = noop;
        this.__lT.onerror = noop;
        this.abort();
        this.__lT = null;
        this.__lV = true;
        return true;
      },
      isDisposed : function(){

        return !!this.__lV;
      },
      _createNativeXhr : function(){

        var xhr = qx.core.Environment.get(G);
        if(xhr === d){

          return new XMLHttpRequest();
        };
        if(xhr == a){

          return new window.ActiveXObject(s);
        };
        qx.Bootstrap.error(this, b);
      },
      _getProtocol : function(){

        var url = this.__lQ;
        var protocolRe = /^(\w+:)\/\//;
        if(url !== null && url.match){

          var match = url.match(protocolRe);
          if(match && match[1]){

            return match[1];
          };
        };
        return window.location.protocol;
      },
      __lT : null,
      __lR : null,
      __lL : null,
      __lM : null,
      __lN : null,
      __lD : null,
      __lO : null,
      __lQ : null,
      __lG : null,
      __iW : null,
      __lV : null,
      __lU : null,
      __lS : null,
      __lP : null,
      __lW : function(){

        this.__lT = this._createNativeXhr();
        this.__lT.onreadystatechange = this.__lL;
        if(this.__lT.onabort){

          this.__lT.onabort = this.__lM;
        };
        this.__lV = this.__lO = this.__lG = false;
      },
      __lX : function(){

        if(!this.__lG){

          this.abort();
        };
      },
      __lY : function(){

        var nxhr = this.__lT,propertiesReadable = true;
        {
        };
        if(this.readyState == nxhr.readyState){

          return;
        };
        this.readyState = nxhr.readyState;
        if(this.readyState === qx.bom.request.Xhr.DONE && this.__lG && !this.__lO){

          return;
        };
        if(!this.__lR && (nxhr.readyState == 2 || nxhr.readyState == 3)){

          return;
        };
        this.status = 0;
        this.statusText = this.responseText = q;
        this.responseXML = null;
        if(this.readyState >= qx.bom.request.Xhr.HEADERS_RECEIVED){

          try{

            this.status = nxhr.status;
            this.statusText = nxhr.statusText;
            this.responseText = nxhr.responseText;
            this.responseXML = nxhr.responseXML;
          } catch(XhrPropertiesNotReadable) {

            propertiesReadable = false;
          };
          if(propertiesReadable){

            this.__me();
            this.__mf();
          };
        };
        this.__ma();
        if(this.readyState == qx.bom.request.Xhr.DONE){

          if(nxhr){

            nxhr.onreadystatechange = function(){
            };
          };
        };
      },
      __ma : function(){

        var that = this;
        if(this.readyState === qx.bom.request.Xhr.DONE){

          window.clearTimeout(this.__lU);
        };
        if(qx.core.Environment.get(r) == A && qx.core.Environment.get(p) < 8){

          if(this.__lR && !this.__lO && this.readyState >= qx.bom.request.Xhr.LOADING){

            if(this.readyState == qx.bom.request.Xhr.LOADING){

              return;
            };
            if(this.readyState == qx.bom.request.Xhr.DONE){

              window.setTimeout(function(){

                if(that.__lV){

                  return;
                };
                that.readyState = 3;
                that._emit(B);
                that.readyState = 4;
                that._emit(B);
                that.__mb();
              });
              return;
            };
          };
        };
        this._emit(B);
        if(this.readyState === qx.bom.request.Xhr.DONE){

          this.__mb();
        };
      },
      __mb : function(){

        if(this.__iW){

          this._emit(I);
          if(qx.core.Environment.get(r) === v){

            this._emit(l);
          };
          this.__iW = false;
        } else {

          if(this.__lG){

            this._emit(o);
          } else {

            if(this.__mc()){

              this._emit(l);
            } else {

              this._emit(n);
            };
          };
        };
        this._emit(m);
      },
      __mc : function(){

        var error;
        if(this._getProtocol() === D){

          error = !this.responseText;
        } else {

          error = !this.statusText;
        };
        return error;
      },
      __md : function(){

        var nxhr = this.__lT;
        this.readyState = qx.bom.request.Xhr.DONE;
        this.__iW = true;
        nxhr.abort();
        this.responseText = q;
        this.responseXML = null;
        this.__ma();
      },
      __me : function(){

        var isDone = this.readyState === qx.bom.request.Xhr.DONE;
        if(this._getProtocol() === D && this.status === 0 && isDone){

          if(!this.__mc()){

            this.status = 200;
          };
        };
        if(this.status === 1223){

          this.status = 204;
        };
        if(qx.core.Environment.get(r) === v){

          if(isDone && this.__lP && !this.__lG && this.status === 0){

            this.status = 304;
          };
        };
      },
      __mf : function(){

        if(qx.core.Environment.get(r) == A && (this.getResponseHeader(F) || q).match(/[^\/]+\/[^\+]+\+xml/) && this.responseXML && !this.responseXML.documentElement){

          var dom = new window.ActiveXObject(C);
          dom.async = false;
          dom.validateOnParse = false;
          dom.loadXML(this.responseText);
          this.responseXML = dom;
        };
      },
      __mg : function(){

        try{

          if(this){

            this.dispose();
          };
        } catch(e) {
        };
      },
      __mh : function(){

        var name = qx.core.Environment.get(r);
        var version = qx.core.Environment.get(u);
        return !(name == A && version < 9 || name == y && version < 3.5);
      },
      __mi : function(){

        if(this.__lV){

          throw new Error(t);
        };
      }
    },
    defer : function(){

      qx.core.Environment.add(k, false);
    }
  });
})();
(function(){

  var a = "function",b = "qx.util.ResponseParser",c = "";
  qx.Bootstrap.define(b, {
    construct : function(parser){

      if(parser !== undefined){

        this.setParser(parser);
      };
    },
    statics : {
      PARSER : {
        json : qx.lang.Json.parse,
        xml : qx.xml.Document.fromString
      }
    },
    members : {
      __mj : null,
      parse : function(response, contentType){

        var parser = this._getParser(contentType);
        if(typeof parser === a){

          if(response !== c){

            return parser.call(this, response);
          };
        };
        return response;
      },
      setParser : function(parser){

        if(typeof qx.util.ResponseParser.PARSER[parser] === a){

          return this.__mj = qx.util.ResponseParser.PARSER[parser];
        };
        {
        };
        return this.__mj = parser;
      },
      _getParser : function(contentType){

        var parser = this.__mj,contentTypeOrig = c,contentTypeNormalized = c;
        if(parser){

          return parser;
        };
        contentTypeOrig = contentType || c;
        contentTypeNormalized = contentTypeOrig.replace(/;.*$/, c);
        if(/^application\/(\w|\.)*\+?json$/.test(contentTypeNormalized)){

          parser = qx.util.ResponseParser.PARSER.json;
        };
        if(/^application\/xml$/.test(contentTypeNormalized)){

          parser = qx.util.ResponseParser.PARSER.xml;
        };
        if(/[^\/]+\/[^\+]+\+xml$/.test(contentTypeOrig)){

          parser = qx.util.ResponseParser.PARSER.xml;
        };
        return parser;
      }
    }
  });
})();
(function(){

  var a = "current: ",b = "/entities/{entity}/instances/",c = "Found inAppHistory",d = "/entities/{entity}/actions/{actionName}",e = "kirra_qooxdoo.Application",f = "/entities/{entity}/instances/{objectId}",g = "/entities/{entity}/instances/{objectId}/actions/{actionName}",h = ".*",i = "[?&]?app-uri\=(.*)\&?",j = "/entities/{entity}/instances/{objectId}/relationships/{relationshipName}/{relatedObjectId}",k = "Falling back to ",l = "/entities/{entity}/instances/{objectId}/relationships/{relationshipName}",m = "You must specify an application URI or path (same server) using the app-uri or app-path query parameters, like '...?app-uri=http://myserver.com/myapp/rest/' or '...?app-path=/myapp/rest/'.",n = "[?&]?app-path\=(.*)\&?",o = "No inAppHistory",p = "/";
  qx.Class.define(e, {
    extend : qx.application.Mobile,
    properties : {
      routing : {
        init : null
      }
    },
    statics : {
      getInstance : function(){

        return this.$$instance;
      }
    },
    members : {
      repository : null,
      inAppHistory : [],
      goBack : function(fallback){

        this.inAppHistory.pop();
        if(this.inAppHistory.length <= 1){

          console.log(o);
          console.log(k + fallback);
          qx.bom.History.getInstance().setState(fallback);
          return;
        };
        var goTo = this.inAppHistory.pop();
        console.log(c);
        console.log(this.inAppHistory);
        qx.bom.History.getInstance().setState(goTo);
        console.log(a + qx.bom.History.getInstance().getState());
      },
      main : function(){

        qx.application.Mobile.prototype.main.call(this);
        {
        };
        var me = this;
        this.$$instance = this;
        var uriMatches = window.location.search.match(i);
        var pathMatches = window.location.search.match(n);
        if(!uriMatches && !pathMatches){

          var abortMessage = m;
          alert(abortMessage);
          throw Error(abortMessage);
        };
        var apiBaseUri = uriMatches ? uriMatches[1] : (window.location.origin + pathMatches[1]);
        this.repository = new kirra_qooxdoo.Repository(apiBaseUri);
        this.mainPage = new kirra_qooxdoo.EntityNavigator(this.repository);
        this.instanceNavigator = new kirra_qooxdoo.InstanceNavigator(this.repository);
        this.instanceForm = new kirra_qooxdoo.InstanceForm(this.repository);
        this.relatedInstanceNavigator = new kirra_qooxdoo.RelatedInstanceNavigator(this.repository);
        this.actionForm = new kirra_qooxdoo.ActionForm(this.repository);
        this.repository.loadApplication(function(application){

          document.title = application.applicationName;
        });
        var manager = new qx.ui.mobile.page.Manager(false);
        manager.addMaster(this.mainPage);
        manager.addDetail([this.instanceNavigator, this.instanceForm, this.actionForm, this.relatedInstanceNavigator]);
        var nm = new qx.application.Routing();
        nm.onGet(p, function(data){

          this.mainPage.show();
        }, this);
        nm.onGet(b, function(data){

          this.instanceNavigator.showFor(data.params.entity);
        }, this);
        nm.onGet(f, function(data){

          this.instanceForm.showFor(data.params.entity, data.params.objectId);
        }, this);
        nm.onGet(l, function(data){

          this.relatedInstanceNavigator.showFor(data.params.entity, data.params.relationshipName, data.params.objectId);
        }, this);
        nm.onGet(g, function(data){

          this.actionForm.showFor(data.params.entity, data.params.actionName, data.params.objectId);
        }, this);
        nm.onGet(j, function(data){

          this.instanceForm.showForRelated(data.params.entity, data.params.objectId, data.params.relationshipName, data.params.relatedObjectId);
        }, this);
        nm.onGet(d, function(data){

          this.actionForm.showFor(data.params.entity, data.params.actionName, data.params.objectId);
        }, this);
        nm.onAny(h, function(data){

          if(me.inAppHistory.indexOf(data.path) >= 0){

            me.inAppHistory = me.inAppHistory.slice(0, me.inAppHistory.indexOf(data.path) + 1);
          } else {

            if(!data.customData || (!data.customData.fromHistory && !data.customData.reverse)){

              me.inAppHistory.push(data.path);
            };
          };
          console.log(me.inAppHistory);
        });
        this.setRouting(nm);
        nm.init();
      }
    }
  });
})();
(function(){

  var a = "request",b = "mshtml",c = "engine.name",d = "changeTitle",e = "abstract",f = "",g = '#',h = "_applyState",i = "Abstract method call",j = "event.hashchange",k = "_applyTitle",l = "changeState",m = "qx.bom.History",n = "String",o = "browser.documentmode",p = "qx.event.type.Data";
  qx.Class.define(m, {
    extend : qx.core.Object,
    type : e,
    construct : function(){

      qx.core.Object.call(this);
      this._baseUrl = window.location.href.split(g)[0] + g;
      this._titles = {
      };
      this._setInitialState();
    },
    events : {
      "request" : p
    },
    statics : {
      SUPPORTS_HASH_CHANGE_EVENT : qx.core.Environment.get(j),
      getInstance : function(){

        var runsInIframe = !(window == window.top);
        if(!this.$$instance){

          if(runsInIframe && qx.core.Environment.get(o) == 9){

            this.$$instance = new qx.bom.HashHistory();
          } else if(runsInIframe && qx.core.Environment.get(c) == b && qx.core.Environment.get(o) < 9){

            this.$$instance = new qx.bom.IframeHistory();
          } else if(this.SUPPORTS_HASH_CHANGE_EVENT){

            this.$$instance = new qx.bom.NativeHistory();
          } else if((qx.core.Environment.get(c) == b)){

            this.$$instance = new qx.bom.IframeHistory();
          } else {

            this.$$instance = new qx.bom.NativeHistory();
          };;;
        };
        return this.$$instance;
      }
    },
    properties : {
      title : {
        check : n,
        event : d,
        nullable : true,
        apply : k
      },
      state : {
        check : n,
        event : l,
        nullable : true,
        apply : h
      }
    },
    members : {
      _titles : null,
      _applyState : function(value, old){

        this._writeState(value);
      },
      _setInitialState : function(){

        this.setState(this._readState());
      },
      _encode : function(value){

        if(qx.lang.Type.isString(value)){

          return encodeURIComponent(value);
        };
        return f;
      },
      _decode : function(value){

        if(qx.lang.Type.isString(value)){

          return decodeURIComponent(value);
        };
        return f;
      },
      _applyTitle : function(title){

        if(title != null){

          document.title = title || f;
        };
      },
      addToHistory : function(state, newTitle){

        if(!qx.lang.Type.isString(state)){

          state = state + f;
        };
        if(qx.lang.Type.isString(newTitle)){

          this.setTitle(newTitle);
          this._titles[state] = newTitle;
        };
        if(this.getState() !== state){

          this._writeState(state);
        };
      },
      navigateBack : function(){

        qx.event.Timer.once(function(){

          history.back();
        }, this, 100);
      },
      navigateForward : function(){

        qx.event.Timer.once(function(){

          history.forward();
        }, this, 100);
      },
      _onHistoryLoad : function(state){

        this.setState(state);
        this.fireDataEvent(a, state);
        if(this._titles[state] != null){

          this.setTitle(this._titles[state]);
        };
      },
      _readState : function(){

        throw new Error(i);
      },
      _writeState : function(){

        throw new Error(i);
      },
      _setHash : function(value){

        var url = this._baseUrl + (value || f);
        var loc = window.location;
        if(url != loc.href){

          loc.href = url;
        };
      },
      _getHash : function(){

        var hash = /#(.*)$/.exec(window.location.href);
        return hash && hash[1] ? hash[1] : f;
      }
    },
    destruct : function(){

      this._titles = null;
    }
  });
})();
(function(){

  var a = "qx.blankpage",b = "undefined",c = "",d = "interval",e = "#",f = "-1000px",g = "hidden",h = "can't initialize iframe",i = "qx.bom.HashHistory",j = "absolute";
  qx.Class.define(i, {
    extend : qx.bom.History,
    construct : function(){

      qx.bom.History.call(this);
      this._baseUrl = null;
      this.__mn();
    },
    members : {
      __mk : null,
      __ml : null,
      __mm : false,
      addToHistory : function(state, newTitle){

        if(!qx.lang.Type.isString(state)){

          state = state + c;
        };
        if(qx.lang.Type.isString(newTitle)){

          this.setTitle(newTitle);
          this._titles[state] = newTitle;
        };
        if(this.getState() !== state){

          this._writeState(state);
        };
      },
      __mn : function(){

        this.__ml = this.__mo();
        document.body.appendChild(this.__ml);
        this.__mp(function(){

          this._baseUrl = this.__ml.contentWindow.document.location.href;
          this.__mq();
        }, this);
      },
      __mo : function(){

        var iframe = qx.bom.Iframe.create({
          src : qx.util.ResourceManager.getInstance().toUri(qx.core.Environment.get(a)) + e
        });
        iframe.style.visibility = g;
        iframe.style.position = j;
        iframe.style.left = f;
        iframe.style.top = f;
        return iframe;
      },
      __mp : function(callback, context, retry){

        if(typeof retry === b){

          retry = 0;
        };
        if(!this.__ml.contentWindow || !this.__ml.contentWindow.document){

          if(retry > 20){

            throw new Error(h);
          };
          qx.event.Timer.once(function(){

            this.__mp(callback, context, ++retry);
          }, this, 10);
          return;
        };
        this.__mm = true;
        callback.call(context || window);
      },
      __mq : function(){

        qx.event.Idle.getInstance().addListener(d, this.__ms, this);
      },
      __mr : function(){

        qx.event.Idle.getInstance().removeListener(d, this.__ms, this);
      },
      __ms : function(){

        var currentState = this._readState();
        if(qx.lang.Type.isString(currentState) && currentState != this.getState()){

          this._onHistoryLoad(currentState);
        };
      },
      _readState : function(){

        var hash = !this._getHash() ? c : this._getHash().substr(1);
        return this._decode(hash);
      },
      _getHash : function(){

        if(!this.__mm){

          return null;
        };
        return this.__ml.contentWindow.document.location.hash;
      },
      _writeState : function(state){

        this._setHash(this._encode(state));
      },
      _setHash : function(value){

        if(!this.__ml || !this._baseUrl){

          return;
        };
        var hash = !this.__ml.contentWindow.document.location.hash ? c : this.__ml.contentWindow.document.location.hash.substr(1);
        if(value != hash){

          this.__ml.contentWindow.document.location.hash = value;
        };
      }
    },
    destruct : function(){

      this.__mr();
      this.__ml = null;
    }
  });
})();
(function(){

  var a = "qx.event.handler.Iframe",b = "load",c = "iframe",d = "navigate";
  qx.Class.define(a, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        load : 1,
        navigate : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : false,
      onevent : qx.event.GlobalError.observeMethod(function(target){

        var currentUrl = qx.bom.Iframe.queryCurrentUrl(target);
        if(currentUrl !== target.$$url){

          qx.event.Registration.fireEvent(target, d, qx.event.type.Data, [currentUrl]);
          target.$$url = currentUrl;
        };
        qx.event.Registration.fireEvent(target, b);
      })
    },
    members : {
      canHandleEvent : function(target, type){

        return target.tagName.toLowerCase() === c;
      },
      registerEvent : function(target, type, capture){
      },
      unregisterEvent : function(target, type, capture){
      }
    },
    defer : function(statics){

      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "Iframe source could not be set!",b = "contentDocument",c = "engine.name",d = "",e = "qx.bom.Iframe",f = "osx",g = "qx.event.handler.Iframe.onevent(this)",h = "os.name",i = "webkit",j = "load",k = "iframe",l = "body";
  qx.Class.define(e, {
    statics : {
      DEFAULT_ATTRIBUTES : {
        onload : g,
        frameBorder : 0,
        frameSpacing : 0,
        marginWidth : 0,
        marginHeight : 0,
        hspace : 0,
        vspace : 0,
        border : 0,
        allowTransparency : true
      },
      create : function(attributes, win){

        var attributes = attributes ? qx.lang.Object.clone(attributes) : {
        };
        var initValues = qx.bom.Iframe.DEFAULT_ATTRIBUTES;
        for(var key in initValues){

          if(attributes[key] == null){

            attributes[key] = initValues[key];
          };
        };
        return qx.dom.Element.create(k, attributes, win);
      },
      getWindow : function(iframe){

        try{

          return iframe.contentWindow;
        } catch(ex) {

          return null;
        };
      },
      getDocument : function(iframe){

        if(b in iframe){

          try{

            return iframe.contentDocument;
          } catch(ex) {

            return null;
          };
        };
        try{

          var win = this.getWindow(iframe);
          return win ? win.document : null;
        } catch(ex) {

          return null;
        };
      },
      getBody : function(iframe){

        try{

          var doc = this.getDocument(iframe);
          return doc ? doc.getElementsByTagName(l)[0] : null;
        } catch(ex) {

          return null;
        };
      },
      setSource : function(iframe, source){

        try{

          if(this.getWindow(iframe) && qx.dom.Hierarchy.isRendered(iframe)){

            try{

              if((qx.core.Environment.get(c) == i) && qx.core.Environment.get(h) == f){

                var contentWindow = this.getWindow(iframe);
                if(contentWindow){

                  contentWindow.stop();
                };
              };
              this.getWindow(iframe).location.replace(source);
            } catch(ex) {

              iframe.src = source;
            };
          } else {

            iframe.src = source;
          };
          this.__mt(iframe);
        } catch(ex) {

          qx.log.Logger.warn(a);
        };
      },
      queryCurrentUrl : function(iframe){

        var doc = this.getDocument(iframe);
        try{

          if(doc && doc.location){

            return doc.location.href;
          };
        } catch(ex) {
        };
        return d;
      },
      __mt : function(iframe){

        var callback = function(){

          qx.bom.Event.removeNativeListener(iframe, j, callback);
          iframe.$$url = qx.bom.Iframe.queryCurrentUrl(iframe);
        };
        qx.bom.Event.addNativeListener(iframe, j, callback);
      }
    }
  });
})();
(function(){

  var a = "request",b = "qx.bom.IframeHistory",c = "engine.name",d = "can't initialize iframe",f = "",g = "mshtml",h = "interval",i = "state",j = '<html><body><div id="state">',k = "-1000px",l = "browser.version",m = "hidden",n = "qx.blankpage",o = "undefined",p = '</div></body></html>',q = "absolute";
  qx.Class.define(b, {
    extend : qx.bom.History,
    construct : function(){

      qx.bom.History.call(this);
      this.__my();
    },
    members : {
      __ml : null,
      __mm : false,
      __mu : null,
      __mv : null,
      __mw : null,
      _setInitialState : function(){

        qx.bom.History.prototype._setInitialState.call(this);
        this.__mw = this._getHash();
      },
      _setHash : function(value){

        qx.bom.History.prototype._setHash.call(this, value);
        this.__mw = this._encode(value);
      },
      addToHistory : function(state, newTitle){

        if(!qx.lang.Type.isString(state)){

          state = state + f;
        };
        if(qx.lang.Type.isString(newTitle)){

          this.setTitle(newTitle);
          this._titles[state] = newTitle;
        };
        if(this.getState() !== state){

          this.setState(state);
        };
        this.fireDataEvent(a, state);
      },
      _onHistoryLoad : function(state){

        this._setState(state);
        this.fireDataEvent(a, state);
        if(this._titles[state] != null){

          this.setTitle(this._titles[state]);
        };
      },
      _setState : function(state){

        this.__mv = true;
        this.setState(state);
        this.__mv = false;
      },
      _applyState : function(value, old){

        if(this.__mv){

          return;
        };
        this._writeState(value);
      },
      _readState : function(){

        if(!this.__mm){

          return this._decode(this._getHash());
        };
        var doc = this.__ml.contentWindow.document;
        var elem = doc.getElementById(i);
        return elem ? this._decode(elem.innerText) : f;
      },
      _writeState : function(state){

        if(!this.__mm){

          this.__mx();
          this.__mu = qx.event.Timer.once(function(){

            this._writeState(state);
          }, this, 50);
          return;
        };
        this.__mx();
        var state = this._encode(state);
        if(qx.core.Environment.get(c) == g && qx.core.Environment.get(l) != 8){

          this._setHash(state);
        };
        var doc = this.__ml.contentWindow.document;
        doc.open();
        doc.write(j + state + p);
        doc.close();
      },
      __mx : function(){

        if(this.__mu){

          this.__mu.stop();
          this.__mu.dispose();
        };
      },
      __my : function(){

        this.__mn(function(){

          qx.event.Idle.getInstance().addListener(h, this.__ms, this);
        });
      },
      __ms : function(e){

        var currentState = null;
        var locationState = this._getHash();
        if(!this.__mA(locationState)){

          currentState = this.__mz(locationState);
        } else {

          currentState = this._readState();
        };
        if(qx.lang.Type.isString(currentState) && currentState != this.getState()){

          this._onHistoryLoad(currentState);
        };
      },
      __mz : function(locationState){

        locationState = this._decode(locationState);
        this._writeState(locationState);
        return locationState;
      },
      __mA : function(locationState){

        return qx.lang.Type.isString(locationState) && locationState == this.__mw;
      },
      __mn : function(handler){

        this.__ml = this.__mo();
        document.body.appendChild(this.__ml);
        this.__mp(function(){

          this._writeState(this.getState());
          if(handler){

            handler.call(this);
          };
        }, this);
      },
      __mo : function(){

        var iframe = qx.bom.Iframe.create({
          src : qx.util.ResourceManager.getInstance().toUri(qx.core.Environment.get(n))
        });
        iframe.style.visibility = m;
        iframe.style.position = q;
        iframe.style.left = k;
        iframe.style.top = k;
        return iframe;
      },
      __mp : function(callback, context, retry){

        if(typeof retry === o){

          retry = 0;
        };
        if(!this.__ml.contentWindow || !this.__ml.contentWindow.document){

          if(retry > 20){

            throw new Error(d);
          };
          qx.event.Timer.once(function(){

            this.__mp(callback, context, ++retry);
          }, this, 10);
          return;
        };
        this.__mm = true;
        callback.call(context || window);
      }
    },
    destruct : function(){

      this.__ml = null;
      if(this.__mu){

        this.__mu.dispose();
        this.__mu = null;
      };
      qx.event.Idle.getInstance().removeListener(h, this.__ms, this);
    }
  });
})();
(function(){

  var a = "qx.bom.NativeHistory",b = "hashchange",c = "engine.name",d = "interval";
  qx.Class.define(a, {
    extend : qx.bom.History,
    construct : function(){

      qx.bom.History.call(this);
      this.__mq();
    },
    members : {
      __mk : null,
      __mq : function(){

        if(qx.bom.History.SUPPORTS_HASH_CHANGE_EVENT){

          var boundFunc = qx.lang.Function.bind(this.__ms, this);
          this.__mk = qx.event.GlobalError.observeMethod(boundFunc);
          qx.bom.Event.addNativeListener(window, b, this.__mk);
        } else {

          qx.event.Idle.getInstance().addListener(d, this.__ms, this);
        };
      },
      __mr : function(){

        if(qx.bom.History.SUPPORTS_HASH_CHANGE_EVENT){

          qx.bom.Event.removeNativeListener(window, b, this.__mk);
        } else {

          qx.event.Idle.getInstance().removeListener(d, this.__ms, this);
        };
      },
      __ms : function(){

        var currentState = this._readState();
        if(qx.lang.Type.isString(currentState) && currentState != this.getState()){

          this._onHistoryLoad(currentState);
        };
      },
      _readState : function(){

        return this._decode(this._getHash());
      },
      _writeState : qx.core.Environment.select(c, {
        "opera" : function(state){

          qx.event.Timer.once(function(){

            this._setHash(this._encode(state));
          }, this, 0);
        },
        "default" : function(state){

          this._setHash(this._encode(state));
        }
      })
    },
    destruct : function(){

      this.__mr();
    }
  });
})();
(function(){

  var a = "qx.bom.client.PhoneGap",b = "cordova",c = "phonegap.notification",d = "notification",e = "Cordova",f = "PhoneGap",g = "phonegap";
  qx.Bootstrap.define(a, {
    statics : {
      getPhoneGap : function(){

        return (b in window || e in window || f in window);
      },
      getNotification : function(){

        return d in navigator;
      }
    },
    defer : function(statics){

      qx.core.Environment.add(g, statics.getPhoneGap);
      qx.core.Environment.add(c, statics.getNotification);
    }
  });
})();
(function(){

  var a = "qx.mobile.nativescroll",b = "minWidth",c = "width",d = "qx.ui.mobile.core.MResize",e = "height",f = "px",g = "minHeight",h = "auto",i = "Boolean";
  qx.Mixin.define(d, {
    properties : {
      fireDomUpdatedOnResize : {
        check : i,
        init : false
      }
    },
    members : {
      __mB : null,
      __mC : null,
      releaseFixedSize : function(){

        var parent = this.getLayoutParent();
        if(parent && parent.getContainerElement()){

          var element = this.getContainerElement();
          qx.bom.element.Style.set(element, e, h);
          qx.bom.element.Style.set(element, c, h);
        };
      },
      fixSize : function(){

        var parent = this.getLayoutParent();
        if(parent && parent.getContainerElement()){

          var height = parent.getContainerElement().offsetHeight;
          var width = parent.getContainerElement().offsetWidth;
          if(height === 0 || width === 0){

            return;
          };
          if(!this.getFireDomUpdatedOnResize()){

            this._setHeight(height);
            this._setWidth(width);
          } else if(this.__mB != height && this.__mC != width){

            this._setHeight(height);
            this._setWidth(width);
            this.__mC = width;
            this.__mB = height;
            this._domUpdated();
          };
        };
      },
      _setHeight : function(height){

        var element = this.getContainerElement();
        if(qx.core.Environment.get(a)){

          qx.bom.element.Style.set(element, g, height + f);
        } else {

          qx.bom.element.Style.set(element, e, height + f);
        };
      },
      _setWidth : function(width){

        var element = this.getContainerElement();
        if(qx.core.Environment.get(a)){

          qx.bom.element.Style.set(element, b, width + f);
        } else {

          qx.bom.element.Style.set(element, c, width + f);
        };
      }
    }
  });
})();
(function(){

  var a = "os.name",b = "start",c = "qx.event.type.Data",d = "page",e = "stop",f = "resume",g = "wait",h = "android",i = "deviceready",j = "back",k = "initialize",l = "menubutton",m = "qx.event.type.Event",n = "pause",o = "phonegap",p = "qx.ui.mobile.page.Page",q = "menu",r = "backbutton";
  qx.Class.define(p, {
    extend : qx.ui.mobile.container.Composite,
    include : qx.ui.mobile.core.MResize,
    construct : function(layout){

      qx.ui.mobile.container.Composite.call(this, layout || new qx.ui.mobile.layout.VBox());
    },
    statics : {
      _currentPage : null,
      _onDeviceReady : function(){

        qx.bom.Event.addNativeListener(document, r, qx.ui.mobile.page.Page._onBackButton);
        qx.bom.Event.addNativeListener(document, l, qx.ui.mobile.page.Page._onMenuButton);
      },
      _onBackButton : function(){

        if(qx.core.Environment.get(o) && qx.core.Environment.get(a) == h){

          var exit = true;
          if(qx.ui.mobile.page.Page._currentPage){

            exit = qx.ui.mobile.page.Page._currentPage.back(true);
          };
          if(exit){

            navigator.app.exitApp();
          };
        };
      },
      _onMenuButton : function(){

        if(qx.core.Environment.get(o) && qx.core.Environment.get(a) == h){

          if(qx.ui.mobile.page.Page._currentPage){

            qx.ui.mobile.page.Page._currentPage.menu();
          };
        };
      }
    },
    events : {
      "initialize" : m,
      "start" : m,
      "stop" : m,
      "pause" : m,
      "resume" : m,
      "back" : c,
      "menu" : m,
      "wait" : m
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : d
      }
    },
    members : {
      __kq : false,
      show : function(properties){

        qx.ui.mobile.page.Page._currentPage = this;
        this.initialize();
        this.start();
        qx.ui.mobile.container.Composite.prototype.show.call(this, properties);
      },
      exclude : function(properties){

        this.stop();
        qx.ui.mobile.container.Composite.prototype.exclude.call(this, properties);
      },
      back : function(triggeredByKeyEvent){

        this.fireDataEvent(j, triggeredByKeyEvent);
        var value = this._back(triggeredByKeyEvent);
        return value || false;
      },
      _back : function(triggeredByKeyEvent){
      },
      menu : function(){

        this.fireEvent(q);
      },
      initialize : function(){

        if(!this.isInitialized()){

          this._initialize();
          this.__kq = true;
          this.fireEvent(k);
        };
      },
      _initialize : function(){
      },
      isInitialized : function(){

        return this.__kq;
      },
      start : function(){

        this._start();
        this.fireEvent(b);
      },
      _start : function(){
      },
      stop : function(){

        this._stop();
        this.fireEvent(e);
      },
      _stop : function(){
      },
      pause : function(){

        this._pause();
        this.fireEvent(n);
      },
      _pause : function(){
      },
      resume : function(){

        this._resume();
        this.fireEvent(f);
      },
      _resume : function(){
      },
      wait : function(){

        this._wait();
        this.fireEvent(g);
      },
      _wait : function(){
      }
    },
    defer : function(statics){

      if(qx.core.Environment.get(o) && qx.core.Environment.get(a) == h){

        qx.bom.Event.addNativeListener(document, i, statics._onDeviceReady);
      };
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.container.INavigation";
  qx.Interface.define(a, {
    members : {
      getTitleWidget : function(){
      },
      getLeftContainer : function(){
      },
      getRightContainer : function(){
      }
    }
  });
})();
(function(){

  var a = "Boolean",b = "content",c = "right-container",d = "_applyContentCssClass",e = "action",f = "__mE",g = "__mI",h = "qx.event.type.Event",i = "_applyShowBackButton",j = "group",k = "String",l = "",m = "changeTitle",n = "_applyShowButton",o = "__mD",p = "left-container",q = "Number",r = "_applyActionButtonIcon",s = "_applyBackButtonText",t = "__mJ",u = "__mH",v = "tap",w = "qx.ui.mobile.page.NavigationPage",z = "_applyActionButtonText",A = "_applyTitle";
  qx.Class.define(w, {
    extend : qx.ui.mobile.page.Page,
    implement : qx.ui.mobile.container.INavigation,
    construct : function(wrapContentByGroup, layout){

      qx.ui.mobile.page.Page.call(this);
      if(wrapContentByGroup != null){

        this._wrapContentByGroup = wrapContentByGroup;
      };
    },
    events : {
      action : h
    },
    properties : {
      title : {
        check : k,
        init : l,
        event : m,
        apply : A
      },
      backButtonText : {
        check : k,
        init : l,
        apply : s
      },
      buttonText : {
        check : k,
        init : l,
        apply : z
      },
      buttonIcon : {
        check : k,
        init : null,
        nullable : true,
        apply : r
      },
      showBackButton : {
        check : a,
        init : false,
        apply : i
      },
      showBackButtonOnTablet : {
        check : a,
        init : false
      },
      showButton : {
        check : a,
        init : false,
        apply : n
      },
      navigationBarHidden : {
        check : a,
        init : false
      },
      navigationBarToggleDuration : {
        check : q,
        init : 0.8
      },
      contentCssClass : {
        check : k,
        init : b,
        nullable : true,
        apply : d
      }
    },
    members : {
      _isTablet : false,
      _wrapContentByGroup : true,
      __mD : null,
      __mE : null,
      __mF : null,
      __mG : null,
      __mH : null,
      __mI : null,
      __mJ : null,
      getTitleWidget : function(){

        if(!this.__mH){

          this.__mH = this._createTitleWidget();
        };
        return this.__mH;
      },
      _createTitleWidget : function(){

        return new qx.ui.mobile.navigationbar.Title(this.getTitle());
      },
      _applyTitle : function(value, old){

        if(this.__mH){

          this.__mH.setValue(value);
        };
      },
      getLeftContainer : function(){

        if(!this.__mI){

          this.__mI = this._createLeftContainer();
        };
        return this.__mI;
      },
      getRightContainer : function(){

        if(!this.__mJ){

          this.__mJ = this._createRightContainer();
        };
        return this.__mJ;
      },
      _createLeftContainer : function(){

        var layout = new qx.ui.mobile.layout.HBox();
        var container = new qx.ui.mobile.container.Composite(layout);
        container.addCssClass(p);
        this.__mD = this._createBackButton();
        this.__mD.addListener(v, this._onBackButtonTap, this);
        this._showBackButton();
        container.add(this.__mD);
        return container;
      },
      _createRightContainer : function(){

        var layout = new qx.ui.mobile.layout.HBox();
        var container = new qx.ui.mobile.container.Composite(layout);
        container.addCssClass(c);
        this.__mE = this._createButton();
        this.__mE.addListener(v, this._onButtonTap, this);
        this._showButton();
        container.add(this.__mE);
        return container;
      },
      _createBackButton : function(){

        return new qx.ui.mobile.navigationbar.BackButton(this.getBackButtonText());
      },
      _createButton : function(){

        return new qx.ui.mobile.navigationbar.Button(this.getButtonText(), this.getButtonIcon());
      },
      scrollTo : function(x, y, time){

        this.__mG.scrollTo(x, y, time);
      },
      scrollToWidget : function(widget, time){

        if(widget){

          this.__mG.scrollToElement(widget.getId(), time);
        };
      },
      getContent : function(){

        return this.__mF;
      },
      _getBackButton : function(){

        return this.__mD;
      },
      _getButton : function(){

        return this.__mE;
      },
      setIsTablet : function(isTablet){

        this._isTablet = isTablet;
      },
      isTablet : function(){

        return this._isTablet;
      },
      _getScrollContainer : function(){

        return this.__mG;
      },
      addAfterNavigationBar : function(widget){

        if(widget && this.__mG){

          this.addBefore(widget, this.__mG);
        };
      },
      _applyBackButtonText : function(value, old){

        if(this.__mD){

          this.__mD.setValue(value);
        };
      },
      _applyActionButtonText : function(value, old){

        if(this.__mE){

          this.__mE.setValue(value);
        };
      },
      _applyActionButtonIcon : function(value, old){

        if(this.__mE){

          this.__mE.setIcon(value);
        };
      },
      _applyShowBackButton : function(value, old){

        this._showBackButton();
      },
      _applyShowButton : function(value, old){

        this._showButton();
      },
      _applyContentCssClass : function(value, old){

        if(this.__mF){

          this.__mF.setDefaultCssClass(value);
        };
      },
      _showBackButton : function(){

        if(this.__mD){

          if(this.getShowBackButton()){

            this.__mD.show();
          } else {

            this.__mD.exclude();
          };
        };
      },
      _showButton : function(){

        if(this.__mE){

          if(this.getShowButton()){

            this.__mE.show();
          } else {

            this.__mE.exclude();
          };
        };
      },
      _initialize : function(){

        qx.ui.mobile.page.Page.prototype._initialize.call(this);
        this.__mG = this._createScrollContainer();
        this.__mF = this._createContent();
        if(this.__mF){

          this.__mG.add(this.__mF, {
            flex : 1
          });
        };
        if(this.__mG){

          this.add(this.__mG, {
            flex : 1
          });
        };
      },
      _createScrollContainer : function(){

        return new qx.ui.mobile.container.Scroll();
      },
      _createContent : function(){

        var content = new qx.ui.mobile.container.Composite();
        content.setDefaultCssClass(this.getContentCssClass());
        if(this._wrapContentByGroup == true){

          content.addCssClass(j);
        };
        return content;
      },
      _onBackButtonTap : function(evt){

        this.back();
      },
      _onButtonTap : function(evt){

        this.fireEvent(e);
      }
    },
    destruct : function(){

      this._disposeObjects(g, t, o, f, u);
      this.__mI = this.__mJ = this.__mD = this.__mE = null;
      this.__mH = this.__mF = this.__mG = null;
      this._isTablet = null;
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.navigationbar.Title",b = "title",c = "h1";
  qx.Class.define(a, {
    extend : qx.ui.mobile.basic.Label,
    properties : {
      wrap : {
        refine : true,
        init : false
      },
      defaultCssClass : {
        refine : true,
        init : b
      }
    },
    members : {
      _getTagName : function(){

        return c;
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.navigationbar.Button",b = "navigationbar-button";
  qx.Class.define(a, {
    extend : qx.ui.mobile.form.Button,
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.navigationbar.BackButton",b = "navigationbar-backbutton";
  qx.Class.define(a, {
    extend : qx.ui.mobile.navigationbar.Button,
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      }
    }
  });
})();
(function(){

  var a = "input",b = "orientationchange",c = "qx.mobile.nativescroll",d = 'TEXTAREA',f = "qx/mobile/js/iscroll.min.js",g = 'INPUT',h = "resize",j = 'SELECT',k = "#",l = "iscroll",m = "scrollbar",n = "load",o = "domupdated",p = "GET",q = "div",r = "qx.ui.mobile.container.MIScroll",s = "pageEnd",t = "undefined",u = "textarea",v = 'LABEL',w = 'iscrollstart';
  qx.Mixin.define(r, {
    construct : function(){

      this.__mL();
      this.__mN();
    },
    members : {
      __mK : null,
      _createScrollElement : function(){

        var scroll = qx.dom.Element.create(q);
        qx.bom.element.Class.add(scroll, l);
        return scroll;
      },
      _getScrollContentElement : function(){

        return this.getContainerElement().childNodes[0];
      },
      _scrollTo : function(x, y, time){

        if(this.__mK){

          this.__mK.scrollTo(x, y, time);
        };
      },
      _scrollToElement : function(elementId, time){

        if(typeof time === t){

          time = 0;
        };
        if(this.__mK){

          this.__mK.scrollToElement(k + elementId, time);
        };
      },
      __mL : function(){

        if(!window.iScroll){

          {

            var resource = f;
          };
          var path = qx.util.ResourceManager.getInstance().toUri(resource);
          {
          };
          var loader = new qx.bom.request.Script();
          loader.on(n, this.__mP, this);
          loader.open(p, path);
          loader.send();
        } else {

          this._setScroll(this.__mM());
        };
      },
      __mM : function(){

        var defaultScrollProperties = this._getDefaultScrollProperties();
        var customScrollProperties = {
        };
        if(this._scrollProperties != null){

          customScrollProperties = this._scrollProperties;
        };
        var iScrollProperties = qx.lang.Object.mergeWith(defaultScrollProperties, customScrollProperties, true);
        return new iScroll(this.getContainerElement(), iScrollProperties);
      },
      _getDefaultScrollProperties : function(){

        var container = this;
        return {
          hideScrollbar : true,
          fadeScrollbar : true,
          hScrollbar : false,
          scrollbarClass : m,
          useTransform : true,
          onScrollEnd : function(){

            if(qx.core.Environment.get(c) == false){

              if(this.y == this.maxScrollY){

                container.fireEvent(s);
              };
            };
          },
          onScrollMove : function(){

            if(qx.core.Environment.get(c) == false){

              if(this.y == this.maxScrollY){

                container.fireEvent(s);
              };
            };
          },
          onBeforeScrollStart : function(e){

            var target = e.target;
            while(target.nodeType != 1){

              target = target.parentNode;
            };
            if(target.tagName != j && target.tagName != g && target.tagName != d && target.tagName != v){

              var elements = [];
              var inputElements = qx.lang.Array.cast(document.getElementsByTagName(a), Array);
              var textAreaElements = qx.lang.Array.cast(document.getElementsByTagName(u), Array);
              elements = elements.concat(inputElements);
              elements = elements.concat(textAreaElements);
              for(var i = 0,length = elements.length;i < length;i++){

                elements[i].blur();
              };
              e.preventDefault();
            };
            if(qx.core.Environment.get(c) == false){

              var iScrollStartEvent = new qx.event.message.Message(w);
              qx.event.message.Bus.getInstance().dispatch(iScrollStartEvent);
            };
          }
        };
      },
      __mN : function(){

        qx.event.Registration.addListener(window, b, this._refresh, this);
        qx.event.Registration.addListener(window, h, this._refresh, this);
        this.addListener(o, this._refresh, this);
      },
      __mO : function(){

        qx.event.Registration.removeListener(window, b, this._refresh, this);
        qx.event.Registration.removeListener(window, h, this._refresh, this);
        this.removeListener(o, this._refresh, this);
      },
      __mP : function(request){

        if(request.status < 400){

          this._setScroll(this.__mM());
        } else {

          {
          };
        };
      },
      _setScroll : function(scroll){

        this.__mK = scroll;
      },
      disable : function(){

        if(this.__mK){

          this.__mK.disable();
        };
      },
      enable : function(){

        if(this.__mK){

          this.__mK.enable();
        };
      },
      _refresh : function(){

        if(this.__mK){

          this.__mK.refresh();
        };
      }
    },
    destruct : function(){

      this.__mO();
      if(this.__mK){

        this.__mK.destroy();
      };
      this.__mK;
    }
  });
})();
(function(){

  var a = "url: ",b = "qx.debug.io",c = "qx.bom.request.Script",d = "Invalid state",e = "head",f = "error",g = "loadend",h = "qx.debug",i = "script",j = "load",k = "Unknown response headers",l = "browser.documentmode",m = "abort",n = "",o = "Received native readyState: loaded",p = "readystatechange",q = "Response header cannot be determined for ",r = "requests made with script transport.",s = "opera",t = "unknown",u = "Open native request with ",v = "Response headers cannot be determined for",w = "mshtml",x = "engine.name",y = "Detected error",z = "Send native request",A = "on",B = "timeout",C = "Unknown environment key at this phase",D = "Received native load";
  qx.Bootstrap.define(c, {
    construct : function(){

      this.__mV();
      this.__mQ = qx.Bootstrap.bind(this._onNativeLoad, this);
      this.__mR = qx.Bootstrap.bind(this._onNativeError, this);
      this.__lD = qx.Bootstrap.bind(this._onTimeout, this);
      this.__mS = document.head || document.getElementsByTagName(e)[0] || document.documentElement;
      this._emitter = new qx.event.Emitter();
      this.timeout = this.__mX() ? 0 : 15000;
    },
    events : {
      "readystatechange" : c,
      "error" : c,
      "loadend" : c,
      "timeout" : c,
      "abort" : c,
      "load" : c
    },
    members : {
      readyState : null,
      status : null,
      statusText : null,
      timeout : null,
      __mT : null,
      on : function(name, listener, ctx){

        this._emitter.on(name, listener, ctx);
        return this;
      },
      open : function(method, url){

        if(this.__lV){

          return;
        };
        this.__mV();
        this.__lG = null;
        this.__lQ = url;
        if(this.__nb(b)){

          qx.Bootstrap.debug(qx.bom.request.Script, u + a + url);
        };
        this._readyStateChange(1);
      },
      setRequestHeader : function(key, value){

        if(this.__lV){

          return null;
        };
        var param = {
        };
        if(this.readyState !== 1){

          throw new Error(d);
        };
        param[key] = value;
        this.__lQ = qx.util.Uri.appendParamsToUrl(this.__lQ, param);
        return this;
      },
      send : function(){

        if(this.__lV){

          return null;
        };
        var script = this.__mY(),head = this.__mS,that = this;
        if(this.timeout > 0){

          this.__iU = window.setTimeout(this.__lD, this.timeout);
        };
        if(this.__nb(b)){

          qx.Bootstrap.debug(qx.bom.request.Script, z);
        };
        head.insertBefore(script, head.firstChild);
        window.setTimeout(function(){

          that._readyStateChange(2);
          that._readyStateChange(3);
        });
        return this;
      },
      abort : function(){

        if(this.__lV){

          return null;
        };
        this.__lG = true;
        this.__na();
        this._emit(m);
        return this;
      },
      _emit : function(event){

        this[A + event]();
        this._emitter.emit(event, this);
      },
      onreadystatechange : function(){
      },
      onload : function(){
      },
      onloadend : function(){
      },
      onerror : function(){
      },
      onabort : function(){
      },
      ontimeout : function(){
      },
      getResponseHeader : function(key){

        if(this.__lV){

          return null;
        };
        if(this.__nb(h)){

          qx.Bootstrap.debug(q + r);
        };
        return t;
      },
      getAllResponseHeaders : function(){

        if(this.__lV){

          return null;
        };
        if(this.__nb(h)){

          qx.Bootstrap.debug(v + r);
        };
        return k;
      },
      setDetermineSuccess : function(check){

        this.__mT = check;
      },
      dispose : function(){

        var script = this.__mU;
        if(!this.__lV){

          if(script){

            script.onload = script.onreadystatechange = null;
            this.__na();
          };
          if(this.__iU){

            window.clearTimeout(this.__iU);
          };
          this.__lV = true;
        };
      },
      isDisposed : function(){

        return !!this.__lV;
      },
      _getUrl : function(){

        return this.__lQ;
      },
      _getScriptElement : function(){

        return this.__mU;
      },
      _onTimeout : function(){

        this.__mW();
        if(!this.__mX()){

          this._emit(f);
        };
        this._emit(B);
        if(!this.__mX()){

          this._emit(g);
        };
      },
      _onNativeLoad : function(){

        var script = this.__mU,determineSuccess = this.__mT,that = this;
        if(this.__lG){

          return;
        };
        if(this.__nb(x) === w && this.__nb(l) < 9){

          if(!(/loaded|complete/).test(script.readyState)){

            return;
          } else {

            if(this.__nb(b)){

              qx.Bootstrap.debug(qx.bom.request.Script, o);
            };
          };
        };
        if(this.__nb(b)){

          qx.Bootstrap.debug(qx.bom.request.Script, D);
        };
        if(determineSuccess){

          if(!this.status){

            this.status = determineSuccess() ? 200 : 500;
          };
        };
        if(this.status === 500){

          if(this.__nb(b)){

            qx.Bootstrap.debug(qx.bom.request.Script, y);
          };
        };
        if(this.__iU){

          window.clearTimeout(this.__iU);
        };
        window.setTimeout(function(){

          that._success();
          that._readyStateChange(4);
          that._emit(j);
          that._emit(g);
        });
      },
      _onNativeError : function(){

        this.__mW();
        this._emit(f);
        this._emit(g);
      },
      __mU : null,
      __mS : null,
      __lQ : n,
      __mQ : null,
      __mR : null,
      __lD : null,
      __iU : null,
      __lG : null,
      __lV : null,
      __mV : function(){

        this.readyState = 0;
        this.status = 0;
        this.statusText = n;
      },
      _readyStateChange : function(readyState){

        this.readyState = readyState;
        this._emit(p);
      },
      _success : function(){

        this.__na();
        this.readyState = 4;
        if(!this.status){

          this.status = 200;
        };
        this.statusText = n + this.status;
      },
      __mW : function(){

        this.__na();
        this.readyState = 4;
        this.status = 0;
        this.statusText = null;
      },
      __mX : function(){

        var isLegacyIe = this.__nb(x) === w && this.__nb(l) < 9;
        var isOpera = this.__nb(x) === s;
        return !(isLegacyIe || isOpera);
      },
      __mY : function(){

        var script = this.__mU = document.createElement(i);
        script.src = this.__lQ;
        script.onerror = this.__mR;
        script.onload = this.__mQ;
        if(this.__nb(x) === w && this.__nb(l) < 9){

          script.onreadystatechange = this.__mQ;
        };
        return script;
      },
      __na : function(){

        var script = this.__mU;
        if(script && script.parentNode){

          this.__mS.removeChild(script);
        };
      },
      __nb : function(key){

        if(qx && qx.core && qx.core.Environment){

          return qx.core.Environment.get(key);
        } else {

          if(key === x){

            return qx.bom.client.Engine.getName();
          };
          if(key === l){

            return qx.bom.client.Browser.getDocumentMode();
          };
          if(key == b){

            return false;
          };
          throw new Error(C);
        };
      }
    },
    defer : function(){

      if(qx && qx.core && qx.core.Environment){

        qx.core.Environment.add(b, false);
      };
    }
  });
})();
(function(){

  var a = "Object",b = "qx.event.message.Message",c = "String";
  qx.Class.define(b, {
    extend : qx.core.Object,
    construct : function(name, data){

      qx.core.Object.call(this);
      if(name != null){

        this.setName(name);
      };
      if(data != null){

        this.setData(data);
      };
    },
    properties : {
      name : {
        check : c
      },
      data : {
        init : null,
        nullable : true
      },
      sender : {
        check : a
      }
    }
  });
})();
(function(){

  var a = "function",b = "qx.event.message.Bus",c = "*",d = "Invalid parameters! ",e = "Object method already subscribed to ",f = "singleton";
  qx.Class.define(b, {
    type : f,
    extend : qx.core.Object,
    statics : {
      getSubscriptions : function(){

        return this.getInstance().getSubscriptions();
      },
      subscribe : function(message, subscriber, context){

        return this.getInstance().subscribe(message, subscriber, context);
      },
      checkSubscription : function(message, subscriber, context){

        return this.getInstance().checkSubscription(message, subscriber, context);
      },
      unsubscribe : function(message, subscriber, context){

        return this.getInstance().unsubscribe(message, subscriber, context);
      },
      dispatch : function(msg){

        return this.getInstance().dispatch.apply(this.getInstance(), arguments);
      },
      dispatchByName : function(name, data){

        return this.getInstance().dispatchByName.apply(this.getInstance(), arguments);
      }
    },
    construct : function(){

      this.__nc = {
      };
    },
    members : {
      __nc : null,
      getSubscriptions : function(){

        return this.__nc;
      },
      subscribe : function(message, subscriber, context){

        if(!message || typeof subscriber != a){

          this.error(d + [message, subscriber, context]);
          return false;
        };
        var sub = this.getSubscriptions();
        if(this.checkSubscription(message)){

          if(this.checkSubscription(message, subscriber, context)){

            this.warn(e + message);
            return false;
          };
          sub[message].push({
            subscriber : subscriber,
            context : context || null
          });
          return true;
        } else {

          sub[message] = [{
            subscriber : subscriber,
            context : context || null
          }];
          return true;
        };
      },
      checkSubscription : function(message, subscriber, context){

        var sub = this.getSubscriptions();
        if(!sub[message] || sub[message].length === 0){

          return false;
        };
        if(subscriber){

          for(var i = 0;i < sub[message].length;i++){

            if(sub[message][i].subscriber === subscriber && sub[message][i].context === (context || null)){

              return true;
            };
          };
          return false;
        };
        return true;
      },
      unsubscribe : function(message, subscriber, context){

        var sub = this.getSubscriptions();
        var subscrList = sub[message];
        if(subscrList){

          if(!subscriber){

            sub[message] = null;
            delete sub[message];
            return true;
          } else {

            if(!context){

              context = null;
            };
            var i = subscrList.length;
            var subscription;
            do {

              subscription = subscrList[--i];
              if(subscription.subscriber === subscriber && subscription.context === context){

                subscrList.splice(i, 1);
                if(subscrList.length === 0){

                  sub[message] = null;
                  delete sub[message];
                };
                return true;
              };
            }while((i));
          };
        };
        return false;
      },
      dispatch : function(msg){

        var sub = this.getSubscriptions();
        var msgName = msg.getName();
        var dispatched = false;
        for(var key in sub){

          var pos = key.indexOf(c);
          if(pos > -1){

            if(pos === 0 || key.substr(0, pos) === msgName.substr(0, pos)){

              this.__nd(sub[key], msg);
              dispatched = true;
            };
          } else {

            if(key === msgName){

              this.__nd(sub[msgName], msg);
              dispatched = true;
            };
          };
        };
        return dispatched;
      },
      dispatchByName : function(name, data){

        var message = new qx.event.message.Message(name, data);
        return this.dispatch(message);
      },
      __nd : function(subscribers, msg){

        for(var i = 0;i < subscribers.length;i++){

          var subscriber = subscribers[i].subscriber;
          var context = subscribers[i].context;
          if(context && context.isDisposed){

            if(context.isDisposed()){

              subscribers.splice(i, 1);
              i--;
            } else {

              subscriber.call(context, msg);
            };
          } else {

            subscriber.call(context, msg);
          };
        };
      }
    }
  });
})();
(function(){

  var a = "os.name",b = "qx.ui.mobile.container.MNativeScroll",c = "touchstart",d = "ios",e = "native",f = "scroll";
  qx.Mixin.define(b, {
    construct : function(){

      this.addCssClass(e);
      if(qx.core.Environment.get(a) == d){

        this.addListener(c, this._onTouchStart, this);
      };
    },
    members : {
      _onTouchStart : function(evt){

        var parentContentElementHeight = this.getLayoutParent().getContentElement().offsetHeight;
        var contentElementHeight = this.getContentElement().scrollHeight;
        if(contentElementHeight > parentContentElementHeight){

          var scrollTop = this.getContentElement().scrollTop;
          var maxScrollTop = contentElementHeight - parentContentElementHeight;
          if(scrollTop === 0){

            this.getContentElement().scrollTop = 1;
          } else if(scrollTop == maxScrollTop){

            this.getContentElement().scrollTop = maxScrollTop - 1;
          };
        } else {

          if(!(evt.getTarget() instanceof qx.ui.mobile.form.Input)){

            evt.preventDefault();
          };
        };
      },
      _createScrollElement : function(){

        return null;
      },
      _getScrollContentElement : function(){

        return null;
      },
      _scrollTo : function(x, y, time){

        this.getContentElement().scrollLeft = x;
        this.getContentElement().scrollTop = y;
      },
      _scrollToElement : function(elementId, time){

        var targetElement = document.getElementById(elementId);
        var offsetParent = qx.bom.element.Location.getOffsetParent(targetElement);
        var location = qx.bom.element.Location.getRelative(offsetParent, targetElement, f, f);
        this._scrollTo(Math.abs(location.left), Math.abs(location.top), time);
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Event",b = "qx.ui.mobile.container.Scroll",c = "scroll",d = "qx.mobile.nativescroll";
  qx.Class.define(b, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(scrollProperties){

      qx.ui.mobile.container.Composite.call(this);
      if(scrollProperties){

        this._scrollProperties = scrollProperties;
      };
    },
    events : {
      pageEnd : a
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : c
      }
    },
    members : {
      _scrollProperties : null,
      _createContainerElement : function(){

        var element = qx.ui.mobile.container.Composite.prototype._createContainerElement.call(this);
        var scrollElement = this._createScrollElement();
        if(scrollElement){

          element.appendChild(scrollElement);
        };
        return element;
      },
      _getContentElement : function(){

        var contentElement = qx.ui.mobile.container.Composite.prototype._getContentElement.call(this);
        var scrollContentElement = this._getScrollContentElement();
        return scrollContentElement || contentElement;
      },
      refresh : function(){

        if(qx.core.Environment.get(d) == false){

          this._refresh();
        };
      },
      scrollTo : function(x, y, time){

        this._scrollTo(x, y, time);
      },
      scrollToElement : function(elementId, time){

        this._scrollToElement(elementId, time);
      }
    },
    defer : function(statics){

      if(qx.core.Environment.get(d) == false){

        qx.Class.include(statics, qx.ui.mobile.container.MIScroll);
      } else {

        qx.Class.include(statics, qx.ui.mobile.container.MNativeScroll);
      };
    }
  });
})();
(function(){

  var a = "/entities/",b = "/instances/",c = "changeSelection",d = "kirra_qooxdoo.EntityNavigator",e = "Main",f = "qx.event.type.Data";
  qx.Class.define(d, {
    extend : qx.ui.mobile.page.NavigationPage,
    construct : function(repository){

      qx.ui.mobile.page.NavigationPage.call(this);
      this.repository = repository;
      this.setTitle(e);
    },
    events : {
      "show" : f
    },
    members : {
      _initialize : function(){

        qx.ui.mobile.page.NavigationPage.prototype._initialize.call(this);
        var me = this;
        var list = this.entityList = new qx.ui.mobile.list.List({
          configureItem : function(item, data, row){

            item.setTitle(data.name);
            item.setSubtitle(data.description);
            item.setShowArrow(true);
            item.data = data;
          }
        });
        this.getContent().add(list);
        list.addListener(c, function(evt){

          var selectedEntity = list.getModel().getItem(evt.getData());
          qx.core.Init.getApplication().getRouting().executeGet(a + selectedEntity.fullName + b);
        }, this);
        this.repository.loadEntities(function(entities){

          var topLevel = [];
          for(var i in entities){

            if(entities[i].topLevel && (entities[i].concrete === undefined || entities[i].concrete) && (entities[i].standalone === undefined || entities[i].standalone))topLevel.push(entities[i]);
          };
          me.entityList.setModel(new qx.data.Array(topLevel));
        });
      },
      _start : function(){

        qx.ui.mobile.page.NavigationPage.prototype._start.call(this);
      }
    }
  });
})();
(function(){

  var a = "changeModel",b = "-",c = "height",d = "qx.data.Array",f = "ul",g = "changeLength",h = "Integer",j = "_applyModel",k = "qx.event.type.Data",l = "",m = "changeLocale",n = "data-selectable",o = "order",p = "qx.ui.mobile.list.List",q = "changeSelection",r = "_applyDelegate",s = "LI",t = "list",u = "changeDelegate",v = "]",w = "tap",x = "qx.dynlocale",y = "[",z = "false",A = "UL",B = "change",C = "changeBubble",D = ".",E = "__ne";
  qx.Class.define(p, {
    extend : qx.ui.mobile.core.Widget,
    construct : function(delegate){

      qx.ui.mobile.core.Widget.call(this);
      this.addListener(w, this._onTap, this);
      this.__ne = new qx.ui.mobile.list.provider.Provider(this);
      if(delegate){

        this.setDelegate(delegate);
      };
      if(qx.core.Environment.get(x)){

        qx.locale.Manager.getInstance().addListener(m, this._onChangeLocale, this);
      };
      this._setLayout(new qx.ui.mobile.layout.VBox());
    },
    events : {
      changeSelection : k
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : t
      },
      delegate : {
        apply : r,
        event : u,
        init : null,
        nullable : true
      },
      model : {
        check : d,
        apply : j,
        event : a,
        nullable : true,
        init : null
      },
      itemCount : {
        check : h,
        init : 0
      }
    },
    members : {
      __ne : null,
      _getTagName : function(){

        return f;
      },
      _onTap : function(evt){

        var element = evt.getOriginalTarget();
        var index = -1;
        if(element.tagName == A){

          return;
        };
        while(element.tagName != s){

          element = element.parentNode;
        };
        if(qx.bom.element.Attribute.get(element, n) != z && qx.dom.Element.hasChild(this.getContainerElement(), element)){

          index = qx.dom.Hierarchy.getElementIndex(element);
        };
        if(index != -1){

          this.fireDataEvent(q, index);
        };
      },
      _applyModel : function(value, old){

        if(old != null){

          old.removeListener(C, this.__nh, this);
        };
        if(value != null){

          value.addListener(C, this.__nh, this);
        };
        if(old != null){

          old.removeListener(B, this.__ng, this);
        };
        if(value != null){

          value.addListener(B, this.__ng, this);
        };
        if(old != null){

          old.removeListener(g, this.__nf, this);
        };
        if(value != null){

          value.addListener(g, this.__nf, this);
        };
        this.__nj();
      },
      _applyDelegate : function(value, old){

        this.__ne.setDelegate(value);
      },
      __nf : function(evt){

        this.__nj();
      },
      _onChangeLocale : qx.core.Environment.select(x, {
        "true" : function(e){

          this.__nj();
        },
        "false" : null
      }),
      __ng : function(evt){

        if(evt && evt.getData() && evt.getData().type == o){

          this.__nj();
        };
      },
      __nh : function(evt){

        if(evt){

          var data = evt.getData();
          var isArray = (qx.lang.Type.isArray(data.old) && qx.lang.Type.isArray(data.value));
          if(!isArray || (isArray && data.old.length == data.value.length)){

            var rows = this._extractRowsToRender(data.name);
            for(var i = 0;i < rows.length;i++){

              this.__ni(rows[i]);
            };
          };
        };
      },
      _extractRowsToRender : function(name){

        var rows = [];
        if(!name){

          return rows;
        };
        var containsPoint = (name.indexOf(D) != -1);
        if(containsPoint){

          var candidate = name.split(D)[0];
          candidate = candidate.replace(y, l);
          candidate = candidate.replace(v, l);
          var isRange = (candidate.indexOf(b) != -1);
          if(isRange){

            var rangeMembers = candidate.split(b);
            var startRange = parseInt(rangeMembers[0], 10);
            var endRange = parseInt(rangeMembers[1], 10);
            for(var i = startRange;i <= endRange;i++){

              rows.push(i);
            };
          } else {

            rows.push(parseInt(candidate.match(/\d+/)[0], 10));
          };
        } else {

          var match = name.match(/\d+/);
          if(match.length == 1){

            rows.push(parseInt(match[0], 10));
          };
        };
        return rows;
      },
      __ni : function(index){

        var model = this.getModel();
        var element = this.getContentElement();
        var itemElement = this.__ne.getItemElement(model.getItem(index), index);
        var oldNode = element.childNodes[index];
        element.replaceChild(itemElement, oldNode);
        this._domUpdated();
      },
      getListItemHeight : function(){

        var listItemHeight = 0;
        if(this.getModel() != null && this.getModel().length > 0){

          var listHeight = qx.bom.element.Style.get(this.getContentElement(), c);
          listItemHeight = parseInt(listHeight) / this.getModel().length;
        };
        return listItemHeight;
      },
      __nj : function(){

        this._setHtml(l);
        var model = this.getModel();
        this.setItemCount(model ? model.getLength() : 0);
        var itemCount = this.getItemCount();
        var element = this.getContentElement();
        for(var index = 0;index < itemCount;index++){

          var itemElement = this.__ne.getItemElement(model.getItem(index), index);
          element.appendChild(itemElement);
        };
        this._domUpdated();
      }
    },
    destruct : function(){

      this._disposeObjects(E);
      if(qx.core.Environment.get(x)){

        qx.locale.Manager.getInstance().removeListener(m, this._onChangeLocale, this);
      };
    }
  });
})();
(function(){

  var a = "_applyDelegate",b = "qx.ui.mobile.list.provider.Provider",c = "__nk",d = "createItemRenderer",e = "changeDelegate";
  qx.Class.define(b, {
    extend : qx.core.Object,
    properties : {
      delegate : {
        event : e,
        init : null,
        nullable : true,
        apply : a
      }
    },
    members : {
      __nk : null,
      _setItemRenderer : function(renderer){

        this.__nk = renderer;
      },
      _getItemRenderer : function(){

        return this.__nk;
      },
      getItemElement : function(data, row){

        this.__nk.reset();
        this._configureItem(data, row);
        return qx.bom.Element.clone(this.__nk.getContainerElement(), true);
      },
      _configureItem : function(data, row){

        var delegate = this.getDelegate();
        if(delegate != null && delegate.configureItem != null){

          delegate.configureItem(this.__nk, data, row);
        };
      },
      _createItemRenderer : function(){

        var createItemRenderer = qx.util.Delegate.getMethod(this.getDelegate(), d);
        var itemRenderer = null;
        if(createItemRenderer == null){

          itemRenderer = new qx.ui.mobile.list.renderer.Default();
        } else {

          itemRenderer = createItemRenderer();
        };
        return itemRenderer;
      },
      _applyDelegate : function(value, old){

        this._setItemRenderer(this._createItemRenderer());
      }
    },
    destruct : function(){

      this._disposeObjects(c);
    }
  });
})();
(function(){

  var a = "qx.util.Delegate";
  qx.Class.define(a, {
    statics : {
      getMethod : function(delegate, specificMethod){

        if(qx.util.Delegate.containsMethod(delegate, specificMethod)){

          return qx.lang.Function.bind(delegate[specificMethod], delegate);
        };
        return null;
      },
      containsMethod : function(delegate, specificMethod){

        var Type = qx.lang.Type;
        if(Type.isObject(delegate)){

          return Type.isFunction(delegate[specificMethod]);
        };
        return false;
      }
    }
  });
})();
(function(){

  var a = "li",b = "_applyAttribute",c = "Boolean",d = "selected",e = "_applySelected",f = "qx.ui.mobile.list.renderer.Abstract",g = "LI",h = "arrow",i = "_applyShowArrow",j = "list-item",k = "abstract";
  qx.Class.define(f, {
    extend : qx.ui.mobile.container.Composite,
    type : k,
    construct : function(layout){

      qx.ui.mobile.container.Composite.call(this, layout);
      this.initSelectable();
      this.initShowArrow();
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : j
      },
      selected : {
        check : c,
        init : false,
        apply : e
      },
      selectable : {
        check : c,
        init : true,
        apply : b
      },
      showArrow : {
        check : c,
        init : false,
        apply : i
      },
      activatable : {
        refine : true,
        init : true
      }
    },
    members : {
      reset : function(){

        {
        };
      },
      _getTagName : function(){

        return a;
      },
      getRowIndexFromEvent : function(evt){

        return this.getRowIndex(evt.getOriginalTarget());
      },
      getRowIndex : function(element){

        while(element.tagName != g){

          element = element.parentNode;
        };
        return qx.dom.Hierarchy.getElementIndex(element);
      },
      _applyShowArrow : function(value, old){

        if(value){

          this.addCssClass(h);
        } else {

          this.removeCssClass(h);
        };
      },
      _applySelected : function(value, old){

        if(value){

          this.addCssClass(d);
        } else {

          this.removeCssClass(d);
        };
      }
    }
  });
})();
(function(){

  var a = "list-itemlabel",b = "",c = "qx.ui.mobile.list.renderer.Default",d = "__nl",e = "__mJ",f = "list-itemimage",g = "__mH",h = "middle",i = "subtitle",j = "__nm";
  qx.Class.define(c, {
    extend : qx.ui.mobile.list.renderer.Abstract,
    construct : function(layout){

      qx.ui.mobile.list.renderer.Abstract.call(this, layout || new qx.ui.mobile.layout.HBox().set({
        alignY : h
      }));
      this._init();
    },
    members : {
      __nl : null,
      __mH : null,
      __nm : null,
      __mJ : null,
      getImageWidget : function(){

        return this.__nl;
      },
      getTitleWidget : function(){

        return this.__mH;
      },
      getSubtitleWidget : function(){

        return this.__nm;
      },
      setImage : function(source){

        this.__nl.setSource(source);
      },
      setTitle : function(title){

        if(title && title.translate){

          this.__mH.setValue(title.translate());
        } else {

          this.__mH.setValue(title);
        };
      },
      setSubtitle : function(subtitle){

        if(subtitle && subtitle.translate){

          this.__nm.setValue(subtitle.translate());
        } else {

          this.__nm.setValue(subtitle);
        };
      },
      _init : function(){

        this.__nl = this._createImage();
        this.add(this.__nl);
        this.__mJ = this._createRightContainer();
        this.add(this.__mJ, {
          flex : 1
        });
        this.__mH = this._createTitle();
        this.__mJ.add(this.__mH);
        this.__nm = this._createSubtitle();
        this.__mJ.add(this.__nm);
      },
      _createRightContainer : function(){

        return new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.VBox());
      },
      _createImage : function(){

        var image = new qx.ui.mobile.basic.Image();
        image.setAnonymous(true);
        image.addCssClass(f);
        return image;
      },
      _createTitle : function(){

        var title = new qx.ui.mobile.basic.Label();
        title.setWrap(false);
        title.addCssClass(a);
        return title;
      },
      _createSubtitle : function(){

        var subtitle = new qx.ui.mobile.basic.Label();
        subtitle.setWrap(false);
        subtitle.addCssClass(i);
        return subtitle;
      },
      reset : function(){

        this.__nl.setSource(null);
        this.__mH.setValue(b);
        this.__nm.setValue(b);
      }
    },
    destruct : function(){

      this._disposeObjects(d, g, j, e);
    }
  });
})();
(function(){

  var a = "qx.data.marshal.MEventBubbling",b = "",c = "]",d = "idBubble-",f = "[",g = "changeBubble",h = ".",j = "qx.event.type.Data";
  qx.Mixin.define(a, {
    events : {
      "changeBubble" : j
    },
    members : {
      _applyEventPropagation : function(value, old, name){

        this.fireDataEvent(g, {
          value : value,
          name : name,
          old : old,
          item : this
        });
        this._registerEventChaining(value, old, name);
      },
      _registerEventChaining : function(value, old, name){

        if(old != null && old.getUserData && old.getUserData(d + this.$$hash) != null){

          var listeners = old.getUserData(d + this.$$hash);
          for(var i = 0;i < listeners.length;i++){

            old.removeListenerById(listeners[i]);
          };
          old.setUserData(d + this.$$hash, null);
        };
        if((value instanceof qx.core.Object) && qx.Class.hasMixin(value.constructor, qx.data.marshal.MEventBubbling)){

          var listener = qx.lang.Function.bind(this.__nn, this, name);
          var id = value.addListener(g, listener, this);
          var listeners = value.getUserData(d + this.$$hash);
          if(listeners == null){

            listeners = [];
            value.setUserData(d + this.$$hash, listeners);
          };
          listeners.push(id);
        };
      },
      __nn : function(name, e){

        var data = e.getData();
        var value = data.value;
        var old = data.old;
        if(qx.Class.hasInterface(e.getTarget().constructor, qx.data.IListData)){

          if(data.name.indexOf){

            var dotIndex = data.name.indexOf(h) != -1 ? data.name.indexOf(h) : data.name.length;
            var bracketIndex = data.name.indexOf(f) != -1 ? data.name.indexOf(f) : data.name.length;
            if(bracketIndex == 0){

              var newName = name + data.name;
            } else if(dotIndex < bracketIndex){

              var index = data.name.substring(0, dotIndex);
              var rest = data.name.substring(dotIndex + 1, data.name.length);
              if(rest[0] != f){

                rest = h + rest;
              };
              var newName = name + f + index + c + rest;
            } else if(bracketIndex < dotIndex){

              var index = data.name.substring(0, bracketIndex);
              var rest = data.name.substring(bracketIndex, data.name.length);
              var newName = name + f + index + c + rest;
            } else {

              var newName = name + f + data.name + c;
            };;
          } else {

            var newName = name + f + data.name + c;
          };
        } else {

          if(parseInt(name) == name && name !== b){

            name = f + name + c;
          };
          var newName = name + h + data.name;
        };
        this.fireDataEvent(g, {
          value : value,
          name : newName,
          old : old,
          item : data.item || e.getTarget()
        });
      }
    }
  });
})();
(function(){

  var a = "-",b = "add",c = "order",d = "add/remove",e = "Boolean",f = "",g = "change",h = "qx.data.Array",j = "Type of the parameter not supported!",k = "0-",l = "remove",m = "0",n = "number",o = "changeBubble",p = "changeLength",q = "qx.event.type.Data";
  qx.Class.define(h, {
    extend : qx.core.Object,
    include : qx.data.marshal.MEventBubbling,
    implement : [qx.data.IListData],
    construct : function(param){

      qx.core.Object.call(this);
      if(param == undefined){

        this.__no = [];
      } else if(arguments.length > 1){

        this.__no = [];
        for(var i = 0;i < arguments.length;i++){

          this.__no.push(arguments[i]);
        };
      } else if(typeof param == n){

        this.__no = new Array(param);
      } else if(param instanceof Array){

        this.__no = qx.lang.Array.clone(param);
      } else {

        this.__no = [];
        this.dispose();
        throw new Error(j);
      };;;
      for(var i = 0;i < this.__no.length;i++){

        this._applyEventPropagation(this.__no[i], null, i);
      };
      this.__np();
      {
      };
    },
    properties : {
      autoDisposeItems : {
        check : e,
        init : false
      }
    },
    events : {
      "change" : q,
      "changeLength" : q
    },
    members : {
      __no : null,
      concat : function(array){

        if(array){

          var newArray = this.__no.concat(array);
        } else {

          var newArray = this.__no.concat();
        };
        return new qx.data.Array(newArray);
      },
      join : function(connector){

        return this.__no.join(connector);
      },
      pop : function(){

        var item = this.__no.pop();
        this.__np();
        this._registerEventChaining(null, item, this.length - 1);
        this.fireDataEvent(o, {
          value : [],
          name : this.length + f,
          old : [item],
          item : this
        });
        this.fireDataEvent(g, {
          start : this.length - 1,
          end : this.length - 1,
          type : l,
          removed : [item],
          added : []
        }, null);
        return item;
      },
      push : function(varargs){

        for(var i = 0;i < arguments.length;i++){

          this.__no.push(arguments[i]);
          this.__np();
          this._registerEventChaining(arguments[i], null, this.length - 1);
          this.fireDataEvent(o, {
            value : [arguments[i]],
            name : (this.length - 1) + f,
            old : [],
            item : this
          });
          this.fireDataEvent(g, {
            start : this.length - 1,
            end : this.length - 1,
            type : b,
            added : [arguments[i]],
            removed : []
          }, null);
        };
        return this.length;
      },
      reverse : function(){

        if(this.length == 0){

          return;
        };
        var oldArray = this.__no.concat();
        this.__no.reverse();
        this.__nq(0, this.length);
        this.fireDataEvent(g, {
          start : 0,
          end : this.length - 1,
          type : c,
          added : [],
          removed : []
        }, null);
        this.fireDataEvent(o, {
          value : this.__no,
          name : k + (this.__no.length - 1),
          old : oldArray,
          item : this
        });
      },
      shift : function(){

        if(this.length == 0){

          return;
        };
        var item = this.__no.shift();
        this.__np();
        this._registerEventChaining(null, item, this.length - 1);
        this.__nq(0, this.length);
        this.fireDataEvent(o, {
          value : [],
          name : m,
          old : [item],
          item : this
        });
        this.fireDataEvent(g, {
          start : 0,
          end : this.length - 1,
          type : l,
          removed : [item],
          added : []
        }, null);
        return item;
      },
      slice : function(from, to){

        return new qx.data.Array(this.__no.slice(from, to));
      },
      splice : function(startIndex, amount, varargs){

        var oldLength = this.__no.length;
        var returnArray = this.__no.splice.apply(this.__no, arguments);
        if(this.__no.length != oldLength){

          this.__np();
        } else if(amount == arguments.length - 2){

          var addedItems = qx.lang.Array.fromArguments(arguments, 2);
          for(var i = 0;i < addedItems.length;i++){

            if(addedItems[i] !== returnArray[i]){

              break;
            };
            if(i == addedItems.length - 1){

              return new qx.data.Array();
            };
          };
        };
        var removed = amount > 0;
        var added = arguments.length > 2;
        if(removed || added){

          var addedItems = qx.lang.Array.fromArguments(arguments, 2);
          if(returnArray.length == 0){

            var type = b;
            var end = startIndex + addedItems.length;
          } else if(addedItems.length == 0){

            var type = l;
            var end = this.length - 1;
          } else {

            var type = d;
            var end = startIndex + Math.abs(addedItems.length - returnArray.length);
          };
          this.fireDataEvent(g, {
            start : startIndex,
            end : end,
            type : type,
            added : addedItems,
            removed : returnArray
          }, null);
        };
        for(var i = 0;i < returnArray.length;i++){

          this._registerEventChaining(null, returnArray[i], i);
        };
        for(var i = 2;i < arguments.length;i++){

          this._registerEventChaining(arguments[i], null, startIndex + (i - 2));
        };
        this.__nq(startIndex + (arguments.length - 2) - amount, this.length);
        var value = [];
        for(var i = 2;i < arguments.length;i++){

          value[i - 2] = arguments[i];
        };
        var endIndex = (startIndex + Math.max(arguments.length - 3, amount - 1));
        var name = startIndex == endIndex ? endIndex : startIndex + a + endIndex;
        this.fireDataEvent(o, {
          value : value,
          name : name + f,
          old : returnArray,
          item : this
        });
        return (new qx.data.Array(returnArray));
      },
      sort : function(func){

        if(this.length == 0){

          return;
        };
        var oldArray = this.__no.concat();
        this.__no.sort.apply(this.__no, arguments);
        if(qx.lang.Array.equals(this.__no, oldArray) === true){

          return;
        };
        this.__nq(0, this.length);
        this.fireDataEvent(g, {
          start : 0,
          end : this.length - 1,
          type : c,
          added : [],
          removed : []
        }, null);
        this.fireDataEvent(o, {
          value : this.__no,
          name : k + (this.length - 1),
          old : oldArray,
          item : this
        });
      },
      unshift : function(varargs){

        for(var i = arguments.length - 1;i >= 0;i--){

          this.__no.unshift(arguments[i]);
          this.__np();
          this.__nq(0, this.length);
          this.fireDataEvent(o, {
            value : [this.__no[0]],
            name : m,
            old : [this.__no[1]],
            item : this
          });
          this.fireDataEvent(g, {
            start : 0,
            end : this.length - 1,
            type : b,
            added : [arguments[i]],
            removed : []
          }, null);
        };
        return this.length;
      },
      toArray : function(){

        return this.__no;
      },
      getItem : function(index){

        return this.__no[index];
      },
      setItem : function(index, item){

        var oldItem = this.__no[index];
        if(oldItem === item){

          return;
        };
        this.__no[index] = item;
        this._registerEventChaining(item, oldItem, index);
        if(this.length != this.__no.length){

          this.__np();
        };
        this.fireDataEvent(o, {
          value : [item],
          name : index + f,
          old : [oldItem],
          item : this
        });
        this.fireDataEvent(g, {
          start : index,
          end : index,
          type : d,
          added : [item],
          removed : [oldItem]
        }, null);
      },
      getLength : function(){

        return this.length;
      },
      indexOf : function(item){

        return this.__no.indexOf(item);
      },
      lastIndexOf : function(item){

        return this.__no.lastIndexOf(item);
      },
      toString : function(){

        if(this.__no != null){

          return this.__no.toString();
        };
        return f;
      },
      contains : function(item){

        return this.__no.indexOf(item) !== -1;
      },
      copy : function(){

        return this.concat();
      },
      insertAt : function(index, item){

        this.splice(index, 0, item).dispose();
      },
      insertBefore : function(before, item){

        var index = this.indexOf(before);
        if(index == -1){

          this.push(item);
        } else {

          this.splice(index, 0, item).dispose();
        };
      },
      insertAfter : function(after, item){

        var index = this.indexOf(after);
        if(index == -1 || index == (this.length - 1)){

          this.push(item);
        } else {

          this.splice(index + 1, 0, item).dispose();
        };
      },
      removeAt : function(index){

        var returnArray = this.splice(index, 1);
        var item = returnArray.getItem(0);
        returnArray.dispose();
        return item;
      },
      removeAll : function(){

        for(var i = 0;i < this.__no.length;i++){

          this._registerEventChaining(null, this.__no[i], i);
        };
        if(this.getLength() == 0){

          return [];
        };
        var oldLength = this.getLength();
        var items = this.__no.concat();
        this.__no.length = 0;
        this.__np();
        this.fireDataEvent(o, {
          value : [],
          name : k + (oldLength - 1),
          old : items,
          item : this
        });
        this.fireDataEvent(g, {
          start : 0,
          end : oldLength - 1,
          type : l,
          removed : items,
          added : []
        }, null);
        return items;
      },
      append : function(array){

        if(array instanceof qx.data.Array){

          array = array.toArray();
        };
        {
        };
        Array.prototype.push.apply(this.__no, array);
        for(var i = 0;i < array.length;i++){

          this._registerEventChaining(array[i], null, this.__no.length + i);
        };
        var oldLength = this.length;
        this.__np();
        var name = oldLength == (this.length - 1) ? oldLength : oldLength + a + (this.length - 1);
        this.fireDataEvent(o, {
          value : array,
          name : name + f,
          old : [],
          item : this
        });
        this.fireDataEvent(g, {
          start : oldLength,
          end : this.length - 1,
          type : b,
          added : array,
          removed : []
        }, null);
      },
      remove : function(item){

        var index = this.indexOf(item);
        if(index != -1){

          this.splice(index, 1).dispose();
          return item;
        };
      },
      equals : function(array){

        if(this.length !== array.length){

          return false;
        };
        for(var i = 0;i < this.length;i++){

          if(this.getItem(i) !== array.getItem(i)){

            return false;
          };
        };
        return true;
      },
      sum : function(){

        var result = 0;
        for(var i = 0;i < this.length;i++){

          result += this.getItem(i);
        };
        return result;
      },
      max : function(){

        var result = this.getItem(0);
        for(var i = 1;i < this.length;i++){

          if(this.getItem(i) > result){

            result = this.getItem(i);
          };
        };
        return result === undefined ? null : result;
      },
      min : function(){

        var result = this.getItem(0);
        for(var i = 1;i < this.length;i++){

          if(this.getItem(i) < result){

            result = this.getItem(i);
          };
        };
        return result === undefined ? null : result;
      },
      forEach : function(callback, context){

        for(var i = 0;i < this.__no.length;i++){

          callback.call(context, this.__no[i], i, this);
        };
      },
      filter : function(callback, self){

        return new qx.data.Array(this.__no.filter(callback, self));
      },
      map : function(callback, self){

        return new qx.data.Array(this.__no.map(callback, self));
      },
      some : function(callback, self){

        return this.__no.some(callback, self);
      },
      every : function(callback, self){

        return this.__no.every(callback, self);
      },
      reduce : function(callback, initValue){

        return this.__no.reduce(callback, initValue);
      },
      reduceRight : function(callback, initValue){

        return this.__no.reduceRight(callback, initValue);
      },
      __np : function(){

        var oldLength = this.length;
        this.length = this.__no.length;
        this.fireDataEvent(p, this.length, oldLength);
      },
      __nq : function(from, to){

        for(var i = from;i < to;i++){

          this._registerEventChaining(this.__no[i], this.__no[i], i);
        };
      }
    },
    destruct : function(){

      for(var i = 0;i < this.__no.length;i++){

        var item = this.__no[i];
        this._applyEventPropagation(null, item, i);
        if(this.isAutoDisposeItems() && item && item instanceof qx.core.Object){

          item.dispose();
        };
      };
      this.__no = null;
    }
  });
})();
(function(){

  var a = "kirra_qooxdoo.AbstractInstanceNavigator",b = "",c = 'Date',d = "Implement this",f = "Back",g = "implement this",h = "No records found",i = ": ",j = ", ",k = "qx.event.type.Data";
  qx.Class.define(a, {
    extend : qx.ui.mobile.page.NavigationPage,
    construct : function(repository){

      qx.ui.mobile.page.NavigationPage.call(this);
      this.repository = repository;
      this.setShowBackButton(true);
      this.setBackButtonText(f);
    },
    events : {
      "show" : k
    },
    members : {
      _getInstanceListEntity : function(){

        throw Error(g);
      },
      _loadInstances : function(callback){

        throw Error(g);
      },
      _buildInstanceList : function(){

        var me = this;
        this._instanceList = new qx.ui.mobile.list.List({
          configureItem : function(item, data, row){

            item.setTitle(data.shorthand);
            item.setSubtitle(b);
            item.setShowArrow(true);
            item.data = data;
            var details = [],value,detail;
            for(name in me._detailProperties){

              value = data.values[name];
              var entity = me._getInstanceListEntity();
              if(entity.properties[name] && entity.properties[name].typeRef && entity.properties[name].typeRef.typeName === c){

                try{

                  value = kirra_qooxdoo.DateFormats.getYMDFormatter().format(kirra_qooxdoo.DateFormats.getISOFormatter().parse(value));
                } catch(e) {
                };
              } else if(data.links[name] && data.links[name].length > 0){

                value = data.links[name][0].shorthand;
              };
              if(value){

                detail = me._detailProperties[name].label;
                if(value !== true)detail += i + value;
                details.push(detail);
              };
            };
            item.setSubtitle(details.join(j));
            item.setShowArrow(true);
          }
        });
        this.getContent().add(me._instanceList);
        return me._instanceList;
      },
      _start : function(){

        qx.ui.mobile.page.NavigationPage.prototype._start.call(this);
      },
      _back : function(){

        throw Error(d);
      },
      reloadInstances : function(){

        var me = this;
        this._loadInstances(function(instances){

          me.getContent().removeAll();
          if(instances.contents.length === 0){

            var nothingToSeeHere = new qx.ui.mobile.form.Label(h);
            me.getContent().add(nothingToSeeHere);
          } else {

            me._instanceList.setModel(new qx.data.Array(instances.contents));
            me.getContent().add(me._instanceList);
          };
        });
      },
      buildDetailProperties : function(entity){

        var me = this;
        var detailProperties = {
        };
        var skippedMnemonic = false;
        for(var p in entity.properties){

          if(entity.properties[p].userVisible){

            if(skippedMnemonic){

              detailProperties[p] = {
                label : entity.properties[p].label
              };
            };
            skippedMnemonic = true;
          };
        };
        for(var r in entity.relationships){

          if(!entity.relationships[r].multiple){

            detailProperties[r] = {
              label : entity.relationships[r].label
            };
          };
        };
        me._detailProperties = detailProperties;
      }
    }
  });
})();
(function(){

  var a = "kirra_qooxdoo.DateFormats",b = "yyyy/MM/dd";
  qx.Class.define(a, {
    extend : qx.core.Object,
    statics : {
      _ymdFormatter : new qx.util.format.DateFormat(b),
      _isoFormatter : new qx.util.format.DateFormat(qx.util.format.DateFormat.ISO_MASKS.isoUtcDateTime),
      getYMDFormatter : function(){

        var me = this;
        me._ymdFormatter.__lf = true;
        return {
          format : function(date){

            return me._ymdFormatter.format(date);
          },
          parse : function(dateString){

            var parsed = me._ymdFormatter.parse(dateString);
            return parsed;
          }
        };
      },
      getISOFormatter : function(){

        var me = this;
        return {
          format : function(date){

            return me._isoFormatter.format(date);
          },
          parse : function(dateString){

            var parsed = new Date(Date.parse(dateString));
            return parsed;
          }
        };
      }
    }
  });
})();
(function(){

  var a = "changeEnabled",b = "_applyWrap",c = "Boolean",d = "qx.ui.mobile.form.Label",e = "no-wrap",f = "label",g = "_applyValue",h = "boxAlignCenter",i = "changeValue",j = "for";
  qx.Class.define(d, {
    extend : qx.ui.mobile.core.Widget,
    construct : function(value){

      qx.ui.mobile.core.Widget.call(this);
      if(value){

        this.setValue(value);
      };
      this.addCssClass(h);
      this._setLayout(new qx.ui.mobile.layout.HBox());
      this.initWrap();
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : f
      },
      value : {
        nullable : true,
        init : null,
        apply : g,
        event : i
      },
      anonymous : {
        refine : true,
        init : false
      },
      wrap : {
        check : c,
        init : true,
        apply : b
      }
    },
    members : {
      __nr : null,
      _getTagName : function(){

        return f;
      },
      _applyValue : function(value, old){

        this._setHtml(value);
      },
      _applyWrap : function(value, old){

        if(value){

          this.removeCssClass(e);
        } else {

          this.addCssClass(e);
        };
      },
      _changeEnabled : function(evt){

        if(evt){

          this.setEnabled(evt.getData());
        };
      },
      setLabelFor : function(elementId){

        if(this.__nr){

          this.__nr.removeListener(a, this._changeEnabled, this);
        };
        this.__nr = qx.ui.mobile.core.Widget.getWidgetById(elementId);
        if(this.__nr){

          this.__nr.addListener(a, this._changeEnabled, this);
          this.setEnabled(this.__nr.getEnabled());
        };
        this._setAttribute(j, elementId);
      }
    },
    destruct : function(){

      if(this.__nr){

        this.__nr.removeListener(a, this._changeEnabled, this);
        this.__nr = null;
      };
    }
  });
})();
(function(){

  var a = "/entities/",b = "/instances/",c = "Action",d = "New",e = "kirra_qooxdoo.InstanceNavigator",f = "Back",g = "changeSelection",h = "tap",j = "/instances/_template",k = "/",l = "/actions/",m = "qx.event.type.Data";
  qx.Class.define(e, {
    extend : kirra_qooxdoo.AbstractInstanceNavigator,
    construct : function(repository){

      kirra_qooxdoo.AbstractInstanceNavigator.call(this);
      this.repository = repository;
      this.setShowBackButton(true);
      this.setBackButtonText(f);
    },
    events : {
      "show" : m
    },
    members : {
      _entityName : null,
      _entity : null,
      _toolbar : null,
      _instanceList : null,
      _initialize : function(){

        kirra_qooxdoo.AbstractInstanceNavigator.prototype._initialize.call(this);
        var me = this;
        var list = this._buildInstanceList();
        list.addListener(g, function(evt){

          var instanceSelected = me._instanceList.getModel().getItem(evt.getData());
          qx.core.Init.getApplication().getRouting().executeGet(a + me._entityName + b + instanceSelected.objectId);
        }, this);
      },
      _back : function(){

        qx.core.Init.getApplication().goBack(k);
      },
      showFor : function(entityName){

        var me = this;
        this._entityName = entityName;
        me.show();
        this.repository.loadEntity(this._entityName, function(entity){

          me._entity = entity;
          me.setTitle(me._entity.label);
          me.buildDetailProperties(me._entity);
          me.buildActions();
          me.reloadInstances();
        });
      },
      _loadInstances : function(callback){

        this.repository.loadInstances(this._entityName, callback);
      },
      _getInstanceListEntity : function(){

        return this._entity;
      },
      buildActions : function(){

        var me = this;
        if(this._toolbar)this._toolbar.destroy();
        var toolbar = this._toolbar = new qx.ui.mobile.toolbar.ToolBar();
        this.add(toolbar);
        if(this._entity.instantiable){

          var actionButton = new qx.ui.mobile.form.Button(d);
          actionButton.addListener(h, function(){

            qx.core.Init.getApplication().getRouting().executeGet(a + me._entityName + j);
          });
          toolbar.add(actionButton);
        };
        var allOps = this._entity.operations;
        var staticActions = [];
        for(var opName in allOps){

          var operation = allOps[opName];
          if(!operation.instanceOperation && operation.kind === c){

            staticActions.push(operation);
          };
        };
        if(staticActions.length == 0)return;
        for(var i in staticActions){

          var actionButton = new qx.ui.mobile.form.Button(staticActions[i].label);
          actionButton.addListener(h, function(){

            if(staticActions[i].parameters.length === 0){

              me.repository.sendStaticAction(me._entity, staticActions[i], {
              }, function(){

                me.reloadInstances();
              });
            } else {

              qx.core.Init.getApplication().getRouting().executeGet(a + me._entityName + l + staticActions[i].name);
            };
          });
          toolbar.add(actionButton);
        };
      }
    }
  });
})();
(function(){

  var a = "middle",b = "qx.ui.mobile.toolbar.ToolBar",c = "touchstart",d = "toolbar";
  qx.Class.define(b, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(layout){

      qx.ui.mobile.container.Composite.call(this, layout);
      if(!layout){

        this.setLayout(new qx.ui.mobile.layout.HBox().set({
          alignY : a
        }));
      };
      this.addListener(c, qx.bom.Event.preventDefault, this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : d
      }
    },
    members : {
      __ns : false,
      add : function(child, layoutProperties){

        if(!(child instanceof qx.ui.mobile.toolbar.Separator)){

          layoutProperties = layoutProperties ? layoutProperties : {
          };
          qx.lang.Object.mergeWith(layoutProperties, {
            flex : 1
          }, false);
        };
        qx.ui.mobile.container.Composite.prototype.add.call(this, child, layoutProperties);
      }
    },
    destruct : function(){

      this.removeListener(c, qx.bom.Event.preventDefault, this);
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.toolbar.Separator",b = "toolbar-separator";
  qx.Class.define(a, {
    extend : qx.ui.mobile.core.Widget,
    construct : function(){

      qx.ui.mobile.core.Widget.call(this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      }
    }
  });
})();
(function(){

  var a = "/entities/",b = 'Entity',c = "/instances/",d = "Action",f = "Save",g = 'Date',h = "index",j = '_template',k = "/relationships/",l = "Back",m = "Related",n = "...",o = "Are you sure?",p = "Yes",q = "tap",r = "/actions/",s = "Delete",t = "No",u = "kirra_qooxdoo.InstanceForm",v = "qx.event.type.Data";
  qx.Class.define(u, {
    extend : qx.ui.mobile.page.NavigationPage,
    construct : function(repository){

      qx.ui.mobile.page.NavigationPage.call(this);
      this.repository = repository;
      this.setShowBackButton(true);
      this.setBackButtonText(l);
    },
    events : {
      "show" : v
    },
    members : {
      _entityName : null,
      _objectId : null,
      _instance : null,
      _entity : null,
      _widgets : {
      },
      _relationshipTabs : {
      },
      _relationshipMenuButton : null,
      _actions : [],
      _relationshipDomains : null,
      _backPath : null,
      _showRelationships : false,
      _initialize : function(){

        qx.ui.mobile.page.NavigationPage.prototype._initialize.call(this);
      },
      _start : function(){

        qx.ui.mobile.page.NavigationPage.prototype._start.call(this);
      },
      _back : function(){

        qx.core.Init.getApplication().goBack(this._backPath);
      },
      showFor : function(entityName, id){

        var me = this;
        me._widgets = {
        } , me._entityName = entityName;
        me._objectId = id;
        me._entity = null;
        me._instance = null;
        me._relationshipDomains = {
        };
        me._relationshipTabs = {
        } , me._showRelationships = true;
        me._backPath = a + entityName + c;
        me.repository.loadEntity(entityName, function(entity){

          me._entity = entity;
          me.buildForm();
          me.repository.loadInstance(entity, me._objectId, me.instanceLoader());
        });
        this.show();
      },
      showForRelated : function(parentEntityName, parentObjectId, relationshipName, childObjectId){

        var me = this;
        me._widgets = {
        } , me._entityName = null;
        me._objectId = null;
        me._entity = null;
        me._instance = null;
        me._relationshipDomains = {
        };
        me._relationshipTabs = {
        } , me._showRelationships = true;
        me._backPath = a + parentEntityName + c + parentObjectId + k + relationshipName;
        me.repository.loadEntity(parentEntityName, function(parentEntity){

          var relationship = parentEntity.relationships[relationshipName];
          me._entityName = relationship.typeRef.fullName;
          me._objectId = childObjectId;
          me.repository.loadEntity(relationship.typeRef.fullName, function(childEntity){

            me._entity = childEntity;
            me.buildForm();
            me.repository.loadInstance(childEntity, me._objectId, me.instanceLoader());
          });
        });
        this.show();
      },
      instanceLoaded : function(instance){

        var me = this;
        me._instance = instance;
        me.populateForm();
      },
      instanceLoader : function(){

        var me = this;
        return function(instance){

          me.instanceLoaded(instance);
        };
      },
      populateForm : function(){

        var me = this;
        if(!this._instance || !this._entity){

          return;
        };
        for(var propertyName in this._instance.values){

          var property = this._entity.properties[propertyName];
          var value = this._instance.values[propertyName];
          var widget = this._widgets[propertyName];
          if(widget){

            if(!me.isNewInstance() || property.editable){

              if(property.typeRef && property.typeRef.typeName === g){

                try{

                  value = kirra_qooxdoo.DateFormats.getYMDFormatter().format(kirra_qooxdoo.DateFormats.getISOFormatter().parse(value));
                } catch(e) {

                  console.log(e);
                };
              };
              widget.setValue(value);
              widget.setVisible && widget.setVisible(true);
            } else {

              widget.setVisible && widget.setVisible(false);
            };
          };
        };
        for(var relationshipName in this._entity.relationships){

          var relationship = this._entity.relationships[relationshipName];
          var widget = this._widgets[relationshipName];
          if(widget){

            var links = this._instance.links[relationshipName];
            if(!me.isNewInstance() || relationship.editable){

              widget.setValue(links && links.length && links[0].shorthand);
              widget.setVisible && widget.setVisible(true);
            } else {

              widget.setVisible && widget.setVisible(false);
            };
          };
        };
        me.buildActions();
      },
      buildActions : function(){

        var me = this;
        this.addToolbar();
        if(me.isNewInstance()){

          return;
        };
        var actionMenuButton = this.actionMenuButton = new qx.ui.mobile.form.Button(n);
        var actionMenu = this.actionMenu = new qx.ui.mobile.dialog.Menu(new qx.data.Array([]), actionMenuButton);
        var menuItems = [];
        this._actions = [];
        var firstLevelItems = [];
        for(var actionName in this._entity.operations){

          var operation = this._entity.operations[actionName];
          if(operation.instanceOperation && operation.kind === d && this._instance.disabledActions[actionName] === undefined)this._actions.push(operation);
        };
        var overflowStartsAt = this._actions.length > 2 ? 1 : Infinity;
        for(var i in this._actions){

          var action = this._actions[i];
          if(i >= overflowStartsAt){

            menuItems.push(action.label);
          } else {

            firstLevelItems.push(action.label);
          };
        };
        for(var i in firstLevelItems){

          var actionButton = new qx.ui.mobile.form.Button(firstLevelItems[i]);
          actionButton.addListener(q, function(e){

            var selected = me.getOperationByLabel(e.getTarget().getLabel());
            me.invokeAction(selected);
          });
          this.toolbar.add(actionButton);
        };
        this.actionMenu.hide();
        this.actionMenu.setItems(new qx.data.Array(menuItems));
        if(menuItems.length > 0){

          actionMenuButton.addListener(q, function(e){

            actionMenu.show();
          }, this);
          actionMenu.addListener(q, function(e){

            var selected = me._actions[actionMenu.getSelectedIndex() + firstLevelItems.length];
            me.invokeAction(selected);
          }, this);
          this.toolbar.add(this.actionMenuButton);
        };
      },
      invokeAction : function(action){

        var me = this;
        if(action.parameters.length == 0){

          me.repository.sendAction(me._entity, me._objectId, action, {
          }, me.instanceLoader());
        } else {

          qx.core.Init.getApplication().getRouting().executeGet(a + me._entityName + c + me._objectId + r + action.name);
        };
      },
      getOperationByLabel : function(operationLabel){

        for(var i in this._entity.operations){

          var op = this._entity.operations[i];
          if(op.label === operationLabel){

            return op;
          };
        };
      },
      buildForm : function(){

        var me = this;
        if(!this.getContent() || !this._entity){

          return;
        };
        this.getContent().removeAll();
        if(me._relationshipMenuButton){

          me._relationshipMenuButton.destroy();
        };
        var form = this.form = new qx.ui.mobile.form.Form();
        for(var i in this._entity.properties){

          if(!me.isNewInstance() || this._entity.properties[i].editable){

            this.buildWidgetFor(form, this._entity.properties[i]);
          };
        };
        var multiRelationships = this._multiRelationships = [];
        for(var i in this._entity.relationships){

          var relationship = this._entity.relationships[i];
          if(relationship.navigable){

            if(relationship.multiple){

              multiRelationships.push(relationship);
            } else {

              this.buildWidgetFor(form, relationship);
            };
          };
        };
        var instanceFormRenderer = new qx.ui.mobile.form.renderer.Single(form);
        if(this._showRelationships){

          this.buildMultiRelationshipViews(instanceFormRenderer);
        };
        this.getContent().add(instanceFormRenderer);
        this.show();
      },
      buildMultiRelationshipViews : function(instanceFormRenderer){

        var me = this;
        var multiRelationships = this._multiRelationships;
        if(multiRelationships.length === 0){

          return;
        };
        var relationshipMenuButton = me._relationshipMenuButton = new qx.ui.mobile.navigationbar.Button(m);
        var relationshipMenuItems = [];
        for(var i in multiRelationships){

          var relationship = multiRelationships[i];
          relationshipMenuItems.push(relationship.label);
        };
        var relationshipMenu = new qx.ui.mobile.dialog.Menu(new qx.data.Array(relationshipMenuItems), relationshipMenuButton);
        relationshipMenuButton.addListener(q, function(){

          relationshipMenu.show();
        }, this);
        relationshipMenu.addListener(q, function(){

          var selected = me._multiRelationships[relationshipMenu.getSelectedIndex()];
          qx.core.Init.getApplication().getRouting().executeGet(a + me._entityName + c + me._objectId + k + selected.name);
        }, this);
        this.getRightContainer().add(relationshipMenuButton);
      },
      isNewInstance : function(){

        return this._objectId === j;
      },
      addToolbar : function(){

        if(this.toolbar)this.toolbar.destroy();
        var toolbar = this.toolbar = new qx.ui.mobile.toolbar.ToolBar();
        this.add(toolbar);
        this.addBasicButtons();
      },
      addBasicButtons : function(){

        var me = this;
        var saveButton = new qx.ui.mobile.form.Button(f);
        saveButton.addListener(q, function(){

          me.saveInstance();
        });
        this.toolbar.add(saveButton);
        if(!me.isNewInstance()){

          var deleteButton = new qx.ui.mobile.form.Button(s);
          var deleteConfirmationPopup = me.buildConfirmation(deleteButton, function(){

            me.repository.deleteInstance(me._instance, function(){

              me._back();
            });
          });
          deleteButton.addListener(q, function(){

            deleteConfirmationPopup.show();
          });
          this.toolbar.add(deleteButton);
        };
      },
      buildConfirmation : function(anchorWidget, toDo){

        var popupWidget = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.VBox());
        popupWidget.add(new qx.ui.mobile.basic.Label(o));
        var buttonsWidget = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.HBox());
        var okButton = new qx.ui.mobile.form.Button(p);
        var cancelButton = new qx.ui.mobile.form.Button(t);
        buttonsWidget.add(okButton, {
          flex : 1
        });
        buttonsWidget.add(cancelButton, {
          flex : 1
        });
        popupWidget.add(buttonsWidget);
        var anchorPopup;
        okButton.addListener(q, function(){

          anchorPopup.hide();
          toDo();
        }, this);
        cancelButton.addListener(q, function(){

          anchorPopup.hide();
        }, this);
        return anchorPopup = new qx.ui.mobile.dialog.Popup(popupWidget, anchorWidget);
      },
      saveInstance : function(){

        var me = this;
        for(var i in this._entity.properties){

          if(me._entity.properties[i].editable){

            me._instance.values[i] = me._widgets[i].getValue();
          };
        };
        for(var i in this._entity.relationships){

          if(me._entity.relationships[i].editable && !me._entity.relationships[i].multiple && me._relationshipDomains[i]){

            var referenceIndex = me._widgets[i]._getAttribute(h);
            var related = me._relationshipDomains[i][referenceIndex];
            me._instance.links[i] = [related];
          };
        };
        me.repository.saveInstance(me._entity, me._instance, function(created){

          qx.core.Init.getApplication().getRouting().executeGet(a + me._entityName + c + created.objectId);
        });
      },
      buildWidgetFor : function(form, property){

        var widget = kirra_qooxdoo.Widgets.createWidget(property, this.instanceEnumerator(property));
        this._widgets[property.name] = widget;
        if(widget.setRequired)widget.setRequired(property.required === true);
        if(widget.setReadOnly)widget.setReadOnly(this.isNewInstance() ? (property.initializable === false) : (property.editable === false));
        if(widget.setEnabled)widget.setEnabled(this.isNewInstance() ? (property.initializable === true) : (property.editable === true));
        form.add(widget, property.label);
      },
      instanceEnumerator : function(property){

        var me = this;
        var kind = property.typeRef && property.typeRef.kind;
        if(kind !== b){

          return me.noOpValueEnumerator;
        };
        return function(property, callback){

          me.repository.listRelationshipDomain(me._entity, me._objectId, property, function(objects){

            me._relationshipDomains[property.name] = objects.contents;
            var shorthands = [];
            for(var i in objects.contents){

              shorthands.push(objects.contents[i].shorthand);
            };
            callback(shorthands);
          });
        };
      },
      noOpValueEnumerator : function(target, callback){

        callback([]);
      }
    }
  });
})();
(function(){

  var a = "Boolean",b = "height",c = "None",d = "__nu",e = "excluded",f = "_updatePosition",g = "item-unselected",h = "Integer",i = "menu",j = "qx.event.type.Data",k = "menu-scroller",l = "px",m = "visible",n = "__nw",o = "changeSelection",p = "String",q = "__nt",r = "qx.ui.mobile.dialog.Menu",s = "_applyNullable",t = "__nv",u = "_applySelectedIndex",v = "_applyClearButtonLabel",w = "item-selected",x = "tap";
  qx.Class.define(r, {
    extend : qx.ui.mobile.dialog.Popup,
    construct : function(itemsModel, anchor){

      this.__nt = this._createSelectionList();
      if(itemsModel){

        this.__nt.setModel(itemsModel);
      };
      this.__nu = new qx.ui.mobile.container.Composite();
      this.__nv = this._createClearButton();
      this.__nw = this._createListScroller(this.__nt);
      this.__nu.add(this.__nw);
      this.__nu.add(this.__nv);
      qx.ui.mobile.dialog.Popup.call(this, this.__nu, anchor);
      if(anchor){

        this.setModal(false);
      } else {

        this.setModal(true);
      };
    },
    events : {
      changeSelection : j
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : i
      },
      selectedItemClass : {
        init : w
      },
      unselectedItemClass : {
        init : g
      },
      nullable : {
        init : false,
        check : a,
        apply : s
      },
      clearButtonLabel : {
        init : c,
        check : p,
        apply : v
      },
      selectedIndex : {
        check : h,
        apply : u,
        nullable : true
      },
      visibleListItems : {
        check : h,
        apply : f,
        nullable : true
      }
    },
    members : {
      __nt : null,
      __nv : null,
      __nw : null,
      __nu : null,
      show : function(){

        qx.ui.mobile.dialog.Popup.prototype.show.call(this);
        this.scrollToItem(this.getSelectedIndex());
      },
      _createClearButton : function(){

        var clearButton = new qx.ui.mobile.form.Button(this.getClearButtonLabel());
        clearButton.addListener(x, this.__ny, this);
        clearButton.exclude();
        return clearButton;
      },
      _createListScroller : function(selectionList){

        var listScroller = new qx.ui.mobile.container.ScrollComposite();
        listScroller.add(selectionList, {
          flex : 1
        });
        listScroller.addCssClass(k);
        listScroller.setHeight(null);
        listScroller.setPreventEvents(false);
        return listScroller;
      },
      _getListScroller : function(){

        return this.__nw;
      },
      _updatePosition : function(){

        var parentHeight = qx.bom.element.Style.get(qx.ui.mobile.dialog.Popup.ROOT.getContentElement(), b);
        var listScrollerHeight = parseInt(parentHeight, 10) * 0.75;
        if(this.getVisibleListItems() !== null){

          var newListScrollerHeight = this.__nt.getListItemHeight() * this.getVisibleListItems();
          if(newListScrollerHeight < listScrollerHeight){

            listScrollerHeight = newListScrollerHeight;
          };
        };
        this.__nw.setHeight(listScrollerHeight + l);
        qx.ui.mobile.dialog.Popup.prototype._updatePosition.call(this);
      },
      _createSelectionList : function(){

        var self = this;
        var selectionList = new qx.ui.mobile.list.List({
          configureItem : function(item, data, row){

            item.setTitle(data);
            item.setShowArrow(false);
            var isItemSelected = (self.getSelectedIndex() == row);
            if(isItemSelected){

              item.removeCssClass(self.getUnselectedItemClass());
              item.addCssClass(self.getSelectedItemClass());
            } else {

              item.removeCssClass(self.getSelectedItemClass());
              item.addCssClass(self.getUnselectedItemClass());
            };
          }
        });
        selectionList.addListener(o, this.__nx, this);
        selectionList.addListener(x, this._onSelectionListTap, this);
        return selectionList;
      },
      getSelectionList : function(){

        return this.__nt;
      },
      _onSelectionListTap : function(){

        this.hideWithDelay(500);
      },
      setItems : function(itemsModel){

        if(this.__nt){

          this.__nt.setModel(null);
          this.__nt.setModel(itemsModel);
        };
      },
      __nx : function(evt){

        this.setSelectedIndex(evt.getData());
      },
      __ny : function(){

        this.fireDataEvent(o, {
          index : null,
          item : null
        });
        this.hide();
      },
      _applySelectedIndex : function(value, old){

        var listModel = this.__nt.getModel();
        if(listModel !== null){

          var selectedItem = listModel.getItem(value);
          this.fireDataEvent(o, {
            index : value,
            item : selectedItem
          });
        };
        this._render();
      },
      _applyNullable : function(value, old){

        if(value){

          this.__nv.setVisibility(m);
        } else {

          this.__nv.setVisibility(e);
        };
      },
      _applyClearButtonLabel : function(value, old){

        this.__nv.setValue(value);
      },
      _render : function(){

        var tmpModel = this.__nt.getModel();
        this.__nt.setModel(null);
        this.__nt.setModel(tmpModel);
      },
      scrollToItem : function(index){

        var scrollY = 0;
        if(index !== null && this.__nt.getModel() != null){

          var listScrollChild = this.__nw.getScrollContainer();
          var listScrollHeight = listScrollChild.getContainerElement().scrollHeight;
          var listItemHeight = listScrollHeight / this.__nt.getModel().length;
          if(listItemHeight){

            scrollY = index * listItemHeight;
          };
        };
        this.__nw.scrollTo(0, -scrollY);
      }
    },
    destruct : function(){

      this.__nt.removeListener(x, this._onSelectionListTap, this);
      qx.ui.mobile.core.Blocker.getInstance().removeListener(x, this.hide, this);
      this._disposeObjects(q, t, n, d);
    }
  });
})();
(function(){

  var a = "touchmove",b = "qx.ui.mobile.container.ScrollComposite",c = "10rem",d = "Boolean",e = 'px',f = "height",g = "px",h = "scroll-container",j = "changeValue",k = "scroll-bounce-ease",l = "_scrollContainer",m = "bounce-ease",n = "_applyHeight",o = 'auto',p = "input",q = "swipe",r = "maxHeight",s = "String",t = "_updateScrollIndicator",u = "touchend",v = "_applyFixedHeight",w = "appear",x = "touchstart",y = "momentum-ease",z = "scrollable-bottom",A = "scroll-container-child",B = "scrollable-top";
  qx.Class.define(b, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(layout){

      qx.ui.mobile.container.Composite.call(this);
      this.__nz = [0, 0];
      this.__nA = [0, 0];
      this.__nB = [0, 0];
      this._scrollContainer = this._createScrollContainer();
      this.addListener(x, this._onTouchStart, this);
      this.addListener(a, this._onTouchMove, this);
      this.addListener(u, this._onTouchEnd, this);
      this.addListener(q, this._onSwipe, this);
      this._setLayout(new qx.ui.mobile.layout.HBox());
      this._add(this._scrollContainer, {
        flex : 1
      });
      this._updateScrollIndicator(this.__nz[1]);
      this.initHeight();
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : h
      },
      scrollableX : {
        init : false,
        check : d
      },
      scrollableY : {
        init : true,
        check : d
      },
      showScrollIndicator : {
        init : true,
        check : d,
        apply : t
      },
      fixedHeight : {
        init : false,
        check : d,
        apply : v
      },
      height : {
        init : c,
        check : s,
        nullable : true,
        apply : n
      }
    },
    members : {
      _scrollContainer : null,
      __nB : null,
      __nz : null,
      __nA : null,
      __nC : null,
      __nD : null,
      __nE : null,
      __nF : true,
      getScrollContainer : function(){

        return this._scrollContainer;
      },
      _createScrollContainer : function(){

        var scrollContainer = new qx.ui.mobile.container.Composite();
        scrollContainer.setTransformUnit(g);
        scrollContainer.addCssClass(A);
        return scrollContainer;
      },
      _onTouchStart : function(evt){

        this.__nC = (this.getScrollableX() && this.getScrollableY()) ? null : this.getScrollableY();
        this._applyNoEasing();
        this.__nB[0] = evt.getViewportLeft();
        this.__nB[1] = evt.getViewportTop();
        this.__nD = 0;
        this.__nE = 0;
        if(this.__nF === true){

          evt.stopPropagation();
        };
      },
      _onTouchMove : function(evt){

        if(this.isScrollableX()){

          this.__nD = evt.getViewportLeft() - this.__nB[0];
          this.__nG();
          if(Math.abs(this.__nE) < 3 || !this.isScrollableY() || !this.__nC){

            this.__nE = 0;
          };
          this.__nA[0] = Math.floor(this.__nz[0] + this.__nD);
          this._scrollContainer.setTranslateX(this.__nA[0]);
        };
        if(this.isScrollableY()){

          this.__nE = evt.getViewportTop() - this.__nB[1];
          this.__nG();
          if(Math.abs(this.__nD) < 3 || !this.isScrollableX() || this.__nC){

            this.__nD = 0;
          };
          this.__nA[1] = Math.floor(this.__nz[1] + this.__nE);
          this._scrollContainer.setTranslateY(this.__nA[1]);
          this._updateScrollIndicator(this.__nA[1]);
        };
        if(this.__nF === true){

          evt.stopPropagation();
          evt.preventDefault();
        };
      },
      __nG : function(){

        if(this.__nC === null){

          this.__nC = Math.abs(this.__nD / this.__nE) < 2;
        };
      },
      _onTouchEnd : function(evt){

        if(this.__nF === true){

          evt.stopPropagation();
        };
        this.scrollTo(this.__nA[0], this.__nA[1]);
      },
      _updateScrollIndicator : function(positionY){

        var targetElement = this._scrollContainer.getContainerElement();
        var needsScrolling = targetElement.scrollHeight > targetElement.offsetHeight;
        if(this.isScrollableY() && this.isShowScrollIndicator() && needsScrolling){

          var lowerLimit = targetElement.scrollHeight - targetElement.offsetHeight - 4;
          if(positionY >= 0){

            this.removeCssClass(B);
          } else {

            this.addCssClass(B);
          };
          if(positionY < -lowerLimit){

            this.removeCssClass(z);
          } else {

            this.addCssClass(z);
          };
        } else {

          this.removeCssClass(B);
          this.removeCssClass(z);
        };
      },
      _onSwipe : function(evt){

        var velocity = Math.abs(evt.getVelocity());
        var swipeDuration = new Date().getTime() - evt.getStartTime();
        if(this.isScrollableY() && this.__nC && swipeDuration < 500){

          this._applyMomentumEasing();
          this.__nA[1] = this.__nA[1] + (velocity * 1.5 * this.__nE);
        };
        this.scrollTo(this.__nA[0], this.__nA[1]);
      },
      scrollTo : function(positionX, positionY){

        var targetElement = this._scrollContainer.getContainerElement();
        var lowerLimitY = targetElement.scrollHeight - this.getContentElement().clientHeight;
        var lowerLimitX = targetElement.scrollWidth - targetElement.offsetWidth - 4;
        var oldY = this._scrollContainer.getTranslateY();
        if(positionY >= 0){

          if(oldY < 0){

            this._applyScrollBounceEasing();
          } else {

            this._applyBounceEasing();
          };
          positionY = 0;
        };
        if(positionY < -lowerLimitY){

          if(oldY > -lowerLimitY){

            this._applyScrollBounceEasing();
          } else {

            this._applyBounceEasing();
          };
          positionY = -lowerLimitY;
        };
        if(!this.__nC){

          if(positionX >= 0){

            this._applyBounceEasing();
            positionX = 0;
          };
          if(positionX < -lowerLimitX){

            this._applyBounceEasing();
            positionX = -lowerLimitX;
          };
        };
        if(this.isScrollableX()){

          this._scrollContainer.setTranslateX(positionX);
          this.__nz[0] = positionX;
        };
        if(this.isScrollableY()){

          this._scrollContainer.setTranslateY(positionY);
          this.__nz[1] = positionY;
        };
        this._updateScrollIndicator(this.__nz[1]);
      },
      add : function(child, options){

        this._scrollContainer.add(child, options);
        this._handleSize(child);
      },
      addAfter : function(child, after, layoutProperties){

        this._scrollContainer.addAfter(child, after, layoutProperties);
        this._handleSize(child);
      },
      addAt : function(child, index, options){

        this._scrollContainer.addAt(child, index, options);
        this._handleSize(child);
      },
      addBefore : function(child, before, layoutProperties){

        this._scrollContainer.addBefore(child, before, layoutProperties);
        this._handleSize(child);
      },
      getChildren : function(){

        return this._scrollContainer.getChildren();
      },
      getLayout : function(){

        return this._scrollContainer.getLayout();
      },
      setLayout : function(layout){

        this._scrollContainer.setLayout(layout);
      },
      hasChildren : function(){

        return this._scrollContainer.getLayout();
      },
      indexOf : function(child){

        this._scrollContainer.indexOf(child);
      },
      remove : function(child){

        this._unhandleSize(child);
        this._scrollContainer.remove(child);
      },
      removeAll : function(){

        var children = this.getChildren();
        for(var i = 0;i < children.length;i++){

          this._unhandleSize(children[i]);
        };
        this._scrollContainer.removeAll();
      },
      removeAt : function(index){

        var children = this.getChildren();
        this._unhandleSize(children[index]);
        this._scrollContainer.removeAt(index);
      },
      _applyFixedHeight : function(value, old){

        this._applyHeight(this.getHeight());
      },
      _applyHeight : function(value, old){

        var cssProperty = r;
        if(this.getFixedHeight() === true){

          cssProperty = f;
        };
        qx.bom.element.Style.set(this.getContainerElement(), cssProperty, this.getHeight());
      },
      _applyNoEasing : function(){

        this._scrollContainer.removeCssClass(y);
        this._scrollContainer.removeCssClass(m);
        this._scrollContainer.removeCssClass(k);
      },
      _applyMomentumEasing : function(){

        this._applyNoEasing();
        this._scrollContainer.addCssClass(y);
      },
      _applyBounceEasing : function(){

        this._applyNoEasing();
        this._scrollContainer.addCssClass(m);
      },
      _applyScrollBounceEasing : function(){

        this._applyNoEasing();
        this._scrollContainer.addCssClass(k);
      },
      _handleSize : function(child){

        if(child instanceof qx.ui.mobile.form.TextArea){

          child.addListener(w, this._fixChildElementsHeight, child);
          child.addListener(p, this._fixChildElementsHeight, child);
          child.addListener(j, this._fixChildElementsHeight, child);
        };
      },
      _unhandleSize : function(child){

        if(child instanceof qx.ui.mobile.form.TextArea){

          child.removeListener(w, this._fixChildElementsHeight, child);
          child.removeListener(p, this._fixChildElementsHeight, child);
          child.removeListener(j, this._fixChildElementsHeight, child);
        };
      },
      _fixChildElementsHeight : function(evt){

        this.getContainerElement().style.height = o;
        this.getContainerElement().style.height = this.getContainerElement().scrollHeight + e;
      },
      setPreventEvents : function(value){

        this.__nF = value;
      }
    },
    destruct : function(){

      this.removeListener(x, this._onTouchStart, this);
      this.removeListener(a, this._onTouchMove, this);
      this.removeListener(u, this._onTouchEnd, this);
      this.removeListener(q, this._onSwipe, this);
      var children = this.getChildren();
      for(var i = 0;i < children.length;i++){

        this._unhandleSize(children[i]);
      };
      this._disposeObjects(l);
      this.__nC = null;
    }
  });
})();
(function(){

  var a = "qx.ui.form.Form",b = "";
  qx.Class.define(a, {
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__nH = [];
      this._buttons = [];
      this._buttonOptions = [];
      this._validationManager = new qx.ui.form.validation.Manager();
      this._resetter = this._createResetter();
    },
    members : {
      __nH : null,
      _validationManager : null,
      _groupCounter : 0,
      _buttons : null,
      _buttonOptions : null,
      _resetter : null,
      add : function(item, label, validator, name, validatorContext, options){

        if(this.__nI()){

          this.__nH.push({
            title : null,
            items : [],
            labels : [],
            names : [],
            options : [],
            headerOptions : {
            }
          });
        };
        this.__nH[this._groupCounter].items.push(item);
        this.__nH[this._groupCounter].labels.push(label);
        this.__nH[this._groupCounter].options.push(options);
        if(name == null){

          name = label.replace(/\s+|&|-|\+|\*|\/|\||!|\.|,|:|\?|;|~|%|\{|\}|\(|\)|\[|\]|<|>|=|\^|@|\\/g, b);
        };
        this.__nH[this._groupCounter].names.push(name);
        this._validationManager.add(item, validator, validatorContext);
        this._resetter.add(item);
      },
      addGroupHeader : function(title, options){

        if(!this.__nI()){

          this._groupCounter++;
        };
        this.__nH.push({
          title : title,
          items : [],
          labels : [],
          names : [],
          options : [],
          headerOptions : options
        });
      },
      addButton : function(button, options){

        this._buttons.push(button);
        this._buttonOptions.push(options || null);
      },
      __nI : function(){

        return this.__nH.length === 0;
      },
      reset : function(){

        this._resetter.reset();
        this._validationManager.reset();
      },
      redefineResetter : function(){

        this._resetter.redefine();
      },
      redefineResetterItem : function(item){

        this._resetter.redefineItem(item);
      },
      validate : function(){

        return this._validationManager.validate();
      },
      getValidationManager : function(){

        return this._validationManager;
      },
      getGroups : function(){

        return this.__nH;
      },
      getButtons : function(){

        return this._buttons;
      },
      getButtonOptions : function(){

        return this._buttonOptions;
      },
      getItems : function(){

        var items = {
        };
        for(var i = 0;i < this.__nH.length;i++){

          var group = this.__nH[i];
          for(var j = 0;j < group.names.length;j++){

            var name = group.names[j];
            items[name] = group.items[j];
          };
        };
        return items;
      },
      _createResetter : function(){

        return new qx.ui.form.Resetter();
      }
    },
    destruct : function(){

      this.__nH = this._buttons = this._buttonOptions = null;
      this._validationManager.dispose();
      this._resetter.dispose();
    }
  });
})();
(function(){

  var a = "Widgets supporting selection can only be validated ",b = "Added widget not supported.",c = "",d = "in the form validator",f = "changeValid",g = "qx.event.type.Event",h = "complete",j = "value instanceof Function || qx.Class.isSubClassOf(value.constructor, qx.ui.form.validation.AsyncValidator)",k = "qx.ui.form.validation.Manager",l = "This field is required",m = "String",n = "qx.event.type.Data";
  qx.Class.define(k, {
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__nJ = [];
      this.__nK = {
      };
      this.setRequiredFieldMessage(qx.locale.Manager.tr(l));
    },
    events : {
      "changeValid" : n,
      "complete" : g
    },
    properties : {
      validator : {
        check : j,
        init : null,
        nullable : true
      },
      invalidMessage : {
        check : m,
        init : c
      },
      requiredFieldMessage : {
        check : m,
        init : c
      },
      context : {
        nullable : true
      }
    },
    members : {
      __nJ : null,
      __nL : null,
      __nK : null,
      __nM : null,
      add : function(formItem, validator, context){

        if(!this.__nR(formItem)){

          throw new Error(b);
        };
        if(this.__nS(formItem) && !formItem.getValue){

          if(validator != null){

            throw new Error(a + d);
          };
        };
        var dataEntry = {
          item : formItem,
          validator : validator,
          valid : null,
          context : context
        };
        this.__nJ.push(dataEntry);
      },
      remove : function(formItem){

        var items = this.__nJ;
        for(var i = 0,len = items.length;i < len;i++){

          if(formItem === items[i].item){

            items.splice(i, 1);
            return formItem;
          };
        };
        return null;
      },
      getItems : function(){

        var items = [];
        for(var i = 0;i < this.__nJ.length;i++){

          items.push(this.__nJ[i].item);
        };
        return items;
      },
      validate : function(){

        var valid = true;
        this.__nM = true;
        var items = [];
        for(var i = 0;i < this.__nJ.length;i++){

          var formItem = this.__nJ[i].item;
          var validator = this.__nJ[i].validator;
          items.push(formItem);
          if(validator == null){

            var validatorResult = this.__nN(formItem);
            valid = valid && validatorResult;
            this.__nM = validatorResult && this.__nM;
            continue;
          };
          var validatorResult = this.__nO(this.__nJ[i], formItem.getValue());
          valid = validatorResult && valid;
          if(validatorResult != null){

            this.__nM = validatorResult && this.__nM;
          };
        };
        var formValid = this.__nP(items);
        if(qx.lang.Type.isBoolean(formValid)){

          this.__nM = formValid && this.__nM;
        };
        valid = formValid && valid;
        this.__nT(valid);
        if(qx.lang.Object.isEmpty(this.__nK)){

          this.fireEvent(h);
        };
        return valid;
      },
      __nN : function(formItem){

        if(formItem.getRequired()){

          if(this.__nS(formItem)){

            var validatorResult = !!formItem.getSelection()[0];
          } else {

            var value = formItem.getValue();
            var validatorResult = !!value || value === 0;
          };
          formItem.setValid(validatorResult);
          var individualMessage = formItem.getRequiredInvalidMessage();
          var message = individualMessage ? individualMessage : this.getRequiredFieldMessage();
          formItem.setInvalidMessage(message);
          return validatorResult;
        };
        return true;
      },
      __nO : function(dataEntry, value){

        var formItem = dataEntry.item;
        var context = dataEntry.context;
        var validator = dataEntry.validator;
        if(this.__nQ(validator)){

          this.__nK[formItem.toHashCode()] = null;
          validator.validate(formItem, formItem.getValue(), this, context);
          return null;
        };
        var validatorResult = null;
        try{

          var validatorResult = validator.call(context || this, value, formItem);
          if(validatorResult === undefined){

            validatorResult = true;
          };
        } catch(e) {

          if(e instanceof qx.core.ValidationError){

            validatorResult = false;
            if(e.message && e.message != qx.type.BaseError.DEFAULTMESSAGE){

              var invalidMessage = e.message;
            } else {

              var invalidMessage = e.getComment();
            };
            formItem.setInvalidMessage(invalidMessage);
          } else {

            throw e;
          };
        };
        formItem.setValid(validatorResult);
        dataEntry.valid = validatorResult;
        return validatorResult;
      },
      __nP : function(items){

        var formValidator = this.getValidator();
        var context = this.getContext() || this;
        if(formValidator == null){

          return true;
        };
        this.setInvalidMessage(c);
        if(this.__nQ(formValidator)){

          this.__nK[this.toHashCode()] = null;
          formValidator.validateForm(items, this, context);
          return null;
        };
        try{

          var formValid = formValidator.call(context, items, this);
          if(formValid === undefined){

            formValid = true;
          };
        } catch(e) {

          if(e instanceof qx.core.ValidationError){

            formValid = false;
            if(e.message && e.message != qx.type.BaseError.DEFAULTMESSAGE){

              var invalidMessage = e.message;
            } else {

              var invalidMessage = e.getComment();
            };
            this.setInvalidMessage(invalidMessage);
          } else {

            throw e;
          };
        };
        return formValid;
      },
      __nQ : function(validator){

        var async = false;
        if(!qx.lang.Type.isFunction(validator)){

          async = qx.Class.isSubClassOf(validator.constructor, qx.ui.form.validation.AsyncValidator);
        };
        return async;
      },
      __nR : function(formItem){

        var clazz = formItem.constructor;
        return qx.Class.hasInterface(clazz, qx.ui.form.IForm);
      },
      __nS : function(formItem){

        var clazz = formItem.constructor;
        return qx.Class.hasInterface(clazz, qx.ui.core.ISingleSelection);
      },
      __nT : function(value){

        var oldValue = this.__nL;
        this.__nL = value;
        if(oldValue != value){

          this.fireDataEvent(f, value, oldValue);
        };
      },
      getValid : function(){

        return this.__nL;
      },
      isValid : function(){

        return this.getValid();
      },
      getInvalidMessages : function(){

        var messages = [];
        for(var i = 0;i < this.__nJ.length;i++){

          var formItem = this.__nJ[i].item;
          if(!formItem.getValid()){

            messages.push(formItem.getInvalidMessage());
          };
        };
        if(this.getInvalidMessage() != c){

          messages.push(this.getInvalidMessage());
        };
        return messages;
      },
      getInvalidFormItems : function(){

        var res = [];
        for(var i = 0;i < this.__nJ.length;i++){

          var formItem = this.__nJ[i].item;
          if(!formItem.getValid()){

            res.push(formItem);
          };
        };
        return res;
      },
      reset : function(){

        for(var i = 0;i < this.__nJ.length;i++){

          var dataEntry = this.__nJ[i];
          dataEntry.item.setValid(true);
        };
        this.__nL = null;
      },
      setItemValid : function(formItem, valid){

        this.__nK[formItem.toHashCode()] = valid;
        formItem.setValid(valid);
        this.__nU();
      },
      setFormValid : function(valid){

        this.__nK[this.toHashCode()] = valid;
        this.__nU();
      },
      __nU : function(){

        var valid = this.__nM;
        for(var hash in this.__nK){

          var currentResult = this.__nK[hash];
          valid = currentResult && valid;
          if(currentResult == null){

            return;
          };
        };
        this.__nT(valid);
        this.__nK = {
        };
        this.fireEvent(h);
      }
    },
    destruct : function(){

      this.__nJ = null;
    }
  });
})();
(function(){

  var a = "qx.ui.form.validation.AsyncValidator";
  qx.Class.define(a, {
    extend : qx.core.Object,
    construct : function(validator){

      qx.core.Object.call(this);
      this.__nV = validator;
    },
    members : {
      __nV : null,
      __nW : null,
      __dh : null,
      __nX : null,
      validate : function(item, value, manager, context){

        this.__nX = false;
        this.__nW = item;
        this.__dh = manager;
        this.__nV.call(context || this, this, value);
      },
      validateForm : function(items, manager, context){

        this.__nX = true;
        this.__dh = manager;
        this.__nV.call(context, items, this);
      },
      setValid : function(valid, message){

        if(this.__nX){

          if(message !== undefined){

            this.__dh.setInvalidMessage(message);
          };
          this.__dh.setFormValid(valid);
        } else {

          if(message !== undefined){

            this.__nW.setInvalidMessage(message);
          };
          this.__dh.setItemValid(this.__nW, valid);
        };
      }
    },
    destruct : function(){

      this.__dh = this.__nW = null;
    }
  });
})();
(function(){

  var a = "qx.ui.core.ISingleSelection",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeSelection" : b
    },
    members : {
      getSelection : function(){

        return true;
      },
      setSelection : function(items){

        return arguments.length == 1;
      },
      resetSelection : function(){

        return true;
      },
      isSelected : function(item){

        return arguments.length == 1;
      },
      isSelectionEmpty : function(){

        return true;
      },
      getSelectables : function(all){

        return arguments.length == 1;
      }
    }
  });
})();
(function(){

  var a = "The given item has not been added.",b = "qx.ui.form.Resetter",c = " not supported for reseting.",d = "Item ";
  qx.Class.define(b, {
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this.__nY = [];
    },
    members : {
      __nY : null,
      add : function(item){

        if(this._supportsValue(item)){

          var init = item.getValue();
        } else if(this.__nS(item)){

          var init = item.getSelection();
        } else if(this.__oc(item)){

          var init = item.getSelection().concat();
        } else {

          throw new Error(d + item + c);
        };;
        this.__nY.push({
          item : item,
          init : init
        });
      },
      reset : function(){

        for(var i = 0;i < this.__nY.length;i++){

          var dataEntry = this.__nY[i];
          this.__oa(dataEntry.item, dataEntry.init);
        };
      },
      resetItem : function(item){

        var init;
        for(var i = 0;i < this.__nY.length;i++){

          var dataEntry = this.__nY[i];
          if(dataEntry.item === item){

            init = dataEntry.init;
            break;
          };
        };
        if(init === undefined){

          throw new Error(a);
        };
        this.__oa(item, init);
      },
      __oa : function(item, init){

        if(this._supportsValue(item)){

          item.setValue(init);
        } else if(this.__nS(item) || this.__oc(item)){

          item.setSelection(init);
        };
      },
      redefine : function(){

        for(var i = 0;i < this.__nY.length;i++){

          var item = this.__nY[i].item;
          this.__nY[i].init = this.__ob(item);
        };
      },
      redefineItem : function(item){

        var dataEntry;
        for(var i = 0;i < this.__nY.length;i++){

          if(this.__nY[i].item === item){

            dataEntry = this.__nY[i];
            break;
          };
        };
        if(dataEntry === undefined){

          throw new Error(a);
        };
        dataEntry.init = this.__ob(dataEntry.item);
      },
      __ob : function(item){

        if(this._supportsValue(item)){

          return item.getValue();
        } else if(this.__nS(item) || this.__oc(item)){

          return item.getSelection();
        };
      },
      __nS : function(formItem){

        var clazz = formItem.constructor;
        return qx.Class.hasInterface(clazz, qx.ui.core.ISingleSelection);
      },
      __oc : function(formItem){

        var clazz = formItem.constructor;
        return qx.Class.hasInterface(clazz, qx.data.controller.ISelection);
      },
      _supportsValue : function(formItem){

        var clazz = formItem.constructor;
        return (qx.Class.hasInterface(clazz, qx.ui.form.IBooleanForm) || qx.Class.hasInterface(clazz, qx.ui.form.IColorForm) || qx.Class.hasInterface(clazz, qx.ui.form.IDateForm) || qx.Class.hasInterface(clazz, qx.ui.form.INumberForm) || qx.Class.hasInterface(clazz, qx.ui.form.IStringForm));
      }
    },
    destruct : function(){

      this.__nY = null;
    }
  });
})();
(function(){

  var a = "qx.data.controller.ISelection";
  qx.Interface.define(a, {
    members : {
      setSelection : function(value){
      },
      getSelection : function(){
      },
      resetSelection : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.ui.form.IBooleanForm",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeValue" : b
    },
    members : {
      setValue : function(value){

        return arguments.length == 1;
      },
      resetValue : function(){
      },
      getValue : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.ui.form.IColorForm",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeValue" : b
    },
    members : {
      setValue : function(value){

        return arguments.length == 1;
      },
      resetValue : function(){
      },
      getValue : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.ui.form.IDateForm",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeValue" : b
    },
    members : {
      setValue : function(value){

        return arguments.length == 1;
      },
      resetValue : function(){
      },
      getValue : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.ui.form.INumberForm",b = "qx.event.type.Data";
  qx.Interface.define(a, {
    events : {
      "changeValue" : b
    },
    members : {
      setValue : function(value){

        return arguments.length == 1;
      },
      resetValue : function(){
      },
      getValue : function(){
      }
    }
  });
})();
(function(){

  var a = "qx.event.type.Data",b = "qx.ui.form.IStringForm";
  qx.Interface.define(b, {
    events : {
      "changeValue" : a
    },
    members : {
      setValue : function(value){

        return arguments.length == 1;
      },
      resetValue : function(){
      },
      getValue : function(){
      }
    }
  });
})();
(function(){

  var a = 'error ',b = "qx.ui.mobile.form.Form";
  qx.Class.define(b, {
    extend : qx.ui.form.Form,
    construct : function(){

      qx.ui.form.Form.call(this);
      this.__od = [];
    },
    members : {
      __oe : null,
      __od : null,
      _createResetter : function(){

        return new qx.ui.mobile.form.Resetter();
      },
      setRenderer : function(renderer){

        this.__oe = renderer;
      },
      validate : function(){

        var validateResult = qx.ui.form.Form.prototype.validate.call(this);
        this.__od = [];
        if(this.__oe != null){

          this.__oe.resetForm();
        };
        var groups = this.getGroups();
        for(var i = 0;i < groups.length;i++){

          var group = groups[i];
          for(var j = 0;j < group.items.length;j++){

            var item = group.items[j];
            if(!item.isValid()){

              this.__od.push(item);
              if(this.__oe != null){

                this.__oe.showErrorForItem(item);
              } else {

                alert(a + item.getInvalidMessage());
              };
            };
          };
        };
        if(this.__oe != null){

          this.__oe._domUpdated();
        };
        return validateResult;
      },
      showRow : function(groupIndex, rowIndex){

        var item = this._getItemByIndex(groupIndex, rowIndex);
        if(item){

          this.__oe.showItem(item);
        };
      },
      hideRow : function(groupIndex, rowIndex){

        var item = this._getItemByIndex(groupIndex, rowIndex);
        if(item){

          this.__oe.hideItem(item);
        };
      },
      _getItemByIndex : function(groupIndex, rowIndex){

        var groups = this.getGroups();
        var group = groups[groupIndex];
        if(group){

          var item = group.items[rowIndex];
          return item;
        };
        return null;
      },
      getInvalidItems : function(){

        return this.__od;
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.form.Resetter";
  qx.Class.define(a, {
    extend : qx.ui.form.Resetter,
    members : {
      _supportsValue : function(formItem){

        var clazz = formItem.constructor;
        return (qx.ui.form.Resetter.prototype._supportsValue.call(this, formItem) || qx.Class.hasMixin(clazz, qx.ui.mobile.form.MValue));
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.form.NumberField",b = "number-field",c = "_onChangeMaximum",d = "min",e = "_onChangeMinimum",f = "number",g = "_onChangeStep",h = "max",i = '',j = "step",k = "Integer";
  qx.Class.define(a, {
    extend : qx.ui.mobile.form.Input,
    include : [qx.ui.mobile.form.MValue, qx.ui.mobile.form.MText],
    implement : [qx.ui.form.IStringForm],
    construct : function(value){

      qx.ui.mobile.form.Input.call(this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      },
      minimum : {
        check : k,
        init : i,
        apply : e
      },
      maximum : {
        check : k,
        init : i,
        apply : c
      },
      step : {
        check : k,
        init : i,
        apply : g
      }
    },
    members : {
      _getType : function(){

        return f;
      },
      _onChangeStep : function(value, old){

        this._setAttribute(j, value);
      },
      _onChangeMaximum : function(value, old){

        this._setAttribute(h, value);
      },
      _onChangeMinimum : function(value, old){

        this._setAttribute(d, value);
      }
    }
  });
})();
(function(){

  var a = "qx.ui.form.renderer.IFormRenderer";
  qx.Interface.define(a, {
    members : {
      addItems : function(items, names, title, itemsOptions, headerOptions){
      },
      addButton : function(button, options){
      }
    }
  });
})();
(function(){

  var a = "form",b = "qx.ui.mobile.form.renderer.AbstractRenderer",c = "Abstract method call",d = "abstract";
  qx.Class.define(b, {
    type : d,
    extend : qx.ui.mobile.core.Widget,
    implement : qx.ui.form.renderer.IFormRenderer,
    construct : function(form){

      qx.ui.mobile.core.Widget.call(this);
      var groups = form.getGroups();
      for(var i = 0;i < groups.length;i++){

        var group = groups[i];
        this.addItems(group.items, group.labels, group.title, group.options, group.headerOptions);
      };
      var buttons = form.getButtons();
      var buttonOptions = form.getButtonOptions();
      for(var i = 0;i < buttons.length;i++){

        this.addButton(buttons[i], buttonOptions[i]);
      };
      form.setRenderer(this);
      this._form = form;
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : a
      }
    },
    members : {
      _form : null,
      addItems : function(items, names, title){

        throw new Error(c);
      },
      addButton : function(button){

        throw new Error(c);
      },
      showErrorForItem : function(item){

        throw new Error(c);
      },
      resetForm : function(){

        throw new Error(c);
      }
    }
  });
})();
(function(){

  var a = "touchmove",b = "toggleButton",c = "data-label-checked",d = "__of",e = "swipe",f = " should be boolean",g = "__oh",h = "value for ",i = "toggleButtonSwitch",j = 'boolean',k = "qx.ui.mobile.form.ToggleButton",l = "OFF",m = "checked",n = "left",o = "tap",p = "ON",q = "data-label-unchecked",r = "__og";
  qx.Class.define(k, {
    extend : qx.ui.mobile.core.Widget,
    include : [qx.ui.mobile.form.MValue, qx.ui.form.MForm, qx.ui.form.MModelProperty, qx.ui.mobile.form.MState],
    implement : [qx.ui.form.IForm, qx.ui.form.IModel],
    construct : function(value, labelChecked, labelUnchecked){

      qx.ui.mobile.core.Widget.call(this);
      if(labelChecked && labelUnchecked){

        this.__of = labelUnchecked;
        this.__og = labelChecked;
      };
      this._setAttribute(c, this.__og);
      this._setAttribute(q, this.__of);
      this.__oh = this._createSwitch();
      this._add(this.__oh);
      if(value){

        this.setValue(value);
      };
      this.addListener(o, this._onTap, this);
      this.addListener(e, this._onSwipe, this);
      this.addListener(a, this._onTouch, this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      }
    },
    members : {
      __oh : null,
      __oi : false,
      __of : l,
      __og : p,
      __oj : 0,
      _getChild : function(){

        return this.__oh;
      },
      _createSwitch : function(){

        var toggleButtonSwitch = new qx.ui.mobile.container.Composite();
        toggleButtonSwitch.addCssClass(i);
        return toggleButtonSwitch;
      },
      _setValue : function(value){

        if(typeof value !== j){

          throw new Error(h + this + f);
        };
        if(value){

          this.addCssClass(m);
        } else {

          this.removeCssClass(m);
        };
        this.__oi = value;
      },
      _getValue : function(){

        return this.__oi;
      },
      toggle : function(){

        this.setValue(!this.getValue());
      },
      _onTap : function(evt){

        if(this._checkLastTouchTime()){

          this.toggle();
        };
      },
      _onTouch : function(evt){

        evt.stopPropagation();
      },
      _onSwipe : function(evt){

        if(this._checkLastTouchTime()){

          var direction = evt.getDirection();
          if(direction == n){

            if(this.__oi == true){

              this.toggle();
            };
          } else {

            if(this.__oi == false){

              this.toggle();
            };
          };
        };
      },
      _checkLastTouchTime : function(){

        var elapsedTime = new Date().getTime() - this.__oj;
        this.__oj = new Date().getTime();
        return elapsedTime > 500;
      }
    },
    destruct : function(){

      this.removeListener(o, this._onTap, this);
      this.removeListener(e, this._onSwipe, this);
      this._disposeObjects(g, d, r);
    }
  });
})();
(function(){

  var a = "tap",b = "span",c = "checkbox",d = "checked",e = "qx.ui.mobile.form.CheckBox";
  qx.Class.define(e, {
    extend : qx.ui.mobile.form.Input,
    include : [qx.ui.mobile.form.MValue],
    construct : function(value){

      qx.ui.mobile.form.Input.call(this);
      if(typeof value != undefined){

        this._state = value;
      };
      this.addListener(a, this._onTap, this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : c
      }
    },
    members : {
      _state : null,
      _getTagName : function(){

        return b;
      },
      _getType : function(){

        return null;
      },
      _onTap : function(){

        this.setValue(!this.getValue());
      },
      _setValue : function(value){

        if(value == true){

          this.addCssClass(d);
        } else {

          this.removeCssClass(d);
        };
        this._setAttribute(d, value);
        this._state = value;
      },
      _getValue : function(){

        return this._state;
      }
    },
    destruct : function(){

      this.removeListener(a, this._onTap, this);
    }
  });
})();
(function(){

  var a = "text",b = "os.name",c = "keypress",d = "qx.ui.mobile.form.TextField",e = "text-field",f = "android",g = "os.version",h = "touchstart",i = "2";
  qx.Class.define(d, {
    extend : qx.ui.mobile.form.Input,
    include : [qx.ui.mobile.form.MValue, qx.ui.mobile.form.MText],
    implement : [qx.ui.form.IStringForm],
    construct : function(value){

      qx.ui.mobile.form.Input.call(this);
      if(qx.core.Environment.get(b) == f && qx.core.Environment.get(g).charAt(0) == i){

        this.addListener(h, this.focus);
      };
      this.addListener(c, this._onKeyPress, this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : e
      }
    },
    members : {
      _getType : function(){

        return a;
      },
      _onKeyPress : function(evt){

        if(evt.getKeyCode() == 13){

          this.blur();
        };
      }
    },
    destruct : function(){

      if(qx.core.Environment.get(b) == f && qx.core.Environment.get(g).charAt(0) == i){

        this.removeListener(h, this.focus);
      };
      this.removeListener(c, this._onKeyPress, this);
    }
  });
})();
(function(){

  var a = "password",b = "password-field",c = "qx.ui.mobile.form.PasswordField";
  qx.Class.define(c, {
    extend : qx.ui.mobile.form.TextField,
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      }
    },
    members : {
      _getType : function(){

        return a;
      }
    }
  });
})();
(function(){

  var a = "qx.ui.mobile.form.RadioGroup",b = "qx.ui.mobile.form.RadioButton",c = "radio",d = "changeValue",e = "tap",f = "span",g = "_applyGroup",h = "checked",i = "qx.event.type.Data";
  qx.Class.define(b, {
    extend : qx.ui.mobile.form.Input,
    include : [qx.ui.mobile.form.MValue],
    construct : function(value){

      qx.ui.mobile.form.Input.call(this);
      this.addListener(e, this._onTap, this);
    },
    events : {
      changeValue : i
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : c
      },
      group : {
        check : a,
        nullable : true,
        apply : g
      }
    },
    members : {
      _state : null,
      _getTagName : function(){

        return f;
      },
      _getType : function(){

        return null;
      },
      _onTap : function(){

        this.fireDataEvent(d, {
        });
        this.setValue(true);
      },
      _applyGroup : function(value, old){

        if(old){

          old.remove(this);
        };
        if(value){

          value.add(this);
        };
      },
      _setValue : function(value){

        if(value == true){

          this.addCssClass(h);
        } else {

          this.removeCssClass(h);
        };
        this._state = value;
      },
      _getValue : function(){

        return this._state;
      }
    },
    destruct : function(){

      qx.event.Registration.removeListener(this, e, this._onTap, this);
    }
  });
})();
(function(){

  var a = "changeModel",b = "text",c = "_validateSelection",d = "Boolean",e = "qx.data.Array",f = "focus",g = "_applyModel",h = "qx.event.type.Data",i = "",j = "change",k = "value",l = "_applySelection",m = "input",n = "Validation Error: Input value is not a number",o = "type",p = "qx.ui.mobile.form.SelectBox",q = "changeSelection",r = "selectbox-selected",s = "selectbox-unselected",t = "_applyNullable",u = "Validation Error: SelectBox is not nullable",v = "Validation Error: Input value is out of model range",w = "selectbox",x = "touchstart",y = "touchend",z = "click",A = "Validation Error: Please apply model before selection",B = "__selectionDialogTitle",C = "__ok";
  qx.Class.define(p, {
    extend : qx.ui.mobile.core.Widget,
    include : [qx.ui.mobile.form.MValue, qx.ui.form.MForm, qx.ui.mobile.form.MText, qx.ui.mobile.form.MState],
    implement : [qx.ui.form.IForm, qx.ui.form.IModel],
    construct : function(){

      qx.ui.mobile.core.Widget.call(this);
      this._setAttribute(o, b);
      this.setReadOnly(true);
      this.addListener(f, this.blur);
      this.__ok = this._createSelectionDialog();
      this.__ok.addListener(q, this._onChangeSelection, this);
    },
    events : {
      changeSelection : h
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : w
      },
      activatable : {
        refine : true,
        init : true
      },
      nullable : {
        init : true,
        check : d,
        apply : t
      },
      model : {
        check : e,
        apply : g,
        event : a,
        nullable : true,
        init : null
      },
      selection : {
        init : null,
        validate : c,
        apply : l,
        nullable : true
      }
    },
    members : {
      __ok : null,
      _getTagName : function(){

        return m;
      },
      _createContainerElement : function(){

        var containerElement = qx.ui.mobile.core.Widget.prototype._createContainerElement.call(this);
        var showSelectionDialog = qx.lang.Function.bind(this.__ol, this);
        qx.bom.Event.addNativeListener(containerElement, z, showSelectionDialog, false);
        qx.bom.Event.addNativeListener(containerElement, y, showSelectionDialog, false);
        qx.bom.Event.addNativeListener(containerElement, z, qx.bom.Event.preventDefault, false);
        qx.bom.Event.addNativeListener(containerElement, x, qx.bom.Event.preventDefault, false);
        return containerElement;
      },
      _createSelectionDialog : function(){

        var menu = new qx.ui.mobile.dialog.Menu();
        menu.setSelectedItemClass(r);
        menu.setUnselectedItemClass(s);
        menu.setHideOnBlockerClick(true);
        return menu;
      },
      getSelectionDialog : function(){

        return this.__ok;
      },
      setDialogTitle : function(title){

        this.__ok.setTitle(title);
      },
      setClearButtonLabel : function(value){

        this.__ok.setClearButtonLabel(value);
      },
      _setValue : function(value){

        if(this.getModel() == null){

          return;
        };
        if(value == i){

          if(this.isNullable()){

            this.setSelection(null);
          } else {

            this.setSelection(0);
          };
        } else if(value != null){

          this.setSelection(this.getModel().indexOf(value));
        } else {

          this.setSelection(null);
        };
      },
      _getValue : function(){

        return this._getAttribute(k);
      },
      _render : function(){

        if(this.getModel() != null && this.getModel().length > 0){

          var selectedItem = this.getModel().getItem(this.getSelection());
          this._setAttribute(k, selectedItem);
        };
        this._domUpdated();
      },
      _applyModel : function(value, old){

        value.addListener(j, this._render, this);
        if(old != null){

          old.removeListener(j, this._render, this);
        };
        this._render();
      },
      __ol : function(){

        if(this.isEnabled() == true){

          this.__ok.setSelectedIndex(this.getSelection());
          this.__ok.setItems(this.getModel());
          this.__ok.show();
        };
      },
      _onChangeSelection : function(evt){

        this.setSelection(evt.getData().index);
        this._render();
      },
      _validateSelection : function(value){

        if(value != null && qx.lang.Type.isNumber(value) == false){

          throw new qx.core.ValidationError(n);
        };
        if(this.getModel() === null){

          throw new qx.core.ValidationError(A);
        };
        if(!this.isNullable() && value === null){

          throw new qx.core.ValidationError(u);
        };
        if(value != null && (value < 0 || value >= this.getModel().getLength())){

          throw new qx.core.ValidationError(v);
        };
      },
      _applySelection : function(value, old){

        var selectedItem = this.getModel().getItem(value);
        this.fireDataEvent(q, {
          index : value,
          item : selectedItem
        });
        this._render();
      },
      _applyNullable : function(value, old){

        this.__ok.setNullable(value);
      }
    },
    destruct : function(){

      this.__ok.removeListener(q, this._onChangeSelection, this);
      this._disposeObjects(C, B);
      this.removeListener(f, this.blur);
    }
  });
})();
(function(){

  var a = "form-row-group-title",b = "_labels",c = 'form-element-error',d = "form-row-group-last",e = 'div',f = "<p>",g = "qx.ui.mobile.form.renderer.Single",h = "form-row-content",j = "</p>",k = "_rows",m = "form-separation-row",n = "ul",o = "exclude",p = "form-row-group-first";
  qx.Class.define(g, {
    extend : qx.ui.mobile.form.renderer.AbstractRenderer,
    construct : function(form){

      this.__om = [];
      this._rows = [];
      this._labels = [];
      qx.ui.mobile.form.renderer.AbstractRenderer.call(this, form);
    },
    statics : {
      ONE_LINE_WIDGETS : [qx.ui.mobile.form.ToggleButton, qx.ui.mobile.form.RadioButton, qx.ui.mobile.form.TextField, qx.ui.mobile.form.PasswordField, qx.ui.mobile.form.NumberField, qx.ui.mobile.form.CheckBox, qx.ui.mobile.form.SelectBox]
    },
    members : {
      _rows : null,
      _labels : null,
      __om : null,
      _getTagName : function(){

        return n;
      },
      _isOneLineWidget : function(item){

        var widgets = qx.ui.mobile.form.renderer.Single.ONE_LINE_WIDGETS;
        for(var i = 0;i < widgets.length;i++){

          var widget = widgets[i];
          if(item instanceof widget){

            return true;
          };
        };
        return false;
      },
      addItems : function(items, names, title){

        if(title !== null){

          this._addGroupHeader(title);
        };
        this._addGroupHeaderRow();
        for(var i = 0,l = items.length;i < l;i++){

          var item = items[i];
          var name = names[i];
          var isLastItem = (i == items.length - 1);
          if(item instanceof qx.ui.mobile.form.TextArea){

            this._addInScrollComposite(item, name);
          } else {

            if(this._isOneLineWidget(item)){

              this._addRow(item, name, new qx.ui.mobile.layout.HBox());
            } else {

              this._addRow(item, name, new qx.ui.mobile.layout.VBox());
            };
          };
          if(!isLastItem){

            this._addSeparationRow();
          };
        };
        this._addGroupFooterRow();
      },
      _addInScrollComposite : function(item, name){

        var scrollContainer = new qx.ui.mobile.container.ScrollComposite();
        scrollContainer.setFixedHeight(true);
        scrollContainer.setShowScrollIndicator(false);
        scrollContainer.add(item, {
          flex : 1
        });
        this._addRow(scrollContainer, name, new qx.ui.mobile.layout.VBox());
      },
      _addInSeparateLines : function(item, name){

        this._addRow(item, name, new qx.ui.mobile.layout.VBox());
      },
      _addInOneLine : function(item, name){

        this._addRow(item, name, new qx.ui.mobile.layout.HBox());
      },
      _addRow : function(item, name, layout){

        var row = new qx.ui.mobile.form.Row(layout);
        row.addCssClass(h);
        if(name !== null){

          var label = new qx.ui.mobile.form.Label(f + name + j);
          label.setLabelFor(item.getId());
          row.add(label, {
            flex : 1
          });
          this._labels.push(label);
        };
        row.add(item);
        this._add(row);
        this._rows.push(row);
      },
      _addSeparationRow : function(){

        var row = new qx.ui.mobile.form.Row();
        row.addCssClass(m);
        this._add(row);
        this._rows.push(row);
      },
      _addGroupHeaderRow : function(){

        var row = new qx.ui.mobile.form.Row();
        row.addCssClass(p);
        this._add(row);
        this._rows.push(row);
      },
      _addGroupFooterRow : function(){

        var row = new qx.ui.mobile.form.Row();
        row.addCssClass(d);
        this._add(row);
        this._rows.push(row);
      },
      _addGroupHeader : function(title){

        var row = new qx.ui.mobile.form.Row();
        row.addCssClass(a);
        var titleLabel = new qx.ui.mobile.basic.Label(title);
        row.add(titleLabel);
        this._add(row);
        this._labels.push(titleLabel);
        this._rows.push(row);
      },
      addButton : function(button){

        var row = new qx.ui.mobile.form.Row(new qx.ui.mobile.layout.HBox());
        row.add(button, {
          flex : 1
        });
        this._add(row);
        this._rows.push(row);
      },
      showErrorForItem : function(item){

        var errorNode = qx.dom.Element.create(e);
        errorNode.innerHTML = item.getInvalidMessage();
        qx.bom.element.Class.add(errorNode, c);
        qx.dom.Element.insertAfter(errorNode, item.getLayoutParent().getContainerElement());
        this.__om.push(errorNode);
      },
      showItem : function(item){

        item.getLayoutParent().removeCssClass(o);
      },
      hideItem : function(item){

        item.getLayoutParent().addCssClass(o);
      },
      resetForm : function(){

        for(var i = 0;i < this.__om.length;i++){

          qx.dom.Element.remove(this.__om[i]);
        };
      }
    },
    destruct : function(){

      this.resetForm();
      this._disposeArray(b);
      this._disposeArray(k);
    }
  });
})();
(function(){

  var a = "li",b = "_applyAttribute",c = "form-row",d = "qx.ui.mobile.form.Row",e = "Boolean";
  qx.Class.define(d, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(layout){

      qx.ui.mobile.container.Composite.call(this, layout);
      this.initSelectable();
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : c
      },
      selectable : {
        check : e,
        init : false,
        apply : b
      }
    },
    members : {
      _getTagName : function(){

        return a;
      }
    }
  });
})();
(function(){

  var a = "Apr",b = "Mar",c = "Aug",d = "kirra_qooxdoo.Widgets",f = "Jul",g = 'function',h = "May",j = "No factory found for: ",k = "Sep",l = "Nov",m = 'create',n = '- None -',o = "",p = "confirmSelection",q = "Oct",r = "Dec",s = "Feb",t = "Jun",u = "Jan",v = '/',w = " : ",x = "tap",y = 'Widget',z = "index";
  qx.Class.define(d, {
    extend : qx.core.Object,
    statics : {
      createWidget : function(property, valueEnumerator){

        valueEnumerator = valueEnumerator || function(property, callback){

          callback([]);
        };
        var kind = property.typeRef && property.typeRef.kind;
        var factoryMethodName = m + kind + y;
        var factory = this[factoryMethodName];
        if(typeof (factory) !== g){

          console.log(j + property.name + w + kind);
          return this.createStringWidget(property, valueEnumerator);
        };
        return factory.call(this, property, valueEnumerator);
      },
      createEntityWidget : function(property, valueEnumerator){

        var me = this;
        var objectField = new qx.ui.mobile.form.TextField();
        objectField.setReadOnly(true);
        var picker = new qx.ui.mobile.dialog.Picker();
        objectField.addListener(x, function(){

          valueEnumerator(property, function(values){

            console.log(values);
            if(!values || values.length === 0){

              return;
            };
            var objectSlot = new qx.data.Array(values);
            picker.removeSlot(0);
            picker.addSlot(objectSlot);
            picker.show();
          });
        });
        picker.addListener(p, function(e){

          var data = e.getData();
          objectField.setValue(data[0].item);
          objectField._setAttribute(z, data[0].index);
        });
        return objectField;
      },
      createPrimitiveWidget : function(property){

        var typeName = property.typeRef && property.typeRef.typeName;
        var factoryMethodName = m + typeName + y;
        var factory = this[factoryMethodName];
        if(typeof (factory) !== g){

          console.log(j + property.name + w + typeName);
          return this.createStringWidget(property);
        };
        return factory.call(this, property);
      },
      createStringWidget : function(attribute){

        return new qx.ui.mobile.form.TextField();
      },
      createIntegerWidget : function(attribute){

        return new qx.ui.mobile.form.TextField();
      },
      createDoubleWidget : function(attribute){

        return new qx.ui.mobile.form.TextField();
      },
      createDateWidget : function(attribute){

        var me = this;
        var dateField = new qx.ui.mobile.form.TextField();
        dateField.setReadOnly(true);
        var days = [];
        for(var day = 1;day <= 31;day++){

          days.push(o + day);
        };
        var daySlot = new qx.data.Array(days);
        var months = [u, s, b, a, h, t, f, c, k, q, l, r];
        var monthSlot = new qx.data.Array(months);
        var years = [];
        for(var year = 1900 + new Date().getYear();year > 1900;year--){

          years.push(o + year);
        };
        var yearSlot = new qx.data.Array(years);
        var picker = new qx.ui.mobile.dialog.Picker();
        picker.addSlot(yearSlot);
        picker.addSlot(monthSlot);
        picker.addSlot(daySlot);
        dateField.addListener(x, function(){

          var parsed;
          try{

            parsed = kirra_qooxdoo.DateFormats.getYMDFormatter().parse(dateField.getValue());
          } catch(e) {

            parsed = new Date();
          };
          var slot0 = parsed.getYear();
          var slot1 = parsed.getMonth();
          var slot2 = parsed.getDate() - 1;
          picker.setSelectedIndex(0, new Date().getYear() - slot0);
          picker.setSelectedIndex(1, slot1);
          picker.setSelectedIndex(2, slot2);
          picker.show();
        });
        picker.addListener(p, function(e){

          var data = e.getData();
          dateField.setValue(data[0].item + v + (months.indexOf(data[1].item) + 1) + v + data[2].item);
        });
        return dateField;
      },
      createMemoWidget : function(attribute){

        return new qx.ui.mobile.form.TextArea();
      },
      createBooleanWidget : function(attribute){

        return new qx.ui.mobile.form.CheckBox();
      },
      createStateMachineWidget : function(attribute){

        return new qx.ui.mobile.form.TextField();
      },
      createEnumerationWidget : function(property){

        var widget = new qx.ui.mobile.form.SelectBox();
        var values = [];
        if(property.enumerationLiterals){

          if(!property.required){

            values.push(n);
          };
          for(var i in property.enumerationLiterals){

            values.push(property.enumerationLiterals[i]);
          };
        };
        widget.setModel(new qx.data.Array(values));
        return widget;
      }
    }
  });
})();
(function(){

  var a = "touchmove",b = "os.name",c = "__oy",d = "height",e = "px",f = "transitionDuration",g = "qx.event.type.Data",h = "200ms",k = "__ov",l = "__ox",m = "0s",n = "changeSelection",o = "ios",p = "picker-container",q = "Choose",r = "touchend",s = "picker-label",t = "qx.ui.mobile.dialog.Picker",u = "px,0px)",v = "confirmSelection",w = "tap",x = "picker-slot",y = "touchstart",z = "Cancel",A = "__ou",B = "mshtml",C = "engine.name",D = "margin",E = "translate3d(0px,",F = "__ow",G = "picker",H = "transform",I = "changeBubble";
  qx.Class.define(t, {
    extend : qx.ui.mobile.dialog.Popup,
    construct : function(anchor){

      this.__on = {
      };
      this.__oo = {
      };
      this.__op = {
      };
      this.__oq = {
      };
      this.__or = [];
      this.__os = [];
      this.__ot = new qx.data.Array();
      this.__ou = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.HBox());
      if(qx.core.Environment.get(C) == B){

        this.__ou.setAnonymous(true);
      };
      this.__ou.addCssClass(p);
      this.__ov = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.VBox());
      this.__ow = new qx.ui.mobile.form.Button(q);
      this.__ow.addListener(w, this.confirm, this);
      this.__ox = new qx.ui.mobile.form.Button(z);
      this.__ox.addListener(w, this.hide, this);
      var buttonContainer = this.__oy = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.HBox());
      buttonContainer.add(this.__ow, {
        flex : 1
      });
      buttonContainer.add(this.__ox, {
        flex : 1
      });
      this.__ov.add(this.__ou);
      this.__ov.add(buttonContainer);
      if(anchor){

        this.setModal(false);
      } else {

        this.setModal(true);
      };
      qx.ui.mobile.dialog.Popup.call(this, this.__ov, anchor);
    },
    events : {
      changeSelection : g,
      confirmSelection : g
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : G
      }
    },
    members : {
      __ot : null,
      __ow : null,
      __ox : null,
      __ou : null,
      __oy : null,
      __ov : null,
      __on : null,
      __oo : null,
      __op : null,
      __oq : null,
      __or : null,
      __os : null,
      __oz : null,
      show : function(){

        qx.ui.mobile.dialog.Popup.prototype.show.call(this);
        this._updateAllSlots();
      },
      confirm : function(){

        this.hide();
        this._fireConfirmSelection();
      },
      getSelectedIndex : function(slotIndex){

        var slotElement = this.__or[slotIndex];
        if(slotElement){

          return this.__os[slotIndex];
        };
        return null;
      },
      setSelectedIndex : function(slotIndex, value, useTransition){

        var slotElement = this.__or[slotIndex];
        if(slotElement){

          if(this._isSelectedIndexValid(slotElement, value)){

            this.__oo[slotElement.id] = value;
            this.__os[slotIndex] = value;
            if(this.isShown()){

              this._updateSlot(slotElement, useTransition);
            };
          };
        };
      },
      setConfirmButtonCaption : function(caption){

        if(this.__ow){

          this.__ow.setValue(caption);
        };
      },
      setCancelButtonCaption : function(caption){

        if(this.__ox){

          this.__ox.setValue(caption);
        };
      },
      getPickerButtonContainer : function(){

        return this.__oy;
      },
      addSlot : function(slotData){

        if(slotData !== null && slotData instanceof qx.data.Array){

          this.__ot.push(slotData);
          slotData.addListener(I, this._onChangeBubble, {
            self : this,
            index : this.__ot.length - 1
          });
          this._render();
        };
      },
      _onChangeBubble : function(evt){

        var newSlotDataLength = evt.getData().value.length;
        var selectedIndex = this.self.getSelectedIndex(this.index);
        var pickerSlot = this.self.__ou.getChildren()[this.index];
        this.self._renderPickerSlotContent(pickerSlot, this.index);
        if(selectedIndex >= newSlotDataLength){

          var newSelectedIndex = newSlotDataLength - 1;
          this.self.setSelectedIndex(this.index, newSelectedIndex, false);
        };
      },
      removeSlot : function(slotIndex){

        if(this.__ot.getLength() > slotIndex && slotIndex > -1){

          var slotData = this.__ot.getItem(slotIndex);
          slotData.removeListener(I, this._onChangeBubble, this);
          this.__ot.removeAt(slotIndex);
          this._render();
        };
      },
      _disposePickerModel : function(){

        for(var i = 0;i < this.__ot.length;i++){

          var slotData = this.__ot.getItem(i);
          slotData.removeListener(I, this._onChangeBubble, this);
        };
        this.__ot.dispose();
      },
      getSlotCount : function(){

        return this.__ot.getLength();
      },
      _increaseSelectedIndex : function(contentElement){

        var oldSelectedIndex = this.__oo[contentElement.id];
        var newSelectedIndex = oldSelectedIndex + 1;
        var slotIndex = this._getSlotIndexByElement(contentElement);
        var model = this._getModelByElement(contentElement);
        if(model.getLength() == newSelectedIndex){

          newSelectedIndex = model.getLength() - 1;
        };
        this.__oo[contentElement.id] = newSelectedIndex;
        this.__os[slotIndex] = newSelectedIndex;
        this._updateSlot(contentElement);
      },
      _decreaseSelectedIndex : function(contentElement){

        var oldSelectedIndex = this.__oo[contentElement.id];
        var newSelectedIndex = oldSelectedIndex - 1;
        var slotIndex = this._getSlotIndexByElement(contentElement);
        if(newSelectedIndex < 0){

          newSelectedIndex = 0;
        };
        this.__oo[contentElement.id] = newSelectedIndex;
        this.__os[slotIndex] = newSelectedIndex;
        this._updateSlot(contentElement);
      },
      _getSlotIndexByElement : function(contentElement){

        var contentElementId = contentElement.id;
        var slotIndex = this.__oq[contentElementId];
        return slotIndex;
      },
      _isSelectedIndexValid : function(contentElement, selectedIndex){

        var modelLength = this._getModelByElement(contentElement).getLength();
        return (selectedIndex < modelLength && selectedIndex >= 0);
      },
      _getModelByElement : function(contentElement){

        var slotIndex = this._getSlotIndexByElement(contentElement);
        return this.__ot.getItem(slotIndex);
      },
      _fireConfirmSelection : function(){

        var model = this.__ot;
        var slotCounter = (model ? model.getLength() : 0);
        var selectionData = [];
        for(var slotIndex = 0;slotIndex < slotCounter;slotIndex++){

          var selectedIndex = this.__os[slotIndex];
          var selectedValue = model.getItem(slotIndex).getItem(selectedIndex);
          var slotData = {
            index : selectedIndex,
            item : selectedValue,
            slot : slotIndex
          };
          selectionData.push(slotData);
        };
        this.fireDataEvent(v, selectionData);
      },
      _fixPickerSlotHeight : function(target){

        this.__oz = qx.bom.element.Style.get(target.children[0], d, 1);
        this.__oz = parseFloat(this.__oz, 10);
        var labelCount = this._getModelByElement(target).length;
        var pickerSlotHeight = labelCount * this.__oz;
        qx.bom.element.Style.set(target, d, pickerSlotHeight + e);
      },
      _onTouchStart : function(evt){

        var target = evt.getCurrentTarget().getContainerElement();
        var touchX = evt.getViewportLeft();
        var touchY = evt.getViewportTop();
        this.__op[target.id] = this.__oo[target.id];
        qx.bom.element.Style.set(target, f, m);
        this.__on[target.id] = {
          x : touchX,
          y : touchY
        };
        this._fixPickerSlotHeight(target);
        evt.preventDefault();
      },
      _onTouchEnd : function(evt){

        var target = evt.getCurrentTarget().getContainerElement();
        var model = this._getModelByElement(target);
        var slotIndex = this._getSlotIndexByElement(target);
        var touchStartPoint = this.__on[target.id];
        if(!touchStartPoint){

          return;
        };
        var deltaY = evt.getViewportTop() - touchStartPoint.y;
        var isSwipe = Math.abs(deltaY) >= this.__oz / 2;
        if(isSwipe){

          this.__oo[target.id] = this.__op[target.id];
          this.__os[slotIndex] = this.__op[target.id];
        } else {

          var viewportTop = evt.getViewportTop();
          var offsetParent = qx.bom.element.Location.getOffsetParent(target);
          var targetTop = qx.bom.element.Location.getTop(offsetParent, D);
          var relativeTop = viewportTop - targetTop;
          var decreaseIncreaseLimit = offsetParent.offsetHeight / 2;
          if(relativeTop < decreaseIncreaseLimit){

            this._decreaseSelectedIndex(target);
          } else if(relativeTop > decreaseIncreaseLimit){

            this._increaseSelectedIndex(target);
          };
        };
        var selectedIndex = this.__oo[target.id];
        var selectedValue = model.getItem(selectedIndex);
        this._updateSlot(target);
        this.fireDataEvent(n, {
          index : selectedIndex,
          item : selectedValue,
          slot : slotIndex
        });
      },
      _onTouchMove : function(evt){

        var target = evt.getCurrentTarget();
        var targetElement = evt.getCurrentTarget().getContainerElement();
        var touchStartPoint = this.__on[targetElement.id];
        if(!touchStartPoint){

          return;
        };
        var deltaY = evt.getViewportTop() - touchStartPoint.y;
        var selectedIndex = this.__oo[targetElement.id];
        var offsetTop = -selectedIndex * this.__oz;
        var targetOffset = deltaY + offsetTop;
        var slotHeight = targetElement.offsetHeight;
        var pickerHeight = parseInt(target.getLayoutParent().getContainerElement().offsetHeight, 10);
        var upperBounce = this.__oz;
        var lowerBounce = (-slotHeight + pickerHeight * 2);
        if(targetOffset > upperBounce){

          targetOffset = upperBounce;
        };
        if(targetOffset < lowerBounce){

          targetOffset = lowerBounce;
        };
        target.setTranslateY(targetOffset);
        var steps = Math.round(-deltaY / this.__oz);
        var newIndex = selectedIndex + steps;
        var modelLength = this._getModelByElement(targetElement).getLength();
        if(newIndex < modelLength && newIndex >= 0){

          this.__op[targetElement.id] = newIndex;
        };
        evt.preventDefault();
      },
      _updateSlot : function(targetElement, useTransition){

        this._fixPickerSlotHeight(targetElement);
        if(typeof useTransition === undefined){

          useTransition = true;
        };
        if(qx.core.Environment.get(b) == o){

          var transitionDuration = h;
          if(useTransition === false){

            transitionDuration = m;
          };
          qx.bom.element.Style.set(targetElement, f, transitionDuration);
        };
        var selectedIndex = this.__oo[targetElement.id];
        var offsetTop = -selectedIndex * this.__oz;
        qx.bom.element.Style.set(targetElement, H, E + offsetTop + u);
      },
      _updateAllSlots : function(){

        for(var i = 0;i < this.__or.length;i++){

          this._updateSlot(this.__or[i]);
        };
      },
      _render : function(){

        this._removePickerSlots();
        this.__os = [];
        this.__or = [];
        this.__oq = {
        };
        this.__oo = {
        };
        var slotCounter = (this.__ot ? this.__ot.getLength() : 0);
        for(var slotIndex = 0;slotIndex < slotCounter;slotIndex++){

          this.__os.push(0);
          var pickerSlot = this._createPickerSlot(slotIndex);
          this.__or.push(pickerSlot.getContentElement());
          this.__ou.add(pickerSlot, {
            flex : 1
          });
          this._renderPickerSlotContent(pickerSlot, slotIndex);
        };
      },
      _renderPickerSlotContent : function(pickerSlot, slotIndex){

        var oldPickerSlotContent = pickerSlot.removeAll();
        for(var i = 0;i < oldPickerSlotContent.length;i++){

          oldPickerSlotContent[i].dispose();
        };
        var slotValues = this.__ot.getItem(slotIndex);
        var slotLength = slotValues.getLength();
        for(var slotValueIndex = 0;slotValueIndex < slotLength;slotValueIndex++){

          var labelValue = slotValues.getItem(slotValueIndex);
          var pickerLabel = this._createPickerValueLabel(labelValue);
          pickerSlot.add(pickerLabel, {
            flex : 1
          });
        };
      },
      _createPickerSlot : function(slotIndex){

        var pickerSlot = new qx.ui.mobile.container.Composite();
        pickerSlot.addCssClass(x);
        pickerSlot.setTransformUnit(e);
        pickerSlot.addListener(y, this._onTouchStart, this);
        pickerSlot.addListener(a, this._onTouchMove, this);
        pickerSlot.addListener(r, this._onTouchEnd, this);
        this.__oq[pickerSlot.getId()] = slotIndex;
        this.__oo[pickerSlot.getId()] = 0;
        return pickerSlot;
      },
      _removePickerSlots : function(){

        var children = this.__ou.getChildren();
        for(var i = children.length - 1;i >= 0;i--){

          var pickerSlot = children[i];
          pickerSlot.removeListener(y, this._onTouchStart, this);
          pickerSlot.removeListener(a, this._onTouchMove, this);
          pickerSlot.removeListener(r, this._onTouchEnd, this);
          var oldPickerSlotContent = pickerSlot.removeAll();
          for(var j = 0;j < oldPickerSlotContent.length;j++){

            oldPickerSlotContent[j].dispose();
          };
          pickerSlot.destroy();
        };
      },
      _createPickerValueLabel : function(textValue){

        var pickerLabel = new qx.ui.mobile.basic.Label(textValue);
        pickerLabel.addCssClass(s);
        return pickerLabel;
      }
    },
    destruct : function(){

      this._disposePickerModel();
      this._removePickerSlots();
      this.__ow.removeListener(w, this.confirm, this);
      this.__ox.removeListener(w, this.hide, this);
      this._disposeObjects(A, c, F, l, k);
    }
  });
})();
(function(){

  var a = "Add",b = "/entities/",c = "/instances/",d = "Link",e = "LINK",f = "/relationships/",g = "Back",h = "Unlink",i = "changeSelection",j = "tap",k = "kirra_qooxdoo.RelatedInstanceNavigator",l = "Delete",m = "/",n = "qx.event.type.Data";
  qx.Class.define(k, {
    extend : kirra_qooxdoo.AbstractInstanceNavigator,
    construct : function(repository){

      kirra_qooxdoo.AbstractInstanceNavigator.call(this);
      this.repository = repository;
      this.setShowBackButton(true);
      this.setBackButtonText(g);
    },
    events : {
      "show" : n
    },
    members : {
      _parentEntityName : null,
      _parentEntity : null,
      _childEntityName : null,
      _childEntity : null,
      _parentObjectId : null,
      _toolbar : null,
      _instanceList : null,
      _initialize : function(){

        kirra_qooxdoo.AbstractInstanceNavigator.prototype._initialize.call(this);
        var me = this;
        var list = me._buildInstanceList();
        list.addListener(i, function(evt){

          var instanceSelected = me._instanceList.getModel().getItem(evt.getData());
          qx.core.Init.getApplication().getRouting().executeGet(b + me._parentEntityName + c + me._parentObjectId + f + me._relationshipName + m + instanceSelected.objectId);
        }, this);
      },
      _start : function(){

        kirra_qooxdoo.AbstractInstanceNavigator.prototype._start.call(this);
      },
      _back : function(){

        var me = this;
        qx.core.Init.getApplication().getRouting().executeGet(b + me._parentEntityName + c + me._parentObjectId, {
          reverse : true
        });
      },
      showFor : function(parentEntityName, relationshipName, parentObjectId){

        var me = this;
        this._parentEntity = null;
        this._childEntityName = null;
        this._childEntity = null;
        this._parentEntityName = parentEntityName;
        this._relationshipName = relationshipName;
        this._parentObjectId = parentObjectId;
        this.show();
        this.repository.loadEntity(parentEntityName, function(parentEntity){

          me._parentEntity = parentEntity;
          me._relationship = parentEntity.relationships[me._relationshipName];
          me._childEntityName = me._relationship.typeRef.fullName;
          me.setTitle(me._relationship.label);
          me.repository.loadEntity(me._childEntityName, function(childEntity){

            me._childEntity = childEntity;
            me.buildDetailProperties(me._childEntity);
            me.reloadInstances();
            me.buildActions();
          });
        });
      },
      _loadInstances : function(callback){

        var me = this;
        this.repository.listRelatedInstances(this._parentEntity, this._parentObjectId, this._relationship, callback);
      },
      _getInstanceListEntity : function(){

        return this._childEntity;
      },
      buildActions : function(){

        var me = this;
        if(this._toolbar)this._toolbar.destroy();
        var toolbar = this._toolbar = new qx.ui.mobile.toolbar.ToolBar();
        this.add(toolbar);
        if(this._relationship.editable){

          if(this._relationship.style === e && !this._relationship.oppositeRequired){

            var addButton = new qx.ui.mobile.form.Button(d);
            addButton.addListener(j, function(){
            });
            toolbar.add(addButton);
            var deleteButton = new qx.ui.mobile.form.Button(h);
            deleteButton.addListener(j, function(){
            });
            toolbar.add(deleteButton);
          } else {

            var addButton = new qx.ui.mobile.form.Button(a);
            addButton.addListener(j, function(){
            });
            toolbar.add(addButton);
            var deleteButton = new qx.ui.mobile.form.Button(l);
            deleteButton.addListener(j, function(){
            });
            toolbar.add(deleteButton);
          };
        };
      }
    }
  });
})();
(function(){

  var a = "/entities/",b = 'Entity',c = "/instances/",d = "kirra_qooxdoo.ActionForm",e = "Cancel",f = "tap",g = "index",h = "qx.event.type.Data";
  qx.Class.define(d, {
    extend : qx.ui.mobile.page.NavigationPage,
    construct : function(repository){

      qx.ui.mobile.page.NavigationPage.call(this);
      this.repository = repository;
    },
    events : {
      "show" : h
    },
    members : {
      _entityName : null,
      _actionName : null,
      _objectId : null,
      _instance : null,
      _entity : null,
      _action : null,
      _parameterDomains : null,
      _widgets : {
      },
      _back : function(){

        var sourcePath = a + this._entityName + c;
        if(this._objectId){

          sourcePath += this._objectId;
        };
        qx.core.Init.getApplication().goBack(sourcePath);
      },
      _initialize : function(){

        qx.ui.mobile.page.NavigationPage.prototype._initialize.call(this);
      },
      _start : function(){

        qx.ui.mobile.page.NavigationPage.prototype._start.call(this);
      },
      showFor : function(entityName, actionName, objectId){

        var me = this;
        me._widgets = {
        } , me._entityName = entityName;
        me._actionName = actionName;
        me._objectId = objectId;
        me._entity = null;
        me._instance = null;
        me._action = null;
        me._parameterDomains = {
        };
        me.repository.loadEntity(entityName, function(entity){

          me._entity = entity;
          me._action = entity.operations[me._actionName];
          me.buildForm();
        });
        this.show();
      },
      buildForm : function(){

        var me = this;
        if(!this.getContent() || !this._entity){

          return;
        };
        this.setTitle(this._action.description ? this._action.description : this._action.label);
        this.getContent().removeAll();
        var form = this.form = new qx.ui.mobile.form.Form();
        for(var i in this._action.parameters){

          this.buildWidgetFor(form, this._action.parameters[i]);
        };
        var actionFormRenderer = new qx.ui.mobile.form.renderer.Single(form);
        this.getContent().add(actionFormRenderer);
        this.addToolbar();
        this.show();
      },
      addToolbar : function(){

        if(this.toolbar)this.toolbar.destroy();
        var toolbar = this.toolbar = new qx.ui.mobile.toolbar.ToolBar();
        this.add(toolbar);
        this.addBasicButtons();
      },
      addBasicButtons : function(){

        var me = this;
        var cancelButton = new qx.ui.mobile.form.Button(e);
        cancelButton.addListener(f, function(){

          me._back();
        });
        this.toolbar.add(cancelButton);
        var okButton = new qx.ui.mobile.form.Button(this._action.label);
        okButton.addListener(f, function(){

          me.executeAction();
        });
        this.toolbar.add(okButton);
      },
      resultHandler : function(){

        var me = this;
        return function(){

          me._back();
        };
      },
      executeAction : function(){

        var me = this;
        var arguments = {
        };
        for(var i in this._action.parameters){

          var parameterName = this._action.parameters[i].name;
          if(this._action.parameters[i].typeRef.kind == b){

            var referenceIndex = me._widgets[parameterName]._getAttribute(g);
            arguments[parameterName] = me._parameterDomains[parameterName][referenceIndex];
          } else {

            arguments[parameterName] = me._widgets[parameterName].getValue();
          };
        };
        if(me._action.instanceOperation){

          me.repository.sendAction(me._entity, me._objectId, me._action, arguments, me.resultHandler());
        } else {

          me.repository.sendStaticAction(me._entity, me._action, arguments, me.resultHandler());
        };
      },
      buildWidgetFor : function(form, parameter){

        var widget = kirra_qooxdoo.Widgets.createWidget(parameter, this.instanceEnumerator(parameter));
        this._widgets[parameter.name] = widget;
        form.add(widget, parameter.label);
      },
      instanceEnumerator : function(parameter){

        var me = this;
        var kind = parameter.typeRef && parameter.typeRef.kind;
        if(kind !== b){

          return me.noOpValueEnumerator;
        };
        return function(parameter, callback){

          me.repository.listParameterDomain(me._entity, me._objectId, me._action, parameter, function(objects){

            me._parameterDomains[parameter.name] = objects.contents;
            var shorthands = [];
            for(var i in objects.contents){

              shorthands.push(objects.contents[i].shorthand);
            };
            callback(shorthands);
          });
        };
      },
      noOpValueEnumerator : function(target, callback){

        callback([]);
      }
    }
  });
})();
(function(){

  var a = "Boolean",b = "master-detail-detail",c = "device.type",d = "paddingLeft",e = "__oJ",f = "Hide",g = "__oC",h = "orientationchange",k = "qx.ui.mobile.page.Manager",l = "String",m = "__oB",n = "resize",o = "start",p = "master-detail-master",q = "tablet",r = "rem",s = "_applyMasterTitle",t = "_onLayoutChange",u = "update",v = "_applyHideMasterButtonCaption",w = "desktop",x = "Master",y = "tap",z = "__oF",A = "undefined";
  qx.Class.define(k, {
    extend : qx.core.Object,
    construct : function(isTablet, root){

      qx.core.Object.call(this);
      root = root || qx.core.Init.getApplication().getRoot();
      if(typeof isTablet !== A && isTablet !== null){

        this.__oA = isTablet;
      } else {

        this.__oA = qx.core.Environment.get(c) == w || qx.core.Environment.get(c) == q;
      };
      this.__oB = this._createDetailNavigation();
      this.__oB.getNavigationBar().hide();
      if(this.__oA){

        this.__oC = this._createMasterNavigation();
        this.__oC.getNavigationBar().hide();
        this.__oD = this._createMasterContainer();
        this.__oE = this._createDetailContainer();
        this.__oF = this._createMasterButton();
        this.__oF.addListener(y, this._onMasterButtonTap, this);
        this.__oG = this._createHideMasterButton();
        this.__oG.addListener(y, this._onHideMasterButtonTap, this);
        this.__oC.addListener(u, this._onMasterContainerUpdate, this);
        this.__oB.addListener(u, this._onDetailContainerUpdate, this);
        root.add(this.__oE, {
          flex : 1
        });
        this.__oD.add(this.__oC, {
          flex : 1
        });
        this.__oE.add(this.__oB, {
          flex : 1
        });
        qx.event.Registration.addListener(window, h, this._onLayoutChange, this);
        this.__oD.addListener(n, this._onLayoutChange, this);
        this.__oC.getLayout().setShowAnimation(false);
        this.__oB.getLayout().setShowAnimation(false);
        this._onLayoutChange();
      } else {

        root.add(this.__oB, {
          flex : 1
        });
      };
    },
    properties : {
      masterTitle : {
        init : x,
        check : l,
        apply : s
      },
      masterContainerHidden : {
        init : false,
        check : a,
        apply : t
      },
      hideMasterButtonCaption : {
        init : f,
        check : l,
        apply : v
      },
      allowMasterHideOnLandscape : {
        init : true,
        check : a,
        apply : e
      },
      hideMasterOnDetailStart : {
        init : true,
        check : a
      }
    },
    members : {
      __oA : null,
      __oB : null,
      __oC : null,
      __oF : null,
      __oG : null,
      __oH : null,
      __oI : null,
      __oD : null,
      __oE : null,
      _createMasterContainer : function(){

        var masterContainer = new qx.ui.mobile.container.Drawer(null, new qx.ui.mobile.layout.HBox());
        masterContainer.addCssClass(p);
        masterContainer.setHideOnParentTouch(false);
        return masterContainer;
      },
      _createDetailContainer : function(){

        var detailContainer = new qx.ui.mobile.container.Composite(new qx.ui.mobile.layout.VBox());
        detailContainer.setDefaultCssClass(b);
        return detailContainer;
      },
      getMasterContainer : function(){

        return this.__oD;
      },
      getDetailContainer : function(){

        return this.__oE;
      },
      getMasterButton : function(){

        return this.__oF;
      },
      getMasterNavigation : function(){

        return this.__oC;
      },
      getDetailNavigation : function(){

        return this.__oB;
      },
      _createMasterButton : function(){

        return new qx.ui.mobile.navigationbar.Button(this.getMasterTitle());
      },
      _createHideMasterButton : function(){

        return new qx.ui.mobile.navigationbar.Button(f);
      },
      _createMasterNavigation : function(){

        return new qx.ui.mobile.container.Navigation();
      },
      _createDetailNavigation : function(){

        return new qx.ui.mobile.container.Navigation();
      },
      addMaster : function(pages){

        if(this.__oA){

          if(pages){

            if(!qx.lang.Type.isArray(pages)){

              pages = [pages];
            };
            for(var i = 0;i < pages.length;i++){

              var masterPage = pages[i];
              qx.event.Registration.addListener(masterPage, o, this._onMasterPageStart, this);
            };
            if(this.__oH){

              this.__oH.concat(pages);
            } else {

              this.__oH = pages;
            };
            this._add(pages, this.__oC);
          };
        } else {

          this.addDetail(pages);
        };
      },
      _onMasterPageStart : function(evt){

        var masterPage = evt.getTarget();
        var masterPageTitle = masterPage.getTitle();
        this.setMasterTitle(masterPageTitle);
      },
      addDetail : function(pages){

        this._add(pages, this.__oB);
        if(pages && this.__oA){

          if(!qx.lang.Type.isArray(pages)){

            pages = [pages];
          };
          for(var i = 0;i < pages.length;i++){

            var detailPage = pages[i];
            qx.event.Registration.addListener(detailPage, o, this._onDetailPageStart, this);
          };
          if(this.__oI){

            this.__oI.concat(pages);
          } else {

            this.__oI = pages;
          };
        };
      },
      _onDetailPageStart : function(evt){

        if(qx.bom.Viewport.isPortrait() && this.isHideMasterOnDetailStart()){

          this.__oD.hide();
        };
      },
      _add : function(pages, target){

        if(!qx.lang.Type.isArray(pages)){

          pages = [pages];
        };
        for(var i = 0;i < pages.length;i++){

          var page = pages[i];
          {
          };
          if(this.__oA && !page.getShowBackButtonOnTablet()){

            page.setShowBackButton(false);
          };
          page.setIsTablet(this.__oA);
          target.add(page);
        };
      },
      _onMasterContainerUpdate : function(evt){

        var widget = evt.getData();
        widget.getRightContainer().remove(this.__oG);
        widget.getRightContainer().add(this.__oG);
      },
      _onDetailContainerUpdate : function(evt){

        var widget = evt.getData();
        widget.getLeftContainer().remove(this.__oF);
        widget.getLeftContainer().add(this.__oF);
      },
      _onMasterButtonTap : function(){

        this.__oD.show();
        if(qx.bom.Viewport.isLandscape()){

          this.setMasterContainerHidden(false);
          this._createDetailContainerGap();
          this.__oF.exclude();
        };
      },
      _onHideMasterButtonTap : function(){

        this.__oD.hide();
        if(qx.bom.Viewport.isLandscape()){

          this.__oF.show();
          this.setMasterContainerHidden(true);
          this._removeDetailContainerGap();
        };
      },
      _onLayoutChange : function(){

        if(!this.__oA){

          return;
        };
        if(qx.bom.Viewport.isLandscape()){

          this.__oD.setTransitionDuration(0);
          if(this.isMasterContainerHidden() === false){

            this._createDetailContainerGap();
            this.__oD.show();
          } else {

            this._removeDetailContainerGap();
            this.__oD.hide();
          };
          this.__oD.setHideOnParentTouch(false);
        } else {

          this.__oD.setTransitionDuration(500);
          this.__oD.setHideOnParentTouch(true);
          this.__oD.hide();
          this._removeDetailContainerGap();
        };
        this.__oJ();
      },
      _createDetailContainerGap : function(){

        qx.bom.element.Style.set(this.__oE.getContainerElement(), d, this.__oD.getSize() / 16 + r);
        qx.event.Registration.fireEvent(window, n);
      },
      _removeDetailContainerGap : function(){

        qx.bom.element.Style.set(this.__oE.getContainerElement(), d, null);
        qx.event.Registration.fireEvent(window, n);
      },
      __oJ : function(){

        if(!this.__oA){

          return;
        };
        if(qx.bom.Viewport.isPortrait()){

          this.__oF.show();
          this.__oG.show();
        } else {

          this.__oF.exclude();
          this.__oG.exclude();
          if(this.isAllowMasterHideOnLandscape()){

            if(this.isMasterContainerHidden()){

              this.__oF.show();
            } else {

              this.__oG.show();
            };
          };
        };
      },
      _applyHideMasterButtonCaption : function(value, old){

        if(this.__oA){

          this.__oG.setLabel(value);
        };
      },
      _applyMasterTitle : function(value, old){

        if(this.__oA){

          this.__oF.setLabel(value);
        };
      }
    },
    destruct : function(){

      if(this.__oH){

        for(var i = 0;i < this.__oH.length;i++){

          var masterPage = this.__oH[i];
          qx.event.Registration.removeListener(masterPage, o, this._onMasterPageStart, this);
        };
      };
      if(this.___detailPages){

        for(var j = 0;j < this.___detailPages.length;j++){

          var detailPage = this.___detailPages[j];
          qx.event.Registration.removeListener(detailPage, o, this._onDetailPageStart, this);
        };
      };
      if(this.__oA){

        this.__oD.removeListener(n, this._onLayoutChange, this);
        qx.event.Registration.removeListener(window, h, this._onLayoutChange, this);
      };
      this.__oH = this.__oI = null;
      this._disposeObjects(m, g, z);
    }
  });
})();
(function(){

  var a = "swipe",b = "Boolean",c = "height",d = "bottom",e = "ms ease-in-out",f = "Integer",g = "qx.event.type.Data",h = "back",i = "drawer-parent",j = "all ",k = "String",l = "below",m = "resize",n = "width",o = "_applyOrientation",p = "transitionEnd",q = "rem",r = "top",s = "right",t = "down",u = "qx.ui.mobile.container.Drawer",v = "blocked",w = "drawer",x = "above",y = "transition",z = "hidden",A = "left",B = "_applySize",C = "_applyPositionZ",D = "up",E = "_applyTransitionDuration",F = "touchstart";
  qx.Class.define(u, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(parent, layout){

      qx.ui.mobile.container.Composite.call(this);
      if(layout){

        this.setLayout(layout);
      };
      this.initOrientation();
      this.initPositionZ();
      if(parent){

        {
        };
        parent.add(this);
      } else {

        qx.core.Init.getApplication().getRoot().add(this);
      };
      this.__jt = this.getLayoutParent();
      this.__jt.addCssClass(i);
      this.__jt.addListener(a, this._onParentSwipe, this);
      this.__jt.addListener(F, this._onParentTouchStart, this);
      this.__jt.addListener(h, this.forceHide, this);
      this.__oK = [0, 0];
      this.__oL = false;
      this.forceHide();
    },
    events : {
      resize : g
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : w
      },
      orientation : {
        check : k,
        init : A,
        apply : o
      },
      size : {
        check : f,
        init : 300,
        apply : B,
        event : m
      },
      hideOnParentTouch : {
        check : b,
        init : true
      },
      touchOffset : {
        check : f,
        init : 20
      },
      transitionDuration : {
        check : f,
        init : 500,
        apply : E
      },
      positionZ : {
        check : [x, l],
        init : x,
        apply : C
      }
    },
    members : {
      __oK : null,
      __jt : null,
      __oL : null,
      __oM : null,
      __oN : null,
      _applyOrientation : function(value, old){

        this.removeCssClass(old);
        this.addCssClass(value);
        this._applySize(this.getSize());
      },
      _applyPositionZ : function(value, old){

        this.removeCssClass(old);
        this.addCssClass(value);
        if(value == x){

          if(this.__jt){

            this.__jt.setTranslateX(null);
            this.__jt.setTranslateY(null);
          };
          this.setTranslateX(null);
          this.setTranslateY(null);
        } else {

          this.__jt.setTranslateX(0);
          this.__jt.setTranslateY(0);
        };
      },
      setWidth : function(value){

        {
        };
        this.setSize(value);
      },
      getWidth : function(){

        {
        };
        return this.getSize();
      },
      resetWidth : function(){

        {
        };
        this.resetSize();
      },
      setHeight : function(value){

        {
        };
        this.setSize(value);
      },
      getHeight : function(){

        {
        };
        return this.getSize();
      },
      resetHeight : function(){

        {
        };
        this.resetSize();
      },
      _toggleParentBlockedState : function(){

        this.__jt.toggleCssClass(v);
      },
      _applySize : function(value){

        var height = null;
        var width = null;
        var remSize = (value / 16);
        if(this.getOrientation() == A || this.getOrientation() == s){

          width = remSize + q;
        } else {

          height = remSize + q;
        };
        this._setStyle(c, height);
        this._setStyle(n, width);
      },
      _onTransitionEnd : function(evt){

        if(evt){

          qx.bom.Element.removeListener(evt.getTarget(), p, this._onTransitionEnd, this);
        };
        this.__oL = false;
        this._disableTransition();
        if(this.isHidden()){

          this.exclude();
          this.__jt.removeCssClass(v);
        };
        if(this.__oM != qx.bom.Viewport.isLandscape()){

          this.show();
        };
      },
      _applyTransitionDuration : function(value, old){

        this.__oN = value > 0;
      },
      show : function(){

        if(this.__oL || !this.isHidden()){

          return;
        };
        qx.ui.mobile.container.Composite.prototype.show.call(this);
        this.__jt.addCssClass(v);
        this.__oM = qx.bom.Viewport.isLandscape();
        if(this.getPositionZ() == l){

          if(this.__jt){

            this.__jt.setTranslateX(0);
            this.__jt.setTranslateY(0);
          };
          this.setTranslateX(0);
          this.setTranslateY(0);
          if(this.getOrientation() == A){

            this.__jt.setTranslateX(this.getSize());
            this.setTranslateX(-this.getSize());
          } else if(this.getOrientation() == s){

            this.__jt.setTranslateX(-this.getSize());
            this.setTranslateX(this.getSize());
          } else if(this.getOrientation() == r){

            this.__jt.setTranslateY(this.getSize());
            this.setTranslateY(-this.getSize());
          } else if(this.getOrientation() == d){

            this.__jt.setTranslateY(-this.getSize());
            this.setTranslateY(this.getSize());
          };;;
        };
        if(this.getTransitionDuration() > 0){

          this._enableTransition();
          this.__oL = true;
          qx.bom.Element.addListener(this._getTransitionTarget().getContentElement(), p, this._onTransitionEnd, this);
          setTimeout(function(){

            this.removeCssClass(z);
          }.bind(this), 0);
        } else {

          this.removeCssClass(z);
          this._onTransitionEnd();
        };
      },
      hide : function(){

        if(this.__oL || this.isHidden()){

          return;
        };
        this.__oM = qx.bom.Viewport.isLandscape();
        if(this.getPositionZ() == l){

          this.__jt.setTranslateX(0);
          this.__jt.setTranslateY(0);
        };
        if(this.getTransitionDuration() > 0){

          this.__oL = true;
          this._enableTransition();
          qx.bom.Element.addListener(this._getTransitionTarget().getContentElement(), p, this._onTransitionEnd, this);
          setTimeout(function(){

            this.addCssClass(z);
          }.bind(this), 0);
        } else {

          this.addCssClass(z);
          this._onTransitionEnd();
        };
      },
      forceHide : function(){

        this._disableTransition();
        if(this.getPositionZ() == l){

          this.__jt.setTranslateX(0);
          this.__jt.setTranslateY(0);
        };
        this.__jt.removeCssClass(v);
        this.addCssClass(z);
        this.exclude();
      },
      isHidden : function(){

        return this.hasCssClass(z);
      },
      _enableTransition : function(){

        qx.bom.element.Style.set(this._getTransitionTarget().getContentElement(), y, j + this.getTransitionDuration() + e);
      },
      _disableTransition : function(){

        qx.bom.element.Style.set(this._getTransitionTarget().getContentElement(), y, null);
      },
      _getTransitionTarget : function(){

        if(this.getPositionZ() == l){

          return this.__jt;
        } else {

          return this;
        };
      },
      toggleVisibility : function(){

        if(this.isHidden()){

          this.show();
          return true;
        } else {

          this.hide();
          return false;
        };
      },
      _onParentTouchStart : function(evt){

        var clientX = evt.getAllTouches()[0].clientX;
        var clientY = evt.getAllTouches()[0].clientY;
        this.__oK = [clientX, clientY];
        var isShown = !this.hasCssClass(z);
        if(isShown && this.isHideOnParentTouch()){

          var location = qx.bom.element.Location.get(this.getContainerElement());
          if(this.getOrientation() == A && this.__oK[0] > location.right || this.getOrientation() == r && this.__oK[1] > location.bottom || this.getOrientation() == d && this.__oK[1] < location.top || this.getOrientation() == s && this.__oK[0] < location.left){

            evt.preventDefault();
            this.hide();
          };
        };
      },
      _onParentSwipe : function(evt){

        var direction = evt.getDirection();
        var isHidden = this.hasCssClass(z);
        if(isHidden){

          var location = qx.bom.element.Location.get(this.getContainerElement());
          if((direction == s && this.getOrientation() == A && this.__oK[0] < location.right + this.getTouchOffset() && this.__oK[0] > location.right) || (direction == A && this.getOrientation() == s && this.__oK[0] > location.left - this.getTouchOffset() && this.__oK[0] < location.left) || (direction == t && this.getOrientation() == r && this.__oK[1] < this.getTouchOffset() + location.bottom && this.__oK[1] > location.bottom) || (direction == D && this.getOrientation() == d && this.__oK[1] > location.top - this.getTouchOffset() && this.__oK[1] < location.top)){

            this.show();
          };
        };
      }
    },
    destruct : function(){

      this.__jt.removeListener(a, this._onParentSwipe, this);
      this.__jt.removeListener(F, this._onParentTouchStart, this);
      this.__jt.removeListener(h, this.forceHide, this);
      this.__oK = this.__oL = this.__jt = this.__oN = null;
    }
  });
})();
(function(){

  var a = "navigation",b = "animationEnd",c = "__oO",d = "update",e = "blocked",f = "qx.ui.mobile.container.Navigation",g = "visible",h = "hidden",i = "__oP",j = "updateLayout",k = "animationStart",l = "__mF",m = "transitionDuration",n = "s",o = "qx.event.type.Data";
  qx.Class.define(f, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(){

      qx.ui.mobile.container.Composite.call(this, new qx.ui.mobile.layout.VBox());
      this.__oO = this._createNavigationBar();
      if(this.__oO){

        this._add(this.__oO);
      };
      this.__mF = this._createContent();
      this._add(this.__mF, {
        flex : 1
      });
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : a
      }
    },
    events : {
      "update" : o
    },
    members : {
      __oO : null,
      __mF : null,
      __oP : null,
      add : function(widget){

        {
        };
        this.getContent().add(widget);
      },
      remove : function(widget){

        {
        };
        this.getContent().remove(widget);
      },
      getContent : function(){

        return this.__mF;
      },
      getLayout : function(){

        return this.__oP;
      },
      getNavigationBar : function(){

        return this.__oO;
      },
      _createContent : function(){

        this.__oP = new qx.ui.mobile.layout.Card();
        var content = new qx.ui.mobile.container.Composite(this.__oP);
        this.__oP.addListener(j, this._onUpdateLayout, this);
        this.getLayout().addListener(k, this._onAnimationStart, this);
        this.getLayout().addListener(b, this._onAnimationEnd, this);
        return content;
      },
      _onAnimationStart : function(){

        this.addCssClass(e);
      },
      _onAnimationEnd : function(){

        this.removeCssClass(e);
      },
      _onUpdateLayout : function(evt){

        var data = evt.getData();
        var widget = data.widget;
        var action = data.action;
        if(action == g){

          this._update(widget);
        };
      },
      _update : function(widget){

        var navigationBar = this.getNavigationBar();
        this._setStyle(m, widget.getNavigationBarToggleDuration() + n);
        if(widget.isNavigationBarHidden()){

          this.addCssClass(h);
        } else {

          navigationBar.show();
          this.removeCssClass(h);
        };
        navigationBar.removeAll();
        var leftContainer = widget.getLeftContainer();
        if(leftContainer){

          navigationBar.add(leftContainer);
        };
        var title = widget.getTitleWidget();
        if(title){

          navigationBar.add(title, {
            flex : 1
          });
        };
        var rightContainer = widget.getRightContainer();
        if(rightContainer){

          navigationBar.add(rightContainer);
        };
        this.fireDataEvent(d, widget);
      },
      _createNavigationBar : function(){

        return new qx.ui.mobile.navigationbar.NavigationBar();
      }
    },
    destruct : function(){

      this.getLayout().removeListener(k, this._onAnimationStart, this);
      this.getLayout().removeListener(b, this._onAnimationEnd, this);
      this._disposeObjects(c, l, i);
      this.__oO = this.__mF = this.__oP = null;
    }
  });
})();
(function(){

  var a = "animationChild",b = "animationParent",c = "MSAnimationEnd",d = "css.transform.3d",e = "Boolean",f = "event.mspointer",g = "layout-card-item",h = "reverse",i = "slide",j = "vbox",k = "visible",l = "out",m = "qx.ui.mobile.layout.Card",n = "boxFlex1",o = "animationEnd",p = "animationStart",q = "Integer",r = "in",s = "String",t = "layout-card",u = "qx.event.type.Data";
  qx.Class.define(m, {
    extend : qx.ui.mobile.layout.Abstract,
    construct : function(){

      qx.ui.mobile.layout.Abstract.call(this);
      this.__oQ = new qx.ui.mobile.layout.CardAnimation();
    },
    events : {
      animationStart : u,
      animationEnd : u
    },
    properties : {
      defaultAnimation : {
        check : s,
        init : i
      },
      showAnimation : {
        check : e,
        init : true
      },
      animationDuration : {
        check : q,
        init : 350
      }
    },
    members : {
      __oR : null,
      __oS : null,
      __oL : null,
      __oT : null,
      __oU : null,
      __oQ : null,
      _getCssClasses : function(){

        return [t, j];
      },
      connectToChildWidget : function(widget){

        qx.ui.mobile.layout.Abstract.prototype.connectToChildWidget.call(this);
        if(widget){

          widget.addCssClass(g);
          widget.addCssClass(n);
          widget.exclude();
        };
      },
      disconnectFromChildWidget : function(widget){

        qx.ui.mobile.layout.Abstract.prototype.disconnectFromChildWidget.call(this);
        widget.removeCssClass(g);
      },
      updateLayout : function(widget, action, properties){

        if(action == k){

          this._showWidget(widget, properties);
        };
        qx.ui.mobile.layout.Abstract.prototype.updateLayout.call(this, widget, action, properties);
      },
      setCardAnimation : function(value){

        this.__oQ = value;
      },
      getCardAnimation : function(){

        return this.__oQ;
      },
      _showWidget : function(widget, properties){

        if(this.__oR == widget){

          return;
        };
        if(this.__oL){

          this.__oW();
        };
        this.__oR = widget;
        if(this.__oS && this.getShowAnimation() && qx.core.Environment.get(d)){

          properties = properties || {
          };
          this.__oT = properties.animation || this.getDefaultAnimation();
          properties.reverse = properties.reverse === null ? false : properties.reverse;
          this.__oU = properties.fromHistory || properties.reverse;
          this.__oV(widget);
        } else {

          this._swapWidget();
        };
      },
      _swapWidget : function(){

        if(this.__oS){

          this.__oS.exclude();
        };
        this.__oS = this.__oR;
      },
      _fixWidgetSize : function(widget){

        if(widget){

          var hasResizeMixin = qx.Class.hasMixin(widget.constructor, qx.ui.mobile.core.MResize);
          if(hasResizeMixin){

            widget.fixSize();
          };
        };
      },
      _releaseWidgetSize : function(widget){

        if(widget){

          var hasResizeMixin = qx.Class.hasMixin(widget.constructor, qx.ui.mobile.core.MResize);
          if(hasResizeMixin){

            widget.releaseFixedSize();
          };
        };
      },
      __oV : function(widget){

        this.__oL = true;
        this.fireDataEvent(p, [this.__oS, widget]);
        var fromElement = this.__oS.getContainerElement();
        var toElement = widget.getContainerElement();
        var onAnimationEnd = qx.lang.Function.bind(this._onAnimationEnd, this);
        if(qx.core.Environment.get(f)){

          qx.bom.Event.addNativeListener(fromElement, c, onAnimationEnd, false);
          qx.bom.Event.addNativeListener(toElement, c, onAnimationEnd, false);
        };
        qx.event.Registration.addListener(fromElement, o, this._onAnimationEnd, this);
        qx.event.Registration.addListener(toElement, o, this._onAnimationEnd, this);
        var fromCssClasses = this.__oX(l);
        var toCssClasses = this.__oX(r);
        this._widget.addCssClass(b);
        var toElementAnimation = this.__oQ.getAnimation(this.__oT, r, this.__oU);
        var fromElementAnimation = this.__oQ.getAnimation(this.__oT, l, this.__oU);
        qx.bom.element.Class.addClasses(toElement, toCssClasses);
        qx.bom.element.Class.addClasses(fromElement, fromCssClasses);
        qx.bom.element.Animation.animate(toElement, toElementAnimation);
        qx.bom.element.Animation.animate(fromElement, fromElementAnimation);
      },
      _onAnimationEnd : function(evt){

        this.__oW();
        this.fireDataEvent(o, [this.__oS, this.__oR]);
      },
      __oW : function(){

        if(this.__oL){

          var fromElement = this.__oS.getContainerElement();
          var toElement = this.__oR.getContainerElement();
          qx.event.Registration.removeListener(fromElement, o, this._onAnimationEnd, this);
          qx.event.Registration.removeListener(toElement, o, this._onAnimationEnd, this);
          qx.bom.element.Class.removeClasses(fromElement, this.__oX(l));
          qx.bom.element.Class.removeClasses(toElement, this.__oX(r));
          this._swapWidget();
          this._widget.removeCssClass(b);
          this.__oL = false;
        };
      },
      __oX : function(direction){

        var classes = [a, this.__oT, direction];
        if(this.__oU){

          classes.push(h);
        };
        return classes;
      }
    }
  });
})();
(function(){

  var a = "engine.name",b = "iteration-event",c = "start-event",d = "qx.event.handler.Transition",e = "css.transition",f = "css.animation",g = "end-event";
  qx.Class.define(d, {
    extend : qx.core.Object,
    implement : qx.event.IEventHandler,
    construct : function(manager){

      qx.core.Object.call(this);
      this.__oY = {
      };
      this.__pa = qx.lang.Function.listener(this._onNative, this);
    },
    statics : {
      PRIORITY : qx.event.Registration.PRIORITY_NORMAL,
      SUPPORTED_TYPES : {
        transitionEnd : 1,
        animationEnd : 1,
        animationStart : 1,
        animationIteration : 1
      },
      TARGET_CHECK : qx.event.IEventHandler.TARGET_DOMNODE,
      IGNORE_CAN_HANDLE : true,
      TYPE_TO_NATIVE : null,
      NATIVE_TO_TYPE : null
    },
    members : {
      __pa : null,
      __oY : null,
      canHandleEvent : function(target, type){
      },
      registerEvent : qx.core.Environment.select(a, {
        "webkit|gecko|mshtml" : function(target, type, capture){

          var hash = qx.core.ObjectRegistry.toHashCode(target) + type;
          var nativeType = qx.event.handler.Transition.TYPE_TO_NATIVE[type];
          this.__oY[hash] = {
            target : target,
            type : nativeType
          };
          qx.bom.Event.addNativeListener(target, nativeType, this.__pa);
        },
        "default" : function(){
        }
      }),
      unregisterEvent : qx.core.Environment.select(a, {
        "webkit|gecko|mshtml" : function(target, type, capture){

          var events = this.__oY;
          if(!events){

            return;
          };
          var hash = qx.core.ObjectRegistry.toHashCode(target) + type;
          if(events[hash]){

            delete events[hash];
          };
          qx.bom.Event.removeNativeListener(target, qx.event.handler.Transition.TYPE_TO_NATIVE[type], this.__pa);
        },
        "default" : function(){
        }
      }),
      _onNative : qx.event.GlobalError.observeMethod(function(nativeEvent){

        qx.event.Registration.fireEvent(nativeEvent.target, qx.event.handler.Transition.NATIVE_TO_TYPE[nativeEvent.type], qx.event.type.Event);
      })
    },
    destruct : function(){

      var event;
      var events = this.__oY;
      for(var id in events){

        event = events[id];
        if(event.target){

          qx.bom.Event.removeNativeListener(event.target, event.type, this.__pa);
        };
      };
      this.__oY = this.__pa = null;
    },
    defer : function(statics){

      var aniEnv = qx.core.Environment.get(f) || {
      };
      var transEnv = qx.core.Environment.get(e) || {
      };
      var n2t = qx.event.handler.Transition.NATIVE_TO_TYPE = {
      };
      var t2n = qx.event.handler.Transition.TYPE_TO_NATIVE = {
        transitionEnd : transEnv[g] || null,
        animationStart : aniEnv[c] || null,
        animationEnd : aniEnv[g] || null,
        animationIteration : aniEnv[b] || null
      };
      for(var type in t2n){

        var nate = t2n[type];
        n2t[nate] = type;
      };
      qx.event.Registration.addHandler(statics);
    }
  });
})();
(function(){

  var a = "_cardAnimationsMap",b = "reverse",c = "qx.ui.mobile.layout.CardAnimation";
  qx.Class.define(c, {
    extend : qx.core.Object,
    construct : function(){

      qx.core.Object.call(this);
      this._cardAnimationsMap = {
        "slide" : {
          "in" : qx.util.Animation.SLIDE_LEFT_IN,
          "out" : qx.util.Animation.SLIDE_LEFT_OUT,
          "reverse" : {
            "in" : qx.util.Animation.SLIDE_RIGHT_IN,
            "out" : qx.util.Animation.SLIDE_RIGHT_OUT
          }
        },
        "fade" : {
          "in" : qx.util.Animation.FADE_IN,
          "out" : qx.util.Animation.FADE_OUT,
          "reverse" : {
            "in" : qx.util.Animation.FADE_IN,
            "out" : qx.util.Animation.FADE_OUT
          }
        },
        "pop" : {
          "in" : qx.util.Animation.POP_IN,
          "out" : qx.util.Animation.POP_OUT,
          "reverse" : {
            "in" : qx.util.Animation.POP_IN,
            "out" : qx.util.Animation.POP_OUT
          }
        },
        "slideup" : {
          "in" : qx.util.Animation.SLIDE_UP_IN,
          "out" : qx.util.Animation.SLIDE_UP_OUT,
          "reverse" : {
            "in" : qx.util.Animation.SLIDE_DOWN_IN,
            "out" : qx.util.Animation.SLIDE_DOWN_OUT
          }
        },
        "flip" : {
          "in" : qx.util.Animation.FLIP_LEFT_IN,
          "out" : qx.util.Animation.FLIP_LEFT_OUT,
          "reverse" : {
            "in" : qx.util.Animation.FLIP_RIGHT_IN,
            "out" : qx.util.Animation.FLIP_RIGHT_OUT
          }
        },
        "swap" : {
          "in" : qx.util.Animation.SWAP_LEFT_IN,
          "out" : qx.util.Animation.SWAP_LEFT_OUT,
          "reverse" : {
            "in" : qx.util.Animation.SWAP_RIGHT_IN,
            "out" : qx.util.Animation.SWAP_RIGHT_OUT
          }
        },
        "cube" : {
          "in" : qx.util.Animation.CUBE_LEFT_IN,
          "out" : qx.util.Animation.CUBE_LEFT_OUT,
          "reverse" : {
            "in" : qx.util.Animation.CUBE_RIGHT_IN,
            "out" : qx.util.Animation.CUBE_RIGHT_OUT
          }
        }
      };
    },
    members : {
      _cardAnimationsMap : null,
      getAnimation : function(animationName, direction, reverse){

        {
        };
        var animation = this._cardAnimationsMap[animationName];
        var animationObject = {
        };
        if(!reverse){

          animationObject = animation[direction];
        } else {

          animationObject = animation[b][direction];
        };
        return animationObject;
      },
      getMap : function(){

        return this._cardAnimationsMap;
      }
    },
    destruct : function(){

      this._disposeObjects(a);
    }
  });
})();
(function(){

  var a = "-90deg",b = "left center",c = "top center",d = "-180px",e = "0%",f = ".2",g = "qx.util.Animation",h = "100%",i = ".5",j = "0",k = ".8",l = "-20deg",m = "90deg",n = "0px",o = "center",p = "-180deg",q = "20deg",r = "0deg",s = "bottom center",t = "-800px",u = "-400px",v = "-70deg",w = "center center",x = "180deg",y = "180px",z = "1",A = "70deg",B = "0% 50%",C = "left top",D = "linear",E = "100% 50%",F = "-100%",G = "ease-out";
  qx.Bootstrap.define(g, {
    statics : {
      SLIDE_LEFT_IN : {
        duration : 350,
        timing : D,
        origin : s,
        keyFrames : {
          '0' : {
            translate : [h]
          },
          '100' : {
            translate : [e]
          }
        }
      },
      SLIDE_LEFT_OUT : {
        duration : 350,
        timing : D,
        origin : s,
        keyFrames : {
          '0' : {
            translate : [n]
          },
          '100' : {
            translate : [F]
          }
        }
      },
      SLIDE_RIGHT_IN : {
        duration : 350,
        timing : D,
        origin : s,
        keyFrames : {
          '0' : {
            translate : [F]
          },
          '100' : {
            translate : [e]
          }
        }
      },
      SLIDE_RIGHT_OUT : {
        duration : 350,
        timing : D,
        origin : s,
        keyFrames : {
          '0' : {
            translate : [n]
          },
          '100' : {
            translate : [h]
          }
        }
      },
      FADE_IN : {
        duration : 350,
        timing : D,
        origin : s,
        keyFrames : {
          '0' : {
            opacity : [j]
          },
          '100' : {
            opacity : [z]
          }
        }
      },
      FADE_OUT : {
        duration : 350,
        timing : D,
        origin : s,
        keyFrames : {
          '0' : {
            opacity : [z]
          },
          '100' : {
            opacity : [j]
          }
        }
      },
      POP_IN : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            scale : [f, f]
          },
          '100' : {
            scale : [z, z]
          }
        }
      },
      POP_OUT : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            scale : [z, z]
          },
          '100' : {
            scale : [f, f]
          }
        }
      },
      SHRINK_HEIGHT : {
        duration : 400,
        timing : D,
        origin : c,
        keep : 100,
        keyFrames : {
          '0' : {
            scale : [z, z],
            opacity : 1
          },
          '100' : {
            scale : [z, j],
            opacity : 0
          }
        }
      },
      GROW_HEIGHT : {
        duration : 400,
        timing : D,
        origin : c,
        keep : 100,
        keyFrames : {
          '0' : {
            scale : [z, j],
            opacity : 0
          },
          '100' : {
            scale : [z, z],
            opacity : 1
          }
        }
      },
      SHRINK_WIDTH : {
        duration : 400,
        timing : D,
        origin : b,
        keep : 100,
        keyFrames : {
          '0' : {
            scale : [z, z],
            opacity : 1
          },
          '100' : {
            scale : [j, z],
            opacity : 0
          }
        }
      },
      GROW_WIDTH : {
        duration : 400,
        timing : D,
        origin : b,
        keep : 100,
        keyFrames : {
          '0' : {
            scale : [j, z],
            opacity : 0
          },
          '100' : {
            scale : [z, z],
            opacity : 1
          }
        }
      },
      SHRINK : {
        duration : 400,
        timing : D,
        origin : C,
        keep : 100,
        keyFrames : {
          '0' : {
            scale : [z, z],
            opacity : 1
          },
          '100' : {
            scale : [j, j],
            opacity : 0
          }
        }
      },
      GROW : {
        duration : 400,
        timing : D,
        origin : C,
        keep : 100,
        keyFrames : {
          '0' : {
            scale : [j, j],
            opacity : 0
          },
          '100' : {
            scale : [z, z],
            opacity : 1
          }
        }
      },
      SLIDE_UP_IN : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            translate : [n, h]
          },
          '100' : {
            translate : [n, n]
          }
        }
      },
      SLIDE_UP_OUT : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            translate : [n, n]
          },
          '100' : {
            translate : [n, n]
          }
        }
      },
      SLIDE_DOWN_IN : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            translate : [n, n]
          },
          '100' : {
            translate : [n, n]
          }
        }
      },
      SLIDE_DOWN_OUT : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            translate : [n, n]
          },
          '100' : {
            translate : [n, h]
          }
        }
      },
      FLIP_LEFT_IN : {
        duration : 350,
        timing : D,
        origin : o,
        keyFrames : {
          '0' : {
            opacity : 0
          },
          '49' : {
            opacity : 0
          },
          '50' : {
            rotate : [r, m],
            scale : [k, z],
            opacity : 1
          },
          '100' : {
            rotate : [r, r],
            scale : [z, z],
            opacity : 1
          }
        }
      },
      FLIP_LEFT_OUT : {
        duration : 350,
        timing : D,
        origin : w,
        keyFrames : {
          '0' : {
            rotate : [r, r],
            scale : [z, z]
          },
          '100' : {
            rotate : [r, p],
            scale : [k, z]
          }
        }
      },
      FLIP_RIGHT_IN : {
        duration : 350,
        timing : D,
        origin : w,
        keyFrames : {
          '0' : {
            opacity : 0
          },
          '49' : {
            opacity : 0
          },
          '50' : {
            rotate : [r, a],
            scale : [k, z],
            opacity : 1
          },
          '100' : {
            rotate : [r, r],
            scale : [z, z],
            opacity : 1
          }
        }
      },
      FLIP_RIGHT_OUT : {
        duration : 350,
        timing : D,
        origin : w,
        keyFrames : {
          '0' : {
            rotate : [r, r],
            scale : [z, z]
          },
          '100' : {
            rotate : [r, x],
            scale : [k, z]
          }
        }
      },
      SWAP_LEFT_IN : {
        duration : 700,
        timing : G,
        origin : w,
        keyFrames : {
          '0' : {
            rotate : [r, v],
            translate : [n, n, t],
            opacity : j
          },
          '35' : {
            rotate : [r, l],
            translate : [d, n, u],
            opacity : z
          },
          '100' : {
            rotate : [r, r],
            translate : [n, n, n],
            opacity : z
          }
        }
      },
      SWAP_LEFT_OUT : {
        duration : 700,
        timing : G,
        origin : w,
        keyFrames : {
          '0' : {
            rotate : [r, r],
            translate : [n, n, n],
            opacity : z
          },
          '35' : {
            rotate : [r, q],
            translate : [d, n, u],
            opacity : i
          },
          '100' : {
            rotate : [r, A],
            translate : [n, n, t],
            opacity : j
          }
        }
      },
      SWAP_RIGHT_IN : {
        duration : 700,
        timing : G,
        origin : w,
        keyFrames : {
          '0' : {
            rotate : [r, A],
            translate : [n, n, t],
            opacity : j
          },
          '35' : {
            rotate : [r, q],
            translate : [d, n, u],
            opacity : z
          },
          '100' : {
            rotate : [r, r],
            translate : [n, n, n],
            opacity : z
          }
        }
      },
      SWAP_RIGHT_OUT : {
        duration : 700,
        timing : G,
        origin : w,
        keyFrames : {
          '0' : {
            rotate : [r, r],
            translate : [n, n, n],
            opacity : z
          },
          '35' : {
            rotate : [r, l],
            translate : [y, n, u],
            opacity : i
          },
          '100' : {
            rotate : [r, v],
            translate : [n, n, t],
            opacity : j
          }
        }
      },
      CUBE_LEFT_IN : {
        duration : 550,
        timing : D,
        origin : E,
        keyFrames : {
          '0' : {
            rotate : [r, m],
            scale : i,
            translate : [j, j, n],
            opacity : [i]
          },
          '100' : {
            rotate : [r, r],
            scale : z,
            translate : [j, j, j],
            opacity : [z]
          }
        }
      },
      CUBE_LEFT_OUT : {
        duration : 550,
        timing : D,
        origin : B,
        keyFrames : {
          '0' : {
            rotate : [r, r],
            scale : z,
            translate : [j, j, n],
            opacity : [z]
          },
          '100' : {
            rotate : [r, a],
            scale : i,
            translate : [j, j, j],
            opacity : [i]
          }
        }
      },
      CUBE_RIGHT_IN : {
        duration : 550,
        timing : D,
        origin : B,
        keyFrames : {
          '0' : {
            rotate : [r, a],
            scale : i,
            translate : [j, j, n],
            opacity : [i]
          },
          '100' : {
            rotate : [r, r],
            scale : z,
            translate : [j, j, j],
            opacity : [z]
          }
        }
      },
      CUBE_RIGHT_OUT : {
        duration : 550,
        timing : D,
        origin : E,
        keyFrames : {
          '0' : {
            rotate : [r, r],
            scale : z,
            translate : [j, j, n],
            opacity : [z]
          },
          '100' : {
            rotate : [r, m],
            scale : i,
            translate : [j, j, j],
            opacity : [i]
          }
        }
      }
    }
  });
})();
(function(){

  var a = "middle",b = "navigationbar",c = "touchstart",d = "qx.ui.mobile.navigationbar.NavigationBar";
  qx.Class.define(d, {
    extend : qx.ui.mobile.container.Composite,
    construct : function(layout){

      qx.ui.mobile.container.Composite.call(this, layout);
      if(!layout){

        this.setLayout(new qx.ui.mobile.layout.HBox().set({
          alignY : a
        }));
      };
      this.addListener(c, qx.bom.Event.preventDefault, this);
    },
    properties : {
      defaultCssClass : {
        refine : true,
        init : b
      }
    },
    destruct : function(){

      this.removeListener(c, qx.bom.Event.preventDefault, this);
    }
  });
})();
(function(){

  var b = "",c = "qx.application.Routing",d = "forward",e = "back",f = "put",g = "get",h = "changeState",k = "/",l = "delete",m = "post";
  qx.Bootstrap.define(c, {
    construct : function(){

      this.__pb = new qx.event.Messaging();
      this.__pc = qx.bom.History.getInstance();
      this.__pc.addListener(h, this.__pg, this);
    },
    statics : {
      DEFAULT_PATH : k,
      __pd : [],
      __pe : []
    },
    members : {
      __pc : null,
      __pb : null,
      __pf : null,
      init : function(defaultRoute){

        {
        };
        var path = this.getState();
        path = this._getPathOrFallback(path, defaultRoute);
        this._executeGet(path, null, true);
      },
      _getPathOrFallback : function(path, defaultPath){

        if(path == b || path == null || !this.__pb.has(g, path)){

          path = defaultPath || qx.application.Routing.DEFAULT_PATH;
        };
        return path;
      },
      onGet : function(route, handler, scope){

        return this.__pb.on(g, route, handler, scope);
      },
      on : function(route, handler, scope){

        return this.onGet(route, handler, scope);
      },
      onPost : function(route, handler, scope){

        return this.__pb.on(m, route, handler, scope);
      },
      onPut : function(route, handler, scope){

        return this.__pb.on(f, route, handler, scope);
      },
      onDelete : function(route, handler, scope){

        return this.__pb.on(l, route, handler, scope);
      },
      onAny : function(route, handler, scope){

        return this.__pb.onAny(route, handler, scope);
      },
      remove : function(id){

        this.__pb.remove(id);
      },
      __pg : function(evt){

        var path = evt.getData();
        path = this._getPathOrFallback(path);
        if(path != this.__pf){

          this._executeGet(path, null, true);
        };
      },
      _executeGet : function(path, customData, fromEvent){

        this.__pf = path;
        var history = this.__pj(path);
        if(history){

          if(!customData){

            customData = history.data.customData || {
            };
            customData.fromHistory = true;
            customData.action = history.action;
            customData.fromEvent = fromEvent;
          } else {

            this.__pi(path, customData);
          };
        } else {

          this.__ph(path, customData);
          qx.application.Routing.__pe = [];
        };
        this.__pc.setState(path);
        this.__pb.emit(g, path, null, customData);
      },
      executeGet : function(path, customData){

        this._executeGet(path, customData);
      },
      execute : function(path, customData){

        this.executeGet(path, customData);
      },
      executePost : function(path, params, customData){

        this.__pb.emit(m, path, params, customData);
      },
      executePut : function(path, params, customData){

        this.__pb.emit(f, path, params, customData);
      },
      executeDelete : function(path, params, customData){

        this.__pb.emit(l, path, params, customData);
      },
      getState : function(){

        return this.__pc.getState();
      },
      __ph : function(path, customData){

        qx.application.Routing.__pd.unshift({
          path : path,
          customData : customData
        });
      },
      __pi : function(path, customData){

        var register = [qx.application.Routing.__pd, qx.application.Routing.__pe];
        for(var i = 0;i < register.length;i++){

          for(var j = 0;j < register[i].length;j++){

            if(register[i][j].path == path){

              register[i][j].customData = customData;
            };
          };
        };
      },
      __pj : function(path){

        var back = qx.application.Routing.__pd;
        var forward = qx.application.Routing.__pe;
        var found = false;
        var entry = null;
        var length = back.length;
        for(var i = 0;i < length;i++){

          if(back[i].path == path){

            entry = back[i];
            var toForward = back.splice(0, i);
            for(var a = 0;a < toForward.length;a++){

              forward.unshift(toForward[a]);
            };
            found = true;
            break;
          };
        };
        if(found){

          return {
            data : entry,
            action : e
          };
        };
        var length = forward.length;
        for(var i = 0;i < length;i++){

          if(forward[i].path == path){

            entry = forward[i];
            var toBack = forward.splice(0, i + 1);
            for(var a = 0;a < toBack.length;a++){

              back.unshift(toBack[a]);
            };
            break;
          };
        };
        if(entry){

          return {
            data : entry,
            action : d
          };
        };
        return entry;
      },
      dispose : function(){

        this.__pc.removeListener(h, this.__pg, this);
      }
    }
  });
})();
(function(){

  var a = "([^\/]+)",b = "^",c = "$",d = "qx.event.Messaging",e = "No listener found for ",f = "any";
  qx.Bootstrap.define(d, {
    construct : function(){

      this._listener = {
      } , this.__pk = 0;
      this.__pl = {
      };
    },
    members : {
      _listener : null,
      __pk : null,
      __pl : null,
      on : function(channel, type, handler, scope){

        return this._addListener(channel, type, handler, scope);
      },
      onAny : function(type, handler, scope){

        return this._addListener(f, type, handler, scope);
      },
      _addListener : function(channel, type, handler, scope){

        var listeners = this._listener[channel] = this._listener[channel] || {
        };
        var id = this.__pk++;
        var params = [];
        var param = null;
        if(qx.lang.Type.isString(type)){

          var paramsRegexp = /\{([\w\d]+)\}/g;
          while((param = paramsRegexp.exec(type)) !== null){

            params.push(param[1]);
          };
          type = new RegExp(b + type.replace(paramsRegexp, a) + c);
        };
        listeners[id] = {
          regExp : type,
          params : params,
          handler : handler,
          scope : scope
        };
        this.__pl[id] = channel;
        return id;
      },
      remove : function(id){

        var channel = this.__pl[id];
        var listener = this._listener[channel];
        delete listener[id];
        delete this.__pl[id];
      },
      has : function(channel, path){

        var listeners = this._listener[channel];
        if(!listeners || qx.lang.Object.isEmpty(listeners)){

          return false;
        };
        for(var id in listeners){

          var listener = listeners[id];
          if(listener.regExp.test(path)){

            return true;
          };
        };
        return false;
      },
      emit : function(channel, path, params, customData){

        this._emit(channel, path, params, customData);
      },
      _emit : function(channel, path, params, customData){

        var listenerMatchedAny = false;
        var listener = this._listener[f];
        listenerMatchedAny = this._emitListeners(channel, path, listener, params, customData);
        var listenerMatched = false;
        listener = this._listener[channel];
        listenerMatched = this._emitListeners(channel, path, listener, params, customData);
        if(!listenerMatched && !listenerMatchedAny){

          qx.Bootstrap.info(e + path);
        };
      },
      _emitListeners : function(channel, path, listeners, params, customData){

        if(!listeners || qx.lang.Object.isEmpty(listeners)){

          return false;
        };
        var listenerMatched = false;
        for(var id in listeners){

          var listener = listeners[id];
          listenerMatched |= this._emitRoute(channel, path, listener, params, customData);
        };
        return listenerMatched;
      },
      _emitRoute : function(channel, path, listener, params, customData){

        var match = listener.regExp.exec(path);
        if(match){

          var params = params || {
          };
          var param = null;
          var value = null;
          match.shift();
          for(var i = 0;i < match.length;i++){

            value = match[i];
            param = listener.params[i];
            if(param){

              params[param] = value;
            } else {

              params[i] = value;
            };
          };
          listener.handler.call(listener.scope, {
            path : path,
            params : params,
            customData : customData
          });
        };
        return match != undefined;
      }
    }
  });
})();

qx.$$loader.init();

